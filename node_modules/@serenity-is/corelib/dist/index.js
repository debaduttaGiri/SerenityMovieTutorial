var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// src/base/symbols.ts
var isAssignableFromSymbol = /* @__PURE__ */ Symbol.for("Serenity.isAssignableFrom");
var isInstanceOfTypeSymbol = /* @__PURE__ */ Symbol.for("Serenity.isInstanceOfType");
var implementedInterfacesSymbol = /* @__PURE__ */ Symbol.for("Serenity.implementedInterfaces");
var localTextTableSymbol = /* @__PURE__ */ Symbol.for("Serenity.localTextTable");
var scriptDataHashSymbol = /* @__PURE__ */ Symbol.for("Serenity.scriptDataHash");
var scriptDataSymbol = /* @__PURE__ */ Symbol.for("Serenity.scriptData");
var typeRegistrySymbol = /* @__PURE__ */ Symbol.for("Serenity.typeRegistry");

// src/base/system-internal.ts
Symbol.typeInfo ??= /* @__PURE__ */ Symbol.for("Serenity.typeInfo");
var globalObject = typeof globalThis !== "undefined" && globalThis || typeof window !== "undefined" && window || typeof self !== "undefined" && self || // @ts-ignore check for global
typeof global !== "undefined" && global || {};
function merge(arr1, arr2) {
  if (!arr1 || !arr2)
    return (arr1 || arr2 || []).slice();
  function distinct(arr) {
    return arr.filter((item, pos) => arr.indexOf(item) === pos);
  }
  __name(distinct, "distinct");
  return distinct(arr1.concat(arr2));
}
__name(merge, "merge");
function getGlobalTypeRegistry() {
  let typeRegistry = globalObject[typeRegistrySymbol];
  if (!typeRegistry)
    typeRegistry = globalObject[typeRegistrySymbol] = {};
  return typeRegistry;
}
__name(getGlobalTypeRegistry, "getGlobalTypeRegistry");
function interfaceIsAssignableFrom(from) {
  return from != null && Array.isArray(from[implementedInterfacesSymbol]) && from[implementedInterfacesSymbol].some((x) => x === this || getTypeNameProp(this) && peekTypeInfo(x)?.typeKind === "interface" && getTypeNameProp(x) === getTypeNameProp(this));
}
__name(interfaceIsAssignableFrom, "interfaceIsAssignableFrom");
function autoRegisterViaTypeInfo(type) {
  if (!Object.prototype.hasOwnProperty.call(type, Symbol.typeInfo))
    return;
  const typeInfo = type[Symbol.typeInfo];
  if (!typeInfo || typeInfo.registered || !typeInfo.typeName)
    return;
  if (typeInfo.typeName.endsWith("."))
    typeInfo.typeName += type.name;
  if (!getGlobalTypeRegistry()[typeInfo.typeName])
    getGlobalTypeRegistry()[typeInfo.typeName] = type;
  if (typeInfo.interfaces?.length && !Object.prototype.hasOwnProperty.call(implementedInterfacesSymbol)) {
    Object.defineProperty(type, implementedInterfacesSymbol, {
      value: merge(type[implementedInterfacesSymbol], typeInfo.interfaces),
      configurable: true
    });
  }
  if (typeInfo.typeKind === "interface" && !Object.prototype.hasOwnProperty.call(type, isAssignableFromSymbol)) {
    Object.defineProperty(type, isAssignableFromSymbol, { value: interfaceIsAssignableFrom, configurable: true });
  }
  typeInfo.registered = true;
  return;
}
__name(autoRegisterViaTypeInfo, "autoRegisterViaTypeInfo");
function internalRegisterType(type, typeName, interfaces, kind) {
  const typeInfo = ensureTypeInfo(type);
  if (kind)
    typeInfo.typeKind = kind;
  if (typeName && typeName !== typeInfo.typeName)
    typeInfo.typeName = typeName;
  if (typeInfo.typeName) {
    if (typeInfo.typeName.endsWith("."))
      typeInfo.typeName += type.name;
    getGlobalTypeRegistry()[typeInfo.typeName] = type;
  }
  if (interfaces?.length && typeInfo.interfaces !== interfaces) {
    interfaces = typeInfo.interfaces = merge(typeInfo.interfaces, interfaces);
    Object.defineProperty(type, implementedInterfacesSymbol, {
      value: merge(type[implementedInterfacesSymbol], interfaces),
      configurable: true
    });
  }
  typeInfo.registered = true;
  return typeInfo;
}
__name(internalRegisterType, "internalRegisterType");
function ensureTypeInfo(type) {
  let typeInfo;
  if (!Object.prototype.hasOwnProperty.call(type, Symbol.typeInfo) || !(typeInfo = type[Symbol.typeInfo])) {
    typeInfo = { typeKind: void 0 };
    Object.defineProperty(type, Symbol.typeInfo, { value: typeInfo, configurable: true, writable: true });
    return typeInfo;
  }
  if (!typeInfo.registered)
    autoRegisterViaTypeInfo(type);
  return typeInfo;
}
__name(ensureTypeInfo, "ensureTypeInfo");
function peekTypeInfo(type) {
  if (!type || !Object.prototype.hasOwnProperty.call(type, Symbol.typeInfo))
    return void 0;
  const typeInfo = type[Symbol.typeInfo];
  if (typeInfo && !typeInfo.registered)
    autoRegisterViaTypeInfo(type);
  return typeInfo;
}
__name(peekTypeInfo, "peekTypeInfo");
function getTypeNameProp(type) {
  return peekTypeInfo(type)?.typeName || void 0;
}
__name(getTypeNameProp, "getTypeNameProp");
function setTypeNameProp(type, value) {
  ensureTypeInfo(type).typeName = value;
  autoRegisterViaTypeInfo(type);
}
__name(setTypeNameProp, "setTypeNameProp");

// src/base/system.ts
var getTypeRegistry = getGlobalTypeRegistry;
var nsSerenity = "Serenity.";
var SerenityNS = "Serenity";
function getGlobalObject() {
  return globalObject;
}
__name(getGlobalObject, "getGlobalObject");
function omitUndefined(x) {
  if (x == null)
    return x;
  let obj = /* @__PURE__ */ Object.create(null);
  Object.entries(x).forEach(([key, value]) => value !== void 0 && (obj[key] = value));
  return obj;
}
__name(omitUndefined, "omitUndefined");
function getNested(from, name) {
  const a2 = name.split(".");
  for (let i2 = 0; i2 < a2.length; i2++) {
    from = from[a2[i2]];
    if (from == null)
      return null;
  }
  return from;
}
__name(getNested, "getNested");
function getType(name, target) {
  let type;
  if (target == null) {
    type = getGlobalTypeRegistry()[name];
    if (type != null || globalObject == void 0 || name === "Object")
      return type;
    target = globalObject;
  }
  type = getNested(target, name);
  if (typeof type !== "function")
    return null;
  return type;
}
__name(getType, "getType");
function getTypeFullName(type) {
  return getTypeNameProp(type) || type.name || (type.toString().match(/^\s*function\s*([^\s(]+)/) || [])[1] || "Object";
}
__name(getTypeFullName, "getTypeFullName");
function getTypeShortName(type) {
  const fullName = getTypeFullName(type);
  const bIndex = fullName?.indexOf("[");
  const nsIndex = fullName?.lastIndexOf(".", bIndex >= 0 ? bIndex : fullName.length);
  return nsIndex > 0 ? fullName.substring(nsIndex + 1) : fullName;
}
__name(getTypeShortName, "getTypeShortName");
function getInstanceType(instance) {
  if (instance == null)
    throw "Can't get instance type of null or undefined!";
  try {
    return instance.constructor;
  } catch (ex) {
    return Object;
  }
}
__name(getInstanceType, "getInstanceType");
function isAssignableFrom(target, fromType) {
  if (target === fromType || fromType.prototype instanceof target)
    return true;
  if (typeof target[isAssignableFromSymbol] === "function")
    return target[isAssignableFromSymbol](fromType);
  return false;
}
__name(isAssignableFrom, "isAssignableFrom");
function isInstanceOfType(instance, type) {
  if (instance == null)
    return false;
  if (typeof type[isInstanceOfTypeSymbol] === "function")
    return type[isInstanceOfTypeSymbol](instance);
  return isAssignableFrom(type, getInstanceType(instance));
}
__name(isInstanceOfType, "isInstanceOfType");
function getBaseType(type) {
  if (type == null || type === Object || !type.prototype || type[Symbol.typeInfo]?.typeKind === "interface")
    return null;
  return Object.getPrototypeOf(type.prototype).constructor;
}
__name(getBaseType, "getBaseType");
function isInterfaceType(type) {
  return typeof type === "function" && peekTypeInfo(type)?.typeKind === "interface";
}
__name(isInterfaceType, "isInterfaceType");
function isAttributeClass(item) {
  return typeof item === "function" && isAssignableFrom(CustomAttribute, item);
}
__name(isAttributeClass, "isAttributeClass");
function isAttributeFactory(item) {
  return typeof item === "function" && item.isAttributeFactory === true;
}
__name(isAttributeFactory, "isAttributeFactory");
function extractInterfaces(intfAndAttr) {
  const result = intfAndAttr?.filter(isInterfaceType);
  if (result && result.length === intfAndAttr.length)
    return intfAndAttr;
  return result;
}
__name(extractInterfaces, "extractInterfaces");
function extractAttributes(intfAndAttr) {
  const result = [];
  if (!intfAndAttr)
    return result;
  for (const item of intfAndAttr) {
    if (isInterfaceType(item))
      continue;
    if (isInstanceOfType(item, CustomAttribute)) {
      result.push(item);
      continue;
    }
    if (isAttributeClass(item)) {
      result.push(new item());
      continue;
    }
    if (isAttributeFactory(item)) {
      result.push(item());
      continue;
    }
    throw new Error("Invalid attribute or interface specifier: " + item);
  }
  return result;
}
__name(extractAttributes, "extractAttributes");
function registerClass(type, name, intfAndAttr) {
  internalRegisterType(type, name, extractInterfaces(intfAndAttr), "class");
  for (const attr of extractAttributes(intfAndAttr)) {
    addCustomAttribute(type, attr);
  }
}
__name(registerClass, "registerClass");
var _a;
_a = Symbol.typeInfo;
var CustomAttribute = class {
  static {
    __name(this, "CustomAttribute");
  }
  static {
    this[_a] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a2, _b;
var EnumKeyAttribute = class extends (_b = CustomAttribute, _a2 = Symbol.typeInfo, _b) {
  constructor(value) {
    super();
    this.value = value;
  }
  static {
    __name(this, "EnumKeyAttribute");
  }
  static {
    this[_a2] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
function registerEnum(enumType, name, enumKey) {
  if (typeof enumType !== "object" || enumType == null)
    throw "Enum type is required in registerEnum!";
  if (name && name.endsWith("."))
    throw "Enum name cannot end with a dot in registerEnum!";
  internalRegisterType(enumType, name, void 0, "enum");
  if (enumKey && enumKey != name) {
    if (!hasCustomAttribute(enumType, EnumKeyAttribute, false))
      addCustomAttribute(enumType, new EnumKeyAttribute(enumKey));
    const typeStore = getGlobalTypeRegistry();
    if (!typeStore[enumKey])
      typeStore[enumKey] = enumType;
  }
}
__name(registerEnum, "registerEnum");
function registerInterface(type, name, intf) {
  internalRegisterType(type, name, extractInterfaces(intf), "interface");
  Object.defineProperty(type, isAssignableFromSymbol, { value: interfaceIsAssignableFrom, configurable: true });
}
__name(registerInterface, "registerInterface");
var Enum;
((Enum2) => {
  Enum2.toString = /* @__PURE__ */ __name((enumType, value) => {
    if (value == null)
      return "";
    if (typeof value !== "number")
      return "" + value;
    const values = enumType;
    if (value === 0 || !peekTypeInfo(enumType)?.enumFlags) {
      for (const i2 in values) {
        if (values[i2] === value) {
          return i2;
        }
      }
      return value == null ? "" : value.toString();
    } else {
      const parts = [];
      for (const i2 in values) {
        if (typeof values[i2] !== "number")
          continue;
        if (values[i2] & value) {
          parts.push(i2);
          value -= values[i2];
        }
      }
      if (value != 0)
        parts.push(value.toString());
      return parts.join(" | ");
    }
  }, "toString");
  Enum2.getValues = /* @__PURE__ */ __name((enumType) => {
    const parts = [];
    const values = enumType;
    for (const i2 in values) {
      if (Object.prototype.hasOwnProperty.call(values, i2) && typeof values[i2] === "number")
        parts.push(values[i2]);
    }
    return parts;
  }, "getValues");
})(Enum || (Enum = {}));
var isEnum = /* @__PURE__ */ __name((type) => {
  return type != null && typeof type === "object" && type[Symbol.typeInfo]?.typeKind == "enum";
}, "isEnum");
function initFormType(typ, nameWidgetPairs) {
  for (let i2 = 0; i2 < nameWidgetPairs.length - 1; i2 += 2) {
    (function(name, widget) {
      Object.defineProperty(typ.prototype, name, {
        get: /* @__PURE__ */ __name(function() {
          return this.w(name, widget);
        }, "get"),
        enumerable: true,
        configurable: true
      });
    })(nameWidgetPairs[i2], nameWidgetPairs[i2 + 1]);
  }
}
__name(initFormType, "initFormType");
var _fieldsProxy = new Proxy({}, { get: /* @__PURE__ */ __name((_2, p2) => p2, "get") });
function fieldsProxy() {
  return _fieldsProxy;
}
__name(fieldsProxy, "fieldsProxy");
function isArrayLike(obj) {
  return typeof obj === "object" && obj != null && typeof obj.length === "number" && typeof obj.nodeType !== "number";
}
__name(isArrayLike, "isArrayLike");
function isPromiseLike(obj) {
  return obj instanceof Promise || typeof obj === "object" && obj != null && typeof obj.then === "function" && typeof obj.catch === "function";
}
__name(isPromiseLike, "isPromiseLike");
var _a3, _b2;
var EditorAttribute = class extends (_b2 = CustomAttribute, _a3 = Symbol.typeInfo, _b2) {
  static {
    __name(this, "EditorAttribute");
  }
  static {
    this[_a3] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a4;
_a4 = Symbol.typeInfo;
var ISlickFormatter = class {
  static {
    __name(this, "ISlickFormatter");
  }
  static {
    this[_a4] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
function registerFormatter(type, name, intfAndAttr) {
  registerClass(type, name, merge([ISlickFormatter], intfAndAttr));
}
__name(registerFormatter, "registerFormatter");
function registerEditor(type, name, intfAndAttr) {
  registerClass(type, name, extractInterfaces(intfAndAttr));
  let attributes = extractAttributes(intfAndAttr);
  addCustomAttribute(type, new EditorAttribute());
  for (const attr of attributes) {
    if (getInstanceType(attr) !== EditorAttribute)
      addCustomAttribute(type, attr);
  }
}
__name(registerEditor, "registerEditor");
function addCustomAttribute(type, attr) {
  let typeInfo = ensureTypeInfo(type);
  if (!typeInfo.customAttributes)
    typeInfo.customAttributes = [attr];
  else
    typeInfo.customAttributes.push(attr);
}
__name(addCustomAttribute, "addCustomAttribute");
function getCustomAttribute(type, attrType, inherit = true) {
  if (!type || attrType == null)
    return null;
  do {
    let attrs = peekTypeInfo(type)?.customAttributes;
    if (attrs) {
      for (let i2 = attrs.length - 1; i2 >= 0; i2--) {
        let attr = attrs[i2];
        if (attr != null && isInstanceOfType(attr, attrType))
          return attr;
      }
    }
  } while (inherit && (type = getBaseType(type)));
}
__name(getCustomAttribute, "getCustomAttribute");
function hasCustomAttribute(type, attrType, inherit = true) {
  return getCustomAttribute(type, attrType, inherit) != null;
}
__name(hasCustomAttribute, "hasCustomAttribute");
function getCustomAttributes(type, attrType, inherit = true) {
  if (!type)
    return [];
  const allTypes = attrType === void 0;
  const result = [];
  do {
    let attrs = peekTypeInfo(type)?.customAttributes;
    if (attrs) {
      for (let i2 = attrs.length - 1; i2 >= 0; i2--) {
        let attr = attrs[i2];
        if (attr && (allTypes || attrType && isInstanceOfType(attr, attrType))) {
          result.push(attr);
        }
      }
    }
  } while (inherit && (type = getBaseType(type)));
  return result;
}
__name(getCustomAttributes, "getCustomAttributes");
function classTypeInfo(typeName, intfAndAttr) {
  const typeInfo = {
    typeKind: "class",
    typeName
  };
  const interfaces = extractInterfaces(intfAndAttr);
  if (interfaces?.length)
    typeInfo.interfaces = interfaces;
  const attrs = extractAttributes(intfAndAttr);
  if (attrs?.length)
    typeInfo.customAttributes = attrs;
  return typeInfo;
}
__name(classTypeInfo, "classTypeInfo");
function editorTypeInfo(typeName, intfAndAttr) {
  const typeInfo = {
    typeKind: "class",
    typeName
  };
  const interfaces = extractInterfaces(intfAndAttr);
  if (interfaces?.length)
    typeInfo.interfaces = interfaces;
  typeInfo.customAttributes = merge([new EditorAttribute()], extractAttributes(intfAndAttr).filter((x) => getInstanceType(x) !== EditorAttribute));
  return typeInfo;
}
__name(editorTypeInfo, "editorTypeInfo");
function formatterTypeInfo(typeName, intfAndAttr) {
  const typeInfo = {
    typeKind: "class",
    typeName,
    interfaces: merge([ISlickFormatter], extractInterfaces(intfAndAttr))
  };
  const attrs = extractAttributes(intfAndAttr);
  if (attrs?.length)
    typeInfo.customAttributes = attrs;
  return typeInfo;
}
__name(formatterTypeInfo, "formatterTypeInfo");
function interfaceTypeInfo(typeName, intf) {
  const typeInfo = {
    typeKind: "interface",
    typeName
  };
  const interfaces = extractInterfaces(intf);
  if (interfaces?.length)
    typeInfo.interfaces = interfaces;
  return typeInfo;
}
__name(interfaceTypeInfo, "interfaceTypeInfo");
function registerType(type) {
  if (!type)
    throw "registerType is called with null target!";
  let typeInfo = peekTypeInfo(type);
  if (!typeInfo)
    throw `registerType is called on type "${type.name}" that does not have a static [Symbol.typeInfo] property!`;
  if (!typeInfo.typeName)
    throw `registerType is called on type "${type.name}", but it's typeInfo property does not have a typeName!`;
}
__name(registerType, "registerType");

// src/base/localtext.ts
function getTable() {
  let localTextTable = getGlobalObject()[localTextTableSymbol];
  if (!localTextTable)
    getGlobalObject()[localTextTableSymbol] = localTextTable = {};
  return localTextTable;
}
__name(getTable, "getTable");
function addLocalText(obj, pre) {
  if (!obj)
    return;
  let table = getTable();
  if (typeof obj === "string") {
    table[obj] = pre;
    return;
  }
  pre ??= "";
  for (let k of Object.keys(obj)) {
    let actual = pre + k;
    let o2 = obj[k];
    if (typeof o2 === "object") {
      addLocalText(o2, actual + ".");
    } else {
      table[actual] = o2;
    }
  }
}
__name(addLocalText, "addLocalText");
function localText(key, defaultText) {
  return getTable()[key] ?? defaultText ?? key ?? "";
}
__name(localText, "localText");
function tryGetText(key) {
  return getTable()[key];
}
__name(tryGetText, "tryGetText");
var proxyTextsPrefixSymbol = /* @__PURE__ */ Symbol.for("Serenity.proxyTextsPrefix");
var proxyTextsTemplateSymbol = /* @__PURE__ */ Symbol.for("Serenity.proxyTextsTemplate");
var proxyTextsModeSymbol = /* @__PURE__ */ Symbol.for("Serenity.proxyTextsMode");
var proxyTextsAsTryCache = /* @__PURE__ */ new WeakMap();
var proxyTextsAsKeyCache = /* @__PURE__ */ new WeakMap();
var localizationProxyHandler = {
  get: /* @__PURE__ */ __name((target, property, receiver) => {
    if (typeof property !== "string") {
      return target[property];
    }
    const prefix = target[proxyTextsPrefixSymbol] ?? "";
    const mode = target[proxyTextsModeSymbol];
    const tpl = target[proxyTextsTemplateSymbol];
    if (!tpl)
      return void 0;
    if (property === "asKey" || property === "asTry") {
      if (mode === property) {
        return function() {
          return receiver;
        };
      }
      return function() {
        const cache = property === "asTry" ? proxyTextsAsTryCache : proxyTextsAsKeyCache;
        let cached = cache.get(target);
        if (!cached) {
          cached = proxyTexts({}, prefix, tpl, property);
          cache.set(target, cached);
        }
        return cached;
      };
    }
    const tpmval = tpl[property];
    const key = prefix + property;
    if (tpmval == null)
      return mode === "asTry" ? tryGetText(key) : mode == "asKey" ? key : localText(key);
    const subProxy = target[property];
    if (subProxy != null)
      return subProxy;
    return target[property] = proxyTexts({}, key + ".", tpmval, mode);
  }, "get"),
  ownKeys: /* @__PURE__ */ __name((target) => Object.keys(target[proxyTextsTemplateSymbol]), "ownKeys")
};
function proxyTexts(obj, pfx, tpl, mode) {
  obj[proxyTextsPrefixSymbol] = pfx ?? "";
  obj[proxyTextsModeSymbol] = mode;
  obj[proxyTextsTemplateSymbol] = tpl;
  return new Proxy(obj, localizationProxyHandler);
}
__name(proxyTexts, "proxyTexts");
var TranslationConfig = {
  /** Retrieves the list of available languages */
  getLanguageList: null,
  /** A function to translate texts based on provided options */
  translateTexts: null
};
var text = localText;
var LT;
((LT2) => {
  LT2.add = addLocalText;
  LT2.getDefault = localText;
})(LT || (LT = {}));
var global2 = getGlobalObject();
var serenity = global2.Serenity || (global2.Serenity = {});
if (!serenity.addLocalText) {
  serenity.addLocalText = addLocalText;
}
var lt = serenity.LT || (serenity.LT = {});
if (!lt.add) {
  lt.add = serenity.addLocalText;
}

// src/base/html.ts
import * as sleekgrid from "@serenity-is/sleekgrid";

// src/base/config.ts
var Config = {
  /**
   * This is the root path of your application. If your application resides under http://localhost/mysite/,
   * your root path is "/mysite/". This variable is automatically initialized by reading from a <link> element
   * with ID "ApplicationPath" from current page, which is usually located in your _LayoutHead.cshtml file
   */
  applicationPath: "/",
  /**
   * Gets the content Security Policy nonce value to be used in script/style tags. This is usually set from server side
   * via a meta tag with name "csp-nonce".
   */
  cspNonce: null,
  /**
   * Gets a default return URL for the application. This is used when a return URL is not specified
   * @param purpose Optional purpose for the return URL, for example "login" or "logout"
   */
  defaultReturnUrl: /* @__PURE__ */ __name((purpose) => Config.applicationPath, "defaultReturnUrl"),
  /**
   * Email validation by default only allows ASCII characters. Set this to true if you want to allow unicode.
   */
  emailAllowOnlyAscii: true,
  /**
   * This is an optional callback that is used to load types lazily when they are not found in the
   * type registry. This is useful when a type is not available in currently loaded scripts 
   * (e.g. chunks / entry modules) but is available via some other means (e.g. a separate script file).
   * The method may return a type or a promise that resolves to a type. If either returns null, 
   * the type is considered to be not found.
   * The method is called with the type key and an optional kind parameter, which is used to distinguish
   * between different kinds of types (e.g. "editor" or "dialog" or "enum") usually based on calling type registry.
   */
  lazyTypeLoader: null,
  /**
   * This is the list of root namespaces that may be searched for types. For example, if you specify an editor type
   * of "MyEditor", first a class with name "MyEditor" will be searched, if not found, search will be followed by
   * "Serenity.MyEditor" and "MyApp.MyEditor" if you added "MyApp" to the list of root namespaces.
   *
   * You should usually add your application root namespace to this list in ScriptInit(ialization).ts file.
   */
  rootNamespaces: ["Serenity"],
  /**
   * This is an optional method for handling when user is not logged in. If a users session is expired 
   * and when a NotAuthorized response is received from a service call, Serenity will call this handler, so
   * you may intercept it and notify user about this situation and ask if she wants to login again...
   */
  notLoggedInHandler: null
};
function resetApplicationPath() {
  Config.applicationPath = "/";
  if (typeof document !== "undefined") {
    const pathLink = document.querySelector("link#ApplicationPath");
    if (pathLink != null) {
      Config.applicationPath = pathLink.getAttribute("href");
    }
  }
}
__name(resetApplicationPath, "resetApplicationPath");
function resetCspNonce() {
  Config.cspNonce = null;
  if (typeof document !== "undefined") {
    Config.cspNonce = document.querySelector('meta[name="csp-nonce"]')?.content ?? document.querySelector("script[nonce]")?.nonce ?? document.querySelector("style[nonce]")?.nonce;
  }
}
__name(resetCspNonce, "resetCspNonce");
resetApplicationPath();
resetCspNonce();

// src/base/html.ts
var esc = {
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;",
  "&": "&amp;"
};
function escFunc(a2) {
  return esc[a2];
}
__name(escFunc, "escFunc");
function htmlEncode(s2) {
  if (s2 == null)
    return "";
  if (typeof s2 !== "string")
    s2 = "" + s2;
  return s2.replace(/[<>"'&]/g, escFunc);
}
__name(htmlEncode, "htmlEncode");
function toggleClass(el, cls, add) {
  if (!el || cls == null || !cls.length)
    return;
  if (cls.indexOf(" ") < 0) {
    el.classList.toggle(cls, add);
    return;
  }
  const k = cls.split(" ").map((x) => x.trim()).filter((x) => x.length);
  for (const a2 of k)
    el.classList.toggle(a2, add);
}
__name(toggleClass, "toggleClass");
function addClass(el, cls) {
  return toggleClass(el, cls, true);
}
__name(addClass, "addClass");
function removeClass(el, cls) {
  return toggleClass(el, cls, false);
}
__name(removeClass, "removeClass");
function appendToNode(parent2, child) {
  if (child == null || child === false)
    return;
  if (isArrayLike(child)) {
    for (let i2 = 0; i2 < child.length; i2++) {
      appendToNode(parent2, child[i2]);
    }
  } else if (typeof child === "string") {
    parent2.appendChild(document.createTextNode(child));
  } else if (child instanceof Node) {
    parent2.appendChild(child);
  } else if (isPromiseLike(child)) {
    const placeholder = parent2.appendChild(document.createComment("Loading content..."));
    child.then((result) => {
      const fragment = document.createDocumentFragment();
      appendToNode(fragment, result);
      placeholder.parentElement?.replaceChild(fragment, placeholder);
    }, (error) => {
      placeholder.textContent = "Error loading content: " + error;
      throw error;
    });
  } else {
    parent2.append(child);
  }
}
__name(appendToNode, "appendToNode");
var SAFE_URL_PATTERN = /^(?:(?:https?|mailto|ftp|tel|file|sms):|[^&:/?#]*(?:[/?#]|$))/gi;
var DATA_URL_PATTERN = /^data:(?:image\/(?:bmp|gif|jpeg|jpg|png|tiff|webp)|video\/(?:mpeg|mp4|ogg|webm)|audio\/(?:mp3|oga|ogg|opus));base64,[a-z0-9+\/]+=*$/i;
function sanitizeUrl(url) {
  url = String(url).trim();
  if (url === "null" || url.length === 0 || url === "about:blank") return "about:blank";
  if (url.match(SAFE_URL_PATTERN) || url.match(DATA_URL_PATTERN)) return url;
  if (url === "javascript:void(0)" || url === "javascript:;") return url;
  return `unsafe:${url}`;
}
__name(sanitizeUrl, "sanitizeUrl");
function getElementReadOnly(el) {
  if (el == null)
    return null;
  if (el.classList.contains("readonly"))
    return true;
  const type = el.getAttribute("type");
  if (el.tagName == "SELECT" || type === "radio" || type === "checkbox")
    return el.hasAttribute("disabled");
  return el.hasAttribute("readonly");
}
__name(getElementReadOnly, "getElementReadOnly");
function setElementReadOnly(elements, value) {
  if (!elements)
    return;
  elements = isArrayLike(elements) ? elements : [elements];
  for (let i2 = 0; i2 < elements.length; i2++) {
    let el = elements[i2];
    if (!el)
      continue;
    const type = el.getAttribute("type");
    el.classList.toggle("readonly", !!value);
    const attr = el.tagName == "SELECT" || type === "radio" || type === "checkbox" ? "disabled" : "readonly";
    value ? el.setAttribute(attr, attr) : el.removeAttribute(attr);
  }
}
__name(setElementReadOnly, "setElementReadOnly");
function parseQueryString(s2) {
  let qs;
  if (s2 === void 0)
    qs = location.search.substring(1, location.search.length);
  else
    qs = s2 || "";
  let result = {};
  let parts = qs.split("&");
  for (let i2 = 0; i2 < parts.length; i2++) {
    let part = parts[i2];
    if (!part.length)
      continue;
    let pair = parts[i2].split("=");
    let name = decodeURIComponent(pair[0]);
    result[name] = pair.length >= 2 ? decodeURIComponent(pair[1]) : name;
  }
  return result;
}
__name(parseQueryString, "parseQueryString");
function isSafeReturnUrl(url) {
  if (!url || typeof url !== "string")
    return false;
  if (!/^\//.test(url))
    return false;
  if (url.includes(":"))
    return false;
  if (url.includes("\\") || /[\0-\x1F\x7F]/.test(url) || /\/\//.test(url.substring(1)))
    return false;
  if (!/^\/[\w\-./?&=%]*$/.test(url))
    return false;
  return true;
}
__name(isSafeReturnUrl, "isSafeReturnUrl");
function getReturnUrl(opt) {
  const q = parseQueryString();
  let returnUrl = q["returnUrl"] || q["ReturnUrl"] || q["ReturnURL"] || q["returnURL"];
  if (returnUrl && !opt?.ignoreUnsafe) {
    if (!isSafeReturnUrl(returnUrl))
      returnUrl = null;
  }
  if (!returnUrl && !opt?.queryOnly)
    returnUrl = Config.defaultReturnUrl(opt?.purpose);
  return returnUrl;
}
__name(getReturnUrl, "getReturnUrl");
function cssEscape(selector) {
  if (typeof CSS !== "undefined" && typeof CSS.escape === "function")
    return CSS.escape(selector);
  const string = String(selector);
  const length = string.length;
  let index = -1;
  let codeUnit;
  let result = "";
  const firstCodeUnit = string.charCodeAt(0);
  if (length == 1 && firstCodeUnit == 45)
    return "\\" + string;
  while (++index < length) {
    codeUnit = string.charCodeAt(index);
    if (codeUnit == 0) {
      result += "\uFFFD";
      continue;
    }
    if (codeUnit >= 1 && codeUnit <= 31 || codeUnit == 127 || index == 0 && codeUnit >= 48 && codeUnit <= 57 || index == 1 && codeUnit >= 48 && codeUnit <= 57 && firstCodeUnit == 45) {
      result += "\\" + codeUnit.toString(16) + " ";
      continue;
    }
    if (codeUnit >= 128 || codeUnit == 45 || codeUnit == 95 || codeUnit >= 48 && codeUnit <= 57 || codeUnit >= 65 && codeUnit <= 90 || codeUnit >= 97 && codeUnit <= 122) {
      result += string.charAt(index);
      continue;
    }
    result += "\\" + string.charAt(index);
  }
  return result;
}
__name(cssEscape, "cssEscape");
var maybeHtmlRegex = /<|>|&|"|'/;
function sanitizeHtml(dirtyHtml) {
  if (!dirtyHtml) {
    return "";
  }
  if (!maybeHtmlRegex.test(dirtyHtml)) {
    return dirtyHtml;
  }
  let sanitizer;
  if (typeof sleekgrid !== "undefined") {
    if (typeof sleekgrid.formatterContext === "function")
      sanitizer = sleekgrid.formatterContext()?.sanitizer;
    if (!sanitizer && typeof sleekgrid.gridDefaults?.sanitizer === "function")
      sanitizer = sleekgrid.gridDefaults.sanitizer;
  }
  if (!sanitizer && typeof globalThis.DOMPurify?.sanitize === "function")
    sanitizer = globalThis.DOMPurify.sanitize;
  if (sanitizer)
    return sanitizer(dirtyHtml);
  if (typeof DOMParser === "undefined") {
    return htmlEncode(dirtyHtml);
  }
  const SAFE_URL_PATTERN2 = /^(?!javascript:|data:|vbscript:)(?:[a-z0-9+.-]+:|[^&:/?#]*(?:[/?#]|$))/i;
  try {
    const parser = new DOMParser();
    const doc = parser.parseFromString(dirtyHtml, "text/html");
    const body = doc.body || (typeof document !== "undefined" ? document.createElement("body") : null);
    if (!body) {
      return htmlEncode(dirtyHtml);
    }
    let targetElement = body;
    if (body.innerHTML.trim() === "" && doc.documentElement && doc.documentElement.innerHTML.trim()) {
      body.innerHTML = doc.documentElement.innerHTML;
    }
    if (body.innerHTML.trim() === "" && doc.documentElement) {
      targetElement = doc.documentElement;
    }
    const dangerousElements = targetElement.querySelectorAll("script, iframe, object, embed, form, input, button, textarea, select, style, link");
    dangerousElements.forEach((el) => el.remove());
    const allElements = targetElement.querySelectorAll("*");
    allElements.forEach((el) => {
      const element = el;
      Array.from(element.attributes).forEach((attr) => {
        const name = attr.name.toLowerCase();
        const value = attr.value;
        if (name.startsWith("on")) {
          element.removeAttribute(attr.name);
          return;
        }
        if (["href", "src", "xlink:href"].indexOf(name) >= 0) {
          if (!SAFE_URL_PATTERN2.test(value)) {
            element.removeAttribute(attr.name);
            return;
          }
        }
        if (value.toLowerCase().indexOf("javascript") >= 0) {
          element.removeAttribute(attr.name);
        }
      });
    });
    return targetElement.innerHTML;
  } catch (e2) {
    return htmlEncode(dirtyHtml);
  }
}
__name(sanitizeHtml, "sanitizeHtml");

// ../domwise/dist/index.js
var hasOwnProperty = Object.hasOwnProperty;
var bindThisHandler = {
  get: /* @__PURE__ */ __name((target, property) => {
    if (hasOwnProperty.call(target, property)) {
      return target[property];
    }
    const m = target[property];
    if (typeof m === "function") {
      return target[property] = m.bind(target);
    }
    return m;
  }, "get")
};
var bindThisProxyMap = /* @__PURE__ */ new WeakMap();
function bindThis(obj) {
  let proxy = bindThisProxyMap.get(obj);
  if (!proxy) {
    proxy = new Proxy(obj, bindThisHandler);
    bindThisProxyMap.set(obj, proxy);
  }
  return proxy;
}
__name(bindThis, "bindThis");
var initPropHookSymbol = /* @__PURE__ */ Symbol.for("Serenity.initPropHook");
function isElement(val) {
  return !!(val && typeof val.nodeType === "number");
}
__name(isElement, "isElement");
function isString(val) {
  return typeof val === "string";
}
__name(isString, "isString");
function isNumber(val) {
  return typeof val === "number";
}
__name(isNumber, "isNumber");
function isObject(val) {
  return typeof val === "object" && val !== null;
}
__name(isObject, "isObject");
function isComponentClass(val) {
  return !!(val && val.isComponent);
}
__name(isComponentClass, "isComponentClass");
function isArrayLike2(val) {
  return isObject(val) && typeof val.length === "number" && typeof val.nodeType !== "number";
}
__name(isArrayLike2, "isArrayLike");
function isPropHook(value) {
  return typeof value === "function" && typeof value[initPropHookSymbol] === "function";
}
__name(isPropHook, "isPropHook");
function isVisibleChild(val) {
  return val != null && typeof val !== "boolean";
}
__name(isVisibleChild, "isVisibleChild");
function className(value) {
  if (Array.isArray(value)) {
    return value.map(className).filter(Boolean).join(" ");
  } else if (isObject(value)) {
    if (Symbol.iterator in value) {
      return className(Array.from(value));
    }
    return Object.keys(value).filter((k) => value[k]).join(" ");
  } else if (isVisibleChild(value)) {
    return "" + value;
  } else {
    return "";
  }
}
__name(className, "className");
function createRef() {
  return Object.seal({ current: null });
}
__name(createRef, "createRef");
function isRefObject(maybeRef) {
  return maybeRef != null && typeof maybeRef === "object" && "current" in maybeRef;
}
__name(isRefObject, "isRefObject");
function setRef(ref, current) {
  if (isRefObject(ref)) {
    ref.current = current;
  } else if (typeof ref === "function") {
    ref(current);
  }
}
__name(setRef, "setRef");
var Component = class {
  static {
    __name(this, "Component");
  }
  static {
    this.isComponent = true;
  }
  constructor(props) {
    this.props = props;
  }
  render() {
    return null;
  }
};
function initComponentClass(Class, attr, children) {
  attr = { ...attr, children };
  const instance = new Class(attr);
  const node = instance.render();
  if ("ref" in attr) {
    setRef(attr.ref, instance);
  }
  return node;
}
__name(initComponentClass, "initComponentClass");
var MathMLNamespace = "http://www.w3.org/1998/Math/MathML";
var mathMLOnlyTags = /* @__PURE__ */ new Set(["math", "annotation", "annotation-xml", "maction", "merror", "mfrac", "mi", "mmultiscripts", "mn", "mo", "mover", "mpadded", "mphantom", "mprescripts", "mroot", "mrow", "ms", "mspace", "msqrt", "mstyle", "msub", "msubsup", "msup", "mtable", "mtd", "mtext", "mtr", "munder", "munderover", "semantics", "menclose", "mfenced"]);
var svgOnlyTags = /* @__PURE__ */ new Set([
  // "a",
  "animate",
  "animateMotion",
  "animateTransform",
  "circle",
  "clipPath",
  "defs",
  "desc",
  "ellipse",
  "feBlend",
  "feColorMatrix",
  "feComponentTransfer",
  "feComposite",
  "feConvolveMatrix",
  "feDiffuseLighting",
  "feDisplacementMap",
  "feDistantLight",
  "feDropShadow",
  "feFlood",
  "feFuncA",
  "feFuncB",
  "feFuncG",
  "feFuncR",
  "feGaussianBlur",
  "feImage",
  "feMerge",
  "feMergeNode",
  "feMorphology",
  "feOffset",
  "fePointLight",
  "feSpecularLighting",
  "feSpotLight",
  "feTile",
  "feTurbulence",
  "filter",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "marker",
  "mask",
  "metadata",
  "mpath",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  // "script",
  "set",
  "stop",
  // "style",
  "svg",
  "switch",
  "symbol",
  "text",
  "textPath",
  // "title",
  "tref",
  "tspan",
  "use",
  "view"
]);
var nonPresentationSVGAttributes = /^(a(ll|t|u)|base[FP]|c(al|lipPathU|on)|di|ed|ex|filter[RU]|g(lyphR|r)|ke|l(en|im)|ma(rker[HUW]|s)|n|pat|pr|point[^e]|re[^n]|s[puy]|st[^or]|ta|textL|vi|xC|y|z)/;
var SVGNamespace = "http://www.w3.org/2000/svg";
var jsxNamespaceURISymbol = /* @__PURE__ */ Symbol.for("Serenity.jsxNamespaceURI");
function currentNamespaceURI(value) {
  const current = globalThis[jsxNamespaceURISymbol];
  if (!arguments.length) {
    return current;
  }
  globalThis[jsxNamespaceURISymbol] = value;
  return current;
}
__name(currentNamespaceURI, "currentNamespaceURI");
function inNamespaceURI(namespaceURI, children) {
  const prev = currentNamespaceURI(namespaceURI);
  let result;
  try {
    result = children();
  } finally {
    currentNamespaceURI(prev);
  }
  return result;
}
__name(inNamespaceURI, "inNamespaceURI");
function inSVGNamespace(fn) {
  return inNamespaceURI(SVGNamespace, fn);
}
__name(inSVGNamespace, "inSVGNamespace");
function inMathMLNamespace(fn) {
  return inNamespaceURI(MathMLNamespace, fn);
}
__name(inMathMLNamespace, "inMathMLNamespace");
function inHTMLNamespace(fn) {
  return inNamespaceURI(null, fn);
}
__name(inHTMLNamespace, "inHTMLNamespace");
var jsxTypeKeySymbol = /* @__PURE__ */ Symbol.for("Serenity.jsxTypeKey");
var shadowRootKey = "ShadowRoot";
function ShadowRootNode({
  children,
  ref,
  ...attr
}) {
  return {
    [jsxTypeKeySymbol]: shadowRootKey,
    ref,
    attr,
    children
  };
}
__name(ShadowRootNode, "ShadowRootNode");
function isShadowRoot(el) {
  return el != null && el[jsxTypeKeySymbol] === shadowRootKey;
}
__name(isShadowRoot, "isShadowRoot");
var disposingListenersSymbol = /* @__PURE__ */ Symbol.for("Serenity.disposingListeners");
var lifecycleRootSymbol = /* @__PURE__ */ Symbol.for("Serenity.lifecycleRoot");
function getDisposingListeners() {
  return globalThis[disposingListenersSymbol] ||= /* @__PURE__ */ new WeakMap();
}
__name(getDisposingListeners, "getDisposingListeners");
function disposingEventListener(ev) {
  if (ev && ev.target && (!ev.currentTarget || ev.currentTarget === ev.target))
    invokeDisposingListeners(ev?.target);
}
__name(disposingEventListener, "disposingEventListener");
function dispatchDisposingEvent(target, opt) {
  if (!target || typeof CustomEvent !== "function")
    return;
  const event = new CustomEvent("disposing", {
    bubbles: opt?.bubbles ?? false,
    cancelable: opt?.cancelable ?? false
  });
  target.dispatchEvent(event);
}
__name(dispatchDisposingEvent, "dispatchDisposingEvent");
function invokeDisposingListeners(node, opt) {
  if (!node)
    return;
  const disposingListeners = getDisposingListeners();
  function invokeFor(el) {
    const listeners = disposingListeners.get(el);
    if (!listeners)
      return;
    disposingListeners.delete(el);
    el.removeEventListener("disposing", disposingEventListener);
    for (const disposer of listeners) {
      try {
        disposer.callback(el);
      } catch {
      }
    }
  }
  __name(invokeFor, "invokeFor");
  if (opt?.descendants && node instanceof Element && node.hasChildNodes()) {
    const descendants = [];
    const iterator = document.createNodeIterator(
      node,
      NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_TEXT | NodeFilter.SHOW_COMMENT
    );
    let currentNode;
    while (currentNode = iterator.nextNode()) {
      if (currentNode !== node) {
        descendants.push(currentNode);
      }
    }
    for (let i2 = 0; i2 < descendants.length; i2++) {
      invokeFor(descendants[i2]);
    }
  }
  if (!opt?.excludeSelf) {
    invokeFor(node);
  }
}
__name(invokeDisposingListeners, "invokeDisposingListeners");
function addDisposingListener(target, handler, regKey) {
  if (!target || !handler)
    return target;
  const disposingListeners = getDisposingListeners();
  let listeners = disposingListeners.get(target);
  if (!listeners) {
    if (typeof target.addEventListener !== "function")
      return target;
    disposingListeners.set(target, listeners = [{
      callback: handler,
      regKey
    }]);
    target.addEventListener("disposing", disposingEventListener, { once: true });
    return target;
  }
  const existing = listeners.find((x) => x.callback === handler);
  if (existing) {
    if (existing.regKey && regKey && existing.regKey !== regKey) {
      throw new Error("A disposing listener with the same callback but different regKey is already registered on the target element.");
    }
    if (regKey && !existing.regKey) {
      existing.regKey = regKey;
    }
    return target;
  }
  listeners.push({ callback: handler, regKey });
  return target;
}
__name(addDisposingListener, "addDisposingListener");
function removeDisposingListener(target, handler, regKey) {
  if (!target || !handler && !regKey)
    return target;
  const disposingListeners = getDisposingListeners();
  const listeners = disposingListeners.get(target);
  if (listeners) {
    for (let index = listeners.length - 1; index >= 0; index--) {
      const listener = listeners[index];
      if (regKey && listener.regKey === regKey || handler && handler === listener.callback) {
        listeners.splice(index, 1);
      }
    }
    if (listeners.length === 0) {
      disposingListeners.delete(target);
      if (typeof target.removeEventListener === "function") {
        target.removeEventListener("disposing", disposingEventListener);
      }
    }
  }
  return target;
}
__name(removeDisposingListener, "removeDisposingListener");
function currentLifecycleRoot(...args) {
  if (args.length > 0) {
    const prev = globalThis[lifecycleRootSymbol] || null;
    globalThis[lifecycleRootSymbol] = args[0] || null;
    return prev;
  }
  return globalThis[lifecycleRootSymbol] || null;
}
__name(currentLifecycleRoot, "currentLifecycleRoot");
function isSignalLike(obj) {
  return obj != null && typeof obj === "object" && typeof obj.subscribe === "function" && typeof obj.peek === "function" && "value" in obj;
}
__name(isSignalLike, "isSignalLike");
var SignalObserveArgsImpl = class {
  static {
    __name(this, "SignalObserveArgsImpl");
  }
  #dispose;
  #node;
  constructor(signal2, lifecycleRoot, lifecycleNode) {
    this.signal = signal2;
    this.isInitial = true;
    this.hasChanged = false;
    this.lifecycleRoot = lifecycleRoot;
    this.#node = lifecycleNode;
  }
  get lifecycleNode() {
    return this.#node;
  }
  #delDispose() {
    removeDisposingListener(this.#node, this.effectDisposer);
    removeDisposingListener(this.#node, this.signal?.derivedDisposer);
  }
  #addDispose() {
    addDisposingListener(this.#node, this.effectDisposer);
    addDisposingListener(this.#node, this.signal?.derivedDisposer);
  }
  get effectDisposer() {
    return this.#dispose;
  }
  set effectDisposer(value) {
    this.#delDispose();
    this.#dispose = value;
    this.#addDispose();
  }
  set lifecycleNode(value) {
    if (value !== this.#node) {
      this.#delDispose();
      this.#node = value;
      this.#addDispose();
    }
  }
};
function observeSignal(signal2, callback, opt) {
  const lifecycleRoot = opt?.useLifecycleRoot ? currentLifecycleRoot() : void 0;
  const args = new SignalObserveArgsImpl(signal2, lifecycleRoot, opt?.lifecycleNode);
  const disposer = args.signal.subscribe(function(value) {
    args.newValue = value;
    if (args.isInitial && this?.dispose) {
      args.effectDisposer = this.dispose.bind(this);
    }
    args.hasChanged = !args.isInitial && args.prevValue !== args.newValue;
    try {
      callback(args);
    } finally {
      args.prevValue = args.newValue;
      args.isInitial = false;
    }
  });
  if (disposer && !args.effectDisposer) {
    args.effectDisposer = disposer;
  }
  return args.effectDisposer;
}
__name(observeSignal, "observeSignal");
function derivedSignal(input, fn) {
  if (!isSignalLike(input)) {
    throw new Error("Input must be a SignalLike");
  }
  const callback = /* @__PURE__ */ __name(() => fn(input.value), "callback");
  if (typeof input.constructor === "function" && input.constructor !== {}.constructor) {
    try {
      let derived = new input.constructor(callback);
      let disposer2;
      if (isSignalLike(derived)) {
        if (derived.peek() === callback) {
          disposer2 = input.subscribe(() => {
            derived.value = callback();
          });
        }
        if (disposer2) {
          derived.derivedDisposer = function() {
            disposer2();
            delete derived.derivedDisposer;
          };
        }
        return derived;
      }
    } catch (error) {
    }
  }
  let primitive;
  const disposer = input.subscribe(() => {
    if (!primitive) {
      primitive = new PrimitiveComputed(callback);
      return;
    }
    primitive.update();
  });
  if (disposer) {
    primitive.derivedDisposer = function() {
      disposer();
      delete primitive.derivedDisposer;
    };
  }
  return primitive;
}
__name(derivedSignal, "derivedSignal");
var PrimitiveComputed = class {
  static {
    __name(this, "PrimitiveComputed");
  }
  #subs = /* @__PURE__ */ new Set();
  #value;
  #fn;
  constructor(fn) {
    this.#fn = fn;
    this.update(true);
  }
  update(force) {
    const newValue = this.#fn();
    if (newValue !== this.#value || force) {
      this.#value = newValue;
      this.#subs.forEach((sub) => sub(newValue));
    }
  }
  subscribe(callback) {
    callback(this.#value);
    this.#subs.add(callback);
    return () => this.#subs.delete(callback);
  }
  peek() {
    return this.#value;
  }
  get value() {
    return this.#value;
  }
};
function appendChild(parent2, child) {
  if (parent2 instanceof window.HTMLTemplateElement) {
    parent2.content.appendChild(child);
  } else {
    parent2.appendChild(child);
  }
}
__name(appendChild, "appendChild");
var fragmentPlaceholderIdx = 0;
var placeholderPrefix = "__domwisefrag_";
function isPlaceholder(node) {
  return node instanceof Comment && node.data.startsWith(placeholderPrefix);
}
__name(isPlaceholder, "isPlaceholder");
function isFragmentWithPlaceholder(node) {
  return node instanceof DocumentFragment && isPlaceholder(node.firstChild);
}
__name(isFragmentWithPlaceholder, "isFragmentWithPlaceholder");
function replaceNode(oldNode, newNode) {
  if (typeof oldNode.replaceWith === "function")
    oldNode.replaceWith(newNode);
  else
    oldNode.parentNode?.replaceChild(newNode, oldNode);
}
__name(replaceNode, "replaceNode");
function wrapAsNode(value) {
  if (value instanceof DocumentFragment) {
    ++fragmentPlaceholderIdx;
    value.prepend(document.createComment(placeholderPrefix + fragmentPlaceholderIdx));
    value.append(document.createComment(placeholderPrefix + fragmentPlaceholderIdx));
    return value;
  }
  if (value instanceof Node) {
    return value;
  }
  if (!isVisibleChild(value)) {
    return document.createComment("");
  }
  return document.createTextNode(value);
}
__name(wrapAsNode, "wrapAsNode");
function appendChildrenWithSignal(parent2, signal2) {
  let prevNode;
  observeSignal(signal2, (args) => {
    if (args.isInitial) {
      prevNode = wrapAsNode(args.newValue);
      let prevNodeNew2 = isFragmentWithPlaceholder(prevNode) ? prevNode.firstChild : prevNode;
      appendChildren(parent2, prevNode);
      args.lifecycleNode = (prevNode = prevNodeNew2) ?? parent2;
      return;
    }
    if (!args.hasChanged) {
      return;
    }
    const newNode = wrapAsNode(args.newValue);
    if (isPlaceholder(prevNode)) {
      let n2;
      while (n2 = prevNode.nextSibling) {
        n2.parentNode?.removeChild(n2);
        if (n2 instanceof Comment && n2.data === prevNode.data) {
          break;
        }
      }
    }
    let prevNodeNew = isFragmentWithPlaceholder(newNode) ? newNode.firstChild : newNode;
    replaceNode(prevNode, newNode);
    args.lifecycleNode = (prevNode = prevNodeNew) ?? parent2;
  });
}
__name(appendChildrenWithSignal, "appendChildrenWithSignal");
function appendChildren(parent2, children) {
  if (!isVisibleChild(children)) return;
  if (isArrayLike2(children)) {
    for (const child of [...children]) {
      appendChildren(parent2, child);
    }
  } else if (isString(children) || isNumber(children)) {
    appendChild(parent2, document.createTextNode(children));
  } else if (isElement(children)) {
    appendChild(parent2, children);
  } else if (isShadowRoot(children)) {
    const shadowRoot = parent2.attachShadow(children.attr);
    appendChildren(shadowRoot, children.children);
    setRef(children.ref, shadowRoot);
  } else if (isSignalLike(children)) {
    appendChildrenWithSignal(parent2, children);
  }
}
__name(appendChildren, "appendChildren");
function unsignalizePrevClass(prev) {
  if (prev == null)
    return prev;
  if (isArrayLike2(prev)) {
    prev = Array.from(prev).map((item) => {
      if (isSignalLike(item))
        return item.peek();
      return item;
    });
  } else if (isObject(prev)) {
    prev = { ...prev };
    Object.entries(prev).forEach(([key, val]) => {
      if (isSignalLike(val))
        prev[key] = val.peek();
    });
  }
  return prev;
}
__name(unsignalizePrevClass, "unsignalizePrevClass");
function clearPrevClass(node, prev) {
  if (prev == null || prev === false || prev === true)
    return;
  prev = unsignalizePrevClass(prev);
  const prevClassNames = (className(prev) ?? "").split(" ");
  for (let cls of prevClassNames) {
    if (cls) {
      node.classList.remove(cls);
    }
  }
}
__name(clearPrevClass, "clearPrevClass");
function assignClass(node, value, prev) {
  if (value == null || value === false) {
    clearPrevClass(node, prev);
    return;
  }
  prev = unsignalizePrevClass(prev);
  if (isArrayLike2(value)) {
    value = Array.from(value).map((x) => {
      let val = x;
      if (isSignalLike(x)) {
        observeSignal(x, (args) => {
          if (args.isInitial) {
            val = args.newValue;
            return;
          }
          applyClassName(node, args.newValue, args.prevValue);
        }, {
          lifecycleNode: node
        });
      }
      return val;
    });
  } else if (isObject(value)) {
    value = { ...value };
    Object.entries(value).forEach(([key, val]) => {
      if (isSignalLike(val)) {
        observeSignal(val, (args) => {
          if (args.isInitial) {
            value[key] = args.newValue;
            return;
          }
          applyClassName(node, Boolean(args.newValue) && key, Boolean(args.prevValue) && key);
        }, {
          lifecycleNode: node
        });
      }
    });
  }
  applyClassName(node, value, prev);
}
__name(assignClass, "assignClass");
function applyClassName(node, value, prev) {
  const newList = (className(value) ?? "").split(" ");
  if (prev) {
    const prevList = (className(prev) ?? "").split(" ");
    for (let cls of prevList) {
      if (cls && !newList.includes(cls)) {
        node.classList.remove(cls);
      }
    }
  }
  for (let cls of newList) {
    cls && node.classList.add(cls);
  }
}
__name(applyClassName, "applyClassName");
function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}
__name(prefixKey, "prefixKey");
var isUnitlessNumber = /* @__PURE__ */ (() => {
  let rec = {
    animationIterationCount: 0,
    borderImageOutset: 0,
    borderImageSlice: 0,
    borderImageWidth: 0,
    boxFlex: 0,
    boxFlexGroup: 0,
    boxOrdinalGroup: 0,
    columnCount: 0,
    columns: 0,
    flex: 0,
    flexGrow: 0,
    flexPositive: 0,
    flexShrink: 0,
    flexNegative: 0,
    flexOrder: 0,
    gridArea: 0,
    gridRow: 0,
    gridRowEnd: 0,
    gridRowSpan: 0,
    gridRowStart: 0,
    gridColumn: 0,
    gridColumnEnd: 0,
    gridColumnSpan: 0,
    gridColumnStart: 0,
    fontWeight: 0,
    lineClamp: 0,
    lineHeight: 0,
    opacity: 0,
    order: 0,
    orphans: 0,
    tabSize: 0,
    widows: 0,
    zIndex: 0,
    zoom: 0,
    // SVG-related properties
    fillOpacity: 0,
    floodOpacity: 0,
    stopOpacity: 0,
    strokeDasharray: 0,
    strokeDashoffset: 0,
    strokeMiterlimit: 0,
    strokeOpacity: 0,
    strokeWidth: 0
  };
  const prefixes = ["Webkit", "ms", "Moz", "O"];
  Object.keys(rec).forEach((prop) => {
    prefixes.forEach((prefix) => {
      rec[prefixKey(prefix, prop)] = 0;
    });
  });
  return rec;
})();
function setStylePropValue(node, key, val) {
  if (key.indexOf("-") === 0) {
    node.style.setProperty(key, val);
  } else if (isNumber(val) && isUnitlessNumber[key] !== 0) {
    node.style[key] = val + "px";
  } else {
    node.style[key] = val;
  }
}
__name(setStylePropValue, "setStylePropValue");
function clearPrevStyle(node, prev) {
  if (prev == null || prev === false || prev === true)
    return;
  if (isString(prev)) {
    node.removeAttribute("style");
    return;
  }
  if (isObject(prev)) {
    Object.keys(prev).forEach((key) => {
      setStylePropValue(node, key, "");
    });
  }
}
__name(clearPrevStyle, "clearPrevStyle");
function assignStyle(node, value, prev) {
  if (value == null || value === false || value === true) {
    clearPrevStyle(node, prev);
    return;
  }
  if (isString(value)) {
    node.setAttribute("style", value);
    return;
  }
  if (isObject(value)) {
    if (isObject(prev)) {
      value = { ...value };
      Object.keys(prev).forEach((key) => {
        if (!(key in value))
          value[key] = "";
      });
    } else
      clearPrevStyle(node, prev);
    Object.entries(value).forEach(([key, val]) => {
      if (isSignalLike(val)) {
        observeSignal(val, (args) => setStylePropValue(node, key, args.newValue), {
          lifecycleNode: node
        });
      } else {
        setStylePropValue(node, key, val);
      }
    });
    return;
  }
}
__name(assignStyle, "assignStyle");
var XLinkNamespace = "http://www.w3.org/1999/xlink";
var XMLNamespace = "http://www.w3.org/XML/1998/namespace";
function normalizeAttribute(s2, separator) {
  return s2.replace(/[A-Z]/g, (match) => separator + match.toLowerCase());
}
__name(normalizeAttribute, "normalizeAttribute");
var mappedKeys = /* @__PURE__ */ new Map([
  ["autoComplete", "autocomplete"],
  ["autoFocus", "autofocus"],
  ["className", "class"],
  ["colSpan", "colspan"],
  ["contentEditable", "contenteditable"],
  ["formNoValidate", "formnovalidate"],
  ["htmlFor", "for"],
  ["innerText", "textContent"],
  ["minLength", "minlength"],
  ["maxLength", "maxlength"],
  ["readOnly", "readonly"],
  ["reffererPolicy", "referrerpolicy"],
  ["rowSpan", "rowspan"],
  ["spellCheck", "spellcheck"],
  ["tabIndex", "tabindex"],
  ["onDoubleClick", "onDblClick"],
  ["onDoubleClickCapture", "onDblClickCapture"]
]);
function assignProp(node, key, value, prev) {
  switch (key) {
    case "dataset":
      if (prev != null) {
        Object.entries(prev).forEach(([k, v2]) => {
          if (v2 != null) {
            delete node.dataset[k];
          }
        });
      }
      Object.entries(value).forEach(([dataKey, dataValue]) => {
        if (dataValue != null) {
          node.dataset[dataKey] = dataValue;
        }
      });
      return;
    case "textContent":
      if (isVisibleChild(value)) {
        node[key] = value;
      } else if (isVisibleChild(prev)) {
        node[key] = "";
      }
      return;
    case "dangerouslySetInnerHTML":
      if (isObject(value)) {
        node.innerHTML = value["__html"];
      } else if (isObject(prev)) {
        node.innerHTML = "";
      }
      return;
    case "value":
      if (value == null) {
        if (prev != null) {
          node[key] = null;
        }
        return;
      } else if (node instanceof window.HTMLSelectElement) {
        return;
      } else if (node instanceof window.HTMLTextAreaElement) {
        node.value = value;
        return;
      }
      break;
    case "class":
      assignClass(node, value, prev);
      return;
    case "spellcheck":
      node.spellcheck = value === "" || value === true || value === "true" ? true : value === false || value === "false" ? false : value;
      return;
    case "draggable":
      if (value === false || value === "false")
        value = "false";
      else if (value === "")
        value = null;
      else if (value === true || value === "true")
        value = "true";
      break;
    case "contenteditable":
      if (value === false)
        value = "false";
      else if (value === "" || value === true)
        value = "true";
      break;
    case "ref":
    case "namespaceURI":
      return;
    case "style":
      assignStyle(node, value, prev);
      return;
    case "on":
    case "onCapture":
      const useCapture = key === "onCapture";
      if (prev != null) {
        Object.entries(prev).forEach(([eventName, eventHandler]) => {
          if (value == null || value[eventName] !== eventHandler) {
            node.removeEventListener(eventName, eventHandler, useCapture);
          }
        });
      }
      Object.entries(value).forEach(([eventName, eventHandler]) => {
        if (prev == null || prev[eventName] !== eventHandler) {
          node.addEventListener(eventName, eventHandler, useCapture);
        }
      });
      return;
    case "xlinkActuate":
    case "xlinkArcrole":
    case "xlinkHref":
    case "xlinkRole":
    case "xlinkShow":
    case "xlinkTitle":
    case "xlinkType":
      node.setAttributeNS(XLinkNamespace, normalizeAttribute(key, ":"), value);
      return;
    case "xmlnsXlink":
      node.setAttribute("xmlns:xlink", value);
      return;
    case "xmlBase":
    case "xmlLang":
    case "xmlSpace":
      node.setAttributeNS(XMLNamespace, normalizeAttribute(key, ":"), value);
      return;
  }
  if (typeof value === "function") {
    if (key[0] === "o" && key[1] === "n") {
      if (prev != null && prev === value)
        return;
      let attribute = key.toLowerCase();
      const useCapture = attribute.endsWith("capture");
      let eventName;
      if (!useCapture && (node[attribute] === null || prev != null && node[attribute] === prev)) {
        node[attribute] = value;
        addDisposingListener(node, nodeEventPropDisposer.bind(node, attribute));
      } else if (useCapture) {
        eventName = attribute.substring(2, attribute.length - 7);
        if (prev != null) {
          node.removeEventListener(eventName, prev, true);
        }
        node.addEventListener(eventName, value, true);
      } else {
        if (attribute in window) {
          const standardEventName = attribute.substring(2);
          eventName = standardEventName;
        } else {
          const customEventName = attribute[2] + key.slice(3);
          eventName = customEventName;
        }
        if (prev != null) {
          node.removeEventListener(eventName, prev);
        }
        node.addEventListener(eventName, value);
      }
    } else {
      console.warn(`A function was provided for JSX ${node.tagName} element ${key} which does not start with "on":`, value, node.tagName);
    }
    return;
  }
  if (isObject(prev)) {
    if (prev === value) {
      return;
    }
    delete node[key];
  }
  if (prev === true) {
    if (prev === value) {
      return;
    }
    node.removeAttribute(key);
  }
  if (prev !== false && prev != null) {
    if (prev === value) {
      return;
    }
    if (node instanceof SVGElement && !nonPresentationSVGAttributes.test(key)) {
      node.removeAttributeNS(normalizeAttribute(key, "-"), value);
    } else {
      node.removeAttribute(key);
    }
  }
  if (isObject(value)) {
    node[key] = value;
    return;
  }
  if (value === true) {
    node.setAttribute(key, "");
    return;
  }
  if (value !== false && value != null) {
    if (node instanceof SVGElement && !nonPresentationSVGAttributes.test(key)) {
      node.setAttribute(normalizeAttribute(key, "-"), value);
    } else {
      node.setAttribute(key, value);
    }
  }
}
__name(assignProp, "assignProp");
function assignProps(node, props) {
  for (let [key, value] of Object.entries(props)) {
    key = mappedKeys.get(key) ?? key;
    if (isSignalLike(value)) {
      observeSignal(value, (args) => assignProp(node, key, args.newValue, args.prevValue), {
        lifecycleNode: node
      });
    } else if (isPropHook(value)) {
      value[initPropHookSymbol](node, key);
    } else {
      assignProp(node, key, value);
    }
  }
  return node;
}
__name(assignProps, "assignProps");
function nodeEventPropDisposer(key) {
  this[key] = null;
}
__name(nodeEventPropDisposer, "nodeEventPropDisposer");
function jsx(tag, props) {
  let { children, ...attr } = props || {};
  let ns = attr.namespaceURI;
  if (!ns) {
    if (svgOnlyTags.has(tag))
      ns = SVGNamespace;
    else if (mathMLOnlyTags.has(tag))
      ns = MathMLNamespace;
    else
      ns = currentNamespaceURI();
  }
  let node;
  if (isString(tag)) {
    node = ns ? document.createElementNS(ns, tag) : document.createElement(tag);
    assignProps(node, attr);
    appendChildren(node, children);
    if (node instanceof window.HTMLSelectElement && attr.value != null) {
      if (attr.multiple === true && Array.isArray(attr.value)) {
        const values = attr.value.map((value) => String(value));
        node.querySelectorAll("option").forEach((option) => option.selected = values.includes(option.value));
      } else {
        node.value = attr.value;
      }
    }
    setRef(attr.ref, node);
  } else if (typeof tag === "function") {
    if (isObject(tag.defaultProps)) {
      attr = { ...tag.defaultProps, ...attr };
    }
    node = isComponentClass(tag) ? initComponentClass(tag, attr, children) : tag({ ...attr, children });
  } else {
    throw new TypeError(`Invalid JSX element type: ${tag}`);
  }
  return node;
}
__name(jsx, "jsx");
function createElement(tag, attr, ...children) {
  if (isString(attr) || Array.isArray(attr)) {
    children.unshift(attr);
    attr = {};
  }
  attr = attr || {};
  if (attr.children != null && !children.length) {
    ({ children, ...attr } = attr);
  }
  return jsx(tag, { ...attr, children });
}
__name(createElement, "createElement");
function useImperativeHandle(ref, init) {
  setRef(ref, init());
}
__name(useImperativeHandle, "useImperativeHandle");
function Fragment(attr) {
  const fragment = document.createDocumentFragment();
  appendChildren(fragment, attr.children);
  return fragment;
}
__name(Fragment, "Fragment");
function useClassList(initialValue) {
  let temp = document.createElement("div");
  if (initialValue != null) {
    temp.className = className(initialValue);
  }
  let list = temp.classList;
  function ClassList() {
    return list;
  }
  __name(ClassList, "ClassList");
  function dispose() {
    list = null;
    temp = null;
  }
  __name(dispose, "dispose");
  Object.defineProperties(
    ClassList,
    Object.getOwnPropertyDescriptors({
      [initPropHookSymbol](node, prop) {
        if (prop !== "class")
          throw new Error("useClassList can only be used for 'class' attribute.");
        temp && removeDisposingListener(temp, dispose);
        list && node?.setAttribute("class", list.value);
        addDisposingListener(temp = node, dispose);
        list = node?.classList ?? list;
      },
      get size() {
        return list.length;
      },
      get value() {
        return list.value;
      },
      add(...tokens) {
        list.add(...tokens);
      },
      remove(...tokens) {
        list.remove(...tokens);
      },
      toggle(token, force) {
        list.toggle(token, force);
      },
      contains(token) {
        return list.contains(token);
      }
    })
  );
  return ClassList;
}
__name(useClassList, "useClassList");
function propBinding(value) {
  if (arguments.length && value !== this.value) {
    this.node && this.prop && assignProp(this.node, this.prop, value, this.value);
    this.value = value;
  }
  return this.value;
}
__name(propBinding, "propBinding");
function propBindingDispose() {
  delete this.node;
}
__name(propBindingDispose, "propBindingDispose");
function propBindingInit(el, key) {
  if (this.node && el && this.node !== el || this.prop && key && this.prop !== key) {
    throw new Error("usePropBinding can only be used with one element and one attribute. Create a new setter for each element / prop.");
  }
  if (this.node)
    removeDisposingListener(this.node, this.dispose);
  this.node = el;
  if (this.node) {
    addDisposingListener(this.node, this.dispose);
    assignProp(this.node, this.prop ??= key, this.value);
  }
}
__name(propBindingInit, "propBindingInit");
function usePropBinding(initialValue) {
  const accessorThis = {
    value: initialValue
  };
  accessorThis.dispose = propBindingDispose.bind(accessorThis);
  const hook = propBinding.bind(accessorThis);
  hook[initPropHookSymbol] = propBindingInit.bind(accessorThis);
  return hook;
}
__name(usePropBinding, "usePropBinding");
function useText(initialValue) {
  const text2 = new Text();
  Object.defineProperty(text2, "toString", {
    value() {
      return this.textContent;
    }
  });
  function setText(value) {
    text2.textContent = value;
  }
  __name(setText, "setText");
  if (initialValue != null) {
    setText(initialValue);
  }
  return [text2, setText];
}
__name(useText, "useText");
var i = /* @__PURE__ */ Symbol.for("preact-signals");
function t() {
  if (!(s > 1)) {
    var i2, t2 = false;
    while (void 0 !== h) {
      var r2 = h;
      h = void 0;
      f++;
      while (void 0 !== r2) {
        var o2 = r2.o;
        r2.o = void 0;
        r2.f &= -3;
        if (!(8 & r2.f) && c(r2)) try {
          r2.c();
        } catch (r3) {
          if (!t2) {
            i2 = r3;
            t2 = true;
          }
        }
        r2 = o2;
      }
    }
    f = 0;
    s--;
    if (t2) throw i2;
  } else s--;
}
__name(t, "t");
function r(i2) {
  if (s > 0) return i2();
  s++;
  try {
    return i2();
  } finally {
    t();
  }
}
__name(r, "r");
var o = void 0;
function n(i2) {
  var t2 = o;
  o = void 0;
  try {
    return i2();
  } finally {
    o = t2;
  }
}
__name(n, "n");
var h = void 0;
var s = 0;
var f = 0;
var v = 0;
function e(i2) {
  if (void 0 !== o) {
    var t2 = i2.n;
    if (void 0 === t2 || t2.t !== o) {
      t2 = { i: 0, S: i2, p: o.s, n: void 0, t: o, e: void 0, x: void 0, r: t2 };
      if (void 0 !== o.s) o.s.n = t2;
      o.s = t2;
      i2.n = t2;
      if (32 & o.f) i2.S(t2);
      return t2;
    } else if (-1 === t2.i) {
      t2.i = 0;
      if (void 0 !== t2.n) {
        t2.n.p = t2.p;
        if (void 0 !== t2.p) t2.p.n = t2.n;
        t2.p = o.s;
        t2.n = void 0;
        o.s.n = t2;
        o.s = t2;
      }
      return t2;
    }
  }
}
__name(e, "e");
function u(i2, t2) {
  this.v = i2;
  this.i = 0;
  this.n = void 0;
  this.t = void 0;
  this.W = null == t2 ? void 0 : t2.watched;
  this.Z = null == t2 ? void 0 : t2.unwatched;
  this.name = null == t2 ? void 0 : t2.name;
}
__name(u, "u");
u.prototype.brand = i;
u.prototype.h = function() {
  return true;
};
u.prototype.S = function(i2) {
  var t2 = this, r2 = this.t;
  if (r2 !== i2 && void 0 === i2.e) {
    i2.x = r2;
    this.t = i2;
    if (void 0 !== r2) r2.e = i2;
    else n(function() {
      var i3;
      null == (i3 = t2.W) || i3.call(t2);
    });
  }
};
u.prototype.U = function(i2) {
  var t2 = this;
  if (void 0 !== this.t) {
    var r2 = i2.e, o2 = i2.x;
    if (void 0 !== r2) {
      r2.x = o2;
      i2.e = void 0;
    }
    if (void 0 !== o2) {
      o2.e = r2;
      i2.x = void 0;
    }
    if (i2 === this.t) {
      this.t = o2;
      if (void 0 === o2) n(function() {
        var i3;
        null == (i3 = t2.Z) || i3.call(t2);
      });
    }
  }
};
u.prototype.subscribe = function(i2) {
  var t2 = this;
  return E(function() {
    var r2 = t2.value, n2 = o;
    o = void 0;
    try {
      i2(r2);
    } finally {
      o = n2;
    }
  }, { name: "sub" });
};
u.prototype.valueOf = function() {
  return this.value;
};
u.prototype.toString = function() {
  return this.value + "";
};
u.prototype.toJSON = function() {
  return this.value;
};
u.prototype.peek = function() {
  var i2 = o;
  o = void 0;
  try {
    return this.value;
  } finally {
    o = i2;
  }
};
Object.defineProperty(u.prototype, "value", { get: /* @__PURE__ */ __name(function() {
  var i2 = e(this);
  if (void 0 !== i2) i2.i = this.i;
  return this.v;
}, "get"), set: /* @__PURE__ */ __name(function(i2) {
  if (i2 !== this.v) {
    if (f > 100) throw new Error("Cycle detected");
    this.v = i2;
    this.i++;
    v++;
    s++;
    try {
      for (var r2 = this.t; void 0 !== r2; r2 = r2.x) r2.t.N();
    } finally {
      t();
    }
  }
}, "set") });
function d(i2, t2) {
  return new u(i2, t2);
}
__name(d, "d");
function c(i2) {
  for (var t2 = i2.s; void 0 !== t2; t2 = t2.n) if (t2.S.i !== t2.i || !t2.S.h() || t2.S.i !== t2.i) return true;
  return false;
}
__name(c, "c");
function a(i2) {
  for (var t2 = i2.s; void 0 !== t2; t2 = t2.n) {
    var r2 = t2.S.n;
    if (void 0 !== r2) t2.r = r2;
    t2.S.n = t2;
    t2.i = -1;
    if (void 0 === t2.n) {
      i2.s = t2;
      break;
    }
  }
}
__name(a, "a");
function l(i2) {
  var t2 = i2.s, r2 = void 0;
  while (void 0 !== t2) {
    var o2 = t2.p;
    if (-1 === t2.i) {
      t2.S.U(t2);
      if (void 0 !== o2) o2.n = t2.n;
      if (void 0 !== t2.n) t2.n.p = o2;
    } else r2 = t2;
    t2.S.n = t2.r;
    if (void 0 !== t2.r) t2.r = void 0;
    t2 = o2;
  }
  i2.s = r2;
}
__name(l, "l");
function y(i2, t2) {
  u.call(this, void 0);
  this.x = i2;
  this.s = void 0;
  this.g = v - 1;
  this.f = 4;
  this.W = null == t2 ? void 0 : t2.watched;
  this.Z = null == t2 ? void 0 : t2.unwatched;
  this.name = null == t2 ? void 0 : t2.name;
}
__name(y, "y");
y.prototype = new u();
y.prototype.h = function() {
  this.f &= -3;
  if (1 & this.f) return false;
  if (32 == (36 & this.f)) return true;
  this.f &= -5;
  if (this.g === v) return true;
  this.g = v;
  this.f |= 1;
  if (this.i > 0 && !c(this)) {
    this.f &= -2;
    return true;
  }
  var i2 = o;
  try {
    a(this);
    o = this;
    var t2 = this.x();
    if (16 & this.f || this.v !== t2 || 0 === this.i) {
      this.v = t2;
      this.f &= -17;
      this.i++;
    }
  } catch (i3) {
    this.v = i3;
    this.f |= 16;
    this.i++;
  }
  o = i2;
  l(this);
  this.f &= -2;
  return true;
};
y.prototype.S = function(i2) {
  if (void 0 === this.t) {
    this.f |= 36;
    for (var t2 = this.s; void 0 !== t2; t2 = t2.n) t2.S.S(t2);
  }
  u.prototype.S.call(this, i2);
};
y.prototype.U = function(i2) {
  if (void 0 !== this.t) {
    u.prototype.U.call(this, i2);
    if (void 0 === this.t) {
      this.f &= -33;
      for (var t2 = this.s; void 0 !== t2; t2 = t2.n) t2.S.U(t2);
    }
  }
};
y.prototype.N = function() {
  if (!(2 & this.f)) {
    this.f |= 6;
    for (var i2 = this.t; void 0 !== i2; i2 = i2.x) i2.t.N();
  }
};
Object.defineProperty(y.prototype, "value", { get: /* @__PURE__ */ __name(function() {
  if (1 & this.f) throw new Error("Cycle detected");
  var i2 = e(this);
  this.h();
  if (void 0 !== i2) i2.i = this.i;
  if (16 & this.f) throw this.v;
  return this.v;
}, "get") });
function w(i2, t2) {
  return new y(i2, t2);
}
__name(w, "w");
function _(i2) {
  var r2 = i2.u;
  i2.u = void 0;
  if ("function" == typeof r2) {
    s++;
    var n2 = o;
    o = void 0;
    try {
      r2();
    } catch (t2) {
      i2.f &= -2;
      i2.f |= 8;
      b(i2);
      throw t2;
    } finally {
      o = n2;
      t();
    }
  }
}
__name(_, "_");
function b(i2) {
  for (var t2 = i2.s; void 0 !== t2; t2 = t2.n) t2.S.U(t2);
  i2.x = void 0;
  i2.s = void 0;
  _(i2);
}
__name(b, "b");
function g(i2) {
  if (o !== this) throw new Error("Out-of-order effect");
  l(this);
  o = i2;
  this.f &= -2;
  if (8 & this.f) b(this);
  t();
}
__name(g, "g");
function p(i2, t2) {
  this.x = i2;
  this.u = void 0;
  this.s = void 0;
  this.o = void 0;
  this.f = 32;
  this.name = null == t2 ? void 0 : t2.name;
}
__name(p, "p");
p.prototype.c = function() {
  var i2 = this.S();
  try {
    if (8 & this.f) return;
    if (void 0 === this.x) return;
    var t2 = this.x();
    if ("function" == typeof t2) this.u = t2;
  } finally {
    i2();
  }
};
p.prototype.S = function() {
  if (1 & this.f) throw new Error("Cycle detected");
  this.f |= 1;
  this.f &= -9;
  _(this);
  a(this);
  s++;
  var i2 = o;
  o = this;
  return g.bind(this, i2);
};
p.prototype.N = function() {
  if (!(2 & this.f)) {
    this.f |= 2;
    this.o = h;
    h = this;
  }
};
p.prototype.d = function() {
  this.f |= 8;
  if (!(1 & this.f)) b(this);
};
p.prototype.dispose = function() {
  this.d();
};
function E(i2, t2) {
  var r2 = new p(i2, t2);
  try {
    r2.c();
  } catch (i3) {
    r2.d();
    throw i3;
  }
  var o2 = r2.d.bind(r2);
  o2[Symbol.dispose] = o2;
  return o2;
}
__name(E, "E");
var signal = d;
var computed = w;
var effect = E;
var batch = r;
var untracked = n;
function useSignal(initialValue) {
  return signal(initialValue);
}
__name(useSignal, "useSignal");
function useUpdatableComputed() {
  const updater = signal(0);
  const factory = /* @__PURE__ */ __name((fn) => {
    return computed(() => {
      updater.value;
      return fn();
    });
  }, "factory");
  const update = /* @__PURE__ */ __name(() => {
    updater.value++;
  }, "update");
  return { computed: factory, update };
}
__name(useUpdatableComputed, "useUpdatableComputed");

// src/base/toastr2.tsx
var initialOptions = {
  closeButton: false,
  closeClass: "toast-close-button",
  closeOnHover: true,
  containerId: "toast-container",
  escapeHtml: true,
  extendedTimeOut: 1e3,
  iconClass: "toast-info",
  messageClass: "toast-message",
  newestOnTop: true,
  onclick: /* @__PURE__ */ __name(() => {
  }, "onclick"),
  onCloseClick: /* @__PURE__ */ __name(() => {
  }, "onCloseClick"),
  onHidden: /* @__PURE__ */ __name(() => {
  }, "onHidden"),
  onShown: /* @__PURE__ */ __name(() => {
  }, "onShown"),
  positionClass: "toast-top-right",
  preventDuplicates: false,
  rtl: false,
  tapToDismiss: true,
  target: "body",
  timeOut: 5e3,
  // Set timeOut to 0 to make it sticky
  titleClass: "toast-title",
  toastClass: "toast"
};
var initialInstance = null;
var Toastr = class {
  static {
    __name(this, "Toastr");
  }
  constructor(options) {
    this.toastId = 0;
    this.previousToast = null;
    this.options = Object.assign(Object.assign({}, initialInstance?.options ?? initialOptions), options);
  }
  getContainer(options, create = false) {
    let container = document.getElementById(options?.containerId ?? this.options.containerId);
    if (container || !create)
      return container;
    container = jsx("div", { id: this.options.containerId, class: options?.positionClass ?? this.options.positionClass });
    document.querySelector(options?.target ?? this.options.target)?.appendChild(container);
    return container;
  }
  error(message, title, opt) {
    return this.notify({
      type: "error",
      iconClass: "toast-error",
      message,
      title
    }, opt);
  }
  warning(message, title, opt) {
    return this.notify({
      type: "warning",
      iconClass: "toast-warning",
      message,
      title
    }, opt);
  }
  success(message, title, opt) {
    return this.notify({
      type: "success",
      iconClass: "toast-success",
      message,
      title
    }, opt);
  }
  info(message, title, opt) {
    return this.notify({
      type: "info",
      iconClass: "toast-info",
      message,
      title
    }, opt);
  }
  subscribe(callback) {
    this.listener = callback;
  }
  publish(args) {
    if (!this.listener) {
      return;
    }
    this.listener(args);
  }
  removeContainerIfEmpty(options) {
    let container = this.getContainer(options);
    if (!container)
      return;
    if (!container.hasChildNodes?.() && container.parentNode)
      container.parentNode.removeChild(container);
  }
  removeToast(toastElement, options) {
    if (!toastElement)
      return;
    if (toastElement !== document.activeElement) {
      toastElement.parentNode?.removeChild(toastElement);
      this.previousToast = null;
      this.removeContainerIfEmpty(options);
    }
  }
  clear(options) {
    let container = this.getContainer(options);
    if (!container)
      return;
    const toastsToClear = Array.from(container.childNodes);
    for (let i2 = toastsToClear.length - 1; i2 >= 0; i2 -= 1)
      this.removeToast(toastsToClear[i2], options);
    this.removeContainerIfEmpty();
  }
  notify(map, opt) {
    opt = Object.assign(Object.assign(Object.assign({}, this.options), map), opt);
    const shouldExit = /* @__PURE__ */ __name((opts, exitMap) => {
      if (opts.preventDuplicates) {
        if (exitMap.message === this.previousToast) {
          return true;
        }
        this.previousToast = exitMap.message || "";
      }
      return false;
    }, "shouldExit");
    if (shouldExit(opt, map)) {
      return null;
    }
    this.toastId += 1;
    const container = this.getContainer(opt, true);
    let intervalId = null;
    const toastEl = jsx("div", {});
    const titleEl = jsx("div", {});
    const messageEl = jsx("div", {});
    const closeEl = !opt.closeButton ? null : opt.closeButton instanceof HTMLElement ? opt.closeButton : jsx("button", { type: "button", children: "\xD7" });
    const response = {
      toastId: this.toastId,
      state: "visible",
      startTime: /* @__PURE__ */ new Date(),
      endTime: void 0,
      opt,
      map
    };
    const hideToast = /* @__PURE__ */ __name((override = null) => {
      if (toastEl === document.activeElement && !override) {
        return;
      }
      this.removeToast(toastEl);
      if (intervalId) {
        clearTimeout(intervalId);
      }
      if (opt.onHidden && response.state !== "hidden") {
        opt.onHidden();
      }
      response.state = "hidden";
      response.endTime = /* @__PURE__ */ new Date();
      this.publish(response);
    }, "hideToast");
    const setAria = /* @__PURE__ */ __name(() => {
      let ariaValue = "";
      switch (opt.iconClass) {
        case "toast-success":
        case "toast-info":
          ariaValue = "polite";
          break;
        default:
          ariaValue = "assertive";
      }
      toastEl.setAttribute("aria-live", ariaValue);
    }, "setAria");
    const handleEvents = /* @__PURE__ */ __name(() => {
      if (opt.closeOnHover) {
        toastEl.addEventListener("mouseover", () => {
          if (intervalId) {
            clearTimeout(intervalId);
          }
        });
        toastEl.addEventListener("mouseout", () => {
          if (opt.timeOut >= 0 && (opt.timeOut > 0 || opt.extendedTimeOut > 0)) {
            intervalId = setTimeout(hideToast, opt.extendedTimeOut);
          }
        });
      }
      if (!opt.onclick && opt.tapToDismiss) {
        toastEl.addEventListener("click", hideToast);
      }
      if (closeEl) {
        closeEl.addEventListener("click", (event) => {
          event.stopPropagation();
          if (opt.onCloseClick) {
            opt.onCloseClick(event);
          }
          hideToast(true);
        });
      }
      if (opt.onclick) {
        toastEl.addEventListener("click", (event) => {
          if (opt.onclick) {
            opt.onclick(event);
          }
          if (opt.tapToDismiss)
            hideToast();
        });
      }
    }, "handleEvents");
    const setTitle = /* @__PURE__ */ __name(() => {
      if (map.title) {
        titleEl.append(map.title);
        addClass(titleEl, opt.titleClass);
        toastEl.appendChild(titleEl);
      }
    }, "setTitle");
    const setMessage = /* @__PURE__ */ __name(() => {
      if (map.message) {
        if (typeof map.message === "string" && !(opt.escapeHtml ?? true)) {
          messageEl.innerHTML = sanitizeHtml(map.message);
        } else {
          messageEl.append(map.message);
        }
        if (opt.preWrap) {
          messageEl.style.whiteSpace = "pre-wrap";
        }
        addClass(messageEl, opt.messageClass);
        toastEl.appendChild(messageEl);
      }
    }, "setMessage");
    const setCloseButton = /* @__PURE__ */ __name(() => {
      if (closeEl) {
        addClass(closeEl, opt.closeClass);
        closeEl.setAttribute("role", "button");
        toastEl.insertBefore(closeEl, toastEl.firstChild);
      }
    }, "setCloseButton");
    const setSequence = /* @__PURE__ */ __name(() => {
      if (opt.newestOnTop) {
        container.insertBefore(toastEl, container.firstChild);
      } else {
        container.appendChild(toastEl);
      }
    }, "setSequence");
    const displayToast = /* @__PURE__ */ __name(() => {
      if (opt.onShown) {
        opt.onShown();
      }
      if (opt.timeOut > 0) {
        intervalId = setTimeout(hideToast, opt.timeOut);
      }
    }, "displayToast");
    const personalizeToast = /* @__PURE__ */ __name(() => {
      toastEl.classList.add("show");
      opt.rtl && toastEl.classList.add("rtl");
      opt.toastClass && addClass(toastEl, opt.toastClass);
      opt.iconClass && addClass(toastEl, opt.iconClass);
      setTitle();
      setMessage();
      setCloseButton();
      setSequence();
      setAria();
    }, "personalizeToast");
    personalizeToast();
    displayToast();
    handleEvents();
    this.publish(response);
    return toastEl;
  }
};
initialInstance = new Toastr();
var toastr2_default = initialInstance;

// src/base/notify.ts
var defaultNotifyOptions = {
  timeOut: 5e3,
  escapeHtml: true,
  positionClass: "position-toast toast-top-full-width"
};
function positionToastContainer(options, create = true) {
  let container = toastr2_default.getContainer(options, create);
  if (!container || !container.classList.contains("position-toast") || typeof document === "undefined" || !document.body)
    return;
  let dialogs = Array.from(document.body.children);
  let dialogIndex = dialogs.findIndex((x) => x.matches(".ui-dialog, .modal.in, .modal.show") && !x.matches('[hidden], [style*="display:none"], [style*="display: none"]') && !x.classList.contains("hidden"));
  let dialog = dialogs[dialogIndex];
  if (dialog) {
    const { top, left, right } = dialog.getBoundingClientRect();
    container.classList.add("positioned-toast");
    container.style.position = "absolute";
    container.style.top = top + 28 + "px";
    container.style.left = left + 6 + "px";
    container.style.width = Math.max(right - left - 12, 150) + "px";
  } else if (container.classList.contains("positioned-toast")) {
    container.classList.remove("positioned-toast");
    container.style.position = "";
    container.style.top = "";
    container.style.left = "";
    container.style.width = "";
  }
}
__name(positionToastContainer, "positionToastContainer");
function getToastrOptions(options) {
  options = Object.assign(Object.assign({}, defaultNotifyOptions), options);
  positionToastContainer(options);
  return options;
}
__name(getToastrOptions, "getToastrOptions");
function showToast(type, message, title, options) {
  return toastr2_default[type](message, title, getToastrOptions(options));
}
__name(showToast, "showToast");
function notifyError(message, title, options) {
  showToast("error", message, title, options);
}
__name(notifyError, "notifyError");
function notifyInfo(message, title, options) {
  showToast("info", message, title, options);
}
__name(notifyInfo, "notifyInfo");
function notifySuccess(message, title, options) {
  showToast("success", message, title, options);
}
__name(notifySuccess, "notifySuccess");
function notifyWarning(message, title, options) {
  showToast("warning", message, title, options);
}
__name(notifyWarning, "notifyWarning");

// src/base/blockui.ts
var blockUICount = 0;
function blockUI(options) {
  function block() {
    if (blockUICount++ > 0 || typeof document === "undefined")
      return;
    const div = document.createElement("div");
    div.className = "blockUI blockOverlay";
    Object.assign(div.style, {
      zIndex: (options?.zIndex ?? 2e3).toString(),
      border: "none",
      margin: "0px",
      padding: "0px",
      width: "100%",
      height: "100%",
      top: "0px",
      left: "0px",
      opacity: "0",
      cursor: "wait",
      position: "fixed"
    });
    document.body.appendChild(div);
  }
  __name(block, "block");
  options?.useTimeout && window.setTimeout(block, 0) || block();
}
__name(blockUI, "blockUI");
function blockUndo() {
  if (blockUICount < 1)
    return;
  if (--blockUICount === 0 && typeof document !== "undefined")
    document.body.querySelector(":scope > .blockUI.blockOverlay")?.remove();
}
__name(blockUndo, "blockUndo");

// src/base/lookup.ts
var Lookup = class {
  static {
    __name(this, "Lookup");
  }
  constructor(options, items) {
    this.items = [];
    this.itemById = {};
    options = options || {};
    this.idField = options.idField;
    this.parentIdField = options.parentIdField;
    this.textField = options.textField;
    if (items != null)
      this.update(items);
  }
  update(value) {
    this.items = [];
    this.itemById = {};
    if (value) {
      for (const k of value) {
        if (k == null || typeof k !== "object")
          this.items.push({ [this.idField]: k, [this.textField]: k });
        else
          this.items.push(k);
      }
    }
    const idField = this.idField;
    if (idField) {
      for (const r2 of this.items) {
        const v2 = r2[idField];
        if (v2 != null) {
          this.itemById[v2] = r2;
        }
      }
    }
  }
};

// src/base/environment.ts
function getjQuery() {
  return typeof jQuery === "function" ? jQuery : typeof $ === "function" && $.fn ? $ : void 0;
}
__name(getjQuery, "getjQuery");
function isBS3() {
  return (getjQuery()?.fn?.modal?.Constructor?.VERSION + "").charAt(0) == "3";
}
__name(isBS3, "isBS3");
function isBS5Plus() {
  return typeof bootstrap !== "undefined" && !!bootstrap.Modal && (bootstrap.Modal.VERSION + "").charAt(0) != "4";
}
__name(isBS5Plus, "isBS5Plus");

// src/base/fluent-events.ts
var stripNameRegex = /\..*/;
var stripUidRegex = /::\d+$/;
var uidEvent = 1;
var customEvents = {
  mouseenter: "mouseover",
  mouseleave: "mouseout"
};
function makeEventUid(prefix) {
  return `${prefix}::${uidEvent++}`;
}
__name(makeEventUid, "makeEventUid");
var eventRegistrySymbol = /* @__PURE__ */ Symbol.for("Serenity.eventRegistry");
function getEventRegistry() {
  return globalThis[eventRegistrySymbol] ||= /* @__PURE__ */ new WeakMap();
}
__name(getEventRegistry, "getEventRegistry");
function notifyDisposingNode(node, opt) {
  if (!node)
    return;
  invokeDisposingListeners(node, opt);
  const eventRegistry = getEventRegistry();
  function invokeFor(el) {
    const events = eventRegistry.get(el);
    if (!events)
      return;
    eventRegistry.delete(el);
    const disposeHandlers = events["disposing"];
    if (disposeHandlers) {
      for (const [_2, handler] of Object.entries(disposeHandlers)) {
        if (typeof handler.callable === "function") {
          try {
            handler.callable.call(el, { target: el });
          } catch {
          }
        }
      }
    }
    for (const [typeEvent, handlers] of Object.entries(events)) {
      for (const [handlerKey, handler] of Object.entries(handlers)) {
        el.removeEventListener(typeEvent, handler, Boolean(handler.delegationSelector));
        delete handlers[handlerKey];
      }
    }
  }
  __name(invokeFor, "invokeFor");
  if (opt?.descendants && node instanceof Element && node.hasChildNodes()) {
    const iterator = document.createNodeIterator(
      node,
      NodeFilter.SHOW_ELEMENT
    );
    let currentNode;
    const descendants = [];
    while (currentNode = iterator.nextNode()) {
      if (currentNode !== node) {
        descendants.push(currentNode);
      }
    }
    for (let i2 = 0; i2 < descendants.length; i2++) {
      invokeFor(descendants[i2]);
    }
  }
  if (!opt?.excludeSelf) {
    invokeFor(node);
  }
}
__name(notifyDisposingNode, "notifyDisposingNode");
function getElementEvents(element) {
  const eventRegistry = getEventRegistry();
  let events = eventRegistry.get(element);
  if (!events)
    eventRegistry.set(element, events = {});
  return events;
}
__name(getElementEvents, "getElementEvents");
function hydrateEvent(obj, meta = {}) {
  for (const [key, value] of Object.entries(meta)) {
    if (key === "bubbles" || key === "cancelable") {
      continue;
    }
    try {
      obj[key] = value;
    } catch {
      Object.defineProperty(obj, key, {
        configurable: true,
        get() {
          return value;
        }
      });
    }
  }
  return obj;
}
__name(hydrateEvent, "hydrateEvent");
function baseHandler(element, fn) {
  return /* @__PURE__ */ __name(function handler(event) {
    hydrateEvent(event, { delegateTarget: element });
    if (handler.oneOff) {
      removeListener(element, event.type, fn);
    }
    return fn.apply(element, [event]);
  }, "handler");
}
__name(baseHandler, "baseHandler");
function delegationHandler(element, selector, fn) {
  return /* @__PURE__ */ __name(function handler(event) {
    const domElements = element.querySelectorAll(selector);
    for (let { target } = event; target && target !== this; target = target.parentNode) {
      for (const domElement of domElements) {
        if (domElement !== target) {
          continue;
        }
        hydrateEvent(event, { delegateTarget: target });
        if (handler.oneOff) {
          removeListener(element, event.type, selector, fn);
        }
        return fn.apply(target, [event]);
      }
    }
  }, "handler");
}
__name(delegationHandler, "delegationHandler");
function findHandler(handlers, callable, delegationSelector = null) {
  return Object.values(handlers).find((event) => event.callable === callable && event.delegationSelector === delegationSelector);
}
__name(findHandler, "findHandler");
function normalizeParameters(originalTypeEvent, handler, delegationFunction) {
  const isDelegated = typeof handler === "string";
  const callable = isDelegated ? delegationFunction : handler || delegationFunction;
  let typeEvent = getTypeEvent(originalTypeEvent);
  if (originalTypeEvent.indexOf(".bs.") >= 0)
    typeEvent = originalTypeEvent;
  return [isDelegated, callable, typeEvent];
}
__name(normalizeParameters, "normalizeParameters");
function addListener(element, originalTypeEvent, handler, delegationFunction, oneOff) {
  if (typeof originalTypeEvent !== "string" || !element) {
    return;
  }
  if (originalTypeEvent === "disposing" && !delegationFunction && typeof handler === "function") {
    addDisposingListener(element, handler);
    return;
  }
  if (originalTypeEvent.startsWith("disposing.") && !delegationFunction && typeof handler === "function") {
    addDisposingListener(element, handler, originalTypeEvent.substring(9));
    return;
  }
  const $2 = getjQuery();
  if ($2) {
    if (typeof element === "string")
      return;
    let $element = $2(element);
    if (oneOff)
      $element.one(originalTypeEvent, handler, delegationFunction);
    else
      $element.on(originalTypeEvent, handler, delegationFunction);
    return;
  }
  let [isDelegated, callable, typeEvent] = normalizeParameters(originalTypeEvent, handler, delegationFunction);
  if (!callable)
    return;
  if (originalTypeEvent in customEvents) {
    const wrapFunction = /* @__PURE__ */ __name((fn2) => {
      return function(event) {
        if (!event.relatedTarget || event.relatedTarget !== event.delegateTarget && !event.delegateTarget.contains(event.relatedTarget)) {
          return fn2.call(this, event);
        }
      };
    }, "wrapFunction");
    callable = wrapFunction(callable);
  }
  const events = getElementEvents(element);
  if (isPollutingKey(typeEvent))
    return;
  const handlers = events[typeEvent] || (events[typeEvent] = /* @__PURE__ */ Object.create(null));
  const previousFunction = findHandler(handlers, callable, isDelegated ? handler : null);
  if (previousFunction) {
    previousFunction.oneOff = previousFunction.oneOff && oneOff;
    return;
  }
  const dotIdx = originalTypeEvent.indexOf(".");
  const ns = dotIdx > -1 ? originalTypeEvent.substring(dotIdx + 1) : "";
  const uid = makeEventUid(ns);
  if (isPollutingKey(uid))
    return;
  const fn = isDelegated ? delegationHandler(element, handler, callable) : baseHandler(element, callable);
  fn.delegationSelector = isDelegated ? handler : null;
  fn.callable = callable;
  fn.oneOff = oneOff;
  fn.uidEvent = uid;
  handlers[uid] = fn;
  element.addEventListener(typeEvent, fn, isDelegated);
}
__name(addListener, "addListener");
function isPollutingKey(key) {
  return key === "__proto__" || key === "constructor" || key === "prototype";
}
__name(isPollutingKey, "isPollutingKey");
function removeHandler(element, events, typeEvent, handler, delegationSelector) {
  const fn = findHandler(events[typeEvent], handler, delegationSelector);
  if (!fn) {
    return;
  }
  element.removeEventListener(typeEvent, fn, Boolean(delegationSelector));
  if (!isPollutingKey(typeEvent) && !isPollutingKey(fn.uidEvent))
    delete events[typeEvent][fn.uidEvent];
}
__name(removeHandler, "removeHandler");
function removeNamespacedHandlers(element, events, typeEvent, namespace) {
  const handlers = events[typeEvent] || {};
  for (const [handlerKey, handler] of Object.entries(handlers)) {
    if (handlerKey.includes(namespace)) {
      removeHandler(element, events, typeEvent, handler.callable, handler.delegationSelector);
    }
  }
}
__name(removeNamespacedHandlers, "removeNamespacedHandlers");
function getTypeEvent(event) {
  event = event.replace(stripNameRegex, "");
  return customEvents[event] || event;
}
__name(getTypeEvent, "getTypeEvent");
function removeListener(element, originalTypeEvent, handler, delegationHandler2) {
  if (typeof originalTypeEvent !== "string" || !element) {
    return;
  }
  if (originalTypeEvent === "disposing" && !delegationHandler2 && typeof handler === "function") {
    removeDisposingListener(element, handler);
    return;
  }
  if (originalTypeEvent.startsWith("disposing.") && !delegationHandler2 && typeof handler === "function") {
    removeDisposingListener(element, handler, originalTypeEvent.substring(9));
    return;
  }
  if (originalTypeEvent.startsWith(".")) {
    removeDisposingListener(element, null, originalTypeEvent.substring(1));
  }
  const $2 = getjQuery();
  if ($2) {
    if (typeof element !== "string") {
      $2(element).off(originalTypeEvent, handler, delegationHandler2);
    }
    return;
  }
  const [isDelegated, callable, typeEvent] = normalizeParameters(originalTypeEvent, handler, delegationHandler2);
  const inNamespace = typeEvent !== originalTypeEvent;
  const events = getElementEvents(element);
  const handlers = events[typeEvent] || {};
  const isNamespace = originalTypeEvent.startsWith(".");
  if (typeof callable !== "undefined") {
    if (!Object.keys(handlers).length) {
      return;
    }
    removeHandler(element, events, typeEvent, callable, isDelegated ? handler : null);
    return;
  }
  if (isNamespace) {
    for (const elementEvent of Object.keys(events)) {
      removeNamespacedHandlers(element, events, elementEvent, originalTypeEvent.slice(1));
    }
  }
  for (const [keyHandlers, handler2] of Object.entries(handlers)) {
    const handlerKey = keyHandlers.replace(stripUidRegex, "");
    if (!inNamespace || originalTypeEvent.includes(handlerKey)) {
      removeHandler(element, events, typeEvent, handler2.callable, handler2.delegationSelector);
    }
  }
}
__name(removeListener, "removeListener");
function triggerEvent(element, type, args) {
  if (typeof type !== "string" || !element) {
    return null;
  }
  const $2 = getjQuery();
  const typeEvent = getTypeEvent(type);
  const inNamespace = type !== typeEvent;
  let jQueryEvent = null;
  let bubbles = args?.bubbles ?? true;
  let nativeDispatch = true;
  let defaultPrevented = false;
  if (inNamespace && $2) {
    jQueryEvent = $2.Event(type, args);
    typeof element !== "string" && $2(element).trigger(jQueryEvent);
    bubbles = bubbles && !jQueryEvent.isPropagationStopped();
    nativeDispatch = !jQueryEvent.isImmediatePropagationStopped();
    defaultPrevented = jQueryEvent.isDefaultPrevented();
  }
  const evt = hydrateEvent(new Event(type, { bubbles, cancelable: args?.cancelable ?? true }), args);
  if (defaultPrevented) {
    evt.preventDefault();
  }
  if (nativeDispatch) {
    element.dispatchEvent(evt);
  }
  if (evt.defaultPrevented && jQueryEvent) {
    jQueryEvent.preventDefault();
  }
  return evt;
}
__name(triggerEvent, "triggerEvent");

// src/base/fluent.ts
var validTagRegex = /^[a-zA-Z][a-zA-Z0-9\-]*$/;
function Fluent(tagOrElement) {
  if (!(this instanceof Fluent)) {
    if (typeof tagOrElement === "string") {
      if (validTagRegex.test(tagOrElement))
        return new Fluent(document.createElement(tagOrElement));
      return new Fluent(null);
    }
    return new Fluent(tagOrElement);
  }
  this.el = tagOrElement;
  return this;
}
__name(Fluent, "Fluent");
((Fluent2) => {
  function on(element, type, handler, delegationHandler2) {
    addListener(
      element,
      type,
      handler,
      delegationHandler2,
      /*oneOff*/
      false
    );
  }
  Fluent2.on = on;
  __name(on, "on");
  function one(element, type, handler, delegationHandler2) {
    addListener(element, type, handler, delegationHandler2, true);
  }
  Fluent2.one = one;
  __name(one, "one");
  function off(element, originalTypeEvent, handler, delegationHandler2) {
    return removeListener(element, originalTypeEvent, handler, delegationHandler2);
  }
  Fluent2.off = off;
  __name(off, "off");
  function trigger(element, type, args) {
    return triggerEvent(element, type, args);
  }
  Fluent2.trigger = trigger;
  __name(trigger, "trigger");
  function addClass2(element, value) {
    toggleClass(element, toClassName(value), true);
  }
  Fluent2.addClass = addClass2;
  __name(addClass2, "addClass");
  function empty(element) {
    if (!element)
      return;
    if (typeof element.hasChildNodes === "function" && element.hasChildNodes()) {
      let $2 = getjQuery();
      if ($2)
        $2(element).empty();
      else {
        notifyDisposingNode(element, { descendants: true, excludeSelf: true });
        element.innerHTML = "";
      }
    } else
      element.innerHTML = "";
  }
  Fluent2.empty = empty;
  __name(empty, "empty");
  function isVisibleLike(element) {
    return !!(element && (element.offsetWidth || element.offsetHeight || element.getClientRects().length));
  }
  Fluent2.isVisibleLike = isVisibleLike;
  __name(isVisibleLike, "isVisibleLike");
  function remove(element) {
    if (!element)
      return;
    let $2 = getjQuery();
    if ($2) {
      $2(element).remove();
    } else {
      notifyDisposingNode(element, { descendants: true });
      element.remove();
    }
  }
  Fluent2.remove = remove;
  __name(remove, "remove");
  function removeClass2(element, value) {
    toggleClass(element, toClassName(value), false);
  }
  Fluent2.removeClass = removeClass2;
  __name(removeClass2, "removeClass");
  function toggle(element, flag) {
    if (element) {
      let hidden = !(flag != null ? flag : "hidden" in element && !!element.hidden || element.classList.contains("hidden") || element.style?.display === "none");
      if ("hidden" in element)
        element.hidden = hidden;
      if (!hidden) {
        if ("style" in element && element.style?.display === "none") {
          element.style.display = "";
        }
        if (element.classList.contains("hidden"))
          element.classList.remove("hidden");
      }
    }
  }
  Fluent2.toggle = toggle;
  __name(toggle, "toggle");
  function toggleClass2(element, value, add) {
    element && toggleClass(element, toClassName(value), add);
  }
  Fluent2.toggleClass = toggleClass2;
  __name(toggleClass2, "toggleClass");
  function toClassName(value) {
    if (typeof value === "string")
      return value;
    if (Array.isArray(value))
      return value.map(toClassName).filter(Boolean).join(" ");
    if (typeof value !== "boolean" && value != null)
      return "" + value;
    return "";
  }
  Fluent2.toClassName = toClassName;
  __name(toClassName, "toClassName");
  function isInputLike(element) {
    return isInputTag(element?.nodeName);
  }
  Fluent2.isInputLike = isInputLike;
  __name(isInputLike, "isInputLike");
  Fluent2.inputLikeSelector = "input,select,textarea,button";
  function isInputTag(tag) {
    return /^(?:input|select|textarea|button)$/i.test(tag);
  }
  Fluent2.isInputTag = isInputTag;
  __name(isInputTag, "isInputTag");
  function isDefaultPrevented(event) {
    return event != null && (!!event.defaultPrevented || typeof event.isDefaultPrevented === "function" && !!event.isDefaultPrevented());
  }
  Fluent2.isDefaultPrevented = isDefaultPrevented;
  __name(isDefaultPrevented, "isDefaultPrevented");
  function eventProp(event, prop) {
    if (!event)
      return void 0;
    if (prop in event) {
      return event[prop];
    }
    if (typeof event.nativeEvent === "object" && prop in event.nativeEvent)
      return event.nativeEvent[prop];
    const originalEvent = event.originalEvent ?? event.nativeEvent?.originalEvent;
    if (typeof originalEvent === "object" && prop in originalEvent)
      return originalEvent[prop];
    if (typeof event.detail === "object" && prop in event.detail)
      return event.detail[prop];
  }
  Fluent2.eventProp = eventProp;
  __name(eventProp, "eventProp");
})(Fluent || (Fluent = {}));
function extractNode(element) {
  return element instanceof Node || typeof element === "string" ? element : element?.el;
}
__name(extractNode, "extractNode");
function extractElement(element) {
  return element instanceof Node || typeof element === "string" ? element : element?.el;
}
__name(extractElement, "extractElement");
Fluent.prototype.addClass = function(value) {
  Fluent.addClass(this.el, value);
  return this;
};
Fluent.prototype.after = function(content) {
  if (this.el) {
    const node = extractNode(content);
    if (node instanceof Element)
      this.el.insertAdjacentElement("afterend", node);
    else if (node instanceof DocumentFragment) {
      Fluent(node).insertAfter(this.el);
    } else if (node != null) {
      this.el.insertAdjacentText("afterend", "" + node);
    }
  }
  return this;
};
Fluent.prototype.append = function(child) {
  this.el && this.el.append(extractNode(child));
  return this;
};
Fluent.prototype.appendTo = function(parent2) {
  if (this.el) {
    parent2 = extractElement(parent2);
    if (!parent2)
      this.el.remove();
    else
      parent2.appendChild(this.el);
  }
  return this;
};
Fluent.prototype.attr = function(name, value) {
  if (value === void 0 && arguments.length < 2)
    return this.el?.getAttribute(name);
  if (this.el) {
    if (value == null || value === false)
      this.el.removeAttribute(name);
    else if (typeof value === "string")
      this.el.setAttribute(name, value);
    else if (typeof value === "number")
      this.el.setAttribute(name, "" + value);
    else
      this.el.setAttribute(name, "true");
  }
  return this;
};
Fluent.prototype.before = function(content) {
  if (this.el) {
    const node = extractNode(content);
    if (node instanceof Element)
      this.el.insertAdjacentElement("beforebegin", node);
    else if (node instanceof DocumentFragment) {
      Fluent(node).insertBefore(this.el);
    } else if (node != null) {
      this.el.insertAdjacentText("beforebegin", "" + node);
    }
  }
  return this;
};
Fluent.prototype.children = function(selector) {
  if (selector == null)
    return Array.from(this.el?.children || []);
  return Array.from(this.el?.children || []).filter((x) => x.matches(selector));
};
Fluent.prototype.class = function(value) {
  this.el && (this.el.className = Fluent.toClassName(value));
  return this;
};
Fluent.prototype.click = function(listener) {
  if (listener === void 0 && !arguments.length) {
    this.el && typeof this.el.click === "function" && this.el.click();
    return this;
  } else {
    return this.on("click", listener);
  }
};
Fluent.prototype.closest = function(selector) {
  return new Fluent(this.el?.closest(selector));
};
Fluent.prototype.each = function(callback) {
  this.el && callback(this.el);
  return this;
};
Fluent.prototype.empty = function() {
  Fluent.empty(this.el);
  return this;
};
Fluent.prototype.focus = function() {
  this.el && typeof this.el.focus === "function" && this.el.focus();
  return this;
};
Fluent.prototype.getNode = function() {
  return this.el;
};
Fluent.prototype.hasClass = function(klass) {
  return !!this.el?.classList.contains(klass);
};
Fluent.prototype.insertAfter = function(referenceNode) {
  if (!this.el)
    return this;
  referenceNode = extractElement(referenceNode);
  let parent2 = referenceNode?.parentNode;
  if (!parent2)
    this.el.remove();
  else
    parent2.insertBefore(this.el, referenceNode.nextSibling);
  return this;
};
Fluent.prototype.isVisibleLike = function() {
  return Fluent.isVisibleLike(this.el);
};
Fluent.prototype.insertBefore = function(referenceNode) {
  if (!this.el)
    return this;
  referenceNode = extractElement(referenceNode);
  let parent2 = referenceNode?.parentElement;
  if (!parent2)
    this.el.remove();
  else
    parent2.insertBefore(this.el, referenceNode);
  return this;
};
Fluent.prototype.prependTo = function(parent2) {
  if (this.el) {
    parent2 = extractElement(parent2);
    if (!parent2)
      this.el.remove();
    else
      parent2.prepend(this.el);
  }
  return this;
};
Fluent.prototype.removeAttr = function(name) {
  this.el?.removeAttribute(name);
  return this;
};
Fluent.prototype.data = function(name, value) {
  if (value === void 0 && arguments.length < 2)
    return this.attr("data-" + name);
  return this.attr("data-" + name, value);
};
Fluent.prototype.off = function(type, handler, delegationHandler2) {
  this.el && removeListener(this.el, type, handler, delegationHandler2);
  return this;
};
Fluent.prototype.on = function(type, handler, delegationHandler2) {
  this.el && addListener(this.el, type, handler, delegationHandler2, false);
  return this;
};
Fluent.prototype.one = function(type, handler, delegationHandler2) {
  this.el && addListener(this.el, type, handler, delegationHandler2, true);
  return this;
};
Fluent.prototype.removeClass = function(value) {
  Fluent.removeClass(this.el, value);
  return this;
};
Fluent.prototype.findFirst = function(selector) {
  return new Fluent(this.el?.querySelector(selector));
};
Fluent.prototype.findAll = function(selector) {
  if (!this.el)
    return [];
  return Array.from(this.el.querySelectorAll(selector));
};
Fluent.prototype.findEach = function(selector, callback) {
  if (!this.el || !callback)
    return this;
  this.el.querySelectorAll(selector).forEach((x, i2) => callback(Fluent(x), i2));
  return this;
};
Fluent.prototype.hide = function() {
  return this.toggle(false);
};
Fluent.prototype.hidden = function(value) {
  if (value === void 0 && arguments.length < 1)
    return this.el?.hidden;
  if (this.el)
    this.el.hidden = !!value;
  return this;
};
Fluent.prototype.matches = function(selector) {
  return !!this.el && typeof this.el.matches === "function" && this.el.matches(selector);
};
Fluent.prototype.nextSibling = function(selector) {
  let sibling = this.el?.nextElementSibling;
  while (sibling && selector != null && !sibling.matches(selector))
    sibling = sibling.nextElementSibling;
  return new Fluent(sibling);
};
Fluent.prototype.parent = function() {
  return new Fluent(this.el?.parentNode);
};
Fluent.prototype.prepend = function(child) {
  this.el && this.el.prepend(extractNode(child));
  return this;
};
Fluent.prototype.prevSibling = function(selector) {
  let sibling = this.el?.previousElementSibling;
  while (sibling && selector != null && !sibling.matches(selector))
    sibling = sibling.previousElementSibling;
  return new Fluent(sibling);
};
Fluent.prototype.remove = function() {
  Fluent.remove(this.el);
  return this;
};
Fluent.prototype.show = function() {
  return this.toggle(true);
};
Fluent.prototype.style = function(callback) {
  if (this.el && this.el.style instanceof CSSStyleDeclaration)
    callback(this.el.style);
  return this;
};
Fluent.prototype.trigger = function(type, args) {
  this.el && triggerEvent(this.el, type, args);
  return this;
};
Fluent.prototype.val = function(value) {
  if (value === void 0 && !arguments.length) {
    if (!this.el)
      return void 0;
    return Fluent.isInputLike(this.el) ? this.el.value : "";
  }
  if (this.el && Fluent.isInputLike(this.el))
    this.el.value = value ?? "";
  return this;
};
Fluent.prototype.text = function(value) {
  if (value === void 0 && !arguments.length)
    return this.el?.textContent;
  if (!this.el)
    return this;
  if (typeof this.el.hasChildNodes === "function" && this.el.hasChildNodes()) {
    let $2 = getjQuery();
    if ($2)
      $2(this.el).text(value ?? "");
    else {
      notifyDisposingNode(this.el, { descendants: true, excludeSelf: true });
      this.el.textContent = value ?? "";
    }
  } else
    this.el.textContent = value ?? "";
  return this;
};
Fluent.prototype.toggle = function(flag) {
  Fluent.toggle(this.el, flag);
  return this;
};
Fluent.prototype.toggleClass = function(value, add) {
  Fluent.toggleClass(this.el, value, add);
  return this;
};
Object.defineProperty(Fluent.prototype, "length", { get: /* @__PURE__ */ __name(function() {
  return this.el ? 1 : 0;
}, "get") });
Object.defineProperty(Fluent.prototype, 0, { get: /* @__PURE__ */ __name(function() {
  return this.el;
}, "get") });
Object.defineProperty(Fluent.prototype, Symbol.iterator, { get: /* @__PURE__ */ __name(function() {
  return (this.el ? [this.el] : [])[Symbol.iterator];
}, "get") });
Fluent.ready = function(callback) {
  if (!callback)
    return;
  let $2 = getjQuery();
  if ($2) {
    $2(callback);
    return;
  }
  if (typeof document !== "undefined" && document.readyState === "loading") {
    const loaded = /* @__PURE__ */ __name(() => {
      document.removeEventListener("DOMContentLoaded", loaded);
      callback();
    }, "loaded");
    document.addEventListener("DOMContentLoaded", loaded);
    return;
  }
  setTimeout(callback, 0);
};
Fluent.byId = function(id) {
  return Fluent(document.getElementById(id));
};
Fluent.findAll = function(selector) {
  return Array.from(document.querySelectorAll(selector));
};
Fluent.findEach = function(selector, callback) {
  if (!callback)
    return;
  document.querySelectorAll(selector).forEach((x) => callback(Fluent(x)));
};
Fluent.findFirst = function(selector) {
  return Fluent(document.querySelector(selector));
};

// src/base/icons.ts
function bgColor(color) {
  return "bg-" + color;
}
__name(bgColor, "bgColor");
function textColor(color) {
  return "text-" + color;
}
__name(textColor, "textColor");
function faIcon(key, color) {
  return "fa fa-" + key + (color ? " " + textColor(color) : "");
}
__name(faIcon, "faIcon");
function fabIcon(key, color) {
  return "fab fa-" + key + (color ? " " + textColor(color) : "");
}
__name(fabIcon, "fabIcon");
function iconClassName(icon) {
  let klass = Array.isArray(icon) ? icon.join(" ") : icon;
  if (!klass)
    return klass;
  if (klass.startsWith("fa-") && klass.indexOf("fa ") < 0 && !klass.endsWith(" fa"))
    return "fa " + klass;
  return klass;
}
__name(iconClassName, "iconClassName");

// src/base/dialogs.tsx
var Dialog = class _Dialog {
  static {
    __name(this, "Dialog");
  }
  constructor(opt, create = true) {
    if (isArrayLike(opt?.element))
      this.el = opt.element[0];
    else if (typeof opt?.element !== "function")
      this.el = opt?.element;
    this.dialogResult = this.el?.dataset.dialogResult;
    if (!create) {
      return;
    }
    this.el ??= document.createElement("div");
    opt = Object.assign({}, _Dialog.defaults, omitUndefined(opt));
    if (opt.closeOnEscape === void 0 && opt.closeButton)
      opt.closeOnEscape = true;
    if (typeof opt.element === "function")
      opt.element(this.el);
    if (this.el.hidden && typeof opt.element !== "function")
      this.el.hidden = false;
    if (opt.preferPanel || !hasBSModal() && !hasUIDialog())
      this.createPanel(opt);
    else if (hasUIDialog() && (!hasBSModal() || !opt.preferBSModal))
      this.createUIDialog(opt);
    else {
      this.createBSModal(opt);
    }
    if (opt.onOpen)
      this.onOpen(opt.onOpen);
    if (opt.onClose)
      this.onClose(opt.onClose);
    if (opt.autoDispose)
      this.onClose(() => setTimeout(bindThis(this).dispose, 0));
    if (opt.title !== void 0) {
      this.title(opt.title);
    }
    if (opt.autoOpen)
      this.open();
  }
  static {
    /** Default set of dialog options */
    this.defaults = {
      autoDispose: true,
      autoOpen: true,
      backdrop: false,
      centered: true,
      closeButton: true,
      fade: false,
      fullScreen: "md-down",
      modal: true,
      preferBSModal: true,
      size: "lg"
    };
  }
  static {
    /** Default set of message dialog options */
    this.messageDefaults = {
      autoDispose: true,
      autoOpen: true,
      backdrop: false,
      centered: true,
      closeButton: true,
      closeOnEscape: true,
      fade: true,
      fullScreen: null,
      htmlEncode: true,
      modal: true,
      preferBSModal: true,
      preWrap: true,
      size: "md"
    };
  }
  /**
   * Gets the dialog instance for the specified element.
   * @param el The dialog body element (.s-Panel, .ui-dialog-content, or .modal-body) or the root element (.modal, .ui-dialog, .s-Panel)
   * @returns The dialog instance, or null if the element is not a dialog.
   */
  static getInstance(el) {
    el = getDialogContentNode(el);
    if (!el)
      return null;
    return new _Dialog({ element: el }, false);
  }
  /** The result code of the button that is clicked. Also attached to the dialog element as data-dialog-result */
  get result() {
    return this.el ? this.el.dataset.dialogResult : this.dialogResult;
  }
  close(result) {
    this.el && (this.el.dataset.dialogResult = result ?? null);
    this.dialogResult = result ?? null;
    const target = getDialogEventsNode(this.el);
    if (!target)
      return;
    if (target.classList.contains("panel-body"))
      closePanel(this.el);
    else if (target.classList.contains("ui-dialog-content"))
      getjQuery()?.(this.el).dialog?.("close");
    else if (target.classList.contains("modal")) {
      if (isBS5Plus()) {
        bootstrap?.Modal?.getInstance?.(target)?.hide?.();
      } else {
        let $2 = getjQuery();
        if ($2?.fn?.modal)
          $2(target).modal?.("close");
      }
    }
    return this;
  }
  /**
   * Adds an event handler that is called when the dialog is closed. If the opt.before is true, the handler is called before the dialog is closed and
   * the closing can be cancelled by calling preventDefault on the event object.
   * @param handler The event handler function
   * @param opt Options to determine whether the handler should be called before the dialog is closed, and whether the handler should be called only once. 
   * The default for oneOff is true unless opt.before is true.
   * @returns The dialog instance
   */
  onClose(handler, opt) {
    const target = getDialogEventsNode(this.el);
    if (!target)
      return;
    const before = opt?.before ?? false;
    const onOrOne = opt?.oneOff ?? !opt?.before ? Fluent.one : Fluent.on;
    if (target.classList.contains("panel-body"))
      onOrOne(target, before ? "panelbeforeclose" : "panelclose", (e2) => handler(this.result, e2));
    else if (target.classList.contains("ui-dialog-content"))
      onOrOne(target, before ? "dialogbeforeclose" : "dialogclose", (e2) => handler(this.result, e2));
    else if (target.classList.contains("modal"))
      onOrOne(target, before ? "hide.bs.modal" : "hidden.bs.modal", (e2) => handler(this.result, e2));
    return this;
  }
  /**
   * Adds an event handler that is called when the dialog is closed. If the opt.before is true, the handler is called before the dialog is closed and
   * the closing can be cancelled by calling preventDefault on the event object. Note that if the dialog is not yet initialized, the first argument must be
   * the body element of the dialog.
   * @param el The dialog body element (.s-Panel, .ui-dialog-content, or .modal-body)
   * @param handler The event handler function
   * @param opt Options to determine whether the handler should be called before the dialog is closed, and whether the handler should be called only once. 
   * The default for oneOff is true unless opt.before is true.
   */
  static onClose(el, handler, opt) {
    const instance = _Dialog.getInstance(el);
    if (instance) {
      instance.onClose(handler, opt);
      return;
    }
    const target = isArrayLike(el) ? el[0] : el;
    if (target) {
      const before = opt?.before ?? false;
      const events = [before ? "panelbeforeclose" : "panelclose", before ? "dialogbeforeclose" : "dialogclose", before ? "modalbeforeclose" : "modalclose"];
      const wrapper = /* @__PURE__ */ __name((e2) => {
        handler(_Dialog.getInstance(el)?.result, e2);
        if (opt?.oneOff ?? !before) {
          events.forEach((type) => Fluent.off(target, type, wrapper));
        }
      }, "wrapper");
      events.forEach((type) => Fluent.on(target, type, wrapper));
    }
  }
  /**
   * Adds an event handler that is called when the dialog is opened. If the second parameter is true, the handler is called before the dialog is opened and
   * the opening can be cancelled by calling preventDefault on the event object.
   * Note that if the dialog is not yet initialized, the first argument must be the body element of the dialog.
   * @param handler The event handler function
   * @param opt Options to determine whether the handler should be called before the dialog is opened, and whether the handler should be called only once. 
   * The default for oneOff is true unless opt.before is true.
   * @returns The dialog instance
   */
  onOpen(handler, opt) {
    const target = getDialogEventsNode(this.el);
    if (!target)
      return;
    const before = opt?.before ?? false;
    const onOrOne = opt?.oneOff ?? !opt?.before ? Fluent.one : Fluent.on;
    if (target.classList.contains("panel-body"))
      onOrOne(target, before ? "panelbeforeopen" : "panelopen", handler);
    else if (target.classList.contains("ui-dialog-content"))
      onOrOne(target, before ? "dialogbeforeopen" : "dialogopen", handler);
    else if (target.classList.contains("modal"))
      onOrOne(target, before ? "show.bs.modal" : "shown.bs.modal", handler);
    return this;
  }
  /**
   * Adds an event handler that is called when the dialog is opened. If the second parameter is true, the handler is called before the dialog is opened and
   * the opening can be cancelled by calling preventDefault on the event object. Note that if the dialog is not yet initialized, the first argument must be
   * the body element of the dialog.
   * @param el The dialog body element (.s-Panel, .ui-dialog-content, or .modal-body)
   * @param handler The event handler function
   * @param opt Options to determine whether the handler should be called before the dialog is opened, and whether the handler should be called only once. 
   * The default for oneOff is true unless opt.before is true.
   * @returns The dialog instance
   */
  static onOpen(el, handler, opt) {
    const instance = _Dialog.getInstance(el);
    if (instance) {
      instance.onOpen(handler, opt);
      return;
    }
    const target = isArrayLike(el) ? el[0] : el;
    if (target) {
      const before = opt?.before ?? false;
      const events = [before ? "panelbeforeopen" : "panelopen", before ? "dialogbeforeopen" : "dialogopen", before ? "modalbeforeopen" : "modalopen"];
      const wrapper = /* @__PURE__ */ __name((e2) => {
        handler(e2);
        if (opt?.oneOff ?? !before) {
          events.forEach((type) => Fluent.off(target, type, wrapper));
        }
      }, "wrapper");
      events.forEach((type) => Fluent.on(target, type, wrapper));
    }
  }
  /** Opens the dialog */
  open() {
    const target = getDialogEventsNode(this.el);
    if (!target)
      return;
    if (target.classList.contains("panel-body"))
      openPanel(this.el);
    else if (target.classList.contains("ui-dialog-content"))
      getjQuery()?.(target).dialog("open");
    else if (target.classList.contains("modal")) {
      if (isBS5Plus()) {
        bootstrap?.Modal?.getInstance?.(target)?.show?.();
      } else {
        let $2 = getjQuery();
        if ($2?.fn?.modal)
          $2(target).modal?.("show");
      }
    }
    return this;
  }
  title(value) {
    let title = this.getHeaderNode()?.querySelector(".modal-title, .panel-titlebar-text, .ui-dialog-title");
    if (value === void 0 && !arguments.length)
      return title?.textContent;
    title && (title.textContent = value);
    return this;
  }
  /** Returns the type of the dialog, or null if no dialog on the current element or if the element is null, e.g. dialog was disposed  */
  get type() {
    const root = getDialogNode(this.el);
    if (!root)
      return null;
    if (root.classList.contains("modal"))
      return "bsmodal";
    if (root.classList.contains("ui-dialog"))
      return "uidialog";
    if (root.classList.contains("s-Panel"))
      return "panel";
    return null;
  }
  /** Gets the body/content element of the dialog */
  getContentNode() {
    return this.el;
  }
  /** Gets the dialog element of the dialog */
  getDialogNode() {
    return getDialogNode(this.el);
  }
  /** Gets the node that receives events for the dialog. It's .ui-dialog-content, .modal, or .panel-body */
  getEventsNode() {
    return getDialogEventsNode(this.el);
  }
  /** Gets the footer element of the dialog */
  getFooterNode() {
    return this.getDialogNode()?.querySelector(".modal-footer, .panel-footer, .ui-dialog-footer");
  }
  /** Gets the header element of the dialog */
  getHeaderNode() {
    return this.getDialogNode()?.querySelector(".modal-header, .panel-titlebar, .ui-dialog-titlebar");
  }
  onButtonClick(e2, btn) {
    e2 ??= new Event("click");
    if (!btn.click) {
      if (btn.result)
        this.close(btn.result);
      return;
    }
    const value = btn.click(e2);
    if (!btn.result)
      return;
    if (isPromiseLike(value))
      value.then((value2) => value2 !== false && !Fluent.isDefaultPrevented(e2) && this.close(btn.result));
    else if (value !== false && !Fluent.isDefaultPrevented(e2))
      this.close(btn.result);
  }
  createBSButtons(footer, buttons) {
    for (let btn of buttons) {
      Fluent(dialogButtonToBS(btn)).appendTo(footer).on("click", (e2) => this.onButtonClick(e2, btn));
    }
  }
  createBSModal(opt) {
    const header = jsx("div", { class: "modal-header", children: jsx("h5", { class: "modal-title" }) });
    let bs5 = isBS5Plus();
    if (opt.closeButton) {
      const closeButton = jsx("button", { type: "button", class: bs5 ? "btn-close" : "close", "aria-label": DialogTexts.CloseButton, children: !bs5 && jsx("span", { "aria-hidden": "true", children: "\u2715" }) });
      closeButton.dataset[bs5 ? "bsDismiss" : "dismiss"] = "modal";
      if (isBS3()) {
        header.prepend(closeButton);
      } else {
        header.appendChild(closeButton);
      }
    }
    this.el.classList.add("modal-body");
    const footer = jsx("div", { class: "modal-footer" });
    if (opt.buttons) {
      this.createBSButtons(footer, opt.buttons);
    }
    const modal = document.body.appendChild(
      jsx("div", { class: ["modal", opt.dialogClass, opt.fade && "fade"], tabindex: -1, children: jsx("div", { class: [
        "modal-dialog",
        opt.size && "modal-" + opt.size,
        opt.fullScreen && "modal-fullscreen" + (typeof opt.fullScreen === "string" ? `-${opt.fullScreen}` : ""),
        opt.centered && "modal-dialog-centered",
        opt.scrollable && "modal-scrollable"
      ], children: jsx("div", { class: "modal-content", children: [
        header,
        this.el,
        footer
      ] }) }) })
    );
    let modalOpt = {
      backdrop: opt.backdrop,
      keyboard: opt.closeOnEscape
    };
    if (opt.providerOptions)
      Object.assign(modalOpt, opt.providerOptions("bsmodal", opt));
    if (bs5 && bootstrap.Modal) {
      const modalObj = new bootstrap.Modal(modal, modalOpt);
      if (modalObj && modalObj._focustrap && modalObj._focustrap._handleFocusin) {
        const org = modalObj._focustrap._handleFocusin;
        modalObj._focustrap._handleFocusin = function(event) {
          if (event.target && event.target.closest(".dropdown-menu, .s-dropdown-menu, .ui-datepicker, .select2-drop, .cke, .cke_dialog, .flatpickr-calendar"))
            return;
          org.apply(this, arguments);
        };
      }
    } else {
      getjQuery()?.(modal)?.modal?.(modalOpt);
    }
    ;
  }
  createPanel(opt) {
    const panel = jsx("div", { hidden: true, class: ["s-Panel", opt.dialogClass], children: jsx("div", { class: "panel-titlebar", children: [
      jsx("div", { class: "panel-titlebar-text" }),
      opt.closeButton && jsx("button", { type: "button", class: "panel-titlebar-close", onClick: this.close.bind(this, null) })
    ] }) });
    this.el.classList.add("panel-body");
    if (this.el.parentElement && this.el.parentElement !== document.body) {
      this.el.parentElement.insertBefore(panel, this.el);
    }
    panel.appendChild(this.el);
    if (opt.buttons) {
      this.createBSButtons(panel.appendChild(jsx("div", { class: "panel-footer" })), opt.buttons);
    }
  }
  createUIDialog(opt) {
    let uiOpt = {
      autoOpen: opt.autoOpen,
      dialogClass: opt.dialogClass,
      title: opt.title,
      modal: opt.modal,
      width: opt.width,
      resizable: false
    };
    if (opt.centered)
      uiOpt.position = { my: "center", at: "center", of: window };
    if (opt.buttons) {
      uiOpt.buttons = opt.buttons.map((btn) => {
        let uiButton = dialogButtonToUI(btn);
        uiButton.click = (e2) => this.onButtonClick(e2, btn);
        return uiButton;
      });
    }
    if (opt.providerOptions)
      uiOpt = Object.assign(uiOpt, opt.providerOptions("uidialog", omitUndefined(opt)));
    getjQuery()?.(this.el).dialog(uiOpt);
  }
  /**
   * Disposes the dialog, removing it from the DOM and unbinding all event handlers.
   */
  dispose() {
    try {
      let target = getDialogEventsNode(this.el) ?? this.el;
      if (!target)
        return;
      try {
        if (target.classList.contains("ui-dialog-content")) {
          getjQuery?.()(target)?.dialog?.("destroy");
          target.classList.remove("ui-dialog-content");
          target = target.closest(".ui-dialog") ?? target;
        } else if (target.classList.contains("modal")) {
          if (!getjQuery() && isBS5Plus()) {
            if (typeof bootstrap !== "undefined")
              bootstrap.Modal?.getInstance(target)?.dispose?.();
          } else {
            getjQuery()?.(target)?.modal?.(isBS3() ? "destroy" : "dispose");
          }
          this.el?.classList.remove("modal-body");
        } else {
          this.el?.classList.remove("panel-body");
          target = target.closest(".s-Panel") ?? target;
        }
      } finally {
        Fluent.remove(target);
      }
    } finally {
      this.el = null;
    }
  }
};
function hasBSModal() {
  return isBS5Plus() || !!getjQuery()?.fn?.modal;
}
__name(hasBSModal, "hasBSModal");
function hasUIDialog() {
  return !!getjQuery()?.ui?.dialog;
}
__name(hasUIDialog, "hasUIDialog");
function uiAndBSButtonNoConflict() {
  const $2 = getjQuery();
  if ($2 && $2.fn?.button?.noConflict && $2.ui?.button) {
    $2.fn.btn = $2.fn.button.noConflict();
  }
}
__name(uiAndBSButtonNoConflict, "uiAndBSButtonNoConflict");
uiAndBSButtonNoConflict();
function dialogButtonToBS(x) {
  let html = htmlEncode(x.text);
  let iconClass = iconClassName(x.icon);
  if (iconClass)
    html = '<i class="' + htmlEncode(iconClass) + '"><i>' + (html ? " " + html : "");
  let button = document.createElement("button");
  button.type = "button";
  button.classList.add("btn");
  Fluent.addClass(button, x.cssClass ?? "btn-secondary");
  if (x.hint)
    button.setAttribute("title", x.hint);
  button.innerHTML = html;
  return button;
}
__name(dialogButtonToBS, "dialogButtonToBS");
function dialogButtonToUI(x) {
  let html = htmlEncode(x.text);
  let iconClass = iconClassName(x.icon);
  if (iconClass)
    html = '<i class="' + htmlEncode(iconClass) + '"></i>' + (html ? " " + html : "");
  let button = {
    text: html,
    click: x.click
  };
  if (x.cssClass)
    button.cssClass = x.cssClass;
  return button;
}
__name(dialogButtonToUI, "dialogButtonToUI");
function okDialogButton(opt) {
  return {
    text: opt?.text != void 0 ? opt.text : DialogTexts.OkButton,
    cssClass: opt?.cssClass != void 0 ? opt.cssClass : "btn-info",
    result: opt?.result != void 0 ? opt.result : "ok",
    click: opt?.click
  };
}
__name(okDialogButton, "okDialogButton");
function yesDialogButton(opt) {
  return {
    text: opt?.text != void 0 ? opt.text : DialogTexts.YesButton,
    cssClass: opt?.cssClass != void 0 ? opt.cssClass : "btn-primary",
    result: opt?.result != void 0 ? opt.result : "yes",
    click: opt?.click
  };
}
__name(yesDialogButton, "yesDialogButton");
function noDialogButton(opt) {
  return {
    text: opt?.text != void 0 ? opt.text : DialogTexts.NoButton,
    cssClass: opt?.cssClass != void 0 ? opt.cssClass : isBS5Plus() ? "btn-danger" : "btn-default",
    result: opt?.result != void 0 ? opt.result : "no",
    click: opt?.click
  };
}
__name(noDialogButton, "noDialogButton");
function cancelDialogButton(opt) {
  return {
    text: opt?.text != void 0 ? opt.text : DialogTexts.CancelButton,
    cssClass: opt?.cssClass != void 0 ? opt.cssClass : isBS5Plus() ? "btn-secondary" : "btn-default",
    result: opt?.result != void 0 ? opt.result : "cancel",
    click: opt?.click
  };
}
__name(cancelDialogButton, "cancelDialogButton");
var DialogTexts;
((DialogTexts2) => {
  const defaultTxt = {
    AlertTitle: "Alert",
    CancelButton: "Cancel",
    CloseButton: "Close",
    ConfirmationTitle: "Confirm",
    InformationTitle: "Information",
    MaximizeHint: "Maximize",
    NoButton: "No",
    OkButton: "OK",
    PromptTitle: "Prompt",
    RestoreHint: "Restore",
    SuccessTitle: "Success",
    WarningTitle: "Warning",
    YesButton: "Yes"
  };
  function get() {
    return htmlEncode(localText("Dialogs." + this, defaultTxt[this]));
  }
  __name(get, "get");
  for (let k of Object.keys(defaultTxt)) {
    Object.defineProperty(DialogTexts2, k, {
      get: get.bind(k)
    });
  }
})(DialogTexts || (DialogTexts = {}));
function closePanel(el) {
  let panel = getDialogNode(el);
  if (!panel || panel.hidden)
    return;
  const eventsNode = getDialogEventsNode(el) ?? panel;
  let event = Fluent.trigger(eventsNode, "panelbeforeclose");
  if (Fluent.isDefaultPrevented(event))
    return;
  panel.hidden = true;
  let uniqueName = panel.dataset.paneluniquename;
  if (uniqueName) {
    document.querySelectorAll(`[data-hiddenby="${uniqueName}"]`).forEach((hiddenBy) => {
      hiddenBy.removeAttribute("data-hiddenby");
    });
  }
  Fluent.trigger(window, "resize");
  document.querySelectorAll(".require-layout").forEach((rl) => Fluent.isVisibleLike(rl) && Fluent.trigger(rl, "layout"));
  Fluent.trigger(eventsNode, "panelclose");
}
__name(closePanel, "closePanel");
function openPanel(element, uniqueName) {
  let panel = getDialogNode(element);
  if (!panel)
    return;
  let container = panel.parentElement && panel.parentElement !== document.body ? panel.parentElement : document.querySelector(".panels-container") ?? document.querySelector("section.content") ?? panel.parentElement ?? document.body;
  if (panel.parentElement !== container) {
    container.appendChild(panel);
  }
  let eventNode = getDialogEventsNode(element) ?? panel;
  let event = Fluent.trigger(eventNode, "panelbeforeopen");
  if (Fluent.isDefaultPrevented(event))
    return;
  panel.dataset.paneluniquename = uniqueName || panel.id || (/* @__PURE__ */ new Date()).getTime().toString();
  function setHideBy(e2) {
    if (e2 === panel || e2.tagName === "LINK" || e2.tagName === "SCRIPT" || e2.hidden || e2.dataset.hiddenby || container && e2.parentElement !== container && !Fluent.isVisibleLike(e2))
      return;
    e2.dataset.hiddenby = panel.dataset.paneluniquename;
  }
  __name(setHideBy, "setHideBy");
  if (container) {
    let c2 = container.children;
    const cl = c2.length;
    for (let i2 = 0; i2 < cl; i2++) {
      setHideBy(c2[i2]);
    }
  }
  document.querySelectorAll(".ui-dialog, .ui-widget-overlay, .modal.show, .modal.in").forEach(setHideBy);
  panel.hidden = false;
  delete panel.dataset.hiddenby;
  Fluent.trigger(eventNode, "panelopen");
}
__name(openPanel, "openPanel");
function getDialogNode(element) {
  if (isArrayLike(element))
    element = element[0];
  if (!element)
    return null;
  return element.closest(".modal, .s-Panel, .ui-dialog");
}
__name(getDialogNode, "getDialogNode");
function getDialogEventsNode(element) {
  if (isArrayLike(element))
    element = element[0];
  if (!element)
    return null;
  return element.closest(".modal, .panel-body, .ui-dialog-content") ?? getDialogNode(element)?.querySelector(".panel-body, .ui-dialog-content");
}
__name(getDialogEventsNode, "getDialogEventsNode");
function getDialogContentNode(element) {
  if (isArrayLike(element))
    element = element[0];
  if (!element)
    return null;
  return element.closest(".modal-body, .panel-body, .ui-dialog-content") ?? getDialogNode(element)?.querySelector(".modal-body, .panel-body, .ui-dialog-content");
}
__name(getDialogContentNode, "getDialogContentNode");
function getMessageBodyHtml(message, options) {
  const div = document.createElement("div");
  div.className = "message";
  let preWrap = options == null || options.preWrap == null && typeof message === "string" || options.preWrap;
  if (preWrap)
    div.style.whiteSpace = "pre-wrap";
  if (typeof message === "string" && message.length) {
    let encode = options == null || options.htmlEncode == null || options.htmlEncode;
    if (encode)
      div.innerText = message;
    else {
      div.innerHTML = sanitizeHtml(message);
    }
  } else {
    div.append(message ?? "");
  }
  return div;
}
__name(getMessageBodyHtml, "getMessageBodyHtml");
function createMessageDialog(opt) {
  if (!hasBSModal() && !hasUIDialog()) {
    const msg = opt.message == null ? "" : typeof opt.message === "string" ? opt.message : opt.message.textContent;
    const result = opt.native(msg);
    opt.options?.onClose?.(result);
    return {
      result
    };
  }
  let options = Object.assign({}, Dialog.messageDefaults, {
    dialogClass: "s-MessageDialog" + (opt.cssClass ? " " + opt.cssClass : ""),
    title: opt.title
  }, opt.options);
  if (options.buttons == void 0) {
    options.buttons = opt.getButtons();
  }
  if (options.providerOptions === void 0) {
    options.providerOptions = (type) => {
      if (type === "uidialog") {
        return {
          width: "40%",
          maxWidth: 450,
          minWidth: 180,
          resizable: false
        };
      }
    };
  }
  if (options.element === void 0) {
    options.element = (el) => el.append(getMessageBodyHtml(opt.message, options));
  }
  return new Dialog(options);
}
__name(createMessageDialog, "createMessageDialog");
function alertDialog(message, options) {
  return createMessageDialog({
    message,
    options,
    cssClass: "s-AlertDialog",
    title: DialogTexts.AlertTitle,
    getButtons: /* @__PURE__ */ __name(() => [okDialogButton({ cssClass: "btn-danger" })], "getButtons"),
    native: /* @__PURE__ */ __name((msg) => {
      alert(msg);
      return "ok";
    }, "native")
  });
}
__name(alertDialog, "alertDialog");
function confirmDialog(message, onYes, options) {
  return createMessageDialog({
    message,
    options,
    cssClass: "s-ConfirmDialog",
    title: DialogTexts.ConfirmationTitle,
    getButtons: /* @__PURE__ */ __name(() => {
      let buttons = [yesDialogButton({ click: onYes }), noDialogButton({ click: options?.onNo })];
      if (options?.cancelButton)
        buttons.push(cancelDialogButton({ click: options?.onCancel }));
      return buttons;
    }, "getButtons"),
    native: /* @__PURE__ */ __name((msg) => {
      const result = window.confirm(msg);
      if (result) {
        onYes?.();
        return "yes";
      } else {
        options?.onNo();
        return result === false ? "no" : "";
      }
    }, "native")
  });
}
__name(confirmDialog, "confirmDialog");
function informationDialog(message, onOk, options) {
  return createMessageDialog({
    message,
    options,
    cssClass: "s-InformationDialog",
    title: DialogTexts.InformationTitle,
    getButtons: /* @__PURE__ */ __name(() => [okDialogButton({ click: onOk, cssClass: "btn-info" })], "getButtons"),
    native: /* @__PURE__ */ __name((msg) => {
      alert(msg);
      onOk?.();
      return "ok";
    }, "native")
  });
}
__name(informationDialog, "informationDialog");
function successDialog(message, onOk, options) {
  return createMessageDialog({
    message,
    options,
    cssClass: "s-SuccessDialog",
    title: DialogTexts.SuccessTitle,
    getButtons: /* @__PURE__ */ __name(() => [okDialogButton({ click: onOk, cssClass: "btn-success" })], "getButtons"),
    native: /* @__PURE__ */ __name((msg) => {
      alert(msg);
      onOk?.();
      return "ok";
    }, "native")
  });
}
__name(successDialog, "successDialog");
function warningDialog(message, options) {
  return createMessageDialog({
    message,
    options,
    cssClass: "s-WarningDialog",
    title: DialogTexts.WarningTitle,
    getButtons: /* @__PURE__ */ __name(() => [okDialogButton({ cssClass: "btn-warning" })], "getButtons"),
    native: /* @__PURE__ */ __name((msg) => {
      alert(msg);
      return "ok";
    }, "native")
  });
}
__name(warningDialog, "warningDialog");
function iframeDialog(options) {
  if (!hasBSModal() && !hasUIDialog()) {
    window.alert(sanitizeHtml(options.html));
    return {
      result: "ok"
    };
  }
  function onOpen(div) {
    if (div) {
      let iframe = div.appendChild(document.createElement("iframe"));
      iframe.style.border = "none";
      iframe.style.width = "100%";
      let content = sanitizeHtml(options.html);
      if (content.indexOf("<body") < 0)
        content = "<body>" + content + "</body>";
      if (content.indexOf("<html") < 0)
        content = "<html>" + content + "</html>";
      iframe.srcdoc = content;
    }
  }
  __name(onOpen, "onOpen");
  return new Dialog({
    title: DialogTexts.AlertTitle,
    dialogClass: "s-IFrameDialog",
    size: "lg",
    autoOpen: true,
    element: /* @__PURE__ */ __name((el) => {
      let div = document.createElement("div");
      div.style.overflow = "hidden";
      div.style.minHeight = "50vh";
      div.style.display = "flex";
      el.append(div);
      onOpen(div);
    }, "element"),
    providerOptions: /* @__PURE__ */ __name((type) => {
      if (type == "uidialog") {
        return {
          width: "60%",
          height: "400"
        };
      }
    }, "providerOptions")
  });
}
__name(iframeDialog, "iframeDialog");
var modalShow = /* @__PURE__ */ __name((e2) => {
  const body = Dialog.getInstance(e2.target)?.getContentNode();
  if (body) {
    const evt = Fluent.trigger(body, "modalbeforeopen");
    if (Fluent.isDefaultPrevented(evt))
      e2.preventDefault();
  }
}, "modalShow");
var modalShown = /* @__PURE__ */ __name((e2) => {
  const body = Dialog.getInstance(e2.target)?.getContentNode();
  if (body) {
    Fluent.trigger(body, "modalopen");
  }
}, "modalShown");
var modalHide = /* @__PURE__ */ __name((e2) => {
  const body = Dialog.getInstance(e2.target)?.getContentNode();
  if (body) {
    const evt = Fluent.trigger(body, "modalbeforeclose");
    if (Fluent.isDefaultPrevented(evt))
      e2.preventDefault();
  }
}, "modalHide");
var modalHidden = /* @__PURE__ */ __name((e2) => {
  const instance = Dialog.getInstance(e2.target);
  try {
    if (document.activeElement && instance && instance.getDialogNode()?.contains(document.activeElement))
      document.activeElement.blur?.();
  } catch {
  }
  const body = Dialog.getInstance(e2.target)?.getContentNode();
  if (body) {
    Fluent.trigger(body, "modalclose");
  }
}, "modalHidden");
function installBsModalEventPropagation() {
  uninstallBsModalEventPropagation();
  if (typeof document === "undefined" || typeof document.addEventListener !== "function")
    return;
  document.addEventListener("show.bs.modal", modalShow);
  document.addEventListener("shown.bs.modal", modalShown);
  document.addEventListener("hide.bs.modal", modalHide);
  document.addEventListener("hidden.bs.modal", modalHidden);
}
__name(installBsModalEventPropagation, "installBsModalEventPropagation");
function uninstallBsModalEventPropagation() {
  if (typeof document === "undefined" || typeof document.removeEventListener !== "function")
    return;
  document.removeEventListener("show.bs.modal", modalShow);
  document.removeEventListener("shown.bs.modal", modalShown);
  document.removeEventListener("hide.bs.modal", modalHide);
  document.removeEventListener("hidden.bs.modal", modalHidden);
}
__name(uninstallBsModalEventPropagation, "uninstallBsModalEventPropagation");
installBsModalEventPropagation();

// src/base/formatting.ts
var Invariant = {
  decimalSeparator: ".",
  groupSeparator: ",",
  decimalDigits: 2,
  negativeSign: "-",
  positiveSign: "+",
  percentSymbol: "%",
  currencySymbol: "$",
  dateSeparator: "/",
  dateOrder: "mdy",
  dateFormat: "MM/dd/yyyy",
  dateTimeFormat: "MM/dd/yyyy HH:mm:ss",
  amDesignator: "AM",
  pmDesignator: "PM",
  timeSeparator: ":",
  firstDayOfWeek: 0,
  dayNames: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDayNames: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  minimizedDayNames: ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"],
  monthNames: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December", ""],
  shortMonthNames: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec", ""],
  stringCompare: /* @__PURE__ */ __name((a2, b2) => a2 < b2 ? -1 : a2 > b2 ? 1 : 0, "stringCompare")
};
var Culture;
function resetCultureSettings() {
  Culture = {
    decimalSeparator: ".",
    groupSeparator: ",",
    dateSeparator: "/",
    dateOrder: "dmy",
    dateFormat: "dd/MM/yyyy",
    dateTimeFormat: "dd/MM/yyyy HH:mm:ss",
    stringCompare: Invariant.stringCompare
  };
  let lang = typeof document !== "undefined" ? document.documentElement?.lang : void 0;
  if (lang == "")
    lang = void 0;
  if (lang !== void 0) {
    try {
      "a".localeCompare("b", lang);
    } catch {
      lang = void 0;
    }
  }
  Culture.stringCompare = (a2, b2) => a2 == null ? b2 == null ? 0 : -1 : b2 == null ? 1 : a2.localeCompare(b2, lang);
  let k;
  for (k in Invariant)
    if (Culture[k] === void 0 && Object.prototype.hasOwnProperty.call(Invariant, k))
      Culture[k] = Invariant[k];
  if (typeof document !== "undefined" && (k = document.querySelector("script#ScriptCulture")?.textContent?.trim())?.length) {
    const sc = JSON.parse(k);
    if (sc.DecimalSeparator != null)
      Culture.decimalSeparator = sc.DecimalSeparator;
    if (sc.GroupSeparator != null && sc.GroupSeparator != Culture.decimalSeparator)
      Culture.groupSeparator = sc.GroupSeparator;
    else if (Culture.groupSeparator == Culture.decimalSeparator)
      Culture.groupSeparator = Culture.decimalSeparator == "." ? "," : ".";
    delete sc.GroupSeparator;
    delete sc.DecimalSeparator;
    for (k in sc) {
      if (Culture[k] === void 0 && Object.prototype.hasOwnProperty.call(sc, k))
        Culture[k.charAt(0).toLowerCase() + k.substring(1)] = sc[k];
    }
  }
}
__name(resetCultureSettings, "resetCultureSettings");
resetCultureSettings();
function insertGroupSeperator(num, dec, grp, neg) {
  let decPart = null;
  const decIndex = num.indexOf(dec);
  if (decIndex > 0) {
    decPart = num.substring(decIndex);
    num = num.substring(0, decIndex);
  }
  const negative = num.startsWith(neg);
  if (negative) {
    num = num.substring(1);
  }
  let groupSize = 3;
  if (num.length < groupSize) {
    return (negative ? neg : "") + (decPart ? num + decPart : num);
  }
  let index = num.length;
  let s2 = "";
  let done = false;
  while (!done) {
    let length = groupSize;
    let startIndex = index - length;
    if (startIndex < 0) {
      groupSize += startIndex;
      length += startIndex;
      startIndex = 0;
      done = true;
    }
    if (!length)
      break;
    const part = num.substring(startIndex, startIndex + length);
    if (s2.length)
      s2 = part + grp + s2;
    else
      s2 = part;
    index -= length;
  }
  if (negative)
    s2 = "-" + s2;
  return decPart ? s2 + decPart : s2;
}
__name(insertGroupSeperator, "insertGroupSeperator");
var _formatRE = /\{\{|\}\}|\{[^\}\{]+\}/g;
function _formatString(format2, l2, values, from) {
  return format2.replace(
    _formatRE,
    function(m) {
      if (m === "{{" || m === "}}")
        return m.charAt(0);
      const index = parseInt(m.substring(1), 10);
      const value = values[index + from];
      if (value == null) {
        return "";
      }
      let formatSpec = null;
      const formatIndex = m.indexOf(":");
      if (formatIndex > 0) {
        formatSpec = m.substring(formatIndex + 1, m.length - 1);
      }
      return _formatObject(value, formatSpec, l2);
    }
  );
}
__name(_formatString, "_formatString");
function stringFormat(format2, ...prm) {
  return _formatString(format2, Culture, arguments, 1);
}
__name(stringFormat, "stringFormat");
function stringFormatLocale(l2, format2, ...prm) {
  return _formatString(format2, l2, arguments, 2);
}
__name(stringFormatLocale, "stringFormatLocale");
function _formatObject(obj, format2, fmt) {
  if (typeof obj === "number")
    return formatNumber(obj, format2, fmt);
  else if (Object.prototype.toString.call(obj) === "[object Date]")
    return formatDate(obj, format2, fmt);
  else if (obj.format)
    return obj.format(format2, fmt ?? Culture);
  return String(obj);
}
__name(_formatObject, "_formatObject");
var round = /* @__PURE__ */ __name((num, d2) => {
  if (typeof num == "undefined" || isNaN(num))
    return Math.round(num);
  num = +num;
  if (num === 0)
    return 0;
  d2 = d2 || 0;
  const s2 = num < 0 ? -1 : 1;
  num = num < 0 ? -num : num;
  if (d2 === 0)
    return Math.round(num) * s2;
  const ep = d2 >= 0 ? "e+" + d2 : "e" + d2;
  const em = d2 >= 0 ? "e-" + d2 : "e+" + -d2;
  return +(Math.round(num + ep) + em) * s2;
}, "round");
var trunc = /* @__PURE__ */ __name((n2) => n2 != null ? n2 > 0 ? Math.floor(n2) : Math.ceil(n2) : null, "trunc");
function formatNumber(num, format2, decOrLoc, grp) {
  if (num == null)
    return "";
  const fmt = typeof decOrLoc !== "string" ? decOrLoc ?? Culture : {
    decimalSeparator: decOrLoc,
    groupSeparator: grp ?? (decOrLoc == "," ? "." : ",")
  };
  if (isNaN(num)) {
    return fmt.nanSymbol ?? Culture.nanSymbol;
  }
  if (format2 === "i") {
    return num.toString();
  }
  if (format2 == null || format2 == "") {
    format2 = "g";
  }
  const dec = fmt.decimalSeparator ?? Culture.decimalSeparator;
  grp = grp ?? fmt.groupSeparator ?? Culture.groupSeparator;
  const neg = fmt.negativeSign ?? Culture.negativeSign;
  let s2 = "";
  let precision = -1;
  if (format2.length > 1) {
    precision = parseInt(format2.substring(1), 10);
  }
  const fs = format2.charAt(0);
  switch (fs) {
    case "g":
    case "G":
      if (precision != -1)
        s2 = num.toFixed(precision);
      else
        s2 = num.toString();
      if (dec != ".")
        s2 = s2.replace(".", dec);
      break;
    case "d":
    case "D":
      s2 = parseInt(Math.abs(num)).toString();
      if (precision != -1)
        s2 = s2.padStart(precision, "0");
      if (num < 0)
        s2 = neg + s2;
      break;
    case "x":
    case "X":
      s2 = parseInt(Math.abs(num)).toString(16);
      if (fs == "X")
        s2 = s2.toUpperCase();
      if (precision != -1)
        s2 = s2.padStart(precision, "0");
      break;
    case "e":
    case "E":
      if (precision == -1)
        s2 = num.toExponential(6);
      else
        s2 = num.toExponential(precision);
      if (fs == "E")
        s2 = s2.toUpperCase();
      break;
    case "f":
    case "F":
    case "n":
    case "N":
      if (precision == -1) {
        precision = fmt.decimalDigits ?? Culture.decimalDigits;
      }
      s2 = num.toFixed(precision).toString();
      if (precision && dec != ".") {
        const index = s2.indexOf(".");
        s2 = s2.substring(0, index) + dec + s2.substring(index + 1);
      }
      if (fs == "n" || fs == "N") {
        s2 = insertGroupSeperator(s2, dec, grp, neg);
      }
      break;
    case "c":
    case "C":
    case "p":
    case "P":
      if (precision == -1) {
        precision = fmt.decimalDigits ?? Culture.decimalDigits;
      }
      let symbol;
      if (fs === "p" || fs == "P") {
        num *= 100;
        symbol = fmt.percentSymbol ?? Culture.percentSymbol;
      } else {
        symbol = fmt.currencySymbol ?? Culture.currencySymbol;
      }
      s2 = num.toFixed(precision).toString();
      if (precision && dec != ".") {
        const index = s2.indexOf(".");
        s2 = s2.substring(0, index) + dec + s2.substring(index + 1);
      }
      s2 = insertGroupSeperator(s2, dec, grp, neg) + symbol;
      break;
    default:
      let prefix = "";
      let mid = "";
      let suffix = "";
      let endPrefix = false;
      let inQuote = false;
      for (let i2 = 0; i2 < format2.length; i2++) {
        let c2 = format2.charAt(i2);
        if (c2 == "'") {
          inQuote = !inQuote;
          continue;
        } else if (!inQuote) {
          if (c2 == "\\") {
            c2 = format2.charAt(i2 + 1) || "";
            i2++;
          } else if (c2 == "#" || c2 == "," || c2 == "." || c2 == "0") {
            endPrefix = true;
            mid += c2;
            continue;
          }
        }
        endPrefix ? suffix += c2 : prefix += c2;
      }
      format2 = mid;
      let r2 = "";
      if (format2.indexOf(".") > -1) {
        let dp = dec;
        let df = format2.substring(format2.lastIndexOf(".") + 1);
        num = roundNumber(num, df.length);
        let dv = num % 1;
        let ds = new String(dv.toFixed(df.length));
        ds = ds.substring(ds.lastIndexOf(".") + 1);
        for (let i2 = 0; i2 < df.length; i2++) {
          if (df.charAt(i2) == "#" && ds.charAt(i2) != "0") {
            dp += ds.charAt(i2);
            continue;
          } else if (df.charAt(i2) == "#" && ds.charAt(i2) == "0") {
            let notParsed = ds.substring(i2);
            if (notParsed.match("[1-9]")) {
              dp += ds.charAt(i2);
              continue;
            } else
              break;
          } else if (df.charAt(i2) == "0")
            dp += ds.charAt(i2);
          else
            dp += df.charAt(i2);
        }
        r2 += dp;
      } else
        num = Math.round(num);
      let ones = Math.floor(num);
      if (num < 0)
        ones = Math.ceil(num);
      let of = "";
      if (format2.indexOf(".") == -1)
        of = format2;
      else
        of = format2.substring(0, format2.indexOf("."));
      let op = "";
      if (!(ones == 0 && of.substring(of.length - 1) == "#")) {
        let oneText = new String(Math.abs(ones));
        let gl = 9999;
        if (of.lastIndexOf(",") != -1)
          gl = of.length - of.lastIndexOf(",") - 1;
        let gc = 0;
        for (let i2 = oneText.length - 1; i2 > -1; i2--) {
          op = oneText.charAt(i2) + op;
          gc++;
          if (gc == gl && i2 != 0) {
            op = grp + op;
            gc = 0;
          }
        }
        if (of.length > op.length) {
          let padStart = of.indexOf("0");
          if (padStart != -1) {
            let padLen = of.length - padStart;
            let pos = of.length - op.length - 1;
            while (op.length < padLen) {
              let pc = of.charAt(pos);
              if (pc == ",")
                pc = grp;
              op = pc + op;
              pos--;
            }
          }
        }
      }
      if (!op && of.indexOf("0", of.length - 1) !== -1)
        op = "0";
      r2 = op + r2;
      if (num < 0)
        r2 = neg + r2;
      if (r2.lastIndexOf(dec) == r2.length - 1) {
        r2 = r2.substring(0, r2.length - 1);
      }
      return prefix + r2 + suffix;
  }
  return s2;
}
__name(formatNumber, "formatNumber");
function parseInteger(s2) {
  if (s2 == null)
    return null;
  s2 = s2.toString().trim();
  if (!s2.length)
    return null;
  let ts = Culture.groupSeparator;
  if (s2 && s2.length && s2.indexOf(ts) > 0) {
    s2 = s2.replace(new RegExp("(\\b\\d{1,3})\\" + ts + "(?=\\d{3}(\\D|$))", "g"), "$1");
  }
  if (!/^[-\+]?\d+$/.test(s2))
    return NaN;
  return parseInt(s2, 10);
}
__name(parseInteger, "parseInteger");
function parseDecimal(s2) {
  if (s2 == null)
    return null;
  s2 = s2.toString().trim();
  if (s2.length == 0)
    return null;
  let ts = Culture.groupSeparator;
  if (s2 && s2.length && s2.indexOf(ts) > 0) {
    s2 = s2.replace(new RegExp("(\\b\\d{1,3})\\" + ts + "(?=\\d{3}(\\D|$))", "g"), "$1");
  }
  if (!new RegExp("^\\s*([-\\+])?(\\d*)\\" + Culture.decimalSeparator + "?(\\d*)\\s*$").test(s2))
    return NaN;
  return parseFloat(s2.toString().replace(Culture.decimalSeparator, "."));
}
__name(parseDecimal, "parseDecimal");
function roundNumber(n2, dec) {
  let power = Math.pow(10, dec || 0);
  let value = (Math.round(n2 * power) / power).toString();
  if (dec > 0) {
    let dp = value.indexOf(".");
    if (dp == -1) {
      value += ".";
      dp = 0;
    } else {
      dp = value.length - (dp + 1);
    }
    while (dp < dec) {
      value += "0";
      dp++;
    }
  }
  return parseFloat(value);
}
__name(roundNumber, "roundNumber");
function toId(id) {
  if (id == null)
    return null;
  if (typeof id == "number")
    return id;
  if (typeof id == "string")
    id = id.trim();
  if (!id.length)
    return null;
  if (id.length >= 15 || !/^-?\d+$/.test(id))
    return id;
  return parseInt(id, 10);
}
__name(toId, "toId");
var _dateFormatRE = /'.*?[^\\]'|dddd|ddd|dd|d|MMMM|MMM|MM|M|yyyy|yy|y|hh|h|HH|H|mm|m|ss|s|tt|t|fff|ff|f|zzz|zz|z|\//g;
function formatDate(d2, format2, locale) {
  if (!d2)
    return "";
  let date;
  if (typeof d2 == "string") {
    date = parseDate(d2, locale?.dateOrder);
    if (!date)
      return "";
    if (isNaN(date.valueOf()))
      return d2;
  } else
    date = d2;
  if (format2 == "i")
    return date.toString();
  if (format2 == "id")
    return date.toDateString();
  if (format2 == "it")
    return date.toTimeString();
  if (locale == null)
    locale = Culture;
  if (format2 == null || format2 == "d")
    format2 = locale.dateFormat ?? Culture.dateFormat;
  else if (format2.length == 1) {
    switch (format2) {
      case "g":
        format2 = (locale.dateTimeFormat ?? Culture.dateTimeFormat).replace(":ss", "");
        break;
      case "G":
        format2 = locale.dateTimeFormat ?? Culture.dateTimeFormat;
        break;
      case "s":
        format2 = "yyyy-MM-ddTHH:mm:ss";
        break;
      case "t":
        format2 = locale.dateTimeFormat && locale.dateFormat ? locale.dateTimeFormat.replace(locale.dateFormat + " ", "") : "HH:mm";
        break;
      case "u":
      case "U":
        format2 = format2 == "u" ? "yyyy-MM-ddTHH:mm:ss.fffZ" : locale.dateTimeFormat ?? Culture.dateTimeFormat;
        date = new Date(
          date.getUTCFullYear(),
          date.getUTCMonth(),
          date.getUTCDate(),
          date.getUTCHours(),
          date.getUTCMinutes(),
          date.getUTCSeconds(),
          date.getUTCMilliseconds()
        );
        break;
    }
  }
  if (format2.charAt(0) == "%") {
    format2 = format2.substring(1);
  }
  const re = _dateFormatRE;
  const sb = [];
  re.lastIndex = 0;
  while (true) {
    const index = re.lastIndex;
    const match = re.exec(format2);
    sb.push(format2.slice(index, match ? match.index : format2.length));
    if (!match) {
      break;
    }
    const fs = match[0];
    let part = fs;
    let n2;
    switch (fs) {
      case "/":
        part = locale.dateSeparator ?? Culture.dateSeparator;
        break;
      case "dddd":
        part = (locale.dayNames ?? Culture.dayNames)[date.getDay()];
        break;
      case "ddd":
        part = (locale.shortDayNames ?? Culture.shortDayNames)[date.getDay()];
        break;
      case "dd":
        part = date.getDate().toString().padStart(2, "0");
        break;
      case "d":
        part = date.getDate().toString();
        break;
      case "MMMM":
        part = (locale.monthNames ?? Culture.monthNames)[date.getMonth()];
        break;
      case "MMM":
        part = (locale.shortMonthNames ?? Culture.shortMonthNames)[date.getMonth()];
        break;
      case "MM":
        part = (date.getMonth() + 1).toString().padStart(2, "0");
        break;
      case "M":
        part = (date.getMonth() + 1).toString();
        break;
      case "yyyy":
        part = date.getFullYear().toString().padStart(4, "0");
        break;
      case "yy":
        part = (date.getFullYear() % 100).toString().padStart(2, "0");
        break;
      case "y":
        part = (date.getFullYear() % 100).toString();
        break;
      case "h":
      case "hh":
        n2 = date.getHours() % 12;
        if (!n2) {
          part = "12";
        } else {
          part = n2.toString();
          if (fs == "hh")
            part = part.padStart(2, "0");
        }
        break;
      case "HH":
        part = date.getHours().toString().padStart(2, "0");
        break;
      case "H":
        part = date.getHours().toString();
        break;
      case "mm":
        part = date.getMinutes().toString().padStart(2, "0");
        break;
      case "m":
        part = date.getMinutes().toString();
        break;
      case "ss":
        part = date.getSeconds().toString().padStart(2, "0");
        break;
      case "s":
        part = date.getSeconds().toString();
        break;
      case "t":
      case "tt":
        part = date.getHours() < 12 ? locale.amDesignator ?? Culture.amDesignator : locale.pmDesignator ?? Culture.pmDesignator;
        if (fs == "t") {
          part = part.charAt(0);
        }
        break;
      case "fff":
        part = date.getMilliseconds().toString().padStart(3, "0");
        break;
      case "ff":
        part = date.getMilliseconds().toString().padStart(3, "0").substring(0, 2);
        break;
      case "f":
        part = date.getMilliseconds().toString().padStart(3, "0").charAt(0);
        break;
      case "z":
        n2 = date.getTimezoneOffset() / 60;
        part = (n2 >= 0 ? "-" : "+") + Math.floor(Math.abs(n2));
        break;
      case "zz":
      case "zzz":
        n2 = date.getTimezoneOffset() / 60;
        part = (n2 >= 0 ? "-" : "+") + Math.floor(Math.abs(n2)).toString().padStart(2, "0");
        if (fs == "zzz") {
          part += (locale.timeSeparator ?? Culture.timeSeparator) + Math.abs(date.getTimezoneOffset() % 60).toString().padStart(2, "0");
        }
        break;
      default:
        if (part.charAt(0) == "'") {
          part = part.substring(1, part.length - 1).replace(/\\'/g, "'");
        }
        break;
    }
    sb.push(part);
  }
  return sb.join("");
}
__name(formatDate, "formatDate");
function formatISODateTimeUTC(d2) {
  if (d2 == null)
    return "";
  let zeropad = /* @__PURE__ */ __name(function(num) {
    return (num < 10 ? "0" : "") + num;
  }, "zeropad");
  let str = d2.getUTCFullYear() + "-" + zeropad(d2.getUTCMonth() + 1) + "-" + zeropad(d2.getUTCDate()) + "T" + zeropad(d2.getUTCHours()) + ":" + zeropad(d2.getUTCMinutes());
  let secs = Number(d2.getUTCSeconds() + "." + (d2.getUTCMilliseconds() < 100 ? "0" : "") + zeropad(d2.getUTCMilliseconds()));
  str += ":" + zeropad(secs) + "Z";
  return str;
}
__name(formatISODateTimeUTC, "formatISODateTimeUTC");
var isoRegexp = /(\d{4,})(?:-(\d{1,2})(?:-(\d{1,2})(?:[T ](\d{1,2}):(\d{1,2})(?::(\d{1,2})(?:\.(\d+))?)?(?:(Z)|([+-])(\d{1,2})(?::(\d{1,2}))?)?)?)?)?/;
function parseISODateTime(s2) {
  if (s2 == null)
    return null;
  if (typeof s2 !== "string")
    s2 = s2 + "";
  if (!s2.length)
    return null;
  if (!isoRegexp.test(s2))
    return /* @__PURE__ */ new Date(NaN);
  return /* @__PURE__ */ new Date(s2 + (s2.length == 10 ? "T00:00:00" : ""));
}
__name(parseISODateTime, "parseISODateTime");
function parseDate(s2, dateOrder) {
  if (!s2 || !s2.length)
    return null;
  s2 = s2.trim();
  if (!s2.length)
    return null;
  if (s2.length >= 10 && s2.charAt(4) === "-" && s2.charAt(7) === "-" && (s2.length === 10 || s2.length > 10 && s2.charAt(10) === "T")) {
    return parseISODateTime(s2);
  }
  if (s2.indexOf(" ") > 0 && s2.indexOf(":") > s2.indexOf(" ") + 1) {
    const datePart = parseDate(s2.substring(0, s2.indexOf(" ")));
    if (!datePart || isNaN(datePart.valueOf()))
      return /* @__PURE__ */ new Date(NaN);
    return parseISODateTime(formatDate(datePart, "yyyy-MM-dd") + "T" + s2.substring(s2.indexOf(" ") + 1).trim());
  }
  let d2, m, y2;
  let dArray = splitDateString(s2);
  if (dArray.length == 3) {
    if (dArray.some((x) => !/^[0-9]+$/.test(x)))
      return /* @__PURE__ */ new Date(NaN);
    dateOrder = dateOrder || Culture.dateOrder;
    switch (dateOrder) {
      case "dmy":
        d2 = parseInt(dArray[0], 10);
        m = parseInt(dArray[1], 10) - 1;
        y2 = parseInt(dArray[2], 10);
        break;
      case "ymd":
        d2 = parseInt(dArray[2], 10);
        m = parseInt(dArray[1], 10) - 1;
        y2 = parseInt(dArray[0], 10);
        break;
      case "mdy":
      default:
        d2 = parseInt(dArray[1], 10);
        m = parseInt(dArray[0], 10) - 1;
        y2 = parseInt(dArray[2], 10);
        break;
    }
    if (isNaN(d2) || isNaN(m) || isNaN(y2) || d2 < 1 || d2 > 31 || m < 0 || m > 11 || y2 > 9999 || y2 < 0)
      return /* @__PURE__ */ new Date(NaN);
    if (y2 < 100) {
      let fullYear = (/* @__PURE__ */ new Date()).getFullYear();
      let shortYearCutoff = fullYear % 100 + 10;
      y2 += fullYear - fullYear % 100 + (y2 <= shortYearCutoff ? 0 : -100);
    }
    return new Date(y2, m, d2);
  } else if (dArray.length == 1) {
    try {
      return new Date(dArray[0]);
    } catch (e2) {
      return /* @__PURE__ */ new Date(NaN);
    }
  }
  return /* @__PURE__ */ new Date(NaN);
}
__name(parseDate, "parseDate");
function splitDateString(s2) {
  s2 = s2?.trim();
  if (!s2?.length)
    return null;
  if (s2.indexOf("/") >= 0)
    return s2.split("/");
  else if (s2.indexOf(".") >= 0)
    return s2.split(".");
  else if (s2.indexOf("-") >= 0)
    return s2.split("-");
  else if (s2.indexOf("\\") >= 0)
    return s2.split("\\");
  else
    return [s2];
}
__name(splitDateString, "splitDateString");

// src/base/errorhandling.tsx
var ErrorHandling;
((ErrorHandling2) => {
  function showServiceError(error, errorInfo, errorMode) {
    const showMessage = errorMode == "notification" ? notifyError : alertDialog;
    if (error || !errorInfo) {
      showMessage(error?.Message ?? error?.Code ?? localText("Services.GenericErrorMessage", "An error occurred while processing your request."));
      return;
    }
    const seeBrowserConsole = !ErrorHandling2.isDevelopmentMode() ? " " + localText("Services.SeeBrowserConsole", "See browser console (F12) for more information.") : "";
    if (!errorInfo.responseText) {
      if (!errorInfo.status) {
        if (errorInfo.statusText != "abort")
          showMessage(localText("Services.UnknownConnectionError", "An error occured while connecting to the server.") + seeBrowserConsole);
      } else if (errorInfo.status == 500)
        showMessage(localText("Services.InternalServerError", "Internal Server Error (500).") + seeBrowserConsole);
      else
        showMessage(stringFormat(localText("Services.HttpError", "HTTP Error {0}."), errorInfo.status) + seeBrowserConsole);
    } else if (errorMode == "notification")
      notifyError(errorInfo.responseText);
    else
      iframeDialog({ html: errorInfo.responseText });
  }
  ErrorHandling2.showServiceError = showServiceError;
  __name(showServiceError, "showServiceError");
  function runtimeErrorHandler(message, filename, lineno, colno, error) {
    try {
      if (!ErrorHandling2.isDevelopmentMode())
        return;
      const errorInfo = error?.stack ?? error?.toString();
      const msg = jsx("div", { children: [
        jsx("p", { children: [
          "Message: ",
          message
        ] }),
        jsx("p", { children: [
          "File: ",
          filename
        ] }),
        jsx("p", { children: [
          "Line: ",
          lineno,
          ", Column: ",
          colno
        ] }),
        errorInfo && jsx("p", { children: errorInfo })
      ] });
      window.setTimeout(function() {
        try {
          notifyError(msg, "SCRIPT ERROR! See browser console (F12) for details.", {
            timeOut: 15e3
          });
        } catch {
        }
      }, 0);
    } catch {
    }
  }
  ErrorHandling2.runtimeErrorHandler = runtimeErrorHandler;
  __name(runtimeErrorHandler, "runtimeErrorHandler");
  function isDevelopmentMode() {
    const hostname = (window.location.hostname ?? "").toLowerCase();
    return hostname === "localhost" || hostname === "127.0.0.1" || hostname === "[::1]" || hostname.endsWith(".local") || hostname.endsWith(".localhost");
  }
  ErrorHandling2.isDevelopmentMode = isDevelopmentMode;
  __name(isDevelopmentMode, "isDevelopmentMode");
  function unhandledRejectionHandler(err) {
    try {
      if (!err || !err.reason)
        return;
      const reason2 = err.reason;
      if (reason2.origin == "serviceCall") {
        err.preventDefault();
        if (!reason2.silent && (reason2.kind ?? "exception") === "exception") {
          console.error(err);
        }
      }
    } catch {
    }
  }
  ErrorHandling2.unhandledRejectionHandler = unhandledRejectionHandler;
  __name(unhandledRejectionHandler, "unhandledRejectionHandler");
})(ErrorHandling || (ErrorHandling = {}));

// src/base/services.ts
function resolveUrl(url) {
  if (url != null && url.charAt(0) == "~" && url.charAt(1) == "/")
    return Config.applicationPath + url.substring(2);
  return url;
}
__name(resolveUrl, "resolveUrl");
function resolveServiceUrl(url) {
  if (url && url.length && url.charAt(0) != "~" && url.charAt(0) != "/" && url.indexOf("://") < 0)
    return resolveUrl("~/Services/") + url;
  return resolveUrl(url);
}
__name(resolveServiceUrl, "resolveServiceUrl");
function getCookie(name) {
  let $2 = getjQuery();
  if (typeof $2?.cookie === "function")
    return $2.cookie(name);
  name += "=";
  for (let ca = document.cookie.split(/;\s*/), i2 = ca.length - 1; i2 >= 0; i2--)
    if (!ca[i2].indexOf(name))
      return ca[i2].replace(name, "");
}
__name(getCookie, "getCookie");
function isSameOrigin(url) {
  const loc = window.location;
  try {
    const u2 = new URL(url, loc.origin);
    return u2.hostname == loc.hostname && u2.port == loc.port && u2.protocol == loc.protocol;
  } catch {
    return false;
  }
}
__name(isSameOrigin, "isSameOrigin");
function getServiceOptions(options) {
  options = Object.assign({
    allowRedirect: true,
    async: true,
    blockUI: true,
    method: "POST"
  }, options);
  const url = options.url = options.service ? resolveServiceUrl(options.service) : resolveUrl(options.url);
  delete options.service;
  options.headers ??= {};
  options.headers["Accept"] ??= "application/json";
  options.headers["Content-Type"] ??= "application/json";
  if (isSameOrigin(url)) {
    const token = getCookie("CSRF-TOKEN");
    if (token)
      options.headers["X-CSRF-TOKEN"] = token;
  }
  return options;
}
__name(getServiceOptions, "getServiceOptions");
var activeRequests = 0;
function requestStarting() {
  activeRequests++;
  let $2 = getjQuery();
  if ($2 && typeof $2.active === "number") {
    $2.active++ === 0 && $2.event?.trigger?.("ajaxStart");
  } else if (activeRequests === 1) {
    typeof document !== "undefined" && document.dispatchEvent(new Event("ajaxStart"));
  }
}
__name(requestStarting, "requestStarting");
function requestFinished() {
  activeRequests--;
  let $2 = getjQuery();
  if ($2 && typeof $2.active === "number") {
    !--$2.active && $2.event?.trigger?.("ajaxStop");
  } else if (!activeRequests) {
    typeof document !== "undefined" && document.dispatchEvent(new Event("ajaxStop"));
  }
}
__name(requestFinished, "requestFinished");
function getActiveRequests() {
  return activeRequests;
}
__name(getActiveRequests, "getActiveRequests");
function serviceFetch(options) {
  if (typeof fetch !== "function")
    return Promise.reject(reason("The fetch method is not available!", "fetch-missing"));
  return (async function() {
    options = getServiceOptions(options);
    const url = options.url;
    requestStarting();
    try {
      options.blockUI && blockUI();
      try {
        let {
          allowRedirect: _1,
          async: _2,
          blockUI: _3,
          request: _4,
          service: _5,
          url: _6,
          onCleanup: _7,
          onError: _8,
          onSuccess: _9,
          ...fetchInit
        } = options;
        fetchInit.body = JSON.stringify(options.request);
        let fetchResponse;
        try {
          fetchResponse = fetchResponse = await fetch(url, fetchInit);
        } catch (ex) {
          if (ex.name === "AbortError") {
            return Promise.reject(reason(
              `Service fetch to '${url}' was aborted!`,
              "abort",
              { cause: ex, url }
            ));
          }
          throw ex;
        }
        if (!fetchResponse.ok) {
          await handleFetchError(fetchResponse, options);
          return Promise.reject(reason(
            `Service fetch to '${url}' resulted in HTTP ${fetchResponse.status} error: ${fetchResponse.statusText}!`,
            "http-error",
            { fetchResponse, url }
          ));
        }
        let response = await fetchResponse.json();
        if (!response)
          return Promise.reject(reason(
            `Received empty response from service fetch to '${url}'!`,
            "empty-response",
            { fetchResponse, url }
          ));
        if (response.Error) {
          handleError(response ?? {}, { status: fetchResponse.status, statusText: fetchResponse.statusText }, options);
          return Promise.reject(reason(
            `Service fetch to '${url}' resulted in error: ${response.Error.Message ?? response.Error.Code}!`,
            "service-error",
            { response, fetchResponse, url }
          ));
        }
        options.onSuccess?.(response);
        return response;
      } finally {
        options.blockUI && blockUndo();
        options.onCleanup && options.onCleanup();
      }
    } finally {
      requestFinished();
    }
  })();
}
__name(serviceFetch, "serviceFetch");
function reason(message, kind, extra) {
  let error;
  if (extra?.cause != null) {
    error = Error(message, { cause: extra.cause });
  } else {
    error = Error(message);
  }
  if (kind != null) {
    error.kind = kind;
  }
  error.origin = "serviceCall";
  if (extra != null) {
    if (error.cause)
      delete extra.cause;
    Object.assign(error, extra);
  }
  return error;
}
__name(reason, "reason");
function serviceCall(options) {
  if (options?.async ?? true)
    return serviceFetch(options);
  let url;
  return new Promise((resolve, reject) => {
    try {
      options = getServiceOptions(options);
      url = options.url;
      const xhr = new XMLHttpRequest();
      xhr.open(options.method, url, false);
      if (options.cache == "no-store")
        options.headers["Cache-Control"] ??= "no-cache, no-store, max-age=0";
      else if (options.cache === "no-cache")
        options.headers["Cache-Control"] ??= "no-cache";
      for (const x in options.headers) {
        xhr.setRequestHeader(x, options.headers[x]);
      }
      requestStarting();
      try {
        if (options.signal) {
          options.signal.addEventListener("abort", () => {
            xhr.abort();
          }, { once: true });
        }
        xhr.send(JSON.stringify(options.request));
        try {
          if (xhr.status !== 200) {
            handleXHRError(xhr, options);
            return reject(reason(
              `Service call to '${url}' resulted in HTTP ${xhr.status} error: ${xhr.statusText}!`,
              "http-error",
              { status: xhr.status, statusText: xhr.statusText, url }
            ));
          }
          let response = JSON.parse(xhr.responseText);
          if (!response)
            return reject(reason(
              `Empty response received on service call to '${url}'!`,
              "empty-response",
              { url }
            ));
          if (response.Error) {
            handleError(response, { status: xhr.status, statusText: xhr.statusText }, options);
            return reject(reason(
              `Service call to '${url}' resulted in error: ${response.Error.Message ?? response.Error.Code}!`,
              "service-error",
              { response, url }
            ));
          }
          options.onSuccess?.(response);
          return resolve(response);
        } finally {
          options.blockUI && blockUndo();
          options.onCleanup && options.onCleanup();
        }
      } finally {
        requestFinished();
      }
    } catch (exception) {
      reject(reason(
        `Service call to '${url}' thrown exception: ${exception.toString()}`,
        "exception",
        { cause: exception, url }
      ));
    }
  });
}
__name(serviceCall, "serviceCall");
function serviceRequest(service, request, onSuccess, options) {
  return serviceCall(Object.assign({
    service,
    request,
    onSuccess
  }, options));
}
__name(serviceRequest, "serviceRequest");
function handleError(response, errorInfo, options) {
  if (Config.notLoggedInHandler != null && response && response.Error && response.Error.Code == "NotLoggedIn" && Config.notLoggedInHandler(options, response)) {
    return;
  }
  if (options?.onError?.(response, errorInfo) || options?.errorMode === "none")
    return;
  ErrorHandling.showServiceError(response?.Error, errorInfo, options?.errorMode);
}
__name(handleError, "handleError");
function handleRedirect(getHeader) {
  let l2 = null;
  try {
    l2 = getHeader("Location");
  } catch ($t1) {
    l2 = null;
  }
  if (l2) {
    window.top.location.href = l2;
    return true;
  }
}
__name(handleRedirect, "handleRedirect");
async function handleFetchError(response, options) {
  if (response.status === 403 && options.allowRedirect && handleRedirect(response.headers.get))
    return;
  if ((response.headers.get("content-type") || "").toLowerCase().indexOf("json") >= 0) {
    const json = await response.json();
    if (json && json.Error) {
      handleError(json, {
        status: response.status,
        statusText: response.statusText
      }, options);
      return;
    }
  }
  handleError(null, {
    status: response.status,
    statusText: response.statusText,
    responseText: await response.text()
  }, options);
}
__name(handleFetchError, "handleFetchError");
function handleXHRError(xhr, options) {
  if (xhr.status === 403 && options.allowRedirect && handleRedirect(xhr.getResponseHeader))
    return;
  if ((xhr.getResponseHeader("content-type") || "").toLowerCase().indexOf("application/json") >= 0) {
    const json = JSON.parse(xhr.responseText);
    if (json && json.Error) {
      handleError(json, { status: xhr.status, statusText: xhr.statusText }, options);
      return;
    }
  }
  handleError(null, { status: xhr.status, statusText: xhr.statusText, responseText: xhr.responseText }, options);
}
__name(handleXHRError, "handleXHRError");

// src/base/scriptdata.ts
function getScriptDataHash(name, reload) {
  let json;
  let scriptDataHash = getGlobalObject()[scriptDataHashSymbol];
  if (!scriptDataHash && typeof document !== "undefined" && (json = (document.querySelector("script#RegisteredScripts")?.innerHTML ?? "").trim()) && json.charAt(0) == "{") {
    const regs = JSON.parse(json);
    getGlobalObject()[scriptDataHashSymbol] = scriptDataHash = {};
    for (const i2 in regs) {
      scriptDataHash[i2] = regs[i2];
    }
  }
  if (!scriptDataHash) {
    if (reload)
      getGlobalObject()[scriptDataHashSymbol] = scriptDataHash = {};
    else
      return null;
  }
  if (reload)
    return scriptDataHash[name] = (/* @__PURE__ */ new Date()).getTime().toString();
  return scriptDataHash[name];
}
__name(getScriptDataHash, "getScriptDataHash");
var scriptDataHooks = {
  /**
   * Provides a hook to override the default fetchScriptData implementation,
   * it falls back to the default implementation if undefined is returned.
   * It is recommended to use this hook mainly for test purposes.
   * If the sync parameter is true (legacy/compat), then the result should be returned synchronously.
   * DynJS parameter is true if the script is requested to be loaded via a dynamic script,
   * and not a JSON request. This parameter is only true for the legacy/compat sync mode.
   */
  fetchScriptData: void 0
};
var fetchPromises = {};
function fetchScriptData(name) {
  const hookResult = scriptDataHooks.fetchScriptData?.(name);
  if (hookResult != void 0) {
    return Promise.resolve(hookResult).then((data) => {
      if (name?.startsWith("Lookup.") && data?.Items && !data.items && !data.itemById)
        return new Lookup(hookResult.Params, hookResult.Items);
      return data;
    });
  }
  let key = name + "?" + (getScriptDataHash(name) ?? "");
  const promise = fetchPromises[key];
  if (promise != null)
    return promise;
  if (typeof fetch === "undefined")
    return Promise.reject("The fetch method is not available!");
  return fetchPromises[key] = (async function() {
    try {
      blockUI();
      try {
        let url = resolveUrl("~/DynamicData/") + name + "?v=" + (getScriptDataHash(name) ?? (/* @__PURE__ */ new Date()).getTime());
        requestStarting();
        try {
          const response = await fetch(url, {
            method: "GET",
            cache: "force-cache",
            headers: {
              "Accept": "application/json"
            }
          });
          if (!response.ok)
            handleScriptDataError(name, response.status, response.statusText ?? "");
          const data = await response.json();
          if (name.startsWith("Lookup."))
            return new Lookup(data.Params, data.Items);
          return data;
        } finally {
          requestFinished();
        }
      } finally {
        blockUndo();
      }
    } finally {
      delete fetchPromises[key];
    }
  })();
}
__name(fetchScriptData, "fetchScriptData");
async function getScriptData(name, reload) {
  let data;
  if (reload) {
    getScriptDataHash(name, true);
    setScriptData(name, void 0);
  } else if ((data = peekScriptData(name)) != null)
    return data;
  data = await fetchScriptData(name);
  setScriptData(name, data);
  return data;
}
__name(getScriptData, "getScriptData");
function ensureScriptDataSync(name, dynJS) {
  let data = peekScriptData(name);
  if (data != null)
    return data;
  data = scriptDataHooks.fetchScriptData?.(name, true, dynJS);
  if (data !== void 0) {
    if (isPromiseLike(data))
      throw new Error("fetchScriptData hook must return data synchronously when sync is true.");
    if (name.startsWith("Lookup.") && data?.Items)
      data = new Lookup(data.Params, data.Items);
    setScriptData(name, data);
    return data;
  }
  const url = resolveUrl(dynJS ? "~/DynJS.axd/" : "~/DynamicData/") + name + (dynJS ? ".js" : "") + "?v=" + (getScriptDataHash(name) ?? (/* @__PURE__ */ new Date()).getTime());
  const xhr = new XMLHttpRequest();
  xhr.open("GET", url, false);
  requestStarting();
  try {
    xhr.send(null);
    if (xhr.status !== 200)
      handleScriptDataError(name, xhr.status, xhr.statusText);
    if (dynJS) {
      const script = document.createElement("script");
      script.text = xhr.responseText;
      document.head.appendChild(script).parentNode.removeChild(script);
      data = peekScriptData(name);
    } else {
      data = JSON.parse(xhr.responseText);
    }
    if (data == null)
      handleScriptDataError(name);
    if (!dynJS && name.startsWith("Lookup."))
      data = new Lookup(data.Params, data.Items);
    setScriptData(name, data);
    return data;
  } finally {
    requestFinished();
  }
}
__name(ensureScriptDataSync, "ensureScriptDataSync");
function getColumnsScript(key) {
  return getScriptData("Columns." + key);
}
__name(getColumnsScript, "getColumnsScript");
function getFormScript(key) {
  return getScriptData("Form." + key);
}
__name(getFormScript, "getFormScript");
function getLookupAsync(key) {
  return getScriptData("Lookup." + key);
}
__name(getLookupAsync, "getLookupAsync");
function getRemoteDataAsync(key) {
  return getScriptData("RemoteData." + key);
}
__name(getRemoteDataAsync, "getRemoteDataAsync");
function getRemoteData(key) {
  return ensureScriptDataSync("RemoteData." + key);
}
__name(getRemoteData, "getRemoteData");
function handleScriptDataError(name, status, statusText, shouldThrow = true) {
  const isLookup = name?.startsWith("Lookup.");
  let message;
  if (status == null && statusText == null || status === 404) {
    if (isLookup)
      message = 'No lookup with key "' + name.substring(7) + '" is registered. Please make sure you have a [LookupScript("' + name.substring(7) + '")] attribute in server side code on top of a row / custom lookup and its key is exactly the same.';
    else
      message = `Cannot load dynamic data: ${name}!`;
    notifyError(message);
  } else if (status == 403 && isLookup) {
    message = 'Access denied while trying to load the lookup: "' + htmlEncode(name.substring(7)) + '". Please check if current user has required permissions for this lookup.\n\nLookups use the ReadPermission of their row by default. You may override that for the lookup like [LookupScript("Some.Lookup", Permission = "?")] to grant all authenticated users to read it (or use "*" for public).\n\nNote that this might be a security risk if the lookup contains sensitive data, so it could be better to set a separate permission for lookups, like "MyModule:Lookups".';
    notifyError(message, null, {
      timeOut: 1e4
    });
  } else {
    message = "An error occurred while trying to load" + (isLookup ? ' the lookup: "' + name.substring(7) : ' dynamic data: "' + name) + '"!. Please check the error message displayed in the console for more info.';
    notifyError(message);
    if (!status)
      console.log("An unknown connection error occurred!");
    else if (status == 500)
      console.log("HTTP 500: Connection refused!");
    else
      console.log("HTTP " + status + ": " + statusText);
  }
  if (shouldThrow)
    throw message;
  return message;
}
__name(handleScriptDataError, "handleScriptDataError");
function peekScriptData(name) {
  return getGlobalObject()[scriptDataSymbol]?.[name];
}
__name(peekScriptData, "peekScriptData");
async function reloadLookupAsync(key) {
  return await getScriptData("Lookup." + key, true);
}
__name(reloadLookupAsync, "reloadLookupAsync");
function setRegisteredScripts(scripts) {
  const t2 = (/* @__PURE__ */ new Date()).getTime().toString();
  let scriptDataHash = getGlobalObject()[scriptDataHashSymbol];
  if (!scriptDataHash)
    getGlobalObject()[scriptDataHashSymbol] = scriptDataHash = {};
  for (const k in scripts) {
    scriptDataHash[k] = scripts[k] || t2;
  }
}
__name(setRegisteredScripts, "setRegisteredScripts");
function setScriptData(name, value) {
  let scriptDataStore = getGlobalObject()[scriptDataSymbol];
  if (!scriptDataStore)
    getGlobalObject()[scriptDataSymbol] = scriptDataStore = {};
  scriptDataStore[name] = value;
  typeof document !== "undefined" && document.dispatchEvent?.(new Event("scriptdatachange." + name));
}
__name(setScriptData, "setScriptData");

// src/base/authorization.ts
var andOrRegex = /[|&]/;
var Authorization;
((Authorization2) => {
  function hasPermission(permission) {
    if (permission == null)
      return false;
    if (permission == "*")
      return true;
    if (permission == "" || permission == "?")
      return !!Authorization2.isLoggedIn;
    const ud = Authorization2.userDefinition;
    if (!ud)
      return false;
    if (ud.IsAdmin)
      return true;
    return isPermissionInSet(ud.Permissions, permission);
  }
  Authorization2.hasPermission = hasPermission;
  __name(hasPermission, "hasPermission");
  async function hasPermissionAsync(permission) {
    if (permission == null)
      return false;
    if (permission == "*")
      return true;
    if (permission == "" || permission == "?")
      return await Authorization2.isLoggedInAsync;
    const ud = await Authorization2.userDefinitionAsync;
    if (!ud)
      return false;
    if (ud.IsAdmin)
      return true;
    return isPermissionInSet(ud.Permissions, permission);
  }
  Authorization2.hasPermissionAsync = hasPermissionAsync;
  __name(hasPermissionAsync, "hasPermissionAsync");
  function isPermissionInSet(permissionSet, permission) {
    if (!permissionSet || permission == null)
      return false;
    if (permissionSet[permission])
      return true;
    if (!andOrRegex.test(permission))
      return false;
    const orParts = permission.split("|");
    for (const r2 of orParts) {
      if (!r2.length)
        continue;
      const andParts = r2.split("&");
      let anyFalse = false;
      for (const n2 of andParts) {
        if (!n2 || !permissionSet[n2]) {
          anyFalse = true;
          break;
        }
      }
      if (!anyFalse)
        return true;
    }
    return false;
  }
  Authorization2.isPermissionInSet = isPermissionInSet;
  __name(isPermissionInSet, "isPermissionInSet");
  function validatePermission(permission) {
    if (!hasPermission(permission)) {
      notifyError(localText("Authorization.AccessDenied"));
      throw new Error(localText("Authorization.AccessDenied"));
    }
  }
  Authorization2.validatePermission = validatePermission;
  __name(validatePermission, "validatePermission");
  async function validatePermissionAsync(permission) {
    if (!await hasPermissionAsync(permission)) {
      notifyError(localText("Authorization.AccessDenied"));
      throw new Error(localText("Authorization.AccessDenied"));
    }
  }
  Authorization2.validatePermissionAsync = validatePermissionAsync;
  __name(validatePermissionAsync, "validatePermissionAsync");
})(Authorization || (Authorization = {}));
Object.defineProperty(Authorization, "isLoggedIn", {
  get: /* @__PURE__ */ __name(function() {
    return !!Authorization.userDefinition?.Username;
  }, "get"),
  configurable: true
});
Object.defineProperty(Authorization, "isLoggedInAsync", {
  get: /* @__PURE__ */ __name(async function() {
    return !!(await Authorization.userDefinitionAsync)?.Username;
  }, "get"),
  configurable: true
});
Object.defineProperty(Authorization, "userDefinition", {
  get: /* @__PURE__ */ __name(function() {
    return getRemoteData("UserData");
  }, "get"),
  configurable: true
});
Object.defineProperty(Authorization, "userDefinitionAsync", {
  get: /* @__PURE__ */ __name(async function() {
    return await getRemoteDataAsync("UserData");
  }, "get"),
  configurable: true
});
Object.defineProperty(Authorization, "username", {
  get: /* @__PURE__ */ __name(function() {
    return Authorization.userDefinition?.Username;
  }, "get"),
  configurable: true
});
Object.defineProperty(Authorization, "usernameAsync", {
  get: /* @__PURE__ */ __name(async function() {
    return (await Authorization.userDefinitionAsync)?.Username;
  }, "get"),
  configurable: true
});

// src/base/criteria.ts
var CriteriaBuilder = class extends Array {
  static {
    __name(this, "CriteriaBuilder");
  }
  /**
   * Creates a between criteria.
   * @param fromInclusive from value
   * @param toInclusive to value
   */
  bw(fromInclusive, toInclusive) {
    return [[this, ">=", fromInclusive], "and", [this, "<=", toInclusive]];
  }
  /**
   * Creates a contains criteria
   * @param value contains value
   */
  contains(value) {
    return [this, "like", "%" + value + "%"];
  }
  /**
   * Creates a endsWith criteria
   * @param value endsWith value
   */
  endsWith(value) {
    return [this, "like", "%" + value];
  }
  /**
   * Creates an equal (=) criteria
   * @param value equal value
   */
  eq(value) {
    return [this, "=", value];
  }
  /**
   * Creates a greater than criteria
   * @param value greater than value
   */
  gt(value) {
    return [this, ">", value];
  }
  /**
   * Creates a greater than or equal criteria
   * @param value greater than or equal value
   */
  ge(value) {
    return [this, ">=", value];
  }
  /**
   * Creates a in criteria
   * @param values in values
   */
  in(values) {
    return [this, "in", [values]];
  }
  /**
   * Creates a IS NULL criteria
   */
  isNull() {
    return ["is null", this];
  }
  /**
   * Creates a IS NOT NULL criteria
   */
  isNotNull() {
    return ["is not null", this];
  }
  /**
   * Creates a less than or equal to criteria
   * @param value less than or equal to value
   */
  le(value) {
    return [this, "<=", value];
  }
  /**
   * Creates a less than criteria
   * @param value less than value
   */
  lt(value) {
    return [this, "<", value];
  }
  /**
   * Creates a not equal criteria
   * @param value not equal value
   */
  ne(value) {
    return [this, "!=", value];
  }
  /**
   * Creates a LIKE criteria
   * @param value like value
   */
  like(value) {
    return [this, "like", value];
  }
  /**
   * Creates a STARTS WITH criteria
   * @param value startsWith value
   */
  startsWith(value) {
    return [this, "like", value + "%"];
  }
  /**
   * Creates a NOT IN criteria
   * @param values array of NOT IN values
   */
  notIn(values) {
    return [this, "not in", [values]];
  }
  /**
   * Creates a NOT LIKE criteria
   * @param value not like value
   */
  notLike(value) {
    return [this, "not like", value];
  }
};
var TOKEN_IDENTIFIER = 1;
var TOKEN_OPERATOR = 2;
var TOKEN_VALUE = 3;
var TOKEN_PARAM = 4;
var ParseError = class extends Error {
  static {
    __name(this, "ParseError");
  }
  constructor(expression, error, position) {
    super('Error parsing expression: "' + expression + '", "' + error + ", position: " + position);
    this.expression = expression;
    this.error = error;
    this.position = position;
  }
};
function tokenize(expression) {
  let end, v2;
  const tokens = [];
  const l2 = expression.length;
  const l1 = expression.length - 1;
  let openParens = 0;
  let index;
  let ch;
  function skipWhiteSpace() {
    while (index < l2) {
      ch = expression.charAt(index);
      if (ch === " " || ch === "	") {
        index++;
        continue;
      }
      break;
    }
  }
  __name(skipWhiteSpace, "skipWhiteSpace");
  function readString() {
    end = index;
    let foundDoubles = false;
    while (end++ < l1) {
      ch = expression.charAt(end);
      if (ch === "'") {
        if (end < l1 && expression.charAt(end + 1) === "'") {
          end++;
          foundDoubles = true;
          continue;
        } else
          break;
      }
    }
    if (end === index || expression.charAt(end) !== "'")
      throw new ParseError(expression, "unterminated string", index);
    v2 = expression.substring(index + 1, end);
    if (foundDoubles)
      v2 = v2.replace(/''/g, "'");
  }
  __name(readString, "readString");
  function readNumber() {
    end = index;
    let foundDot = false;
    while (end < l1) {
      ch = expression.charAt(end + 1);
      if (ch >= "0" && ch <= "9" || !foundDot && ch === ".") {
        if (ch === ".")
          foundDot = true;
        end++;
      } else
        break;
    }
    v2 = parseFloat(expression.substring(index, end + 1));
  }
  __name(readNumber, "readNumber");
  function readIdentifier() {
    end = index;
    while (end < l1) {
      ch = expression.charAt(end + 1);
      if (ch == "_" || ch >= "A" && ch <= "Z" || ch >= "a" && ch <= "z" || ch >= "0" && ch <= "9") {
        end++;
      } else
        break;
    }
    v2 = expression.substring(index, end + 1);
  }
  __name(readIdentifier, "readIdentifier");
  function readParam() {
    index++;
    readIdentifier();
    if (!v2.length)
      throw new ParseError(expression, "expected parameter name", index);
  }
  __name(readParam, "readParam");
  function readArrayList() {
    skipWhiteSpace();
    if (index >= l2 || expression.charAt(index) != "(") {
      if (index < l2 && expression.charAt(index) == "@") {
        readParam();
        index = end;
        return;
      }
      throw new ParseError(expression, "expected parenthesis", index);
    }
    index++;
    let values = [];
    while (true) {
      skipWhiteSpace();
      if (index >= l2)
        throw new ParseError(expression, "expected parenthesis", index);
      ch = expression.charAt(index);
      if (ch == ",") {
        if (values.length == 0)
          throw new ParseError(expression, "unexpected comma", index);
        index++;
        skipWhiteSpace();
        ch = expression.charAt(index);
      } else if (ch == ")") {
        break;
      } else if (values.length > 0)
        throw new ParseError(expression, "expected comma", index);
      if (ch === "'") {
        readString();
        values.push(v2);
        index = end + 1;
        continue;
      }
      if (ch >= "0" && ch <= "9") {
        readNumber();
        values.push(v2);
        index = end + 1;
        continue;
      }
      if (ch == "n") {
        readIdentifier();
        if (v2 === "null") {
          values.push(null);
          index = end + 1;
          continue;
        }
      }
      throw new ParseError(expression, "unexpected token", index);
    }
    v2 = values;
  }
  __name(readArrayList, "readArrayList");
  for (index = 0; index < l2; index++) {
    ch = expression.charAt(index);
    if (ch === " " || ch === "	")
      continue;
    if (ch == "_" || ch >= "A" && ch <= "Z" || ch >= "a" && ch <= "z") {
      readIdentifier();
      let w2 = v2.toLowerCase();
      if (w2 == "is") {
        index = end + 1;
        skipWhiteSpace();
        readIdentifier();
        w2 = v2.toLowerCase();
        if (w2 === "not") {
          index = end + 1;
          skipWhiteSpace();
          readIdentifier();
          if (v2.toLowerCase() === "null") {
            tokens.push({
              t: TOKEN_OPERATOR,
              v: "is not null"
            });
          } else
            throw new ParseError(expression, 'expected "null"', index);
        } else if (w2 === "null") {
          tokens.push({
            t: TOKEN_OPERATOR,
            v: "is null"
          });
        } else
          throw new ParseError(expression, 'expected "null" or "not" keyword', index);
      } else if (w2 === "and" || w2 === "or" || w2 === "xor") {
        tokens.push({
          t: TOKEN_OPERATOR,
          v: w2
        });
      } else if (w2 === "not") {
        const currentEnd = end;
        index = end + 1;
        skipWhiteSpace();
        readIdentifier();
        w2 = v2.toLowerCase();
        if (w2 === "in") {
          index = end + 1;
          tokens.push({
            t: TOKEN_OPERATOR,
            v: "not in"
          });
          readArrayList();
          if (typeof v2 === "string") {
            tokens.push({
              t: TOKEN_PARAM,
              v: v2
            });
          } else {
            tokens.push({
              t: TOKEN_VALUE,
              v: v2
            });
          }
        } else if (w2 === "like") {
          tokens.push({
            t: TOKEN_OPERATOR,
            v: "not like"
          });
          index = end;
        } else {
          tokens.push({
            t: TOKEN_OPERATOR,
            v: "not"
          });
          index = currentEnd;
        }
        continue;
      } else if (w2 === "in") {
        tokens.push({
          t: TOKEN_OPERATOR,
          v: "in"
        });
        index = end + 1;
        readArrayList();
        if (typeof v2 === "string") {
          tokens.push({
            t: TOKEN_PARAM,
            v: v2
          });
        } else {
          tokens.push({
            t: TOKEN_VALUE,
            v: v2
          });
        }
        continue;
      } else if (w2 === "like") {
        tokens.push({
          t: TOKEN_OPERATOR,
          v: "like"
        });
      } else {
        tokens.push({
          t: TOKEN_IDENTIFIER,
          v: v2
        });
      }
      index = end;
      continue;
    }
    if (ch === "@") {
      readParam();
      tokens.push({
        t: TOKEN_PARAM,
        v: v2
      });
      index = end;
      continue;
    }
    if ((ch === "-" || ch === "+") && index < l1 && expression.charAt(index + 1) >= "0" && expression.charAt(index + 1) <= "9" || ch >= "0" && ch <= "9") {
      end = index;
      readNumber();
      tokens.push({
        t: TOKEN_VALUE,
        v: v2
      });
      index = end;
      continue;
    }
    if (ch === "'") {
      end = index;
      readString();
      tokens.push({
        t: TOKEN_VALUE,
        v: v2
      });
      index = end;
      continue;
    }
    if (ch === "=") {
      tokens.push({
        t: TOKEN_OPERATOR,
        v: ch
      });
      continue;
    }
    if (ch === "(") {
      openParens++;
      tokens.push({
        t: TOKEN_OPERATOR,
        v: ch
      });
      continue;
    }
    if (ch == ")") {
      if (openParens <= 0)
        throw new ParseError(expression, "unexpected parenthesis", index);
      openParens--;
      tokens.push({
        t: TOKEN_OPERATOR,
        v: ch
      });
      continue;
    }
    if (ch === ">" || ch === "<") {
      if (index < l1 && expression.charAt(index + 1) === "=") {
        tokens.push({
          t: TOKEN_OPERATOR,
          v: ch + "="
        });
        index++;
      } else {
        tokens.push({
          t: TOKEN_OPERATOR,
          v: ch
        });
      }
      continue;
    }
    throw new ParseError(expression, "unknown token", index);
  }
  if (openParens > 0)
    throw new ParseError(expression, "missing parenthesis", index);
  return tokens;
}
__name(tokenize, "tokenize");
var operatorPrecedence = {
  "=": 4,
  ">": 4,
  "<": 4,
  ">=": 4,
  "<=": 4,
  "<>": 4,
  "!=": 4,
  "like": 5,
  "not like": 5,
  "in": 5,
  "not in": 5,
  "is null": 5,
  "is not null": 5,
  "not": 6,
  "and": 7,
  "xor": 8,
  "or": 9
};
function shuntingYard(tokens) {
  const result = [];
  const stack = [];
  for (const token of tokens) {
    if (token.t === TOKEN_OPERATOR) {
      const precedence = operatorPrecedence[token.v];
      if (precedence != null) {
        while (stack.length) {
          const prev = stack[stack.length - 1];
          if (prev.t !== TOKEN_OPERATOR || prev.v == "(")
            break;
          const prevPrecedence = operatorPrecedence[prev.v];
          if (prevPrecedence == null || prevPrecedence > precedence)
            break;
          result.push(stack.pop());
        }
        stack.push(token);
      } else if (token.v === "(") {
        stack.push(token);
      } else if (token.v === ")") {
        while (stack.length && (stack[stack.length - 1].t !== TOKEN_OPERATOR || stack[stack.length - 1].v !== "(")) {
          result.push(stack.pop());
        }
        stack.pop();
      } else
        result.push(token);
    } else
      result.push(token);
  }
  while (stack.length) {
    const tok = stack.pop();
    if (tok.t == TOKEN_OPERATOR && (tok.v === "(" || tok.v === ")"))
      throw "Mismatched parentheses in criteria expression!";
    result.push(tok);
  }
  return result;
}
__name(shuntingYard, "shuntingYard");
function rpnTokensToCriteria(rpnTokens, getParam) {
  const stack = [];
  for (const token of rpnTokens) {
    switch (token.t) {
      case TOKEN_IDENTIFIER: {
        stack.push([token.v]);
        break;
      }
      case TOKEN_VALUE: {
        stack.push(Array.isArray(token.v) ? [token.v] : token.v);
        break;
      }
      case TOKEN_PARAM: {
        if (!getParam)
          throw new Error("getParam must be passed for parameterized expressions!");
        const prm = getParam(token.v);
        stack.push(Array.isArray(prm) ? [prm] : prm);
        break;
      }
      case TOKEN_OPERATOR: {
        switch (token.v) {
          case "not":
          case "is null":
          case "is not null":
            if (!stack.length)
              throw new Error(`Unary operator "${token.v}" requires a value!`);
            stack.push([token.v, stack.pop()]);
            break;
          default:
            if (stack.length < 2)
              throw new Error(`Binary operator "${token.v}" requires two values!`);
            const r2 = stack.pop();
            const l2 = stack.pop();
            stack.push([l2, token.v, r2]);
            break;
        }
        break;
      }
      default:
        throw new Error("Invalid operator type: " + token.t + "!");
    }
  }
  if (stack.length != 1)
    throw new Error("Error evaluating expression!");
  return stack.pop();
}
__name(rpnTokensToCriteria, "rpnTokensToCriteria");
function internalParse(expression, getParam) {
  const tokens = tokenize(expression);
  const rpnTokens = shuntingYard(tokens);
  return rpnTokensToCriteria(rpnTokens, getParam);
}
__name(internalParse, "internalParse");
function parseCriteria(exprOrStrings, ...values) {
  if (!exprOrStrings?.length)
    return [];
  if (typeof exprOrStrings === "string") {
    return internalParse(
      exprOrStrings,
      values == null || values[0] == null ? null : (name) => values[0][name]
    );
  } else if (!values.length)
    return internalParse(exprOrStrings.join(""));
  const expression = String.raw({ raw: exprOrStrings }, ...values.map((x, i2) => "@__" + i2));
  return internalParse(expression, (name) => name.startsWith("__") ? values[parseInt(name.substring(2), 10)] : void 0);
}
__name(parseCriteria, "parseCriteria");
var CriteriaOperator = /* @__PURE__ */ ((CriteriaOperator2) => {
  CriteriaOperator2["paren"] = "()";
  CriteriaOperator2["not"] = "not";
  CriteriaOperator2["isNull"] = "is null";
  CriteriaOperator2["isNotNull"] = "is not null";
  CriteriaOperator2["exists"] = "exists";
  CriteriaOperator2["and"] = "and";
  CriteriaOperator2["or"] = "or";
  CriteriaOperator2["xor"] = "xor";
  CriteriaOperator2["eq"] = "=";
  CriteriaOperator2["ne"] = "!=";
  CriteriaOperator2["gt"] = ">";
  CriteriaOperator2["ge"] = ">=";
  CriteriaOperator2["lt"] = "<";
  CriteriaOperator2["le"] = "<=";
  CriteriaOperator2["in"] = "in";
  CriteriaOperator2["notIn"] = "not in";
  CriteriaOperator2["like"] = "like";
  CriteriaOperator2["notLike"] = "not like";
  return CriteriaOperator2;
})(CriteriaOperator || {});
function Criteria(field) {
  const builder = CriteriaBuilder.of(field);
  !builder.eq && (builder.__proto__ = CriteriaBuilder.prototype);
  return builder;
}
__name(Criteria, "Criteria");
Criteria.and = /* @__PURE__ */ __name(function and(c1, c2, ...rest) {
  let result = Criteria.join(c1, "and", c2);
  if (rest) {
    for (const k of rest)
      result = Criteria.join(result, "and", k);
  }
  return result;
}, "and");
Criteria.Operator = CriteriaOperator;
Criteria.isEmpty = /* @__PURE__ */ __name(function isEmpty(c2) {
  return c2 == null || c2.length === 0 || c2.length === 1 && typeof c2[0] === "string" && c2[0].length === 0;
}, "isEmpty");
Criteria.join = /* @__PURE__ */ __name(function join(c1, op, c2) {
  if (Criteria.isEmpty(c1))
    return c2;
  if (Criteria.isEmpty(c2))
    return c1;
  return [c1, op, c2];
}, "join");
Criteria.not = /* @__PURE__ */ __name(function not(c2) {
  return ["not", c2];
}, "not");
Criteria.or = /* @__PURE__ */ __name(function or(c1, c2, ...rest) {
  let result = Criteria.join(c1, "or", c2);
  if (rest) {
    for (const k of rest)
      result = Criteria.join(result, "or", k);
  }
  return result;
}, "or");
Criteria.paren = /* @__PURE__ */ __name(function parent(c2) {
  return Criteria.isEmpty(c2) ? c2 : ["()", c2];
}, "parent");
Criteria.parse = parseCriteria;

// src/base/debounce.ts
function debounce(func, wait, immediate) {
  let timeout, args, context, timestamp, result;
  if (null == wait) wait = 100;
  const later = /* @__PURE__ */ __name(function() {
    const last = Date.now() - timestamp;
    if (last < wait && last >= 0) {
      timeout = setTimeout(later, wait - last);
    } else {
      timeout = null;
      if (!immediate)
        result = func.apply(context, args);
      if (!timeout)
        context = args = null;
    }
  }, "later");
  const debounced = /* @__PURE__ */ __name(function() {
    context = this;
    args = arguments;
    timestamp = Date.now();
    if (!timeout) {
      timeout = setTimeout(later, wait);
      if (immediate)
        result = func.apply(context, args);
    }
    return result;
  }, "debounced");
  debounced.clear = function() {
    if (timeout) {
      clearTimeout(timeout);
      timeout = null;
    }
  };
  debounced.flush = function() {
    if (timeout) {
      result = func.apply(context, args);
      context = args = null;
      clearTimeout(timeout);
      timeout = null;
    }
  };
  return debounced;
}
__name(debounce, "debounce");

// src/base/propertyitem.ts
var SummaryType = /* @__PURE__ */ ((SummaryType3) => {
  SummaryType3[SummaryType3["Disabled"] = -1] = "Disabled";
  SummaryType3[SummaryType3["None"] = 0] = "None";
  SummaryType3[SummaryType3["Sum"] = 1] = "Sum";
  SummaryType3[SummaryType3["Avg"] = 2] = "Avg";
  SummaryType3[SummaryType3["Min"] = 3] = "Min";
  SummaryType3[SummaryType3["Max"] = 4] = "Max";
  return SummaryType3;
})(SummaryType || {});
registerEnum(SummaryType, "Serenity.SummaryType");

// src/base/servicetypes.ts
var ColumnSelection = /* @__PURE__ */ ((ColumnSelection2) => {
  ColumnSelection2[ColumnSelection2["List"] = 0] = "List";
  ColumnSelection2[ColumnSelection2["KeyOnly"] = 1] = "KeyOnly";
  ColumnSelection2[ColumnSelection2["Details"] = 2] = "Details";
  ColumnSelection2[ColumnSelection2["None"] = 3] = "None";
  ColumnSelection2[ColumnSelection2["IdOnly"] = 4] = "IdOnly";
  ColumnSelection2[ColumnSelection2["Lookup"] = 5] = "Lookup";
  return ColumnSelection2;
})(ColumnSelection || {});
var RetrieveColumnSelection = /* @__PURE__ */ ((RetrieveColumnSelection2) => {
  RetrieveColumnSelection2[RetrieveColumnSelection2["details"] = 0] = "details";
  RetrieveColumnSelection2[RetrieveColumnSelection2["keyOnly"] = 1] = "keyOnly";
  RetrieveColumnSelection2[RetrieveColumnSelection2["list"] = 2] = "list";
  RetrieveColumnSelection2[RetrieveColumnSelection2["none"] = 3] = "none";
  RetrieveColumnSelection2[RetrieveColumnSelection2["idOnly"] = 4] = "idOnly";
  RetrieveColumnSelection2[RetrieveColumnSelection2["lookup"] = 5] = "lookup";
  return RetrieveColumnSelection2;
})(RetrieveColumnSelection || {});

// src/base/texts/servicestexts.ts
var textsProxy = proxyTexts({}, "", {
  Controls: {
    ImageUpload: {}
  },
  Enums: {
    ImageCheckResult: {}
  },
  Validation: {}
});
var DataValidationTexts = textsProxy.Validation;
var FileUploadTexts = textsProxy.Controls.ImageUpload;
var ImageCheckResultTexts = textsProxy.Enums.ImageCheckResult;

// src/base/texts/webtexts.ts
var textsProxy2 = proxyTexts({}, "", {
  Controls: {
    CheckTreeEditor: {},
    ColumnPickerDialog: {},
    DataGrid: {},
    DateTimeEditor: {},
    EntityDialog: {},
    EntityGrid: {},
    FilterPanel: {
      OperatorFormats: {},
      OperatorNames: {}
    },
    HtmlContentEditor: {},
    Pager: {},
    PropertyGrid: {},
    QuickSearch: {},
    SelectEditor: {}
  },
  Dialogs: {},
  Validation: {}
});
var CheckTreeEditorTexts = textsProxy2.Controls.CheckTreeEditor;
var ColumnPickerDialogTexts = textsProxy2.Controls.ColumnPickerDialog;
var DataGridTexts = textsProxy2.Controls.DataGrid;
var DateTimeEditorTexts = textsProxy2.Controls.DateTimeEditor;
var EntityDialogTexts = textsProxy2.Controls.EntityDialog;
var EntityGridTexts = textsProxy2.Controls.EntityGrid;
var FilterPanelTexts = textsProxy2.Controls.FilterPanel;
var FormValidationTexts = textsProxy2.Validation;
var HtmlContentEditorTexts = textsProxy2.Controls.HtmlContentEditor;
var PagerTexts = textsProxy2.Controls.Pager;
var PropertyGridTexts = textsProxy2.Controls.PropertyGrid;
var QuickSearchTexts = textsProxy2.Controls.QuickSearch;
var SelectEditorTexts = textsProxy2.Controls.SelectEditor;

// src/base/tooltip.ts
var Tooltip = class _Tooltip {
  static {
    __name(this, "Tooltip");
  }
  constructor(el, opt, create = true) {
    this.el = isArrayLike(el) ? el[0] : el;
    if (create === true && this.el) {
      let instance = _Tooltip.getInstance(this.el);
      if (instance) {
        opt?.title !== void 0 && this.setTitle(opt.title);
      } else {
        opt ??= {};
        opt.trigger ??= "hover focus";
        let $2 = getjQuery();
        if ($2?.fn?.tooltip)
          $2(this.el).tooltip(opt);
        else if (typeof bootstrap !== "undefined" && bootstrap.Tooltip)
          new bootstrap.Tooltip(this.el, opt);
        else if (opt?.title !== void 0)
          this.el.setAttribute("title", opt.title);
      }
    }
  }
  static {
    this.defaults = {
      trigger: "click hover"
    };
  }
  dispose() {
    let instance = _Tooltip.existingInstance(this.el);
    if (!instance)
      return;
    instance[isBS3() ? "destroy" : "dispose"]?.();
  }
  delayedDispose(delay = 5e3) {
    setTimeout(bindThis(this).dispose, delay);
  }
  delayedHide(delay = 5e3) {
    setTimeout(bindThis(this).hide, delay);
  }
  static existingInstance(el) {
    let instance;
    if (typeof bootstrap !== "undefined")
      instance = bootstrap?.Tooltip?.getInstance?.(el);
    if (instance)
      return instance;
    const $2 = getjQuery();
    if ($2?.fn?.tooltip)
      return $2(el).data()["bs.tooltip"];
    return null;
  }
  static getInstance(el) {
    let instance = _Tooltip.existingInstance(isArrayLike(el) ? el[0] : el);
    if (!instance)
      return null;
    return new _Tooltip(el, null, false);
  }
  static get isAvailable() {
    return !!(typeof bootstrap !== "undefined" && bootstrap.Tooltip || getjQuery()?.fn?.tooltip);
  }
  setTitle(value) {
    if (!this.el)
      return this;
    this.el.setAttribute("title", value);
    let instance = _Tooltip.existingInstance(this.el);
    if (!instance)
      return this;
    this.el.setAttribute(`data-${isBS5Plus() ? "bs-" : ""}original-title`, value ?? "");
    instance?.[isBS3() ? "fixTitle" : "_fixTitle"]?.();
    const inner = instance?.tip?.querySelector?.(".tooltip-inner");
    inner && (inner.textContent = value ?? "");
    instance?.update?.();
    return this;
  }
  toggle(show) {
    if (!this.el)
      return this;
    let instance = _Tooltip.existingInstance(this.el);
    if (!instance)
      return this;
    instance?.[show ? "show" : "hide"]?.();
    return this;
  }
  hide() {
    return this.toggle(false);
  }
  show() {
    return this.toggle(true);
  }
};

// src/base/uploader.ts
function alwaysTrue() {
  return true;
}
__name(alwaysTrue, "alwaysTrue");
var Uploader = class _Uploader {
  static {
    __name(this, "Uploader");
  }
  constructor(opt) {
    this.opt = opt = Object.assign({}, _Uploader.defaults, opt);
    if (this.opt.batchHandler === void 0)
      this.opt.batchHandler = (batch2, uploader) => uploader.uploadBatch(batch2);
    if (this.opt.errorHandler === void 0)
      this.opt.errorHandler = _Uploader.errorHandler;
    if (opt.input) {
      if (opt.accept)
        opt.input.setAttribute("accept", opt.accept);
      if (opt.multiple)
        opt.input.setAttribute("multiple", "multiple");
      this.watchInput(opt.input);
    }
    if (isArrayLike(opt.dropZone)) {
      for (let i2 = 0; i2 < opt.dropZone.length; i2++)
        opt.dropZone[i2] && this.watchDropZone(opt.dropZone[i2]);
    } else if (opt.dropZone) {
      this.watchDropZone(opt.dropZone);
    }
  }
  newBatch(event, isFirst) {
    this.batch = {
      event,
      filePaths: [],
      formData: new FormData(),
      isFirst
    };
  }
  async addToBatch(file, filePath) {
    this.batch.filePaths.push(filePath);
    this.batch.formData.set(this.opt.name, file, filePath);
    if (!this.isMultiple() || this.opt.batchSize && this.batch.filePaths.length >= this.opt.batchSize) {
      await this.endBatch(false);
    }
  }
  async endBatch(final) {
    if (this.batch?.filePaths?.length) {
      const batch2 = this.batch;
      await this.opt.batchHandler?.(batch2, this);
      this.newBatch(batch2.event, false);
    }
    if (final) {
      this.opt.allStop?.();
      Fluent.trigger(this.opt.input, "allStop");
    }
  }
  static {
    this.defaults = {
      autoClear: true,
      batchSize: 1,
      name: "files[]"
    };
  }
  static {
    this.requestDefaults = {
      responseType: "json"
    };
  }
  isMultiple() {
    return !!(this.opt.multiple ?? this.opt?.input?.multiple);
  }
  getTypePredicate() {
    if (this.opt.ignoreType)
      return alwaysTrue;
    let acceptVal = this.opt.accept ?? this.opt?.input?.getAttribute("accept");
    if (!acceptVal)
      return alwaysTrue;
    const accepts = acceptVal.toLowerCase().split(",").map((accept) => {
      return accept.split("/").map((part) => part.trim());
    }).filter((acceptParts) => acceptParts.length === 2);
    return (type) => {
      const [typeMain, typeSub] = (type ?? "").toLowerCase().split("/").map((s2) => s2.trim());
      for (const [acceptMain, acceptSub] of accepts) {
        if (typeMain === acceptMain && (acceptSub === "*" || typeSub === acceptSub)) {
          return true;
        }
      }
      return false;
    };
  }
  getMatchingItems(list) {
    let predicate = this.getTypePredicate();
    let results = Array.from(list ?? []).filter((x) => x.kind === "file" && predicate(x.type));
    return this.isMultiple() ? results : [results[0]];
  }
  watchInput(input) {
    input.addEventListener("change", async (e2) => {
      if (e2.target?.files?.length) {
        try {
          await this.arrayApi(e2, e2.target.files);
        } finally {
          if (this.opt.autoClear)
            e2.target.value = null;
        }
      } else {
        this.opt.changeCallback?.(e2);
      }
    });
  }
  watchDropZone(node) {
    const stop = /* @__PURE__ */ __name((e2) => e2.preventDefault(), "stop");
    node.addEventListener("dragover", stop);
    node.addEventListener("dragenter", (e2) => {
      node.dragEnterCount = (node.dragEnterCount || 0) + 1;
      if (node.dragEnterCount > 1) {
        return;
      }
      if (e2.dataTransfer === null) {
        node.classList.add("drop-invalid");
        return;
      }
      const matchingFiles = this.getMatchingItems(e2.dataTransfer.items);
      const validDrop = e2.dataTransfer && e2.dataTransfer.items.length ? matchingFiles[0] !== void 0 : true;
      node.classList.toggle("drop-valid", !!validDrop);
      node.classList.toggle("drop-invalid", !validDrop);
    });
    node.addEventListener("dragleave", (e2) => {
      node.dragEnterCount = Math.max((node.dragEnterCount || 0) - 1, 0);
      if (node.dragEnterCount === 0) {
        node.dragEnterCount = 0;
        node.classList.remove("drop-valid");
        node.classList.remove("drop-invalid");
        return;
      }
      if (e2.dataTransfer === null) {
        node.classList.add("drop-invalid");
        return;
      }
    });
    node.addEventListener("drop", (e2) => {
      e2.preventDefault();
      if (e2.dataTransfer.items?.[0]?.webkitGetAsEntry() || e2.dataTransfer.items?.[0]?.getAsEntry()) {
        this.entriesApi(e2, e2.dataTransfer.items);
      } else if (e2.dataTransfer.files) {
        this.arrayApi(e2, e2.dataTransfer.files);
      } else {
        this.opt.changeCallback?.(e2);
      }
    });
    node.addEventListener("paste", (e2) => {
      if (e2.clipboardData.items?.[0]?.webkitGetAsEntry() || e2.clipboardData.items?.[0]?.getAsEntry()) {
        this.entriesApi(e2, e2.clipboardData.items);
      } else if (e2.clipboardData.files) {
        this.arrayApi(e2, e2.clipboardData.files);
      } else {
        this.opt.changeCallback?.(e2);
      }
    });
  }
  async arrayApi(e2, fileList) {
    this.newBatch(e2, true);
    let predicate = this.getTypePredicate();
    let filteredFiles = Array.from(fileList).filter((x) => predicate(x.type));
    if (!this.isMultiple() && filteredFiles.length > 0)
      filteredFiles = [filteredFiles[0]];
    for (const file of filteredFiles) {
      await this.addToBatch(file, file.webkitRelativePath || file.name);
    }
    await this.endBatch(true);
  }
  async entriesApi(e2, items) {
    this.newBatch(e2, true);
    let predicate = this.getTypePredicate();
    let multiple = this.isMultiple();
    const skipRest = /* @__PURE__ */ __name(() => !multiple && this.batch?.filePaths?.length > 0, "skipRest");
    let readDirectory = /* @__PURE__ */ __name(async (entry, path) => {
      if (!path)
        path = entry.name;
      await readEntries(entry, null, null, async (entries2) => {
        for (const entry2 of entries2) {
          if (skipRest())
            break;
          if (entry2.isFile) {
            await new Promise((resolve) => {
              entry2.file(async (file) => {
                if (predicate(file.type))
                  await this.addToBatch(file, `${path}/${file.name}`);
                resolve(void 0);
              }, resolve.bind(void 0));
            });
          } else if (entry2.isDirectory) {
            await readDirectory(entry2, `${path}/${entry2.name}`);
          }
        }
      });
    }, "readDirectory");
    let readEntries = /* @__PURE__ */ __name(async (entry, reader, oldEntries, cb) => {
      const dirReader = reader || entry.createReader();
      let allEntries = oldEntries ? oldEntries.slice() : [];
      while (true) {
        const entries2 = await new Promise((resolve) => {
          dirReader.readEntries(resolve, () => resolve([]));
        });
        if (skipRest()) {
          break;
        }
        if (entries2.length) {
          allEntries = allEntries.concat(entries2);
        } else {
          break;
        }
      }
      cb(allEntries);
    }, "readEntries");
    const entries = Array.from(items).map((x) => x.webkitGetAsEntry?.() ?? x.getAsEntry?.()).filter((x) => !!x);
    for (let i2 = 0; i2 < entries.length; i2++) {
      if (skipRest())
        return;
      let entry = entries[i2];
      await new Promise(async (resolve) => {
        if (entry.isFile) {
          entry.file(async (file) => {
            if (!skipRest() && predicate(file.type))
              await this.addToBatch(file, file.name);
            resolve(void 0);
          }, resolve.bind(void 0));
        } else if (entry.isDirectory) {
          await readDirectory(entry, null);
        }
      });
    }
    this.endBatch(true);
  }
  async uploadBatch(batch2, request) {
    if (!batch2 || !batch2.formData)
      return;
    request = Object.assign({}, _Uploader.requestDefaults);
    if (request.url === void 0)
      request.url = resolveUrl("~/File/TemporaryUpload");
    if (batch2.isFirst) {
      this.opt.allStart?.();
      Fluent.trigger(this.opt.input, "allStart");
    }
    this.opt.batchStart?.({ batch: batch2 });
    Fluent.trigger(this.opt.input, "batchStart", { detail: batch2 });
    try {
      await new Promise((resolve, reject) => {
        try {
          const xhr = new XMLHttpRequest();
          xhr.open("POST", request.url);
          let json = request.responseType !== "text";
          if (isSameOrigin(request.url)) {
            const token = getCookie("CSRF-TOKEN");
            if (token)
              xhr.setRequestHeader("X-CSRF-TOKEN", token);
          }
          if (request.headers) {
            for (const name of Object.keys(request.headers)) {
              xhr.setRequestHeader(name, request.headers[name]);
            }
          }
          const onerror = /* @__PURE__ */ __name((data) => {
            data = Object.assign({
              batch: batch2,
              request,
              response: json ? tryGetJson(xhr) : xhr.responseText,
              xhr
            }, data);
            try {
              try {
                this.opt.errorHandler?.(data);
              } finally {
                reject(data);
              }
            } catch (exception) {
              console.log(exception);
            }
          }, "onerror");
          xhr.onload = (event) => {
            try {
              if (xhr.status === 200) {
                const data = {
                  batch: batch2,
                  event,
                  request,
                  response: json ? tryGetJson(xhr) : xhr.responseText,
                  xhr
                };
                this.opt.batchSuccess?.(data);
                Fluent.trigger(this.opt.input, "batchSuccess", { detail: data });
                resolve(data);
              } else {
                onerror({ event });
              }
            } catch (exception) {
              onerror({ event, exception });
            }
          };
          xhr.onerror = (event) => onerror({ event });
          xhr.onprogress = (event) => {
            try {
              if (event.lengthComputable) {
                const data = {
                  batch: batch2,
                  loaded: event.loaded,
                  total: event.total
                };
                this.opt.batchProgress?.(data);
                Fluent.trigger(this.opt.input, "batchProgress", { detail: data });
              }
            } catch {
            }
          };
          xhr.send(batch2.formData);
        } catch (exception) {
          const data = {
            exception,
            batch: batch2,
            request
          };
          try {
            this.opt.errorHandler?.(data);
          } finally {
            reject(data);
          }
        }
      });
    } finally {
      this.opt.batchStop?.({ batch: batch2 });
      Fluent.trigger(this.opt.input, "batchStop", { detail: batch2 });
    }
  }
  static errorHandler(data) {
    if (data?.exception) {
      console.error(data.exception);
      notifyError(data.exception.toString?.() ?? "Exception occured!");
      return;
    }
    if (data?.response?.Error?.Message) {
      notifyError(data.response.Error.Message);
      return;
    }
    let xhr = data?.xhr;
    if (!xhr) {
      notifyError("An error occurred during file upload.");
      return;
    }
    const html = xhr.responseText;
    if (html) {
      iframeDialog({ html });
      return;
    }
    if (!xhr.status) {
      if (xhr.statusText != "abort")
        notifyError("An unknown connection error occurred! Check browser console for details.");
      return;
    }
    if (xhr.status == 500) {
      notifyError("HTTP 500: Connection refused! Check browser console for details.");
      return;
    }
    notifyError("HTTP " + xhr.status + " error! Check browser console for details.");
  }
};
function tryGetJson(xhr) {
  try {
    return JSON.parse(xhr.responseText);
  } catch {
    return null;
  }
}
__name(tryGetJson, "tryGetJson");

// src/base/validator.tsx
function messageKey(method) {
  return "msg" + method.charAt(0).toUpperCase() + method.substring(1).toLowerCase();
}
__name(messageKey, "messageKey");
var validatorMap = /* @__PURE__ */ new WeakMap();
function validatorEventDelegate(event) {
  let isContentEditable = Validator.isContentEditable(this);
  if (!this.form && isContentEditable) {
    this.form = this.closest("form");
    this.name = this.getAttribute("name");
  }
  const validator = validatorMap.get(this.form);
  if (!validator)
    return;
  const eventType = "on" + event.type.replace(/^validate/, "");
  const settings = validator.settings;
  if (event.type == "focusin" && Validator.isValidatableElement(this))
    validator.lastActive = this;
  if (typeof settings[eventType] === "function" && (!settings.ignore || !this.matches(settings.ignore))) {
    settings[eventType](this, event, validator);
  }
}
__name(validatorEventDelegate, "validatorEventDelegate");
var customValidateRules = /* @__PURE__ */ new WeakMap();
var SIMPLE_EMAIL_REGEX = /^[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@(?:[a-zA-Z0-9-]+\.)*[a-zA-Z0-9-]+$/;
var Validator = class _Validator {
  constructor(form, options) {
    this.dependTypes = {
      "boolean": /* @__PURE__ */ __name(function(param) {
        return param;
      }, "boolean"),
      "string": /* @__PURE__ */ __name(function(param, element) {
        return !!element.form.querySelector(param);
      }, "string"),
      "function": /* @__PURE__ */ __name(function(param, element) {
        return param(element);
      }, "function")
    };
    if (validatorMap.get(form))
      throw "Form already has a Validator instance!";
    validatorMap.set(form, this);
    form.setAttribute("novalidate", "novalidate");
    this.settings = Object.assign(true, {}, _Validator.defaults, options);
    this.currentForm = form;
    if (this.settings.onsubmit) {
      const selector = "[type=submit],button:not([type])";
      Fluent.on(this.currentForm, "click.validator", selector, (event) => {
        const button = event.currentTarget?.matches?.(selector) ? event.currentTarget : event.target.closest?.(selector) ?? event.target;
        ;
        this.submitButton = button;
        if (button.classList?.contains("cancel")) {
          this.cancelSubmit = true;
        }
        if (button.getAttribute("formnovalidate") != null) {
          this.cancelSubmit = true;
        }
      });
      Fluent.on(this.currentForm, "submit.validator", (event) => {
        if (this.settings.debug) {
          event.preventDefault();
        }
        const stopSubmit = /* @__PURE__ */ __name(() => {
          event.preventDefault();
          event.stopPropagation();
          return false;
        }, "stopSubmit");
        const handle = /* @__PURE__ */ __name(() => {
          let hidden, result;
          if (this.submitButton && (this.settings.submitHandler || this.formSubmitted)) {
            hidden = this.currentForm.appendChild(jsx("input", { type: "hidden", name: this.submitButton.name, value: this.submitButton.value }));
          }
          if (this.settings.submitHandler && !this.settings.debug) {
            result = this.settings.submitHandler(this.currentForm, event, this);
            if (hidden) {
              Fluent.remove(hidden);
            }
            if (result !== void 0) {
              if (result === false)
                return stopSubmit();
              return result;
            }
            return stopSubmit();
          }
          return true;
        }, "handle");
        if (this.cancelSubmit) {
          this.cancelSubmit = false;
          return handle();
        }
        if (this.form()) {
          if (this.pendingRequest) {
            this.formSubmitted = true;
            return stopSubmit();
          }
          return handle();
        } else {
          this.focusInvalid();
          return stopSubmit();
        }
      });
    }
    this.init();
  }
  static {
    __name(this, "Validator");
  }
  static optional(element, value) {
    if (value === void 0)
      value = _Validator.elementValue(element);
    return !_Validator.methods.required(value, element) && "dependency-mismatch";
  }
  static {
    this.autoCreateRanges = false;
  }
  static {
    this.defaults = {
      messages: {},
      rules: {},
      errorClass: "error",
      pendingClass: "pending",
      validClass: "valid",
      errorElement: "label",
      focusInvalid: true,
      onsubmit: true,
      ignore: '[hidden], [hidden] *, [style*="display:none"], [style*="display: none"] *, .hidden *, input[type=hidden], .no-validate',
      normalizer: /* @__PURE__ */ __name(function(value) {
        return typeof value === "string" ? value.trim() : value;
      }, "normalizer"),
      onfocusin: /* @__PURE__ */ __name(function(element, event, validator) {
        validator.lastActive = element;
      }, "onfocusin"),
      onfocusout: /* @__PURE__ */ __name(function(element, event, validator) {
        if (_Validator.isCheckOrRadio(element))
          return;
        if (!(element.name in validator.submitted)) {
          const optional = _Validator.optional(element);
          if (optional === "dependency-mismatch" && !_Validator.rules(validator.validationTargetFor(element) || element)?.required)
            return;
          if (element.tagName.toLowerCase() === "textarea" && element.style.visibility === "hidden") {
            return;
          }
        }
        validator.element(element);
      }, "onfocusout"),
      onkeyup: /* @__PURE__ */ __name(function(element, event, validator) {
        if (event.key === "Tab" && _Validator.elementValue(element) === "" || _Validator.excludedModifierKeys.has(event.key)) {
          return;
        } else if (element.name in validator.submitted || element.name in validator.invalid) {
          validator.element(element);
        }
      }, "onkeyup"),
      onclick: /* @__PURE__ */ __name(function(element, event, validator) {
        if (element.name in validator.submitted) {
          validator.element(element);
        } else if (element.parentNode.name in validator.submitted) {
          validator.element(element.parentNode);
        }
      }, "onclick"),
      highlight: /* @__PURE__ */ __name(function(element, errorClass, validClass) {
        if (element.type === "radio") {
          this.findByName(element.name).forEach((x) => {
            x.classList.add(errorClass);
            x.classList.remove(validClass);
          });
        } else {
          if (errorClass != null && errorClass.length)
            element.classList.add(errorClass);
          if (validClass != null && validClass.length)
            element.classList.remove(validClass);
          const hl = _Validator.getHighlightTarget(element);
          if (hl && hl.classList) {
            if (errorClass != null && errorClass.length)
              hl.classList.add(errorClass);
            if (validClass != null && validClass.length)
              hl.classList.remove(validClass);
          }
        }
      }, "highlight"),
      unhighlight: /* @__PURE__ */ __name(function(element, errorClass, validClass) {
        if (element.type === "radio") {
          this.findByName(element.name).forEach((x) => {
            x.classList.remove(errorClass);
            x.classList.add(validClass);
          });
        } else {
          if (errorClass != null && errorClass.length)
            element.classList.remove(errorClass);
          if (validClass != null && validClass.length)
            element.classList.add(validClass);
          const hl = _Validator.getHighlightTarget(element);
          if (hl && hl.classList) {
            if (errorClass != null && errorClass.length)
              hl.classList.remove(errorClass);
            if (validClass != null && validClass.length)
              hl.classList.add(validClass);
          }
        }
      }, "unhighlight")
    };
  }
  static {
    this.messages = {
      required: "Validation.Required",
      remote: "Please fix this field.",
      email: "Validation.Email",
      dateQ: "Validation.DateInvalid",
      decimalQ: "Validation.Decimal",
      dateISO: "Please enter a valid date (ISO).",
      integerQ: "Validation.Integer",
      number: "Please enter a valid number.",
      digits: "Validation.Digits",
      equalTo: "Please enter the same value again.",
      maxlength: "Validation.MaxLength",
      minlength: "Validation.MinLength",
      rangelength: "Please enter a value between {0} and {1} characters long.",
      range: "Validation.Range",
      max: "Please enter a value less than or equal to {0}.",
      min: "Please enter a value greater than or equal to {0}.",
      step: "Please enter a multiple of {0}.",
      url: "Validation.Url",
      xss: "Validation.Xss"
    };
  }
  static {
    this.methods = {
      required: /* @__PURE__ */ __name(function(value, element) {
        if (element instanceof HTMLSelectElement) {
          const val = _Validator.elementValue(element);
          return val && val.length > 0;
        }
        if (_Validator.isCheckOrRadio(element)) {
          return _Validator.getLength(value, element) > 0;
        }
        return value != null && (typeof value === "number" || value.length > 0);
      }, "required"),
      customValidate: /* @__PURE__ */ __name(function(value, element) {
        let result = _Validator.optional(element, value);
        if (!element || !!result)
          return result;
        let rules = customValidateRules.get(element);
        if (!rules)
          return true;
        for (let k in rules) {
          let handlers = rules[k];
          for (let handler of handlers) {
            if (typeof handler === "function") {
              let message = handler(element);
              if (message != null) {
                return message;
              }
            }
          }
        }
        return true;
      }, "customValidate"),
      dateQ: /* @__PURE__ */ __name(function(value, element) {
        const o2 = _Validator.optional(element, value);
        if (o2)
          return o2;
        const d2 = parseDate(value);
        if (!d2 || isNaN(d2.valueOf()))
          return false;
        const z = new Date(d2);
        z.setHours(0, 0, 0, 0);
        return z.getTime() === d2.getTime();
      }, "dateQ"),
      dateTimeQ: /* @__PURE__ */ __name(function(value, element) {
        const o2 = _Validator.optional(element, value);
        if (o2)
          return o2;
        const d2 = parseDate(value);
        if (!d2 || isNaN(d2.valueOf()))
          return false;
        return true;
      }, "dateTimeQ"),
      /*
          hourAndMin: function (value: string, element: any) {
              return Validator.optional(element, value) || !isNaN(parseHourAndMin(value));
          }
      
          dayHourAndMin: function (value: string, element: any) {
              return Validator.optional(element, value) || !isNaN(parseDayHourAndMin(value));
          });
          */
      decimalQ: /* @__PURE__ */ __name(function(value, element) {
        return _Validator.optional(element, value) || !isNaN(parseDecimal(value));
      }, "decimalQ"),
      integerQ: /* @__PURE__ */ __name(function(value, element) {
        return _Validator.optional(element, value) || !isNaN(parseInteger(value));
      }, "integerQ"),
      /**
       * Validates whether the input value is an email in accordance to RFC822 specification, with a top level domain.
       */
      email: /* @__PURE__ */ __name((value, element) => {
        const o2 = _Validator.optional(element, value);
        if (o2)
          return o2;
        if (typeof value !== "string") {
          return false;
        }
        if (value.length > 1e3)
          return false;
        if (value.indexOf("..") >= 0) {
          return false;
        }
        if (!Config.emailAllowOnlyAscii) {
          if (/[\x00-\x1F\x7F-\x9F]/.test(value)) {
            return false;
          }
          const atIndex = value.indexOf("@");
          if (atIndex <= 0 || atIndex === value.length - 1) {
            return false;
          }
          const domain = value.substring(atIndex + 1);
          return domain.includes(".") && domain.length >= 3;
        }
        return SIMPLE_EMAIL_REGEX.test(value);
      }, "email"),
      minlength: /* @__PURE__ */ __name(function(value, element, param) {
        const length = Array.isArray(value) ? value.length : _Validator.getLength(value, element);
        return _Validator.optional(element, value) || length >= param;
      }, "minlength"),
      maxlength: /* @__PURE__ */ __name(function(value, element, param) {
        const length = Array.isArray(value) ? value.length : _Validator.getLength(value, element);
        return _Validator.optional(element, value) || length <= param;
      }, "maxlength"),
      rangelength: /* @__PURE__ */ __name(function(value, element, param) {
        const length = Array.isArray(value) ? value.length : _Validator.getLength(value, element);
        return _Validator.optional(element, value) || length >= param[0] && length <= param[1];
      }, "rangelength"),
      min: /* @__PURE__ */ __name(function(value, element, param) {
        return _Validator.optional(element, value) || value >= param;
      }, "min"),
      max: /* @__PURE__ */ __name(function(value, element, param) {
        return _Validator.optional(element, value) || value <= param;
      }, "max"),
      range: /* @__PURE__ */ __name(function(value, element, param) {
        return _Validator.optional(element, value) || value >= param[0] && value <= param[1];
      }, "range"),
      url: /* @__PURE__ */ __name(function(value, element) {
        return _Validator.optional(element, value) || /^(?:(?:(?:https?|ftp):)?\/\/)(?:(?:[^\]\[?\/<~#`!@$^&*()+=}|:";',>{ ]|%[0-9A-Fa-f]{2})+(?::(?:[^\]\[?\/<~#`!@$^&*()+=}|:";',>{ ]|%[0-9A-Fa-f]{2})*)?@)?(?:(?!(?:10|127)(?:\.\d{1,3}){3})(?!(?:169\.254|192\.168)(?:\.\d{1,3}){2})(?!172\.(?:1[6-9]|2\d|3[0-1])(?:\.\d{1,3}){2})(?:[1-9]\d?|1\d\d|2[01]\d|22[0-3])(?:\.(?:1?\d{1,2}|2[0-4]\d|25[0-5])){2}(?:\.(?:[1-9]\d?|1\d\d|2[0-4]\d|25[0-4]))|(?:(?:[a-z0-9\u00a1-\uffff][a-z0-9\u00a1-\uffff_-]{0,62})?[a-z0-9\u00a1-\uffff]\.)+(?:[a-z\u00a1-\uffff]{2,}\.?))(?::\d{2,5})?(?:[/?#]\S*)?$/i.test(value?.toString());
      }, "url")
    };
  }
  static getInstance(element) {
    element = isArrayLike(element) ? element[0] : element;
    if (!element)
      return null;
    if (element instanceof HTMLFormElement)
      return validatorMap.get(element);
    else if (element.form instanceof HTMLFormElement)
      return validatorMap.get(element.form);
    let form = element.closest?.("form");
    if (form instanceof HTMLFormElement)
      return validatorMap.get(form);
    return null;
  }
  init() {
    this.submitted = {};
    this.pendingRequest = 0;
    this.pending = {};
    this.invalid = {};
    this.reset();
    ["focusin.validator", "focusout.validator", "keyup.validator"].forEach((type) => {
      Fluent.on(this.currentForm, type, "[type='text'], [type='password'], [type='file'], select, textarea, [type='number'], [type='search'], [type='tel'], [type='url'], [type='email'], [type='datetime'], [type='date'], [type='month'], [type='week'], [type='time'], [type='datetime-local'], [type='range'], [type='color'], [type='radio'], [type='checkbox'], [contenteditable], [type='button']", validatorEventDelegate);
    });
    Fluent.on(this.currentForm, "click.validator", "select, option, [type='radio'], [type='checkbox']", validatorEventDelegate);
    if (this.settings.invalidHandler) {
      Fluent.on(this.currentForm, "invalid-form.validator", (e2) => this.settings.invalidHandler(e2, this));
    }
  }
  /**
   * Checks if `element` is validatable (`input`, `select`, `textarea`).
   * @param element The element to check.
   * @returns `true` if validatable, otherwise `false`.
   */
  static isValidatableElement(element) {
    return element instanceof HTMLInputElement || element instanceof HTMLSelectElement || element instanceof HTMLTextAreaElement;
  }
  static isCheckOrRadio(element) {
    return element instanceof HTMLInputElement && /radio|checkbox/i.test(element.type);
  }
  static getLength(value, element) {
    if (element instanceof HTMLSelectElement)
      return element.querySelectorAll("option:checked").length;
    if (element instanceof HTMLInputElement && _Validator.isCheckOrRadio(element)) {
      if (!element.name)
        return element.checked ? 1 : 0;
      return Array.from(element.form?.querySelectorAll(`[name=${element.name}]`)).filter((x) => x.checked).length || 0;
    }
    return typeof value === "number" ? ("" + value).length : value.length;
  }
  static isContentEditable(element) {
    let val = element.getAttribute("contenteditable");
    return val != null && val !== "false";
  }
  static elementValue(element) {
    if (element instanceof Element && _Validator.isContentEditable(element)) {
      return element.textContent;
    }
    let val;
    if (element instanceof HTMLInputElement) {
      if (element.type === "radio" || element.type === "checkbox") {
        if (element.name && element.form) {
          const values = Array.from(element.form.querySelectorAll(`input[name=${cssEscape(element.name)}]`)).map((el) => el.checked ? null : el.value);
          if (values.length > 1)
            return values.filter((x) => x != null);
          return values[0];
        }
        return element.checked ? element.value : null;
      }
      if (element.type === "number") {
        if (typeof element.validity !== "undefined")
          return element.validity.badInput ? NaN : element.valueAsNumber;
        else
          return element.valueAsNumber;
      }
      if (element.type === "file") {
        val = element.value ?? "";
        if (val.substring(0, 12) === "C:\\fakepath\\") {
          return val.substring(12);
        }
        let idx = val.lastIndexOf("/");
        if (idx >= 0) {
          return val.substring(idx + 1);
        }
        idx = val.lastIndexOf("\\");
        if (idx >= 0) {
          return val.substring(idx + 1);
        }
        return val;
      }
      val = element.value;
    } else if (element instanceof HTMLTextAreaElement) {
      val = element.value;
    } else if (element instanceof HTMLSelectElement) {
      val = element.value;
    }
    if (typeof val === "string")
      return val.replace(/\r/g, "");
    return val;
  }
  static valid(element) {
    element = element instanceof HTMLFormElement ? element : isArrayLike(element) ? element[0] : element;
    if (!element)
      return false;
    let validator = _Validator.getInstance(element);
    if (!validator)
      return false;
    if (element instanceof HTMLFormElement)
      return validator.form();
    return validator.element(element);
  }
  static rules(element, command, argument) {
    let isContentEditable = _Validator.isContentEditable(element);
    if (element == null) {
      return;
    }
    if (!element.form && isContentEditable) {
      element.form = element.closest("form");
      element.name = element.getAttribute("name");
    }
    if (element.form == null) {
      return;
    }
    if (command) {
      const validator = _Validator.getInstance(element.form);
      if (validator) {
        let settings = validator.settings;
        let staticRules = settings.rules;
        let existingRules = _Validator.staticRules(element);
        switch (command) {
          case "add":
            Object.assign(existingRules, argument);
            delete existingRules.messages;
            staticRules[element.name] = existingRules;
            if (argument.messages && !isPollutingKey2(element.name)) {
              settings.messages[element.name] = Object.assign(settings.messages[element.name], argument.messages);
            }
            break;
          case "remove":
            if (!argument) {
              delete staticRules[element.name];
              return existingRules;
            }
            const filtered = {};
            argument.split(/\s/).forEach((method) => {
              filtered[method] = existingRules[method];
              delete existingRules[method];
            });
            return filtered;
        }
      }
    }
    let data = _Validator.normalizeRules(
      Object.assign(
        {},
        _Validator.classRules(element),
        _Validator.attributeRules(element),
        _Validator.dataRules(element),
        _Validator.staticRules(element)
      ),
      element
    );
    let param;
    if (data.required) {
      let param2 = data.required;
      delete data.required;
      data = Object.assign({ required: param2 }, data);
    }
    if (data.remote) {
      param = data.remote;
      delete data.remote;
      data = Object.assign(data, { remote: param });
    }
    return data;
  }
  form() {
    this.checkForm();
    Object.assign(this.submitted, this.errorMap);
    this.invalid = Object.assign({}, this.errorMap);
    if (!this.valid()) {
      Fluent.trigger(this.currentForm, "invalid-form", { validator: this });
    }
    this.showErrors();
    return this.valid();
  }
  checkForm() {
    this.prepareForm();
    for (let i2 = 0, elements = this.currentElements = this.elements(); elements[i2]; i2++) {
      this.check(elements[i2]);
    }
    return this.valid();
  }
  element(element) {
    const checkElement = this.validationTargetFor(element);
    let result = true, rs;
    if (checkElement === void 0) {
      delete this.invalid[element.name];
    } else {
      this.prepareElement(checkElement);
      this.currentElements = [checkElement];
      rs = this.check(checkElement) !== false;
      result = result && rs;
      if (rs) {
        this.invalid[checkElement.name] = false;
      } else {
        this.invalid[checkElement.name] = true;
      }
      this.showErrors();
      if (rs)
        element.setAttribute("aria-invalid", "true");
      else
        element.removeAttribute("aria-invalid");
    }
    return result;
  }
  showErrors(errors) {
    if (errors) {
      Object.assign(this.errorMap, errors);
      this.errorList = Object.keys(this.errorMap).map((name) => ({
        message: this.errorMap[name],
        element: this.findByName(name)[0]
      }));
      this.successList = this.successList.filter(function(element) {
        return !(element.name in errors);
      });
    }
    if (this.settings.showErrors) {
      this.settings.showErrors(this.errorMap, this.errorList, this);
    } else {
      this.defaultShowErrors();
    }
  }
  resetForm() {
    this.invalid = {};
    this.submitted = {};
    this.prepareForm();
    this.hideErrors();
    const elements = this.elements();
    elements.forEach((x) => {
      delete x.previousValue;
      x.removeAttribute("aria-invalid");
    });
    this.resetElements(elements);
  }
  resetElements(elements) {
    let i2;
    if (this.settings.unhighlight) {
      for (i2 = 0; elements[i2]; i2++) {
        this.settings.unhighlight(elements[i2], this.settings.errorClass, "", this);
        this.findByName(elements[i2].name).forEach((x) => x.classList.remove(this.settings.validClass));
      }
    } else {
      elements.forEach((x) => {
        x.classList.remove(this.settings.errorClass);
        x.classList.remove(this.settings.validClass);
      });
    }
  }
  numberOfInvalids() {
    return _Validator.objectLength(this.invalid);
  }
  static objectLength(obj) {
    let count2 = 0, i2;
    for (i2 in obj) {
      if (obj[i2] !== void 0 && obj[i2] !== null && obj[i2] !== false) {
        count2++;
      }
    }
    return count2;
  }
  hideErrors() {
    this.hideThese(this.toHide);
  }
  hideThese(errors) {
    errors.forEach((x) => {
      x.textContent = "";
      x.hidden = true;
    });
  }
  valid() {
    return this.size() === 0;
  }
  size() {
    return this.errorList.length;
  }
  focusInvalid() {
    if (this.settings.abortHandler)
      this.settings.abortHandler(this);
    if (this.settings.focusInvalid) {
      try {
        const lastActive = this.findLastActive() || this.errorList.length && this.errorList[0].element;
        if (lastActive && Fluent.isVisibleLike(lastActive)) {
          lastActive.focus?.();
          Fluent.trigger(lastActive, "focusin");
        }
      } catch (e2) {
      }
    }
  }
  findLastActive() {
    const lastActive = this.lastActive;
    return lastActive && this.errorList.filter((n2) => n2.element.name === lastActive.name).length === 1 && lastActive;
  }
  elements() {
    const rulesCache = {};
    return Array.from(this.currentForm.querySelectorAll("input, select, textarea, [contenteditable]")).filter((x) => {
      if ((x instanceof HTMLButtonElement || x instanceof HTMLInputElement) && (x.type === "submit" || x.type === "reset" || x.type === "image"))
        return false;
      if (x.disabled || x.closest("fieldset")?.disabled)
        return false;
      if (x.matches(this.settings.ignore))
        return false;
      const name = x.name || x.getAttribute("name");
      const isContentEditable = _Validator.isContentEditable(x);
      if (!name && this.settings.debug && window.console) {
        console.error("%o has no name assigned", this);
      }
      if (isContentEditable) {
        x.form = x.closest("form");
        x.name = name;
      }
      if (x.form !== this.currentForm) {
        return false;
      }
      if (name in rulesCache || !_Validator.objectLength(_Validator.rules(x))) {
        return false;
      }
      rulesCache[name] = true;
      return true;
    });
  }
  errors() {
    const errorClass = this.settings.errorClass.split(" ").join(".");
    return Array.from(this.currentForm.querySelectorAll(this.settings.errorElement + "." + errorClass));
  }
  resetInternals() {
    this.successList = [];
    this.errorList = [];
    this.errorMap = {};
    this.toShow = [];
    this.toHide = [];
  }
  reset() {
    this.resetInternals();
    this.currentElements = [];
  }
  resetAll() {
    this.resetForm();
  }
  prepareForm() {
    this.reset();
    this.toHide = this.errors();
  }
  prepareElement(element) {
    this.reset();
    this.toHide = this.errorsFor(element);
  }
  check(element) {
    element = this.validationTargetFor(element);
    const rules = _Validator.rules(element), rulesCount = Object.keys(rules).length;
    let dependencyMismatch = false, val = _Validator.elementValue(element), result, method, rule, normalizer;
    this.abortRequest(element);
    if (typeof rules.normalizer === "function") {
      normalizer = rules.normalizer;
    } else if (typeof this.settings.normalizer === "function") {
      normalizer = this.settings.normalizer;
    }
    if (normalizer) {
      val = normalizer(val, element);
      delete rules.normalizer;
    }
    for (method in rules) {
      rule = { method, parameters: rules[method] };
      try {
        result = _Validator.methods[method].call(this, val, element, rule.parameters);
        if (result === "dependency-mismatch" && rulesCount === 1) {
          dependencyMismatch = true;
          continue;
        }
        dependencyMismatch = false;
        if (result === "pending") {
          const errorsFor = this.errorsFor(element);
          this.toHide = this.toHide.filter((x) => !errorsFor.includes(x));
          return;
        }
        if (typeof result === "string" && result !== "dependency-mismatch" && result !== "pending") {
          element.dataset[messageKey(method)] = result;
          result = false;
        }
        if (!result) {
          this.formatAndAdd(element, rule);
          return false;
        }
      } catch (e2) {
        if (this.settings.debug && window.console) {
          console.log("Exception occurred when checking element " + element.id + ", check the '" + rule.method + "' method.", e2);
        }
        if (e2 instanceof TypeError) {
          e2.message += ".  Exception occurred when checking element " + element.id + ", check the '" + rule.method + "' method.";
        }
        throw e2;
      }
    }
    if (dependencyMismatch) {
      return;
    }
    if (_Validator.objectLength(rules)) {
      this.successList.push(element);
    }
    return true;
  }
  // Return the custom message for the given element and validation method
  // specified in the element's HTML5 data attribute
  // return the generic message if present and no method specific message is present
  customDataMessage(element, method) {
    return element.dataset[messageKey(method)] || element.dataset.msg;
  }
  // Return the custom message for the given element name and validation method
  customMessage(name, method) {
    const m = this.settings.messages[name];
    return m && (typeof m == "string" ? m : m[method]);
  }
  // Return the first defined argument, allowing empty strings
  findDefined(...args) {
    for (let i2 = 0; i2 < arguments.length; i2++) {
      if (arguments[i2] !== void 0) {
        return arguments[i2];
      }
    }
    return void 0;
  }
  defaultMessage(element, rule) {
    let message = this.findDefined(
      this.customMessage(element.name, rule.method),
      this.customDataMessage(element, rule.method),
      void 0,
      _Validator.messages[rule.method],
      "Warning: No message defined for " + element.name
    ), theregex = /\$?\{(\d+)\}/g;
    if (typeof message === "function")
      return message.call(this, rule.parameters, element);
    message = localText(message, message);
    if (theregex.test(message))
      return stringFormat(message, rule.parameters);
    return message;
  }
  formatAndAdd(element, rule) {
    const message = this.defaultMessage(element, rule);
    this.errorList.push({
      message,
      element,
      method: rule.method
    });
    this.errorMap[element.name] = message;
    this.submitted[element.name] = message;
  }
  defaultShowErrors() {
    let i2, elements, error;
    for (i2 = 0; this.errorList[i2]; i2++) {
      error = this.errorList[i2];
      if (this.settings.highlight) {
        this.settings.highlight.call(this, error.element, this.settings.errorClass, this.settings.validClass);
      }
      this.showLabel(error.element, error.message);
    }
    if (this.errorList.length) {
      this.toShow = this.toShow;
    }
    if (this.settings.success) {
      for (i2 = 0; this.successList[i2]; i2++) {
        this.showLabel(this.successList[i2]);
      }
    }
    if (this.settings.unhighlight) {
      for (i2 = 0, elements = this.validElements(); elements[i2]; i2++) {
        this.settings.unhighlight.call(this, elements[i2], this.settings.errorClass, this.settings.validClass);
      }
    }
    this.toHide = this.toHide.filter((x) => !this.toShow.includes(x));
    this.hideErrors();
    this.toShow.forEach((x) => Fluent.toggle(x, true));
  }
  validElements() {
    let invalids = this.invalidElements();
    return this.currentElements.filter((x) => !invalids.includes(x));
  }
  invalidElements() {
    return this.errorList.map((x) => x.element);
  }
  showLabel(element, message) {
    let errors = this.errorsFor(element), elementID = this.idOrName(element), describedBy = element.getAttribute("aria-describedby"), error;
    if (errors.length) {
      errors.forEach((x) => {
        x.classList.remove(this.settings.validClass);
        x.classList.add(this.settings.errorClass);
      });
      errors.forEach((x) => {
        x.textContent = message || "";
      });
    } else {
      error = Fluent(this.settings.errorElement).class(this.settings.errorClass).attr("id", elementID + "-error").getNode();
      error.textContent = message || "";
      const place = error;
      if (this.settings.errorPlacement) {
        this.settings.errorPlacement(place, element, this);
      } else {
        Fluent(place).insertAfter(element);
      }
      if (error.nodeName == "LABEL") {
        error.setAttribute("for", elementID);
      } else if (!error.closest("label[for='" + cssEscape(elementID) + "']")) {
        const errorID = error.getAttribute("id");
        if (!describedBy) {
          describedBy = errorID;
        } else if (!describedBy.match(new RegExp("\\b" + cssEscape(errorID) + "\\b"))) {
          describedBy += " " + errorID;
        }
        element.setAttribute("aria-describedby", describedBy);
      }
      errors = [error];
    }
    if (!message && this.settings.success) {
      errors.forEach((x) => {
        x.textContent = "";
      });
      if (typeof this.settings.success === "string") {
        errors.forEach((x) => {
          x.classList.add(this.settings.success);
        });
      } else {
        this.settings.success(error, element);
      }
    }
    errors.forEach((x) => {
      this.toShow.push(x);
    });
    this.errorsFor(element).forEach((element2) => {
      if (element2?.parentNode?.classList?.contains("vx")) {
        element2.setAttribute("title", element2.textContent);
        if (message && element2.classList.contains("error"))
          element2.classList.remove("checked");
      }
    });
  }
  errorsFor(element) {
    const name = cssEscape(this.idOrName(element)), describer = element.getAttribute("aria-describedby");
    let selector = "label[for='" + name + "'], label[for='" + name + "'] *";
    if (describer) {
      selector = selector + ", #" + cssEscape(describer).replace(/\s+/g, ", #");
    }
    return this.errors().filter((x) => x.matches(selector));
  }
  idOrName(element) {
    return _Validator.isCheckOrRadio(element) ? element.name : element.id || element.name;
  }
  validationTargetFor(element) {
    let elements = [element];
    if (_Validator.isCheckOrRadio(element)) {
      elements = this.findByName(element.name);
    }
    return elements.filter((x) => !x.matches(this.settings.ignore))[0];
  }
  findByName(name) {
    return Array.from(this.currentForm.querySelectorAll("[name='" + cssEscape(name) + "']"));
  }
  depend(param, element) {
    return this.dependTypes[typeof param] ? this.dependTypes[typeof param](param, element) : true;
  }
  startRequest(element) {
    if (!this.pending[element.name]) {
      this.pendingRequest++;
      element.classList.add(this.settings.pendingClass);
      this.pending[element.name] = new AbortController();
    }
  }
  stopRequest(element, valid) {
    let formSubmitted = this.formSubmitted;
    this.pendingRequest--;
    if (this.pendingRequest < 0) {
      this.pendingRequest = 0;
    }
    delete this.pending[element.name];
    element.classList.remove(this.settings.pendingClass);
    if (valid && this.pendingRequest === 0 && this.formSubmitted && this.form() && this.pendingRequest === 0) {
      Fluent.trigger(this.currentForm, "submit");
      this.formSubmitted = false;
    } else if (!valid && this.pendingRequest === 0 && this.formSubmitted) {
      Fluent.trigger(this.currentForm, "invalid-form", { validator: this });
      this.formSubmitted = false;
    }
    if (!valid && this.pendingRequest == 0 && formSubmitted && this.settings.abortHandler) {
      this.settings.abortHandler(this);
    }
  }
  abortRequest(element) {
    if (this.pending[element.name]) {
      this.pending[element.name].abort();
      this.pendingRequest--;
      if (this.pendingRequest < 0) {
        this.pendingRequest = 0;
      }
      delete this.pending[element.name];
      element.classList.remove(this.settings.pendingClass);
    }
  }
  previousValue(element, method) {
    method = typeof method === "string" && method || "remote";
    return element.previousValue || (element.previousValue = {
      old: null,
      valid: true,
      message: this.defaultMessage(element, { method })
    });
  }
  // Cleans up all forms and elements, removes validator-specific events
  destroy() {
    this.resetForm();
    Fluent.off(this.currentForm, ".validator");
    validatorMap.delete(this.currentForm);
    delete this.currentForm;
  }
  static {
    this.classRuleSettings = {
      required: { required: true },
      email: { email: true },
      url: { url: true },
      date: { date: true },
      dateISO: { dateISO: true },
      number: { number: true },
      digits: { digits: true },
      creditcard: { creditcard: true },
      customValidate: { customValidate: true }
    };
  }
  static addClassRules(className2, rules) {
    if (typeof className2 === "string") {
      this.classRuleSettings[className2] = rules;
    } else {
      Object.assign(this.classRuleSettings, className2);
    }
  }
  static classRules(element) {
    let rules = {};
    let classes = element.getAttribute("class");
    if (classes) {
      classes.split(" ").forEach((klass) => {
        if (klass in _Validator.classRuleSettings) {
          Object.assign(rules, _Validator.classRuleSettings[klass]);
        }
      });
    }
    return rules;
  }
  static normalizeAttributeRule(rules, type, method, value) {
    if (/min|max|step/.test(method) && (type === null || /number|range|text/.test(type))) {
      value = Number(value);
      if (isNaN(value)) {
        value = void 0;
      }
    }
    if (value || value === 0) {
      rules[method] = value;
    } else if (type === method && type !== "range") {
      rules[type === "date" ? "dateISO" : method] = true;
    }
  }
  static attributeRules(element) {
    const rules = {};
    const type = element.getAttribute("type");
    let value;
    for (const method in _Validator.methods) {
      if (method === "required") {
        value = element.getAttribute(method);
        if (value === "") {
          value = true;
        }
        value = !!value;
      } else {
        value = element.getAttribute(method);
      }
      if (value == null)
        continue;
      _Validator.normalizeAttributeRule(rules, type, method, value);
    }
    if (rules.maxlength && /-1|2147483647|524288/.test(rules.maxlength)) {
      delete rules.maxlength;
    }
    return rules;
  }
  static dataRules(element) {
    const rules = {}, type = element.getAttribute("type");
    let method, value;
    for (method in _Validator.methods) {
      value = element.dataset["rule" + method.charAt(0).toUpperCase() + method.substring(1).toLowerCase()];
      if (value === "") {
        value = true;
      }
      this.normalizeAttributeRule(rules, type, method, value);
    }
    return rules;
  }
  static staticRules(element) {
    let rules = {};
    const validator = _Validator.getInstance(element.form);
    if (validator.settings.rules) {
      rules = validator.settings.rules[element.name] || {};
    }
    return rules;
  }
  static normalizeRules(rules, element) {
    Object.keys(rules).forEach((prop) => {
      let val = rules[prop];
      if (val === false) {
        delete rules[prop];
        return;
      }
      if (val.param || val.depends) {
        let keepRule = true;
        switch (typeof val.depends) {
          case "string":
            keepRule = !!element.form.querySelector(val.depends);
            break;
          case "function":
            keepRule = val.depends.call(element, element);
            break;
        }
        if (keepRule) {
          rules[prop] = val.param !== void 0 ? val.param : true;
        } else {
          const validator = _Validator.getInstance(element.form);
          if (validator)
            validator.resetElements([element]);
          delete rules[prop];
        }
      }
    });
    Object.keys(rules).forEach((rule) => {
      let parameter = rules[rule];
      rules[rule] = typeof parameter === "function" && rule !== "normalizer" ? parameter(element) : parameter;
    });
    ["minlength", "maxlength"].forEach((x) => {
      if (rules[x]) {
        rules[x] = Number(rules[x]);
      }
    });
    ["rangelength", "range"].forEach((x) => {
      let parts;
      if (rules[x]) {
        if (Array.isArray(rules[x])) {
          rules[x] = [Number(rules[x][0]), Number(rules[x][1])];
        } else if (typeof rules[x] === "string") {
          parts = rules[x].replace(/[\[\]]/g, "").split(/[\s,]+/);
          rules[x] = [Number(parts[0]), Number(parts[1])];
        }
      }
    });
    if (_Validator.autoCreateRanges) {
      if (rules.min != null && rules.max != null) {
        rules.range = [rules.min, rules.max];
        delete rules.min;
        delete rules.max;
      }
      if (rules.minlength != null && rules.maxlength != null) {
        rules.rangelength = [rules.minlength, rules.maxlength];
        delete rules.minlength;
        delete rules.maxlength;
      }
    }
    return rules;
  }
  static addMethod(name, method, message) {
    _Validator.methods[name] = method;
    _Validator.messages[name] = message !== void 0 ? message : _Validator.messages[name];
    if (method.length < 3) {
      _Validator.addClassRules(name, { [name]: true });
    }
  }
  static getHighlightTarget(el) {
    const hl = el.dataset.vxHighlight;
    if (hl)
      return document.getElementById(hl);
    else if (el.tagName.toLowerCase() === "textarea" && el.style.visibility == "hidden" && el.id)
      return document.getElementById("cke_" + el.id) ?? document.getElementById("tiptap_" + el.id);
    else if (el.classList.contains("select2-offscreen") && el.id)
      return document.getElementById("s2id_" + el.id);
  }
  static addCustomRule(element, rule, uniqueName) {
    element = isArrayLike(element) ? element[0] : element;
    if (!element)
      return;
    element.classList.add("customValidate");
    let rules = customValidateRules.get(element);
    if (!rules)
      customValidateRules.set(element, rules = {});
    uniqueName ??= "";
    rules[uniqueName] ??= [];
    rules[uniqueName].push(rule);
  }
  static removeCustomRule(element, uniqueName) {
    element = isArrayLike(element) ? element[0] : element;
    if (!element)
      return;
    const rules = customValidateRules.get(element);
    if (rules) {
      delete rules[uniqueName];
      if (!Object.keys(rules).length) {
        customValidateRules.delete(element);
        element.classList.remove("customValidate");
      }
    } else
      element.classList.remove("customValidate");
  }
  static {
    this.excludedModifierKeys = /* @__PURE__ */ new Set([
      "Shift",
      "Control",
      "Alt",
      "CapsLock",
      "End",
      "Home",
      "ArrowLeft",
      "ArrowUp",
      "ArrowRight",
      "ArrowDown",
      "Insert",
      "NumLock",
      "AltGr"
    ]);
  }
};
function isPollutingKey2(key) {
  return key === "__proto__" || key === "constructor" || key === "prototype";
}
__name(isPollutingKey2, "isPollutingKey");
var addValidationRule = Validator.addCustomRule;
var removeValidationRule = Validator.removeCustomRule;

// src/compat/arrays-compat.ts
function any(array, predicate) {
  return array.some(predicate);
}
__name(any, "any");
function count(array, predicate) {
  let count2 = 0;
  for (let x of array)
    if (predicate(x))
      count2++;
  return count2;
}
__name(count, "count");
function first(array, predicate) {
  for (let x of array)
    if (predicate(x))
      return x;
  throw new Error("first:No element satisfies the condition.!");
}
__name(first, "first");
function groupBy(items, getKey) {
  let result = {
    byKey: /* @__PURE__ */ Object.create(null),
    inOrder: []
  };
  for (let index = 0; index < items.length; index++) {
    const item = items[index];
    let key = getKey(item) ?? "";
    let group = result.byKey[key];
    if (group === void 0) {
      group = {
        order: result.inOrder.length,
        key,
        items: [item],
        start: index
      };
      result.byKey[key] = group;
      result.inOrder.push(group);
    } else {
      group.items.push(item);
    }
  }
  return result;
}
__name(groupBy, "groupBy");
function indexOf(array, predicate) {
  for (let i2 = 0; i2 < array.length; i2++)
    if (predicate(array[i2]))
      return i2;
  return -1;
}
__name(indexOf, "indexOf");
function insert(obj, index, item) {
  if (obj.insert)
    obj.insert(index, item);
  else if (Array.isArray(obj))
    obj.splice(index, 0, item);
  else
    throw new Error("Object does not support insert!");
}
__name(insert, "insert");
var isArray = Array.isArray;
function single(array, predicate) {
  let match;
  let found = false;
  for (let x of array)
    if (predicate(x)) {
      if (found)
        throw new Error("single:sequence contains more than one element.");
      found = true;
      match = x;
    }
  if (!found)
    throw new Error("single:No element satisfies the condition.");
  return match;
}
__name(single, "single");
function toGrouping(items, getKey) {
  let lookup = {};
  for (let x of items) {
    let key = getKey(x) ?? "";
    let d2 = lookup[key];
    if (!d2) {
      d2 = lookup[key] = [];
    }
    d2.push(x);
  }
  return lookup;
}
__name(toGrouping, "toGrouping");
function tryFirst(array, predicate) {
  for (let x of array)
    if (predicate(x))
      return x;
}
__name(tryFirst, "tryFirst");

// src/compat/dialogs-compat.ts
var alert2 = alertDialog;
var confirm = confirmDialog;
var information = informationDialog;
var success = successDialog;
var warning = warningDialog;

// src/compat/formatting-compat.ts
function turkishLocaleToLower(a2) {
  if (!a2)
    return a2;
  return a2.replace(/i/g, "\u0130").replace(/ı/g, "I").toLowerCase();
}
__name(turkishLocaleToLower, "turkishLocaleToLower");
function turkishLocaleToUpper(a2) {
  if (!a2)
    return a2;
  return a2.replace(/i/g, "\u0130").replace(/ı/g, "I").toUpperCase();
}
__name(turkishLocaleToUpper, "turkishLocaleToUpper");
var turkishLocaleCompare = Culture.stringCompare;
var format = stringFormat;
var localeFormat = stringFormatLocale;
function formatDayHourAndMin(n2) {
  if (n2 == null)
    return "";
  if (n2 === 0)
    return "0";
  let days = Math.floor(n2 / 24 / 60);
  let txt = "";
  if (days > 0) {
    txt += days.toString();
  }
  let mins = Math.floor(n2 % (24 * 60) / 60).toString().padStart(2, "0") + ":" + (n2 % 60).toString().padStart(2, "0");
  if (mins != "00:00") {
    if (days > 0)
      txt += ".";
    txt += mins;
  }
  return txt;
}
__name(formatDayHourAndMin, "formatDayHourAndMin");
function parseHourAndMin(value) {
  let v2 = value?.trim() ?? "";
  if (!v2.length)
    return null;
  if (v2.length < 4 || v2.length > 5)
    return NaN;
  let h2, m;
  if (v2.charAt(1) == ":") {
    h2 = parseInteger(v2.substring(0, 1));
    m = parseInteger(v2.substring(2, 4));
  } else {
    if (v2.charAt(2) != ":")
      return NaN;
    h2 = parseInteger(v2.substring(0, 2));
    m = parseInteger(v2.substring(3, 5));
  }
  if (isNaN(h2) || isNaN(m) || h2 < 0 || h2 > 23 || m < 0 || m > 59)
    return NaN;
  return h2 * 60 + m;
}
__name(parseHourAndMin, "parseHourAndMin");
function parseDayHourAndMin(s2) {
  let days;
  let v2 = s2?.trim() ?? "";
  if (!v2.length)
    return null;
  let p2 = v2.split(".");
  if (p2.length == 1) {
    days = parseInteger(p2[0]);
    if (!isNaN(days))
      return days * 24 * 60;
    return parseHourAndMin(p2[0]);
  } else if (p2.length == 2) {
    days = parseInteger(p2[0]);
    let hm = parseHourAndMin(p2[1]);
    if (isNaN(days) || !hm || isNaN(hm))
      return NaN;
    return days * 24 * 60 + hm;
  } else
    return NaN;
}
__name(parseDayHourAndMin, "parseDayHourAndMin");

// src/compat/html-compat.ts
function addEmptyOption(select) {
  addOption(select, "", SelectEditorTexts.EmptyItemText);
}
__name(addEmptyOption, "addEmptyOption");
function addOption(select, key, text2) {
  const option = document.createElement("option");
  option.value = key ?? "";
  option.textContent = text2 ?? "";
  (isArrayLike(select) ? select[0] : select)?.append(option);
}
__name(addOption, "addOption");
var attrEncode = htmlEncode;
function clearOptions(select) {
  select = isArrayLike(select) ? select[0] : select;
  if (select)
    select.innerHTML = "";
}
__name(clearOptions, "clearOptions");
function findElementWithRelativeId(element, relativeId, context) {
  const from = isArrayLike(element) ? element[0] : element;
  const doc = typeof document === "undefined" ? null : document;
  if (from == null)
    return null;
  let noContext = false;
  if (context === void 0) {
    context = from.getRootNode();
    noContext = true;
  }
  let fromId = from.id ?? "";
  while (true) {
    let res = context?.querySelector("#" + fromId + relativeId);
    if (!res && noContext)
      res = doc?.getElementById(fromId + relativeId);
    if (!res && fromId.length) {
      res = context?.querySelector("#" + fromId + "_" + relativeId);
      if (!res && noContext)
        res = doc?.getElementById(fromId + "_" + relativeId);
    }
    if (res || !fromId.length)
      return res ?? null;
    let idx = fromId.lastIndexOf("_");
    if (idx <= 0)
      fromId = "";
    else
      fromId = fromId.substring(0, idx);
  }
}
__name(findElementWithRelativeId, "findElementWithRelativeId");
function newBodyDiv() {
  const element = document.createElement("div");
  document.body.append(element);
  return element;
}
__name(newBodyDiv, "newBodyDiv");
function outerHtml(element) {
  const el = document.createElement("i");
  el.append((isArrayLike(element) ? element[0] : element).cloneNode(true));
  return el.innerHTML;
}
__name(outerHtml, "outerHtml");

// src/compat/init-global-mappings.ts
function copyProps(source, target) {
  if (!source || !target)
    return;
  Object.keys(source).forEach(function(key) {
    if (source[key] != null && target[key] == null && key !== "_") {
      Object.defineProperty(target, key, {
        get: /* @__PURE__ */ __name(function() {
          return source[key];
        }, "get"),
        set: /* @__PURE__ */ __name(function(v2) {
          source[key] = v2;
        }, "set"),
        enumerable: true,
        configurable: true
      });
    }
  });
}
__name(copyProps, "copyProps");
function initGlobalMappings({
  corelib,
  globals,
  domwise,
  sleekgrid: sleekgrid2,
  extensions,
  proextensions,
  bootstrap: bootstrap2,
  flatpickr: flatpickr2,
  glightbox,
  mousetrap,
  nprogress,
  sortable
}) {
  globals = globals ?? getGlobalObject();
  if (corelib) {
    if (!globals.Serenity) {
      globals.Serenity = corelib;
    } else if (globals.Serenity !== corelib) {
      copyProps(corelib, globals.Serenity);
    }
  }
  if (corelib || sleekgrid2 || domwise || extensions || proextensions) {
    if (!globals.Serenity) {
      globals.Serenity = /* @__PURE__ */ Object.create(null);
    }
    if (sleekgrid2) {
      if (!globals.Slick) {
        globals.Slick = sleekgrid2;
      } else if (globals.Slick !== sleekgrid2) {
        copyProps(sleekgrid2, globals.Slick);
      }
      if (globals.Serenity !== sleekgrid2) {
        copyProps(sleekgrid2, globals.Serenity);
      }
      ["Aggregators", "AggregateFormatting"].forEach(function(x) {
        globals.Slick[x] = globals.Slick[x] || {};
        copyProps(globals.Serenity[x], globals.Slick[x]);
      });
      ["RemoteView"].forEach(function(x) {
        globals.Slick[x] = globals.Serenity[x];
      });
    } else if (globals.Slick && globals.Serenity !== globals.Slick) {
      copyProps(globals.Slick, globals.Serenity);
    }
    if (!globals.Q) {
      globals.Q = globals.Serenity;
    } else if (globals.Q !== globals.Serenity) {
      copyProps(globals.Serenity, globals.Q);
    }
    if (domwise && globals.Serenity !== domwise) {
      copyProps(domwise, globals.Serenity);
    }
    if (extensions) {
      if (globals.Serenity !== extensions) {
        copyProps(extensions, globals.Serenity);
      }
      if (!globals.Serenity.Extensions) {
        globals.Serenity.Extensions = extensions;
      } else if (globals.Serenity.Extensions !== extensions) {
        copyProps(extensions, globals.Serenity.Extensions);
      }
    }
    if (proextensions) {
      if (globals.Serenity !== proextensions) {
        copyProps(proextensions, globals.Serenity);
      }
    }
  }
  if (bootstrap2) {
    if (typeof bootstrap2.default === "object")
      bootstrap2 = bootstrap2.default;
    globals.bootstrap = bootstrap2;
  }
  if (mousetrap) {
    if (typeof mousetrap.default === "function")
      mousetrap = mousetrap.default;
    globals.Mousetrap = mousetrap;
  }
  if (sortable) {
    if (typeof sortable.default === "function")
      sortable = sortable.default;
    globals.Sortable = sortable;
  }
  if (nprogress) {
    if (typeof nprogress.default === "object")
      nprogress = nprogress.default;
    globals.NProgress = nprogress;
  }
  if (glightbox) {
    if (typeof glightbox.default === "function")
      glightbox = glightbox.default;
    globals.GLightbox = glightbox;
  }
  if (flatpickr2) {
    if (typeof flatpickr2.default === "function")
      flatpickr2 = flatpickr2.default;
    globals.flatpickr = flatpickr2;
    initFlatpickrLocale(flatpickr2);
  }
}
__name(initGlobalMappings, "initGlobalMappings");
function initFlatpickrLocale(flatpickr2) {
  if (!flatpickr2 || !flatpickr2.l10ns)
    return;
  let culture = typeof document === "undefined" ? "en" : (document.documentElement?.lang || "en").toLowerCase();
  if (flatpickr2.l10ns[culture]) {
    flatpickr2.localize(flatpickr2.l10ns[culture]);
  } else {
    culture = culture.split("-")[0];
    flatpickr2.l10ns[culture] && flatpickr2.localize(flatpickr2.l10ns[culture]);
  }
}
__name(initFlatpickrLocale, "initFlatpickrLocale");

// src/compat/layouttimer.ts
var LayoutTimer;
((LayoutTimer2) => {
  let timeout;
  let nextKey = 0;
  let regCount = 0;
  const regs = {};
  function startTimer() {
    if (timeout == null && regCount > 0) {
      timeout = setTimeout(onTimeout, 100);
    }
  }
  __name(startTimer, "startTimer");
  function clearTimer() {
    if (timeout != null) {
      clearTimeout(timeout);
      timeout = null;
    }
  }
  __name(clearTimer, "clearTimer");
  function getSize(el) {
    if (el === window) {
      return {
        width: window.innerWidth,
        height: window.innerHeight
      };
    } else {
      return {
        width: el.offsetWidth,
        height: el.offsetHeight
      };
    }
  }
  __name(getSize, "getSize");
  function onTimeout() {
    for (const key in regs) {
      const reg = regs[key];
      try {
        const el = reg.element();
        if (!el)
          continue;
        let { width: w2, height: h2 } = getSize(el);
        if (w2 <= 0 || h2 <= 0) {
          reg.storedWidth = w2;
          reg.storedHeight = h2;
          reg.debouncedTimes = 0;
          continue;
        }
        let debounced = false;
        try {
          if (reg.width && reg.storedWidth !== w2 || reg.height && reg.storedHeight !== h2 || !reg.width && !reg.height && (!w2 !== !reg.storedWidth || !h2 !== !reg.storedHeight)) {
            if (reg.debounceTimes > 0 && ++reg.debouncedTimes <= reg.debounceTimes) {
              debounced = true;
              continue;
            }
            try {
              reg.debouncedTimes = 0;
              reg.handler();
            } finally {
              ({ width: w2, height: h2 } = getSize(el));
            }
          }
        } finally {
          if (!debounced) {
            reg.storedWidth = w2;
            reg.storedHeight = h2;
            reg.debouncedTimes = 0;
          }
        }
      } catch (e2) {
        console.log(e2);
      }
    }
    clearTimer();
    startTimer();
  }
  __name(onTimeout, "onTimeout");
  function store(key) {
    const reg = regs[key];
    if (!reg)
      return;
    const el = reg.element();
    if (!el)
      return;
    const { width: w2, height: h2 } = getSize(el);
    reg.storedWidth = w2;
    reg.storedHeight = h2;
    reg.debouncedTimes = 0;
  }
  LayoutTimer2.store = store;
  __name(store, "store");
  function trigger(key) {
    const reg = regs[key];
    if (!reg)
      return;
    store(key);
    if (reg.storedWidth > 0 && reg.storedHeight > 0) {
      reg.handler();
      reg.debouncedTimes = 0;
    }
    store(key);
  }
  LayoutTimer2.trigger = trigger;
  __name(trigger, "trigger");
  function onSizeChange(element, handler, opt) {
    if (handler == null)
      throw "Layout handler can't be null!";
    regs[++nextKey] = {
      element,
      handler,
      width: opt?.width !== false,
      height: opt?.height !== false,
      debounceTimes: opt?.debounceTimes || 0,
      debouncedTimes: 0
    };
    regCount++;
    store(nextKey);
    startTimer();
    return nextKey;
  }
  LayoutTimer2.onSizeChange = onSizeChange;
  __name(onSizeChange, "onSizeChange");
  function onWidthChange(element, handler, opt) {
    return onSizeChange(element, handler, { width: true, height: false, debounceTimes: opt?.debounceTimes });
  }
  LayoutTimer2.onWidthChange = onWidthChange;
  __name(onWidthChange, "onWidthChange");
  function onHeightChange(element, handler, opt) {
    return onSizeChange(element, handler, { width: false, height: true, debounceTimes: opt?.debounceTimes });
  }
  LayoutTimer2.onHeightChange = onHeightChange;
  __name(onHeightChange, "onHeightChange");
  function onShown(element, handler, opt) {
    return onSizeChange(element, handler, { width: false, height: false, debounceTimes: opt?.debounceTimes });
  }
  LayoutTimer2.onShown = onShown;
  __name(onShown, "onShown");
  function off(key) {
    const reg = regs[key];
    if (!reg)
      return 0;
    delete regs[key];
    regCount--;
    if (regCount <= 0)
      clearTimer();
    return 0;
  }
  LayoutTimer2.off = off;
  __name(off, "off");
})(LayoutTimer || (LayoutTimer = {}));
function executeOnceWhenVisible(el, callback) {
  el = isArrayLike(el) ? el[0] : el;
  if (!el)
    return null;
  if (el.offsetWidth > 0 && el.offsetHeight > 0) {
    callback();
    return null;
  }
  const timer = LayoutTimer.onShown(() => el, () => {
    LayoutTimer.off(timer);
    callback();
  });
  return timer;
}
__name(executeOnceWhenVisible, "executeOnceWhenVisible");
function executeEverytimeWhenVisible(el, callback, callNowIfVisible) {
  el = isArrayLike(el) ? el[0] : el;
  if (!el)
    return null;
  if (callNowIfVisible && el.offsetWidth > 0 && el.offsetHeight > 0) {
    callback();
  }
  const timer = LayoutTimer.onShown(() => el, () => {
    callback();
  });
  return timer;
}
__name(executeEverytimeWhenVisible, "executeEverytimeWhenVisible");

// src/compat/router.ts
var Router;
((Router2) => {
  let oldURL;
  let resolving = 0;
  let autoinc = 0;
  let ignoreHashLock = 0;
  let ignoreHashUntil = 0;
  let hashAnchorClickValue;
  let hashAnchorClickTime;
  Router2.enabled = true;
  function isEquivalentUrl(url1, url2) {
    return url1 == url2 || url1 == url2 + "#" || url2 == url1 + "#";
  }
  __name(isEquivalentUrl, "isEquivalentUrl");
  function navigate(newHash, tryBack, silent) {
    if (!Router2.enabled || resolving > 0)
      return;
    newHash = newHash || "";
    newHash = newHash.replace(/^#/, "");
    newHash = !newHash ? "" : "#" + newHash;
    const newURL = window.location.href.replace(/#$/, "").replace(/#.*$/, "") + newHash;
    if (newURL != window.location.href) {
      if (tryBack && oldURL != null && isEquivalentUrl(oldURL, newURL)) {
        if (silent)
          ignoreHashChange();
        oldURL = null;
        window.history.back();
        return;
      }
      if (silent)
        ignoreHashChange();
      oldURL = window.location.href;
      window.location.hash = newHash;
    }
  }
  Router2.navigate = navigate;
  __name(navigate, "navigate");
  function replace(newHash, tryBack) {
    navigate(newHash, tryBack, true);
  }
  Router2.replace = replace;
  __name(replace, "replace");
  function replaceLast(newHash, tryBack) {
    if (!Router2.enabled)
      return;
    let current = window.location.hash || "";
    if (current.charAt(0) == "#")
      current = current.substring(1);
    const parts = current.split("/+/");
    if (parts.length > 1) {
      if (newHash && newHash.length) {
        parts[parts.length - 1] = newHash;
        newHash = parts.join("/+/");
      } else {
        parts.splice(parts.length - 1, 1);
        newHash = parts.join("/+/");
      }
    }
    Router2.replace(newHash, tryBack);
  }
  Router2.replaceLast = replaceLast;
  __name(replaceLast, "replaceLast");
  const ignoredSelector = ".s-MessageDialog, .s-MessageModal, .s-PromptDialog, .route-ignore";
  function isIgnoredDialog(el) {
    return !!(el?.closest(ignoredSelector) || Dialog.getInstance(el)?.getContentNode()?.closest(ignoredSelector));
  }
  __name(isIgnoredDialog, "isIgnoredDialog");
  function isVisibleOrHiddenBy(el) {
    return el.offsetWidth > 0 && el.offsetHeight > 0 || // if visible
    !!(!el.closest(".hidden, [hidden]") && el.closest("[data-hiddenby]"));
  }
  __name(isVisibleOrHiddenBy, "isVisibleOrHiddenBy");
  function getVisibleOrHiddenByDialogs() {
    const visibleDialogs = Array.from(document.querySelectorAll(".modal, .panel-body, .ui-dialog-content")).filter(isVisibleOrHiddenBy).filter((x) => !isIgnoredDialog(x));
    visibleDialogs.sort((a2, b2) => {
      return parseInt(a2.dataset.qrouterorder || "0", 10) - parseInt(b2.dataset.qrouterorder || "0", 10);
    });
    return visibleDialogs;
  }
  __name(getVisibleOrHiddenByDialogs, "getVisibleOrHiddenByDialogs");
  let pendingDialogHash;
  let pendingDialogElement;
  let pendingDialogOwner;
  let pendingDialogPreHash;
  function onDialogOpen(ownerEl, element, dialogHash) {
    const route = [];
    element = isArrayLike(element) ? element[0] : element;
    if (element && pendingDialogElement && element === pendingDialogElement || element.contains(pendingDialogElement)) {
      dialogHash = pendingDialogHash ?? dialogHash;
      ownerEl = pendingDialogOwner;
    }
    pendingDialogHash = null;
    pendingDialogElement = null;
    pendingDialogOwner = null;
    pendingDialogPreHash = null;
    ownerEl = isArrayLike(ownerEl) ? ownerEl[0] : ownerEl;
    const ownerIsDialog = ownerEl?.matches(".ui-dialog-content, .panel-body, .modal-content");
    const ownerDlgInst = Dialog.getInstance(ownerEl);
    let value = dialogHash();
    let idPrefix;
    if (ownerDlgInst) {
      const dialogs = getVisibleOrHiddenByDialogs();
      const index = dialogs.indexOf(ownerDlgInst.getEventsNode());
      for (let i2 = 0; i2 <= index; i2++) {
        const q = dialogs[i2].dataset.qroute;
        if (q && q.length)
          route.push(q);
      }
      if (!ownerIsDialog) {
        idPrefix = ownerDlgInst?.getContentNode()?.getAttribute("id");
        if (idPrefix) {
          idPrefix += "_";
          const id = ownerEl?.getAttribute("id");
          if (id && id.startsWith(idPrefix))
            value = id.substring(idPrefix.length) + "@" + value;
        }
      }
    } else {
      const id = ownerEl?.getAttribute("id");
      if (id && (!ownerEl.classList.contains("route-handler") || document.querySelector(".route-handler")?.getAttribute("id") != id))
        value = id + "@" + value;
    }
    route.push(value);
    element.dataset.qroute = value;
    Router2.replace(route.join("/+/"));
  }
  __name(onDialogOpen, "onDialogOpen");
  function dialog(owner, element, dialogHash) {
    if (!Router2.enabled)
      return;
    const el = isArrayLike(element) ? element[0] : element;
    pendingDialogElement = el;
    pendingDialogHash = dialogHash;
    pendingDialogOwner = isArrayLike(owner) ? owner[0] : owner;
    pendingDialogPreHash = resolvingPreRoute;
  }
  Router2.dialog = dialog;
  __name(dialog, "dialog");
  let resolvingPreRoute;
  let resolveIndex = 0;
  Router2.mightBeRouteRegex = /^(new$|edit\/|![0-9]+$)/;
  function resolve(newHash) {
    resolveIndex++;
    if (!Router2.enabled) {
      return "disabled";
    }
    const resolvingCurrent = newHash == null;
    newHash = newHash ?? window.location.hash ?? "";
    if (newHash.charAt(0) == "#")
      newHash = newHash.substring(1);
    const newParts = newHash.split("/+/");
    if (resolvingCurrent && (hashAnchorClickTime && (/* @__PURE__ */ new Date()).getTime() - hashAnchorClickTime < 100) && hashAnchorClickValue === newHash && (newHash != "" || window.location.href.indexOf("#") >= 0) && newParts.length == 1 && !newParts.some((x) => Router2.mightBeRouteRegex.test(x))) {
      return "skipped";
    }
    resolving++;
    try {
      let closeMessages = function() {
        if (closedMessages) {
          return;
        }
        closedMessages = true;
        Array.from(document.querySelectorAll(".s-MessageDialog")).reverse().forEach((x) => {
          if (Fluent.isVisibleLike(x)) {
            Dialog.getInstance(x)?.close();
          }
        });
      };
      __name(closeMessages, "closeMessages");
      const dialogs = getVisibleOrHiddenByDialogs();
      const oldParts = dialogs.map((el) => el.dataset.qroute);
      let same = 0;
      while (same < dialogs.length && same < newParts.length && oldParts[same] == newParts[same]) {
        same++;
      }
      let closedMessages = false;
      for (let i2 = same; i2 < dialogs.length; i2++) {
        const d2 = dialogs[i2];
        Dialog.getInstance(d2)?.close("router");
        closeMessages();
      }
      for (let i2 = same; i2 < Math.min(newParts.length, 5); i2++) {
        let route = newParts[i2];
        const routeParts = route.split("@");
        let handler;
        if (routeParts.length == 2) {
          const dialog2 = i2 > 0 ? dialogs[i2 - 1] : null;
          if (dialog2) {
            const idPrefix = Dialog.getInstance(dialog2)?.getContentNode().getAttribute("id") ?? dialog2.getAttribute("id");
            if (idPrefix) {
              handler = document.querySelector("#" + idPrefix + "_" + routeParts[0]);
              if (handler) {
                route = routeParts[1];
              }
            }
          }
          if (!handler) {
            handler = document.querySelector("#" + routeParts[0]);
            if (handler) {
              route = routeParts[1];
            }
          }
          if (!handler)
            return "missinghandler";
        }
        if (!handler) {
          handler = i2 > 0 ? dialogs[i2 - 1] : document.querySelector(".route-handler");
        }
        if (route.startsWith("!"))
          return "shebang";
        resolvingPreRoute = newParts.slice(0, i2).join("/+/");
        try {
          closeMessages();
          Fluent.trigger(handler, "handleroute", {
            route,
            parts: newParts,
            index: i2,
            isInitial: resolveIndex <= 3
          });
        } finally {
          resolvingPreRoute = null;
          return "calledhandler";
        }
      }
    } finally {
      resolving--;
    }
  }
  Router2.resolve = resolve;
  __name(resolve, "resolve");
  function hashChange(_2) {
    if (ignoreHashLock > 0) {
      if ((/* @__PURE__ */ new Date()).getTime() > ignoreHashUntil) {
        ignoreHashLock = 0;
      } else {
        ignoreHashLock--;
        return;
      }
    }
    resolve();
  }
  __name(hashChange, "hashChange");
  function ignoreHashChange(expiration) {
    ignoreHashLock++;
    ignoreHashUntil = Math.max(ignoreHashUntil, (/* @__PURE__ */ new Date()).getTime() + (expiration ?? 1e3));
  }
  Router2.ignoreHashChange = ignoreHashChange;
  __name(ignoreHashChange, "ignoreHashChange");
  window.addEventListener("hashchange", hashChange, false);
  let routerOrder = 1;
  if (typeof document !== "undefined") {
    let onDocumentDialogOpen = function(event) {
      if (!Router2.enabled)
        return;
      const dlg = event.target;
      if (!dlg || isIgnoredDialog(dlg))
        return;
      dlg.dataset.qrouterorder = (routerOrder++).toString();
      if (dlg.dataset.qroute)
        return;
      dlg.dataset.qprhash = resolvingPreRoute ?? pendingDialogPreHash ?? window.location.hash;
      let owner = getVisibleOrHiddenByDialogs().filter((x) => x !== dlg).pop();
      if (!owner)
        owner = document.documentElement;
      onDialogOpen(owner, dlg, () => {
        return "!" + (++autoinc).toString(36);
      });
    }, shouldTryBack = function(e2) {
      if (isIgnoredDialog(e2.target))
        return false;
      if (e2.target?.closest?.(".s-MessageDialog, .s-MessageModal") || e2.key === "Escape")
        return true;
      let orgEvent = e2.originalEvent ?? e2;
      if (!orgEvent)
        return false;
      if (orgEvent.key === "Escape" || orgEvent.target?.matches?.(".close, .panel-titlebar-close, .ui-dialog-titlebar-close"))
        return true;
      return false;
    }, closeHandler = function(e2) {
      const dlg = e2.target;
      if (!dlg || isIgnoredDialog(e2.target))
        return;
      delete dlg.dataset.qroute;
      const prhash = dlg.dataset.qprhash;
      let tryBack = shouldTryBack(e2);
      if (prhash != null)
        Router2.replace(prhash, tryBack);
      else
        Router2.replaceLast("", tryBack);
    };
    __name(onDocumentDialogOpen, "onDocumentDialogOpen");
    __name(shouldTryBack, "shouldTryBack");
    __name(closeHandler, "closeHandler");
    Fluent.on(document, "dialogopen", ".ui-dialog-content", onDocumentDialogOpen);
    Fluent.on(document, "shown.bs.modal", ".modal", onDocumentDialogOpen);
    Fluent.on(document, "panelopen", ".panel-body", onDocumentDialogOpen);
    Fluent.on(document, "click", (e2) => {
      if (!Fluent.isDefaultPrevented(e2)) {
        const a2 = e2.target.closest?.('a[href^="#"]');
        if (a2) {
          hashAnchorClickTime = (/* @__PURE__ */ new Date()).getTime();
          hashAnchorClickValue = a2.hash.substring(1);
        }
      }
    });
    Fluent.on(document, "dialogclose.qrouter", closeHandler);
    Fluent.on(document, "hidden.bs.modal", closeHandler);
    Fluent.on(document, "panelclose.qrouter", closeHandler);
  }
})(Router || (Router = {}));

// src/compat/layout.ts
function initWidgetPage(widgetOrType, props, defaultElement, noRoute) {
  let widget;
  if (widgetOrType?.domNode) {
    if (props && typeof props.element === "function") {
      props.element(widgetOrType.domNode);
    }
    widget = widgetOrType;
  } else {
    props ??= {};
    let oldFunction;
    if (defaultElement) {
      if (typeof props.element === "function") {
        oldFunction = props.element;
        props.element = defaultElement;
      } else {
        props.element ??= defaultElement;
      }
    }
    widget = new widgetOrType(props);
    oldFunction?.(widget.domNode);
    widget.init();
  }
  initFullHeightGridPage(widget.domNode, { setHeight: false, noRoute });
  return widget;
}
__name(initWidgetPage, "initWidgetPage");
function GridPageInit({ type, props }) {
  return initWidgetPage(type, props, "#GridDiv").domNode;
}
__name(GridPageInit, "GridPageInit");
function PanelPageInit({ type, props }) {
  return initWidgetPage(type, props, "#Panel", false).domNode;
}
__name(PanelPageInit, "PanelPageInit");
function gridPageInit(gridOrType, props) {
  return initWidgetPage(gridOrType, props, "#GridDiv");
}
__name(gridPageInit, "gridPageInit");
function panelPageInit(panelOrType, props) {
  return initWidgetPage(panelOrType, props, "#PanelDiv", true);
}
__name(panelPageInit, "panelPageInit");
function initFullHeightGridPage(gridDiv, opt) {
  const el = isArrayLike(gridDiv) ? gridDiv[0] : gridDiv instanceof HTMLElement ? gridDiv : gridDiv.domNode;
  document.documentElement.classList.add("full-height-page");
  el.classList.add("responsive-height");
  let setHeight = opt?.setHeight ?? (getjQuery() && (!el.classList.contains("s-DataGrid") && !el.classList.contains("s-Panel")));
  let layout = /* @__PURE__ */ __name(function() {
    setHeight && layoutFillHeight(el);
    Fluent.trigger(el, "layout");
  }, "layout");
  let layoutTimerKey;
  if (document.body.classList.contains("has-layout-event")) {
    Fluent.on(document.body, "layout", layout);
  } else if (window.Metronic?.addResizeHandler) {
    window.Metronic.addResizeHandler(layout);
  } else {
    layoutTimerKey = LayoutTimer.onSizeChange(() => window, layout, { debounceTimes: 1 });
  }
  layout();
  addDisposingListener(el, () => {
    LayoutTimer.off(layoutTimerKey);
    Fluent.off(document.body, "layout", layout);
  });
  if (!opt?.noRoute && typeof document !== "undefined" && !document.body?.getAttribute?.("data-fhrouteinit")) {
    document.body?.setAttribute?.("data-fhrouteinit", "true");
    typeof Router !== "undefined" && Router.resolve?.();
  }
}
__name(initFullHeightGridPage, "initFullHeightGridPage");
function layoutFillHeightValue(element) {
  let h2 = 0;
  let $2 = getjQuery();
  element = isArrayLike(element) ? element[0] : element;
  if (!$2 && element)
    return parseInt(getComputedStyle(element).height, 10);
  $2(element).parent().children().not(element).each(function(i2, e2) {
    let q = $2(e2);
    if (q.is(":visible")) {
      h2 += q.outerHeight(true);
    }
  });
  h2 = $2(element).parent().height() - h2;
  if ($2(element).css("box-sizing") !== "border-box") {
    h2 = h2 - ($2(element).outerHeight(true) - $2(element).height());
  }
  return h2;
}
__name(layoutFillHeightValue, "layoutFillHeightValue");
function layoutFillHeight(element) {
  let h2 = layoutFillHeightValue(element);
  let n2 = Math.round(h2) + "px";
  element = isArrayLike(element) ? element[0] : element;
  if (element.style.height != n2)
    element.style.height = n2;
}
__name(layoutFillHeight, "layoutFillHeight");
function isMobileView() {
  return typeof window !== "undefined" && (window.matchMedia?.("(max-width: 767px)")?.matches ?? window.innerWidth < 768);
}
__name(isMobileView, "isMobileView");
function triggerLayoutOnShow(element) {
  element = isArrayLike(element) ? element[0] : element;
  if (!element)
    return;
  executeEverytimeWhenVisible(element, function() {
    Fluent.trigger(element, "layout");
  }, true);
}
__name(triggerLayoutOnShow, "triggerLayoutOnShow");
function centerDialog(el) {
  el = isArrayLike(el) ? el[0] : el;
  let dlg = el.closest(".ui-dialog");
  if (!dlg)
    return;
  dlg = getjQuery()(dlg);
  dlg.position?.({ at: "center center", of: window });
  let pos = dlg.position?.();
  if (pos.left < 0)
    dlg.css("left", "0px");
  if (pos.top < 0)
    dlg.css("top", "0px");
}
__name(centerDialog, "centerDialog");

// src/compat/scriptdata-compat.ts
var ScriptData;
((ScriptData2) => {
  ScriptData2.canLoad = canLoadScriptData;
  ScriptData2.ensure = ensureScriptDataSync;
  ScriptData2.set = setScriptData;
  function bindToChange(name, onChange) {
    if (typeof document !== "undefined" && document.addEventListener) {
      const unbind = /* @__PURE__ */ __name(function() {
        onChange && typeof document !== "undefined" && document.removeEventListener?.("scriptdatachange." + name, onChange);
        onChange = null;
      }, "unbind");
      document.addEventListener("scriptdatachange." + name, onChange);
      return unbind;
    }
  }
  ScriptData2.bindToChange = bindToChange;
  __name(bindToChange, "bindToChange");
  function reload(name, dynJS) {
    getScriptDataHash(name, true);
    setScriptData(name, null);
    return (0, ScriptData2.ensure)(name, dynJS);
  }
  ScriptData2.reload = reload;
  __name(reload, "reload");
  async function reloadAsync(name) {
    return await getScriptData(name, true);
  }
  ScriptData2.reloadAsync = reloadAsync;
  __name(reloadAsync, "reloadAsync");
})(ScriptData || (ScriptData = {}));
function canLoadScriptData(name) {
  return peekScriptData(name) != null || getScriptDataHash(name) != null;
}
__name(canLoadScriptData, "canLoadScriptData");
function getLookup(key) {
  return ScriptData.ensure("Lookup." + key);
}
__name(getLookup, "getLookup");
function reloadLookup(key) {
  return ScriptData.reload("Lookup." + key);
}
__name(reloadLookup, "reloadLookup");
function getColumns(key) {
  return getColumnsData(key).items;
}
__name(getColumns, "getColumns");
async function getColumnsAsync(key) {
  return (await getColumnsScript(key)).items;
}
__name(getColumnsAsync, "getColumnsAsync");
function getColumnsData(key) {
  return ScriptData.ensure("Columns." + key);
}
__name(getColumnsData, "getColumnsData");
var getColumnsDataAsync = getColumnsScript;
function getForm(key) {
  return getFormData(key).items;
}
__name(getForm, "getForm");
async function getFormAsync(key) {
  return (await getFormScript(key)).items;
}
__name(getFormAsync, "getFormAsync");
function getFormData(key) {
  return ScriptData.ensure("Form." + key);
}
__name(getFormData, "getFormData");
var getFormDataAsync = getFormScript;
var compatExports = {
  canLoadScriptData,
  getColumns,
  getColumnsAsync,
  getForm,
  getFormAsync,
  getLookup,
  getLookupAsync,
  getRemoteData,
  getRemoteDataAsync,
  reloadLookup,
  reloadLookupAsync,
  ScriptData
};
var global3 = getGlobalObject();
var serenity2 = global3.Serenity || (global3.Serenity = /* @__PURE__ */ Object.create(null));
for (const i2 in compatExports)
  if (serenity2[i2] == null)
    serenity2[i2] = compatExports[i2];

// src/compat/services-compat.tsx
function setEquality(request, field, value) {
  if (request.EqualityFilter == null) {
    request.EqualityFilter = {};
  }
  request.EqualityFilter[field] = value;
}
__name(setEquality, "setEquality");
function postToCommon(url, form, target) {
  if (isSameOrigin(resolveUrl(url))) {
    const csrfToken = getCookie("CSRF-TOKEN");
    if (csrfToken) {
      form.appendChild(jsx("input", { type: "hidden", name: "__RequestVerificationToken", value: csrfToken }));
    }
  }
  form.method = "POST";
  form.action = url;
  form.appendChild(jsx("input", { type: "submit" }));
  target && (form.target = target);
  document.body.appendChild(form);
  form.submit();
  window.setTimeout(function() {
    form.remove();
  }, 0);
}
__name(postToCommon, "postToCommon");
function postToService(options) {
  postToCommon(options.url ? resolveUrl(options.url) : resolveServiceUrl(options.service), jsx("form", { children: jsx("input", { type: "hidden", name: "request", value: JSON.stringify(options.request) }) }), options.target);
}
__name(postToService, "postToService");
function postToUrl(options) {
  postToCommon(resolveUrl(options.url), jsx("form", { children: options.params && Object.keys(options.params).map(
    (k) => jsx("input", { type: "hidden", name: k, value: options.params[k] })
  ) }), options.target);
}
__name(postToUrl, "postToUrl");

// src/compat/strings-compat.ts
function endsWith(s2, suffix) {
  return s2.endsWith(suffix);
}
__name(endsWith, "endsWith");
function isEmptyOrNull(s2) {
  return s2 == null || s2.length === 0;
}
__name(isEmptyOrNull, "isEmptyOrNull");
function isTrimmedEmpty(s2) {
  return !s2?.trim();
}
__name(isTrimmedEmpty, "isTrimmedEmpty");
function padLeft(s2, len, ch = " ") {
  s2 = s2 == null ? "" : s2.toString();
  if (s2.padStart)
    return s2.padStart(len, ch);
  while (s2.length < len)
    s2 = ch + s2;
  return s2;
}
__name(padLeft, "padLeft");
function startsWith(s2, prefix) {
  return s2.startsWith(prefix);
}
__name(startsWith, "startsWith");
function toSingleLine(str) {
  return replaceAll(replaceAll(trimToEmpty(str), "\r\n", " "), "\n", " ").trim();
}
__name(toSingleLine, "toSingleLine");
var trimEnd = /* @__PURE__ */ __name(function(s2) {
  return s2 == null ? "" : s2.trimEnd?.() ?? s2.replace(/(?<!\s)\s+$/, "");
}, "trimEnd");
var trimStart = /* @__PURE__ */ __name(function(s2) {
  return s2 == null ? "" : s2.trimStart?.() ?? s2.replace(/^\s*/, "");
}, "trimStart");
function trim(s2) {
  return s2?.trim();
}
__name(trim, "trim");
function trimToEmpty(s2) {
  return (s2 ?? "").trim();
}
__name(trimToEmpty, "trimToEmpty");
function trimToNull(s2) {
  if (s2 == null)
    return null;
  s2 = trim(s2);
  return s2.length === 0 ? null : s2;
}
__name(trimToNull, "trimToNull");
function replaceAll(str, find, replace) {
  str = String(str ?? "");
  return str.replaceAll?.(find, replace) ?? str.split(find).join(replace);
}
__name(replaceAll, "replaceAll");
function zeroPad(n2, len) {
  if (n2 == null)
    return "";
  let s2 = n2.toString();
  while (s2.length < len)
    s2 = "0" + s2;
  return s2;
}
__name(zeroPad, "zeroPad");

// src/compat/system-compat.ts
function coalesce(a2, b2) {
  return a2 ?? b2;
}
__name(coalesce, "coalesce");
function isValue(a2) {
  return a2 != null;
}
__name(isValue, "isValue");
function extend(a2, b2) {
  return Object.assign(a2, b2);
}
__name(extend, "extend");
var today = /* @__PURE__ */ __name(() => {
  const d2 = /* @__PURE__ */ new Date();
  return new Date(d2.getFullYear(), d2.getMonth(), d2.getDate());
}, "today");
function deepClone(a2) {
  let result = a2;
  const type = {}.toString.call(a2).slice(8, -1);
  if (type == "Set") {
    return new Set([...a2].map((value) => deepClone(value)));
  }
  if (type == "Map") {
    return new Map([...a2].map((kv) => [deepClone(kv[0]), deepClone(kv[1])]));
  }
  if (type == "Date") {
    return new Date(a2.getTime());
  }
  if (type == "RegExp") {
    return RegExp(a2.source, getRegExpFlags(a2));
  }
  if (type == "Array" || type == "Object") {
    result = Array.isArray(a2) ? [] : {};
    for (const key in a2) {
      result[key] = deepClone(a2[key]);
    }
  }
  return result;
}
__name(deepClone, "deepClone");
function getRegExpFlags(regExp) {
  if (typeof regExp.source.flags == "string") {
    return regExp.source.flags;
  } else {
    const flags = [];
    regExp.global && flags.push("g");
    regExp.ignoreCase && flags.push("i");
    regExp.multiline && flags.push("m");
    regExp.sticky && flags.push("y");
    regExp.unicode && flags.push("u");
    return flags.join("");
  }
}
__name(getRegExpFlags, "getRegExpFlags");
var TypeMemberKind = /* @__PURE__ */ ((TypeMemberKind2) => {
  TypeMemberKind2[TypeMemberKind2["field"] = 4] = "field";
  TypeMemberKind2[TypeMemberKind2["property"] = 16] = "property";
  return TypeMemberKind2;
})(TypeMemberKind || {});
function getTypeMembers(type, memberKinds) {
  const result = [];
  do {
    const members = peekTypeInfo(type)?.["members"];
    if (members) {
      for (const member of members) {
        if (member && (memberKinds == null || member.kind & memberKinds) && !result.some((x) => x.name === member.name))
          result.push(member);
      }
    }
  } while (type = getBaseType(type));
  return result;
}
__name(getTypeMembers, "getTypeMembers");
function merge2(arr1, arr2) {
  if (!arr1 || !arr2)
    return (arr1 || arr2 || []).slice();
  function distinct(arr) {
    return arr.filter((item, pos) => arr.indexOf(item) === pos);
  }
  __name(distinct, "distinct");
  return distinct(arr1.concat(arr2));
}
__name(merge2, "merge");
function addTypeMember(type, member) {
  if (!type)
    throw new Error("addTypeMember: type is null");
  const typeInfo = ensureTypeInfo(type);
  const members = typeInfo["members"] ??= [];
  const existing = members.find((m) => m.name === member.name);
  if (existing) {
    existing.kind ??= member.kind;
    member.attr && (existing.attr = merge2(existing.attr, member.attr));
    member.getter != null && (existing.getter = member.getter);
    member.setter != null && (existing.setter = member.setter);
    return existing;
  } else {
    members.push(member);
    return member;
  }
}
__name(addTypeMember, "addTypeMember");
function getTypes() {
  const result = [];
  const types = getGlobalTypeRegistry();
  for (const t2 in types) {
    if (Object.prototype.hasOwnProperty.call(types, t2))
      result.push(types[t2]);
  }
  return result;
}
__name(getTypes, "getTypes");
function clearKeys(d2) {
  for (const n2 in d2) {
    if (Object.prototype.hasOwnProperty.call(d2, n2))
      delete d2[n2];
  }
}
__name(clearKeys, "clearKeys");
function keyOf(prop) {
  return prop;
}
__name(keyOf, "keyOf");
function cast(instance, type) {
  if (instance == null)
    return instance;
  else if (isInstanceOfType(instance, type))
    return instance;
  throw "Cannot cast object to type " + getTypeFullName(type);
}
__name(cast, "cast");
function safeCast(instance, type) {
  return isInstanceOfType(instance, type) ? instance : null;
}
__name(safeCast, "safeCast");

// src/compat/validation.ts
function validatorAbortHandler(validator) {
  delete validator.settings.abortHandler;
  validator.settings.submitHandler = function() {
    return false;
  };
}
__name(validatorAbortHandler, "validatorAbortHandler");
function validateOptions(options) {
  return Object.assign({}, {
    errorPlacement: /* @__PURE__ */ __name(function(place, elem) {
      const element = isArrayLike(elem) ? elem[0] : elem;
      let field = null;
      let vx = element.getAttribute("data-vx-id");
      if (vx) {
        field = document.querySelector("#" + vx);
      }
      if (!field) {
        field = element.closest("div.field");
        if (field) {
          let inner = field.querySelector("div.vx");
          if (inner)
            field = inner;
        } else
          field = element.parentElement;
      }
      field?.append?.(isArrayLike(place) ? place[0] : place);
    }, "errorPlacement"),
    submitHandler: /* @__PURE__ */ __name(function() {
      return false;
    }, "submitHandler"),
    invalidHandler: /* @__PURE__ */ __name(function(event, validator) {
      notifyError(FormValidationTexts.InvalidFormMessage);
      validator.errorList.forEach((x) => {
        let element = isArrayLike(x.element) ? x.element[0] : x.element;
        element.closest(".category.collapsed")?.querySelectorAll(
          ":scope > .category-title"
        )?.forEach((el) => el.click());
      });
      if (validator.errorList.length) {
        const el = validator.errorList[0].element;
        if (!el)
          return;
        let $2 = getjQuery();
        const bsPaneId = el.closest(".tab-content>.tab-pane[id]:not(.active)")?.getAttribute("id");
        if (bsPaneId) {
          let selector = 'a[href="#' + bsPaneId + '"]';
          $2 && $2(selector).click();
          document.querySelector(selector)?.click();
        }
        const uiPaneId = el.closest(".ui-tabs-panel[id]:not(.ui-tabs-panel-active)")?.getAttribute("id");
        if (uiPaneId) {
          let selector = 'a[href="#' + uiPaneId + '"]';
          $2 ? $2(selector).click() : document.querySelector(selector)?.click();
        }
        new Tooltip(Validator.getHighlightTarget(el) ?? el, { title: validator.errorList[0].message }).show().delayedDispose();
      }
    }, "invalidHandler"),
    success: /* @__PURE__ */ __name(function(label) {
      label = isArrayLike(label) ? label[0] : label;
      label && label.classList.add("checked");
    }, "success")
  }, options);
}
__name(validateOptions, "validateOptions");
var ValidationHelper;
((ValidationHelper2) => {
  function asyncSubmit(form, validateBeforeSave, submitHandler) {
    const validator = Validator.getInstance(form);
    const valSettings = validator.settings;
    if (valSettings.abortHandler) {
      return false;
    }
    if (validateBeforeSave != null && validateBeforeSave() === false) {
      return false;
    }
    valSettings.abortHandler = validatorAbortHandler;
    valSettings.submitHandler = function() {
      submitHandler?.();
      return false;
    };
    Fluent.trigger(isArrayLike(form) ? form[0] : form, "submit");
    return true;
  }
  ValidationHelper2.asyncSubmit = asyncSubmit;
  __name(asyncSubmit, "asyncSubmit");
  function submit(form, validateBeforeSave, submitHandler) {
    const validator = Validator.getInstance(form);
    const valSettings = validator.settings;
    if (valSettings.abortHandler) {
      return false;
    }
    if (validateBeforeSave != null && validateBeforeSave() === false) {
      return false;
    }
    if (!validator.form()) {
      return false;
    }
    submitHandler?.();
    return true;
  }
  ValidationHelper2.submit = submit;
  __name(submit, "submit");
  function getValidator(elem) {
    return Validator.getInstance(elem);
  }
  ValidationHelper2.getValidator = getValidator;
  __name(getValidator, "getValidator");
  function validateElement(elem) {
    const validator = getValidator(elem);
    if (validator)
      validator.element(isArrayLike(elem) ? elem[0] : elem);
  }
  ValidationHelper2.validateElement = validateElement;
  __name(validateElement, "validateElement");
})(ValidationHelper || (ValidationHelper = {}));

// src/compat/jquery-compat.ts
function applyJQueryUIFixes($2) {
  if (!$2 || !$2.ui || !$2.ui.dialog || !$2.ui.dialog.prototype)
    return false;
  $2.ui.dialog.prototype._allowInteraction = function(event) {
    if (event.target?.closest?.(".ui-dialog")) {
      return true;
    }
    return !!event.target?.closest?.(".dropdown-menu, .s-dropdown-menu, .ui-datepicker, .select2-drop, .cke, .cke_dialog, .modal, #support-modal");
  };
  (function(orig) {
    $2.ui.dialog.prototype._focusTabbable = function() {
      if (isMobileView) {
        this.uiDialog && this.uiDialog.focus();
        return;
      }
      orig.call(this);
    };
  })($2.ui.dialog.prototype._focusTabbable);
  (function(orig) {
    $2.ui.dialog.prototype._createTitlebar = function() {
      orig.call(this);
      this.uiDialogTitlebar.find(".ui-dialog-titlebar-close").html(`<i class="${faIcon("times")}" />`);
    };
  })($2.ui.dialog.prototype._createTitlebar);
}
__name(applyJQueryUIFixes, "applyJQueryUIFixes");
function applyCleanDataPatch($2) {
  if (!$2 || !$2.fn || $2.isMock)
    return;
  if (!$2.toJSON)
    $2.toJSON = JSON.stringify;
  if (!$2.parseJSON)
    $2.parseJSON = JSON.parse;
  $2.cleanData = /* @__PURE__ */ (function(orig) {
    return function(elements) {
      let events, element, i2;
      const cloned = elements;
      for (i2 = 0; (element = cloned[i2]) != null; i2++) {
        try {
          invokeDisposingListeners(element);
          events = $2._data(element, "events");
          if (events && events.disposing) {
            let handlers = events.disposing;
            delete events.disposing;
            for (const x of handlers) {
              if (x && typeof x.handler === "function") {
                try {
                  x.handler.call(element, { target: element });
                } catch {
                }
              }
            }
          }
        } catch (e2) {
        }
      }
      orig(elements);
    };
  })($2.cleanData);
}
__name(applyCleanDataPatch, "applyCleanDataPatch");
function applyAjaxCSRFToken($2) {
  $2?.ajaxSetup?.({
    beforeSend: /* @__PURE__ */ __name(function(xhr, opt) {
      if (!opt || !opt.crossDomain) {
        const token = getCookie("CSRF-TOKEN");
        if (token)
          xhr.setRequestHeader("X-CSRF-TOKEN", token);
      }
    }, "beforeSend")
  });
}
__name(applyAjaxCSRFToken, "applyAjaxCSRFToken");
function jQueryPatch() {
  let $2 = getjQuery();
  if (!$2)
    return false;
  applyJQueryUIFixes($2);
  applyCleanDataPatch($2);
  applyAjaxCSRFToken($2);
  return true;
}
__name(jQueryPatch, "jQueryPatch");
!jQueryPatch() && Fluent.ready(jQueryPatch);

// src/interfaces/ibooleanvalue.ts
var _a5;
_a5 = Symbol.typeInfo;
var IBooleanValue = class {
  static {
    __name(this, "IBooleanValue");
  }
  static {
    this[_a5] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/idialog.ts
var _a6;
_a6 = Symbol.typeInfo;
var IDialog = class {
  static {
    __name(this, "IDialog");
  }
  static {
    this[_a6] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/idoublevalue.ts
var _a7;
_a7 = Symbol.typeInfo;
var IDoubleValue = class {
  static {
    __name(this, "IDoubleValue");
  }
  static {
    this[_a7] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/ieditdialog.ts
var _a8;
_a8 = Symbol.typeInfo;
var IEditDialog = class {
  static {
    __name(this, "IEditDialog");
  }
  static {
    this[_a8] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/igeteditvalue.ts
var _a9;
_a9 = Symbol.typeInfo;
var IGetEditValue = class {
  static {
    __name(this, "IGetEditValue");
  }
  static {
    this[_a9] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/ireadonly.ts
var _a10;
_a10 = Symbol.typeInfo;
var IReadOnly = class {
  static {
    __name(this, "IReadOnly");
  }
  static {
    this[_a10] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/iseteditvalue.ts
var _a11;
_a11 = Symbol.typeInfo;
var ISetEditValue = class {
  static {
    __name(this, "ISetEditValue");
  }
  static {
    this[_a11] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/istringvalue.ts
var _a12;
_a12 = Symbol.typeInfo;
var IStringValue = class {
  static {
    __name(this, "IStringValue");
  }
  static {
    this[_a12] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/interfaces/ivalidaterequired.ts
var _a13;
_a13 = Symbol.typeInfo;
var IValidateRequired = class {
  static {
    __name(this, "IValidateRequired");
  }
  static {
    this[_a13] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/slick/aggregators.ts
var Aggregators;
((Aggregators2) => {
  class Avg {
    constructor(field) {
      this.field = field;
    }
    static {
      __name(this, "Avg");
    }
    init() {
      this.count = 0;
      this.nonNullCount = 0;
      this.sum = 0;
    }
    accumulate(item) {
      const val = item[this.field];
      this.count++;
      if (val != null && val !== "" && !isNaN(val)) {
        this.nonNullCount++;
        this.sum += typeof val === "number" ? val : parseFloat(val);
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals.avg) {
        groupTotals.avg = {};
      }
      groupTotals.avg[this.field] = this.nonNullCount != 0 ? this.sum / this.nonNullCount : null;
    }
    static {
      this.summaryType = 2 /* Avg */;
    }
    static {
      this.aggregateKey = "avg";
    }
    static get displayName() {
      return localText("Enums.Serenity.SummaryType.Avg", "Avg");
    }
  }
  Aggregators2.Avg = Avg;
  class WeightedAvg {
    constructor(field, weightedField) {
      this.field = field;
      this.weightedField = weightedField;
    }
    static {
      __name(this, "WeightedAvg");
    }
    init() {
      this.sum = 0;
      this.weightedSum = 0;
    }
    accumulate(item) {
      const val = item[this.field];
      let valWeighted = item[this.weightedField];
      if (WeightedAvg.isValid(val) && WeightedAvg.isValid(valWeighted)) {
        valWeighted = typeof valWeighted === "number" ? valWeighted : parseFloat(valWeighted);
        this.weightedSum += valWeighted;
        this.sum += valWeighted * (typeof val === "number" ? val : parseFloat(val));
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals.avg) {
        groupTotals.avg = {};
      }
      if (this.sum && this.weightedSum) {
        groupTotals.avg[this.field] = this.sum / this.weightedSum;
      }
    }
    static isValid(val) {
      return val !== null && val !== "" && !isNaN(val);
    }
    static {
      this.aggregateKey = "weightedAvg";
    }
    static get displayName() {
      return localText("Enums.Serenity.SummaryType.WeightedAvg", "Weighted Avg");
    }
  }
  Aggregators2.WeightedAvg = WeightedAvg;
  class Min {
    static {
      __name(this, "Min");
    }
    constructor(field) {
      this.field = field;
    }
    init() {
      this.min = null;
    }
    accumulate(item) {
      const val = item[this.field];
      if (val != null && val !== "" && !isNaN(val)) {
        if (this.min == null || val < this.min) {
          this.min = val;
        }
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals.min) {
        groupTotals.min = {};
      }
      groupTotals.min[this.field] = this.min;
    }
    static {
      this.summaryType = 3 /* Min */;
    }
    static {
      this.aggregateKey = "min";
    }
    static get displayName() {
      return localText("Enums.Serenity.SummaryType.Min", "Min");
    }
  }
  Aggregators2.Min = Min;
  class Max {
    constructor(field) {
      this.field = field;
    }
    static {
      __name(this, "Max");
    }
    init() {
      this.max = null;
    }
    accumulate(item) {
      const val = item[this.field];
      if (val != null && val !== "" && !isNaN(val)) {
        if (this.max == null || val > this.max) {
          this.max = val;
        }
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals.max) {
        groupTotals.max = {};
      }
      groupTotals.max[this.field] = this.max;
    }
    static {
      this.summaryType = 4 /* Max */;
    }
    static {
      this.aggregateKey = "max";
    }
    static get displayName() {
      return localText("Enums.Serenity.SummaryType.Max", "Max");
    }
  }
  Aggregators2.Max = Max;
  class Sum {
    static {
      __name(this, "Sum");
    }
    constructor(field) {
      this.field = field;
    }
    init() {
      this.sum = 0;
    }
    accumulate(item) {
      const val = item[this.field];
      if (val != null && val !== "" && !isNaN(val)) {
        this.sum += typeof val === "number" ? val : parseFloat(val);
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals.sum) {
        groupTotals.sum = {};
      }
      groupTotals.sum[this.field] = this.sum;
    }
    static {
      this.summaryType = 1 /* Sum */;
    }
    static {
      this.aggregateKey = "sum";
    }
    static get displayName() {
      return localText("Enums.Serenity.SummaryType.Sum", "Sum");
    }
  }
  Aggregators2.Sum = Sum;
})(Aggregators || (Aggregators = {}));

// src/slick/aggregatortyperegistry.ts
var AggregatorTypeRegistry;
((AggregatorTypeRegistry2) => {
  let byKey = /* @__PURE__ */ Object.create(null);
  function register(cls) {
    if (!cls.aggregateKey)
      throw new Error("Aggregator class must have a static aggregateKey property to be registered.");
    byKey[cls.aggregateKey] = cls;
    if (cls.summaryType != null && cls.summaryType !== -1 /* Disabled */ && cls.summaryType !== 0 /* None */)
      byKey[cls.summaryType] = cls;
  }
  AggregatorTypeRegistry2.register = register;
  __name(register, "register");
  function reset() {
    byKey = /* @__PURE__ */ Object.create(null);
    for (const cls of Object.keys(Aggregators).map((k) => Aggregators[k])) {
      if (cls.aggregateKey)
        AggregatorTypeRegistry2.register(cls);
    }
  }
  AggregatorTypeRegistry2.reset = reset;
  __name(reset, "reset");
  function tryGet(aggKey) {
    if (aggKey == null || aggKey == -1 /* Disabled */ || aggKey == 0 /* None */)
      return void 0;
    return byKey[aggKey];
  }
  AggregatorTypeRegistry2.tryGet = tryGet;
  __name(tryGet, "tryGet");
})(AggregatorTypeRegistry || (AggregatorTypeRegistry = {}));
AggregatorTypeRegistry.reset();

// src/slick/aggregateformatting.tsx
import { applyFormatterResultToCellNode, convertCompatFormatter, formatterContext as formatterContext2, gridDefaults as gridDefaults2, NonDataRow } from "@serenity-is/sleekgrid";
var AggregateFormatting;
((AggregateFormatting2) => {
  const contentOnly2 = {
    contentOnly: true
  };
  function groupTotalsFormat(ctx) {
    const totals = ctx.item;
    const column = ctx.column;
    const field = column?.field;
    if (!totals)
      return "";
    const summaryType = column.summaryType ?? column?.sourceItem?.summaryType;
    if (!column?.field || summaryType === -1 /* Disabled */) {
      return jsx("span", { class: "aggregate-disabled" });
    }
    if (summaryType === 0 /* None */) {
      return jsx("span", { class: "aggregate-none" });
    }
    let aggType;
    let aggKey;
    if (column.summaryType != null) {
      aggType = AggregatorTypeRegistry.tryGet(column.summaryType);
      aggKey = aggType?.aggregateKey;
    } else {
      aggKey = Object.keys(totals).find((aggKey2) => totals[aggKey2]?.[field] != null) ?? Object.keys(totals).find((aggKey2) => totals[aggKey2]?.[field] === null);
      if (aggKey)
        aggType = AggregatorTypeRegistry.tryGet(aggKey);
    }
    if (!aggKey)
      return "";
    const value = totals[aggKey]?.[field];
    let displayName = aggType?.displayName;
    if (!displayName) {
      const textKey = aggKey.substring(0, 1).toUpperCase() + aggKey.substring(1);
      displayName = localText("Enums.Serenity.SummaryType." + textKey, textKey);
    }
    const span = jsx("span", { class: "aggregate agg-" + aggKey, title: displayName });
    const formatter = column.format ?? (column.formatter ? convertCompatFormatter(column.formatter) : null);
    function defaultFormatValue() {
      if (typeof value === "number") {
        const displayFormat = column.sourceItem?.displayFormat ?? "#,##0.##";
        return ctx.escape(formatNumber(value, displayFormat));
      }
      return ctx.escape(value);
    }
    __name(defaultFormatValue, "defaultFormatValue");
    let fmtResult;
    if (formatter != null) {
      const cellItem = new NonDataRow();
      cellItem[field] = value;
      try {
        fmtResult = formatter(formatterContext2({
          grid: ctx.grid,
          escape: ctx.escape,
          sanitizer: ctx.sanitizer,
          column,
          item: cellItem,
          value,
          purpose: ctx.purpose ?? "group-totals"
        }));
      } catch (e2) {
        fmtResult = defaultFormatValue();
      }
    } else {
      fmtResult = defaultFormatValue();
    }
    applyFormatterResultToCellNode(ctx, fmtResult, span, contentOnly2);
    return span;
  }
  AggregateFormatting2.groupTotalsFormat = groupTotalsFormat;
  __name(groupTotalsFormat, "groupTotalsFormat");
  function initGridDefaults() {
    if (gridDefaults2 != null && gridDefaults2.groupTotalsFormat === void 0)
      gridDefaults2.groupTotalsFormat = AggregateFormatting2.groupTotalsFormat;
  }
  AggregateFormatting2.initGridDefaults = initGridDefaults;
  __name(initGridDefaults, "initGridDefaults");
})(AggregateFormatting || (AggregateFormatting = {}));

// src/slick/remoteview.ts
import { convertCompatFormatter as convertCompatFormatter2, EventEmitter, Group, GroupItemMetadataProvider, GroupTotals } from "@serenity-is/sleekgrid";
var RemoteView = class _RemoteView {
  constructor(options) {
    this.errorMessage = null;
    this.filterCache = [];
    this.filteredItems = [];
    this.groupingInfos = [];
    this.groups = [];
    this.idxById = {};
    this.items = [];
    this.loading = false;
    this.page = 1;
    this.populateCalls = 0;
    this.populateLocks = 0;
    this.prevRefreshHints = {};
    this.refreshHints = {};
    this.rows = [];
    this.rowsById = null;
    this.sortAsc = true;
    this.suspend = 0;
    this.toggledGroupsByLevel = [];
    this.totalCount = null;
    this.totalRows = 0;
    this.updated = null;
    this.onDataChanged = new EventEmitter();
    this.onDataLoaded = new EventEmitter();
    this.onDataLoading = new EventEmitter();
    this.onGroupCollapsed = new EventEmitter();
    this.onGroupExpanded = new EventEmitter();
    this.onPagingInfoChanged = new EventEmitter();
    this.onRowCountChanged = new EventEmitter();
    this.onRowsChanged = new EventEmitter();
    this.onRowsOrCountChanged = new EventEmitter();
    this.formatGroupValue = /* @__PURE__ */ __name((ctx) => {
      if (!ctx.item || ctx.item.level == null) {
        return ctx.escape(ctx.item?.value);
      }
      const gi = this.groupingInfos[ctx.item.level];
      if (!gi) {
        return ctx.escape(ctx.item?.value);
      }
      if (!gi.format && gi.formatter) {
        const formatter = gi.formatter;
        gi.format = convertCompatFormatter2((_1, _2, _3, _4, item) => formatter(item));
      }
      if (gi.format) {
        return gi.format(ctx);
      } else {
        return ctx.escape(ctx.item?.value);
      }
    }, "formatGroupValue");
    options ??= {};
    AggregateFormatting.initGridDefaults();
    this.groupItemMetadataProvider = options.groupItemMetadataProvider;
    this.localSort = options.localSort ?? false;
    this.idProperty = options.idField ?? "id";
    this.contentType = options.contentType ?? "application/json";
    this.dataType = options.dataType ?? "json";
    this.filter = options.filter ?? null;
    this.itemMetadataCallback = options.getItemMetadata;
    this.params = options.params ?? {};
    this.onSubmit = options.onSubmit ?? null;
    this.url = options.url || null;
    this.rowsPerPage = options.rowsPerPage || 0;
    this.seekToPage = options.seekToPage || 1;
    this.onAjaxCall = options.onAjaxCall || null;
    this.onProcessData = options.onProcessData || null;
    this.method = options.method || "POST";
    this.errormsg = options.errormsg;
    this.sortBy = typeof options.sortBy == "string" && options.sortBy !== "" ? [options.sortBy] : options.sortBy || [];
    options.url && options.autoLoad && this.populate();
  }
  static {
    __name(this, "RemoteView");
  }
  static {
    /** Default configuration for grouping information */
    this.groupingInfoDefaults = {
      comparer: /* @__PURE__ */ __name(function(a2, b2) {
        return a2.value === b2.value ? 0 : a2.value > b2.value ? 1 : -1;
      }, "comparer"),
      aggregateEmpty: false,
      aggregateCollapsed: false,
      aggregateChildGroups: false,
      collapsed: false,
      displayTotalsRow: true,
      lazyTotalsCalculation: false,
      predefinedValues: []
    };
  }
  beginUpdate() {
    this.suspend++;
  }
  endUpdate() {
    this.suspend--;
    if (this.suspend <= 0)
      this.refresh();
  }
  /**
   * Sets hints for the next refresh operation to optimize performance.
   * @param hints Object containing refresh hints like isFilterNarrowing, isFilterExpanding, etc.
   */
  setRefreshHints(hints) {
    this.refreshHints = hints;
  }
  updateIdxById(startingIndex) {
    startingIndex = startingIndex || 0;
    let id;
    for (let i2 = startingIndex, l2 = this.items.length; i2 < l2; i2++) {
      id = this.items[i2][this.idProperty];
      if (id === void 0) {
        throw `Each data element must implement a unique '${this.idProperty}' property. Object at index '${i2}' has no identity value: ${JSON.stringify(this.items[i2])}`;
      }
      this.idxById[id] = i2;
    }
  }
  ensureIdUniqueness() {
    let id;
    for (let i2 = 0, l2 = this.items.length; i2 < l2; i2++) {
      id = this.items[i2][this.idProperty];
      if (id === void 0 || this.idxById[id] !== i2) {
        throw `Each data element must implement a unique '${this.idProperty}' property. Object at index '${i2}' ` + (id == void 0 ? "has no identity value: " : "has repeated identity value '" + id + "': ") + JSON.stringify(this.items[i2]);
      }
    }
  }
  getItems() {
    return this.items;
  }
  getIdPropertyName() {
    return this.idProperty;
  }
  setItems(data, newIdProperty) {
    if (newIdProperty != null && typeof newIdProperty == "string")
      this.idProperty = newIdProperty;
    this.items = this.filteredItems = data;
    if (this.localSort) {
      this.items.sort(this.getSortComparer());
    }
    this.idxById = {};
    this.rowsById = null;
    this.grandTotals = {};
    this.updateIdxById();
    this.ensureIdUniqueness();
    if (this.suspend) {
      this.recalc(this.items);
    } else {
      this.refresh();
    }
    this.onDataChanged.notify({ dataView: this }, null, this);
  }
  setPagingOptions(args) {
    let anyChange = false;
    if (args.rowsPerPage != void 0 && this.rowsPerPage != args.rowsPerPage) {
      this.rowsPerPage = args.rowsPerPage;
      anyChange = true;
    }
    if (args.page != void 0) {
      let newPage;
      if (!this.rowsPerPage)
        newPage = 1;
      else if (this.totalCount == null)
        newPage = args.page;
      else
        newPage = Math.min(args.page, Math.ceil(this.totalCount / this.rowsPerPage) + 1);
      if (newPage < 1)
        newPage = 1;
      if (newPage != this.page) {
        this.seekToPage = newPage;
        anyChange = true;
      }
    }
    if (anyChange)
      this.populate();
  }
  getPagingInfo() {
    return {
      rowsPerPage: this.rowsPerPage,
      page: this.page,
      totalCount: this.totalCount,
      loading: this.loading != null && this.loading != false,
      error: this.errorMessage,
      dataView: this
    };
  }
  getSortComparer() {
    if (this.sortComparer != null)
      return this.sortComparer;
    const cols = [];
    const asc = [];
    const sorts = this.sortBy || [];
    for (const s2 of sorts) {
      if (s2 == null)
        continue;
      if (s2.length > 5 && s2.toLowerCase().substring(s2.length - 5).toLowerCase() == " desc") {
        asc.push(false);
        cols.push(s2.substring(0, s2.length - 5));
      } else {
        asc.push(true);
        cols.push(s2);
      }
    }
    return function(a2, b2) {
      for (let i2 = 0, l2 = cols.length; i2 < l2; i2++) {
        const field = cols[i2];
        const sign = asc[i2] ? 1 : -1;
        const value1 = a2[field], value2 = b2[field];
        const result = (value1 == value2 ? 0 : value1 > value2 ? 1 : -1) * sign;
        if (result != 0) {
          return result;
        }
      }
      return 0;
    };
  }
  sort(comparer, ascending) {
    this.sortAsc = ascending;
    if (ascending === false) {
      this.items.reverse();
    }
    this.sortComparer = comparer;
    this.items.sort(this.getSortComparer());
    if (ascending === false) {
      this.items.reverse();
    }
    this.idxById = {};
    this.updateIdxById();
    this.refresh();
  }
  getLocalSort() {
    return this.localSort;
  }
  setLocalSort(value) {
    if (this.localSort != value) {
      this.localSort = value;
      this.sort();
    }
  }
  reSort() {
    this.sort(this.sortComparer, this.sortAsc);
  }
  getFilteredItems() {
    return this.filteredItems;
  }
  getFilter() {
    return this.filter;
  }
  setFilter(filterFn) {
    this.filter = filterFn;
    this.refresh();
  }
  getGrouping() {
    return this.groupingInfos;
  }
  setSummaryOptions(summary) {
    this.grandAggregators = summary?.aggregators || [];
    this.grandTotals = {};
    this.setGrouping(this.groupingInfos || []);
  }
  getGrandTotals() {
    this.grandTotals ??= {};
    if (!this.grandTotals.initialized) {
      this.grandAggregators ??= [];
      let agg, idx = this.grandAggregators.length;
      while (idx--) {
        agg = this.grandAggregators[idx];
        agg.init();
        for (const item of this.items)
          agg.accumulate(item);
        agg.storeResult(this.grandTotals);
      }
      this.grandTotals.initialized = true;
    }
    return this.grandTotals;
  }
  setGrouping(groupingInfo) {
    if (!this.groupItemMetadataProvider) {
      this.groupItemMetadataProvider = new GroupItemMetadataProvider();
    }
    this.groups = [];
    this.toggledGroupsByLevel = [];
    groupingInfo = groupingInfo || [];
    this.groupingInfos = groupingInfo instanceof Array ? groupingInfo : [groupingInfo];
    for (let i2 = 0; i2 < this.groupingInfos.length; i2++) {
      const gi = this.groupingInfos[i2] = Object.assign({}, _RemoteView.groupingInfoDefaults, this.groupingInfos[i2]);
      gi.getterIsAFn ??= typeof gi.getter == "function";
      this.toggledGroupsByLevel[i2] = {};
    }
    this.refresh();
  }
  getItemByIdx(i2) {
    return this.items[i2];
  }
  getIdxById(id) {
    return this.idxById[id];
  }
  ensureRowsByIdCache() {
    if (!this.rowsById) {
      this.rowsById = {};
      for (let i2 = 0, l2 = this.rows.length; i2 < l2; i2++) {
        this.rowsById[this.rows[i2][this.idProperty]] = i2;
      }
    }
  }
  getRowByItem(item) {
    this.ensureRowsByIdCache();
    return this.rowsById[item[this.idProperty]];
  }
  getRowById(id) {
    this.ensureRowsByIdCache();
    return this.rowsById[id];
  }
  getItemById(id) {
    return this.items[this.idxById[id]];
  }
  /**
   * Maps an array of items to their corresponding row indices.
   * @param itemArray Array of items to map
   * @returns Array of row indices
   */
  mapItemsToRows(itemArray) {
    const rows = [];
    this.ensureRowsByIdCache();
    for (let i2 = 0, l2 = itemArray.length; i2 < l2; i2++) {
      const row = this.rowsById[itemArray[i2][this.idProperty]];
      if (row != null) {
        rows[rows.length] = row;
      }
    }
    return rows;
  }
  /**
   * Maps an array of IDs to their corresponding row indices.
   * @param idArray Array of item IDs to map
   * @returns Array of row indices
   */
  mapIdsToRows(idArray) {
    const rows = [];
    this.ensureRowsByIdCache();
    for (let i2 = 0, l2 = idArray.length; i2 < l2; i2++) {
      const row = this.rowsById[idArray[i2]];
      if (row != null) {
        rows[rows.length] = row;
      }
    }
    return rows;
  }
  /**
   * Maps an array of row indices to their corresponding item IDs.
   * @param rowArray Array of row indices to map
   * @returns Array of item IDs
   */
  mapRowsToIds(rowArray) {
    const ids = [];
    for (let i2 = 0, l2 = rowArray.length; i2 < l2; i2++) {
      if (rowArray[i2] < this.rows.length) {
        ids[ids.length] = this.rows[rowArray[i2]][this.idProperty];
      }
    }
    return ids;
  }
  updateItem(id, item) {
    if (this.idxById[id] === void 0) {
      throw new Error("Invalid id");
    }
    if (id !== item[this.idProperty]) {
      const newId = item[this.idProperty];
      if (newId == null) {
        throw new Error("Cannot update item to associate with a null id");
      }
      if (this.idxById[newId] !== void 0) {
        throw new Error("Cannot update item to associate with a non-unique id");
      }
      this.idxById[newId] = this.idxById[id];
      delete this.idxById[id];
      if (this.updated && this.updated[id]) {
        delete this.updated[id];
      }
      id = newId;
    }
    this.items[this.idxById[id]] = item;
    if (!this.updated) {
      this.updated = {};
    }
    this.updated[id] = true;
    this.refresh();
  }
  insertItem(insertBefore, item) {
    this.items.splice(insertBefore, 0, item);
    this.updateIdxById(insertBefore);
    this.refresh();
  }
  addItem(item) {
    this.items.push(item);
    this.updateIdxById(this.items.length - 1);
    this.refresh();
  }
  deleteItem(id) {
    const idx = this.idxById[id];
    if (idx === void 0) {
      throw "Invalid id";
    }
    delete this.idxById[id];
    this.items.splice(idx, 1);
    this.updateIdxById(idx);
    this.refresh();
  }
  sortedAddItem(item) {
    this.insertItem(this.sortedIndex(item), item);
  }
  sortedUpdateItem(id, item) {
    if (this.idxById[id] === void 0 || id !== item[this.idProperty]) {
      throw new Error("Invalid or non-matching id " + this.idxById[id]);
    }
    const comparer = this.getSortComparer();
    const oldItem = this.getItemById(id);
    if (comparer(oldItem, item) !== 0) {
      this.deleteItem(id);
      this.sortedAddItem(item);
    } else {
      this.updateItem(id, item);
    }
  }
  sortedIndex(searchItem) {
    let low = 0, high = this.items.length;
    const comparer = this.getSortComparer();
    while (low < high) {
      const mid = low + high >>> 1;
      if (comparer(this.items[mid], searchItem) === -1) {
        low = mid + 1;
      } else {
        high = mid;
      }
    }
    return low;
  }
  getRows() {
    return this.rows;
  }
  getLength() {
    return this.rows.length;
  }
  getItem(i2) {
    const item = this.rows[i2];
    const group = item;
    if (group && group.__group && group.totals && !group.totals.initialized) {
      const gi = this.groupingInfos[group.level];
      if (!gi.displayTotalsRow) {
        this.calculateTotals(group.totals);
        group.formatValue = this.formatGroupValue;
      }
    } else if (item && item.__groupTotals && !item.initialized) {
      this.calculateTotals(item);
    }
    return item;
  }
  getItemMetadata(row) {
    const item = this.rows[row];
    if (item === void 0) {
      return null;
    }
    if (item.__group) {
      return this.groupItemMetadataProvider.getGroupRowMetadata(item);
    }
    if (item.__groupTotals) {
      return this.groupItemMetadataProvider.getTotalsRowMetadata(item);
    }
    return this.itemMetadataCallback && this.itemMetadataCallback(item, row) || null;
  }
  expandCollapseAllGroups(level, collapse) {
    if (level == null) {
      for (let i2 = 0; i2 < this.groupingInfos.length; i2++) {
        this.toggledGroupsByLevel[i2] = {};
        this.groupingInfos[i2].collapsed = collapse;
        if (collapse === true) {
          this.onGroupCollapsed.notify({ level: i2, groupingKey: null, dataView: this }, null, this);
        } else {
          this.onGroupExpanded.notify({ level: i2, groupingKey: null, dataView: this }, null, this);
        }
      }
    } else {
      this.toggledGroupsByLevel[level] = {};
      this.groupingInfos[level].collapsed = collapse;
      if (collapse === true) {
        this.onGroupCollapsed.notify({ level, groupingKey: null, dataView: this }, null, this);
      } else {
        this.onGroupExpanded.notify({ level, groupingKey: null, dataView: this }, null, this);
      }
    }
    this.refresh();
  }
  collapseAllGroups(level) {
    this.expandCollapseAllGroups(level, true);
  }
  expandAllGroups(level) {
    this.expandCollapseAllGroups(level, false);
  }
  resolveLevelAndGroupingKey(args) {
    const arg0 = args[0];
    if (args.length === 1 && arg0.indexOf(groupingDelimiter) !== -1) {
      return { level: arg0.split(groupingDelimiter).length - 1, groupingKey: arg0 };
    } else {
      return { level: args.length - 1, groupingKey: args.join(groupingDelimiter) };
    }
  }
  expandCollapseGroup(args, collapse) {
    const opts = this.resolveLevelAndGroupingKey(args);
    this.toggledGroupsByLevel[opts.level][opts.groupingKey] = !this.groupingInfos[opts.level].collapsed !== !collapse;
    if (collapse)
      this.onGroupCollapsed.notify({ level: opts.level, groupingKey: opts.groupingKey, dataView: this }, null, this);
    else
      this.onGroupExpanded.notify({ level: opts.level, groupingKey: opts.groupingKey, dataView: this }, null, this);
    this.refresh();
  }
  collapseGroup(constArgs) {
    const args = Array.prototype.slice.call(arguments);
    this.expandCollapseGroup(args, true);
  }
  expandGroup(constArgs) {
    const args = Array.prototype.slice.call(arguments);
    this.expandCollapseGroup(args, false);
  }
  getGroups() {
    return this.groups;
  }
  getOrCreateGroup(groupsByVal, val, level, parentGroup, groups) {
    let group = groupsByVal[val];
    if (!group) {
      group = new Group();
      group.value = val;
      group.level = level;
      group.groupingKey = (parentGroup ? parentGroup.groupingKey + groupingDelimiter : "") + val;
      groups[groups.length] = group;
      groupsByVal[val] = group;
    }
    return group;
  }
  extractGroups(items, parentGroup) {
    let group;
    let val;
    const groups = [];
    const groupsByVal = {};
    let item;
    const level = parentGroup ? parentGroup.level + 1 : 0;
    const groupingInfo = this.groupingInfos[level];
    for (let i1 = 0, l2 = groupingInfo.predefinedValues.length; i1 < l2; i1++) {
      val = groupingInfo.predefinedValues[i1];
      group = this.getOrCreateGroup(groupsByVal, val, level, parentGroup, groups);
    }
    for (let i2 = 0, l2 = items.length; i2 < l2; i2++) {
      item = items[i2];
      val = groupingInfo.getterIsAFn ? groupingInfo.getter(item) : item[groupingInfo.getter];
      group = this.getOrCreateGroup(groupsByVal, val, level, parentGroup, groups);
      group.rows[group.count++] = item;
    }
    if (level < this.groupingInfos.length - 1) {
      for (let i3 = 0; i3 < groups.length; i3++) {
        group = groups[i3];
        group.groups = this.extractGroups(group.rows, group);
      }
    }
    if (groups.length) {
      this.addTotals(groups, level);
    }
    groups.sort(this.groupingInfos[level].comparer);
    return groups;
  }
  calculateTotals(totals) {
    const group = totals.group;
    const gi = this.groupingInfos[group.level];
    const isLeafLevel = group.level == this.groupingInfos.length;
    const aggregators = gi.aggregators ?? this.grandAggregators ?? [];
    let agg, idx = aggregators.length;
    if (!isLeafLevel && gi.aggregateChildGroups) {
      let i2 = group.groups.length;
      while (i2--) {
        if (!group.groups[i2].totals.initialized) {
          this.calculateTotals(group.groups[i2].totals);
        }
      }
    }
    while (idx--) {
      agg = aggregators[idx];
      agg.init();
      const items = !isLeafLevel && gi.aggregateChildGroups ? group.groups : group.rows;
      for (const item of items) {
        agg.accumulate(item);
      }
      agg.storeResult(totals);
    }
    totals.initialized = true;
  }
  addGroupTotals(group) {
    const gi = this.groupingInfos[group.level];
    const totals = new GroupTotals();
    totals.group = group;
    group.totals = totals;
    if (!gi.lazyTotalsCalculation) {
      this.calculateTotals(totals);
    }
  }
  addTotals(groups, level) {
    level = level || 0;
    const gi = this.groupingInfos[level];
    const aggregators = gi?.aggregators ?? this.grandAggregators ?? [];
    const groupCollapsed = !!gi?.collapsed;
    const toggledGroups = this.toggledGroupsByLevel[level];
    let idx = groups.length, g2;
    while (idx--) {
      g2 = groups[idx];
      if (g2.collapsed && !gi?.aggregateCollapsed) {
        continue;
      }
      if (g2.groups) {
        this.addTotals(g2.groups, level + 1);
      }
      if (aggregators.length && (gi.aggregateEmpty || g2.rows.length || g2.groups && g2.groups.length)) {
        this.addGroupTotals(g2);
      }
      g2.collapsed = groupCollapsed !== !!toggledGroups[g2.groupingKey];
      g2.formatValue = this.formatGroupValue;
    }
  }
  flattenGroupedRows(groups, level) {
    level = level || 0;
    const gi = this.groupingInfos[level];
    const groupedRows = [];
    let rows, gl = 0, g2;
    for (let i2 = 0, l2 = groups.length; i2 < l2; i2++) {
      g2 = groups[i2];
      groupedRows[gl++] = g2;
      if (!g2.collapsed) {
        rows = g2.groups ? this.flattenGroupedRows(g2.groups, level + 1) : g2.rows;
        for (let j = 0, jj = rows.length; j < jj; j++) {
          groupedRows[gl++] = rows[j];
        }
      }
      if (g2.totals && gi.displayTotalsRow && (!g2.collapsed || gi.aggregateCollapsed)) {
        groupedRows[gl++] = g2.totals;
      }
    }
    return groupedRows;
  }
  batchFilter(items) {
    const retval = [];
    let idx = 0;
    for (let i2 = 0, ii = items.length; i2 < ii; i2++) {
      if (this.filter(items[i2], this)) {
        retval[idx++] = items[i2];
      }
    }
    return retval;
  }
  batchFilterWithCaching(items, cache) {
    const retval = [];
    let idx = 0, item;
    for (let i2 = 0, ii = items.length; i2 < ii; i2++) {
      item = items[i2];
      if (cache[i2]) {
        retval[idx++] = item;
      } else if (this.filter(item, this)) {
        retval[idx++] = item;
        cache[i2] = true;
      }
    }
    return retval;
  }
  getFilteredAndPagedItems(items) {
    if (this.filter) {
      if (this.refreshHints?.isFilterNarrowing) {
        this.filteredItems = this.batchFilter(this.filteredItems);
      } else if (this.refreshHints?.isFilterExpanding) {
        this.filteredItems = this.batchFilterWithCaching(items, this.filterCache);
      } else if (!this.refreshHints?.isFilterUnchanged) {
        this.filteredItems = this.batchFilter(items);
      }
    } else {
      this.filteredItems = items.concat();
    }
    return { totalRows: this.filteredItems.length, rows: this.filteredItems };
  }
  getRowDiffs(rows, newRows) {
    let item, r2, eitherIsNonData, diff = [];
    let from = 0, to = newRows.length;
    if (this.refreshHints?.ignoreDiffsBefore) {
      from = Math.max(
        0,
        Math.min(newRows.length, this.refreshHints.ignoreDiffsBefore)
      );
    }
    if (this.refreshHints?.ignoreDiffsAfter) {
      to = Math.min(
        newRows.length,
        Math.max(0, this.refreshHints.ignoreDiffsAfter)
      );
    }
    for (let i2 = from, rl = rows.length; i2 < to; i2++) {
      if (i2 >= rl) {
        diff[diff.length] = i2;
      } else {
        item = newRows[i2];
        r2 = rows[i2];
        if (this.groupingInfos.length && (eitherIsNonData = item.__nonDataRow || r2.__nonDataRow) && item.__group !== r2.__group || item.__group && !item.equals(r2) || eitherIsNonData && // no good way to compare totals since they are arbitrary DTOs
        // deep object comparison is pretty expensive
        // always considering them 'dirty' seems easier for the time being
        (item.__groupTotals || r2.__groupTotals) || item[this.idProperty] != r2[this.idProperty] || this.updated && this.updated[item[this.idProperty]]) {
          diff[diff.length] = i2;
        }
      }
    }
    return diff;
  }
  recalc(_items) {
    this.rowsById = null;
    if (this.refreshHints?.isFilterNarrowing != this.prevRefreshHints?.isFilterNarrowing || this.refreshHints?.isFilterExpanding != this.prevRefreshHints?.isFilterExpanding) {
      this.filterCache = [];
    }
    const filteredItems = this.getFilteredAndPagedItems(_items);
    this.totalRows = filteredItems.totalRows;
    let newRows = filteredItems.rows;
    this.grandTotals = {};
    this.groups = [];
    if (this.groupingInfos.length) {
      this.groups = this.extractGroups(newRows);
      if (this.groups.length) {
        newRows = this.flattenGroupedRows(this.groups);
      }
    }
    const diff = this.getRowDiffs(this.rows, newRows);
    this.rows = newRows;
    return diff;
  }
  refresh() {
    if (this.suspend) {
      return;
    }
    const countBefore = this.rows.length;
    const totalRowsBefore = this.totalRows;
    const diff = this.recalc(this.items);
    this.updated = null;
    this.prevRefreshHints = this.refreshHints;
    this.refreshHints = {};
    if (totalRowsBefore !== this.totalRows) {
      this.onPagingInfoChanged.notify({ pagingInfo: this.getPagingInfo(), dataView: this }, null, this);
    }
    if (countBefore !== this.rows.length) {
      this.onRowCountChanged.notify({ previous: countBefore, current: this.rows.length, dataView: this }, null, this);
    }
    if (diff.length > 0) {
      this.onRowsChanged.notify({ rows: diff, dataView: this }, null, this);
    }
    if (countBefore !== this.rows.length || diff.length > 0) {
      this.onRowsOrCountChanged.notify({
        rowsDiff: diff,
        previousRowCount: countBefore,
        currentRowCount: this.rows.length,
        rowCountChanged: countBefore !== this.rows.length,
        rowsChanged: diff.length > 0,
        dataView: this
      }, null, this);
    }
  }
  /***
   * Wires the grid and the DataView together to keep row selection tied to item ids.
   * This is useful since, without it, the grid only knows about rows, so if the items
   * move around, the same rows stay selected instead of the selection moving along
   * with the items.
   *
   * NOTE:  This doesn't work with cell selection model.
   *
   * @param sleekGrid The grid to sync selection with.
   * @param preserveHidden Whether to keep selected items that go out of the
   *     view due to them getting filtered out.
   * @param preserveHiddenOnSelectionChange Whether to keep selected items
   *     that are currently out of the view (see preserveHidden) as selected when selection
   *     changes.
   * @return An event that notifies when an internal list of selected row ids
   *     changes.  This is useful since, in combination with the above two options, it allows
   *     access to the full list selected row ids, and not just the ones visible to the grid.
   */
  syncGridSelection(sleekGrid, preserveHidden, preserveHiddenOnSelectionChange) {
    let inHandler;
    ;
    let selectedRowIds = this.mapRowsToIds(sleekGrid.getSelectedRows());
    const onSelectedRowIdsChanged = new EventEmitter();
    const self2 = this;
    function setSelectedRowIds(rowIds) {
      if (selectedRowIds.join(",") == rowIds.join(",")) {
        return;
      }
      selectedRowIds = rowIds;
      onSelectedRowIdsChanged.notify({
        "grid": sleekGrid,
        "ids": selectedRowIds,
        "dataView": this
      }, null, self2);
    }
    __name(setSelectedRowIds, "setSelectedRowIds");
    function update() {
      if (selectedRowIds.length > 0) {
        inHandler = true;
        const selectedRows = self2.mapIdsToRows(selectedRowIds);
        if (!preserveHidden) {
          setSelectedRowIds(self2.mapRowsToIds(selectedRows));
        }
        sleekGrid.setSelectedRows(selectedRows);
        inHandler = false;
      }
    }
    __name(update, "update");
    sleekGrid.onSelectedRowsChanged.subscribe(function() {
      if (inHandler) {
        return;
      }
      const newSelectedRowIds = self2.mapRowsToIds(sleekGrid.getSelectedRows());
      if (!preserveHiddenOnSelectionChange || !sleekGrid.getOptions().multiSelect) {
        setSelectedRowIds(newSelectedRowIds);
      } else {
        const existing = selectedRowIds.filter((id) => self2.getRowById(id) === void 0);
        setSelectedRowIds(existing.concat(newSelectedRowIds));
      }
    });
    this.onRowsChanged.subscribe(update);
    this.onRowCountChanged.subscribe(update);
    return onSelectedRowIdsChanged;
  }
  syncGridCellCssStyles(grid, key) {
    let hashById;
    let inHandler;
    storeCellCssStyles(grid.getCellCssStyles(key));
    const self2 = this;
    function storeCellCssStyles(hash) {
      hashById = {};
      for (const row in hash) {
        const id = self2.rows[row][self2.idProperty];
        hashById[id] = hash[row];
      }
    }
    __name(storeCellCssStyles, "storeCellCssStyles");
    function update() {
      if (hashById) {
        inHandler = true;
        self2.ensureRowsByIdCache();
        const newHash = {};
        for (const id in hashById) {
          const row = self2.rowsById[id];
          if (row != void 0) {
            newHash[row] = hashById[id];
          }
        }
        grid.setCellCssStyles(key, newHash);
        inHandler = false;
      }
    }
    __name(update, "update");
    const subFunc = /* @__PURE__ */ __name(function(e2, args) {
      if (inHandler) {
        return;
      }
      if (key != args.key) {
        return;
      }
      if (args.hash) {
        storeCellCssStyles(args.hash);
      } else {
        grid.onCellCssStylesChanged.unsubscribe(subFunc);
        self2.onRowsOrCountChanged.unsubscribe(update);
      }
    }, "subFunc");
    grid.onCellCssStylesChanged.subscribe(subFunc);
    this.onRowsOrCountChanged.subscribe(update);
  }
  addData(data) {
    if (this.onProcessData && data)
      data = this.onProcessData(data, this) || data;
    this.errorMessage = null;
    this.loading && typeof this.loading !== "boolean" && this.loading.abort();
    this.loading = false;
    if (!data) {
      this.errorMessage = this.errormsg ?? PagerTexts.DefaultLoadError;
      this.onPagingInfoChanged.notify({
        dataView: this,
        pagingInfo: this.getPagingInfo()
      });
      return false;
    }
    data.TotalCount = data.TotalCount || 0;
    data.Entities = data.Entities || [];
    if (!data.Skip || !this.rowsPerPage && !data.Take)
      data.Page = 1;
    else
      data.Page = Math.ceil(data.Skip / (data.Take || this.rowsPerPage)) + 1;
    this.page = data.Page;
    this.totalCount = data.TotalCount;
    this.setItems(data.Entities);
    this.onPagingInfoChanged.notify({ pagingInfo: this.getPagingInfo(), dataView: this });
  }
  populate() {
    if (this.populateLocks > 0) {
      this.populateCalls++;
      return;
    }
    this.populateCalls = 0;
    this.loading && typeof this.loading !== "boolean" && this.loading.abort();
    if (this.onSubmit) {
      const gh = this.onSubmit(this);
      if (gh === false)
        return false;
    }
    this.onDataLoading.notify({ dataView: this });
    if (!this.url)
      return false;
    if (!this.seekToPage)
      this.seekToPage = 1;
    let request = {};
    const skip = (this.seekToPage - 1) * this.rowsPerPage;
    if (skip)
      request.Skip = skip;
    if (this.rowsPerPage)
      request.Take = this.rowsPerPage;
    if (this.sortBy && this.sortBy.length) {
      if (typeof this.sortBy !== "string")
        request.Sort = this.sortBy;
      else {
        request.Sort = [this.sortBy];
      }
    }
    if (this.params) {
      request = Object.assign(request, this.params);
    }
    const controller = new AbortController();
    const self2 = this;
    const serviceOptions = {
      allowRedirect: false,
      cache: "no-store",
      method: this.method,
      headers: {
        "Content-Type": this.contentType,
        "Accept": this.dataType == "json" || this.dataType == "application/json" ? "application/json" : this.dataType
      },
      request,
      url: this.url,
      signal: controller.signal,
      errorMode: "notification",
      onSuccess: /* @__PURE__ */ __name(function(response) {
        self2.addData(response);
      }, "onSuccess"),
      onCleanup: /* @__PURE__ */ __name(function() {
        self2.loading = false;
        self2.onPagingInfoChanged.notify({ pagingInfo: self2.getPagingInfo(), dataView: self2 });
        self2.onDataLoaded.notify({ dataView: self2 });
      }, "onCleanup")
    };
    if (this.onAjaxCall) {
      const ah = this.onAjaxCall(this, serviceOptions);
      if (ah === false) {
        this.loading = false;
        this.onPagingInfoChanged.notify({ pagingInfo: this.getPagingInfo(), dataView: this });
        return false;
      }
    }
    serviceCall(serviceOptions);
    this.onPagingInfoChanged.notify({ pagingInfo: this.getPagingInfo(), dataView: this });
    this.loading = controller;
  }
  populateLock() {
    if (this.populateLocks == 0)
      this.populateCalls = 0;
    this.populateLocks++;
  }
  populateUnlock() {
    if (this.populateLocks > 0) {
      this.populateLocks--;
      if (this.populateLocks == 0 && this.populateCalls > 0)
        this.populate();
    }
  }
  getGroupItemMetadataProvider() {
    return this.groupItemMetadataProvider;
  }
  setGroupItemMetadataProvider(value) {
    this.groupItemMetadataProvider = value;
  }
  /** @deprecated Gets the ID property name, for compatibility */
  get idField() {
    return this.idProperty;
  }
};
var groupingDelimiter = ":|:";

// src/types/attributes.ts
var _a14, _b3;
var CloseButtonAttribute = class extends (_b3 = CustomAttribute, _a14 = Symbol.typeInfo, _b3) {
  constructor(value = true) {
    super();
    this.value = value;
  }
  static {
    __name(this, "CloseButtonAttribute");
  }
  static {
    this[_a14] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a15, _b4;
var ElementAttribute = class extends (_b4 = CustomAttribute, _a15 = Symbol.typeInfo, _b4) {
  constructor(value) {
    super();
    this.value = value;
  }
  static {
    __name(this, "ElementAttribute");
  }
  static {
    this[_a15] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a16, _b5;
var AdvancedFilteringAttribute = class extends (_b5 = CustomAttribute, _a16 = Symbol.typeInfo, _b5) {
  constructor(value = true) {
    super();
    this.value = value;
  }
  static {
    __name(this, "AdvancedFilteringAttribute");
  }
  static {
    this[_a16] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a17, _b6;
var MaximizableAttribute = class extends (_b6 = CustomAttribute, _a17 = Symbol.typeInfo, _b6) {
  constructor(value = true) {
    super();
    this.value = value;
  }
  static {
    __name(this, "MaximizableAttribute");
  }
  static {
    this[_a17] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a18, _b7;
var OptionAttribute = class extends (_b7 = CustomAttribute, _a18 = Symbol.typeInfo, _b7) {
  static {
    __name(this, "OptionAttribute");
  }
  static {
    this[_a18] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a19, _b8;
var PanelAttribute = class extends (_b8 = CustomAttribute, _a19 = Symbol.typeInfo, _b8) {
  constructor(value = true) {
    super();
    this.value = value;
  }
  static {
    __name(this, "PanelAttribute");
  }
  static {
    this[_a19] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a20, _b9;
var ResizableAttribute = class extends (_b9 = CustomAttribute, _a20 = Symbol.typeInfo, _b9) {
  constructor(value = true) {
    super();
    this.value = value;
  }
  static {
    __name(this, "ResizableAttribute");
  }
  static {
    this[_a20] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var _a21, _b10;
var StaticPanelAttribute = class extends (_b10 = CustomAttribute, _a21 = Symbol.typeInfo, _b10) {
  constructor(value = true) {
    super();
    this.value = value;
  }
  static {
    __name(this, "StaticPanelAttribute");
  }
  static {
    this[_a21] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};
var Attributes;
((Attributes2) => {
  function advancedFiltering(value = true) {
    return new AdvancedFilteringAttribute(value);
  }
  Attributes2.advancedFiltering = advancedFiltering;
  __name(advancedFiltering, "advancedFiltering");
  function closeButton(value = true) {
    return new CloseButtonAttribute(value);
  }
  Attributes2.closeButton = closeButton;
  __name(closeButton, "closeButton");
  function resizable(value = true) {
    return new ResizableAttribute(value);
  }
  Attributes2.resizable = resizable;
  __name(resizable, "resizable");
  function maximizable(value = true) {
    return new MaximizableAttribute(value);
  }
  Attributes2.maximizable = maximizable;
  __name(maximizable, "maximizable");
  function panel(value = true) {
    return new PanelAttribute(value);
  }
  Attributes2.panel = panel;
  __name(panel, "panel");
  function staticPanel(value = true) {
    return new StaticPanelAttribute(value);
  }
  Attributes2.staticPanel = staticPanel;
  __name(staticPanel, "staticPanel");
  Object.keys(Attributes2).forEach((key) => Attributes2[key].isAttributeFactory = true);
})(Attributes || (Attributes = {}));
var FilterableAttribute = AdvancedFilteringAttribute;

// src/types/captureoperationtype.ts
var CaptureOperationType = /* @__PURE__ */ ((CaptureOperationType2) => {
  CaptureOperationType2[CaptureOperationType2["Before"] = 0] = "Before";
  CaptureOperationType2[CaptureOperationType2["Delete"] = 1] = "Delete";
  CaptureOperationType2[CaptureOperationType2["Insert"] = 2] = "Insert";
  CaptureOperationType2[CaptureOperationType2["Update"] = 3] = "Update";
  return CaptureOperationType2;
})(CaptureOperationType || {});
registerEnum(CaptureOperationType, "Serenity.CaptureOperationType");

// src/types/decorators.ts
var Decorators;
((Decorators2) => {
  function registerType2() {
    return function(target, _context) {
      if (_context && typeof _context.addInitializer === "function") {
        _context.addInitializer(() => {
          registerType(target);
        });
      } else
        registerType(target);
    };
  }
  Decorators2.registerType = registerType2;
  __name(registerType2, "registerType");
  function registerClass2(nameOrIntf, intf2) {
    return function(target, _context) {
      if (typeof nameOrIntf == "string")
        registerClass(target, nameOrIntf, intf2);
      else
        registerClass(target, null, nameOrIntf);
    };
  }
  Decorators2.registerClass = registerClass2;
  __name(registerClass2, "registerClass");
  function registerInterface2(nameOrIntf, intf2) {
    return function(target, _context) {
      if (typeof nameOrIntf == "string")
        registerInterface(target, nameOrIntf, intf2);
      else
        registerInterface(target, null, nameOrIntf);
    };
  }
  Decorators2.registerInterface = registerInterface2;
  __name(registerInterface2, "registerInterface");
  function registerEditor2(nameOrIntf, intf2) {
    return function(target, _context) {
      if (typeof nameOrIntf == "string")
        registerEditor(target, nameOrIntf, intf2);
      else
        registerEditor(target, null, nameOrIntf);
    };
  }
  Decorators2.registerEditor = registerEditor2;
  __name(registerEditor2, "registerEditor");
  function registerEnum2(target, enumKey2, name) {
    registerEnum(target, name, enumKey2);
    if (enumKey2)
      addCustomAttribute(target, new EnumKeyAttribute(enumKey2));
  }
  Decorators2.registerEnum = registerEnum2;
  __name(registerEnum2, "registerEnum");
  function registerEnumType(target, name, enumKey2) {
    registerEnum2(target, enumKey2 ?? name, name);
  }
  Decorators2.registerEnumType = registerEnumType;
  __name(registerEnumType, "registerEnumType");
  function registerFormatter2(nameOrIntf = [ISlickFormatter], intf2 = [ISlickFormatter]) {
    return function(target, _context) {
      if (typeof nameOrIntf == "string")
        registerFormatter(target, nameOrIntf, intf2);
      else
        registerFormatter(target, null, nameOrIntf);
    };
  }
  Decorators2.registerFormatter = registerFormatter2;
  __name(registerFormatter2, "registerFormatter");
  function enumKey(value) {
    return function(target, _context) {
      addCustomAttribute(target, new EnumKeyAttribute(value));
    };
  }
  Decorators2.enumKey = enumKey;
  __name(enumKey, "enumKey");
  function option() {
    return function(target, propertyKey) {
      const isGetSet = propertyKey?.startsWith("get_") || propertyKey?.startsWith("set_");
      const memberName = isGetSet ? propertyKey.substring(4) : propertyKey;
      addTypeMember(target.constructor, {
        name: memberName,
        attr: [new OptionAttribute()],
        kind: isGetSet ? 16 /* property */ : 4 /* field */,
        getter: isGetSet ? "get_" + memberName : null,
        setter: isGetSet ? "set_" + memberName : null
      });
    };
  }
  Decorators2.option = option;
  __name(option, "option");
  function closeButton(value = true) {
    return function(target, _context) {
      addCustomAttribute(target, new CloseButtonAttribute(value));
    };
  }
  Decorators2.closeButton = closeButton;
  __name(closeButton, "closeButton");
  function editor() {
    return function(target, _context) {
      addCustomAttribute(target, new EditorAttribute());
    };
  }
  Decorators2.editor = editor;
  __name(editor, "editor");
  function element(value) {
    return function(target, _context) {
      addCustomAttribute(target, new ElementAttribute(value));
    };
  }
  Decorators2.element = element;
  __name(element, "element");
  function advancedFiltering(value = true) {
    return function(target, _context) {
      addCustomAttribute(target, new AdvancedFilteringAttribute(value));
    };
  }
  Decorators2.advancedFiltering = advancedFiltering;
  __name(advancedFiltering, "advancedFiltering");
  Decorators2.filterable = advancedFiltering;
  function maximizable(value = true) {
    return function(target, _context) {
      addCustomAttribute(target, new MaximizableAttribute(value));
    };
  }
  Decorators2.maximizable = maximizable;
  __name(maximizable, "maximizable");
  function panel(value = true) {
    return function(target, _context) {
      addCustomAttribute(target, new PanelAttribute(value));
    };
  }
  Decorators2.panel = panel;
  __name(panel, "panel");
  function resizable(value = true) {
    return function(target, _context) {
      addCustomAttribute(target, new ResizableAttribute(value));
    };
  }
  Decorators2.resizable = resizable;
  __name(resizable, "resizable");
  function responsive(value = true) {
    return function(target, _context) {
    };
  }
  Decorators2.responsive = responsive;
  __name(responsive, "responsive");
  function staticPanel(value = true) {
    return function(target, _context) {
      addCustomAttribute(target, new StaticPanelAttribute(value));
    };
  }
  Decorators2.staticPanel = staticPanel;
  __name(staticPanel, "staticPanel");
})(Decorators || (Decorators = {}));

// src/types/basetyperegistry.ts
var BaseTypeRegistry = class {
  static {
    __name(this, "BaseTypeRegistry");
  }
  /**
   * Creates a new type registry instance.
   * @param options Configuration options for the registry
   */
  constructor(options) {
    this.loadKind = options.loadKind;
    this.defaultSuffix = options.defaultSuffix;
  }
  /**
   * Gets a secondary type key for the given type.
   * Only enums override this to provide legacy enum key support.
   * @param type The type to get a secondary key for
   * @returns The secondary key, or undefined if none
   */
  getSecondaryTypeKey(type) {
    return void 0;
  }
  /**
   * Determines if the given type matches the criteria for this registry.
   * Subclasses should override this to define which types they handle.
   * @param type The type to check
   * @returns True if the type matches this registry's criteria
   */
  isMatchingType(type) {
    return true;
  }
  /**
   * Searches for a type in the global type registry and root namespaces.
   * @param key The key to search for
   * @returns The found type, or null if not found
   */
  searchSystemTypes(key) {
    let type = getType(key);
    if (type != null && this.isMatchingType(type))
      return type;
    for (const ns of Config.rootNamespaces) {
      const k = ns + "." + key;
      type = this.registeredTypes[k];
      if (type)
        return type;
      type = getType(ns + "." + key);
      if (type != null && this.isMatchingType(type))
        return type;
    }
  }
  /**
   * Initializes the registry by scanning the global type registry
   * and building the local cache of matching types.
   */
  init() {
    this.registeredTypes = {};
    for (const [fullName, type] of Object.entries(getGlobalTypeRegistry())) {
      if (!this.isMatchingType(type))
        continue;
      this.registeredTypes[fullName] = type;
      const akey = this.getSecondaryTypeKey?.(type);
      if (akey && akey !== fullName)
        this.registeredTypes[akey] = type;
    }
    if (this.defaultSuffix) {
      for (const key of Object.keys(this.registeredTypes)) {
        if (key.endsWith(this.defaultSuffix)) {
          const p2 = key.substring(0, key.length - this.defaultSuffix.length);
          if (p2 && !this.registeredTypes[p2])
            this.registeredTypes[p2] = this.registeredTypes[key];
        }
      }
    }
  }
  /**
   * Gets a type by key, throwing an error if not found.
   * @param key The key to look up
   * @returns The found type
   * @throws When the type is not found
   */
  get(key) {
    const type = this.tryGet(key);
    if (type)
      return type;
    this.loadError(key);
  }
  /**
   * Gets a type by key, attempting lazy loading if not found.
   * @param key The key to look up
   * @returns The found type or a promise that resolves to it
   * @throws When the type cannot be found or loaded
   */
  getOrLoad(key) {
    const type = this.tryGetOrLoad(key);
    if (type) {
      if (isPromiseLike(type)) {
        return type.then((t2) => {
          if (!t2)
            this.loadError(key);
          return t2;
        });
      }
      return type;
    }
    this.loadError(key);
  }
  /**
   * Clears the registry cache, forcing re-initialization on next access.
   */
  reset() {
    this.registeredTypes = null;
  }
  /**
   * Attempts to get a type by key without throwing errors.
   * @param key The key to look up
   * @returns The found type, or null if not found
   */
  tryGet(key) {
    if (!key)
      return null;
    if (this.registeredTypes == null)
      this.init();
    let type = this.registeredTypes[key];
    if (type)
      return type;
    type = this.searchSystemTypes(key);
    if (type == null && this.defaultSuffix && !key.endsWith(this.defaultSuffix))
      type = this.registeredTypes[key + this.defaultSuffix] ?? this.searchSystemTypes(key + this.defaultSuffix);
    if (type) {
      this.registeredTypes[key] = type;
      const akey = this.getSecondaryTypeKey?.(type);
      if (akey && key != akey)
        this.registeredTypes[akey] = type;
      if (this.defaultSuffix && key.endsWith(this.defaultSuffix)) {
        const p2 = key.substring(0, key.length - this.defaultSuffix.length);
        if (p2 && !this.registeredTypes[p2])
          this.registeredTypes[p2] = this.registeredTypes[key];
      }
      return type;
    }
    return type;
  }
  /**
   * Attempts to get a type by key, with lazy loading support.
   * @param key The key to look up
   * @returns The found type, a promise that resolves to it, or null if not found
   */
  tryGetOrLoad(key) {
    let type = this.tryGet(key);
    if (type)
      return type;
    if (key && Config.lazyTypeLoader) {
      let promise = Config.lazyTypeLoader(key, this.loadKind);
      if (!promise && this.defaultSuffix) {
        promise = Config.lazyTypeLoader(key + this.defaultSuffix, this.loadKind);
      }
      if (!promise) {
        for (const ns of Config.rootNamespaces) {
          const k = ns + "." + key;
          if (promise = Config.lazyTypeLoader(k, this.loadKind))
            break;
          if (this.defaultSuffix && (promise = Config.lazyTypeLoader(k + this.defaultSuffix, this.loadKind)))
            break;
        }
      }
      if (isPromiseLike(promise)) {
        return promise.then((t2) => {
          if (t2 && this.isMatchingType(t2)) {
            this.registeredTypes[key] = t2;
            return t2;
          }
          return null;
        });
      }
      if (promise && this.isMatchingType(promise)) {
        this.registeredTypes[key] = promise;
        return promise;
      }
      return null;
    }
    return type;
  }
};

// src/types/dialogtyperegistry.ts
var DialogTypeRegistryImpl = class extends BaseTypeRegistry {
  static {
    __name(this, "DialogTypeRegistryImpl");
  }
  constructor() {
    super({
      loadKind: "dialog",
      defaultSuffix: "Dialog"
    });
  }
  isMatchingType(type) {
    return isAssignableFrom(IDialog, type);
  }
  loadError(key) {
    const message = `The dialog class "${key}" was not found.

Ensure that the dialog type includes a line similar to the following (using the correct full name):
static [Symbol.typeInfo] = this.registerClass("MyProject.MyModule.MyDialog");

Also, side-effect import this dialog class from the current page.ts, grid.ts, or dialog.ts file. For example:
import "./path/to/MyDialog.ts";

If you encounter this error from an editor with the InplaceAdd option, verify that the lookup key and dialog type name match case-sensitively, excluding the "Dialog" suffix. Specify the DialogType property in the LookupEditor attribute if it does not match.

After applying the fixes, build the project by running "npm run build" from the project folder.`;
    notifyError(message, "", { preWrap: true, timeOut: 5e3 });
    throw new Error(message);
  }
};
var DialogTypeRegistry = new DialogTypeRegistryImpl();

// src/ui/widgets/widgetutils.ts
var elementMap = /* @__PURE__ */ new WeakMap();
function getWidgetName(type) {
  return getTypeFullName(type)?.replace(/\./g, "_");
}
__name(getWidgetName, "getWidgetName");
function associateWidget(widget) {
  if (!widget || !widget.domNode)
    return;
  let type = getInstanceType(widget);
  let name = getWidgetName(type);
  var widgets = elementMap.get(widget.domNode);
  if (widgets) {
    if (widgets[name])
      throw new Error(`The element already has widget '${name}!`);
    widgets[name] = widget;
  } else {
    elementMap.set(widget.domNode, {
      [name]: widget
    });
  }
}
__name(associateWidget, "associateWidget");
function deassociateWidget(widget) {
  if (!widget || !widget.domNode)
    return;
  let type = getInstanceType(widget);
  let name = getWidgetName(type);
  var widgets = elementMap.get(widget.domNode);
  if (widgets) {
    delete widgets[name];
    if (!Object.keys(widgets).length)
      elementMap.delete(widget.domNode);
  }
}
__name(deassociateWidget, "deassociateWidget");
function tryGetWidget(element, type) {
  if (typeof element === "string") {
    element = document.querySelector(element);
  } else if (isArrayLike(element))
    element = element[0];
  if (!element)
    return null;
  let widgets = elementMap.get(element);
  if (!widgets)
    return null;
  var keys = Object.keys(widgets);
  if (!keys.length)
    return null;
  if (!type)
    return widgets[keys[0]] ?? null;
  var name = getWidgetName(type);
  var widget = widgets[name];
  if (widget)
    return widgets[name];
  for (var key of Object.keys(widgets)) {
    widget = widgets[key];
    if (widget && isAssignableFrom(type, getInstanceType(widget)))
      return widget;
  }
  return null;
}
__name(tryGetWidget, "tryGetWidget");
function getWidgetFrom(element, type) {
  let selector;
  if (typeof element === "string") {
    selector = element;
    element = document.querySelector(selector);
  }
  if (!element)
    throw new Error(`Searching for widget of type '${getTypeFullName(type) ?? "Widget"}' on a non-existent element! (${selector ?? "unknown"})`);
  var widget = tryGetWidget(element, type);
  if (!widget) {
    var message = `Element (${selector ?? "unknown"}) has no widget of type '${getTypeFullName(type)}'! If you have recently changed editor type of a property in a form class, or changed data type in row (which also changes editor type) your script side Form definition might be out of date. Make sure your project builds successfully and transformations are executed.`;
    notifyError(message, "", null);
    throw new Error(message);
  }
  return widget;
}
__name(getWidgetFrom, "getWidgetFrom");
Fluent.prototype.getWidget = function(type) {
  return getWidgetFrom(this, type);
};
Fluent.prototype.tryGetWidget = function(type) {
  return tryGetWidget(this, type);
};
function useIdPrefix(prefix) {
  return new Proxy({ _: prefix ?? "" }, idPrefixHandler);
}
__name(useIdPrefix, "useIdPrefix");
var idPrefixHandler = {
  get(target, p2) {
    if (p2.startsWith("#"))
      return "#" + target._ + p2.substring(1);
    return target._ + p2;
  }
};
function applyGetWidgetExtensions($2) {
  if (!$2 || !$2.fn)
    return false;
  $2.fn.tryGetWidget = /* @__PURE__ */ __name(function tryGetWidget$(type) {
    return tryGetWidget(this[0], type);
  }, "tryGetWidget$");
  $2.fn.getWidget = /* @__PURE__ */ __name(function getWidget$(type) {
    if (!this?.length)
      throw new Error(`Searching for widget of type '${getTypeFullName(type)}' on a non-existent element! (${this?.selector})`);
    return getWidgetFrom(this[0], type);
  }, "getWidget$");
}
__name(applyGetWidgetExtensions, "applyGetWidgetExtensions");
!applyGetWidgetExtensions(getjQuery()) && Fluent.ready(() => applyGetWidgetExtensions(getjQuery()));

// src/ui/editors/select2.ts
var lastMousePosition = { x: 0, y: 0 };
var KEY = {
  TAB: 9,
  ENTER: 13,
  ESC: 27,
  SPACE: 32,
  LEFT: 37,
  UP: 38,
  RIGHT: 39,
  DOWN: 40,
  SHIFT: 16,
  CTRL: 17,
  ALT: 18,
  PAGE_UP: 33,
  PAGE_DOWN: 34,
  HOME: 36,
  END: 35,
  BACKSPACE: 8,
  DELETE: 46,
  isArrow: /* @__PURE__ */ __name(function(k) {
    k = k.which ? k.which : k;
    switch (k) {
      case KEY.LEFT:
      case KEY.RIGHT:
      case KEY.UP:
      case KEY.DOWN:
        return true;
    }
    return false;
  }, "isArrow"),
  isControl: /* @__PURE__ */ __name(function(e2) {
    var k = e2.which;
    switch (k) {
      case KEY.SHIFT:
      case KEY.CTRL:
      case KEY.ALT:
        return true;
    }
    if (e2.metaKey) return true;
    return false;
  }, "isControl"),
  isFunctionKey: /* @__PURE__ */ __name(function(k) {
    k = k.which ? k.which : k;
    return k >= 112 && k <= 123;
  }, "isFunctionKey")
};
var DIACRITICS = { "\u24B6": "A", "\uFF21": "A", "\xC0": "A", "\xC1": "A", "\xC2": "A", "\u1EA6": "A", "\u1EA4": "A", "\u1EAA": "A", "\u1EA8": "A", "\xC3": "A", "\u0100": "A", "\u0102": "A", "\u1EB0": "A", "\u1EAE": "A", "\u1EB4": "A", "\u1EB2": "A", "\u0226": "A", "\u01E0": "A", "\xC4": "A", "\u01DE": "A", "\u1EA2": "A", "\xC5": "A", "\u01FA": "A", "\u01CD": "A", "\u0200": "A", "\u0202": "A", "\u1EA0": "A", "\u1EAC": "A", "\u1EB6": "A", "\u1E00": "A", "\u0104": "A", "\u023A": "A", "\u2C6F": "A", "\uA732": "AA", "\xC6": "AE", "\u01FC": "AE", "\u01E2": "AE", "\uA734": "AO", "\uA736": "AU", "\uA738": "AV", "\uA73A": "AV", "\uA73C": "AY", "\u24B7": "B", "\uFF22": "B", "\u1E02": "B", "\u1E04": "B", "\u1E06": "B", "\u0243": "B", "\u0182": "B", "\u0181": "B", "\u24B8": "C", "\uFF23": "C", "\u0106": "C", "\u0108": "C", "\u010A": "C", "\u010C": "C", "\xC7": "C", "\u1E08": "C", "\u0187": "C", "\u023B": "C", "\uA73E": "C", "\u24B9": "D", "\uFF24": "D", "\u1E0A": "D", "\u010E": "D", "\u1E0C": "D", "\u1E10": "D", "\u1E12": "D", "\u1E0E": "D", "\u0110": "D", "\u018B": "D", "\u018A": "D", "\u0189": "D", "\uA779": "D", "\u01F1": "DZ", "\u01C4": "DZ", "\u01F2": "Dz", "\u01C5": "Dz", "\u24BA": "E", "\uFF25": "E", "\xC8": "E", "\xC9": "E", "\xCA": "E", "\u1EC0": "E", "\u1EBE": "E", "\u1EC4": "E", "\u1EC2": "E", "\u1EBC": "E", "\u0112": "E", "\u1E14": "E", "\u1E16": "E", "\u0114": "E", "\u0116": "E", "\xCB": "E", "\u1EBA": "E", "\u011A": "E", "\u0204": "E", "\u0206": "E", "\u1EB8": "E", "\u1EC6": "E", "\u0228": "E", "\u1E1C": "E", "\u0118": "E", "\u1E18": "E", "\u1E1A": "E", "\u0190": "E", "\u018E": "E", "\u24BB": "F", "\uFF26": "F", "\u1E1E": "F", "\u0191": "F", "\uA77B": "F", "\u24BC": "G", "\uFF27": "G", "\u01F4": "G", "\u011C": "G", "\u1E20": "G", "\u011E": "G", "\u0120": "G", "\u01E6": "G", "\u0122": "G", "\u01E4": "G", "\u0193": "G", "\uA7A0": "G", "\uA77D": "G", "\uA77E": "G", "\u24BD": "H", "\uFF28": "H", "\u0124": "H", "\u1E22": "H", "\u1E26": "H", "\u021E": "H", "\u1E24": "H", "\u1E28": "H", "\u1E2A": "H", "\u0126": "H", "\u2C67": "H", "\u2C75": "H", "\uA78D": "H", "\u24BE": "I", "\uFF29": "I", "\xCC": "I", "\xCD": "I", "\xCE": "I", "\u0128": "I", "\u012A": "I", "\u012C": "I", "\u0130": "I", "\xCF": "I", "\u1E2E": "I", "\u1EC8": "I", "\u01CF": "I", "\u0208": "I", "\u020A": "I", "\u1ECA": "I", "\u012E": "I", "\u1E2C": "I", "\u0197": "I", "\u24BF": "J", "\uFF2A": "J", "\u0134": "J", "\u0248": "J", "\u24C0": "K", "\uFF2B": "K", "\u1E30": "K", "\u01E8": "K", "\u1E32": "K", "\u0136": "K", "\u1E34": "K", "\u0198": "K", "\u2C69": "K", "\uA740": "K", "\uA742": "K", "\uA744": "K", "\uA7A2": "K", "\u24C1": "L", "\uFF2C": "L", "\u013F": "L", "\u0139": "L", "\u013D": "L", "\u1E36": "L", "\u1E38": "L", "\u013B": "L", "\u1E3C": "L", "\u1E3A": "L", "\u0141": "L", "\u023D": "L", "\u2C62": "L", "\u2C60": "L", "\uA748": "L", "\uA746": "L", "\uA780": "L", "\u01C7": "LJ", "\u01C8": "Lj", "\u24C2": "M", "\uFF2D": "M", "\u1E3E": "M", "\u1E40": "M", "\u1E42": "M", "\u2C6E": "M", "\u019C": "M", "\u24C3": "N", "\uFF2E": "N", "\u01F8": "N", "\u0143": "N", "\xD1": "N", "\u1E44": "N", "\u0147": "N", "\u1E46": "N", "\u0145": "N", "\u1E4A": "N", "\u1E48": "N", "\u0220": "N", "\u019D": "N", "\uA790": "N", "\uA7A4": "N", "\u01CA": "NJ", "\u01CB": "Nj", "\u24C4": "O", "\uFF2F": "O", "\xD2": "O", "\xD3": "O", "\xD4": "O", "\u1ED2": "O", "\u1ED0": "O", "\u1ED6": "O", "\u1ED4": "O", "\xD5": "O", "\u1E4C": "O", "\u022C": "O", "\u1E4E": "O", "\u014C": "O", "\u1E50": "O", "\u1E52": "O", "\u014E": "O", "\u022E": "O", "\u0230": "O", "\xD6": "O", "\u022A": "O", "\u1ECE": "O", "\u0150": "O", "\u01D1": "O", "\u020C": "O", "\u020E": "O", "\u01A0": "O", "\u1EDC": "O", "\u1EDA": "O", "\u1EE0": "O", "\u1EDE": "O", "\u1EE2": "O", "\u1ECC": "O", "\u1ED8": "O", "\u01EA": "O", "\u01EC": "O", "\xD8": "O", "\u01FE": "O", "\u0186": "O", "\u019F": "O", "\uA74A": "O", "\uA74C": "O", "\u01A2": "OI", "\uA74E": "OO", "\u0222": "OU", "\u24C5": "P", "\uFF30": "P", "\u1E54": "P", "\u1E56": "P", "\u01A4": "P", "\u2C63": "P", "\uA750": "P", "\uA752": "P", "\uA754": "P", "\u24C6": "Q", "\uFF31": "Q", "\uA756": "Q", "\uA758": "Q", "\u024A": "Q", "\u24C7": "R", "\uFF32": "R", "\u0154": "R", "\u1E58": "R", "\u0158": "R", "\u0210": "R", "\u0212": "R", "\u1E5A": "R", "\u1E5C": "R", "\u0156": "R", "\u1E5E": "R", "\u024C": "R", "\u2C64": "R", "\uA75A": "R", "\uA7A6": "R", "\uA782": "R", "\u24C8": "S", "\uFF33": "S", "\u1E9E": "S", "\u015A": "S", "\u1E64": "S", "\u015C": "S", "\u1E60": "S", "\u0160": "S", "\u1E66": "S", "\u1E62": "S", "\u1E68": "S", "\u0218": "S", "\u015E": "S", "\u2C7E": "S", "\uA7A8": "S", "\uA784": "S", "\u24C9": "T", "\uFF34": "T", "\u1E6A": "T", "\u0164": "T", "\u1E6C": "T", "\u021A": "T", "\u0162": "T", "\u1E70": "T", "\u1E6E": "T", "\u0166": "T", "\u01AC": "T", "\u01AE": "T", "\u023E": "T", "\uA786": "T", "\uA728": "TZ", "\u24CA": "U", "\uFF35": "U", "\xD9": "U", "\xDA": "U", "\xDB": "U", "\u0168": "U", "\u1E78": "U", "\u016A": "U", "\u1E7A": "U", "\u016C": "U", "\xDC": "U", "\u01DB": "U", "\u01D7": "U", "\u01D5": "U", "\u01D9": "U", "\u1EE6": "U", "\u016E": "U", "\u0170": "U", "\u01D3": "U", "\u0214": "U", "\u0216": "U", "\u01AF": "U", "\u1EEA": "U", "\u1EE8": "U", "\u1EEE": "U", "\u1EEC": "U", "\u1EF0": "U", "\u1EE4": "U", "\u1E72": "U", "\u0172": "U", "\u1E76": "U", "\u1E74": "U", "\u0244": "U", "\u24CB": "V", "\uFF36": "V", "\u1E7C": "V", "\u1E7E": "V", "\u01B2": "V", "\uA75E": "V", "\u0245": "V", "\uA760": "VY", "\u24CC": "W", "\uFF37": "W", "\u1E80": "W", "\u1E82": "W", "\u0174": "W", "\u1E86": "W", "\u1E84": "W", "\u1E88": "W", "\u2C72": "W", "\u24CD": "X", "\uFF38": "X", "\u1E8A": "X", "\u1E8C": "X", "\u24CE": "Y", "\uFF39": "Y", "\u1EF2": "Y", "\xDD": "Y", "\u0176": "Y", "\u1EF8": "Y", "\u0232": "Y", "\u1E8E": "Y", "\u0178": "Y", "\u1EF6": "Y", "\u1EF4": "Y", "\u01B3": "Y", "\u024E": "Y", "\u1EFE": "Y", "\u24CF": "Z", "\uFF3A": "Z", "\u0179": "Z", "\u1E90": "Z", "\u017B": "Z", "\u017D": "Z", "\u1E92": "Z", "\u1E94": "Z", "\u01B5": "Z", "\u0224": "Z", "\u2C7F": "Z", "\u2C6B": "Z", "\uA762": "Z", "\u24D0": "a", "\uFF41": "a", "\u1E9A": "a", "\xE0": "a", "\xE1": "a", "\xE2": "a", "\u1EA7": "a", "\u1EA5": "a", "\u1EAB": "a", "\u1EA9": "a", "\xE3": "a", "\u0101": "a", "\u0103": "a", "\u1EB1": "a", "\u1EAF": "a", "\u1EB5": "a", "\u1EB3": "a", "\u0227": "a", "\u01E1": "a", "\xE4": "a", "\u01DF": "a", "\u1EA3": "a", "\xE5": "a", "\u01FB": "a", "\u01CE": "a", "\u0201": "a", "\u0203": "a", "\u1EA1": "a", "\u1EAD": "a", "\u1EB7": "a", "\u1E01": "a", "\u0105": "a", "\u2C65": "a", "\u0250": "a", "\uA733": "aa", "\xE6": "ae", "\u01FD": "ae", "\u01E3": "ae", "\uA735": "ao", "\uA737": "au", "\uA739": "av", "\uA73B": "av", "\uA73D": "ay", "\u24D1": "b", "\uFF42": "b", "\u1E03": "b", "\u1E05": "b", "\u1E07": "b", "\u0180": "b", "\u0183": "b", "\u0253": "b", "\u24D2": "c", "\uFF43": "c", "\u0107": "c", "\u0109": "c", "\u010B": "c", "\u010D": "c", "\xE7": "c", "\u1E09": "c", "\u0188": "c", "\u023C": "c", "\uA73F": "c", "\u2184": "c", "\u24D3": "d", "\uFF44": "d", "\u1E0B": "d", "\u010F": "d", "\u1E0D": "d", "\u1E11": "d", "\u1E13": "d", "\u1E0F": "d", "\u0111": "d", "\u018C": "d", "\u0256": "d", "\u0257": "d", "\uA77A": "d", "\u01F3": "dz", "\u01C6": "dz", "\u24D4": "e", "\uFF45": "e", "\xE8": "e", "\xE9": "e", "\xEA": "e", "\u1EC1": "e", "\u1EBF": "e", "\u1EC5": "e", "\u1EC3": "e", "\u1EBD": "e", "\u0113": "e", "\u1E15": "e", "\u1E17": "e", "\u0115": "e", "\u0117": "e", "\xEB": "e", "\u1EBB": "e", "\u011B": "e", "\u0205": "e", "\u0207": "e", "\u1EB9": "e", "\u1EC7": "e", "\u0229": "e", "\u1E1D": "e", "\u0119": "e", "\u1E19": "e", "\u1E1B": "e", "\u0247": "e", "\u025B": "e", "\u01DD": "e", "\u24D5": "f", "\uFF46": "f", "\u1E1F": "f", "\u0192": "f", "\uA77C": "f", "\u24D6": "g", "\uFF47": "g", "\u01F5": "g", "\u011D": "g", "\u1E21": "g", "\u011F": "g", "\u0121": "g", "\u01E7": "g", "\u0123": "g", "\u01E5": "g", "\u0260": "g", "\uA7A1": "g", "\u1D79": "g", "\uA77F": "g", "\u24D7": "h", "\uFF48": "h", "\u0125": "h", "\u1E23": "h", "\u1E27": "h", "\u021F": "h", "\u1E25": "h", "\u1E29": "h", "\u1E2B": "h", "\u1E96": "h", "\u0127": "h", "\u2C68": "h", "\u2C76": "h", "\u0265": "h", "\u0195": "hv", "\u24D8": "i", "\uFF49": "i", "\xEC": "i", "\xED": "i", "\xEE": "i", "\u0129": "i", "\u012B": "i", "\u012D": "i", "\xEF": "i", "\u1E2F": "i", "\u1EC9": "i", "\u01D0": "i", "\u0209": "i", "\u020B": "i", "\u1ECB": "i", "\u012F": "i", "\u1E2D": "i", "\u0268": "i", "\u0131": "i", "\u24D9": "j", "\uFF4A": "j", "\u0135": "j", "\u01F0": "j", "\u0249": "j", "\u24DA": "k", "\uFF4B": "k", "\u1E31": "k", "\u01E9": "k", "\u1E33": "k", "\u0137": "k", "\u1E35": "k", "\u0199": "k", "\u2C6A": "k", "\uA741": "k", "\uA743": "k", "\uA745": "k", "\uA7A3": "k", "\u24DB": "l", "\uFF4C": "l", "\u0140": "l", "\u013A": "l", "\u013E": "l", "\u1E37": "l", "\u1E39": "l", "\u013C": "l", "\u1E3D": "l", "\u1E3B": "l", "\u017F": "l", "\u0142": "l", "\u019A": "l", "\u026B": "l", "\u2C61": "l", "\uA749": "l", "\uA781": "l", "\uA747": "l", "\u01C9": "lj", "\u24DC": "m", "\uFF4D": "m", "\u1E3F": "m", "\u1E41": "m", "\u1E43": "m", "\u0271": "m", "\u026F": "m", "\u24DD": "n", "\uFF4E": "n", "\u01F9": "n", "\u0144": "n", "\xF1": "n", "\u1E45": "n", "\u0148": "n", "\u1E47": "n", "\u0146": "n", "\u1E4B": "n", "\u1E49": "n", "\u019E": "n", "\u0272": "n", "\u0149": "n", "\uA791": "n", "\uA7A5": "n", "\u01CC": "nj", "\u24DE": "o", "\uFF4F": "o", "\xF2": "o", "\xF3": "o", "\xF4": "o", "\u1ED3": "o", "\u1ED1": "o", "\u1ED7": "o", "\u1ED5": "o", "\xF5": "o", "\u1E4D": "o", "\u022D": "o", "\u1E4F": "o", "\u014D": "o", "\u1E51": "o", "\u1E53": "o", "\u014F": "o", "\u022F": "o", "\u0231": "o", "\xF6": "o", "\u022B": "o", "\u1ECF": "o", "\u0151": "o", "\u01D2": "o", "\u020D": "o", "\u020F": "o", "\u01A1": "o", "\u1EDD": "o", "\u1EDB": "o", "\u1EE1": "o", "\u1EDF": "o", "\u1EE3": "o", "\u1ECD": "o", "\u1ED9": "o", "\u01EB": "o", "\u01ED": "o", "\xF8": "o", "\u01FF": "o", "\u0254": "o", "\uA74B": "o", "\uA74D": "o", "\u0275": "o", "\u01A3": "oi", "\u0223": "ou", "\uA74F": "oo", "\u24DF": "p", "\uFF50": "p", "\u1E55": "p", "\u1E57": "p", "\u01A5": "p", "\u1D7D": "p", "\uA751": "p", "\uA753": "p", "\uA755": "p", "\u24E0": "q", "\uFF51": "q", "\u024B": "q", "\uA757": "q", "\uA759": "q", "\u24E1": "r", "\uFF52": "r", "\u0155": "r", "\u1E59": "r", "\u0159": "r", "\u0211": "r", "\u0213": "r", "\u1E5B": "r", "\u1E5D": "r", "\u0157": "r", "\u1E5F": "r", "\u024D": "r", "\u027D": "r", "\uA75B": "r", "\uA7A7": "r", "\uA783": "r", "\u24E2": "s", "\uFF53": "s", "\xDF": "s", "\u015B": "s", "\u1E65": "s", "\u015D": "s", "\u1E61": "s", "\u0161": "s", "\u1E67": "s", "\u1E63": "s", "\u1E69": "s", "\u0219": "s", "\u015F": "s", "\u023F": "s", "\uA7A9": "s", "\uA785": "s", "\u1E9B": "s", "\u24E3": "t", "\uFF54": "t", "\u1E6B": "t", "\u1E97": "t", "\u0165": "t", "\u1E6D": "t", "\u021B": "t", "\u0163": "t", "\u1E71": "t", "\u1E6F": "t", "\u0167": "t", "\u01AD": "t", "\u0288": "t", "\u2C66": "t", "\uA787": "t", "\uA729": "tz", "\u24E4": "u", "\uFF55": "u", "\xF9": "u", "\xFA": "u", "\xFB": "u", "\u0169": "u", "\u1E79": "u", "\u016B": "u", "\u1E7B": "u", "\u016D": "u", "\xFC": "u", "\u01DC": "u", "\u01D8": "u", "\u01D6": "u", "\u01DA": "u", "\u1EE7": "u", "\u016F": "u", "\u0171": "u", "\u01D4": "u", "\u0215": "u", "\u0217": "u", "\u01B0": "u", "\u1EEB": "u", "\u1EE9": "u", "\u1EEF": "u", "\u1EED": "u", "\u1EF1": "u", "\u1EE5": "u", "\u1E73": "u", "\u0173": "u", "\u1E77": "u", "\u1E75": "u", "\u0289": "u", "\u24E5": "v", "\uFF56": "v", "\u1E7D": "v", "\u1E7F": "v", "\u028B": "v", "\uA75F": "v", "\u028C": "v", "\uA761": "vy", "\u24E6": "w", "\uFF57": "w", "\u1E81": "w", "\u1E83": "w", "\u0175": "w", "\u1E87": "w", "\u1E85": "w", "\u1E98": "w", "\u1E89": "w", "\u2C73": "w", "\u24E7": "x", "\uFF58": "x", "\u1E8B": "x", "\u1E8D": "x", "\u24E8": "y", "\uFF59": "y", "\u1EF3": "y", "\xFD": "y", "\u0177": "y", "\u1EF9": "y", "\u0233": "y", "\u1E8F": "y", "\xFF": "y", "\u1EF7": "y", "\u1E99": "y", "\u1EF5": "y", "\u01B4": "y", "\u024F": "y", "\u1EFF": "y", "\u24E9": "z", "\uFF5A": "z", "\u017A": "z", "\u1E91": "z", "\u017C": "z", "\u017E": "z", "\u1E93": "z", "\u1E95": "z", "\u01B6": "z", "\u0225": "z", "\u0240": "z", "\u2C6C": "z", "\uA763": "z", "\u0386": "\u0391", "\u0388": "\u0395", "\u0389": "\u0397", "\u038A": "\u0399", "\u03AA": "\u0399", "\u038C": "\u039F", "\u038E": "\u03A5", "\u03AB": "\u03A5", "\u038F": "\u03A9", "\u03AC": "\u03B1", "\u03AD": "\u03B5", "\u03AE": "\u03B7", "\u03AF": "\u03B9", "\u03CA": "\u03B9", "\u0390": "\u03B9", "\u03CC": "\u03BF", "\u03CD": "\u03C5", "\u03CB": "\u03C5", "\u03B0": "\u03C5", "\u03C9": "\u03C9", "\u03C2": "\u03C3" };
var nextUid = /* @__PURE__ */ (function() {
  var counter = 1;
  return function() {
    return counter++;
  };
})();
function reinsertElement(element) {
  var placeholder = document.createTextNode("");
  Fluent(placeholder).insertBefore(element);
  Fluent(element).insertBefore(placeholder);
  placeholder.remove();
}
__name(reinsertElement, "reinsertElement");
function indexOf2(value, array) {
  var i2 = 0, l2 = array.length;
  for (; i2 < l2; i2 = i2 + 1) {
    if (equal(value, array[i2])) return i2;
  }
  return -1;
}
__name(indexOf2, "indexOf");
var scrollBarDimensions = null;
function getOffset(el) {
  var box = el.getBoundingClientRect();
  var docElem = document.documentElement;
  return {
    top: box.top + window.scrollY - docElem.clientTop,
    left: box.left + window.scrollX - docElem.clientLeft
  };
}
__name(getOffset, "getOffset");
function measureScrollbar() {
  var $template = document.createElement("div");
  $template.classList.add("select2-measure-scrollbar");
  document.body.appendChild($template);
  var dim = {
    width: $template.offsetWidth - $template.clientWidth,
    height: $template.offsetHeight - $template.clientHeight
  };
  $template.remove();
  return dim;
}
__name(measureScrollbar, "measureScrollbar");
function equal(a2, b2) {
  if (a2 === b2) return true;
  if (a2 === void 0 || b2 === void 0) return false;
  if (a2 === null || b2 === null) return false;
  if (a2.constructor === String) return a2 + "" === b2 + "";
  if (b2.constructor === String) return b2 + "" === a2 + "";
  return false;
}
__name(equal, "equal");
function splitVal(string, separator) {
  var val, i2, l2;
  if (string === null || string.length < 1) return [];
  val = string.split(separator);
  for (i2 = 0, l2 = val.length; i2 < l2; i2 = i2 + 1) val[i2] = val[i2].trim();
  return val;
}
__name(splitVal, "splitVal");
function parsePx(str) {
  var value = parseFloat(str);
  if (isNaN(value))
    return 0;
  return value;
}
__name(parsePx, "parsePx");
function getSideBorderPadding(el) {
  if (!el)
    return 0;
  var style = getComputedStyle(el);
  var p2 = ["border-left-width", "border-right-width", "padding-left", "padding-right"];
  var delta = 0;
  for (var val of p2)
    delta += parsePx(style.getPropertyValue(val)) || 0;
  return delta;
}
__name(getSideBorderPadding, "getSideBorderPadding");
function getOuterHeightWithMargins(el) {
  const style = getComputedStyle(el);
  return el.getBoundingClientRect().height + parseFloat(style.marginTop) + parseFloat(style.marginBottom);
}
__name(getOuterHeightWithMargins, "getOuterHeightWithMargins");
function installKeyUpChangeEvent(element) {
  var key = "keyupChangeValue";
  Fluent.on(element, "keydown", function() {
    if (element.dataset[key] === void 0) {
      element.dataset[key] = element.value;
    }
  });
  Fluent.on(element, "keyup", function() {
    var val = element.dataset[key];
    if (val !== void 0 && element.value !== val) {
      delete element.dataset[key];
      Fluent.trigger(element, "keyup-change");
    }
  });
}
__name(installKeyUpChangeEvent, "installKeyUpChangeEvent");
function installFilteredMouseMove(element) {
  Fluent.on(element, "mousemove", function(e2) {
    var lastpos = lastMousePosition;
    if (lastpos === void 0 || lastpos.x !== e2.pageX || lastpos.y !== e2.pageY) {
      Fluent.trigger(e2.target, "mousemove-filtered", { pageX: e2.pageX, pageY: e2.pageY });
    }
  });
}
__name(installFilteredMouseMove, "installFilteredMouseMove");
function debounce2(quietMillis, fn, ctx) {
  ctx = ctx || void 0;
  var timeout;
  return function() {
    var args = arguments;
    clearTimeout(timeout);
    timeout = window.setTimeout(function() {
      fn.apply(ctx, args);
    }, quietMillis);
  };
}
__name(debounce2, "debounce");
function installDebouncedScroll(threshold, element) {
  var notify = debounce2(threshold, function(args) {
    Fluent.trigger(element, "scroll-debounced", args);
  });
  Fluent.on(element, "scroll", function(e2) {
    if (e2.target === element) notify();
  });
}
__name(installDebouncedScroll, "installDebouncedScroll");
function focus($el) {
  if ($el === document.activeElement) return;
  window.setTimeout(function() {
    var el = $el, pos = $el.value?.length || 0, range;
    $el.focus();
    var isVisible = el.offsetWidth > 0 || el.offsetHeight > 0;
    if (isVisible && el === document.activeElement) {
      if (el.setSelectionRange) {
        el.setSelectionRange(pos, pos);
      } else if (el.createTextRange) {
        range = el.createTextRange();
        range.collapse(false);
        range.select();
      }
    }
  }, 0);
}
__name(focus, "focus");
function getCursorInfo(el) {
  var offset = 0;
  var length = 0;
  if ("selectionStart" in el) {
    offset = el.selectionStart;
    length = el.selectionEnd - offset;
  } else if ("selection" in document) {
    el.focus();
    var sel = document.selection.createRange();
    length = document.selection.createRange().text.length;
    sel.moveStart("character", -el.value.length);
    offset = sel.text.length - length;
  }
  return { offset, length };
}
__name(getCursorInfo, "getCursorInfo");
function killEvent(event) {
  event.preventDefault();
  event.stopPropagation();
}
__name(killEvent, "killEvent");
function killEventImmediately(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}
__name(killEventImmediately, "killEventImmediately");
var sizer;
function measureTextWidth(e2) {
  if (!sizer) {
    var style = getComputedStyle(e2, null);
    sizer = document.createElement("div");
    Object.assign(sizer.style, {
      position: "absolute",
      left: "-10000px",
      top: "-10000px",
      display: "none",
      fontSize: style.fontSize,
      fontFamily: style.fontFamily,
      fontStyle: style.fontStyle,
      fontWeight: style.fontWeight,
      letterSpacing: style.letterSpacing,
      textTransform: style.textTransform,
      whiteSpace: "nowrap"
    });
    sizer.setAttribute("class", "select2-sizer");
    document.body.appendChild(sizer);
  }
  sizer.textContent = e2.value;
  return sizer.offsetWidth;
}
__name(measureTextWidth, "measureTextWidth");
function syncCssClasses(dest, src, adapter) {
  var classes, replacements = [], adapted;
  classes = dest.getAttribute("class")?.trim();
  if (classes) {
    classes = "" + classes;
    classes.split(/\s+/).forEach(function(kls) {
      if (kls.indexOf("select2-") === 0) {
        replacements.push(kls);
      }
    });
  }
  classes = src.getAttribute("class")?.trim();
  if (classes) {
    classes = "" + classes;
    classes.split(/\s+/).forEach(function(kls) {
      if (kls.indexOf("select2-") !== 0) {
        adapted = adapter(kls);
        if (adapted) {
          replacements.push(adapted);
        }
      }
    });
  }
  dest.setAttribute("class", replacements.join(" "));
}
__name(syncCssClasses, "syncCssClasses");
function markMatch(text2, term, markup, escapeMarkup) {
  var match = Select2.stripDiacritics(text2.toUpperCase()).indexOf(Select2.stripDiacritics(term.toUpperCase())), tl = term.length;
  if (match < 0) {
    markup.push(escapeMarkup(text2));
    return;
  }
  markup.push(escapeMarkup(text2.substring(0, match)));
  markup.push("<span class='select2-match'>");
  markup.push(escapeMarkup(text2.substring(match, match + tl)));
  markup.push("</span>");
  markup.push(escapeMarkup(text2.substring(match + tl, text2.length)));
}
__name(markMatch, "markMatch");
function defaultEscapeMarkup(markup) {
  var replace_map = {
    "\\": "&#92;",
    "&": "&amp;",
    "<": "&lt;",
    ">": "&gt;",
    '"': "&quot;",
    "'": "&#39;",
    "/": "&#47;"
  };
  return String(markup).replace(/[&<>"'\/\\]/g, function(match) {
    return replace_map[match];
  });
}
__name(defaultEscapeMarkup, "defaultEscapeMarkup");
var Select2 = class _Select2 {
  static {
    __name(this, "Select2");
  }
  constructor(opts, create = true) {
    this.el = opts.element;
    if (!create) {
      return;
    }
    opts = Object.assign({}, opts);
    var multiple;
    if (opts.element.tagName.toLowerCase() === "select") {
      multiple = opts.element.multiple;
    } else {
      multiple = opts.multiple || false;
      if ("tags" in opts) {
        opts.multiple = multiple = true;
      }
    }
    var select2 = multiple ? new MultiSelect2() : new SingleSelect2();
    select2.init(opts);
  }
  get instance() {
    if (!this.el)
      return null;
    return this.el.select2;
  }
  close() {
    this.instance?.close();
  }
  get container() {
    return this.instance?.container;
  }
  get dropdown() {
    return this.instance?.dropdown;
  }
  destroy() {
    this.instance?.destroy();
  }
  get data() {
    return this.instance?.data();
  }
  set data(value) {
    this.instance?.data(value);
  }
  disable() {
    this.instance?.disable();
  }
  enable(enabled) {
    this.instance?.enable(enabled);
  }
  focus() {
    this.instance?.focus();
  }
  get isFocused() {
    return this.instance?.isFocused();
  }
  get isMultiple() {
    return this.instance instanceof MultiSelect2;
  }
  get opened() {
    return this.instance?.opened();
  }
  open() {
    return this.instance?.open();
  }
  positionDropdown() {
    this.instance?.positionDropdown();
  }
  readonly(value) {
    this.instance?.readonly(value);
  }
  get search() {
    return this.instance?.search;
  }
  get val() {
    return this.instance?.val();
  }
  set val(value) {
    this.instance?.val(value);
  }
  static getInstance(el) {
    if (!el || !el.select2)
      return null;
    return new _Select2({ element: el }, false);
  }
  static {
    this.ajaxDefaults = {
      params: {
        method: "GET",
        cache: false,
        dataType: "json"
      }
    };
  }
  static {
    // plugin defaults, accessible to users
    this.defaults = {
      width: "copy",
      loadMorePadding: 0,
      closeOnSelect: true,
      openOnEnter: true,
      containerCss: {},
      dropdownCss: {},
      containerCssClass: "",
      dropdownCssClass: "",
      formatAjaxError: /* @__PURE__ */ __name(() => SelectEditorTexts.AjaxError, "formatAjaxError"),
      formatInputTooLong: /* @__PURE__ */ __name((input, max) => stringFormat(SelectEditorTexts.InputTooLong, input.length - max, max, input.length), "formatInputTooLong"),
      formatInputTooShort: /* @__PURE__ */ __name((input, min) => stringFormat(SelectEditorTexts.InputTooShort, min - input.length, min, input.length), "formatInputTooShort"),
      formatLoadMore: /* @__PURE__ */ __name((pageNumber) => stringFormat(SelectEditorTexts.LoadMore, pageNumber), "formatLoadMore"),
      formatMatches: /* @__PURE__ */ __name((matches) => matches === 1 ? SelectEditorTexts.SingleMatch : stringFormat(SelectEditorTexts.MultipleMatches, matches), "formatMatches"),
      formatNoMatches: /* @__PURE__ */ __name(() => SelectEditorTexts.NoMatches, "formatNoMatches"),
      formatResult: /* @__PURE__ */ __name(function(result, _2, query, escapeMarkup) {
        var markup = [];
        markMatch(result.text, query.term, markup, escapeMarkup);
        return markup.join("");
      }, "formatResult"),
      formatResultCssClass: /* @__PURE__ */ __name(function(data) {
        return data.css;
      }, "formatResultCssClass"),
      formatSearching: /* @__PURE__ */ __name(() => SelectEditorTexts.Searching, "formatSearching"),
      formatSelection: /* @__PURE__ */ __name((data, _2, escapeMarkup) => data ? escapeMarkup(data.text) : void 0, "formatSelection"),
      formatSelectionCssClass: /* @__PURE__ */ __name(function() {
        return void 0;
      }, "formatSelectionCssClass"),
      formatSelectionTooBig: /* @__PURE__ */ __name((limit) => stringFormat(SelectEditorTexts.SelectionTooBig, limit), "formatSelectionTooBig"),
      sortResults: /* @__PURE__ */ __name((results) => results, "sortResults"),
      minimumResultsForSearch: 0,
      minimumInputLength: 0,
      maximumInputLength: null,
      maximumSelectionSize: 0,
      id: /* @__PURE__ */ __name(function(e2) {
        return e2 == void 0 ? null : e2.id;
      }, "id"),
      matcher: /* @__PURE__ */ __name((term, text2) => _Select2.stripDiacritics("" + text2).toUpperCase().indexOf(_Select2.stripDiacritics("" + term).toUpperCase()) >= 0, "matcher"),
      separator: ",",
      tokenSeparators: [],
      tokenizer: defaultTokenizer,
      escapeMarkup: defaultEscapeMarkup,
      blurOnChange: false,
      selectOnBlur: false,
      adaptContainerCssClass: /* @__PURE__ */ __name(function(c2) {
        return c2;
      }, "adaptContainerCssClass"),
      adaptDropdownCssClass: /* @__PURE__ */ __name(function(c2) {
        return null;
      }, "adaptDropdownCssClass"),
      nextSearchTerm: /* @__PURE__ */ __name(function(selectedObject, currentSearchTerm) {
        return void 0;
      }, "nextSearchTerm"),
      searchInputPlaceholder: "",
      createSearchChoicePosition: "top",
      shouldFocusInput: /* @__PURE__ */ __name(function(instance) {
        var supportsTouchEvents = "ontouchstart" in window || "msMaxTouchPoints" in navigator;
        if (!supportsTouchEvents) {
          return true;
        }
        if (instance.opts.minimumResultsForSearch < 0) {
          return false;
        }
        return true;
      }, "shouldFocusInput")
    };
  }
  static stripDiacritics(str) {
    function match(a2) {
      return DIACRITICS[a2] || a2;
    }
    __name(match, "match");
    return str.replace(/[^\u0000-\u007E]/g, match);
  }
};
function ajax(options) {
  var timeout, quietMillis = options.quietMillis || 100, ajaxUrl = options.url, self2 = this;
  return function(query) {
    clearTimeout(timeout);
    timeout = window.setTimeout(function() {
      var data = options.data, url = ajaxUrl, deprecated = {
        cache: options.cache || false
      }, params = Object.assign({}, Select2.ajaxDefaults.params, deprecated);
      data = data ? data.call(self2, query.term, query.page, query.context) : null;
      url = typeof url === "function" ? url.call(self2, query.term, query.page, query.context) : url;
      if (options.params) {
        if (typeof options.params === "function") {
          Object.assign(params, options.params.call(self2));
        } else {
          Object.assign(params, options.params);
        }
      }
      Object.assign(params, {
        url,
        data,
        onSuccess: /* @__PURE__ */ __name(function(response) {
          var results = options.results(response, query.page, query);
          query.callback(results);
        }, "onSuccess"),
        onError: /* @__PURE__ */ __name(function(response, info) {
          var results = {
            errorInfo: info,
            hasError: true,
            results: response
          };
          query.callback(results);
        }, "onError")
      });
      serviceCall(params);
    }, quietMillis);
  };
}
__name(ajax, "ajax");
function local(options) {
  var data = options, dataText, tmp, text2 = /* @__PURE__ */ __name(function(item) {
    return "" + item.text;
  }, "text");
  if (Array.isArray(data)) {
    tmp = data;
    data = { results: tmp };
  }
  if (typeof data !== "function") {
    tmp = data;
    data = /* @__PURE__ */ __name(function() {
      return tmp;
    }, "data");
  }
  var dataItem = data();
  if (dataItem.text) {
    text2 = dataItem.text;
    if (typeof text2 !== "function") {
      dataText = dataItem.text;
      text2 = /* @__PURE__ */ __name(function(item) {
        return item[dataText];
      }, "text");
    }
  }
  return function(query) {
    var t2 = query.term, filtered = { results: [] }, process;
    if (t2 === "") {
      query.callback(data());
      return;
    }
    process = /* @__PURE__ */ __name(function(datum, collection) {
      var group, attr;
      datum = datum[0];
      if (datum.children) {
        group = {};
        for (attr in datum) {
          if (datum.hasOwnProperty(attr)) group[attr] = datum[attr];
        }
        group.children = [];
        datum.children.forEach(function(childDatum) {
          process(childDatum, group.children);
        });
        if (group.children.length || query.matcher(t2, text2(group), datum)) {
          collection.push(group);
        }
      } else {
        if (query.matcher(t2, text2(datum), datum)) {
          collection.push(datum);
        }
      }
    }, "process");
    data().results.forEach(function(datum) {
      process(datum, filtered.results);
    });
    query.callback(filtered);
  };
}
__name(local, "local");
function tags(data) {
  var isFunc = typeof data === "function";
  return function(query) {
    var t2 = query.term, filtered = { results: [] };
    var result = isFunc ? data(query) : data;
    if (Array.isArray(result)) {
      result.forEach(function(item) {
        var isObject2 = item.text !== void 0, text2 = isObject2 ? item.text : item;
        if (t2 === "" || query.matcher(t2, text2)) {
          filtered.results.push(isObject2 ? item : { id: item, text: item });
        }
      });
      query.callback(filtered);
    }
  };
}
__name(tags, "tags");
function checkFormatter(formatter, formatterName) {
  if (typeof formatter === "function") return true;
  if (!formatter) return false;
  if (typeof formatter === "string") return true;
  throw new Error(formatterName + " must be a string, function, or falsy value");
}
__name(checkFormatter, "checkFormatter");
function evaluate(val, context, ..._2) {
  if (typeof val === "function") {
    var args = Array.prototype.slice.call(arguments, 2);
    return val.apply(context, args);
  }
  return val;
}
__name(evaluate, "evaluate");
function countResults(results) {
  var count2 = 0;
  results.forEach(function(item) {
    if (item.children) {
      count2 += countResults(item.children);
    } else {
      count2++;
    }
  });
  return count2;
}
__name(countResults, "countResults");
function defaultTokenizer(input, selection, selectCallback, opts) {
  var original = input, dupe = false, token, index, i2, l2, separator;
  if (!opts.createSearchChoice || !opts.tokenSeparators || opts.tokenSeparators.length < 1) return void 0;
  while (true) {
    index = -1;
    for (i2 = 0, l2 = opts.tokenSeparators.length; i2 < l2; i2++) {
      separator = opts.tokenSeparators[i2];
      index = input.indexOf(separator);
      if (index >= 0) break;
    }
    if (index < 0) break;
    token = input.substring(0, index);
    input = input.substring(index + separator.length);
    if (token.length > 0) {
      token = opts.createSearchChoice.call(this, token, selection);
      if (token !== void 0 && token !== null && opts.id(token) !== void 0 && opts.id(token) !== null) {
        dupe = false;
        for (i2 = 0, l2 = selection.length; i2 < l2; i2++) {
          if (equal(opts.id(token), opts.id(selection[i2]))) {
            dupe = true;
            break;
          }
        }
        if (!dupe) selectCallback(token);
      }
    }
  }
  if (original !== input) return input;
}
__name(defaultTokenizer, "defaultTokenizer");
function cleanupElements() {
  var self2 = this;
  Array.from(arguments).forEach(function(element) {
    Fluent.remove(self2[element]);
    self2[element] = null;
  });
}
__name(cleanupElements, "cleanupElements");
var resultsSelector = ".select2-results";
var AbstractSelect2 = class {
  static {
    __name(this, "AbstractSelect2");
  }
  init(opts) {
    var results, search;
    this.opts = opts = this.prepareOpts(opts);
    this.id = opts.id;
    if (opts.element.select2 != null) {
      opts.element.select2.destroy?.();
    }
    this.container = this.createContainer();
    this.containerId = "s2id_" + (opts.element.getAttribute("id") || "autogen" + nextUid());
    this.containerEventName = this.containerId.replace(/\\/g, "\\\\").replace(/([.])/g, "_").replace(/([;&,\-\.\+\*\~':"\!\^#$%@\[\]\(\)=>\|])/g, "\\$1");
    this.container.setAttribute("id", this.containerId);
    this.container.setAttribute("title", opts.element.getAttribute("title") ?? "");
    syncCssClasses(this.container, this.opts.element, this.opts.adaptContainerCssClass);
    const computedStyle = getComputedStyle(opts.element);
    const safeProperties = [
      "width",
      "height",
      "min-width",
      "min-height",
      "max-width",
      "max-height",
      "margin",
      "margin-top",
      "margin-right",
      "margin-bottom",
      "margin-left",
      "padding",
      "padding-top",
      "padding-right",
      "padding-bottom",
      "padding-left",
      "border",
      "border-width",
      "border-style",
      "border-color",
      "border-top",
      "border-right",
      "border-bottom",
      "border-left",
      "border-top-width",
      "border-right-width",
      "border-bottom-width",
      "border-left-width",
      "border-top-style",
      "border-right-style",
      "border-bottom-style",
      "border-left-style",
      "border-top-color",
      "border-right-color",
      "border-bottom-color",
      "border-left-color",
      "box-sizing",
      "display",
      "position",
      "float",
      "clear"
    ];
    copyStyle(this.container.style, this.opts.element.style);
    Object.assign(this.container.style, evaluate(opts.containerCss, this.opts.element));
    Fluent.addClass(this.container, evaluate(opts.containerCssClass, this.opts.element));
    this.elementTabIndex = this.opts.element.getAttribute("tabindex");
    this.opts.element.select2 = this;
    this.opts.element.setAttribute("tabindex", "-1");
    Fluent(this.container).insertBefore(this.opts.element);
    Fluent.on(this.opts.element, "click.select2", killEvent);
    this.container.select2 = this;
    this.dropdown = this.container.querySelector(".select2-drop");
    syncCssClasses(this.dropdown, this.opts.element, this.opts.adaptDropdownCssClass);
    Fluent.addClass(this.dropdown, evaluate(opts.dropdownCssClass, this.opts.element));
    this.dropdown.select2 = this;
    Fluent.on(this.dropdown, "click", killEvent);
    this.results = results = this.container.querySelector(resultsSelector);
    this.search = search = this.container.querySelector("input.select2-input");
    this.queryCount = 0;
    this.resultsPage = 0;
    this.context = null;
    this.initContainer();
    Fluent.on(this.container, "click", killEvent);
    installFilteredMouseMove(this.results);
    const boundThis = bindThis(this);
    Fluent.on(this.dropdown, "mousemove-filtered", resultsSelector, boundThis.highlightUnderEvent);
    ["touchstart", "touchmove", "touchend"].forEach((ev) => this.dropdown.addEventListener(ev, boundThis.handleDropdownTouchStartMoveEnd, { passive: true }));
    this.dropdown.addEventListener("touchmove", boundThis.handleDropdownTouchMove, { passive: true });
    ["touchstart", "touchend"].forEach((ev) => this.dropdown.addEventListener(ev, boundThis.handleDropdownTouchStartEnd), { passive: true });
    Fluent.on(this.dropdown, "click", boundThis.handleDropdownClick);
    installDebouncedScroll(80, this.results);
    Fluent.on(this.dropdown, "scroll-debounced", resultsSelector, boundThis.loadMoreIfNeeded);
    Fluent.on(this.container, "change", ".select2-input", function(e2) {
      e2.stopPropagation();
    });
    Fluent.on(this.dropdown, "change", ".select2-input", function(e2) {
      e2.stopPropagation();
    });
    installKeyUpChangeEvent(search);
    ["keyup-change", "input", "paste"].forEach((ev) => Fluent.on(search, ev, boundThis.handleSearchInput));
    Fluent.on(search, "focus", boundThis.handleSearchFocus);
    Fluent.on(search, "blur", boundThis.handleSearchBlur);
    Fluent.on(this.dropdown, "mouseup", resultsSelector, boundThis.handleDropdownMouseUp);
    ["click mouseup mousedown touchstart touchend focusin"].forEach((ev) => Fluent.on(this.dropdown, ev, handleDropdownTrap));
    this.nextSearchTerm = void 0;
    if (typeof this.opts.initSelection === "function") {
      this.initSelection();
      this.monitorSource();
    }
    if (opts.maximumInputLength !== null) {
      this.search.setAttribute("maxlength", "" + opts.maximumInputLength);
    }
    var disabled = opts.element.disabled;
    if (disabled === void 0) disabled = false;
    this.enable(!disabled);
    var readonly = opts.element.readOnly;
    if (readonly === void 0) readonly = false;
    this.readonly(readonly);
    scrollBarDimensions = scrollBarDimensions || measureScrollbar();
    this.autofocus = opts.element.autofocus;
    opts.element.autofocus = false;
    if (this.autofocus) this.focus();
    this.search.setAttribute("placeholder", opts.searchInputPlaceholder);
  }
  destroy() {
    var element = this.opts.element, select2 = element?.select2, self2 = this;
    this.close();
    if (element && element.detachEvent) {
      element.detachEvent("onpropertychange", this.handleMonitorSync);
    }
    if (this.propertyObserver) {
      this.propertyObserver.disconnect();
      this.propertyObserver = null;
    }
    if (select2 !== void 0) {
      select2.container?.remove();
      select2.dropdown?.remove();
      if (element) {
        element.classList.remove("select2-offscreen");
        delete element.select2;
        Fluent.off(element, ".select2");
        element.autofocus = this.autofocus || false;
      }
      if (this.elementTabIndex) {
        element.setAttribute("tabindex", this.elementTabIndex);
      } else {
        element.removeAttribute("tabindex");
      }
      delete element.style.display;
    }
    if (this.search) {
      ["keyup-change", "input", "paste"].forEach((ev) => Fluent.off(this.search, ev, this.handleSearchInput));
      Fluent.off(this.search, "focus", this.handleSearchFocus);
      Fluent.off(this.search, "blur", this.handleSearchBlur);
    }
    if (this.dropdown) {
      Fluent.off(this.dropdown, "mousemove-filtered", this.highlightUnderEvent);
      ["click mouseup mousedown touchstart touchend focusin"].forEach((ev) => Fluent.off(this.dropdown, ev, handleDropdownTrap));
      Fluent.off(this.dropdown, "mouseup", resultsSelector, this.handleDropdownMouseUp);
    }
    cleanupElements.call(
      this,
      "container",
      "dropdown",
      "results",
      "search"
    );
  }
  optionToData(element) {
    if (element instanceof HTMLOptionElement) {
      return {
        id: element.value,
        text: element.textContent,
        element,
        css: element.getAttribute("class"),
        disabled: element.disabled,
        locked: equal(element.getAttribute("locked"), "locked") || equal(element.getAttribute("locked"), true)
      };
    } else if (element instanceof HTMLOptGroupElement) {
      return {
        text: element.getAttribute("label"),
        children: [],
        element,
        css: element.getAttribute("class")
      };
    }
  }
  handleDropdownMouseUp(e2) {
    if (e2.target.closest(".select2-result-selectable")) {
      this.highlightUnderEvent(e2);
      this.selectHighlighted(e2);
    }
  }
  handleDropdownTouchStartMoveEnd(e2) {
    if (e2.target.closest?.(resultsSelector)) {
      this._touchEvent = true;
    }
  }
  handleDropdownTouchMove(e2) {
    e2.target.closest?.(resultsSelector) && this.touchMoved();
  }
  handleDropdownTouchStartEnd(e2) {
    e2.target.closest?.(resultsSelector) && this.clearTouchMoved();
  }
  handleDropdownClick(e2) {
    if (this._touchEvent) {
      this._touchEvent = false;
      this.selectHighlighted();
    }
  }
  prepareOpts(opts) {
    var element, select, idKey, ajaxUrl, self2 = this;
    element = opts.element;
    if (element.tagName.toLowerCase() === "select") {
      this.select = select = opts.element;
    }
    if (select) {
      ["id", "multiple", "ajax", "query", "createSearchChoice", "initSelection", "data", "tags"].forEach(function(opt) {
        if (opt in opts && opts[opt] != null) {
          throw new Error("Option '" + this + "' is not allowed for Select2 when attached to a <select> element.");
        }
      });
    }
    opts = Object.assign({}, {
      populateResults: /* @__PURE__ */ __name(function(container, results, query) {
        var id = this.opts.id;
        let populate = /* @__PURE__ */ __name(function(results2, container2, depth) {
          var i2, l2, result, selectable, disabled, compound, node, label, innerContainer, formatted;
          results2 = opts.sortResults(results2, container2, query);
          var nodes = [];
          for (i2 = 0, l2 = results2.length; i2 < l2; i2 = i2 + 1) {
            result = results2[i2];
            disabled = result.disabled === true;
            selectable = !disabled && id(result) !== void 0;
            compound = result.children && result.children.length > 0;
            node = document.createElement("li");
            node.classList.add("select2-results-dept-" + depth);
            node.classList.add("select2-result");
            node.classList.add(selectable ? "select2-result-selectable" : "select2-result-unselectable");
            if (disabled) {
              node.classList.add("select2-disabled");
            }
            if (compound) {
              node.classList.add("select2-result-with-children");
            }
            Fluent.addClass(node, self2.opts.formatResultCssClass(result));
            node.setAttribute("role", "presentation");
            label = document.createElement("div");
            label.classList.add("select2-result-label");
            label.setAttribute("id", "select2-result-label-" + nextUid());
            label.setAttribute("role", "option");
            formatted = opts.formatResult(result, label, query, self2.opts.escapeMarkup);
            if (formatted !== void 0) {
              if (formatted instanceof Node) {
                Fluent.empty(label);
                label.appendChild(formatted);
              } else
                label.innerHTML = formatted ?? "";
              node.append(label);
            }
            if (compound) {
              innerContainer = document.createElement("ul");
              innerContainer.classList.add("select2-result-sub");
              populate(result.children, innerContainer, depth + 1);
              node.append(innerContainer);
            }
            node.select2data = result;
            nodes.push(node);
          }
          container2.append(...nodes);
        }, "populate");
        populate(results, container, 0);
      }, "populateResults")
    }, Select2.defaults, opts);
    if (typeof opts.id !== "function") {
      idKey = opts.id;
      opts.id = function(e2) {
        return e2[idKey];
      };
    }
    if (opts.element.dataset.select2Tags) {
      if ("tags" in opts) {
        throw "tags specified as both an attribute 'data-select2-tags' and in options of Select2 " + opts.element.getAttribute("id");
      }
      opts.tags = JSON.parse(opts.element.dataset.select2Tags);
    }
    if (select) {
      opts.query = (query) => {
        var data = { results: [], more: false }, term = query.term, children;
        let process = /* @__PURE__ */ __name(function(element2, collection) {
          var group;
          if (element2 instanceof HTMLOptionElement) {
            if (query.matcher(term, element2.textContent, element2)) {
              collection.push(self2.optionToData(element2));
            }
          } else if (element2 instanceof HTMLOptGroupElement) {
            group = self2.optionToData(element2);
            Array.from(element2.children).forEach(function(elm) {
              process(elm, group.children);
            });
            if (group.children.length > 0) {
              collection.push(group);
            }
          }
        }, "process");
        children = Array.from(element.children);
        if (this.getPlaceholder() !== void 0 && children.length > 0) {
          var placeholderOption = this.getPlaceholderOption();
          if (placeholderOption) {
            children = children.filter((x) => x !== placeholderOption);
          }
        }
        children.forEach((elm) => process(elm, data.results));
        query.callback(data);
      };
      opts.id = function(e2) {
        return e2.id;
      };
    } else {
      if (!("query" in opts)) {
        if ("ajax" in opts) {
          ajaxUrl = opts.element.dataset.ajaxUrl;
          if (ajaxUrl && ajaxUrl.length > 0) {
            opts.ajax.url = ajaxUrl;
          }
          opts.query = ajax.call(opts.element, opts.ajax);
        } else if ("data" in opts) {
          opts.query = local(opts.data);
        } else if ("tags" in opts) {
          opts.query = tags(opts.tags);
          if (opts.createSearchChoice === void 0) {
            opts.createSearchChoice = function(term) {
              return { id: term?.trim(), text: term?.trim() };
            };
          }
          if (opts.initSelection === void 0) {
            opts.initSelection = function(element2, callback) {
              var data = [];
              splitVal(element2.value, opts.separator).forEach(function(id) {
                var obj = { id, text: id }, tags2 = opts.tags;
                if (typeof tags2 === "function") tags2 = tags2();
                tags2.forEach(function(tag) {
                  if (equal(tag.id, obj.id)) {
                    obj = tag;
                    return false;
                  }
                });
                data.push(obj);
              });
              callback(data);
            };
          }
        }
      }
    }
    if (typeof opts.query !== "function") {
      throw "query function not defined for Select2 " + opts.element.getAttribute("id");
    }
    if (opts.createSearchChoicePosition === "top") {
      opts.createSearchChoicePosition = function(list, item) {
        list.unshift(item);
      };
    } else if (opts.createSearchChoicePosition === "bottom") {
      opts.createSearchChoicePosition = function(list, item) {
        list.push(item);
      };
    } else if (typeof opts.createSearchChoicePosition !== "function") {
      throw "invalid createSearchChoicePosition option must be 'top', 'bottom' or a custom function";
    }
    return opts;
  }
  handleMonitorFocus(e2) {
    this.focus();
  }
  handleMonitorChange(e2) {
    if (this.opts.element.dataset.select2ChangeTriggered !== "true") {
      this.initSelection();
    }
  }
  handleMonitorSync() {
    const el = this.opts.element;
    var disabled = el.disabled;
    if (disabled === void 0) disabled = false;
    this.enable(!disabled);
    var readonly = el.readOnly;
    if (readonly === void 0) readonly = false;
    this.readonly(readonly);
    syncCssClasses(this.container, this.opts.element, this.opts.adaptContainerCssClass);
    Fluent.addClass(this.container, evaluate(this.opts.containerCssClass, this.opts.element));
    syncCssClasses(this.dropdown, this.opts.element, this.opts.adaptDropdownCssClass);
    Fluent.addClass(this.dropdown, evaluate(this.opts.dropdownCssClass, this.opts.element));
  }
  /**
   * Monitor the original element for changes and update select2 accordingly
   */
  monitorSource() {
    var el = this.opts.element, observer;
    const boundThis = bindThis(this);
    Fluent.on(el, "change.select2", boundThis.handleMonitorChange);
    Fluent.on(el, "focus.select2", boundThis.handleMonitorFocus);
    if (el && el.attachEvent) {
      el.attachEvent("onpropertychange", boundThis.handleMonitorSync);
    }
    observer = window.MutationObserver;
    if (observer !== void 0) {
      if (this.propertyObserver) {
        delete this.propertyObserver;
        this.propertyObserver = null;
      }
      this.propertyObserver = new observer(function(mutations) {
        mutations.forEach(boundThis.handleMonitorSync);
      });
      this.propertyObserver.observe(el, { attributes: true, subtree: false });
    }
  }
  triggerSelect(data) {
    var evt = { val: this.id(data), object: data, choice: data };
    var event = Fluent.trigger(this.opts.element, "select2-selecting", evt);
    return !Fluent.isDefaultPrevented(event);
  }
  /**
   * Triggers the change event on the source element
   */
  triggerChange(details) {
    details = details || {};
    details = Object.assign({}, details, { val: this.val() });
    this.opts.element.dataset.select2ChangeTriggered = "true";
    Fluent.trigger(this.opts.element, "change", details);
    delete this.opts.element.dataset.select2ChangeTriggered;
    this.opts.element.click();
    if (this.opts.blurOnChange)
      this.opts.element.blur();
  }
  isInterfaceEnabled() {
    return this.enabledInterface === true;
  }
  enableInterface() {
    var enabled = this._enabled && !this._readonly, disabled = !enabled;
    if (enabled === this.enabledInterface) return false;
    this.container.classList.toggle("select2-container-disabled", disabled);
    this.close();
    this.enabledInterface = enabled;
    return true;
  }
  enable(enabled) {
    if (enabled === void 0) enabled = true;
    if (this._enabled === enabled) return;
    this._enabled = enabled;
    this.opts.element.disabled = !enabled;
    this.enableInterface();
  }
  disable() {
    this.enable(false);
  }
  readonly(enabled) {
    if (enabled === void 0) enabled = false;
    if (this._readonly === enabled) return;
    this._readonly = enabled;
    this.opts.element.readOnly = enabled;
    this.enableInterface();
  }
  opened() {
    return this.container ? this.container.classList.contains("select2-dropdown-open") : false;
  }
  positionDropdown() {
    var dropdown = this.dropdown, offset = getOffset(this.container), height = this.container.offsetHeight, width = this.container.offsetWidth, dropHeight = dropdown.offsetHeight, windowWidth = window.innerWidth, windowHeight = window.innerHeight, viewPortRight = window.scrollX + windowWidth, viewportBottom = window.scrollY + windowHeight, dropTop = offset.top + height, dropLeft = offset.left, enoughRoomBelow = dropTop + dropHeight <= viewportBottom, enoughRoomAbove = offset.top - dropHeight >= window.scrollY, dropWidth = dropdown.offsetWidth, enoughRoomOnRight = dropLeft + dropWidth <= viewPortRight, aboveNow = dropdown.classList.contains("select2-drop-above"), bodyOffset, above, changeDirection, resultsListNode;
    if (aboveNow) {
      above = true;
      if (!enoughRoomAbove && enoughRoomBelow) {
        changeDirection = true;
        above = false;
      }
    } else {
      above = false;
      if (!enoughRoomBelow && enoughRoomAbove) {
        changeDirection = true;
        above = true;
      }
    }
    if (changeDirection) {
      Fluent.toggleClass(dropdown, "select2-display-none", true);
      offset = getOffset(this.container);
      height = this.container.offsetHeight;
      width = this.container.offsetWidth;
      viewPortRight = window.scrollX + windowWidth;
      viewportBottom = window.scrollY + windowHeight;
      dropTop = offset.top + height;
      dropLeft = offset.left;
      enoughRoomOnRight = dropLeft + dropWidth <= viewPortRight;
      Fluent.toggleClass(dropdown, "select2-display-none", false);
      this.focusSearch();
    }
    if (this.opts.dropdownAutoWidth) {
      resultsListNode = dropdown.querySelector(".select2-results");
      dropdown.classList.add("select2-drop-auto-width");
      dropdown.style.width = "";
      dropWidth = dropdown.offsetWidth + (resultsListNode.scrollHeight === resultsListNode.clientHeight ? 0 : scrollBarDimensions.width);
      dropWidth > width ? width = dropWidth : dropWidth = width;
      dropHeight = dropdown.offsetHeight;
      enoughRoomOnRight = dropLeft + dropWidth <= viewPortRight;
    } else {
      this.container.classList.remove("select2-drop-auto-width");
    }
    if (getComputedStyle(document.body).position !== "static") {
      bodyOffset = getOffset(document.body);
      dropTop -= bodyOffset.top;
      dropLeft -= bodyOffset.left;
    }
    if (!enoughRoomOnRight) {
      dropLeft = offset.left + this.container.offsetWidth - dropWidth;
    }
    let css = {
      left: dropLeft + "px",
      width: width + "px"
    };
    if (above) {
      css.top = offset.top - dropHeight + "px";
      css.bottom = "auto";
      this.container.classList.add("select2-drop-above");
      dropdown.classList.add("select2-drop-above");
    } else {
      css.top = dropTop + "px";
      css.bottom = "auto";
      this.container.classList.remove("select2-drop-above");
      dropdown.classList.remove("select2-drop-above");
    }
    css = Object.assign(css, evaluate(this.opts.dropdownCss, this.opts.element));
    Object.assign(dropdown.style, css);
  }
  shouldOpen() {
    if (this.opened()) return false;
    if (this._enabled === false || this._readonly === true) return false;
    var event = Fluent.trigger(this.opts.element, "select2-opening");
    return !Fluent.isDefaultPrevented(event);
  }
  clearDropdownAlignmentPreference() {
    this.container.classList.remove("select2-drop-above");
    this.dropdown.classList.remove("select2-drop-above");
  }
  /**
   * Opens the dropdown
   *
   * @return {Boolean} whether or not dropdown was opened. This method will return false if, for example,
   * the dropdown is already open, or if the 'open' event listener on the element called preventDefault().
   */
  open() {
    if (!this.shouldOpen()) return false;
    this.opening();
    Fluent.on(document, "mousemove.select2Event", function(e2) {
      lastMousePosition.x = e2.pageX;
      lastMousePosition.y = e2.pageY;
    });
    return true;
  }
  /**
   * Performs the opening of the dropdown
   */
  opening() {
    var cid = this.containerEventName, scroll = "scroll." + cid, resize = "resize." + cid, orient = "orientationchange." + cid, mask;
    this.container.classList.add("select2-dropdown-open", "select2-container-active");
    this.clearDropdownAlignmentPreference();
    var dropdownParent = (typeof this.opts.dropdownParent === "function" ? this.opts.dropdownParent(this.opts.element) : null) ?? document?.body;
    if (dropdownParent && this.dropdown !== dropdownParent.lastElementChild) {
      dropdownParent.appendChild(this.dropdown);
    }
    mask = document.getElementById("select2-drop-mask");
    if (!mask) {
      mask = document.createElement("div");
      mask.setAttribute("id", "select2-drop-mask");
      mask.setAttribute("class", "select2-drop-mask");
      mask.hidden = true;
      document.body.appendChild(mask);
      ["mousedown", "touchstart", "click"].forEach((ev) => mask.addEventListener(ev, function(e2) {
        reinsertElement(mask);
        var dropdown = document.getElementById("select2-drop"), self2;
        if (dropdown) {
          self2 = dropdown.select2;
          if (self2.opts.selectOnBlur) {
            self2.selectHighlighted({ noFocus: true });
          }
          self2.close();
          e2.type !== "touchstart" && e2.preventDefault();
          e2.stopPropagation();
        }
      }, { passive: ev === "touchstart" }));
    }
    if (this.dropdown.previousElementSibling != mask) {
      Fluent(mask).insertBefore(this.dropdown);
    }
    document.getElementById("select2-drop")?.removeAttribute("id");
    this.dropdown.setAttribute("id", "select2-drop");
    Fluent.toggle(mask, true);
    this.positionDropdown();
    Fluent.toggleClass(this.dropdown, "select2-display-none", false);
    this.positionDropdown();
    this.dropdown.classList.add("select2-drop-active");
    var that = this;
    var parent2 = this.container.parentElement;
    var parents = [];
    while (parent2) {
      parents.push(parent2);
      parent2 = parent2.parentElement;
    }
    parents.push(window);
    parents.forEach((parent3) => {
      [resize, scroll, orient].forEach((ev) => {
        Fluent.on(parent3, ev, function() {
          if (that.opened()) that.positionDropdown();
        });
      });
    });
  }
  close() {
    if (!this.opened()) return;
    var cid = this.containerEventName, scroll = "scroll." + cid, resize = "resize." + cid, orient = "orientationchange." + cid;
    var parent2 = this.container.parentElement;
    var parents = [];
    while (parent2) {
      parents.push(parent2);
      parent2 = parent2.parentElement;
    }
    parents.push(window);
    parents.forEach((parent3) => {
      Fluent.off(parent3, scroll);
      Fluent.off(parent3, resize);
      Fluent.off(parent3, orient);
    });
    this.clearDropdownAlignmentPreference();
    Fluent(document.getElementById("select2-drop-mask")).hide();
    this.dropdown.removeAttribute("id");
    Fluent.toggleClass(this.dropdown, "select2-display-none", true);
    this.container.classList.remove("select2-dropdown-open", "select2-container-active");
    Fluent.empty(this.results);
    Fluent.off(document, "mousemove.select2Event");
    this.clearSearch();
    this.search.classList.remove("select2-active");
    this.search.parentElement?.classList.remove("select2-active");
    Fluent.trigger(this.opts.element, "select2-close");
  }
  /**
   * Opens control, sets input value, and updates results.
   */
  externalSearch(term) {
    this.open();
    this.search.value = term;
    this.updateResults(false);
  }
  clearSearch() {
  }
  getMaximumSelectionSize() {
    return evaluate(this.opts.maximumSelectionSize, this.opts.element);
  }
  ensureHighlightVisible() {
    var results = this.results, index, child, hb, rb, y2, more, topOffset;
    index = this.highlight();
    if (index < 0) return;
    if (index == 0) {
      results.scrollTop = 0;
      return;
    }
    var children = this.findHighlightableChoices().map((x) => x.querySelector(".select2-result-label"));
    child = children[index];
    if (!child)
      return;
    topOffset = (getOffset(child) || {}).top || 0;
    hb = topOffset + getOuterHeightWithMargins(child);
    if (index === children.length - 1) {
      more = results.querySelector("li.select2-more-results");
      if (more) {
        hb = getOffset(more).top + getOuterHeightWithMargins(more);
      }
    }
    rb = getOffset(results).top + getOuterHeightWithMargins(results);
    if (hb > rb) {
      results.scrollTop = results.scrollTop + (hb - rb);
    }
    y2 = topOffset - getOffset(results).top;
    if (y2 < 0 && getComputedStyle(child).display != "none") {
      results.scrollTop = results.scrollTop + y2;
    }
  }
  findHighlightableChoices() {
    return Array.from(this.results.querySelectorAll(".select2-result-selectable:not(.select2-disabled):not(.select2-selected)"));
  }
  moveHighlight(delta) {
    var choices = this.findHighlightableChoices(), index = this.highlight();
    while (index > -1 && index < choices.length) {
      index += delta;
      var choice = choices[index];
      if (choice && choice.classList.contains("select2-result-selectable") && !choice.classList.contains("select2-disabled") && !choice.classList.contains("select2-selected")) {
        this.highlight(index);
        break;
      }
    }
  }
  highlight(index) {
    var choices = this.findHighlightableChoices(), choice, data;
    if (arguments.length === 0) {
      return choices.findIndex((x) => x.classList.contains("select2-highlighted"));
    }
    if (index >= choices.length) index = choices.length - 1;
    if (index < 0) index = 0;
    this.removeHighlight();
    choice = choices[index];
    choice?.classList.add("select2-highlighted");
    this.search.setAttribute("aria-activedescendant", choice?.querySelector(".select2-result-label")?.getAttribute("id"));
    this.ensureHighlightVisible();
    data = choice?.select2data;
    if (data) {
      Fluent.trigger(this.opts.element, "select2-highlight", { val: this.id(data), choice: data });
    }
  }
  removeHighlight() {
    this.results.querySelectorAll(".select2-highlighted").forEach((x) => x.classList.remove("select2-highlighted"));
  }
  touchMoved() {
    this._touchMoved = true;
  }
  clearTouchMoved() {
    this._touchMoved = false;
  }
  countSelectableResults() {
    return this.findHighlightableChoices().length;
  }
  highlightUnderEvent(event) {
    var el = event.target?.closest?.(".select2-result-selectable");
    if (el && !el.classList.contains("select2-highlighted")) {
      var choices = this.findHighlightableChoices();
      this.highlight(choices.indexOf(el));
    } else if (!el) {
      this.removeHighlight();
    }
  }
  loadMoreIfNeeded() {
    var results = this.results, more = results.querySelector("li.select2-more-results"), below, page = this.resultsPage + 1, self2 = this, term = this.search.value, context = this.context;
    if (!more) return;
    below = getOffset(more).top - getOffset(results).top - results.getBoundingClientRect().height;
    if (below <= this.opts.loadMorePadding) {
      more.classList.add("select2-active");
      this.opts.query({
        element: this.opts.element,
        term,
        page,
        context,
        matcher: this.opts.matcher,
        callback: /* @__PURE__ */ __name((data) => {
          if (!self2.opened()) return;
          this.dropdown?.classList.add("select2-position-fixed");
          try {
            self2.opts.populateResults.call(this, results, data.results, { term, page, context });
            self2.postprocessResults(data, false, false);
            if (data.more === true) {
              results.appendChild(more);
              var loadMore = evaluate(self2.opts.formatLoadMore, self2.opts.element, page + 1);
              Fluent.empty(more);
              if (loadMore instanceof Node)
                more.appendChild(loadMore);
              else
                more.textContent = loadMore ?? "";
              window.setTimeout(function() {
                self2.loadMoreIfNeeded();
              }, 10);
            } else {
              more.remove();
            }
            self2.positionDropdown();
          } finally {
            this.dropdown?.classList.remove("select2-position-fixed");
          }
          self2.resultsPage = page;
          self2.context = data.context;
          Fluent.trigger(this.opts.element, "select2-loaded", { items: data });
        }, "callback")
      });
    }
  }
  /**
   * Default tokenizer function which does nothing
   */
  tokenize() {
    return void 0;
  }
  handleSearchInput() {
    this.updateResults();
  }
  handleSearchFocus() {
    this.search?.classList.add("select2-focused");
  }
  handleSearchBlur() {
    this.search?.classList.remove("select2-focused");
  }
  /**
   * @param initial whether or not this is the call to this method right after the dropdown has been opened
   */
  updateResults(initial) {
    var search = this.search, results = this.results, opts = this.opts, data, self2 = this, input, term = search.value, lastTerm = this.container.dataset.select2LastTerm, queryNumber;
    if (initial !== true && lastTerm && equal(term, lastTerm)) return;
    this.container.dataset.select2LastTerm = term;
    if (initial !== true && (this.showSearchInput === false || !this.opened())) {
      return;
    }
    function postRender() {
      search.classList.remove("select2-active");
      search.parentElement?.classList.remove("select2-active");
      self2.positionDropdown();
    }
    __name(postRender, "postRender");
    function createLi(klass, html) {
      if (klass == null && html === void 0)
        return;
      var li = document.createElement("li");
      li.classList.add(klass);
      if (html instanceof Node)
        li.appendChild(html);
      else
        li.innerHTML = html ?? "";
      return li;
    }
    __name(createLi, "createLi");
    function render(klass, html) {
      self2.dropdown?.classList.add("select2-position-fixed");
      try {
        Fluent.empty(results);
        var li = createLi(klass, html);
        if (li != null)
          results.appendChild(li);
        postRender();
      } finally {
        self2.dropdown?.classList.remove("select2-position-fixed");
      }
    }
    __name(render, "render");
    queryNumber = ++this.queryCount;
    var maxSelSize = this.getMaximumSelectionSize();
    if (maxSelSize >= 1) {
      data = this.data();
      if (Array.isArray(data) && data.length >= maxSelSize && checkFormatter(opts.formatSelectionTooBig, "formatSelectionTooBig")) {
        render("select2-selection-limit", evaluate(opts.formatSelectionTooBig, opts.element, maxSelSize));
        return;
      }
    }
    if (search.value?.length < opts.minimumInputLength) {
      if (checkFormatter(opts.formatInputTooShort, "formatInputTooShort")) {
        render("select2-no-results", evaluate(opts.formatInputTooShort, opts.element, search.value, opts.minimumInputLength));
      } else {
        render("");
      }
      if (initial && this.showSearch) this.showSearch(true);
      return;
    }
    if (opts.maximumInputLength && search.value?.length > opts.maximumInputLength) {
      if (checkFormatter(opts.formatInputTooLong, "formatInputTooLong")) {
        render("select2-no-results", evaluate(opts.formatInputTooLong, opts.element, search.value, opts.maximumInputLength));
      } else {
        render("");
      }
      return;
    }
    if (opts.formatSearching && this.findHighlightableChoices().length === 0) {
      render("select2-searching", evaluate(opts.formatSearching, opts.element) + "</li>");
    }
    search.classList.add("select2-active");
    search.parentElement?.classList.add("select2-active");
    this.removeHighlight();
    input = this.tokenize();
    if (input != void 0 && input != null) {
      search.value = input ?? "";
    }
    this.resultsPage = 1;
    opts.query({
      element: opts.element,
      term: search.value,
      page: this.resultsPage,
      context: null,
      matcher: opts.matcher,
      callback: /* @__PURE__ */ __name((data2) => {
        var def;
        if (queryNumber != this.queryCount) {
          return;
        }
        if (!this.opened()) {
          this.search.classList.remove("select2-active");
          return;
        }
        if (data2.hasError !== void 0 && checkFormatter(opts.formatAjaxError, "formatAjaxError")) {
          render("select2-ajax-error", evaluate(opts.formatAjaxError, opts.element, data2));
          return;
        }
        this.context = data2.context === void 0 ? null : data2.context;
        if (this.opts.createSearchChoice && search.value !== "") {
          def = this.opts.createSearchChoice.call(self2, search.value, data2.results);
          if (def !== void 0 && def !== null && self2.id(def) !== void 0 && self2.id(def) !== null) {
            if (data2.results.filter(
              function() {
                return equal(self2.id(this), self2.id(def));
              }
            ).length === 0) {
              this.opts.createSearchChoicePosition(data2.results, def);
            }
          }
        }
        if (data2.results.length === 0 && checkFormatter(opts.formatNoMatches, "formatNoMatches")) {
          render("select2-no-results", evaluate(opts.formatNoMatches, opts.element, search.value));
          return;
        }
        this.dropdown?.classList.add("select2-position-fixed");
        try {
          Fluent.empty(results);
          self2.opts.populateResults.call(this, results, data2.results, { term: search.value, page: this.resultsPage, context: null });
          if (data2.more === true && checkFormatter(opts.formatLoadMore, "formatLoadMore")) {
            results.appendChild(createLi("select2-more-results", evaluate(opts.formatLoadMore, opts.element, this.resultsPage)));
            window.setTimeout(function() {
              self2.loadMoreIfNeeded();
            }, 10);
          }
          this.postprocessResults(data2, initial);
          postRender();
        } finally {
          this.dropdown?.classList.remove("select2-position-fixed");
        }
        Fluent.trigger(this.opts.element, "select2-loaded", { items: data2 });
      }, "callback")
    });
  }
  cancel(e2) {
    this.close();
  }
  blur() {
    if (this.opts.selectOnBlur)
      this.selectHighlighted({ noFocus: true });
    this.close();
    this.container.classList.remove("select2-container-active");
    if (this.search === document.activeElement) {
      this.search.blur();
    }
    this.clearSearch();
    this.selection.querySelectorAll(".select2-search-choice-focus").forEach((x) => x.classList.remove("select2-search-choice-focus"));
  }
  focusSearch() {
    focus(this.search);
  }
  selectHighlighted(options) {
    if (this._touchMoved) {
      this.clearTouchMoved();
      return;
    }
    var index = this.highlight(), highlighted = this.results.querySelector(".select2-highlighted"), data = highlighted?.closest(".select2-result")?.select2data;
    if (data) {
      this.highlight(index);
      this.onSelect(data, options);
    } else if (options && options.noFocus) {
      this.close();
    }
  }
  getPlaceholder() {
    var placeholderOption;
    return this.opts.element.getAttribute("placeholder") || this.opts.element.dataset.placeholder || this.opts.placeholder || ((placeholderOption = this.getPlaceholderOption()) !== void 0 ? placeholderOption?.textContent : void 0);
  }
  getPlaceholderOption() {
    if (this.select) {
      var firstOption = this.select.querySelector(":scope > option");
      if (this.opts.placeholderOption !== void 0) {
        return this.opts.placeholderOption === "first" && firstOption || typeof this.opts.placeholderOption === "function" && this.opts.placeholderOption(this.select);
      } else if (firstOption.textContent?.trim() === "" && firstOption.value === "") {
        return firstOption;
      }
    }
  }
  /**
   * Get the desired width for the container element.  This is
   * derived first from option `width` passed to select2, then
   * the inline 'style' on the original element, and finally
   * falls back to the jQuery calculated element width.
   */
  initContainerWidth() {
    function resolveContainerWidth() {
      var style, attrs, matches, i2, l2, attr;
      if (this.opts.width === "off") {
        return null;
      } else if (this.opts.width === "element") {
        return this.opts.element.offsetWidth === 0 ? "auto" : this.opts.element.offsetWidth + "px";
      } else if (this.opts.width === "copy" || this.opts.width === "resolve") {
        style = this.opts.element.getAttribute("style");
        if (style) {
          attrs = style.split(";");
          for (i2 = 0, l2 = attrs.length; i2 < l2; i2 = i2 + 1) {
            attr = attrs[i2].replace(/\s/g, "");
            matches = attr.match(/^width:(([-+]?([0-9]*\.)?[0-9]+)(px|em|ex|%|in|cm|mm|pt|pc))/i);
            if (matches !== null && matches.length >= 1)
              return matches[1];
          }
        }
        if (this.opts.width === "resolve") {
          style = getComputedStyle(this.opts.element).width;
          if (style.indexOf("%") > 0) return style;
          return this.opts.element.offsetWidth === 0 ? "auto" : this.opts.element.offsetWidth + "px";
        }
        return null;
      } else if (typeof this.opts.width === "function") {
        return this.opts.width();
      } else {
        return this.opts.width;
      }
    }
    __name(resolveContainerWidth, "resolveContainerWidth");
    ;
    var width = resolveContainerWidth.call(this);
    if (width !== null) {
      this.container.style.width = width;
    }
  }
};
var SingleSelect2 = class extends AbstractSelect2 {
  static {
    __name(this, "SingleSelect2");
  }
  createContainer() {
    var container = document.createElement("div");
    container.classList.add("select2-container");
    container.innerHTML = "<a href='javascript:void(0)' class='select2-choice' tabindex='-1'><span class='select2-chosen'>&#160;</span><abbr class='select2-search-choice-close'></abbr><span class='select2-arrow' role='presentation'><b role='presentation'></b></span></a><label for='' class='select2-offscreen'></label><input class='select2-focusser select2-offscreen' type='text' aria-haspopup='true' role='button' /><div class='select2-drop select2-display-none'><div class='select2-search'><label for='' class='select2-offscreen'></label><input type='text' autocomplete='off' autocorrect='off' autocapitalize='off' spellcheck='false' class='select2-input' role='combobox' aria-expanded='true'aria-autocomplete='list' /></div><ul class='select2-results' role='listbox'></ul></div>";
    return container;
  }
  enableInterface() {
    var result = super.enableInterface();
    if (result) {
      this.focusser.disabled = !this.isInterfaceEnabled();
    }
    return result;
  }
  opening() {
    var el, range, len;
    if (this.opts.minimumResultsForSearch >= 0) {
      this.showSearch(true);
    }
    super.opening();
    if (this.showSearchInput !== false) {
      this.search.value = this.focusser.value;
    }
    if (this.opts.shouldFocusInput(this)) {
      this.search.focus();
      el = this.search;
      if (el.createTextRange) {
        range = el.createTextRange();
        range.collapse(false);
        range.select();
      } else if (el.setSelectionRange) {
        len = this.search.value.length;
        el.setSelectionRange(len, len);
      }
    }
    if (this.search.value === "") {
      if (this.nextSearchTerm != void 0) {
        this.search.value = this.nextSearchTerm ?? "";
        this.search.select();
      }
    }
    this.focusser.disabled = true;
    this.focusser.value = "";
    this.updateResults(true);
    Fluent.trigger(this.opts.element, "select2-open");
  }
  close() {
    if (!this.opened()) return;
    super.close();
    this.focusser.disabled = false;
    if (this.opts.shouldFocusInput(this)) {
      this.focusser.focus();
    }
  }
  focus() {
    if (this.opened()) {
      this.close();
    } else {
      this.focusser.disabled = false;
      if (this.opts.shouldFocusInput(this)) {
        this.focusser.focus();
      }
    }
  }
  isFocused() {
    return this.container.classList.contains("select2-container-active");
  }
  cancel(e2) {
    super.cancel(e2);
    this.focusser.disabled = false;
    if (this.opts.shouldFocusInput(this)) {
      this.focusser.focus();
    }
  }
  destroy() {
    document.querySelector("label[for='" + this.focusser.getAttribute("id") + "']")?.setAttribute(
      "for",
      this.opts.element.getAttribute("id")
    );
    super.destroy();
    cleanupElements.call(
      this,
      "selection",
      "focusser"
    );
  }
  initContainer() {
    var selection, container = this.container, dropdown = this.dropdown, idSuffix = nextUid(), elementLabel;
    if (this.opts.minimumResultsForSearch < 0) {
      this.showSearch(false);
    } else {
      this.showSearch(true);
    }
    this.selection = selection = container.querySelector(".select2-choice");
    this.focusser = container.querySelector(".select2-focusser");
    selection.querySelector(".select2-chosen")?.setAttribute("id", "select2-chosen-" + idSuffix);
    this.focusser.setAttribute("aria-labelledby", "select2-chosen-" + idSuffix);
    this.results.setAttribute("id", "select2-results-" + idSuffix);
    this.search.setAttribute("aria-owns", "select2-results-" + idSuffix);
    this.focusser.setAttribute("id", "s2id_autogen" + idSuffix);
    elementLabel = document.querySelector("label[for='" + this.opts.element.getAttribute("id") + "']");
    this.focusser.previousElementSibling.textContent = elementLabel?.textContent ?? "";
    this.focusser.previousElementSibling.setAttribute("for", this.focusser.getAttribute("id"));
    var originalTitle = this.opts.element.getAttribute("title");
    this.opts.element.setAttribute("title", originalTitle || (elementLabel?.textContent ?? ""));
    this.focusser.setAttribute("tabindex", this.elementTabIndex);
    this.search.setAttribute("id", this.focusser.getAttribute("id") + "_search");
    this.search.previousElementSibling.textContent = document.querySelector("label[for='" + this.focusser.getAttribute("id") + "']")?.textContent ?? "";
    this.search.previousElementSibling.setAttribute("for", this.search.getAttribute("id"));
    Fluent.on(this.search, "keydown", (e2) => {
      if (!this.isInterfaceEnabled()) return;
      if (229 == e2.keyCode) return;
      if (e2.which === KEY.PAGE_UP || e2.which === KEY.PAGE_DOWN) {
        killEvent(e2);
        return;
      }
      switch (e2.which) {
        case KEY.UP:
        case KEY.DOWN:
          this.moveHighlight(e2.which === KEY.UP ? -1 : 1);
          killEvent(e2);
          return;
        case KEY.ENTER:
          this.selectHighlighted();
          killEvent(e2);
          return;
        case KEY.TAB:
          this.selectHighlighted({ noFocus: true });
          return;
        case KEY.ESC:
          this.cancel(e2);
          killEvent(e2);
          return;
      }
    });
    Fluent.on(this.search, "blur", (e2) => {
      if (document.activeElement === document.body) {
        window.setTimeout(() => {
          if (this.opened()) {
            this.search.focus();
          }
        }, 0);
      }
    });
    Fluent.on(this.focusser, "keydown", (e2) => {
      if (!this.isInterfaceEnabled()) return;
      if (e2.which === KEY.TAB || KEY.isControl(e2) || KEY.isFunctionKey(e2) || e2.which === KEY.ESC) {
        return;
      }
      if (this.opts.openOnEnter === false && e2.which === KEY.ENTER) {
        killEvent(e2);
        return;
      }
      if (e2.which == KEY.DOWN || e2.which == KEY.UP || e2.which == KEY.ENTER && this.opts.openOnEnter) {
        if (e2.altKey || e2.ctrlKey || e2.shiftKey || e2.metaKey) return;
        this.open();
        killEvent(e2);
        return;
      }
      if (e2.which == KEY.DELETE || e2.which == KEY.BACKSPACE) {
        if (this.opts.allowClear) {
          this.clear();
        }
        killEvent(e2);
        return;
      }
    });
    installKeyUpChangeEvent(this.focusser);
    ["keyup-change", "input"].forEach((ev) => Fluent.on(this.focusser, ev, (e2) => {
      if (this.opts.minimumResultsForSearch >= 0) {
        e2.stopPropagation();
        if (this.opened()) return;
        this.open();
      }
    }));
    Fluent.on(selection, "click", "abbr", (e2) => {
      if (!this.isInterfaceEnabled()) return;
      this.clear();
      killEventImmediately(e2);
      this.close();
      this.selection.focus();
    });
    Fluent.on(selection, "dragstart", (e2) => {
      e2.preventDefault();
      return false;
    });
    Fluent.on(selection, "click", (e2) => {
      if (!this.container.classList.contains("select2-container-active")) {
        Fluent.trigger(this.opts.element, "select2-focus");
      }
      if (this.opened()) {
        this.close();
      } else if (this.isInterfaceEnabled()) {
        this.open();
      }
      killEvent(e2);
    });
    ["mousedown", "touchstart"].forEach((ev) => dropdown.addEventListener(ev, () => {
      if (this.opts.shouldFocusInput(this)) {
        this.search.focus();
      }
    }, { passive: true }));
    Fluent.on(selection, "focus", (e2) => {
      killEvent(e2);
    });
    Fluent.on(this.focusser, "focus", () => {
      if (!this.container.classList.contains("select2-container-active")) {
        Fluent.trigger(this.opts.element, "select2-focus");
      }
      this.container.classList.add("select2-container-active");
    });
    Fluent.on(this.focusser, "blur", () => {
      if (!this.opened()) {
        this.container.classList.remove("select2-container-active");
        Fluent.trigger(this.opts.element, "select2-blur");
      }
    });
    Fluent.on(this.search, "focus", () => {
      if (!this.container.classList.contains("select2-container-active")) {
        Fluent.trigger(this.opts.element, "select2-focus");
      }
      this.container.classList.add("select2-container-active");
    });
    this.initContainerWidth();
    this.opts.element.classList.add("select2-offscreen");
    this.setPlaceholder();
  }
  clear(triggerChange) {
    var data = this.selection?.select2data;
    if (data) {
      var evt = Fluent.trigger(this.opts.element, "select2-clearing");
      if (Fluent.isDefaultPrevented(evt)) {
        return;
      }
      var placeholderOption = this.getPlaceholderOption();
      this.opts.element.value = placeholderOption ? placeholderOption.value : "";
      Fluent.empty(this.selection.querySelector(".select2-chosen"));
      delete this.selection.select2data;
      this.setPlaceholder();
      if (triggerChange !== false) {
        Fluent.trigger(this.opts.element, "select2-removed", { val: this.id(data), choice: data });
        this.triggerChange({ removed: data });
      }
    }
  }
  /**
   * Sets selection based on source element's value
   */
  initSelection() {
    if (this.isPlaceholderOptionSelected()) {
      this.updateSelection(null);
      this.close();
      this.setPlaceholder();
    } else {
      var self2 = this;
      this.opts.initSelection.call(null, this.opts.element, function(selected) {
        if (!self2.container)
          return;
        if (selected !== void 0 && selected !== null) {
          self2.updateSelection(selected);
          self2.close();
          self2.setPlaceholder();
          self2.nextSearchTerm = self2.opts.nextSearchTerm(selected, self2.search.value);
        }
      });
    }
  }
  isPlaceholderOptionSelected() {
    var placeholderOption;
    if (this.getPlaceholder() === void 0) return false;
    return (placeholderOption = this.getPlaceholderOption()) !== void 0 && placeholderOption.selected || this.opts.element.value === "" || this.opts.element.value === void 0 || this.opts.element.value === null;
  }
  prepareOpts(opts) {
    opts = super.prepareOpts(opts);
    var self2 = this;
    if (opts.element.tagName.toLowerCase() === "select") {
      opts.initSelection = function(element, callback) {
        var selected = element.querySelector("option:checked:not(:disabled)");
        callback(self2.optionToData(selected));
      };
    } else if ("data" in opts) {
      opts.initSelection = opts.initSelection || function(element, callback) {
        var id = element.value;
        var match = null;
        opts.query({
          matcher: /* @__PURE__ */ __name(function(term, text2, el) {
            var is_match = equal(id, opts.id(el));
            if (is_match) {
              match = el;
            }
            return is_match;
          }, "matcher"),
          callback: typeof callback !== "function" ? () => {
          } : function() {
            callback(match);
          }
        });
      };
    }
    return opts;
  }
  getPlaceholder() {
    if (this.select) {
      if (this.getPlaceholderOption() === void 0) {
        return void 0;
      }
    }
    return super.getPlaceholder();
  }
  setPlaceholder() {
    var placeholder = this.getPlaceholder();
    if (this.isPlaceholderOptionSelected() && placeholder !== void 0) {
      if (this.select && this.getPlaceholderOption() === void 0) return;
      var chosen = this.selection.querySelector(".select2-chosen");
      chosen && (chosen.textContent = placeholder);
      this.selection.classList.add("select2-default");
      this.container.classList.remove("select2-allowclear");
    }
  }
  postprocessResults(data, initial, noHighlightUpdate) {
    var selected = 0, self2 = this;
    this.findHighlightableChoices().forEach((elm, i2) => {
      if (equal(self2.id(elm.select2data), self2.opts.element.value)) {
        selected = i2;
        return false;
      }
    });
    if (noHighlightUpdate !== false) {
      if (initial === true && selected >= 0) {
        this.highlight(selected);
      } else {
        this.highlight(0);
      }
    }
    if (initial === true) {
      var min = this.opts.minimumResultsForSearch;
      if (min >= 0) {
        this.showSearch(countResults(data.results) >= min);
      }
    }
  }
  showSearch(showSearchInput) {
    if (this.showSearchInput === showSearchInput) return;
    this.showSearchInput = showSearchInput;
    this.dropdown.querySelector(".select2-search")?.classList.toggle("select2-search-hidden", !showSearchInput);
    this.dropdown.querySelector(".select2-search")?.classList.toggle("select2-offscreen", !showSearchInput);
    this.dropdown.classList.toggle("select2-with-searchbox", showSearchInput);
    this.container.classList.toggle("select2-with-searchbox", showSearchInput);
  }
  onSelect(data, options) {
    if (!this.triggerSelect(data)) {
      return;
    }
    var old = this.opts.element.value, oldData = this.data();
    this.opts.element.value = this.id(data);
    this.updateSelection(data);
    Fluent.trigger(this.opts.element, "select2-selected", { val: this.id(data), choice: data });
    this.nextSearchTerm = this.opts.nextSearchTerm(data, this.search.value);
    this.close();
    if ((!options || !options.noFocus) && this.opts.shouldFocusInput(this)) {
      this.focusser.focus();
    }
    if (!equal(old, this.id(data))) {
      this.triggerChange({ added: data, removed: oldData });
    }
  }
  updateSelection(data) {
    if (!this.selection)
      return;
    var container = this.selection.querySelector(".select2-chosen"), formatted, cssClass;
    this.selection.select2data = data;
    Fluent.empty(container);
    if (data !== null) {
      formatted = this.opts.formatSelection(data, container, this.opts.escapeMarkup);
    }
    if (formatted !== void 0) {
      if (formatted instanceof Node)
        container.appendChild(formatted);
      else
        container.innerHTML = formatted;
    }
    cssClass = this.opts.formatSelectionCssClass(data, container);
    if (cssClass !== void 0) {
      container?.classList.add(cssClass);
    }
    this.selection.classList.remove("select2-default");
    if (this.opts.allowClear && this.getPlaceholder() !== void 0) {
      this.container?.classList.add("select2-allowclear");
    }
  }
  val(val, triggerChange) {
    var triggerChange = false, data = null, self2 = this, oldData = this.data();
    if (arguments.length === 0) {
      return this.opts.element.value;
    }
    if (arguments.length > 1) {
      triggerChange = arguments[1];
    }
    if (this.select) {
      this.select.value = val ?? "";
      var selected = this.select.querySelector("option:checked");
      if (selected)
        data = self2.optionToData(selected);
      this.updateSelection(data);
      this.setPlaceholder();
      if (triggerChange) {
        this.triggerChange({ added: data, removed: oldData });
      }
    } else {
      if (!val && val !== 0) {
        this.clear(triggerChange);
        return;
      }
      if (this.opts.initSelection === void 0) {
        throw new Error("cannot call val() if initSelection() is not defined");
      }
      this.opts.element.value = val;
      this.opts.initSelection(this.opts.element, function(data2) {
        self2.opts.element.value = !data2 ? "" : self2.id(data2);
        self2.updateSelection(data2);
        self2.setPlaceholder();
        if (triggerChange) {
          self2.triggerChange({ added: data2, removed: oldData });
        }
      });
    }
  }
  clearSearch() {
    this.search.value = "";
    this.focusser.value = "";
  }
  data(value) {
    var data, triggerChange = false;
    if (arguments.length === 0) {
      data = this.selection?.select2data;
      if (data == void 0) data = null;
      return data;
    } else {
      if (arguments.length > 1) {
        triggerChange = arguments[1];
      }
      if (!value) {
        this.clear(triggerChange);
      } else {
        data = this.data();
        this.opts.element.value = !value ? "" : this.id(value);
        this.updateSelection(value);
        if (triggerChange) {
          this.triggerChange({ added: value, removed: data });
        }
      }
    }
  }
};
var MultiSelect2 = class extends AbstractSelect2 {
  static {
    __name(this, "MultiSelect2");
  }
  createContainer() {
    var container = document.createElement("div");
    container.classList.add("select2-container", "select2-container-multi");
    container.innerHTML = "<ul class='select2-choices'><li class='select2-search-field'><label for='' class='select2-offscreen'></label><input type='text' autocomplete='off' autocorrect='off' autocapitalize='off' spellcheck='false' class='select2-input'></li></ul><div class='select2-drop select2-drop-multi select2-display-none'><ul class='select2-results'></ul></div>";
    return container;
  }
  prepareOpts(opts) {
    opts = super.prepareOpts(opts);
    var self2 = this;
    if (opts.element.tagName.toLowerCase() === "select") {
      opts.initSelection = function(element, callback) {
        var data = [];
        element.querySelectorAll("option:checked:not(:disabled)").forEach((elm) => {
          data.push(self2.optionToData(elm));
        });
        callback(data);
      };
    } else if ("data" in opts) {
      opts.initSelection = opts.initSelection || function(element, callback) {
        var ids = splitVal(element.value, opts.separator);
        var matches = [];
        opts.query({
          matcher: /* @__PURE__ */ __name(function(term, text2, el) {
            var is_match = ids.some((id) => equal(id, opts.id(el)));
            if (is_match) {
              matches.push(el);
            }
            return is_match;
          }, "matcher"),
          callback: typeof callback !== "function" ? () => {
          } : function() {
            var ordered = [];
            for (var i2 = 0; i2 < ids.length; i2++) {
              var id = ids[i2];
              for (var j = 0; j < matches.length; j++) {
                var match = matches[j];
                if (equal(id, opts.id(match))) {
                  ordered.push(match);
                  matches.splice(j, 1);
                  break;
                }
              }
            }
            callback(ordered);
          }
        });
      };
    }
    return opts;
  }
  selectChoice(choice) {
    var selected = this.container.querySelector(".select2-search-choice-focus");
    if (selected && choice && choice === selected) {
    } else {
      if (selected) {
        Fluent.trigger(this.opts.element, "choice-deselected", { choice: selected });
        selected.classList.remove("select2-search-choice-focus");
      }
      if (choice) {
        this.close();
        choice.classList.add("select2-search-choice-focus");
        Fluent.trigger(this.opts.element, "choice-selected", { choice });
      }
    }
  }
  destroy() {
    document.querySelector("label[for='" + this.search.getAttribute("id") + "']")?.setAttribute(
      "for",
      this.opts.element.getAttribute("id")
    );
    super.destroy();
    cleanupElements.call(
      this,
      "searchContainer",
      "selection"
    );
  }
  initContainer() {
    var selector = ".select2-choices";
    this.searchContainer = this.container.querySelector(".select2-search-field");
    var selection;
    this.selection = selection = this.container.querySelector(selector);
    var _this = this;
    Fluent.on(this.selection, "click", ".select2-search-choice:not(.select2-locked)", (e2) => {
      _this.search.focus();
      _this.selectChoice(e2.target.closest(".select2-search-choice"));
    });
    this.search.setAttribute("id", "s2id_autogen" + nextUid());
    this.search.previousElementSibling.textContent = document.querySelector("label[for='" + this.opts.element.getAttribute("id") + "']")?.textContent;
    this.search.previousElementSibling.setAttribute("for", this.search.getAttribute("id"));
    ["input", "paste"].forEach((ev) => Fluent.on(this.search, ev, () => {
      if (this.search.getAttribute("placeholder") && this.search.value.length === 0) return;
      if (!this.isInterfaceEnabled()) return;
      if (!this.opened()) {
        this.open();
      }
    }));
    this.search.setAttribute("tabindex", this.elementTabIndex);
    this.keydowns = 0;
    Fluent.on(this.search, "keydown", (e2) => {
      if (!this.isInterfaceEnabled()) return;
      ++this.keydowns;
      var selected = selection.querySelector(".select2-search-choice-focus");
      var prev = Fluent(selected).prevSibling(".select2-search-choice:not(.select2-locked)").getNode();
      var next = Fluent(selected).nextSibling(".select2-search-choice:not(.select2-locked)").getNode();
      var pos = getCursorInfo(this.search);
      if (selected && (e2.which == KEY.LEFT || e2.which == KEY.RIGHT || e2.which == KEY.BACKSPACE || e2.which == KEY.DELETE || e2.which == KEY.ENTER)) {
        var selectedChoice = selected;
        if (e2.which == KEY.LEFT && prev) {
          selectedChoice = prev;
        } else if (e2.which == KEY.RIGHT) {
          selectedChoice = next ? next : null;
        } else if (e2.which === KEY.BACKSPACE) {
          if (this.unselect(selected)) {
            this.search.style.width = "10px";
            selectedChoice = prev ? prev : next;
          }
        } else if (e2.which == KEY.DELETE) {
          if (this.unselect(selected)) {
            this.search.style.width = "10px";
            selectedChoice = next ? next : null;
          }
        } else if (e2.which == KEY.ENTER) {
          selectedChoice = null;
        }
        this.selectChoice(selectedChoice);
        killEvent(e2);
        if (!selectedChoice || !selectedChoice) {
          this.open();
        }
        return;
      } else if ((e2.which === KEY.BACKSPACE && this.keydowns == 1 || e2.which == KEY.LEFT) && (pos.offset == 0 && !pos.length)) {
        this.selectChoice(Array.from(selection.querySelectorAll(".select2-search-choice:not(.select2-locked)")).pop());
        killEvent(e2);
        return;
      } else {
        this.selectChoice(null);
      }
      if (this.opened()) {
        switch (e2.which) {
          case KEY.UP:
          case KEY.DOWN:
            this.moveHighlight(e2.which === KEY.UP ? -1 : 1);
            killEvent(e2);
            return;
          case KEY.ENTER:
            this.selectHighlighted();
            killEvent(e2);
            return;
          case KEY.TAB:
            this.selectHighlighted({ noFocus: true });
            this.close();
            return;
          case KEY.ESC:
            this.cancel(e2);
            killEvent(e2);
            return;
        }
      }
      if (e2.which === KEY.TAB || KEY.isControl(e2) || KEY.isFunctionKey(e2) || e2.which === KEY.BACKSPACE || e2.which === KEY.ESC) {
        return;
      }
      if (e2.which === KEY.ENTER) {
        if (this.opts.openOnEnter === false) {
          return;
        } else if (e2.altKey || e2.ctrlKey || e2.shiftKey || e2.metaKey) {
          return;
        }
      }
      this.open();
      if (e2.which === KEY.PAGE_UP || e2.which === KEY.PAGE_DOWN) {
        killEvent(e2);
      }
      if (e2.which === KEY.ENTER) {
        killEvent(e2);
      }
    });
    Fluent.on(this.search, "keyup", (e2) => {
      this.keydowns = 0;
      this.resizeSearch();
    });
    Fluent.on(this.search, "blur", (e2) => {
      this.container.classList.remove("select2-container-active");
      this.search.classList.remove("select2-focused");
      this.selectChoice(null);
      if (!this.opened()) this.clearSearch();
      e2.stopImmediatePropagation();
      Fluent.trigger(this.opts.element, "select2-blur");
    });
    Fluent.on(this.container, "click", selector, (e2) => {
      if (!this.isInterfaceEnabled()) return;
      if (e2.target?.closest(".select2-search-choice")) {
        return;
      }
      this.selectChoice(null);
      this.clearPlaceholder();
      if (!this.container.classList.contains("select2-container-active")) {
        Fluent.trigger(this.opts.element, "select2-focus");
      }
      this.open();
      this.focusSearch();
      e2.preventDefault();
    });
    Fluent.on(this.container, "focus", selector, () => {
      if (!this.isInterfaceEnabled()) return;
      if (!this.container.classList.contains("select2-container-active")) {
        Fluent.trigger(this.opts.element, "select2-focus");
      }
      this.container.classList.add("select2-container-active");
      this.dropdown.classList.add("select2-drop-active");
      this.clearPlaceholder();
    });
    this.initContainerWidth();
    this.opts.element.classList.add("select2-offscreen");
    this.clearSearch();
  }
  enableInterface() {
    var result = super.enableInterface();
    if (result) {
      this.search.disabled = !this.isInterfaceEnabled();
    }
    return result;
  }
  initSelection() {
    if (this.opts.element.value === "" && this.opts.element.textContent === "") {
      this.updateSelection([]);
      this.close();
      this.clearSearch();
    }
    if (this.select || this.opts.element.value !== "") {
      var self2 = this;
      this.opts.initSelection.call(null, this.opts.element, function(data) {
        if (!self2.container)
          return;
        if (data !== void 0 && data !== null) {
          self2.updateSelection(data);
          self2.close();
          self2.clearSearch();
        }
      });
    }
  }
  clearSearch() {
    var placeholder = this.getPlaceholder(), maxWidth = this.getMaxSearchWidth();
    if (placeholder !== void 0 && this.getVal().length === 0 && !this.search.classList.contains("select2-focused")) {
      this.search.value = placeholder ?? "";
      this.search.classList.add("select2-default");
      this.search.style.width = maxWidth > 0 ? maxWidth + "px" : getComputedStyle(this.container).width;
    } else {
      this.search.value = "";
      this.search.style.width = "10px";
    }
  }
  clearPlaceholder() {
    if (this.search.classList.contains("select2-default")) {
      this.search.value = "";
      this.search.classList.remove("select2-default");
    }
  }
  opening() {
    this.clearPlaceholder();
    this.resizeSearch();
    super.opening();
    this.focusSearch();
    if (this.search.value === "") {
      if (this.nextSearchTerm != void 0) {
        this.search.value = this.nextSearchTerm ?? "";
        this.search.select();
      }
    }
    this.updateResults(true);
    if (this.opts.shouldFocusInput(this)) {
      this.search.focus();
    }
    Fluent.trigger(this.opts.element, "select2-open");
  }
  close() {
    if (!this.opened()) return;
    super.close();
  }
  focus() {
    this.close();
    this.search.focus();
  }
  isFocused() {
    return this.search.classList.contains("select2-focused");
  }
  updateSelection(data) {
    var ids = [], filtered = [], self2 = this;
    data.forEach(function(x) {
      if (indexOf2(self2.id(x), ids) < 0) {
        ids.push(self2.id(x));
        filtered.push(x);
      }
    });
    data = filtered;
    this.selection.querySelectorAll(".select2-search-choice").forEach((el) => Fluent.remove(el));
    data.forEach(function(x) {
      self2.addSelectedChoice(x);
    });
    self2.postprocessResults();
  }
  tokenize() {
    var input = this.search.value;
    input = this.opts.tokenizer.call(this, input, this.data(), bindThis(this).onSelect, this.opts);
    if (input != null && input != void 0) {
      this.search.value = input ?? "";
      if (input.length > 0) {
        this.open();
      }
    }
    return void 0;
  }
  onSelect(data, options) {
    if (!this.triggerSelect(data) || data.text === "") {
      return;
    }
    this.addSelectedChoice(data);
    Fluent.trigger(this.opts.element, "selected", { val: this.id(data), choice: data });
    this.nextSearchTerm = this.opts.nextSearchTerm(data, this.search.value);
    this.clearSearch();
    this.updateResults();
    if (this.select || !this.opts.closeOnSelect) this.postprocessResults(void 0, false, this.opts.closeOnSelect === true);
    if (this.opts.closeOnSelect) {
      this.close();
      this.search.style.width = "10px";
    } else {
      if (this.countSelectableResults() > 0) {
        this.search.style.width = "10px";
        this.resizeSearch();
        if (this.getMaximumSelectionSize() > 0 && this.val().length >= this.getMaximumSelectionSize()) {
          this.updateResults(true);
        } else {
          if (this.nextSearchTerm != void 0) {
            this.search.value = this.nextSearchTerm ?? "";
            this.updateResults();
            this.search.select();
          }
        }
        this.positionDropdown();
      } else {
        this.close();
        this.search.style.width = "10px";
      }
    }
    this.triggerChange({ added: data });
    if (!options || !options.noFocus)
      this.focusSearch();
  }
  cancel(e2) {
    this.close();
    this.focusSearch();
  }
  addSelectedChoice(data) {
    var enableChoice = !data.locked;
    var choice = document.createElement("li"), id = this.id(data), val = this.getVal(), formatted, cssClass;
    choice.classList.add("select2-search-choice");
    choice.appendChild(document.createElement("div"));
    if (enableChoice) {
      var a2 = choice.appendChild(document.createElement("a"));
      a2.classList.add("select2-search-choice-close");
      a2.setAttribute("href", "#");
      a2.setAttribute("tabindex", "-1");
    } else {
      choice.classList.add("select2-locked");
    }
    var div = choice.querySelector("div");
    formatted = this.opts.formatSelection(data, div, this.opts.escapeMarkup);
    if (formatted != void 0) {
      if (formatted instanceof Node)
        div.appendChild(formatted);
      else
        div.innerHTML = formatted ?? "";
    }
    cssClass = this.opts.formatSelectionCssClass(data, div);
    if (cssClass != void 0) {
      Fluent.addClass(choice, cssClass);
    }
    if (enableChoice) {
      var close = choice.querySelector(".select2-search-choice-close");
      Fluent.on(close, "mousedown", killEvent);
      ["click", "dblclick"].forEach((ev) => Fluent.on(close, ev, (e2) => {
        if (!this.isInterfaceEnabled()) return;
        this.unselect(e2.target);
        this.selection.querySelector(".select2-search-choice-focus")?.classList.remove("select2-search-choice-focus");
        killEvent(e2);
        this.close();
        this.focusSearch();
      }));
      Fluent.on(close, "focus", () => {
        if (!this.isInterfaceEnabled()) return;
        this.container.classList.add("select2-container-active");
        this.dropdown.classList.add("select2-drop-active");
      });
    }
    choice.select2data = data;
    Fluent(choice).insertBefore(this.searchContainer);
    val.push(id);
    this.setVal(val);
  }
  unselect(selected) {
    var val = this.getVal(), data, index;
    selected = selected.closest(".select2-search-choice");
    if (!selected) {
      throw "Invalid argument: " + selected + ". Must be .select2-search-choice";
    }
    data = selected.select2data;
    if (!data) {
      return;
    }
    var evt = Fluent.trigger(this.opts.element, "select2-removing", { val: this.id(data), choice: data });
    if (Fluent.isDefaultPrevented(evt)) {
      return false;
    }
    while ((index = indexOf2(this.id(data), val)) >= 0) {
      val.splice(index, 1);
      this.setVal(val);
      if (this.select) this.postprocessResults();
    }
    selected.remove();
    Fluent.trigger(this.opts.element, "select2-removed", { val: this.id(data), choice: data });
    this.triggerChange({ removed: data });
    return true;
  }
  postprocessResults(data, initial, noHighlightUpdate) {
    var val = this.getVal(), choices = this.results.querySelectorAll(".select2-result"), compound = this.results.querySelectorAll(".select2-result-with-children"), self2 = this;
    choices.forEach(function(choice) {
      var id = self2.id(choice.select2data);
      if (indexOf2(id, val) >= 0) {
        choice.classList.add("select2-selected");
        choice.querySelectorAll(".select2-result-selectable").forEach((x) => x.classList.add("select2-selected"));
      }
    });
    compound.forEach(function(choice) {
      if (!choice.matches(".select2-result-selectable") && !choice.querySelector(".select2-result-selectable:not(.select2-selected)")) {
        choice.classList.add("select2-selected");
      }
    });
    if (this.highlight() == -1 && noHighlightUpdate !== false) {
      self2.highlight(0);
    }
    if (!this.opts.createSearchChoice && !Array.from(choices).some((x) => x.matches(".select2-result:not(.select2-selected)"))) {
      if (!data || data && !data.more && !this.results.querySelector(".select2-no-results")) {
        if (checkFormatter(self2.opts.formatNoMatches, "formatNoMatches")) {
          var noResults = evaluate(self2.opts.formatNoMatches, self2.opts.element, self2.search.value);
          var li = document.createElement("li");
          li.classList.add("select2-no-results");
          if (noResults instanceof Node)
            li.appendChild(noResults);
          else
            li.innerHTML = noResults ?? "";
        }
      }
    }
  }
  getMaxSearchWidth() {
    return this.selection.getBoundingClientRect().width - getSideBorderPadding(this.search);
  }
  resizeSearch() {
    var minimumWidth, left, maxWidth, containerLeft, searchWidth, sideBorderPadding = getSideBorderPadding(this.search);
    minimumWidth = measureTextWidth(this.search) + 10;
    left = getOffset(this.search).left;
    maxWidth = this.selection.getBoundingClientRect().width;
    containerLeft = getOffset(this.selection).left;
    searchWidth = maxWidth - (left - containerLeft) - sideBorderPadding;
    if (searchWidth < minimumWidth) {
      searchWidth = maxWidth - sideBorderPadding;
    }
    if (searchWidth < 40) {
      searchWidth = maxWidth - sideBorderPadding;
    }
    if (searchWidth <= 0) {
      searchWidth = minimumWidth;
    }
    this.search.style.width = Math.floor(searchWidth) + "px";
  }
  getVal() {
    var val;
    if (this.select) {
      val = Array.from(this.select.selectedOptions).map((x) => x.value);
      return val === null ? [] : val;
    } else {
      val = this.opts.element.value;
      return splitVal(val, this.opts.separator);
    }
  }
  setVal(val) {
    if (this.select) {
      val ??= [];
      var opt = this.select.options;
      for (var i2 = 0; i2 < opt.length; i2++) {
        var o2 = opt.item(i2);
        o2.selected = val.includes(o2.value);
      }
    } else {
      var unique = val || [];
      unique = unique.filter((x, i3) => unique.indexOf(x) === i3);
      this.opts.element.value = unique.length === 0 ? "" : unique.join(this.opts.separator);
    }
  }
  buildChangeDetails(old, current) {
    var current = current.slice(0), old = old.slice(0);
    for (var i2 = 0; i2 < current.length; i2++) {
      for (var j = 0; j < old.length; j++) {
        if (equal(this.opts.id(current[i2]), this.opts.id(old[j]))) {
          current.splice(i2, 1);
          if (i2 > 0) {
            i2--;
          }
          old.splice(j, 1);
          j--;
        }
      }
    }
    return { added: current, removed: old };
  }
  val(val, triggerChange) {
    var self2 = this;
    if (arguments.length === 0) {
      return this.getVal();
    }
    var oldData = this.data();
    if (!oldData) oldData = [];
    if (!val && val !== 0) {
      this.opts.element.value = "";
      this.updateSelection([]);
      this.clearSearch();
      if (triggerChange) {
        this.triggerChange({ added: this.data(), removed: oldData });
      }
      return;
    }
    this.setVal(val);
    if (this.select) {
      this.opts.initSelection(this.select, bindThis(this).updateSelection);
      if (triggerChange) {
        this.triggerChange(this.buildChangeDetails(oldData, this.data()));
      }
    } else {
      if (this.opts.initSelection === void 0) {
        throw new Error("val() cannot be called if initSelection() is not defined");
      }
      this.opts.initSelection(this.opts.element, function(data) {
        var ids = data.map(self2.id);
        self2.setVal(ids);
        self2.updateSelection(data);
        self2.clearSearch();
        if (triggerChange) {
          self2.triggerChange(self2.buildChangeDetails(oldData, self2.data()));
        }
      });
    }
    this.clearSearch();
  }
  onSortStart() {
    if (this.select) {
      throw new Error("Sorting of elements is not supported when attached to <select>. Attach to <input type='hidden'/> instead.");
    }
    this.search.style.width = "0px";
    this.searchContainer.hidden = true;
  }
  onSortEnd() {
    var val = [], self2 = this;
    this.searchContainer.hidden = false;
    Fluent(this.searchContainer.parentElement).append(this.searchContainer);
    this.resizeSearch();
    this.selection.querySelectorAll(".select2-search-choice").forEach((el) => {
      val.push(self2.opts.id(el.select2data));
    });
    this.setVal(val);
    this.triggerChange();
  }
  data(values, triggerChange) {
    var self2 = this, ids, old;
    if (arguments.length === 0) {
      var result = [];
      this.selection.querySelectorAll(":scope > .select2-search-choice").forEach((x) => result.push(x.select2data));
      return result;
    } else {
      old = this.data();
      if (!values) {
        values = [];
      }
      ids = values.map(self2.opts.id);
      this.setVal(ids);
      this.updateSelection(values);
      this.clearSearch();
      if (triggerChange) {
        this.triggerChange(this.buildChangeDetails(old, this.data()));
      }
    }
  }
};
function handleDropdownTrap(e2) {
  e2.stopPropagation();
}
__name(handleDropdownTrap, "handleDropdownTrap");
var safeStyleProperties = [
  "width",
  "height",
  "min-width",
  "min-height",
  "max-width",
  "max-height",
  "margin",
  "margin-top",
  "margin-right",
  "margin-bottom",
  "margin-left",
  "padding",
  "padding-top",
  "padding-right",
  "padding-bottom",
  "padding-left",
  "border",
  "border-width",
  "border-style",
  "border-color",
  "border-top",
  "border-right",
  "border-bottom",
  "border-left",
  "border-top-width",
  "border-right-width",
  "border-bottom-width",
  "border-left-width",
  "border-top-style",
  "border-right-style",
  "border-bottom-style",
  "border-left-style",
  "border-top-color",
  "border-right-color",
  "border-bottom-color",
  "border-left-color",
  "box-sizing",
  "display",
  "position",
  "float",
  "clear"
];
function copyStyle(from, to) {
  for (var prop of safeStyleProperties) {
    if (from.getPropertyValue(prop)) {
      to.setProperty(prop, from.getPropertyValue(prop));
    }
  }
}
__name(copyStyle, "copyStyle");

// src/ui/editors/combobox.ts
var Combobox = class _Combobox {
  static {
    __name(this, "Combobox");
  }
  static {
    this.defaults = {
      pageSize: 100,
      typeDelay: 200
    };
  }
  constructor(opt, create = true) {
    if (isArrayLike(opt?.element))
      this.el = opt.element[0];
    else if (typeof opt?.element !== "function")
      this.el = opt?.element;
    if (!create)
      return;
    opt = Object.assign({}, _Combobox.defaults, opt);
    this.createSelect2(opt);
  }
  createSelect2(opt) {
    var select2Opt = {
      element: this.el,
      multiple: opt.multiple,
      placeholder: opt.placeholder || null,
      allowClear: opt.allowClear,
      createSearchChoicePosition: "bottom"
    };
    select2Opt.query = (query) => {
      var pageSize = opt.pageSize;
      var searchQuery = {
        searchTerm: query.term?.trim() || null,
        skip: (query.page - 1) * pageSize,
        take: pageSize,
        checkMore: true
      };
      this.abortPendingQuery();
      var select2 = Select2.getInstance(this.el);
      function setActive(value) {
        select2?.search?.classList.toggle("select2-active", value);
        select2?.search?.parentElement?.classList.toggle("select2-active", value);
      }
      __name(setActive, "setActive");
      this.el.typeTimeoutFn = () => {
        this.abortPendingQuery();
        setActive(true);
        searchQuery.signal = (this.el.queryLoading = new AbortController()).signal;
        const cleanup = /* @__PURE__ */ __name(() => {
          delete this.el.queryLoading;
          setActive(false);
        }, "cleanup");
        try {
          const then = /* @__PURE__ */ __name((result) => {
            delete this.el.queryLoading;
            setActive(false);
            query.callback({
              results: result.items,
              more: result.more
            });
          }, "then");
          var searchResult = opt.search(searchQuery);
          if (isPromiseLike(searchResult)) {
            searchResult.then(then, cleanup);
          } else {
            cleanup();
            searchResult && then(searchResult);
          }
        } catch (e2) {
          cleanup();
          throw e2;
        }
      };
      this.el.typeTimeout = setTimeout(this.el.typeTimeoutFn, !query.term ? 0 : opt.typeDelay);
    };
    select2Opt.initSelection = (element, callback) => {
      var el = isArrayLike(element) ? element[0] : element;
      var val = el.value;
      if (val == null || val == "") {
        callback(null);
        return;
      }
      var isMultiple = opt.multiple;
      var idList = isMultiple ? val.split(",") : [val];
      var searchQuery = {
        idList,
        initSelection: true
      };
      const then = /* @__PURE__ */ __name((result) => {
        cleanup();
        if (isMultiple) {
          callback(result.items);
        } else if (!result.items || !result.items.length) {
          if (opt.arbitraryValues) {
            callback({
              id: val,
              text: val
            });
          } else
            callback(null);
        } else {
          var item = result.items[0];
          callback(item);
        }
      }, "then");
      const cleanup = /* @__PURE__ */ __name(() => {
        delete el.initSelectionLoading;
      }, "cleanup");
      el.initSelectionLoading && el.initSelectionLoading?.abort?.();
      searchQuery.signal = (el.initSelectionLoading = new AbortController()).signal;
      try {
        let searchResult = opt.search(searchQuery);
        if (isPromiseLike(searchResult)) {
          searchResult.then(then, cleanup);
        } else {
          cleanup();
          if (searchResult)
            then(searchResult);
        }
      } catch (e2) {
        cleanup();
        throw e2;
      }
    };
    if (opt.createSearchChoice)
      select2Opt.createSearchChoice = opt.createSearchChoice;
    if (opt.formatResult)
      select2Opt.formatResult = opt.formatResult;
    if (opt.formatSelection)
      select2Opt.formatSelection = opt.formatSelection;
    if (opt.providerOptions)
      select2Opt = Object.assign(select2Opt, opt.providerOptions("select2", opt));
    new Select2(select2Opt);
    Fluent.on(this.el, "execute-search", () => {
      if (!this.el || !this.el.typeTimeout || !this.el.typeTimeoutFn) {
        return;
      }
      this.el.typeTimeoutFn();
      delete this.el.typeTimeout;
      delete this.el.typeTimeoutFn;
    });
  }
  abortPendingQuery() {
    if (!this.el)
      return;
    this.el.queryLoading && this.el.queryLoading?.abort?.();
    this.el.queryLoading = false;
    if (this.el.typeTimeout) {
      clearTimeout(this.el.typeTimeout);
      delete this.el.typeTimeout;
      delete this.el.typeTimeoutFn;
    }
  }
  abortInitSelection() {
    if (!this.el)
      return;
    this.el.initSelection && this.el.initSelection?.abort?.();
    delete this.el.initSelection;
  }
  dispose() {
    if (!this.el)
      return;
    this.abortInitSelection();
    this.abortPendingQuery();
    Select2.getInstance(this.el)?.destroy();
    Fluent.off(this.el, "execute-search");
  }
  get container() {
    if (!this.el)
      return null;
    return Select2.getInstance(this.el)?.container;
  }
  get type() {
    if (!this.el)
      return null;
    if (Select2.getInstance(this.el))
      return "select2";
    return null;
  }
  get isMultiple() {
    if (!this.el)
      return false;
    var select2 = Select2.getInstance(this.el);
    if (select2)
      return select2.isMultiple;
    return this.el.getAttribute("multiple") != null;
  }
  getSelectedItem() {
    var select2 = Select2.getInstance(this.el);
    if (select2) {
      var item = select2.data;
      if (Array.isArray(item))
        return item[0];
      return item;
    }
  }
  getSelectedItems() {
    var select2 = Select2.getInstance(this.el);
    if (select2) {
      var item = select2.data;
      if (Array.isArray(item))
        return item;
      if (!item)
        return [];
      return [item];
    }
    return [];
  }
  getValue() {
    if (!this.el)
      return null;
    var select2 = Select2.getInstance(this.el);
    if (select2) {
      var val = select2.val;
      if (Array.isArray(val))
        return val.join(",");
      return val;
    }
    return this.el.value;
  }
  getValues() {
    if (!this.el)
      return [];
    let val;
    let select2 = Select2.getInstance(this.el);
    if (select2)
      val = select2.val;
    else
      val = this.el.value;
    if (Array.isArray(val))
      return val;
    if (val == null || val === "")
      return [];
    return [val];
  }
  setValue(value, triggerChange = false) {
    if (!this.el)
      return;
    if (value == this.getValue())
      return;
    var val = value;
    if (value && this.isMultiple) {
      val = value.split(String.fromCharCode(44)).map((x) => x?.trim() || null).filter((x1) => x1 != null);
    }
    this.el.dataset.comboboxsettingvalue = "true";
    try {
      let select2 = Select2.getInstance(this.el);
      if (select2) {
        select2.val = val;
      } else {
        this.el.value = val;
      }
      if (triggerChange)
        Fluent.trigger(this.el, "change");
    } finally {
      delete this.el.dataset.comboboxsettingvalue;
    }
  }
  setValues(value, triggerChange = false) {
    if (value == null || value.length === 0) {
      this.setValue(null, triggerChange);
      return;
    }
    this.setValue(value.join(","), triggerChange);
  }
  closeDropdown() {
    Select2.getInstance(this.el)?.close();
  }
  openDropdown() {
    Select2.getInstance(this.el)?.open();
  }
  static getInstance(el) {
    if (!el || !Select2.getInstance(isArrayLike(el) ? el[0] : el))
      return null;
    return new _Combobox({ element: el }, false);
  }
};
function stripDiacritics(str) {
  if (!str)
    return str;
  return Select2.stripDiacritics(str);
}
__name(stripDiacritics, "stripDiacritics");

// src/ui/editors/editorutils.tsx
var EditorUtils;
((EditorUtils2) => {
  function getDisplayText(editor) {
    var combobox = Combobox.getInstance(editor.domNode);
    if (combobox) {
      var data = combobox.getSelectedItems();
      if (!data)
        return "";
      return data.map((x) => x.text).join(", ");
    }
    var value = getValue(editor);
    if (value == null) {
      return "";
    }
    if (typeof value === "string")
      return value;
    if (value instanceof Boolean)
      return !!value ? FilterPanelTexts.asTry().OperatorNames["true"] ?? "True" : FilterPanelTexts.asTry().OperatorNames["false"] ?? "False";
    return value.toString();
  }
  EditorUtils2.getDisplayText = getDisplayText;
  __name(getDisplayText, "getDisplayText");
  var dummy = { name: "_" };
  function getValue(editor) {
    var target = {};
    saveValue(editor, dummy, target);
    return target["_"];
  }
  EditorUtils2.getValue = getValue;
  __name(getValue, "getValue");
  function saveValue(editor, item, target) {
    var getEditValue = safeCast(editor, IGetEditValue);
    if (getEditValue != null) {
      getEditValue.getEditValue(item, target);
      return;
    }
    var stringValue = safeCast(editor, IStringValue);
    if (stringValue != null) {
      target[item.name] = stringValue.get_value();
      return;
    }
    var booleanValue = safeCast(editor, IBooleanValue);
    if (booleanValue != null) {
      target[item.name] = booleanValue.get_value();
      return;
    }
    var doubleValue = safeCast(editor, IDoubleValue);
    if (doubleValue != null) {
      var value = doubleValue.get_value();
      target[item.name] = isNaN(value) ? null : value;
      return;
    }
    if (editor.getEditValue != null) {
      editor.getEditValue(item, target);
      return;
    }
    if (Fluent.isInputLike(editor.domNode)) {
      target[item.name] = editor.domNode.value;
      return;
    }
  }
  EditorUtils2.saveValue = saveValue;
  __name(saveValue, "saveValue");
  function setValue(editor, value) {
    var source = { _: value };
    loadValue(editor, dummy, source);
  }
  EditorUtils2.setValue = setValue;
  __name(setValue, "setValue");
  function loadValue(editor, item, source) {
    var setEditValue = safeCast(editor, ISetEditValue);
    if (setEditValue != null) {
      setEditValue.setEditValue(source, item);
      return;
    }
    var stringValue = safeCast(editor, IStringValue);
    if (stringValue != null) {
      var value = source[item.name];
      if (value != null) {
        value = value.toString();
      }
      stringValue.set_value(cast(value, String));
      return;
    }
    var booleanValue = safeCast(editor, IBooleanValue);
    if (booleanValue != null) {
      var value1 = source[item.name];
      if (typeof value1 === "number") {
        booleanValue.set_value(value1 > 0);
      } else {
        booleanValue.set_value(!!value1);
      }
      return;
    }
    var doubleValue = safeCast(editor, IDoubleValue);
    if (doubleValue != null) {
      var d2 = source[item.name];
      if (!!(d2 == null || isInstanceOfType(d2, String) && isTrimmedEmpty(cast(d2, String)))) {
        doubleValue.set_value(null);
      } else if (isInstanceOfType(d2, String)) {
        doubleValue.set_value(cast(parseDecimal(cast(d2, String)), Number));
      } else if (isInstanceOfType(d2, Boolean)) {
        doubleValue.set_value(!!d2 ? 1 : 0);
      } else {
        doubleValue.set_value(cast(d2, Number));
      }
      return;
    }
    if (editor.setEditValue != null) {
      editor.setEditValue(source, item);
      return;
    }
    if (Fluent.isInputLike(editor.domNode)) {
      var v2 = source[item.name];
      editor.domNode.value = v2 ?? "";
      return;
    }
  }
  EditorUtils2.loadValue = loadValue;
  __name(loadValue, "loadValue");
  function setReadonly(elements, value) {
    elements = isArrayLike(elements) ? elements : [elements];
    for (var i2 = 0; i2 < elements.length; i2++) {
      let el = elements[i2];
      if (el == null)
        continue;
      if (!(el instanceof Node)) {
        if (typeof el.set_readOnly === "function") {
          el.set_readOnly(!!value);
          continue;
        }
        el = el.domNode;
        if (!el)
          continue;
      } else {
        const widget = tryGetWidget(el);
        if (widget != null && typeof widget.set_readOnly === "function") {
          widget.set_readOnly(!!value);
          continue;
        }
      }
      setElementReadOnly(el, value);
    }
  }
  EditorUtils2.setReadonly = setReadonly;
  __name(setReadonly, "setReadonly");
  EditorUtils2.setReadOnly = setReadonly;
  function setRequired(widget, isRequired) {
    var req = safeCast(widget, IValidateRequired);
    if (req != null) {
      req.set_required(isRequired);
    } else if (Fluent.isInputLike(widget.domNode)) {
      widget.domNode.classList.toggle("required", !!isRequired);
    }
    var gridField = widget.domNode.closest(".field");
    var hasSupItem = gridField?.querySelector("sup");
    if (isRequired && !hasSupItem && gridField) {
      Fluent(jsx("sup", { title: PropertyGridTexts.RequiredHint, children: "*" })).prependTo(gridField.querySelector(".caption"));
    } else if (!isRequired && hasSupItem) {
      Fluent(hasSupItem).remove();
    }
  }
  EditorUtils2.setRequired = setRequired;
  __name(setRequired, "setRequired");
  function setContainerReadOnly(container, readOnly) {
    container = isArrayLike(container) ? container[0] : container;
    if (!readOnly) {
      if (!container.classList.contains("readonly-container"))
        return;
      container.classList.remove("readonly-container");
      container.querySelectorAll(".editor.container-readonly").forEach((el) => {
        el.classList.remove("container-readonly");
        EditorUtils2.setReadOnly(el, false);
      });
      return;
    }
    container.classList.add("readonly-container");
    container.querySelectorAll(".editor:not(.container-readonly)").forEach((el) => {
      var w2 = tryGetWidget(el);
      if (w2 != null) {
        if (w2["get_readOnly"]) {
          if (w2["get_readOnly"]())
            return;
        } else if (el.matches("[readonly]") || el.matches("[disabled]") || el.matches(".readonly") || el.matches(".disabled"))
          return;
        el.classList.add("container-readonly");
        EditorUtils2.setReadOnly(w2, true);
      } else {
        if (el.matches("[readonly]") || el.matches("[disabled]") || el.matches(".readonly") || el.matches(".disabled"))
          return;
        el.classList.add("container-readonly");
        setElementReadOnly(el, true);
      }
    });
  }
  EditorUtils2.setContainerReadOnly = setContainerReadOnly;
  __name(setContainerReadOnly, "setContainerReadOnly");
})(EditorUtils || (EditorUtils = {}));

// src/ui/widgets/widgetinternal.ts
var isFragmentWorkaround = /* @__PURE__ */ Symbol();
function ensureParentOrFragment(node) {
  if (!node || node.parentNode)
    return node;
  let fragment = document.createDocumentFragment();
  fragment.appendChild(node);
  fragment[isFragmentWorkaround] = true;
  return node;
}
__name(ensureParentOrFragment, "ensureParentOrFragment");
function handleElementProp(type, props) {
  let elementProp = props?.element;
  let domNode;
  if (typeof elementProp == "string") {
    domNode = document.querySelector(elementProp);
    if (domNode == null)
      throw `The element ${elementProp} specified for the ${getTypeFullName(type)} is not found in the DOM!`;
  } else {
    if (isArrayLike(elementProp)) {
      elementProp = elementProp[0];
    }
    if (elementProp instanceof HTMLElement) {
      domNode = elementProp;
    } else {
      domNode = createElementFor(type);
      if (typeof elementProp === "function")
        elementProp(domNode);
    }
  }
  return ensureParentOrFragment(domNode);
}
__name(handleElementProp, "handleElementProp");
function createElementFor(type) {
  var elementAttr = getCustomAttribute(type, ElementAttribute);
  if (elementAttr) {
    let node;
    let wrap = document.createElement("div");
    wrap.innerHTML = elementAttr.value;
    node = wrap.children[0];
    if (!node) {
      wrap.remove();
      return type.createDefaultElement();
    }
    node.parentNode?.removeChild(node);
    return node;
  } else {
    return type.createDefaultElement();
  }
}
__name(createElementFor, "createElementFor");
function setElementProps(widget, props) {
  let el = widget.domNode;
  if (!el || !props)
    return;
  if (typeof props.id === "string")
    el.id = props.id;
  if (typeof props.name === "string")
    el.setAttribute("name", props.name);
  if (typeof props.placeholder === "string")
    el.setAttribute("placeholder", props.placeholder);
  if (typeof props.class === "string")
    addClass(el, props.class);
  if (typeof props.className === "string")
    addClass(el, props.className);
  if (typeof props.maxLength === "number")
    el.setAttribute("maxLength", (props.maxLength || 0).toString());
  else if (typeof props.maxlength === "number")
    el.setAttribute("maxLength", (props.maxlength || 0).toString());
  if (props.required != null) {
    try {
      EditorUtils.setRequired(widget, props.required);
    } catch {
    }
  }
  if (props.readOnly != null) {
    try {
      EditorUtils.setReadOnly(widget, props.readOnly);
    } catch {
    }
  }
  if (props.initialValue != null) {
    try {
      EditorUtils.setValue(this, props.initialValue);
    } catch {
    }
  }
}
__name(setElementProps, "setElementProps");

// src/ui/widgets/widget.ts
var afterRenderSymbol = /* @__PURE__ */ Symbol();
var _a22;
_a22 = Symbol.typeInfo;
var _Widget = class _Widget {
  static {
    __name(this, "Widget");
  }
  static {
    this.nextWidgetNumber = 0;
  }
  constructor(props) {
    if (isArrayLike(props)) {
      this.domNode = ensureParentOrFragment(props[0]);
      this.options = {};
    } else {
      this.options = props ?? {};
      this.domNode = handleElementProp(getInstanceType(this), this.options);
    }
    delete this.options.element;
    setElementProps(this, this.props);
    this.uniqueName = getWidgetName(getInstanceType(this)) + (_Widget.nextWidgetNumber++).toString();
    associateWidget(this);
    addDisposingListener(this.domNode, bindThis(this).destroy, this.uniqueName);
    this.idPrefix = this.options?.idPrefix ?? this.uniqueName + "_";
    this[afterRenderSymbol] = [];
    this.addCssClass();
    !this.deferRender() && this.internalRenderContents();
  }
  destroy() {
    if (this.domNode) {
      removeDisposingListener(this.domNode, bindThis(this).destroy);
      deassociateWidget(this);
      toggleClass(this.domNode, this.getCssClass(), false);
      Fluent.off(this.domNode, "." + this.uniqueName);
      delete this.domNode;
    }
  }
  static createDefaultElement() {
    return document.createElement("div");
  }
  /**
   * Returns a Fluent(this.domNode) object
   */
  get element() {
    return Fluent(this.domNode);
  }
  addCssClass() {
    addClass(this.domNode, this.getCssClass());
  }
  deferRender() {
    return false;
  }
  getCssClass() {
    var type = getInstanceType(this);
    var classList = [];
    var fullClass = getTypeFullName(type).replace(/\./g, "-");
    classList.push(fullClass);
    for (let k of Config.rootNamespaces) {
      if (fullClass.startsWith(k + "-")) {
        classList.push(fullClass.substring(k.length + 1));
        break;
      }
    }
    classList.push(getTypeShortName(type));
    return classList.filter((v2, i2, a2) => a2.indexOf(v2) === i2).map((s2) => "s-" + s2).join(" ");
  }
  static getWidgetName(type) {
    return getWidgetName(type);
  }
  addValidationRule(rule, uniqueName) {
    addValidationRule(
      this.domNode,
      typeof rule === "function" ? rule : uniqueName,
      typeof rule === "function" ? uniqueName ?? this.uniqueName : rule
    );
  }
  byId(id) {
    return this.element.findFirst("#" + this.idPrefix + id);
  }
  findById(id) {
    return this.domNode?.querySelector("#" + this.idPrefix + id);
  }
  getGridField() {
    return Fluent(this.domNode.closest(".field"));
  }
  change(handler) {
    Fluent.on(this.domNode, "change." + this.uniqueName, handler);
  }
  changeSelect2(handler) {
    Fluent.on(this.domNode, "change." + this.uniqueName, (e2) => {
      if (e2.target?.dataset?.comboboxsettingvalue)
        return;
      handler(e2);
    });
  }
  static create(params) {
    let props = params.options ?? {};
    let node = handleElementProp(params.type, props);
    params.container && (isArrayLike(params.container) ? params.container[0] : params.container)?.appendChild(node);
    params.element?.(Fluent(node));
    props.element = node;
    let widget = new params.type(props);
    widget.init();
    params.init?.(widget);
    return widget;
  }
  getCustomAttribute(attrType, inherit = true) {
    return getCustomAttribute(getInstanceType(this), attrType, inherit);
  }
  afterRender(callback) {
    if (!callback)
      return;
    const queue = this[afterRenderSymbol];
    if (!queue)
      callback();
    else
      queue.push(callback);
  }
  init() {
    this.deferRender() && this.internalRenderContents();
    return this;
  }
  /**
   * Returns the main element for this widget or the document fragment.
   * As widgets may get their elements from props unlike regular JSX widgets, 
   * this method should not be overridden. Override renderContents() instead.
   */
  render() {
    let el = this.init().domNode;
    let parent2 = el?.parentNode;
    if (parent2 instanceof DocumentFragment && parent2.childNodes.length > 1 && parent2[isFragmentWorkaround])
      return parent2;
    return el;
  }
  internalRenderContents() {
    const queue = this[afterRenderSymbol];
    if (queue) {
      let contents = this.renderContents();
      if (this.domNode && contents)
        appendToNode(this.domNode, contents);
      delete this[afterRenderSymbol];
      for (var callback of queue) callback();
    }
  }
  renderContents() {
    if (this.legacyTemplateRender())
      return void 0;
    return this.options.children;
  }
  legacyTemplateRender() {
    if (typeof this.getTemplate !== "function")
      return;
    var template = this.getTemplate();
    if (typeof template !== "string")
      return;
    template = template.replace(new RegExp("~_", "g"), htmlEncode(this.idPrefix));
    this.domNode.innerHTML = template;
    return true;
  }
  get props() {
    return this.options;
  }
  syncOrAsyncThen(syncMethod, asyncMethod, then) {
    if (!this.useAsync?.())
      then.call(this, syncMethod.call(this));
    else
      asyncMethod.call(this).then(then.bind(this));
  }
  useIdPrefix() {
    return useIdPrefix(this.idPrefix);
  }
  static {
    // jsx-dom >= 8.1.5 requires isComponent as a static property
    this.isComponent = true;
  }
  static registerClass(typeName, intfAndAttr) {
    if (Object.prototype.hasOwnProperty.call(this, Symbol.typeInfo) && this[Symbol.typeInfo])
      throw new Error(`Type ${this.name} already has a typeInfo property!`);
    const typeInfo = this[Symbol.typeInfo] = classTypeInfo(typeName, intfAndAttr);
    registerType(this);
    return typeInfo;
  }
  static registerEditor(typeName, intfAndAttr) {
    if (Object.prototype.hasOwnProperty.call(this, Symbol.typeInfo) && this[Symbol.typeInfo])
      throw new Error(`Type ${this.name} already has a typeInfo property!`);
    const typeInfo = this[Symbol.typeInfo] = editorTypeInfo(typeName, intfAndAttr);
    registerType(this);
    return typeInfo;
  }
  static {
    this[_a22] = this.registerClass(nsSerenity);
  }
};
var Widget = _Widget;
var TemplatedWidget = Widget;
Object.defineProperties(Widget.prototype, { isReactComponent: { value: true } });

// src/types/editortyperegistry.ts
var EditorTypeRegistryImpl = class extends BaseTypeRegistry {
  static {
    __name(this, "EditorTypeRegistryImpl");
  }
  constructor() {
    super({
      loadKind: "editor",
      defaultSuffix: "Editor"
    });
  }
  isMatchingType(type) {
    return hasCustomAttribute(type, EditorAttribute, false) || isAssignableFrom(Widget, type);
  }
  loadError(key) {
    const message = `The editor class "${key}" was not found.

Ensure that the editor type includes a line similar to the following (using the correct full name):
static [Symbol.typeInfo] = this.registerEditor("MyProject.MyModule.MyEditor");

Also, side-effect import this editor class from the current page.ts, grid.ts, or dialog.ts file. For example:
import "./path/to/MyEditor.ts";

After applying the fixes, build the project by running "npm run build" from the project folder.`;
    notifyError(message, "", { preWrap: true, timeOut: 5e3 });
    throw new Error(message);
  }
};
var EditorTypeRegistry = new EditorTypeRegistryImpl();

// src/types/enumtyperegistry.ts
var EnumTypeRegistryImpl = class extends BaseTypeRegistry {
  static {
    __name(this, "EnumTypeRegistryImpl");
  }
  constructor() {
    super({
      loadKind: "enum",
      defaultSuffix: null
    });
  }
  getSecondaryTypeKey(type) {
    return getCustomAttribute(type, EnumKeyAttribute, false)?.value;
  }
  isMatchingType(type) {
    return isEnum(type);
  }
  loadError(key) {
    const message = `The enum type "${key}" was not found.

If you have recently defined this enum type in server-side code, ensure that your project builds successfully, then run "dotnet sergen servertypings".

Also, verify that there is such an enum type under the project root namespace, and its namespace parts start with capital letters, like MyProject.MyModule.MyEnum.

If using ES modules, make sure the enum type is registered with Serenity.registerEnum('MyProject.MyModule.MyEnum') (using the full name of your enum type), and side-effect import this enum from the current page.ts, grid.ts, or dialog.ts file. For example:
import "./path/to/MyEnum.ts";

After applying the fixes, build the project by running "npm run build" from the project folder.`;
    notifyError(message, "", { preWrap: true, timeOut: 5e3 });
    throw new Error(message);
  }
};
var EnumTypeRegistry = new EnumTypeRegistryImpl();

// src/types/formattertyperegistry.ts
var FormatterTypeRegistryImpl = class extends BaseTypeRegistry {
  static {
    __name(this, "FormatterTypeRegistryImpl");
  }
  constructor() {
    super({
      loadKind: "formatter",
      defaultSuffix: "Formatter"
    });
  }
  isMatchingType(type) {
    return isAssignableFrom(ISlickFormatter, type);
  }
  loadError(key) {
    const message = `The formatter class "${key}" was not found.

Ensure that the formatter type includes a line similar to the following (using the correct full name):
static [Symbol.typeInfo] = formatterTypeInfo("MyProject.MyModule.MyEditor"); static { registerType(this); }

Alternatively, use a legacy decorator such as:
@Decorators.registerFormatter('MyProject.MyModule.MyFormatter')

Make sure to side-effect import this formatter class from the current page.ts, grid.ts, or dialog.ts file. For example:
import "./path/to/MyFormatter.ts";

After applying the fixes, build the project by running "npm run build" from the project folder.`;
    notifyError(message, "", { preWrap: true, timeOut: 5e3 });
    throw new Error(message);
  }
};
var FormatterTypeRegistry = new FormatterTypeRegistryImpl();

// src/ui/widgets/reflectionoptionssetter.ts
var ReflectionOptionsSetter;
((ReflectionOptionsSetter2) => {
  function getPropertyValue(o2, property) {
    var d2 = o2;
    var getter = d2["get_" + property];
    if (!!!(typeof getter === "undefined")) {
      return getter.apply(o2);
    }
    var camelCase = makeCamelCase(property);
    getter = d2["get_" + camelCase];
    if (!!!(typeof getter === "undefined")) {
      return getter.apply(o2);
    }
    return d2[camelCase];
  }
  ReflectionOptionsSetter2.getPropertyValue = getPropertyValue;
  __name(getPropertyValue, "getPropertyValue");
  function setPropertyValue(o2, property, value) {
    var d2 = o2;
    var setter = d2["set_" + property];
    if (!!!(typeof setter === "undefined")) {
      setter.apply(o2, [value]);
      return;
    }
    var camelCase = makeCamelCase(property);
    setter = d2["set_" + camelCase];
    if (!!!(typeof setter === "undefined")) {
      setter.apply(o2, [value]);
      return;
    }
    d2[camelCase] = value;
  }
  ReflectionOptionsSetter2.setPropertyValue = setPropertyValue;
  __name(setPropertyValue, "setPropertyValue");
  function makeCamelCase(s2) {
    if (!s2) {
      return s2;
    }
    if (s2 === "ID") {
      return "id";
    }
    var hasNonUppercase = false;
    var numUppercaseChars = 0;
    for (var index = 0; index < s2.length; index++) {
      if (s2.charCodeAt(index) >= 65 && s2.charCodeAt(index) <= 90) {
        numUppercaseChars++;
      } else {
        hasNonUppercase = true;
        break;
      }
    }
    if (!hasNonUppercase && s2.length !== 1 || numUppercaseChars === 0) {
      return s2;
    } else if (numUppercaseChars > 1) {
      return s2.substring(0, numUppercaseChars - 1).toLowerCase() + s2.substring(numUppercaseChars - 1);
    } else if (s2.length === 1) {
      return s2.toLowerCase();
    } else {
      return s2.substring(0, 1).toLowerCase() + s2.substring(1);
    }
  }
  __name(makeCamelCase, "makeCamelCase");
  function set(target, options) {
    if (options == null) {
      return;
    }
    var type = getInstanceType(target);
    if (type === Object) {
      return;
    }
    var props = getTypeMembers(type, 16 /* property */);
    var propList = props.filter((x) => !!x.setter && x?.attr?.some((a2) => isInstanceOfType(a2, OptionAttribute)));
    var propByName = {};
    for (var k of propList) {
      propByName[makeCamelCase(k.name)] = k;
    }
    var fields = getTypeMembers(type, 4 /* field */);
    var fieldList = fields.filter((x) => x.attr?.some((a2) => isInstanceOfType(a2, OptionAttribute)));
    var fieldByName = {};
    for (var $t2 = 0; $t2 < fieldList.length; $t2++) {
      var k1 = fieldList[$t2];
      fieldByName[makeCamelCase(k1.name)] = k1;
    }
    var keys = Object.keys(options);
    for (var k2 of keys) {
      var v2 = options[k2];
      var cc = makeCamelCase(k2);
      var p2 = propByName[cc] || propByName[k2];
      if (p2 != null) {
        var func = target[p2.setter];
        func && func.call(target, v2);
      } else {
        var f2 = fieldByName[cc] || fieldByName[k2];
        f2 && (target[f2.name] = v2);
      }
    }
  }
  ReflectionOptionsSetter2.set = set;
  __name(set, "set");
})(ReflectionOptionsSetter || (ReflectionOptionsSetter = {}));

// src/ui/helpers/tabsextensions.tsx
var TabsExtensions;
((TabsExtensions2) => {
  var navLinkSelector = ":scope > ul > li > a.nav-link, :scope > li > a.nav-link, :scope > a.nav-link, :scope > ul > li > a.ui-tabs-anchor, :scope > li > a.ui-tabs-anchor";
  var navLinkSelectorActive = ":scope > ul > li > a.nav-link.active, :scope > li > a.nav-link.active, :scope > a.nav-link.active, :scope > ul > li.ui-tabs-active > a, :scope > li.ui-tabs-active > a";
  function setDisabled(tabs, tabKey, isDisabled) {
    tabs = isArrayLike(tabs) ? tabs[0] : tabs;
    if (!tabs)
      return;
    var ibk = indexByKey(tabs);
    if (!ibk)
      return;
    var index = ibk[tabKey];
    if (index == null) {
      return;
    }
    if (typeof tabs === "string")
      return;
    let $2 = getjQuery();
    if (!$2 || !$2(tabs)?.data?.().uiTabs) {
      var anchors = Array.from(tabs.querySelectorAll(navLinkSelector));
      if (index < anchors.length) {
        if (isDisabled && anchors[index].classList.contains("active")) {
          var newIndex = anchors.findIndex((x, i2) => i2 !== index && !x.classList.contains("disabled"));
          if (newIndex >= 0)
            anchors[newIndex].click();
        }
        anchors[index].classList.toggle("disabled", !!isDisabled);
      }
      return;
    }
    if (isDisabled && index === $2(tabs)?.tabs?.("option", "active")) {
      $2(tabs).tabs?.("option", "active", 0);
    }
    $2(tabs).tabs?.(isDisabled ? "disable" : "enable", index);
  }
  TabsExtensions2.setDisabled = setDisabled;
  __name(setDisabled, "setDisabled");
  function toggle(tabs, tabKey, visible) {
    tabs = isArrayLike(tabs) ? tabs[0] : tabs;
    if (!tabs)
      return;
    var ibk = indexByKey(tabs);
    if (!ibk)
      return;
    var index = ibk[tabKey];
    if (index == null) {
      return;
    }
    if (typeof tabs === "string")
      return;
    let $2 = getjQuery();
    if (!$2 || !$2(tabs).data?.().uiTabs) {
      var anchors = Array.from(tabs.querySelectorAll(navLinkSelector));
      if (index < anchors.length) {
        if (!visible && anchors[index].classList.contains("active")) {
          var newIndex = anchors.findIndex((x, i2) => i2 !== index && !x.classList.contains("disabled") && getComputedStyle(x).display !== "none");
          if (newIndex >= 0)
            anchors[newIndex].click();
        }
        anchors[index].hidden = !visible;
      }
      return;
    }
    if (!visible && index === $2(tabs).tabs?.("option", "active")) {
      $2(tabs).tabs?.("option", "active", 0);
    }
    $2(tabs).children("ul").children("li").eq(index).toggle(visible);
  }
  TabsExtensions2.toggle = toggle;
  __name(toggle, "toggle");
  function activeTabKey(tabs) {
    tabs = isArrayLike(tabs) ? tabs[0] : tabs;
    if (!tabs || typeof tabs === "string")
      return;
    let $2 = getjQuery();
    if (!$2 || !$2(tabs).data?.().uiTabs) {
      return extractTabKey2(tabs.querySelector(navLinkSelectorActive));
    }
    return extractTabKey2($2(tabs).children("ul").children("li").eq($2(tabs).tabs?.("option", "active")).children("a")[0]);
  }
  TabsExtensions2.activeTabKey = activeTabKey;
  __name(activeTabKey, "activeTabKey");
  function extractTabKey2(el) {
    if (!el)
      return "";
    var tabKey = el.dataset.tabkey;
    if (tabKey)
      return tabKey;
    var href = el.getAttribute("href").toString();
    var prefix = "_Tab";
    var lastIndex = href.lastIndexOf(prefix);
    if (lastIndex >= 0) {
      return href.substring(lastIndex + prefix.length);
    }
    return href;
  }
  __name(extractTabKey2, "extractTabKey");
  function indexByKey(tabs) {
    var indexByKey2 = {};
    tabs = isArrayLike(tabs) ? tabs[0] : tabs;
    if (!tabs)
      return indexByKey2;
    tabs.querySelectorAll(navLinkSelector).forEach(function(el, index) {
      indexByKey2[extractTabKey2(el)] = index;
    });
    return indexByKey2;
  }
  TabsExtensions2.indexByKey = indexByKey;
  __name(indexByKey, "indexByKey");
  function selectTab(tabs, tabKey) {
    tabs = isArrayLike(tabs) ? tabs[0] : tabs;
    if (!tabs || typeof tabs === "string")
      return;
    var index;
    if (typeof tabKey === "number")
      index = tabKey;
    else {
      var ibk = indexByKey(tabs);
      if (!ibk)
        return;
      var index = ibk[tabKey];
      if (index == null) {
        return;
      }
    }
    var $2 = getjQuery();
    if (!$2 || !$2(tabs)?.data?.().uiTabs) {
      var anchors = Array.from(tabs.querySelectorAll(navLinkSelector));
      if (index < anchors.length) {
        anchors[index].click();
      }
      return;
    }
    if (index !== $2(tabs).tabs?.("option", "active")) {
      $2(tabs).tabs?.("option", "active", index);
    }
  }
  TabsExtensions2.selectTab = selectTab;
  __name(selectTab, "selectTab");
  function initialize(tabs, activeChange) {
    tabs = isArrayLike(tabs) ? tabs[0] : tabs;
    if (!tabs || typeof tabs === "string")
      return null;
    let $2 = getjQuery();
    if ($2?.fn?.tabs) {
      var t2 = $2(tabs).tabs?.({});
      if (activeChange)
        t2?.on("tabsactivate", activeChange);
      return Fluent(tabs);
    } else {
      let ul = tabs.matches("ul") ? tabs : tabs.querySelector(":scope > ul");
      if (ul && !ul.classList.contains("nav-tabs") && !ul.classList.contains("nav-underline")) {
        ul.classList.add("nav", "nav-tabs");
        let activeLink;
        ul.querySelectorAll(":scope > li").forEach((li) => {
          li.classList.add("nav-item");
          let a2 = li.querySelector(":scope > a");
          if (a2) {
            a2.classList.add("nav-link");
            a2.dataset.bsToggle = "tab";
            a2.setAttribute("role", "tab");
            if (a2.classList.contains("ui-tabs-active")) {
              a2.classList.add("active");
              a2.classList.remove("ui-tabs-active");
              activeLink = a2;
            }
          }
        });
        if (!activeLink) {
          activeLink = ul.querySelector(":scope > li > a");
          if (activeLink) {
            activeLink.classList.add("active");
          }
        }
        const container = tabs.appendChild(jsx("div", { class: "tab-content" }));
        tabs.querySelectorAll(":scope>.tab-pane").forEach((pane) => {
          pane.classList.add("pt-3");
          container.appendChild(pane);
          if (activeLink && activeLink.getAttribute("href") === "#" + pane.id) {
            pane.classList.add("show", "active");
          }
        });
      }
      if (activeChange) {
        Fluent.on(tabs, "shown.bs.tab", activeChange);
      }
      return Fluent(tabs);
    }
  }
  TabsExtensions2.initialize = initialize;
  __name(initialize, "initialize");
  function destroy(tabs) {
    if (!tabs || typeof tabs === "string")
      return;
    let $2 = getjQuery();
    if ($2?.(tabs).data?.().uiTabs) {
      $2(tabs)?.tabs?.("destroy");
      return;
    }
    if (isArrayLike(tabs))
      tabs = tabs[0];
    if (tabs && typeof bootstrap !== "undefined" && bootstrap?.Tab?.getInstance) {
      for (const anchor of Array.from(tabs.querySelectorAll(navLinkSelector))) {
        const inst = bootstrap.Tab.getInstance(anchor);
        if (inst && typeof inst.dispose === "function") {
          inst.dispose();
          break;
        }
      }
    }
  }
  TabsExtensions2.destroy = destroy;
  __name(destroy, "destroy");
})(TabsExtensions || (TabsExtensions = {}));

// src/ui/widgets/toolbar.tsx
function ToolbarButton(tb) {
  const btn = Fluent(
    jsx(
      "div",
      {
        class: ["tool-button", tb.cssClass, tb.icon && "icon-tool-button", !tb.title && "no-text"],
        title: !!tb.hint && tb.hint,
        onClick: (e2) => {
          if (tb.onClick && !e2.currentTarget?.classList.contains("disabled")) {
            tb.onClick(e2);
          }
        },
        children: jsx("span", { class: "button-inner", children: [
          tb.icon && jsx(Fragment, { children: [
            jsx("i", { class: iconClassName(tb.icon) }),
            " "
          ] }),
          tb.title
        ] })
      }
    )
  );
  if (tb.action != null)
    btn.data("action", tb.action);
  if (tb.visible === false)
    btn.hide();
  if (tb.disabled != null && typeof tb.disabled !== "function")
    btn.toggleClass("disabled", !!tb.disabled);
  if (typeof tb.visible === "function" || typeof tb.disabled == "function") {
    btn.on("updateInterface", () => {
      if (typeof tb.visible === "function")
        btn.toggle(tb.visible());
      if (typeof tb.disabled === "function")
        btn.toggleClass("disabled", !!tb.disabled());
    });
  }
  const node = btn.getNode();
  if (tb.ref) {
    tb.ref(node);
    delete tb.ref;
  }
  return node;
}
__name(ToolbarButton, "ToolbarButton");
var _a23, _b11;
var Toolbar = class extends (_b11 = Widget, _a23 = Symbol.typeInfo, _b11) {
  static {
    __name(this, "Toolbar");
  }
  static {
    this[_a23] = this.registerClass(nsSerenity);
  }
  renderContents() {
    let group = jsx("div", { class: "tool-group" });
    this.element.addClass("s-Toolbar clearfix").append(group);
    var buttons = this.options.buttons || [];
    var currentCount = 0;
    for (var i2 = 0; i2 < buttons.length; i2++) {
      var button = buttons[i2];
      if (button.separator && currentCount > 0) {
        group = group.parentElement.appendChild(jsx("div", { class: "tool-group" }));
        currentCount = 0;
      }
      this.createButton(group, button);
      currentCount++;
    }
    return group;
  }
  destroy() {
    this.domNode.querySelectorAll("div.tool-button").forEach((el) => Fluent.off(el, "click"));
    if (this.mouseTrap) {
      if (this.mouseTrap.__listeners) {
        this.mouseTrap.__listeners.forEach((li) => {
          li.node.removeEventListener(li.type, li.fn);
        });
        this.mouseTrap.__listeners = null;
      }
      if (!!this.mouseTrap.destroy) {
        this.mouseTrap.destroy();
      } else {
        this.mouseTrap.reset();
      }
      this.mouseTrap = null;
    }
    super.destroy();
  }
  createButton(container, tb) {
    if (isArrayLike(container)) {
      container = container[0];
    }
    if (tb.separator === "right" || tb.separator === "both") {
      container.appendChild(jsx("div", { class: "separator" }));
    }
    let button = ToolbarButton(tb);
    container.append(button);
    if (tb.hotkey && window["Mousetrap"] != null) {
      if (!this.mouseTrap) {
        const mouseTrap = window["Mousetrap"];
        const el = tb.hotkeyContext || this.options.hotkeyContext || window.document.documentElement;
        const prevAddEventListener = el.addEventListener;
        const listeners = [];
        el.addEventListener = function(type, fn, flag) {
          listeners.push({ node: this, type, fn });
          prevAddEventListener.call(this, type, fn, flag);
        };
        this.mouseTrap = mouseTrap(el);
        this.mouseTrap.__listeners = listeners;
        delete el.addEventListener;
        if (el.addEventListener !== prevAddEventListener) {
          el.addEventListener = prevAddEventListener;
        }
      }
      this.mouseTrap.bind(tb.hotkey, function() {
        if (getComputedStyle(button).display !== "none") {
          Fluent.trigger(button, "click");
        }
        return tb.hotkeyAllowDefault;
      });
    }
    return button;
  }
  findButton(className2) {
    if (className2 != null && className2.startsWith(".")) {
      className2 = className2.substring(1);
    }
    return Fluent(this.domNode.querySelector("div.tool-button." + className2));
  }
  updateInterface() {
    this.domNode.querySelectorAll(".tool-button").forEach(function(el) {
      Fluent.trigger(el, "updateInterface", { bubbles: false });
    });
  }
};

// src/ui/dialogs/basedialog-internal.ts
function getCssSize(element, name) {
  var cssSize = getComputedStyle(element).getPropertyValue(name);
  if (cssSize == null || !cssSize.endsWith("px"))
    return null;
  cssSize = cssSize.substring(0, cssSize.length - 2);
  let i2 = parseInt(cssSize, 10);
  if (i2 == null || isNaN(i2) || i2 == 0)
    return null;
  return i2;
}
__name(getCssSize, "getCssSize");
function applyCssSizes(opt, dialogClass) {
  let size;
  let dialog = document.createElement("div");
  try {
    dialog.hidden = true;
    addClass(dialog, dialogClass);
    document.body.append(dialog);
    var sizeHelper = document.createElement("div");
    sizeHelper.classList.add("size");
    dialog.append(sizeHelper);
    size = getCssSize(sizeHelper, "minWidth");
    if (size != null)
      opt.minWidth = size;
    size = getCssSize(sizeHelper, "width");
    if (size != null)
      opt.width = size;
    size = getCssSize(sizeHelper, "height");
    if (size != null)
      opt.height = size;
    size = getCssSize(sizeHelper, "minHeight");
    if (size != null)
      opt.minHeight = size;
  } finally {
    dialog.remove();
  }
}
__name(applyCssSizes, "applyCssSizes");
function handleUIDialogResponsive(domNode) {
  let $2 = getjQuery();
  if (!$2)
    return;
  var dlg = $2(domNode)?.dialog();
  var uiDialog = $2(domNode).closest(".ui-dialog");
  if (!uiDialog.length)
    return;
  if (isMobileView()) {
    var data = $2(domNode).data("responsiveData");
    if (!data) {
      data = {};
      data.draggable = dlg.dialog("option", "draggable");
      data.resizable = dlg.dialog("option", "resizable");
      data.position = dlg.css("position");
      var pos = uiDialog.position();
      data.left = pos.left;
      data.top = pos.top;
      data.width = uiDialog.width();
      data.height = uiDialog.height();
      data.contentHeight = $2(domNode).height();
      $2(domNode).data("responsiveData", data);
      dlg.dialog("option", "draggable", false);
      dlg.dialog("option", "resizable", false);
    }
    uiDialog.addClass("mobile-layout");
    uiDialog.css({ left: "0px", top: "0px", width: $2(window).width() + "px", height: $2(window).height() + "px", position: "fixed" });
    $2(document.body).scrollTop(0);
    layoutFillHeight(domNode);
  } else {
    var d2 = $2(domNode).data("responsiveData");
    if (d2) {
      dlg.dialog("option", "draggable", d2.draggable);
      dlg.dialog("option", "resizable", d2.resizable);
      $2(domNode).closest(".ui-dialog").css({ left: "0px", top: "0px", width: d2.width + "px", height: d2.height + "px", position: d2.position });
      $2(domNode).height(d2.contentHeight);
      uiDialog.removeClass("mobile-layout");
      $2(domNode).removeData("responsiveData");
    }
  }
}
__name(handleUIDialogResponsive, "handleUIDialogResponsive");

// src/ui/dialogs/dialogextensions.ts
var DialogExtensions;
((DialogExtensions2) => {
  function dialogResizable(dialog, w2, h2, mw, mh) {
    let $2 = getjQuery();
    if (!$2)
      return;
    var dlg = $2(dialog)?.dialog?.();
    if (!dlg)
      return;
    dlg.dialog("option", "resizable", true);
    if (mw != null) {
      dlg.dialog("option", "minWidth", mw);
    }
    if (w2 != null) {
      dlg.dialog("option", "width", w2);
    }
    if (mh != null) {
      dlg.dialog("option", "minHeight", mh);
    }
    if (h2 != null) {
      dlg.dialog("option", "height", h2);
    }
  }
  DialogExtensions2.dialogResizable = dialogResizable;
  __name(dialogResizable, "dialogResizable");
  function dialogMaximizable(dialog) {
    let $2 = getjQuery();
    if (!$2)
      return;
    $2(dialog).dialogExtend?.({
      closable: true,
      maximizable: true,
      dblclick: "maximize",
      icons: { maximize: "ui-icon-maximize-window" }
    });
  }
  DialogExtensions2.dialogMaximizable = dialogMaximizable;
  __name(dialogMaximizable, "dialogMaximizable");
})(DialogExtensions || (DialogExtensions = {}));
function registerDialogExtendPlugin() {
  let $2 = getjQuery();
  if (!$2 || !$2.widget)
    return false;
  if ($2.fn?.dialogExtend)
    return true;
  $2.widget("ui.dialogExtend", {
    options: {
      dblclick: true,
      load: null,
      beforeRestore: null,
      restore: null,
      maximizable: false,
      beforeMaximize: null,
      maximize: null
    },
    _create: /* @__PURE__ */ __name(function() {
      this._state = "normal";
      this._initButtons();
      this._setState("normal");
      return this._trigger("load");
    }, "_create"),
    _setState: /* @__PURE__ */ __name(function(state) {
      $2(this.domNode).removeClass("ui-dialog-" + this._state).addClass("ui-dialog-" + state);
      return this._state = state;
    }, "_setState"),
    _initButtons: /* @__PURE__ */ __name(function() {
      var _this = this;
      this._addButton("maximize", this.options.maximizable, DialogTexts.MaximizeHint, faIcon("window-maximize"));
      this._addButton("restore", false, DialogTexts.RestoreHint, faIcon("window-restore"));
      var titlebar = $2(this.domNode).closest(".ui-dialog").children(".ui-dialog-titlebar");
      titlebar.dblclick(function() {
        if (_this.options.dblclick) {
          if (_this._state !== "normal") {
            return _this.restore();
          } else {
            return _this[_this.options.dblclick]();
          }
        }
      }).select(function() {
        return false;
      });
    }, "_initButtons"),
    _addButton: /* @__PURE__ */ __name(function(name, show, hint, icon) {
      var _this = this;
      var titlebar = $2(this.domNode).closest(".ui-dialog").children(".ui-dialog-titlebar");
      var closeButton = titlebar.find(".ui-dialog-titlebar-close").first();
      var button = $2('<button class="ui-button ui-corner-all ui-button-icon-only ui-dialog-titlebar-' + name + '" href="javascript:;" tabindex="-1"><i class="' + icon + '"></i></a>').attr("title", hint).toggle(show).click(function(e2) {
        e2.preventDefault();
        return _this[name]();
      });
      if (closeButton)
        button.insertBefore(closeButton);
      else
        button.appendTo(titlebar);
      return button;
    }, "_addButton"),
    maximize: /* @__PURE__ */ __name(function() {
      var newHeight, newWidth;
      newHeight = $2(window).height() - 1;
      newWidth = $2(window).width() - 1;
      this._trigger("beforeMaximize");
      if (this._state !== "normal") {
        this._restore();
      }
      this._saveSnapshot();
      var el = $2(this.domNode);
      if (el.dialog("option", "draggable")) {
        el.dialog("widget").draggable("option", "handle", null).find(".ui-dialog-draggable-handle").css("cursor", "text").end();
      }
      el.dialog("widget").css("position", "fixed").find(".ui-dialog-content").show().dialog("widget").find(".ui-dialog-buttonpane").show().end().find(".ui-dialog-content").dialog("option", {
        resizable: false,
        draggable: false,
        height: newHeight,
        width: newWidth,
        position: {
          of: window,
          my: "left top",
          at: "left top"
        }
      });
      this._setState("maximized");
      this._toggleButtons();
      if (this.original_config_resizable)
        $2(this.domNode).closest(".ui-dialog").triggerHandler("resize");
      return this._trigger("maximize");
    }, "maximize"),
    _restore_maximized: /* @__PURE__ */ __name(function() {
      var el = $2(this.domNode);
      var original = this._snapshot || { config: {}, size: {}, position: {}, titlebar: {} };
      el.dialog("widget").css("position", original.position.mode).find(".ui-dialog-titlebar").css("white-space", original.titlebar.wrap).end().find(".ui-dialog-content").dialog("option", {
        resizable: original.config.resizable,
        draggable: original.config.draggable,
        height: original.size.height,
        width: original.size.width,
        maxHeight: original.size.maxHeight,
        position: {
          of: window,
          my: "left top",
          at: "left+" + original.position.left + " top+" + original.position.top
        }
      });
      if (el.dialog("option", "draggable")) {
        return el.dialog("widget").draggable("option", "handle", el.dialog("widget").find(".ui-dialog-draggable-handle").length ? el.dialog("widget").find(".ui-dialog-draggable-handle") : ".ui-dialog-titlebar").find(".ui-dialog-draggable-handle").css("cursor", "move");
      }
    }, "_restore_maximized"),
    state: /* @__PURE__ */ __name(function() {
      return this._state;
    }, "state"),
    restore: /* @__PURE__ */ __name(function() {
      this._trigger("beforeRestore");
      this._restore();
      this._setState("normal");
      this._toggleButtons();
      if (this.original_config_resizable)
        $2(this.domNode).closest(".ui-dialog").triggerHandler("resize");
      return this._trigger("restore");
    }, "restore"),
    _restore: /* @__PURE__ */ __name(function() {
      if (this._state !== "normal") {
        return this["_restore_" + this._state]();
      }
    }, "_restore"),
    _saveSnapshot: /* @__PURE__ */ __name(function() {
      if (this._state === "normal") {
        var el = $2(this.domNode);
        this._snapshot = {
          config: {
            resizable: el.dialog("option", "resizable"),
            draggable: el.dialog("option", "draggable")
          },
          size: {
            height: el.dialog("widget").outerHeight(),
            width: el.dialog("option", "width"),
            maxHeight: el.dialog("option", "maxHeight")
          },
          position: {
            mode: el.dialog("widget").css("position"),
            left: el.dialog("widget").offset().left - $2("body").scrollLeft(),
            top: el.dialog("widget").offset().top - $2("body").scrollTop()
          },
          titlebar: {
            wrap: el.dialog("widget").find(".ui-dialog-titlebar").css("white-space")
          }
        };
      }
    }, "_saveSnapshot"),
    _toggleButtons: /* @__PURE__ */ __name(function() {
      var uiDialog = $2(this.domNode).closest(".ui-dialog");
      uiDialog.find(".ui-dialog-titlebar-restore").toggle(this._state !== "normal");
      uiDialog.find(".ui-dialog-titlebar-maximize").toggle(this._state !== "maximized");
    }, "_toggleButtons")
  });
}
__name(registerDialogExtendPlugin, "registerDialogExtendPlugin");
!registerDialogExtendPlugin() && Fluent.ready(registerDialogExtendPlugin);

// src/ui/dialogs/basedialog.tsx
var _a24, _b12;
var BaseDialog = class extends (_b12 = Widget, _a24 = Symbol.typeInfo, _b12) {
  static {
    __name(this, "BaseDialog");
  }
  static {
    this[_a24] = this.registerClass(nsSerenity, [IDialog]);
  }
  static createDefaultElement() {
    return document.body.appendChild(jsx("div", { hidden: true }));
  }
  constructor(props) {
    super(props);
    this.domNode.setAttribute("id", this.domNode.getAttribute("id") || this.uniqueName);
    this.initValidator();
    this.initTabs();
    this.initToolbar();
  }
  destroy() {
    TabsExtensions.destroy(this.tabs);
    this.tabs = null;
    if (this.toolbar) {
      this.toolbar.destroy();
      this.toolbar = null;
    }
    if (this.validator) {
      this.validator?.destroy?.();
      this.byId("Form").remove();
      this.validator = null;
    }
    const dialog = this.dialog;
    if (dialog) {
      Fluent.off(this.domNode, "." + this.uniqueName);
      this.dialog = null;
      dialog.dispose();
    }
    Fluent.off(window, "." + this.uniqueName);
    super.destroy();
  }
  addCssClass() {
  }
  getInitialDialogTitle() {
    return "";
  }
  isStaticPanel() {
    return this.getCustomAttribute(StaticPanelAttribute)?.value === true;
  }
  getDialogOptions() {
    return {
      preferPanel: this.isStaticPanel() ? true : this.getCustomAttribute(PanelAttribute)?.value,
      autoOpen: false,
      buttons: this.getDialogButtons(),
      closeButton: this.isStaticPanel() ? false : this.getCustomAttribute(CloseButtonAttribute)?.value,
      dialogClass: (this.getCssClass() ?? "") + " flex-layout",
      element: this.domNode,
      size: "lg",
      onClose: /* @__PURE__ */ __name((result) => {
        this.onDialogClose(result);
      }, "onClose"),
      onOpen: /* @__PURE__ */ __name(() => {
        this.onDialogOpen();
      }, "onOpen"),
      width: Math.min(window.innerWidth, 920),
      providerOptions: /* @__PURE__ */ __name((type) => {
        if (type === "uidialog") {
          var opt = {};
          applyCssSizes(opt, this.getCssClass());
          opt.resizable = this.getCustomAttribute(ResizableAttribute)?.value;
          return opt;
        }
      }, "providerOptions"),
      title: this.dialogTitle ?? this.getInitialDialogTitle() ?? ""
    };
  }
  initDialog() {
    this.domNode.hidden = false;
    if (this.dialog?.type == "uidialog") {
      this.initUIDialog();
      Fluent.on(this.domNode.closest(".ui-dialog"), "resize." + this.uniqueName, bindThis(this).arrange);
    }
  }
  initUIDialog() {
    let element = getjQuery()(this.domNode);
    DialogExtensions.dialogResizable(element);
    Fluent.on(window, "resize." + this.uniqueName, () => {
      if (element.width() > 0 && element.height() > 0)
        this.handleResponsive();
    });
    Fluent.on(this.domNode, "dialogopen." + this.uniqueName, bindThis(this).handleResponsive);
    if (this.getCustomAttribute(MaximizableAttribute)?.value) {
      DialogExtensions.dialogMaximizable(element);
    }
  }
  dialogOpen(asPanel) {
    if (!this.dialog) {
      let opt = this.getDialogOptions();
      if (asPanel != null)
        opt.preferPanel = asPanel;
      this.dialog = new Dialog(opt);
      this.initDialog();
    }
    this.dialog.open();
  }
  onDialogOpen() {
    if (!isMobileView())
      this.element.findFirst("input:not([type=hidden]), textarea, select").focus();
    this.arrange();
    TabsExtensions.selectTab(this.tabs, 0);
  }
  /** Attaches a dialog/modal/panel close event handler. See Dialog.close for more info. */
  onClose(handler, opt) {
    Dialog.onClose(this.element, handler, opt);
  }
  /** Attaches a dialog/modal/panel open event handler. See Dialog.open for more info. */
  onOpen(handler, opt) {
    Dialog.onOpen(this.element, handler, opt);
  }
  getToolbarButtons() {
    return [];
  }
  initToolbar() {
    var toolbarDiv = this.findById("Toolbar");
    if (!toolbarDiv)
      return;
    var hotkeyContext = this.domNode.closest(".ui-dialog") ?? this.domNode.closest(".modal") ?? this.domNode;
    this.toolbar = new Toolbar({ element: toolbarDiv, buttons: this.getToolbarButtons(), hotkeyContext }).init();
  }
  getValidatorOptions() {
    return {};
  }
  initValidator() {
    var form = this.findById("Form");
    if (form instanceof HTMLFormElement) {
      var valOptions = this.getValidatorOptions();
      this.validator = new Validator(form, validateOptions(valOptions));
    }
  }
  resetValidation() {
    this.validator && this.validator.resetAll();
  }
  validateForm() {
    return this.validator == null || !!this.validator.form();
  }
  arrange() {
    this.domNode.querySelectorAll(".require-layout").forEach((el) => {
      Fluent.isVisibleLike(el) && Fluent.trigger(el, "layout");
    });
  }
  onDialogClose(result) {
    document.dispatchEvent(new Event("click"));
    window.setTimeout(() => {
      let domNode = this.domNode;
      this.destroy();
      if (domNode) {
        Fluent.remove(domNode);
      }
      positionToastContainer(defaultNotifyOptions, false);
    }, 0);
  }
  getDialogButtons() {
    return [];
  }
  dialogClose(result) {
    this.dialog?.close(result ?? null);
  }
  get dialogTitle() {
    return this.dialog?.title() ?? this.domNode.dataset.dialogtitle;
  }
  set dialogTitle(value) {
    this.domNode.dataset.dialogtitle = value;
    this.dialog?.title(value ?? "");
  }
  initTabs() {
    var tabsDiv = this.findById("Tabs");
    if (!tabsDiv)
      return;
    this.tabs = TabsExtensions.initialize(tabsDiv, bindThis(this).arrange);
  }
  handleResponsive() {
    handleUIDialogResponsive(this.domNode);
  }
};
var TemplatedDialog = BaseDialog;

// src/ui/helpers/gridutils.tsx
import { RowMoveManager } from "@serenity-is/sleekgrid";

// src/ui/datagrid/quicksearchinput.tsx
var _a25, _b13;
var QuickSearchInput = class extends (_b13 = Widget, _a25 = Symbol.typeInfo, _b13) {
  static {
    __name(this, "QuickSearchInput");
  }
  static {
    this[_a25] = this.registerClass(nsSerenity);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    this.domNode.title = QuickSearchTexts.Hint;
    this.domNode.placeholder = QuickSearchTexts.Placeholder;
    this.lastValue = (this.domNode.value ?? "").trim();
    const boundThis = bindThis(this);
    Fluent.on(this.domNode, "keyup." + this.uniqueName, boundThis.checkIfValueChanged);
    Fluent.on(this.domNode, "change." + this.uniqueName, boundThis.checkIfValueChanged);
    this.domNode.before(jsx("span", { class: "quick-search-icon", children: jsx("i", {}) }));
    if (this.options.fields?.length) {
      this.domNode.before(
        jsx("div", { class: "dropdown quick-search-field", children: [
          jsx("a", { class: "quick-search-field-toggle", title: QuickSearchTexts.FieldSelection, "data-bs-toggle": "dropdown", ref: (el) => this.fieldLink = el }),
          jsx("ul", { class: "dropdown-menu", children: this.options.fields.map((item) => jsx("a", { class: "dropdown-item", href: "#", onClick: (e2) => {
            e2.preventDefault();
            this.fieldChanged = item !== this.field;
            this.field = item;
            this.updateInputPlaceHolder();
            this.checkIfValueChanged();
          }, children: item.title ?? "" })) })
        ] })
      );
      this.field = this.options.fields[0];
      this.updateInputPlaceHolder();
    }
    Fluent.on(this.domNode, "execute-search." + this.uniqueName, () => {
      if (this.timer)
        clearTimeout(this.timer);
      this.searchNow((this.domNode.value ?? "").trim());
    });
  }
  checkIfValueChanged() {
    if (this.domNode.classList.contains("ignore-change")) {
      return;
    }
    var value = this.get_value();
    if (value == this.lastValue && (!this.fieldChanged || !value)) {
      this.fieldChanged = false;
      return;
    }
    this.fieldChanged = false;
    if (!!this.timer) {
      clearTimeout(this.timer);
    }
    var self2 = this;
    this.timer = window.setTimeout(function() {
      self2.searchNow(value);
    }, this.options.typeDelay ?? 500);
    this.lastValue = value;
  }
  get_value() {
    return (this.domNode.value ?? "").trim();
  }
  get_field() {
    return this.field;
  }
  set_field(value) {
    if (this.field !== value) {
      this.fieldChanged = true;
      this.field = value;
      this.updateInputPlaceHolder();
      this.checkIfValueChanged();
    }
  }
  updateInputPlaceHolder() {
    this.fieldLink && (this.fieldLink.textContent = this.field?.title ?? "");
  }
  restoreState(value, field) {
    this.fieldChanged = false;
    this.field = field;
    var value = (value ?? "").trim();
    this.domNode.value = value;
    this.lastValue = value;
    if (!!this.timer) {
      clearTimeout(this.timer);
      this.timer = null;
    }
    this.updateInputPlaceHolder();
  }
  searchNow(value) {
    this.domNode.parentElement?.classList.toggle(
      this.options.filteredParentClass ?? "s-QuickSearchFiltered",
      !!(value.length > 0)
    );
    let klass = this.options.loadingParentClass ?? "s-QuickSearchLoading";
    this.domNode.classList.add(klass);
    this.domNode.parentElement?.classList.add(klass);
    var done = /* @__PURE__ */ __name((results) => {
      this.domNode.classList.remove(klass);
      this.domNode.parentElement?.classList.remove(klass);
      if (!results) {
        var el = this.domNode.closest(".s-QuickSearchBar")?.querySelector(".quick-search-icon i");
        if (el) {
          el.classList.add("s-shake-effect");
          setTimeout(() => el.classList.remove("s-shake-effect"), 2e3);
        }
      }
    }, "done");
    const args = { field: this.field?.name, query: value, done };
    this.options.beforeSearch?.(args);
    if (this.options.onSearch != null) {
      this.options.onSearch(args.field, args.query, args.done);
    } else if (this.options.search != null) {
      this.options.search(args);
    } else {
      done(true);
    }
  }
};

// src/ui/helpers/gridutils.tsx
var GridUtils;
((GridUtils2) => {
  function addToggleButton(toolDiv, cssClass, callback, hint, initial) {
    const btn = jsx("div", { class: ["s-ToggleButton", cssClass, initial && "pressed"], children: jsx("a", { href: "#", title: hint ?? "", onClick: (e2) => {
      e2.preventDefault();
      btn.classList.toggle("pressed");
      const pressed = btn.classList.contains("pressed");
      callback && callback(pressed);
    } }) });
    (isArrayLike(toolDiv) ? toolDiv[0] : toolDiv).prepend(btn);
  }
  GridUtils2.addToggleButton = addToggleButton;
  __name(addToggleButton, "addToggleButton");
  function addIncludeDeletedToggle(toolDiv, view, hint, initial) {
    var includeDeleted = false;
    var oldSubmit = view.onSubmit;
    view.onSubmit = function(v2) {
      v2.params.IncludeDeleted = includeDeleted;
      if (oldSubmit != null) {
        return oldSubmit(v2);
      }
      return true;
    };
    if (hint == null)
      hint = EntityGridTexts.IncludeDeletedToggle;
    addToggleButton(
      toolDiv,
      "s-IncludeDeletedToggle",
      function(pressed) {
        includeDeleted = pressed;
        view.seekToPage = 1;
        view.populate();
      },
      hint,
      initial
    );
    addDisposingListener(isArrayLike(toolDiv) ? toolDiv[0] : toolDiv, function() {
      view.onSubmit = null;
      oldSubmit = null;
    });
  }
  GridUtils2.addIncludeDeletedToggle = addIncludeDeletedToggle;
  __name(addIncludeDeletedToggle, "addIncludeDeletedToggle");
  function addQuickSearch({ container, fields, beforeSearch, search, view }) {
    const el = jsx("input", { type: "text" });
    container = isArrayLike(container) ? container[0] : container;
    container.prepend(jsx("div", { class: ["s-QuickSearchBar", fields?.length && "has-quick-search-fields"], children: el }));
    let lastDoneEvent = null;
    const input = new QuickSearchInput({
      element: el,
      fields,
      beforeSearch,
      search: search ?? ((args) => {
        lastDoneEvent = args.done;
        if (view) {
          view.seekToPage = 1;
          view.populate();
        }
      })
    });
    if (view) {
      let oldSubmit = view.onSubmit;
      view.onSubmit = function(v2) {
        if (input) {
          var searchText = input.get_value();
          if (searchText && searchText.length > 0) {
            v2.params.ContainsText = searchText;
          } else {
            delete v2.params["ContainsText"];
          }
          var searchField = input.get_field()?.name;
          if (searchField != null && searchField.length > 0) {
            v2.params.ContainsField = searchField;
          } else {
            delete v2.params["ContainsField"];
          }
        }
        if (oldSubmit != null)
          return oldSubmit(v2);
        return true;
      };
      const onDataLoaded = /* @__PURE__ */ __name(function() {
        if (lastDoneEvent != null) {
          lastDoneEvent(view.getLength() > 0);
          lastDoneEvent = null;
        }
      }, "onDataLoaded");
      view.onDataLoaded.subscribe(onDataLoaded);
      addDisposingListener(el, function() {
        view?.onDataLoaded?.unsubscribe(onDataLoaded);
        if (view && oldSubmit) {
          view.onSubmit = oldSubmit;
          view = null;
          oldSubmit = null;
        }
      });
    }
    return input;
  }
  GridUtils2.addQuickSearch = addQuickSearch;
  __name(addQuickSearch, "addQuickSearch");
  function addQuickSearchInput(toolDiv, view, fields, onChange) {
    return addQuickSearch({
      container: toolDiv,
      fields,
      view,
      beforeSearch: onChange,
      search: /* @__PURE__ */ __name(() => {
      }, "search")
    });
  }
  GridUtils2.addQuickSearchInput = addQuickSearchInput;
  __name(addQuickSearchInput, "addQuickSearchInput");
  function addQuickSearchInputCustom(container, search, fields) {
    return addQuickSearch({
      container,
      fields,
      search: /* @__PURE__ */ __name((args) => search(args.field, args.query, args.done), "search")
    });
  }
  GridUtils2.addQuickSearchInputCustom = addQuickSearchInputCustom;
  __name(addQuickSearchInputCustom, "addQuickSearchInputCustom");
  function makeOrderable(grid, handleMove) {
    var moveRowsPlugin = new RowMoveManager({ cancelEditOnDrag: true });
    moveRowsPlugin.onBeforeMoveRows.subscribe(function(e2, data) {
      for (var i2 = 0; !!(i2 < data.rows.length); i2++) {
        if (!!(data.rows[i2] === data.insertBefore || data.rows[i2] === data.insertBefore - 1)) {
          e2.stopPropagation();
          return false;
        }
      }
      return true;
    });
    moveRowsPlugin.onMoveRows.subscribe(function(_2, data) {
      handleMove(data.rows, data.insertBefore);
      try {
        grid.setSelectedRows([]);
      } catch {
      }
    });
    grid.registerPlugin(moveRowsPlugin);
  }
  GridUtils2.makeOrderable = makeOrderable;
  __name(makeOrderable, "makeOrderable");
  function makeOrderableWithUpdateRequest(dataGrid, getId, getDisplayOrder, service, getUpdateRequest) {
    makeOrderable(dataGrid.getGrid(), function(rows, insertBefore) {
      if (rows.length === 0) {
        return;
      }
      var order;
      var index = insertBefore;
      if (index < 0) {
        order = 1;
      } else if (insertBefore >= dataGrid.getGrid().getDataLength()) {
        order = getDisplayOrder(dataGrid.getGrid().getDataItem(dataGrid.getGrid().getDataLength() - 1)) ?? 0;
        if (order === 0) {
          order = insertBefore + 1;
        } else {
          order = order + 1;
        }
      } else {
        order = getDisplayOrder(dataGrid.getGrid().getDataItem(insertBefore)) ?? 0;
        if (order === 0) {
          order = insertBefore + 1;
        }
      }
      var i2 = 0;
      var next = null;
      next = /* @__PURE__ */ __name(function() {
        serviceRequest(
          service,
          getUpdateRequest(getId(dataGrid.getGrid().getDataItem(rows[i2])), order++),
          () => {
            i2++;
            if (i2 < rows.length) {
              next();
            } else {
              dataGrid.getView().populate();
            }
          }
        );
      }, "next");
      next();
    });
  }
  GridUtils2.makeOrderableWithUpdateRequest = makeOrderableWithUpdateRequest;
  __name(makeOrderableWithUpdateRequest, "makeOrderableWithUpdateRequest");
})(GridUtils || (GridUtils = {}));

// src/ui/datagrid/columnpickerdialog.tsx
var _a26, _b14;
var ColumnPickerDialog = class _ColumnPickerDialog extends (_b14 = BaseDialog, _a26 = Symbol.typeInfo, _b14) {
  static {
    __name(this, "ColumnPickerDialog");
  }
  static {
    this[_a26] = this.registerClass(nsSerenity, [Attributes.resizable]);
  }
  constructor(opt) {
    super(opt);
    opt = this.options;
    this.columns = (typeof opt.columns === "function" ? opt.columns() : opt.columns) ?? opt.dataGrid?.getGrid()?.getAllColumns?.() ?? opt.sleekGrid?.getAllColumns?.() ?? [];
    this.columns = this.columns.slice();
    this.defaultOrder = typeof opt.defaultOrder === "function" ? opt.defaultOrder() : opt.defaultOrder;
    this.defaultVisible = typeof opt.defaultVisible === "function" ? opt.defaultVisible() : opt.defaultVisible;
    if (!this.defaultOrder || !this.defaultVisible) {
      const initialColumns = opt.dataGrid?.initialSettings?.columns;
      if (initialColumns && initialColumns.length) {
        this.defaultOrder ??= initialColumns.map((x) => x.id);
        this.defaultVisible ??= initialColumns.filter((x) => x.visible === true).map((x) => x.id);
      }
    }
    this.defaultOrder ??= this.columns.map((x) => x.id);
    this.defaultVisible ??= this.columns.filter((x) => x.visible !== false).map((x) => x.id);
    this.colById = {};
    for (let c2 of this.columns) {
      this.colById[c2.id] = c2;
    }
    const sleekGrid = opt.sleekGrid ?? opt.dataGrid?.getGrid();
    this.toggleColumnsHandler = opt.toggleColumns ?? ((columnIds, show) => {
      const toggledColumns = [];
      for (let columnId of columnIds) {
        const column = this.colById[columnId];
        if (!column)
          continue;
        show ??= column?.visible === false;
        if (column.visible === false && show) {
          column.visible = true;
        } else if (column.visible !== false && !show) {
          column.visible = false;
        } else
          continue;
        toggledColumns.push(column);
      }
      if (toggledColumns.length) {
        sleekGrid?.invalidateColumns();
        return toggledColumns;
      }
    });
    this.reorderColumnsHandler = opt.reorderColumns ?? ((columnIds, setVisible) => {
      sleekGrid?.reorderColumns(columnIds, { notify: true, setVisible });
      return true;
    });
    this.onChangeHandler = opt.onChange ?? ((args) => {
      const persistPromise = Promise.resolve(this.options.dataGrid?.persistSettings?.());
      if (args.toggledColumns?.some((c2) => c2.visible !== false) || args.reorderedColumns || args.restoredDefaults) {
        return persistPromise.then(() => {
          const sleekGrid2 = this.options.sleekGrid ?? this.options.dataGrid?.getGrid();
          const remoteView = sleekGrid2?.getData?.();
          if (remoteView?.populate) {
            remoteView.populate();
          }
        });
      }
      return persistPromise;
    });
    this.element.on("click", ".toggle-visibility", bindThis(this).handleToggleClick);
  }
  destroy() {
    delete this.toggleColumnsHandler;
    delete this.colById;
    delete this.columns;
    delete this.defaultOrder;
    delete this.defaultVisible;
    delete this.onChangeHandler;
    delete this.reorderColumnsHandler;
    delete this.searchInput;
    delete this.toggleAllCheckbox;
    this.element.off("click", this.handleToggleClick);
    super.destroy();
  }
  toggleColumns(columnIds, show) {
    const result = this.toggleColumnsHandler(columnIds, show);
    if (result && result.length) {
      this.onChange({ toggledColumns: result, reorderedColumns: false, restoredDefaults: false });
    }
    return result;
  }
  onChange(args) {
    return this.onChangeHandler(args);
  }
  handleToggleClick(e2) {
    const columnId = e2.target?.closest("li")?.dataset.key;
    if (!columnId)
      return;
    this.toggleColumns([columnId]);
    queueMicrotask(() => {
      const column = this.colById[columnId];
      if (column) {
        const input = this.list?.querySelector(`li[data-key='${columnId}'] input`);
        input && (input.checked = column.visible !== false);
      }
      this.updateToggleAllValue();
    });
    this.updateToggleAllValue();
  }
  renderContents() {
    this.dialogTitle = ColumnPickerDialogTexts.Title;
    return jsx("div", { class: "columns-container", children: [
      jsx("div", { class: "search-bar d-flex align-items-center", children: [
        jsx("div", { class: "form-check", children: jsx("input", { type: "checkbox", id: this.uniqueName + "_ToggleAll", class: "form-check-input toggle-all", ref: (ref) => this.toggleAllCheckbox = ref, onClick: bindThis(this).handleToggleAllClick }) }),
        jsx("div", { class: "search-sep" }),
        jsx("div", { class: "s-QuickSearchBar flex-grow-1", ref: bindThis(this).createSearch }),
        jsx("div", { class: "search-sep" }),
        jsx(
          "button",
          {
            id: this.uniqueName + "_RestoreDefaults",
            class: "btn btn-sm btn-outline-danger float-end",
            type: "button",
            title: ColumnPickerDialogTexts.RestoreDefaults,
            onClick: bindThis(this).handleRestoreDefaults,
            children: jsx("i", { class: faIcon("redo") })
          }
        )
      ] }),
      jsx("div", { class: "column-list", children: jsx("ul", { ref: (ref) => this.list = ref }) })
    ] });
  }
  createSearch(div) {
    const input = GridUtils.addQuickSearch({
      container: div,
      search: bindThis(this).handleSearch
    });
    input.element.attr("id", this.uniqueName + "_Search");
    this.searchInput = input.domNode;
  }
  reorderColumns(columnIds, setVisible, restoredDefaults) {
    if (this.reorderColumnsHandler(columnIds, setVisible)) {
      this.onChange({ toggledColumns: null, reorderedColumns: true, restoredDefaults });
    }
  }
  handleRestoreDefaults() {
    this.reorderColumns(this.defaultOrder, this.defaultVisible, true);
    let liByKey = {};
    Array.from(this.list.childNodes).forEach((el) => {
      liByKey[el.dataset.key] = el;
    });
    let last = null;
    for (let id of this.defaultOrder) {
      let li = liByKey[id];
      if (!li)
        continue;
      if (last == null)
        this.list.prepend(li);
      else
        Fluent(li).insertAfter(last);
      last = li;
      let key = li.dataset.key;
      delete liByKey[key];
    }
    for (let key in liByKey)
      this.list.append(liByKey[key]);
    if (this.defaultVisible) {
      this.list.querySelectorAll("input.toggle-visibility").forEach((input) => input.checked = false);
      for (let id of this.defaultVisible) {
        const li = this.list.querySelector(`li[data-key='${id}']`);
        if (li) {
          const input = li.querySelector("input.toggle-visibility");
          if (input) {
            input.checked = true;
          }
        }
      }
    }
    this.updateToggleAllValue();
  }
  handleToggleAllClick() {
    const show = this.toggleAllCheckbox.checked;
    const columnIds = [];
    this.list.querySelectorAll("li:not([hidden]) input.toggle-visibility:not([disabled])").forEach((input) => {
      if (!!input.checked !== !!show) {
        input.checked = show;
        const li = input.closest("li");
        const colId = li?.dataset.key;
        if (colId) {
          columnIds.push(colId);
        }
      }
    });
    this.toggleColumns(columnIds, show);
    this.updateToggleAllValue();
  }
  updateToggleAllValue() {
    const inputs = this.list.querySelectorAll("li:not([hidden]) input.toggle-visibility:not([disabled])");
    return this.toggleAllCheckbox.checked = Array.from(inputs).every((x) => x.checked);
  }
  handleSearch({ query, done }) {
    query = stripDiacritics(query?.trim().toLowerCase() ?? "");
    if (query.length && !this.list.style.height) {
      this.list.style.height = this.list.offsetHeight + "px";
    } else if (!query.length && this.list.style.height) {
      this.list.style.height = "";
    }
    let found = false;
    for (let li of Array.from(this.list.children)) {
      const colId = li.dataset.key;
      const col = this.colById[colId];
      const title = stripDiacritics(!col ? "" : (this.getTitle(col) ?? "").toLowerCase());
      const match = title.indexOf(query) >= 0;
      li.hidden = !match;
      if (match)
        found = true;
    }
    this.updateToggleAllValue();
    done(found);
  }
  static createToolButton(optOrDataGrid) {
    const opt = optOrDataGrid && "getGrid" in optOrDataGrid ? { dataGrid: optOrDataGrid } : optOrDataGrid;
    function onClick() {
      _ColumnPickerDialog.openDialog(opt);
    }
    __name(onClick, "onClick");
    opt.dataGrid?.element.on("handleroute." + opt.dataGrid.uniqueName, (e2, arg) => {
      if (arg && !arg.handled && arg.route == "columns") {
        onClick();
      }
    });
    return {
      hint: ColumnPickerDialogTexts.Title,
      action: "column-picker",
      cssClass: "column-picker-button",
      icon: faIcon("th-list", "blue"),
      onClick
    };
  }
  getDialogOptions() {
    var opt = super.getDialogOptions();
    opt.size = "sm";
    opt.width = 300;
    return opt;
  }
  getDialogButtons() {
    return null;
  }
  getTitle(col) {
    if (col.id == "__select__")
      return "[x]";
    return col.name || col.toolTip || col.id;
  }
  isTogglable(col) {
    return col.togglable !== false;
  }
  isMovable(col) {
    return col.movable !== false;
  }
  getPinInfo(col) {
    const sleekGrid = this.options?.sleekGrid ?? this.options?.dataGrid?.getGrid?.();
    const rtl = sleekGrid?.getOptions?.()?.rtl;
    const layoutInfo = sleekGrid?.getLayoutInfo?.();
    const pinned = col.frozen && layoutInfo?.supportPinnedCols;
    let pinnedRight = pinned && layoutInfo?.supportPinnedEnd && col.frozen === "end";
    if (rtl && pinned) {
      pinnedRight = !pinnedRight;
    }
    return { pinned, pinnedRight };
  }
  createColumnItem(col) {
    const togglable = this.isTogglable(col);
    const movable = this.isMovable(col);
    const { pinned, pinnedRight } = this.getPinInfo(col);
    const pinText = tryGetText("Controls.ColumnHeaderMenu.Pin");
    const pinHint = pinned && pinText ? `${pinText}: ${pinnedRight ? localText("Controls.ColumnHeaderMenu.PinRight", "") : localText("Controls.ColumnHeaderMenu.PinLeft", "")}` : null;
    return jsx("li", { class: [!togglable && "cant-hide", !movable && "not-movable"], "data-key": col.id, children: jsx("div", { class: "form-check", children: [
      jsx("input", { type: "checkbox", id: this.uniqueName + "_col_" + col.id, class: "form-check-input toggle-visibility", disabled: !togglable, checked: col.visible !== false }),
      jsx("span", { class: "drag-handle", style: { visibility: !movable && "hidden" }, children: jsx("i", { class: faIcon("braille") }) }),
      jsx("span", { class: "form-check-label", children: [
        this.getTitle(col),
        pinned ? jsx("span", { class: "ms-2 opacity-25", title: pinHint, children: PinImage({ right: pinnedRight }) }) : null
      ] })
    ] }) });
  }
  handleSortableEnd() {
    const newOrder = Array.from(this.list.children).map((li) => li.dataset?.key).filter((id) => id != null);
    this.reorderColumns(newOrder, null, false);
  }
  createColumnItems() {
    const columns = this.columns;
    for (const column of columns) {
      this.list.append(this.createColumnItem(column));
    }
    this.updateToggleAllValue();
    if (typeof globalThis.Sortable !== "undefined" && typeof globalThis.Sortable.create !== "undefined") {
      globalThis.Sortable.create(this.list, {
        handle: ".drag-handle, .form-check-label",
        filter: ".not-movable, input",
        group: this.uniqueName + "_group",
        onEnd: bindThis(this).handleSortableEnd
      });
    }
  }
  onDialogOpen() {
    this.createColumnItems();
    super.onDialogOpen();
    this.searchInput?.focus();
  }
  static openDialog(opt) {
    const picker = new _ColumnPickerDialog(opt);
    picker.dialogOpen();
    if (opt.dataGrid) {
      Router?.dialog?.(opt.dataGrid.element, picker.domNode, () => "columns");
    }
  }
};
var PinImage = /* @__PURE__ */ __name(({ right }) => {
  return jsx("svg", { xmlns: "http://www.w3.org/2000/svg", style: { transform: right ? "rotate(270deg)" : null, width: "1rem", height: "1rem" }, fill: "currentColor", class: "bi bi-pin-angle", viewBox: "0 0 16 16", children: jsx("path", { d: "M9.828.722a.5.5 0 0 1 .354.146l4.95 4.95a.5.5 0 0 1 0 .707c-.48.48-1.072.588-1.503.588-.177 0-.335-.018-.46-.039l-3.134 3.134a6 6 0 0 1 .16 1.013c.046.702-.032 1.687-.72 2.375a.5.5 0 0 1-.707 0l-2.829-2.828-3.182 3.182c-.195.195-1.219.902-1.414.707s.512-1.22.707-1.414l3.182-3.182-2.828-2.829a.5.5 0 0 1 0-.707c.688-.688 1.673-.767 2.375-.72a6 6 0 0 1 1.013.16l3.134-3.133a3 3 0 0 1-.04-.461c0-.43.108-1.022.589-1.503a.5.5 0 0 1 .353-.146m.122 2.112v-.002zm0-.002v.002a.5.5 0 0 1-.122.51L6.293 6.878a.5.5 0 0 1-.511.12H5.78l-.014-.004a5 5 0 0 0-.288-.076 5 5 0 0 0-.765-.116c-.422-.028-.836.008-1.175.15l5.51 5.509c.141-.34.177-.753.149-1.175a5 5 0 0 0-.192-1.054l-.004-.013v-.001a.5.5 0 0 1 .12-.512l3.536-3.535a.5.5 0 0 1 .532-.115l.096.022c.087.017.208.034.344.034q.172.002.343-.04L9.927 2.028q-.042.172-.04.343a1.8 1.8 0 0 0 .062.46z" }) });
}, "PinImage");

// src/ui/datagrid/datagrid.tsx
import { AutoTooltips, SleekGrid } from "@serenity-is/sleekgrid";

// src/base/pubsub.ts
var PubSub = class {
  static {
    __name(this, "PubSub");
  }
  #handlers = [];
  subscribe(fn) {
    this.#handlers.push(fn);
  }
  unsubscribe(fn) {
    for (let i2 = this.#handlers.length - 1; i2 >= 0; i2--) {
      if (this.#handlers[i2] === fn) {
        this.#handlers.splice(i2, 1);
      }
    }
  }
  notify(e2, opt) {
    for (let handler of this.#handlers) {
      handler(e2);
      if (opt?.isCancelled(e2)) {
        break;
      }
    }
  }
  clear() {
    this.#handlers = [];
  }
};

// src/ui/helpers/subdialoghelper.ts
var SubDialogHelper;
((SubDialogHelper2) => {
  function bindToDataChange(dialog, owner, dataChange, useTimeout) {
    var uniqueName = owner["uniqueName"];
    dialog.element.on("ondatachange." + uniqueName, function(e2) {
      if (typeof e2.operationType === "undefined" && e2.originalEvent && typeof e2.originalEvent.operationType !== "undefined")
        e2 = e2.originalEvent;
      if (useTimeout) {
        window.setTimeout(function() {
          dataChange(e2);
        }, 0);
      } else {
        dataChange(e2);
      }
    });
    addDisposingListener(dialog.node, () => function() {
      Fluent.off(dialog.domNode, "ondatachange." + uniqueName);
    }, dialog.uniqueName);
    return dialog;
  }
  SubDialogHelper2.bindToDataChange = bindToDataChange;
  __name(bindToDataChange, "bindToDataChange");
  function triggerDataChange(dialog) {
    Fluent.trigger(dialog.domNode, "ondatachange");
    return dialog;
  }
  SubDialogHelper2.triggerDataChange = triggerDataChange;
  __name(triggerDataChange, "triggerDataChange");
  function triggerDataChanged(element) {
    Fluent.trigger(isArrayLike(element) ? element[0] : element, "ondatachange");
  }
  SubDialogHelper2.triggerDataChanged = triggerDataChanged;
  __name(triggerDataChanged, "triggerDataChanged");
  function bubbleDataChange(dialog, owner, useTimeout) {
    return bindToDataChange(dialog, owner, function(e2) {
      Fluent.trigger(owner.domNode, "ondatachange");
    }, useTimeout);
  }
  SubDialogHelper2.bubbleDataChange = bubbleDataChange;
  __name(bubbleDataChange, "bubbleDataChange");
  function cascade(cascadedDialog, ofElement) {
    Fluent.one(cascadedDialog.domNode, "dialogopen", function(e2) {
      var $2 = getjQuery();
      if ($2 && $2.fn && $2.fn.dialog) {
        $2(cascadedDialog.domNode).dialog("option", "position", cascadedDialogOffset(ofElement));
      }
    });
    return cascadedDialog;
  }
  SubDialogHelper2.cascade = cascade;
  __name(cascade, "cascade");
  function cascadedDialogOffset(element) {
    return { my: "left top", at: "left+20 top+20", of: isArrayLike(element) ? element[0] : element };
  }
  SubDialogHelper2.cascadedDialogOffset = cascadedDialogOffset;
  __name(cascadedDialogOffset, "cascadedDialogOffset");
})(SubDialogHelper || (SubDialogHelper = {}));

// src/ui/editors/cascadedwidgetlink.ts
var _a27;
_a27 = Symbol.typeInfo;
var CascadedWidgetLink = class {
  constructor(parentType, widget, parentChange) {
    this.parentType = parentType;
    this.widget = widget;
    this.parentChange = parentChange;
    this.bind();
    addDisposingListener(this.widget.domNode, () => {
      this.unbind();
      this.widget = null;
      this.parentChange = null;
    }, this.widget.uniqueName);
  }
  static {
    __name(this, "CascadedWidgetLink");
  }
  static {
    this[_a27] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  bind() {
    if (!this._parentID) {
      return null;
    }
    var parent2 = tryGetWidget(findElementWithRelativeId(this.widget.domNode, this._parentID), this.parentType);
    if (parent2 != null) {
      Fluent.on(parent2.domNode, "change." + this.widget.uniqueName, () => {
        this.parentChange(parent2);
      });
      return parent2;
    } else {
      notifyError("Can't find cascaded parent element with ID: " + this._parentID + "!", "", null);
      return null;
    }
  }
  unbind() {
    if (!this._parentID) {
      return null;
    }
    var parent2 = tryGetWidget(findElementWithRelativeId(this.widget.domNode, this._parentID), this.parentType);
    if (parent2 != null) {
      Fluent.off(parent2.domNode, "." + this.widget.uniqueName);
    }
    return parent2;
  }
  get_parentID() {
    return this._parentID;
  }
  set_parentID(value) {
    if (this._parentID !== value) {
      this.unbind();
      this._parentID = value;
      this.bind();
    }
  }
};

// src/ui/editors/editorwidget.ts
var _a28, _b15;
var EditorWidget = class extends (_b15 = Widget, _a28 = Symbol.typeInfo, _b15) {
  static {
    __name(this, "EditorWidget");
  }
  static {
    this[_a28] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
  }
  get readOnly() {
    return typeof this?.get_readOnly === "function" ? !!this.get_readOnly() : getElementReadOnly(this.domNode);
  }
  set readOnly(value) {
    if (typeof this?.set_readOnly === "function") {
      this.set_readOnly(!!value);
    } else if (this.domNode) {
      setElementReadOnly(this.domNode, value);
    }
  }
};

// src/ui/editors/comboboxeditor.tsx
var _a29, _b16;
var ComboboxEditor = class _ComboboxEditor extends (_b16 = EditorWidget, _a29 = Symbol.typeInfo, _b16) {
  static {
    __name(this, "ComboboxEditor");
  }
  static {
    this[_a29] = this.registerClass(nsSerenity, [ISetEditValue, IGetEditValue, IStringValue, IReadOnly]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "hidden" });
  }
  constructor(props) {
    super(props);
    let hidden = this.domNode;
    this._items = [];
    this._itemById = {};
    var emptyItemText = this.emptyItemText();
    if (emptyItemText != null) {
      hidden.setAttribute("placeholder", emptyItemText);
    }
    var comboboxOptions = this.getComboboxOptions();
    comboboxOptions.element = hidden;
    this.combobox = new Combobox(comboboxOptions);
    hidden.setAttribute("type", "text");
    Fluent.on(hidden, "change." + this.uniqueName, (e2) => {
      if (!e2.target?.dataset?.comboboxsettingvalue)
        ValidationHelper.validateElement(hidden);
    });
    this.setCascadeFrom(this.options.cascadeFrom);
    if (this.useInplaceAdd())
      this.addInplaceCreate(SelectEditorTexts.InplaceAdd, null);
  }
  destroy() {
    this.combobox?.dispose();
    this.combobox = null;
    super.destroy();
  }
  hasAsyncSource() {
    return false;
  }
  asyncSearch(query) {
    return Promise.resolve({
      items: [],
      more: false
    });
  }
  getTypeDelay() {
    return this.options["typeDelay"] ?? 200;
  }
  emptyItemText() {
    return this.domNode.getAttribute("placeholder") ?? SelectEditorTexts.EmptyItemText;
  }
  getPageSize() {
    return this.options["pageSize"] ?? 100;
  }
  getIdField() {
    return this.options["idField"];
  }
  itemId(item) {
    var value = item[this.getIdField()];
    if (value == null)
      return "";
    return value.toString();
  }
  getTextField() {
    return this.options["textField"] ?? this.getIdField();
  }
  itemText(item) {
    var value = item[this.getTextField()];
    if (value == null)
      return "";
    return value.toString();
  }
  itemDisabled(item) {
    return false;
  }
  mapItem(item) {
    return {
      id: this.itemId(item),
      text: this.itemText(item),
      disabled: this.itemDisabled(item),
      source: item
    };
  }
  mapItems(items) {
    return items.map(bindThis(this).mapItem);
  }
  allowClear() {
    return this.options.allowClear != null ? !!this.options.allowClear : this.emptyItemText() != null;
  }
  isMultiple() {
    return !!this.options.multiple;
  }
  abortPendingQuery() {
    this.combobox?.abortPendingQuery();
  }
  getComboboxOptions() {
    var emptyItemText = this.emptyItemText();
    var opt = {
      multiple: this.isMultiple(),
      placeholder: emptyItemText || null,
      allowClear: this.allowClear(),
      arbitraryValues: this.isAutoComplete()
    };
    if (this.hasAsyncSource()) {
      opt.search = (query) => this.asyncSearch(query).then((result) => {
        let items = this.mapItems(result.items || []);
        if (query.initSelection) {
          const itemById = {};
          for (var x of items) {
            itemById[x.id] = x;
          }
          const newItems = (query.idList || []).map((id) => itemById[id]).filter((x2) => x2 != null);
          if (items.length == newItems.length) {
            items = newItems;
          }
        }
        const mappedResult = {
          items,
          more: result.more
        };
        if (this.isAutoComplete() && query.idList && items.length < query.idList.length) {
          for (var v2 of query.idList) {
            if (!items.some((z) => z.id == v2)) {
              items.push({
                id: v2,
                text: v2
              });
            }
          }
        }
        this._itemById ??= {};
        for (var x of items)
          this._itemById[x.id] = x;
        return mappedResult;
      });
    } else {
      opt.search = (query) => {
        let items;
        if (query.initSelection) {
          items = (query.idList || []).map((id) => this._itemById[id] || this._items.find((z) => z.id == id)).filter((x) => x != null);
        } else {
          items = _ComboboxEditor.filterByText(this._items, (x) => x.text, query.searchTerm);
        }
        if (this.isAutoComplete() && query.idList && items.length < query.idList.length) {
          this._itemById ??= {};
          for (var v2 of query.idList) {
            if (!items.some((z) => z.id == v2)) {
              var item = {
                id: v2,
                text: v2
              };
              items.push(item);
              this._itemById[item.id] = item;
            }
          }
        }
        return {
          items: items.slice(query.skip, query.take ? query.skip + query.take : items.length),
          more: query.take && items.length > 0 && items.length > query.skip + query.take
        };
      };
    }
    if (this.options.minimumResultsForSearch != null)
      opt.minimumResultsForSearch = this.options.minimumResultsForSearch;
    if (this.isAutoComplete() || this.useInplaceAdd())
      opt.createSearchChoice = this.getCreateSearchChoice(null);
    return opt;
  }
  get_delimited() {
    return !!this.options.delimited;
  }
  get items() {
    if (this.hasAsyncSource())
      throw new Error("Can't read items property of an async select editor!");
    return this._items || [];
  }
  set items(value) {
    if (this.hasAsyncSource())
      throw new Error("Can't set items of an async select editor!");
    this._items = value || [];
    this._itemById = {};
    for (var item of this._items)
      this._itemById[item.id] = item;
  }
  get itemById() {
    if (this.hasAsyncSource())
      throw new Error("Can't read items property of an async select editor!");
    return this._itemById;
  }
  set itemById(value) {
    if (this.hasAsyncSource())
      throw new Error("Can't set itemById of an async select editor!");
    this._itemById = value || {};
  }
  clearItems() {
    if (this.hasAsyncSource())
      throw new Error("Can't clear items of an async select editor!");
    this._items.length = 0;
    this._itemById = {};
  }
  addItem(item) {
    if (this.hasAsyncSource())
      throw new Error("Can't add item to an async select editor!");
    this._items.push(item);
    this._itemById[item.id] = item;
  }
  addOption(key, text2, source, disabled) {
    this.addItem({
      id: key,
      text: text2,
      source,
      disabled
    });
  }
  addInplaceCreate(addTitle, editTitle) {
    var self2 = this;
    addTitle = addTitle ?? SelectEditorTexts.InplaceAdd;
    editTitle = editTitle ?? SelectEditorTexts.InplaceEdit;
    const inplaceButton = jsx("a", { class: "inplace-button inplace-create", title: addTitle, onClick: (e2) => {
      self2.inplaceCreateClick(e2);
    }, children: jsx("b", {}) });
    this.domNode.after(inplaceButton);
    this.getComboboxContainer()?.classList.add("has-inplace-button");
    this.domNode.classList.add("has-inplace-button");
    this.element.on("change", () => {
      var isNew = this.isMultiple() || !this.get_value();
      inplaceButton.title = isNew ? addTitle : editTitle;
      inplaceButton.classList.toggle("edit", !isNew);
    });
    this.element.on("change", (e2) => {
      if (e2.target.dataset.comboboxsettingvalue)
        return;
      if (this.isMultiple()) {
        var values = this.get_values();
        if (values.length > 0 && values[values.length - 1] == (-2147483648).toString()) {
          this.set_values(values.slice(0, values.length - 1));
          this.inplaceCreateClick(e2);
        }
      } else if (this.get_value() == (-2147483648).toString()) {
        this.set_value(null);
        this.inplaceCreateClick(e2);
      }
    });
    if (this.isMultiple()) {
      Fluent.on(this.getComboboxContainer(), "dblclick." + this.uniqueName, ".select2-search-choice", (e3) => {
        var q = Fluent(e3.target);
        if (!q.hasClass("select2-search-choice")) {
          q = q.closest(".select2-search-choice");
        }
        var index = Array.from(q.parent().getNode()?.children || []).indexOf(q.getNode());
        var values1 = this.get_values();
        if (index == null || index < 0 || index >= this.get_values().length) {
          return;
        }
        e3["editItem"] = values1[index];
        this.inplaceCreateClick(e3);
      });
    }
  }
  useInplaceAdd() {
    return !this.isAutoComplete() && this.options.inplaceAdd && (this.options.inplaceAddPermission == null || Authorization.hasPermission(this.options.inplaceAddPermission));
  }
  isAutoComplete() {
    return !!this.options.autoComplete;
  }
  getCreateSearchChoice(getName) {
    return (s2) => {
      this.lastCreateTerm = s2;
      s2 = (stripDiacritics(s2) ?? "").toLowerCase();
      if (isTrimmedEmpty(s2)) {
        return null;
      }
      if ((this._items || []).some((x) => {
        var text2 = getName ? getName(x.source) : x.text;
        return stripDiacritics(text2 ?? "").toLowerCase() == s2;
      }))
        return null;
      if (!(this._items || []).some((x1) => {
        return (stripDiacritics(x1.text) ?? "").toLowerCase().indexOf(s2) !== -1;
      })) {
        if (this.isAutoComplete()) {
          return {
            id: this.lastCreateTerm,
            text: this.lastCreateTerm
          };
        }
        return {
          id: (-2147483648).toString(),
          text: SelectEditorTexts.NoResultsClickToDefine
        };
      }
      if (this.isAutoComplete()) {
        return {
          id: this.lastCreateTerm,
          text: this.lastCreateTerm
        };
      }
      return {
        id: (-2147483648).toString(),
        text: SelectEditorTexts.ClickToDefine
      };
    };
  }
  setEditValue(source, property) {
    var val = source[property.name];
    if (Array.isArray(val)) {
      this.set_values(val);
    } else {
      this.set_value(val == null ? null : val.toString());
    }
  }
  getEditValue(property, target) {
    if (!this.isMultiple() || this.get_delimited()) {
      target[property.name] = this.get_value();
    } else {
      target[property.name] = this.get_values();
    }
  }
  getComboboxContainer() {
    return Combobox.getInstance(this.domNode)?.container;
  }
  get_items() {
    return this.items;
  }
  get_itemByKey() {
    return this.itemById;
  }
  static filterByText(items, getText, term) {
    if (term == null || term.length == 0)
      return items;
    term = stripDiacritics(term).toUpperCase();
    var contains = [];
    function filter(item) {
      var text2 = getText(item);
      if (text2 == null || !text2.length)
        return false;
      text2 = stripDiacritics(text2).toUpperCase();
      if (text2.startsWith(term))
        return true;
      if (text2.indexOf(term) >= 0)
        contains.push(item);
      return false;
    }
    __name(filter, "filter");
    return items.filter(filter).concat(contains);
  }
  get_value() {
    return this.combobox ? this.combobox.getValue() : this.domNode?.value;
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (this.combobox) {
      this.combobox.setValue(
        value,
        /*triggerChange*/
        true
      );
    } else if (this.domNode) {
      this.domNode.value = value;
    }
    this.updateInplaceReadOnly();
  }
  set value(v2) {
    this.set_value(v2);
  }
  get selectedItem() {
    let selectedValue = this.get_value();
    if (selectedValue && this._itemById) {
      let item = this._itemById[selectedValue];
      if (item)
        return item.source;
    }
    return null;
  }
  get selectedItems() {
    let selectedValues = this.values;
    var result = [];
    for (var value of selectedValues) {
      if (value && this._itemById) {
        let item = this._itemById[value];
        if (item && item.source)
          result.push(item.source);
        else
          result.push(null);
      }
    }
    return result;
  }
  get_values() {
    return this.combobox?.getValues();
  }
  get values() {
    return this.get_values();
  }
  set_values(value) {
    this.combobox?.setValues(value);
  }
  set values(value) {
    this.set_values(value);
  }
  get_text() {
    var combobox = Combobox.getInstance(this.domNode);
    if (combobox)
      return combobox.getSelectedItems()?.map((x) => x.text).join(", ");
    return this.domNode.value;
  }
  get text() {
    return this.get_text();
  }
  get_readOnly() {
    return this.domNode.getAttribute("readonly") != null;
  }
  updateInplaceReadOnly() {
    var readOnly = this.get_readOnly() && (this.isMultiple() || !this.value);
    let el = this.element.nextSibling(".inplace-create").getNode();
    if (el) {
      el.setAttribute("disabled", readOnly ? "disabled" : "");
      el.style.opacity = readOnly ? "0.1" : "";
      el.style.cursor = readOnly ? "default" : "";
    }
  }
  set_readOnly(value) {
    if (value !== this.get_readOnly()) {
      setElementReadOnly(this.domNode, value);
      this.updateInplaceReadOnly();
    }
  }
  getCascadeFromValue(parent2) {
    return EditorUtils.getValue(parent2);
  }
  setCascadeFrom(value) {
    if (!value) {
      if (this.cascadeLink != null) {
        this.cascadeLink.set_parentID(null);
        this.cascadeLink = null;
      }
      this.options.cascadeFrom = null;
      return;
    }
    this.cascadeLink = new CascadedWidgetLink(Widget, this, (p2) => {
      this.set_cascadeValue(this.getCascadeFromValue(p2));
    });
    this.cascadeLink.set_parentID(value);
    this.options.cascadeFrom = value;
  }
  get_cascadeFrom() {
    return this.options.cascadeFrom;
  }
  get cascadeFrom() {
    return this.get_cascadeFrom();
  }
  set_cascadeFrom(value) {
    if (value !== this.options.cascadeFrom) {
      this.setCascadeFrom(value);
      this.updateItems();
    }
  }
  set cascadeFrom(value) {
    this.set_cascadeFrom(value);
  }
  get_cascadeField() {
    return this.options.cascadeField ?? this.options.cascadeFrom;
  }
  get cascadeField() {
    return this.get_cascadeField();
  }
  set_cascadeField(value) {
    this.options.cascadeField = value;
  }
  set cascadeField(value) {
    this.set_cascadeField(value);
  }
  get_cascadeValue() {
    return this.options.cascadeValue;
  }
  get cascadeValue() {
    return this.get_cascadeValue();
  }
  set_cascadeValue(value) {
    if (this.options.cascadeValue !== value) {
      this.options.cascadeValue = value;
      this.set_value(null);
      this.updateItems();
    }
  }
  set cascadeValue(value) {
    this.set_cascadeValue(value);
  }
  get_filterField() {
    return this.options.filterField;
  }
  get filterField() {
    return this.get_filterField();
  }
  set_filterField(value) {
    this.options.filterField = value;
  }
  set filterField(value) {
    this.set_filterField(value);
  }
  get_filterValue() {
    return this.options.filterValue;
  }
  get filterValue() {
    return this.get_filterValue();
  }
  set_filterValue(value) {
    if (this.options.filterValue !== value) {
      this.options.filterValue = value;
      this.set_value(null);
      this.updateItems();
    }
  }
  set filterValue(value) {
    this.set_filterValue(value);
  }
  cascadeItems(items) {
    var val = this.get_cascadeValue();
    if (val == null || val === "") {
      if (this.get_cascadeField()) {
        return [];
      }
      return items;
    }
    var key = val.toString();
    var fld = this.get_cascadeField();
    return items.filter((x) => {
      var itemKey = x[fld];
      return !!(itemKey != null && itemKey.toString() === key);
    });
  }
  filterItems(items) {
    var val = this.get_filterValue();
    if (val == null || val === "") {
      return items;
    }
    var key = val.toString();
    var fld = this.get_filterField();
    return items.filter((x) => {
      var itemKey = x[fld];
      return !!(itemKey != null && itemKey.toString() === key);
    });
  }
  updateItems() {
  }
  getDialogTypeKey() {
    if (this.options.dialogType != null) {
      return this.options.dialogType;
    }
    return null;
  }
  createEditDialog(callback) {
    var dialogTypeKey = this.getDialogTypeKey();
    var dialogType = DialogTypeRegistry.getOrLoad(dialogTypeKey);
    const then = /* @__PURE__ */ __name((dialogType2) => {
      var dialog = new dialogType2({}).init?.();
      callback?.(dialog);
    }, "then");
    isPromiseLike(dialogType) ? dialogType.then(then) : then(dialogType);
  }
  initNewEntity(entity) {
    if (this.get_cascadeField()) {
      entity[this.get_cascadeField()] = this.get_cascadeValue();
    }
    if (this.get_filterField()) {
      entity[this.get_filterField()] = this.get_filterValue();
    }
    if (this.onInitNewEntity != null) {
      this.onInitNewEntity(entity);
    }
  }
  setEditDialogReadOnly(dialog) {
    dialog.element && dialog.element.findFirst && dialog.element.findFirst(".tool-button.delete-button").addClass("disabled").off("click", void 0);
  }
  editDialogDataChange() {
  }
  setTermOnNewEntity(entity, term, dialog) {
    if (term && typeof dialog?.getNameProperty === "function") {
      const nameProperty = dialog.getNameProperty();
      if (nameProperty) {
        entity[nameProperty] = term;
      }
    }
  }
  inplaceCreateClick(e2) {
    if (this.get_readOnly() && (this.isMultiple() && !e2["editItem"] || !this.value))
      return;
    this.createEditDialog((dialog) => {
      if (this.get_readOnly())
        this.setEditDialogReadOnly(dialog);
      SubDialogHelper.bindToDataChange(dialog, this, (dci) => {
        this.editDialogDataChange();
        this.updateItems();
        this.lastCreateTerm = null;
        if ((dci.operationType === "create" || dci.operationType === "update") && dci.entityId != null) {
          var id = dci.entityId.toString();
          if (this.isMultiple()) {
            var values = this.get_values().slice();
            if (values.indexOf(id) < 0) {
              values.push(id);
            }
            this.set_values(null);
            this.set_values(values.slice());
          } else {
            this.set_value(null);
            this.set_value(id);
          }
        } else if (this.isMultiple() && dci.operationType === "delete" && dci.entityId != null) {
          var id1 = dci.entityId.toString();
          var values1 = this.get_values().slice();
          var idx1 = values1.indexOf(id1);
          if (idx1 >= 0)
            values1.splice(idx1, 1);
          this.set_values(values1.slice());
        } else if (!this.isMultiple()) {
          this.set_value(null);
        }
      }, true);
      var editItem = e2["editItem"];
      if (editItem != null) {
        dialog.load(editItem, () => {
          dialog.dialogOpen(this.openDialogAsPanel);
        }, null);
      } else if (this.isMultiple() || !this.get_value()) {
        var entity = {};
        this.setTermOnNewEntity(entity, this.lastCreateTerm?.trim() ?? "", dialog);
        this.initNewEntity(entity);
        dialog.load(entity, () => {
          dialog.dialogOpen(this.openDialogAsPanel);
        }, null);
      } else {
        dialog.load(this.get_value(), () => {
          dialog.dialogOpen(this.openDialogAsPanel);
        }, null);
      }
    });
  }
  openDropdown() {
    Combobox.getInstance(this.domNode)?.openDropdown();
  }
};

// src/ui/filtering/ifiltering.ts
var _a30;
_a30 = Symbol.typeInfo;
var IFiltering = class {
  static {
    __name(this, "IFiltering");
  }
  static {
    this[_a30] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/ui/filtering/filteringtyperegistry.ts
var FilteringTypeRegistryImpl = class extends BaseTypeRegistry {
  static {
    __name(this, "FilteringTypeRegistryImpl");
  }
  constructor() {
    super({
      loadKind: "filtering",
      defaultSuffix: "Filtering"
    });
  }
  isMatchingType(type) {
    return isAssignableFrom(IFiltering, type);
  }
  loadError(key) {
    const message = `The filtering handler class "${key}" was not found!`;
    notifyError(message);
    throw new Error(message);
  }
};
var FilteringTypeRegistry = new FilteringTypeRegistryImpl();

// src/ui/filtering/filterstore.ts
var _a31;
_a31 = Symbol.typeInfo;
var _FilterStore = class _FilterStore {
  static {
    __name(this, "FilterStore");
  }
  static {
    this[_a31] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  constructor(fields) {
    this.items = [];
    if (fields == null)
      throw new Error("FilterStore: 'fields' argument is null!");
    this.fields = fields.slice();
    this.get_fields().sort(function(x, y2) {
      var titleX = tryGetText(x.title);
      if (titleX == null) {
        titleX = x.title;
        if (titleX == null)
          titleX = x.name;
      }
      var titleY = tryGetText(y2.title);
      if (titleY == null) {
        titleY = y2.title;
        if (titleY == null)
          titleY = y2.name;
      }
      return Culture.stringCompare(titleX, titleY);
    });
    this.fieldByName = {};
    for (var field of fields) {
      this.get_fieldByName()[field.name] = field;
    }
  }
  static getCriteriaFor(items) {
    if (items == null)
      return [""];
    var inParens = false;
    var currentBlock = [""];
    var isBlockOr = false;
    var criteria = [""];
    for (var i2 = 0; i2 < items.length; i2++) {
      var line = items[i2];
      if (line.leftParen || inParens && line.rightParen) {
        if (!Criteria.isEmpty(currentBlock)) {
          if (inParens)
            currentBlock = Criteria.paren(currentBlock);
          if (isBlockOr)
            criteria = Criteria.or(criteria, currentBlock);
          else
            criteria = Criteria.and(criteria, currentBlock);
          currentBlock = [""];
        }
        inParens = false;
      }
      if (line.leftParen) {
        isBlockOr = line.isOr;
        inParens = true;
      }
      if (line.isOr)
        currentBlock = Criteria.or(currentBlock, line.criteria);
      else
        currentBlock = Criteria.and(currentBlock, line.criteria);
    }
    if (!Criteria.isEmpty(currentBlock)) {
      if (isBlockOr)
        criteria = Criteria.or(criteria, Criteria.paren(currentBlock));
      else
        criteria = Criteria.and(criteria, Criteria.paren(currentBlock));
    }
    return criteria;
  }
  static getDisplayTextFor(items) {
    if (items == null)
      return "";
    var inParens = false;
    var displayText = "";
    for (var i2 = 0; i2 < items.length; i2++) {
      var line = items[i2];
      if (inParens && (line.rightParen || line.leftParen)) {
        displayText += ")";
        inParens = false;
      }
      if (displayText.length > 0) {
        displayText += " " + FilterPanelTexts[line.isOr ? "Or" : "And"] + " ";
      }
      if (line.leftParen) {
        displayText += "(";
        inParens = true;
      }
      displayText += line.displayText;
    }
    if (inParens) {
      displayText += ")";
    }
    return displayText;
  }
  get_fields() {
    return this.fields;
  }
  get_fieldByName() {
    return this.fieldByName;
  }
  get_items() {
    return this.items;
  }
  raiseChanged() {
    this.displayText = null;
    this.changed && this.changed(this, {});
  }
  add_changed(value) {
    this.changed = delegateCombine(this.changed, value);
  }
  remove_changed(value) {
    this.changed = delegateRemove(this.changed, value);
  }
  get_activeCriteria() {
    return _FilterStore.getCriteriaFor(this.items);
  }
  get_displayText() {
    if (this.displayText == null)
      this.displayText = _FilterStore.getDisplayTextFor(this.items);
    return this.displayText;
  }
};
var FilterStore = _FilterStore;
var _mkdel = /* @__PURE__ */ __name((targets) => {
  var delegate = /* @__PURE__ */ __name(function() {
    if (targets.length === 2) {
      return targets[1].apply(targets[0], arguments);
    } else {
      var clone = targets.slice();
      for (var i2 = 0; i2 < clone.length; i2 += 2) {
        if (delegateContains(targets, clone[i2], clone[i2 + 1])) {
          clone[i2 + 1].apply(clone[i2], arguments);
        }
      }
      return null;
    }
  }, "delegate");
  delegate._targets = targets;
  return delegate;
}, "_mkdel");
function delegateCombine(delegate1, delegate2) {
  if (!delegate1) {
    if (!delegate2._targets) {
      return delegate2;
    }
    return delegate2;
  }
  if (!delegate2) {
    if (!delegate1._targets) {
      return delegate1;
    }
    return delegate1;
  }
  var targets1 = delegate1._targets ? delegate1._targets : [null, delegate1];
  var targets2 = delegate2._targets ? delegate2._targets : [null, delegate2];
  return _mkdel(targets1.concat(targets2));
}
__name(delegateCombine, "delegateCombine");
function delegateRemove(delegate1, delegate2) {
  if (!delegate1 || delegate1 === delegate2) {
    return null;
  }
  if (!delegate2) {
    return delegate1;
  }
  var targets = delegate1._targets;
  var object = null;
  var method;
  if (delegate2._targets) {
    object = delegate2._targets[0];
    method = delegate2._targets[1];
  } else {
    method = delegate2;
  }
  for (var i2 = 0; i2 < targets.length; i2 += 2) {
    if (targets[i2] === object && targets[i2 + 1] === method) {
      if (targets.length === 2) {
        return null;
      }
      var t2 = targets.slice();
      t2.splice(i2, 2);
      return _mkdel(t2);
    }
  }
  return delegate1;
}
__name(delegateRemove, "delegateRemove");
function delegateContains(targets, object, method) {
  for (var i2 = 0; i2 < targets.length; i2 += 2) {
    if (targets[i2] === object && targets[i2 + 1] === method) {
      return true;
    }
  }
  return false;
}
__name(delegateContains, "delegateContains");

// src/ui/filtering/filterwidgetbase.ts
var _a32, _b17;
var FilterWidgetBase = class extends (_b17 = Widget, _a32 = Symbol.typeInfo, _b17) {
  static {
    __name(this, "FilterWidgetBase");
  }
  static {
    this[_a32] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.store = new FilterStore([]);
    this.onFilterStoreChanged = () => this.filterStoreChanged();
    this.store.add_changed(this.onFilterStoreChanged);
  }
  destroy() {
    if (this.store) {
      this.store.remove_changed(this.onFilterStoreChanged);
      this.onFilterStoreChanged = null;
      this.store = null;
    }
    super.destroy();
  }
  filterStoreChanged() {
  }
  get_store() {
    return this.store;
  }
  set_store(value) {
    if (this.store !== value) {
      if (this.store != null)
        this.store.remove_changed(this.onFilterStoreChanged);
      this.store = value || new FilterStore([]);
      this.store.add_changed(this.onFilterStoreChanged);
      this.filterStoreChanged();
    }
  }
};

// src/ui/filtering/filterpanel.tsx
var _a33, _b18;
var FilterFieldSelect = class extends (_b18 = ComboboxEditor, _a33 = Symbol.typeInfo, _b18) {
  static {
    __name(this, "FilterFieldSelect");
  }
  static {
    this[_a33] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    for (var field of this.options.fields) {
      this.addOption(field.name, localText(field.title, field.title ?? field.name), field);
    }
  }
  emptyItemText() {
    if (!this.value) {
      return FilterPanelTexts.SelectField;
    }
    return null;
  }
  getComboboxOptions() {
    var opt = super.getComboboxOptions();
    opt.allowClear = false;
    return opt;
  }
};
var _a34, _b19;
var FilterOperatorSelect = class extends (_b19 = ComboboxEditor, _a34 = Symbol.typeInfo, _b19) {
  static {
    __name(this, "FilterOperatorSelect");
  }
  static {
    this[_a34] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    for (var op of this.options.source) {
      var title = op.title ?? FilterPanelTexts.OperatorNames.asTry()[op.key] ?? op.key;
      this.addOption(op.key, title, op);
    }
    if (this.options.source.length && this.options.source[0])
      this.value = this.options.source[0].key;
  }
  emptyItemText() {
    return null;
  }
  getComboboxOptions() {
    var opt = super.getComboboxOptions();
    opt.allowClear = false;
    return opt;
  }
};
var _a35, _b20;
var FilterPanel = class extends (_b20 = FilterWidgetBase, _a35 = Symbol.typeInfo, _b20) {
  static {
    __name(this, "FilterPanel");
  }
  static {
    this[_a35] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("s-FilterPanel'");
    this.rowsDiv = this.findById("Rows");
    this.updateButtons();
  }
  get showInitialLine() {
    return this._showInitialLine;
  }
  set showInitialLine(value) {
    if (this._showInitialLine !== value) {
      this._showInitialLine = value;
      if (this.showInitialLine && !this.rowsDiv.lastElementChild) {
        this.addEmptyRow(false);
      }
    }
  }
  filterStoreChanged() {
    super.filterStoreChanged();
    this.updateRowsFromStore();
  }
  updateRowsFromStore() {
    Fluent(this.rowsDiv).empty();
    var items = this.get_store().get_items();
    for (var item of items) {
      this.addEmptyRow(false);
      var rowDiv = this.rowsDiv.lastElementChild;
      var divl = rowDiv.querySelector("div.l");
      divl.querySelector(".leftparen").classList.toggle("active", !!item.leftParen);
      divl.querySelector(".rightparen").classList.toggle("active", !!item.rightParen);
      var andor = divl.querySelector(".andor");
      andor.classList.toggle("or", !!item.isOr);
      andor.textContent = localText(!!item.isOr ? "Controls.FilterPanel.Or" : "Controls.FilterPanel.And");
      var fieldSelect = getWidgetFrom(rowDiv.querySelector("div.f input.field-select"), FilterFieldSelect);
      fieldSelect.value = item.field;
      this.rowFieldChange(rowDiv);
      var operatorSelect = getWidgetFrom(rowDiv.querySelector(":scope div.o > input.op-select"), FilterOperatorSelect);
      operatorSelect.set_value(item.operator);
      this.rowOperatorChange(rowDiv);
      var filtering = this.getFilteringFor(rowDiv);
      if (filtering != null) {
        filtering.set_operator({ key: item.operator });
        filtering.loadState(item.state);
      }
    }
    if (this.showInitialLine && !this.rowsDiv.childElementCount) {
      this.addEmptyRow(false);
    }
    this.updateParens();
  }
  get showSearchButton() {
    return this._showSearchButton;
  }
  set showSearchButton(value) {
    if (this._showSearchButton !== value) {
      this._showSearchButton = value;
      this.updateButtons();
    }
  }
  renderContents() {
    const id = this.useIdPrefix();
    return jsx(Fragment, { children: [
      jsx("div", { id: id.Rows, class: "filter-lines" }),
      jsx("div", { id: id.Buttons, class: "buttons", children: [
        jsx("button", { id: id.AddButton, type: "button", class: "btn btn-primary add", onClick: (e2) => this.addButtonClick(e2), children: FilterPanelTexts.AddFilter }),
        jsx("button", { id: id.SearchButton, type: "button", class: "btn btn-success search", onClick: (e2) => this.searchButtonClick(e2), ref: (el) => this.searchButton = el, children: FilterPanelTexts.SearchButton }),
        jsx("button", { id: id.ResetButton, type: "button", class: "btn btn-danger reset", onClick: (e2) => this.resetButtonClick(e2), ref: (el) => this.resetButton = el, children: FilterPanelTexts.ResetButton })
      ] }),
      jsx("div", { style: { clear: "both" } })
    ] });
  }
  searchButtonClick(e2) {
    e2.preventDefault();
    this.search();
  }
  get_hasErrors() {
    return !!this.rowsDiv.querySelector(":scope > div.v > span.error");
  }
  search() {
    this.rowsDiv.querySelectorAll(":scope > div.v > span.error").forEach((x) => x.remove());
    var filterLines = [];
    var errorText = null;
    for (var i2 = 0; i2 < this.rowsDiv.children.length; i2++) {
      var row = this.rowsDiv.children[i2];
      var filtering = this.getFilteringFor(row);
      if (filtering == null) {
        continue;
      }
      var field = this.getFieldFor(row);
      var op = getWidgetFrom(row.querySelector("div.o input.op-select"), FilterOperatorSelect).value;
      if (op == null || op.length === 0) {
        errorText = FilterPanelTexts.InvalidOperator;
        break;
      }
      var line = {};
      line.field = field.name;
      line.operator = op;
      var divL = row.querySelector("div.l");
      line.isOr = !!divL.querySelector("a.andor.or");
      line.leftParen = !!row.querySelector("div.l a.leftparen.active");
      line.rightParen = !!row.querySelector("div.l a.rightparen.active");
      filtering.set_operator({ key: op });
      var criteria;
      try {
        criteria = filtering.getCriteria();
      } catch (ex) {
        errorText = ex.message ?? ex.toString();
        break;
      }
      line.criteria = criteria.criteria;
      line.state = filtering.saveState();
      line.displayText = criteria.displayText;
      filterLines.push(line);
    }
    if (errorText != null) {
      row.querySelector("div.v").appendChild(jsx("span", { class: "error", title: errorText }));
      row.querySelector("div.v input")?.focus();
      return;
    }
    var items = this.get_store().get_items();
    items.length = 0;
    items.push.apply(items, filterLines);
    this.get_store().raiseChanged();
  }
  addButtonClick(e2) {
    this.addEmptyRow(true);
    e2.preventDefault();
  }
  resetButtonClick(e2) {
    e2.preventDefault();
    if (this.updateStoreOnReset) {
      if (this.get_store().get_items().length > 0) {
        this.get_store().get_items().length = 0;
        this.get_store().raiseChanged();
      }
    }
    Fluent(this.rowsDiv).empty();
    this.updateButtons();
    if (this.showInitialLine) {
      this.addEmptyRow(false);
    }
  }
  findEmptyRow() {
    var result = null;
    Array.from(this.rowsDiv.children).forEach(function(row) {
      var fieldInput = row.querySelector("div.f input.field-select");
      if (!fieldInput)
        return;
      var val = fieldInput.value;
      if (!val) {
        result = row;
        return false;
      }
    });
    return result;
  }
  addEmptyRow(popupField) {
    const emptyRow = this.findEmptyRow();
    if (emptyRow != null) {
      const fieldSelect2 = emptyRow.querySelector("input.field-select");
      fieldSelect2?.focus();
      popupField && Combobox.getInstance(fieldSelect2)?.openDropdown();
      return emptyRow;
    }
    const boundThis = bindThis(this);
    const isLastRowOr = !!this.rowsDiv.lastElementChild?.querySelector("a.andor.or");
    const fieldSelect = jsx("input", { class: "field-select", type: "hidden" });
    const row = this.rowsDiv.appendChild(
      jsx("div", { class: "filter-line", children: [
        jsx(
          "a",
          {
            class: "delete",
            title: FilterPanelTexts.RemoveField,
            onClick: boundThis.deleteRowClick,
            children: jsx("span", {})
          }
        ),
        jsx("div", { class: "l", hidden: true, children: [
          jsx("a", { class: "rightparen", href: "#", onClick: boundThis.leftRightParenClick, children: ")" }),
          jsx(
            "a",
            {
              class: ["andor", isLastRowOr && "or"],
              href: "#",
              title: FilterPanelTexts.ChangeAndOr,
              onClick: boundThis.andOrClick,
              children: FilterPanelTexts[isLastRowOr ? "Or" : "And"]
            }
          ),
          jsx("a", { class: "leftparen", href: "#", onClick: boundThis.leftRightParenClick, children: "(" })
        ] }),
        jsx("div", { class: "f", children: fieldSelect }),
        jsx("div", { class: "o" }),
        jsx("div", { class: "v" })
      ] })
    );
    new FilterFieldSelect({ element: fieldSelect, fields: this.get_store().get_fields() }).changeSelect2(bindThis(this).onRowFieldChange);
    this.updateButtons();
    fieldSelect.focus();
    popupField && Combobox.getInstance(fieldSelect)?.openDropdown();
    return row;
  }
  onRowFieldChange(e2) {
    var row = e2.target.closest("div.filter-line");
    this.rowFieldChange(row);
    row.querySelector("div.o input.op-select")?.focus();
  }
  rowFieldChange(row) {
    delete row.__Filtering;
    this.removeFiltering(row);
    this.populateOperatorList(row);
    this.rowOperatorChange(row);
    this.updateParens();
    this.updateButtons();
  }
  removeFiltering(row) {
    delete row.__Filtering;
    delete row.__FilteringField;
  }
  populateOperatorList(row) {
    var opDiv = row.querySelector("div.o");
    Fluent(opDiv).empty();
    var filtering = this.getFilteringFor(row);
    if (filtering == null)
      return;
    const hidden = opDiv.appendChild(jsx("input", { type: "hidden", class: "op-select" }));
    var operators = filtering.getOperators();
    var opSelect = new FilterOperatorSelect({ element: hidden, source: operators });
    opSelect.changeSelect2(bindThis(this).onRowOperatorChange);
  }
  getFieldFor(row) {
    if (!row) {
      return null;
    }
    var select = getWidgetFrom(row.querySelector("div.f input.field-select"), FilterFieldSelect);
    if (!select || !select.value) {
      return null;
    }
    return this.get_store().get_fieldByName()[select.get_value()];
  }
  getFilteringFor(row) {
    var field = this.getFieldFor(row);
    if (field == null)
      return null;
    var filtering = row.__Filtering;
    if (filtering != null)
      return filtering;
    var filteringType = FilteringTypeRegistry.get(
      field.filteringType ?? "String"
    );
    var editorDiv = row.querySelector("div.v");
    filtering = new filteringType(field.filteringParams ?? {});
    ReflectionOptionsSetter.set(filtering, field.filteringParams);
    filtering.set_container(editorDiv);
    filtering.set_field(field);
    row.__Filtering = filtering;
    return filtering;
  }
  onRowOperatorChange(e2) {
    var row = e2.target.closest("div.filter-line");
    this.rowOperatorChange(row);
    row.querySelectorAll("div.v input, div.v textarea, div.v select").forEach((el) => {
      if (Fluent.isVisibleLike(el)) {
        try {
          el.focus();
        } catch {
        }
      }
      return false;
    });
  }
  rowOperatorChange(row) {
    if (!row) {
      return;
    }
    var editorDiv = row.querySelector("div.v");
    Fluent(editorDiv).empty();
    var filtering = this.getFilteringFor(row);
    if (filtering == null)
      return;
    var operatorSelect = getWidgetFrom(row.querySelector("div.o input.op-select"), FilterOperatorSelect);
    if (!operatorSelect.get_value())
      return;
    var ops = filtering.getOperators().filter(function(x) {
      return x.key === operatorSelect.value;
    });
    var op = ops.length > 0 ? ops[0] : null;
    if (op == null)
      return;
    filtering.set_operator(op);
    filtering.createEditor();
  }
  deleteRowClick(e2) {
    e2.preventDefault();
    var row = e2.target.closest("div.filter-line");
    row.remove();
    if (!this.rowsDiv.childElementCount) {
      this.search();
    }
    this.updateParens();
    this.updateButtons();
  }
  updateButtons() {
    Fluent.toggle(this.searchButton, this.rowsDiv.childElementCount >= 1 && this.showSearchButton);
    Fluent.toggle(this.resetButton, this.rowsDiv.childElementCount >= 1);
  }
  andOrClick(e2) {
    e2.preventDefault();
    var andor = e2.target;
    andor.classList.toggle("or");
    andor.textContent = FilterPanelTexts[andor.classList.contains("or") ? "Or" : "And"];
  }
  leftRightParenClick(e2) {
    e2.preventDefault();
    e2.target.classList.toggle("active");
    this.updateParens();
  }
  updateParens() {
    var rows = Array.from(this.rowsDiv.children);
    var inParen = false;
    rows.forEach((row, index) => {
      row.classList.remove("paren-start");
      row.classList.remove("paren-end");
      var divL = row.querySelector("div.l");
      if (!divL)
        return;
      divL.hidden = rows.length === 1 ? true : false;
      if (index === 0)
        divL.querySelectorAll("a.rightparen, a.andor").forEach((el) => el.hidden = true);
      else {
        divL.hidden = false;
        divL.querySelectorAll("a.lefparen, a.andor").forEach((el) => el.hidden = false);
      }
      var lp = divL.querySelector("a.leftparen");
      var rp = divL.querySelector("a.rightparen");
      if (rp.classList.contains("active") && inParen) {
        inParen = false;
        if (index > 0) {
          rows[index - 1].classList.add("paren-end");
        }
      }
      if (lp.classList.contains("active")) {
        inParen = true;
        if (index > 0) {
          rows[index - 1].classList.add("paren-start");
        }
      }
    });
  }
};

// src/ui/filtering/filterdialog.tsx
var _a36, _b21;
var FilterDialog = class extends (_b21 = BaseDialog, _a36 = Symbol.typeInfo, _b21) {
  static {
    __name(this, "FilterDialog");
  }
  static {
    this[_a36] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.filterPanel = new FilterPanel({ element: this.findById("FilterPanel") });
    this.filterPanel.showInitialLine = true;
    this.filterPanel.showSearchButton = false;
    this.filterPanel.updateStoreOnReset = false;
    this.dialogTitle = FilterPanelTexts.DialogTitle;
  }
  get_filterPanel() {
    return this.filterPanel;
  }
  renderContents() {
    return jsx("div", { id: this.useIdPrefix().FilterPanel });
  }
  getDialogOptions() {
    var opt = super.getDialogOptions();
    opt.fullScreen = "lg-down";
    return opt;
  }
  getDialogButtons() {
    return [
      okDialogButton({
        click: /* @__PURE__ */ __name((e2) => {
          this.filterPanel.search();
          if (this.filterPanel.get_hasErrors()) {
            e2.preventDefault();
            notifyError(FilterPanelTexts.FixErrorsMessage, "", null);
            return;
          }
        }, "click")
      }),
      cancelDialogButton()
    ];
  }
};

// src/ui/filtering/filterdisplaybar.tsx
var _a37, _b22;
var FilterDisplayBar = class extends (_b22 = FilterWidgetBase, _a37 = Symbol.typeInfo, _b22) {
  static {
    __name(this, "FilterDisplayBar");
  }
  static {
    this[_a37] = this.registerClass(nsSerenity);
  }
  renderContents() {
    var openFilterDialog = /* @__PURE__ */ __name((e2) => {
      e2.preventDefault();
      var dialog = new FilterDialog({});
      dialog.get_filterPanel().set_store(this.get_store());
      dialog.dialogOpen(null);
    }, "openFilterDialog");
    return jsx("div", { children: [
      jsx("a", { class: "reset", title: FilterPanelTexts.ResetFilterHint, onClick: (e2) => {
        e2.preventDefault();
        this.get_store().get_items().length = 0;
        this.get_store().raiseChanged();
      } }),
      jsx("a", { class: "edit", onClick: openFilterDialog, children: FilterPanelTexts.EditFilter }),
      jsx("div", { class: "current", children: [
        jsx("span", { class: "cap", children: FilterPanelTexts.EffectiveFilter }),
        jsx("a", { class: "txt", onClick: openFilterDialog })
      ] })
    ] });
  }
  filterStoreChanged() {
    super.filterStoreChanged();
    var displayText = this.get_store().get_displayText()?.trim() || null;
    this.element.findFirst(".current").toggle(displayText != null);
    this.element.findFirst(".reset").toggle(displayText != null);
    if (displayText == null)
      displayText = FilterPanelTexts.EffectiveEmpty;
    this.element.findFirst(".txt").text("[" + displayText + "]");
  }
  static createToolButton(opt) {
    return {
      hint: FilterPanelTexts.EditFilter,
      action: "edit-filter",
      cssClass: "edit-filter-button",
      icon: faIcon("filter"),
      ...opt
    };
  }
};

// src/ui/helpers/editlink.tsx
function EditLink(props) {
  const cssClass = ["s-EditLink"];
  if (props.itemType) {
    cssClass.push(`s-${replaceAll(props.itemType, ".", "-")}-Link`);
  }
  if (props.cssClass) {
    cssClass.push(props.cssClass);
  }
  return jsx(
    "a",
    {
      class: cssClass,
      href: "#" + replaceAll(props.itemType ?? "", ".", "-") + "/" + (props.itemId ?? ""),
      "data-item-type": props.itemType,
      "data-item-id": props.itemId,
      tabindex: props.tabindex ?? props.tabIndex,
      children: props.children
    }
  );
}
__name(EditLink, "EditLink");
var skipEditLinkFormatPurposes = /* @__PURE__ */ new Set([
  "excel-export",
  "group-header",
  "group-totals",
  "grand-totals",
  "header-filter",
  "pdf-export",
  "print"
]);

// src/ui/helpers/lazyloadhelper.ts
var LazyLoadHelper;
((LazyLoadHelper2) => {
  LazyLoadHelper2.executeOnceWhenShown = executeOnceWhenVisible;
  LazyLoadHelper2.executeEverytimeWhenShown = executeEverytimeWhenVisible;
})(LazyLoadHelper || (LazyLoadHelper = {}));

// src/ui/helpers/propertyitemcolumnconverter.tsx
var PropertyItemColumnConverter;
((PropertyItemColumnConverter2) => {
  function toColumns(items) {
    var result = [];
    if (items == null) {
      return result;
    }
    for (var i2 = 0; i2 < items.length; i2++) {
      result.push(PropertyItemColumnConverter2.toColumn(items[i2]));
    }
    return result;
  }
  PropertyItemColumnConverter2.toColumns = toColumns;
  __name(toColumns, "toColumns");
  function toColumn(item) {
    var result = {
      field: item.unbound ? null : item.name,
      id: item.unbound ? item.name : null,
      sourceItem: item,
      cssClass: item.cssClass,
      headerCssClass: item.headerCssClass,
      sortable: item.sortable !== false,
      sortOrder: item.sortOrder ?? 0,
      width: item.width != null ? item.width : 80,
      summaryType: item.summaryType,
      minWidth: item.minWidth ?? 30,
      maxWidth: item.maxWidth == null || item.maxWidth === 0 ? null : item.maxWidth,
      resizable: item.resizable == null || !!item.resizable,
      togglable: item.allowHide === false ? false : void 0
    };
    result.visible = item.visible !== false && item.filterOnly !== true && (item.readPermission == null || Authorization.hasPermission(item.readPermission));
    var name = tryGetText(item.title);
    if (name == null)
      name = item.title;
    result.name = name;
    if (item.alignment != null && item.alignment.length > 0) {
      if (result.cssClass) {
        result.cssClass += " align-" + item.alignment;
      } else {
        result.cssClass = "align-" + item.alignment;
      }
    }
    if (item.pin != null && item.pin !== false) {
      result.frozen = item.pin === "end" ? "end" : "start";
    }
    if (!item.formatterType)
      return result;
    const formatterType = isPromiseLike(item.formatterType) || typeof item.formatterType === "function" ? item.formatterType : FormatterTypeRegistry.getOrLoad(item.formatterType);
    const then = /* @__PURE__ */ __name((formatterType2) => {
      var formatter = new formatterType2(item.formatterParams ?? {});
      if (item.formatterParams != null) {
        ReflectionOptionsSetter.set(formatter, item.formatterParams);
      }
      formatter?.initializeColumn?.(result);
      result.format = (ctx) => formatter.format(ctx);
    }, "then");
    if (isPromiseLike(formatterType)) {
      result.format = (ctx) => {
        if (ctx.row != null && ctx.cell != null && ctx.grid) {
          const grid = ctx.grid;
          const row = ctx.row;
          const cell = ctx.cell;
          formatterType.then(() => {
            grid.updateCell?.(row, cell);
          });
        }
        return "";
      };
      formatterType.then(then);
    } else {
      then(formatterType);
    }
    return result;
  }
  PropertyItemColumnConverter2.toColumn = toColumn;
  __name(toColumn, "toColumn");
})(PropertyItemColumnConverter || (PropertyItemColumnConverter = {}));

// src/ui/helpers/slickformatting.tsx
import { applyFormatterResultToCellNode as applyFormatterResultToCellNode2 } from "@serenity-is/sleekgrid";
var SlickFormatting;
((SlickFormatting2) => {
  function itemLink(itemType, idField, getText, cssClass, encode = true) {
    return function(ctx) {
      let fmtResult;
      if (getText == null) {
        encode = true;
        fmtResult = ctx.value;
      } else {
        fmtResult = getText(ctx);
      }
      fmtResult = fmtResult instanceof Node ? fmtResult : encode ? ctx.escape(fmtResult) : fmtResult ?? "";
      if (ctx.item?.__nonDataRow || ctx.purpose && skipEditLinkFormatPurposes.has(ctx.purpose)) {
        return fmtResult;
      }
      const itemId = ctx.item?.[idField];
      const extraClass = cssClass == null ? "" : cssClass(ctx);
      const encItemType = encodeURIComponent(replaceAll(itemType, ".", "-"));
      const encItemId = itemId != null ? encodeURIComponent(itemId.toString()) : null;
      const link = jsx(
        "a",
        {
          class: [`s-EditLink s-${replaceAll(itemType, ".", "-")}Link`, extraClass],
          href: itemId != null ? "#" + encItemType + "/" + encItemId : null,
          "data-item-type": itemType,
          "data-item-id": itemId
        }
      );
      applyFormatterResultToCellNode2(ctx, fmtResult, link, contentOnly);
      return link;
    };
  }
  SlickFormatting2.itemLink = itemLink;
  __name(itemLink, "itemLink");
  function treeToggle(getView, getId, formatter) {
    return function(ctx) {
      const text2 = formatter(ctx);
      const view = getView();
      const indent = ctx.item._indent ?? 0;
      const spacer = jsx("span", { class: "s-TreeIndent", style: { width: 15 * indent + "px" } });
      const toggle = jsx("span", { class: "s-TreeToggle" });
      const id = getId(ctx.item);
      const idx = view.getIdxById(id);
      let next = view.getItemByIdx(idx + 1);
      if (next != null) {
        var nextIndent = next._indent ?? 0;
        if (nextIndent > indent) {
          if (!!!!ctx.item._collapsed) {
            toggle.classList.add("s-TreeExpand");
          } else {
            toggle.classList.add("s-TreeCollapse");
          }
        }
      }
      if (ctx.enableHtmlRendering && typeof text2 === "string" && text2.length) {
        return spacer.outerHTML + toggle.outerHTML + text2;
      } else {
        return jsx(Fragment, { children: [
          spacer,
          toggle,
          text2
        ] });
      }
    };
  }
  SlickFormatting2.treeToggle = treeToggle;
  __name(treeToggle, "treeToggle");
})(SlickFormatting || (SlickFormatting = {}));
var contentOnly = { contentOnly: true };

// src/ui/helpers/slickhelper.tsx
var SlickHelper;
((SlickHelper2) => {
  function setDefaults(columns, localTextPrefix) {
    for (var col of columns) {
      col.sortable = col.sortable != null ? col.sortable : true;
      var id = col.id;
      if (id == null) {
        id = col.field;
      }
      col.id = id;
      if (localTextPrefix != null && col.id != null && (col.name == null || col.name.startsWith("~"))) {
        var key = col.name != null ? col.name.substring(1) : col.id;
        col.name = localText(localTextPrefix + key);
      }
    }
    return columns;
  }
  SlickHelper2.setDefaults = setDefaults;
  __name(setDefaults, "setDefaults");
})(SlickHelper || (SlickHelper = {}));

// src/ui/helpers/dateediting.ts
function dateInputChangeHandler(e2) {
  if (Culture.dateOrder !== "dmy" && Culture.dateOrder !== "mdy")
    return;
  var input = e2.target;
  if (input?.getAttribute("type") === "date")
    return;
  var val = input.value ?? "";
  if (val.length >= 6 && val.length <= 8 && /^[0-9]*$/g.test(val)) {
    input.value = val.substring(0, 2) + Culture.dateSeparator + val.substring(2, 4) + Culture.dateSeparator + val.substring(4);
    input.value = val.substring(0, 2) + Culture.dateSeparator + val.substring(2, 4) + Culture.dateSeparator + val.substring(4);
  }
  val = input.value ?? "";
  if (val.length >= 5) {
    var d2 = parseDate(val);
    if (d2 && !isNaN(d2.valueOf()))
      input.value = formatDate(d2, null) ?? "";
  }
}
__name(dateInputChangeHandler, "dateInputChangeHandler");
function isDigit(c2) {
  return c2 >= "0" && c2 <= "9";
}
__name(isDigit, "isDigit");
function dateInputKeyupHandler(e2) {
  var input = e2.target;
  if (input?.getAttribute("type") == "date") {
    return;
  }
  if (input.getAttribute("readonly") != null || input.getAttribute("disabled") != null)
    return;
  var val = input.value ?? "";
  if (!!(val.length === 0 || input.selectionEnd !== val.length))
    return;
  if (val.indexOf(Culture.dateSeparator + Culture.dateSeparator) >= 0) {
    input.value = val.split(Culture.dateSeparator + Culture.dateSeparator).join(Culture.dateSeparator);
    return;
  }
  if (e2.key === "slash" || e2.key === "NumpadDivide" || e2.key === "/") {
    if (val[val.length - 1] !== Culture.dateSeparator)
      return;
    switch (val.length) {
      case 2: {
        if (isDigit(val[0])) {
          val = "0" + val;
          break;
        } else {
          return;
        }
      }
      case 4: {
        if (isDigit(val[0]) && isDigit(val[2]) && val[1] == Culture.dateSeparator) {
          val = "0" + val[0] + Culture.dateSeparator + "0" + val[2] + Culture.dateSeparator;
          break;
        } else {
          return;
        }
      }
      case 5: {
        if (isDigit(val[0]) && isDigit(val[2]) && isDigit(val[3]) && val[1] === Culture.dateSeparator) {
          val = "0" + val;
          break;
        } else if (isDigit(val[0]) && isDigit(val[1]) && isDigit(val[3]) && // 12/4/ -> 12/04/
        val[2] === Culture.dateSeparator) {
          val = val[0] + val[1] + Culture.dateSeparator + "0" + val[3] + Culture.dateSeparator;
          break;
        } else {
          break;
        }
      }
      default: {
        return;
      }
    }
    input.value = val ?? "";
  }
  if (Culture.dateOrder !== "dmy")
    return;
  if (!(val.length < 6 && (e2.key >= "0" && e2.key <= "9" || e2.key >= "Numpad0" && e2.key <= "Numpad9") && isDigit(val[val.length - 1])))
    return;
  switch (val.length) {
    case 1:
      if (val.charCodeAt(0) <= 51)
        return;
      val = "0" + val;
      break;
    case 2:
      if (!isDigit(val[0]))
        return;
      break;
    case 3:
      if (!isDigit(val[0]) || val[1] !== Culture.dateSeparator || val.charCodeAt(2) <= 49) {
        return;
      }
      val = "0" + val[0] + Culture.dateSeparator + "0" + val[2];
      break;
    case 4:
      if (val[1] == Culture.dateSeparator) {
        if (!isDigit(val[0]) || !isDigit(val[2])) {
          return;
        }
        val = "0" + val;
        break;
      } else if (val[2] == Culture.dateSeparator) {
        if (!isDigit(val[0]) || !isDigit(val[1]) || val.charCodeAt(3) <= 49) {
          return;
        }
        val = val[0] + val[1] + Culture.dateSeparator + "0" + val[3];
        break;
      } else
        return;
    case 5:
      if (val[2] !== Culture.dateSeparator || !isDigit(val[0]) || !isDigit(val[1]) || !isDigit(val[3])) {
        return;
      }
      break;
    default:
      return;
  }
  input.value = val + Culture.dateSeparator;
}
__name(dateInputKeyupHandler, "dateInputKeyupHandler");
function flatPickrTrigger(input) {
  var button = document.createElement("button");
  button.type = "button";
  button.classList.add("ui-datepicker-trigger");
  const listener = /* @__PURE__ */ __name(() => {
    if (!input.classList.contains("readonly") && input.getAttribute("readonly") == null) {
      input._flatpickr?.open?.();
      input._flatpickr?.calendarContainer?.focus?.();
    }
  }, "listener");
  button.addEventListener("click", listener);
  const removeListener2 = /* @__PURE__ */ __name(() => button.removeEventListener("click", listener), "removeListener");
  addDisposingListener(button, removeListener2);
  addDisposingListener(input, removeListener2);
  return button;
}
__name(flatPickrTrigger, "flatPickrTrigger");
function jQueryDatepickerZIndexWorkaround(input) {
  let $2 = getjQuery();
  if (!$2)
    return;
  let dialog = input?.closest(".ui-dialog");
  if (!dialog)
    return;
  var dialogIndex = parseInt(getComputedStyle(dialog).zIndex, 10);
  if (dialogIndex == null || isNaN(dialogIndex))
    return;
  setTimeout(() => {
    let widget = $2(input).datepicker("widget");
    if (!widget || !widget.length)
      return;
    let zIndex = parseInt(widget.css("z-index"));
    if (!isNaN(zIndex) && zIndex <= dialogIndex)
      widget.css("z-index", dialogIndex + 1);
  }, 0);
}
__name(jQueryDatepickerZIndexWorkaround, "jQueryDatepickerZIndexWorkaround");
function jQueryDatepickerInitialization() {
  let $2 = getjQuery();
  if (!$2?.datepicker?.regional?.en)
    return false;
  let order = Culture.dateOrder;
  let s2 = Culture.dateSeparator;
  let culture = typeof document === "undefined" ? "en" : (document.documentElement.lang || "en").toLowerCase();
  if (!$2.datepicker.regional[culture]) {
    culture = culture.split("-")[0];
    if (!$2.datepicker.regional[culture]) {
      culture = "en";
    }
  }
  $2.datepicker.setDefaults($2.datepicker.regional["en"]);
  $2.datepicker.setDefaults($2.datepicker.regional[culture]);
  $2.datepicker.setDefaults({
    dateFormat: order == "mdy" ? "mm" + s2 + "dd" + s2 + "yy" : order == "ymd" ? "yy" + s2 + "mm" + s2 + "dd" : "dd" + s2 + "mm" + s2 + "yy",
    buttonText: "",
    buttonImage: null,
    buttonImageOnly: false,
    showOn: "both",
    showButtonPanel: true,
    changeMonth: true,
    changeYear: true
  });
  return true;
}
__name(jQueryDatepickerInitialization, "jQueryDatepickerInitialization");

// src/ui/editors/dateeditor.tsx
var _a38, _b23;
var DateEditor = class _DateEditor extends (_b23 = EditorWidget, _a38 = Symbol.typeInfo, _b23) {
  static {
    __name(this, "DateEditor");
  }
  static {
    this[_a38] = this.registerEditor(nsSerenity, [IStringValue, IReadOnly]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    let $2 = getjQuery();
    if (typeof flatpickr !== "undefined" && (_DateEditor.useFlatpickr || !$2?.fn?.datepicker)) {
      var options = this.getFlatpickrOptions(this.domNode);
      flatpickr(this.domNode, options);
      this.createFlatPickrTrigger();
    } else if ($2?.fn?.datepicker) {
      $2(this.domNode).datepicker({
        showOn: "button",
        beforeShow: /* @__PURE__ */ __name((inp, inst) => {
          if (this.get_readOnly())
            return false;
          _DateEditor.uiPickerZIndexWorkaround(this.domNode);
          return true;
        }, "beforeShow"),
        yearRange: this.options?.yearRange ?? "-100:+50"
      });
    } else {
      this.domNode.setAttribute("type", "date");
    }
    Fluent.on(this.domNode, "keydown." + this.uniqueName, (e2) => {
      if (this.get_readOnly() || e2.key !== " ") {
        return;
      }
      e2.preventDefault();
      if (this.get_valueAsDate() != today()) {
        this.setToToday(true);
      }
    });
    Fluent.on(this.domNode, "keyup." + this.uniqueName, (e2) => {
      if (this.get_readOnly()) {
        return;
      }
      _DateEditor.dateInputKeyup(e2);
    });
    Fluent.on(this.domNode, "change." + this.uniqueName, _DateEditor.dateInputChange);
    addValidationRule(this.domNode, () => {
      var value = this.get_value();
      if (!value) {
        return null;
      }
      if (this.get_minValue() && Invariant.stringCompare(formatDate(value, "yyyy-MM-dd"), formatDate(this.get_minValue(), "yyyy-MM-dd")) < 0) {
        return stringFormat(FormValidationTexts.MinDate, formatDate(this.get_minValue(), null));
      }
      if (this.get_maxValue() && Invariant.stringCompare(formatDate(value, "yyyy-MM-dd"), formatDate(this.get_maxValue(), "yyyy-MM-dd")) > 0) {
        return stringFormat(FormValidationTexts.MaxDate, formatDate(this.get_maxValue(), null));
      }
      return null;
    });
    this.set_sqlMinMax(true);
  }
  setToToday(triggerChange) {
    this.set_valueAsDate(today());
    triggerChange && Fluent.trigger(this.domNode, "change");
  }
  destroy() {
    if (this.domNode && this.domNode._flatpickr) {
      this.domNode._flatpickr.destroy?.();
      delete this.domNode._flatpickr;
    }
    super.destroy();
  }
  get_value() {
    var value = this.domNode.value?.trim();
    if (!value) {
      return null;
    }
    return formatDate(value, "yyyy-MM-dd");
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (value == null) {
      this.domNode.value = "";
    } else if (value.toLowerCase() === "today" || value.toLowerCase() === "now") {
      this.domNode.value = formatDate(today(), this.domNode.getAttribute("type") === "date" ? "yyyy-MM-dd" : null);
    } else {
      this.domNode.value = formatDate(value, this.domNode.getAttribute("type") === "date" ? "yyyy-MM-dd" : null);
    }
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_valueAsDate() {
    if (!this.get_value())
      return null;
    return parseISODateTime(this.get_value());
  }
  get valueAsDate() {
    return this.get_valueAsDate();
  }
  set_valueAsDate(value) {
    if (value == null) {
      this.set_value(null);
    }
    this.set_value(formatDate(value, "yyyy-MM-dd"));
  }
  set valueAsDate(v2) {
    this.set_valueAsDate(v2);
  }
  get_readOnly() {
    return this.domNode.classList.contains("readonly") || this.domNode.getAttribute("readonly") != null;
  }
  set_readOnly(value) {
    if (value !== this.get_readOnly()) {
      setElementReadOnly(this.domNode, value);
      let trg = this.element.nextSibling(".ui-datepicker-trigger").getNode();
      trg && (trg.style.opacity = value ? "0.1" : "1");
    }
  }
  get_minValue() {
    return this.options.minValue;
  }
  set_minValue(value) {
    this.options.minValue = value;
  }
  get_maxValue() {
    return this.options.maxValue;
  }
  set_maxValue(value) {
    this.options.maxValue = value;
  }
  get_minDate() {
    return parseISODateTime(this.get_minValue());
  }
  set_minDate(value) {
    this.set_minValue(formatDate(value, "yyyy-MM-dd"));
  }
  get_maxDate() {
    return parseISODateTime(this.get_maxValue());
  }
  set_maxDate(value) {
    this.set_maxValue(formatDate(value, "yyyy-MM-dd"));
  }
  get_sqlMinMax() {
    return this.get_minValue() === "1753-01-01" && this.get_maxValue() === "9999-12-31";
  }
  set_sqlMinMax(value) {
    if (value) {
      this.set_minValue("1753-01-01");
      this.set_maxValue("9999-12-31");
    } else {
      this.set_minValue(null);
      this.set_maxValue(null);
    }
  }
  static {
    this.dateInputChange = /* @__PURE__ */ __name(function(e2) {
      dateInputChangeHandler(e2);
    }, "dateInputChange");
  }
  static dateInputKeyup(e2) {
    dateInputKeyupHandler(e2);
  }
  getFlatpickrOptions(input) {
    var opt = {
      clickOpens: false,
      allowInput: true,
      dateFormat: Culture.dateOrder.split("").join(Culture.dateSeparator).replace("y", "Y"),
      onChange: /* @__PURE__ */ __name(() => {
      }, "onChange"),
      disable: [
        (d2) => this.get_readOnly() && formatDate(d2, "d") != formatDate(this.domNode?.value, "d")
      ]
    };
    if (this.domNode.closest(".modal"))
      opt.appendTo = this.domNode.closest(".modal");
    else {
      setTimeout(() => {
        var modal = this.domNode?.closest(".modal");
        if (modal && !opt.static && !opt.appendTo && this.domNode && this.domNode._flatpickr && this.domNode._flatpickr.calendarContainer && this.domNode._flatpickr.calendarContainer.parentElement !== modal) {
          modal.appendChild(this.domNode._flatpickr.calendarContainer);
        }
      }, 0);
    }
    return opt;
  }
  createFlatPickrTrigger() {
    if (!this.domNode)
      return;
    return Fluent(flatPickrTrigger(this.domNode)).insertAfter(this.domNode).getNode();
  }
  static uiPickerZIndexWorkaround(el) {
    let input = isArrayLike(el) ? el[0] : el;
    if (!input)
      return;
    jQueryDatepickerZIndexWorkaround(input);
  }
};
!jQueryDatepickerInitialization() && Fluent.ready(jQueryDatepickerInitialization);

// src/ui/editors/datetimeeditor.tsx
var _a39, _b24;
var DateTimeEditor = class _DateTimeEditor extends (_b24 = EditorWidget, _a39 = Symbol.typeInfo, _b24) {
  static {
    __name(this, "DateTimeEditor");
  }
  static {
    this[_a39] = this.registerEditor(nsSerenity, [IStringValue, IReadOnly]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("s-DateTimeEditor");
    let $2 = getjQuery();
    if (this.options.inputOnly) {
      this.domNode.classList.add("dateTimeQ");
    } else if (typeof flatpickr !== "undefined" && (DateEditor.useFlatpickr || !$2?.fn?.datepicker || this.options.seconds)) {
      this.domNode.classList.add("dateTimeQ");
      flatpickr(this.domNode, this.getFlatpickrOptions());
      this.createFlatPickrTrigger();
    } else if ($2?.fn?.datepicker) {
      this.domNode.classList.add("dateQ");
      let $3 = getjQuery();
      $3(this.domNode).datepicker({
        showOn: "button",
        beforeShow: /* @__PURE__ */ __name(() => {
          if (this.get_readOnly())
            return false;
          DateEditor.uiPickerZIndexWorkaround(this.domNode);
          return true;
        }, "beforeShow"),
        yearRange: this.options.yearRange ?? "-100:+50"
      });
      Fluent.on(this.domNode, "change." + this.uniqueName, (e2) => {
        this.lastSetValue = null;
        DateEditor.dateInputChange(e2);
      });
      this.time = jsx("select", { class: "editor s-DateTimeEditor time" });
      var after = this.domNode.nextElementSibling;
      if (after?.classList.contains("ui-datepicker-trigger")) {
        Fluent(this.time).insertAfter(after);
      } else {
        after = this.domNode.previousElementSibling;
        if (after?.classList.contains("ui-datepicker-trigger")) {
          Fluent(this.time).insertBefore(after);
        } else {
          Fluent(this.time).insertAfter(this.domNode);
        }
      }
      Fluent.on(this.time, "change", () => {
        this.lastSetValue = null;
        Fluent.trigger(this.domNode, "change");
      });
      var timeOpt = _DateTimeEditor.getTimeOptions(
        this.options.startHour ?? 0,
        0,
        this.options.endHour ?? 23,
        59,
        this.options.intervalMinutes ?? 5
      );
      for (var t2 of timeOpt) {
        addOption(this.time, t2, t2);
      }
      addValidationRule(this.domNode, (e1) => {
        var value = this.get_value();
        if (!value) {
          return null;
        }
        if (this.get_minValue() && Invariant.stringCompare(value, this.get_minValue()) < 0) {
          return stringFormat(FormValidationTexts.MinDate, formatDate(this.get_minValue(), null));
        }
        if (this.get_maxValue() && Invariant.stringCompare(value, this.get_maxValue()) > 0) {
          return stringFormat(FormValidationTexts.MaxDate, formatDate(this.get_maxValue(), null));
        }
        return null;
      });
    } else {
      this.domNode.setAttribute("type", "datetime");
      this.domNode.classList.add("dateTimeQ");
    }
    Fluent.on(this.domNode, "keydown." + this.uniqueName, (e2) => {
      if (this.get_readOnly() || e2.key !== " ")
        return;
      var input = this.domNode;
      if (input && !input.value?.trim()?.length || input.selectionStart === 0 && input.selectionEnd === input.value?.length) {
        e2.preventDefault();
        this.setToNow(true);
      }
    });
    Fluent.on(this.domNode, "keyup." + this.uniqueName, (e2) => {
      if (this.get_readOnly())
        return;
      var before = this.domNode.value;
      DateEditor.dateInputKeyup(e2);
      if (before != this.domNode.value)
        this.lastSetValue = null;
    });
    this.set_sqlMinMax(true);
    if (!this.options.inputOnly) {
      (this.time ?? this.domNode).after(
        jsx("i", { class: "inplace-button inplace-now", title: this.getInplaceNowText(), onClick: () => {
          if (this.get_readOnly())
            return;
          this.setToNow(true);
        }, children: jsx("b", {}) })
      );
    }
  }
  setToNow(triggerChange) {
    this.lastSetValue = null;
    this.set_valueAsDate(/* @__PURE__ */ new Date());
    triggerChange && Fluent.trigger(this.domNode, "change");
  }
  destroy() {
    if (this.domNode && this.domNode._flatpickr && this.domNode._flatpickr.destroy) {
      this.domNode._flatpickr.destroy();
    }
    super.destroy();
  }
  getFlatpickrOptions() {
    var opt = {
      clickOpens: false,
      allowInput: true,
      enableTime: true,
      time_24hr: true,
      enableSeconds: !!this.options.seconds,
      minuteIncrement: this.options.intervalMinutes ?? 5,
      dateFormat: Culture.dateOrder.split("").join(Culture.dateSeparator).replace("y", "Y") + " H:i" + (this.options.seconds ? ":S" : ""),
      onChange: /* @__PURE__ */ __name(() => {
        this.lastSetValue = null;
      }, "onChange"),
      disable: [
        (d2) => this.get_readOnly() && formatDate(d2, "d") != formatDate(this.domNode?.value, "d")
      ]
    };
    if (this.domNode.closest(".modal"))
      opt.appendTo = this.domNode.closest(".modal");
    else {
      setTimeout(() => {
        var modal = this.domNode?.closest(".modal");
        if (modal && !opt.static && !opt.appendTo && this.domNode && this.domNode._flatpickr && this.domNode._flatpickr.calendarContainer && this.domNode._flatpickr.calendarContainer.parentElement !== modal) {
          modal.appendChild(this.domNode._flatpickr.calendarContainer);
        }
      }, 0);
    }
    return opt;
  }
  createFlatPickrTrigger() {
    if (!this.domNode)
      return;
    return Fluent(flatPickrTrigger(this.domNode)).insertAfter(this.domNode).getNode();
  }
  get_value() {
    var value = this.domNode.value?.trim();
    if (value != null && value.length === 0) {
      return null;
    }
    var result;
    if (this.time) {
      var datePart = formatDate(value, "yyyy-MM-dd");
      var timePart = this.time.value;
      result = datePart + "T" + timePart + ":00.000";
    } else
      result = formatDate(parseDate(this.domNode.value), "yyyy-MM-ddTHH:mm:ss.fff");
    if (this.options.useUtc)
      result = formatISODateTimeUTC(parseISODateTime(result));
    if (this.lastSetValue != null && this.lastSetValueGet == result)
      return this.lastSetValue;
    return result;
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (!value) {
      this.domNode.value = "";
      this.time && (this.time.value = "00:00");
    } else if (value.toLowerCase() === "today") {
      if (this.time) {
        this.domNode.value = formatDate(today(), null);
        this.time.value = "00:00";
      } else {
        this.domNode.value = formatDate(value, this.getDisplayFormat());
      }
    } else {
      var val = value.toLowerCase() === "now" ? /* @__PURE__ */ new Date() : parseISODateTime(value);
      if (this.time) {
        val = _DateTimeEditor.roundToMinutes(val, this.options.intervalMinutes ?? 5);
        this.domNode.value = formatDate(val, null);
        this.time.value = formatDate(val, "HH:mm");
      } else
        this.domNode.value = formatDate(val, this.getDisplayFormat());
    }
    this.lastSetValue = null;
    if (value && value.toLowerCase() != "today" && value.toLowerCase() != "now") {
      this.lastSetValueGet = this.get_value();
      this.lastSetValue = value;
    }
  }
  getInplaceNowText() {
    return DateTimeEditorTexts.asTry().SetToNow ?? "set to now";
  }
  getDisplayFormat() {
    return this.options.seconds ? Culture.dateTimeFormat : Culture.dateTimeFormat.replace(":ss", "");
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_valueAsDate() {
    if (!this.get_value())
      return null;
    return parseISODateTime(this.get_value());
  }
  get valueAsDate() {
    return this.get_valueAsDate();
  }
  set_valueAsDate(value) {
    if (value == null) {
      this.set_value(null);
    }
    this.set_value(formatDate(value, "yyyy-MM-ddTHH:mm" + (this.options.seconds ? ":ss" : "")));
  }
  set valueAsDate(value) {
    this.set_valueAsDate(value);
  }
  get_minValue() {
    return this.options.minValue;
  }
  set_minValue(value) {
    this.options.minValue = value;
  }
  get_maxValue() {
    return this.options.maxValue;
  }
  set_maxValue(value) {
    this.options.maxValue = value;
  }
  get_minDate() {
    return parseISODateTime(this.get_minValue());
  }
  set_minDate(value) {
    this.set_minValue(formatDate(value, "yyyy-MM-ddTHH:mm:ss"));
  }
  get_maxDate() {
    return parseISODateTime(this.get_maxValue());
  }
  set_maxDate(value) {
    this.set_maxValue(formatDate(value, "yyyy-MM-ddTHH:mm:ss"));
  }
  get_sqlMinMax() {
    return this.get_minValue() === "1753-01-01" && this.get_maxValue() === "9999-12-31";
  }
  set_sqlMinMax(value) {
    if (value) {
      this.set_minValue("1753-01-01");
      this.set_maxValue("9999-12-31");
    } else {
      this.set_minValue(null);
      this.set_maxValue(null);
    }
  }
  get_readOnly() {
    return this.domNode.classList.contains("readonly") || this.domNode.getAttribute("readonly") != null;
  }
  set_readOnly(value) {
    if (value !== this.get_readOnly()) {
      setElementReadOnly(this.domNode, value);
      let trg = this.element.nextSibling(".ui-datepicker-trigger").getNode();
      trg && (trg.style.opacity = value ? "0.1" : "1");
      let now = this.element.nextSibling(".inplace-now").getNode();
      now && (now.style.opacity = value ? "0.1" : "1");
      this.time && setElementReadOnly(this.time, value);
    }
  }
  static roundToMinutes(date, minutesStep) {
    date = new Date(date.getTime());
    var m = trunc(round(date.getMinutes() / minutesStep) * minutesStep);
    date.setMinutes(m);
    date.setSeconds(0);
    date.setMilliseconds(0);
    return date;
  }
  static {
    this.getTimeOptions = /* @__PURE__ */ __name(function(fromHour, fromMin, toHour, toMin, stepMins) {
      var list = [];
      if (toHour >= 23) {
        toHour = 23;
      }
      if (toMin >= 60) {
        toMin = 59;
      }
      var hour = fromHour;
      var min = fromMin;
      while (true) {
        if (hour > toHour || hour === toHour && min > toMin) {
          break;
        }
        var t2 = (hour >= 10 ? "" : "0") + hour + ":" + (min >= 10 ? "" : "0") + min;
        list.push(t2);
        min += stepMins;
        if (min >= 60) {
          min -= 60;
          hour++;
        }
      }
      return list;
    }, "getTimeOptions");
  }
};

// src/ui/editors/selecteditor.ts
var _a40, _b25;
var SelectEditor = class extends (_b25 = ComboboxEditor, _a40 = Symbol.typeInfo, _b25) {
  static {
    __name(this, "SelectEditor");
  }
  static {
    this[_a40] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.updateItems();
  }
  getItems() {
    return this.options.items || [];
  }
  emptyItemText() {
    if (this.options.emptyOptionText) {
      return this.options.emptyOptionText;
    }
    return super.emptyItemText();
  }
  updateItems() {
    var items = this.getItems();
    this.clearItems();
    if (items.length > 0) {
      var isStrings = typeof items[0] === "string";
      for (var item of items) {
        var key = isStrings ? item : item[0];
        var text2 = isStrings ? item : item[1] ?? item[0];
        this.addOption(key, text2, item, false);
      }
    }
  }
};

// src/ui/datagrid/quickfilterbar.tsx
var _a41, _b26;
var QuickFilterBar = class _QuickFilterBar extends (_b26 = Widget, _a41 = Symbol.typeInfo, _b26) {
  static {
    __name(this, "QuickFilterBar");
  }
  static {
    this[_a41] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("quick-filters-bar", "clear");
    var filters = this.options.filters;
    for (var f2 = 0; f2 < filters.length; f2++) {
      var filter = filters[f2];
      this.add(filter);
    }
    this.options.idPrefix = this.options.idPrefix ?? this.uniqueName + "_";
  }
  static {
    this.itemDataMap = /* @__PURE__ */ new WeakMap();
  }
  static getItemData(filterItem) {
    return this.itemDataMap.get(filterItem);
  }
  addSeparator() {
    this.domNode.append(jsx("hr", {}));
  }
  add(opt) {
    if (opt == null)
      throw new Error("QuickFilterBar.add(): 'opt' argument is null!");
    if (opt.separator) {
      this.addSeparator();
    }
    const qfElement = this.domNode.appendChild(jsx("div", { class: "quick-filter-item", "data-qffield": opt.field }));
    var title = localText(opt.title, opt.title);
    if (title == null) {
      title = this.options.getTitle ? this.options.getTitle(opt) : null;
      if (title == null) {
        title = opt.field;
      }
    }
    qfElement.appendChild(jsx("span", { class: "quick-filter-label", children: title }));
    const qfData = {};
    if (opt.displayText != null) {
      qfData.displayText = opt.displayText;
    }
    if (opt.saveState != null) {
      qfData.saveState = opt.saveState;
    }
    if (opt.loadState != null) {
      qfData.loadState = opt.loadState;
    }
    if (qfData.displayText || qfData.saveState || qfData.loadState) {
      _QuickFilterBar.itemDataMap.set(qfElement, qfData);
    }
    if (opt.cssClass) {
      Fluent.addClass(qfElement, opt.cssClass);
    }
    var widget = Widget.create({
      type: opt.type,
      options: {
        element: /* @__PURE__ */ __name((el) => {
          if (opt.field)
            el.setAttribute("id", this.options.idPrefix + opt.field);
          el.setAttribute("placeholder", " ");
          qfElement.append(el);
          if (opt.element != null) {
            opt.element(Fluent(el));
          }
        }, "element"),
        ...opt.options
      }
    });
    opt.init?.(widget);
    var submitHandler = /* @__PURE__ */ __name((request) => {
      if (qfElement.classList.contains("ignore")) {
        return;
      }
      request.EqualityFilter = request.EqualityFilter || {};
      var value = EditorUtils.getValue(widget);
      var active = !!value?.toString();
      if (opt.handler != null) {
        var args = {
          field: opt.field,
          request,
          equalityFilter: request.EqualityFilter,
          value,
          active,
          widget,
          handled: true
        };
        opt.handler(args);
        qfElement.classList.toggle("quick-filter-active", !!args.active);
        if (!args.handled) {
          request.EqualityFilter[opt.field] = value;
        }
      } else {
        request.EqualityFilter[opt.field] = value;
        qfElement.classList.toggle("quick-filter-active", !!active);
      }
    }, "submitHandler");
    widget.changeSelect2((e1) => {
      window.setTimeout(() => this.onChange && this.onChange(e1), 0);
    });
    this.add_submitHandlers(submitHandler);
    Fluent.on(widget.domNode, "cleanup." + this.uniqueName, (x) => {
      this.remove_submitHandlers(submitHandler);
    });
    return widget;
  }
  addDateRange(field, title) {
    return this.add(_QuickFilterBar.dateRange(field, title));
  }
  static dateRange(field, title) {
    var end = null;
    return {
      field,
      type: DateEditor,
      title,
      element: /* @__PURE__ */ __name(function(el) {
        end = new DateEditor({ element: /* @__PURE__ */ __name((el2) => Fluent(el2).insertAfter(el), "element") });
        Fluent.on(end.domNode, "change." + end.uniqueName, () => el.trigger("change"));
        el.after(jsx("span", { class: "range-separator", children: "-" }));
      }, "element"),
      handler: /* @__PURE__ */ __name(function(args) {
        var date1 = parseDate(args.widget.value);
        if (date1) {
          if (isNaN(date1.valueOf())) {
            notifyWarning(FormValidationTexts.DateInvalid, "", null);
            args.widget.domNode.value = "";
            date1 = null;
          } else {
            args.request.Criteria = Criteria.and(
              args.request.Criteria,
              Criteria(args.field).ge(args.widget.value)
            );
          }
        }
        var date2 = parseDate(end.value);
        if (date2) {
          if (isNaN(date2?.valueOf())) {
            notifyWarning(FormValidationTexts.DateInvalid, "", null);
            end.domNode.value = "";
            date2 = null;
          } else {
            var next = new Date(end.valueAsDate.valueOf());
            next.setDate(next.getDate() + 1);
            args.request.Criteria = Criteria.and(
              args.request.Criteria,
              Criteria(args.field).lt(formatDate(next, "yyyy-MM-dd"))
            );
          }
        }
        args.active = !!(date1 || date2);
      }, "handler"),
      displayText: /* @__PURE__ */ __name(function(w2, l2) {
        var v1 = EditorUtils.getDisplayText(w2);
        var v2 = EditorUtils.getDisplayText(end);
        if (!v1 && !v2)
          return null;
        var text1 = l2 + " >= " + v1;
        var text2 = l2 + " <= " + v2;
        if (v1 && v2) {
          return text1 + " " + (FilterPanelTexts.asTry().And ?? "and") + " " + text2;
        } else if (v1) {
          return text1;
        } else {
          return text2;
        }
      }, "displayText"),
      saveState: /* @__PURE__ */ __name(function(w1) {
        return [EditorUtils.getValue(w1), EditorUtils.getValue(end)];
      }, "saveState"),
      loadState: /* @__PURE__ */ __name(function(w2, state) {
        if (state == null || !Array.isArray(state) || state.length !== 2) {
          state = [null, null];
        }
        EditorUtils.setValue(w2, state[0]);
        EditorUtils.setValue(end, state[1]);
      }, "loadState")
    };
  }
  addDateTimeRange(field, title) {
    return this.add(_QuickFilterBar.dateTimeRange(field, title));
  }
  static dateTimeRange(field, title, useUtc) {
    var end = null;
    return {
      field,
      type: DateTimeEditor,
      title,
      element: /* @__PURE__ */ __name(function(el) {
        end = new DateTimeEditor({
          element: /* @__PURE__ */ __name((el2) => Fluent(el2).insertAfter(el), "element"),
          useUtc: useUtc == null ? void 0 : useUtc
        });
        Fluent.on(end.domNode, "change." + end.uniqueName, () => el.trigger("change"));
        el.after(jsx("span", { class: "range-separator", children: "-" }));
      }, "element"),
      init: /* @__PURE__ */ __name(function(w2) {
        Fluent.on(w2.domNode.parentElement?.querySelector(".time"), "change", () => Fluent.trigger(w2.domNode, "change"));
      }, "init"),
      handler: /* @__PURE__ */ __name(function(args) {
        var date1 = parseDate(args.widget.value);
        if (date1) {
          if (isNaN(date1?.valueOf())) {
            notifyWarning(FormValidationTexts.DateInvalid, "", null);
            args.widget.value = "";
            date1 = null;
          } else {
            args.request.Criteria = Criteria.and(
              args.request.Criteria,
              Criteria(args.field).ge(args.widget.value)
            );
          }
        }
        var date2 = parseDate(end.value);
        if (date2) {
          if (isNaN(date2?.valueOf())) {
            notifyWarning(FormValidationTexts.DateInvalid, "", null);
            end.value = "";
            date2 = null;
          } else {
            args.request.Criteria = Criteria.and(
              args.request.Criteria,
              Criteria(args.field).le(end.value)
            );
          }
        }
        args.active = !!(date1 || date2);
      }, "handler"),
      displayText: /* @__PURE__ */ __name(function(w2, l2) {
        var v1 = EditorUtils.getDisplayText(w2);
        var v2 = EditorUtils.getDisplayText(end);
        if (!v1 && !v2) {
          return null;
        }
        var text1 = l2 + " >= " + v1;
        var text2 = l2 + " <= " + v2;
        if (v1 && v2) {
          return text1 + " " + (FilterPanelTexts.asTry().And ?? "and") + " " + text2;
        } else if (v1) {
          return text1;
        } else {
          return text2;
        }
      }, "displayText"),
      saveState: /* @__PURE__ */ __name(function(w1) {
        return [EditorUtils.getValue(w1), EditorUtils.getValue(end)];
      }, "saveState"),
      loadState: /* @__PURE__ */ __name(function(w2, state) {
        if (state == null || !Array.isArray(state) || state.length !== 2) {
          state = [null, null];
        }
        EditorUtils.setValue(w2, state[0]);
        EditorUtils.setValue(end, state[1]);
      }, "loadState"),
      options: useUtc == null ? null : { useUtc }
    };
  }
  addBoolean(field, title, yes, no) {
    return this.add(_QuickFilterBar.boolean(field, title, yes, no));
  }
  static boolean(field, title, yes, no) {
    var opt = {};
    var items = [];
    var trueText = yes;
    if (trueText == null) {
      trueText = FilterPanelTexts.OperatorNames.true;
    }
    items.push(["1", trueText]);
    var falseText = no;
    if (falseText == null) {
      falseText = FilterPanelTexts.OperatorNames.false;
    }
    items.push(["0", falseText]);
    opt.items = items;
    return {
      field,
      type: SelectEditor,
      title,
      options: opt,
      handler: /* @__PURE__ */ __name(function(args) {
        args.equalityFilter[args.field] = !args.value?.toString() ? null : !!toId(args.value);
      }, "handler")
    };
  }
  destroy() {
    this.submitHandlers = null;
    super.destroy();
  }
  onSubmit(request) {
    this.submitHandlers && this.submitHandlers(request);
  }
  add_submitHandlers(action) {
    this.submitHandlers = delegateCombine(this.submitHandlers, action);
  }
  remove_submitHandlers(action) {
    this.submitHandlers = delegateRemove(this.submitHandlers, action);
  }
  clear_submitHandlers() {
  }
  find(type, field) {
    const selector = "#" + this.options.idPrefix + field;
    return getWidgetFrom(this.domNode?.querySelector(selector) ?? selector, type);
  }
  tryFind(type, field) {
    const selector = "#" + this.options.idPrefix + field;
    return tryGetWidget(this.domNode?.querySelector(selector) ?? selector, type);
  }
};

// src/ui/datagrid/datagrid-persistence.ts
var defaultGridPersistenceFlags = {
  columnPinning: true,
  columnWidths: true,
  columnVisibility: true,
  sortColumns: true,
  filterItems: true,
  quickFilters: true,
  quickFilterText: false,
  quickSearch: false,
  includeDeleted: true
};
var omitAllGridPersistenceFlags = {
  columnPinning: false,
  columnWidths: false,
  columnVisibility: false,
  sortColumns: false,
  filterItems: false,
  quickFilters: false,
  quickFilterText: false,
  quickSearch: false,
  includeDeleted: false
};
function getCurrentSettings(opt) {
  const flags = Object.assign({}, defaultGridPersistenceFlags, opt.flags || {});
  const settings = {
    flags
  };
  if (flags.columnVisibility || flags.columnWidths || flags.columnPinning || flags.sortColumns) {
    settings.columns = [];
    const sortColumns = opt.sleekGrid.getSortColumns();
    const columns = opt.sleekGrid.getAllColumns();
    for (const column of columns) {
      const p2 = {
        id: column.id
      };
      if (flags.columnPinning) {
        p2.pin = column.frozen ? column.frozen !== "end" ? "start" : "end" : false;
      }
      if (flags.columnVisibility) {
        p2.visible = column.visible !== false;
      }
      if (flags.columnWidths) {
        p2.width = column.width;
      }
      if (flags.sortColumns) {
        const sort = sortColumns.findIndex((x) => x.columnId == column.id);
        if (sort >= 0) {
          p2.sort = sortColumns[sort].sortAsc !== false ? sort + 1 : -sort - 1;
        }
      }
      settings.columns.push(p2);
    }
  }
  if (flags.includeDeleted && opt.includeDeletedToggle) {
    settings.includeDeleted = opt.includeDeletedToggle.matches(".pressed");
  }
  if (flags.filterItems && opt.filterStore) {
    settings.filterItems = opt.filterStore.get_items().slice();
  }
  if (flags.quickSearch) {
    const qsInput = opt.toolbarNode?.querySelector(".s-QuickSearchInput");
    if (qsInput) {
      const qsWidget = tryGetWidget(qsInput, QuickSearchInput);
      if (qsWidget) {
        settings.quickSearchField = qsWidget.get_field();
        settings.quickSearchText = qsWidget.domNode.value;
      }
    }
  }
  if (flags.quickFilters && opt.quickFiltersDiv != null) {
    settings.quickFilters = {};
    opt.quickFiltersDiv.querySelectorAll(".quick-filter-item").forEach((filterItem) => {
      const field = filterItem.dataset.qffield;
      if (!field?.length) {
        return;
      }
      const widget = tryGetWidget("#" + opt.uniqueName + "_QuickFilter_" + field);
      if (!widget)
        return;
      const qfData = QuickFilterBar.getItemData(filterItem);
      const state = typeof qfData?.saveState === "function" ? qfData.saveState(widget) : EditorUtils.getValue(widget);
      settings.quickFilters[field] = state;
      if (flags.quickFilterText && filterItem.classList.contains("quick-filter-active")) {
        const filterLabel = filterItem.querySelector(".quick-filter-label")?.textContent ?? "";
        let displayText;
        if (typeof qfData?.displayText === "function") {
          displayText = qfData.displayText(widget, filterLabel);
        } else {
          displayText = filterLabel + " = " + EditorUtils.getDisplayText(widget);
        }
        if (displayText?.length) {
          if (settings.quickFilterText?.length) {
            settings.quickFilterText += " " + FilterPanelTexts.And + " ";
            settings.quickFilterText += displayText;
          } else {
            settings.quickFilterText = displayText;
          }
        }
      }
    });
  }
  return settings;
}
__name(getCurrentSettings, "getCurrentSettings");
function restoreSettingsFrom(opt) {
  let allColumns = opt.sleekGrid.getAllColumns();
  let colById = /* @__PURE__ */ Object.create(null);
  for (let c2 of allColumns) {
    colById[c2.id] = c2;
  }
  const flags = Object.assign({}, defaultGridPersistenceFlags, opt.flags || {});
  const settings = opt.settings || {};
  if (settings.columns != null) {
    if (flags.columnPinning && (settings.flags?.columnPinning ?? settings.columns.some((x) => "pin" in x))) {
      for (let x1 of settings.columns) {
        if (x1.id != null) {
          const column = colById[x1.id];
          if (column != null) {
            column.frozen = x1.pin === "start" || x1.pin === "end" ? x1.pin : null;
          }
        }
      }
    }
    if (flags.columnWidths && (settings.flags?.columnWidths ?? settings.columns.some((c2) => "width" in c2))) {
      for (let x2 of settings.columns) {
        if (x2.id != null && x2.width != null && x2.width !== 0) {
          const column1 = colById[x2.id];
          if (column1 != null) {
            column1.width = x2.width;
          }
        }
      }
    }
    if (flags.sortColumns && (settings.flags?.sortColumns ?? settings.columns.some((c2) => "sort" in c2))) {
      const list = [];
      const sortColumns = settings.columns.filter(function(x3) {
        return x3.id != null && (x3.sort ?? 0) !== 0;
      });
      sortColumns.sort(function(a2, b2) {
        return Math.abs(a2.sort) - Math.abs(b2.sort);
      });
      for (let x4 of sortColumns) {
        const column2 = colById[x4.id];
        if (column2 != null) {
          list.push({
            columnId: x4.id,
            sortAsc: x4.sort > 0
          });
        }
      }
      opt.view.sortBy = list.map(function(x5) {
        return x5.columnId + (x5.sortAsc === false ? " DESC" : "");
      });
      opt.sleekGrid.setSortColumns(list);
    }
    if (flags.columnVisibility && (settings.flags?.columnVisibility ?? settings.columns.some((x) => "visible" in x))) {
      const visibleColumns = settings.columns.filter(
        (x) => x.id != null && x.visible === true && colById[x.id] && opt.canShowColumn(colById[x.id])
      ).map((x) => x.id);
      opt.sleekGrid.setVisibleColumns(visibleColumns, { notify: false });
    } else {
      opt.sleekGrid.invalidateColumns();
    }
    opt.sleekGrid.invalidate();
  }
  if (settings.filterItems != null && flags.filterItems && opt.filterStore) {
    const items = opt.filterStore.get_items();
    items.length = 0;
    items.push.apply(items, settings.filterItems);
    opt.filterStore.raiseChanged();
  }
  if (settings.includeDeleted != null && flags.includeDeleted) {
    if (opt.includeDeletedToggle && !!settings.includeDeleted !== opt.includeDeletedToggle.classList.contains("pressed")) {
      Fluent.trigger(opt.includeDeletedToggle.querySelector("a"), "click");
    }
  }
  if (settings.quickFilters != null && flags.quickFilters && opt.quickFiltersDiv != null) {
    opt.quickFiltersDiv.querySelectorAll(".quick-filter-item").forEach((e2) => {
      const field = e2.dataset.qffield;
      if (!field?.length) {
        return;
      }
      const widget = tryGetWidget("#" + cssEscape(opt.uniqueName + "_QuickFilter_" + field));
      if (widget == null) {
        return;
      }
      const state = settings.quickFilters[field];
      const loadState = QuickFilterBar.getItemData(e2)?.loadState;
      if (typeof loadState === "function") {
        loadState(widget, state);
      } else {
        EditorUtils.setValue(widget, state);
      }
    });
  }
  if (flags.quickSearch && (settings.flags?.quickSearch ?? (settings.quickSearchField !== void 0 || settings.quickSearchText !== void 0))) {
    const qsInput = opt.toolbarNode.querySelector(".s-QuickSearchInput");
    if (qsInput) {
      const qsWidget = tryGetWidget(qsInput, QuickSearchInput);
      qsWidget && qsWidget.restoreState(settings.quickSearchText, settings.quickSearchField);
    }
  }
}
__name(restoreSettingsFrom, "restoreSettingsFrom");

// src/ui/datagrid/datagrid-defaults.tsx
var dataGridDefaults = {
  /** Default column width delta. This value if specified, is added to the width of columns defined server side. Default is null */
  columnWidthDelta: null,
  /** Default column width scale. This value if specified, is multiplied with the width of columns defined server side. Default is null */
  columnWidthScale: null,
  /** Controls whether to enable advanced filtering, e.g. via filter dialog/bar. Default is null. */
  enableAdvancedFiltering: null,
  /** Controls whether to open dialogs as panels. Default is null. */
  openDialogsAsPanel: null,
  /** Default row height. Default is null. */
  rowHeight: null,
  /** Default persistence flags. Defaults are true except quickSearch and quickFilterText */
  persistenceFlags: defaultGridPersistenceFlags,
  /** Default persistence storage. Default is null */
  persistenceStorage: null
};

// src/ui/editors/stringeditor.tsx
var _a42, _b27;
var StringEditor = class extends (_b27 = EditorWidget, _a42 = Symbol.typeInfo, _b27) {
  static {
    __name(this, "StringEditor");
  }
  static {
    this[_a42] = this.registerEditor(nsSerenity, [IStringValue]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  get value() {
    return this.domNode.value;
  }
  get_value() {
    return this.value;
  }
  set value(value) {
    this.domNode.value = value ?? "";
  }
  set_value(value) {
    this.value = value;
  }
};

// src/ui/filtering/filteroperator.ts
var FilterOperators;
((FilterOperators2) => {
  FilterOperators2.isTrue = "true";
  FilterOperators2.isFalse = "false";
  FilterOperators2.contains = "contains";
  FilterOperators2.startsWith = "startswith";
  FilterOperators2.EQ = "eq";
  FilterOperators2.NE = "ne";
  FilterOperators2.GT = "gt";
  FilterOperators2.GE = "ge";
  FilterOperators2.LT = "lt";
  FilterOperators2.LE = "le";
  FilterOperators2.BW = "bw";
  FilterOperators2.IN = "in";
  FilterOperators2.isNull = "isnull";
  FilterOperators2.isNotNull = "isnotnull";
  FilterOperators2.toCriteriaOperator = {
    eq: "=",
    ne: "!=",
    gt: ">",
    ge: ">=",
    lt: "<",
    le: "<="
  };
})(FilterOperators || (FilterOperators = {}));

// src/ui/filtering/iquickfiltering.ts
var _a43;
_a43 = Symbol.typeInfo;
var IQuickFiltering = class {
  static {
    __name(this, "IQuickFiltering");
  }
  static {
    this[_a43] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/ui/filtering/basefiltering.ts
var _a44;
_a44 = Symbol.typeInfo;
var BaseFiltering = class {
  static {
    __name(this, "BaseFiltering");
  }
  get_field() {
    return this.field;
  }
  set_field(value) {
    this.field = value;
  }
  get_container() {
    return this.container;
  }
  set_container(value) {
    this.container = value;
  }
  get_operator() {
    return this.operator;
  }
  set_operator(value) {
    this.operator = value;
  }
  appendNullableOperators(list) {
    if (!this.isNullable()) {
      return list;
    }
    list.push({ key: FilterOperators.isNotNull });
    list.push({ key: FilterOperators.isNull });
    return list;
  }
  appendComparisonOperators(list) {
    list.push({ key: FilterOperators.EQ });
    list.push({ key: FilterOperators.NE });
    list.push({ key: FilterOperators.LT });
    list.push({ key: FilterOperators.LE });
    list.push({ key: FilterOperators.GT });
    list.push({ key: FilterOperators.GE });
    return list;
  }
  isNullable() {
    return this.get_field().required !== true;
  }
  createEditor() {
    switch (this.get_operator().key) {
      case "true":
      case "false":
      case "isnull":
      case "isnotnull": {
        return;
      }
      case "contains":
      case "startswith":
      case "eq":
      case "ne":
      case "lt":
      case "le":
      case "gt":
      case "ge": {
        const input = document.createElement("input");
        input.type = "text";
        Fluent(this.get_container()).empty().append(input);
        return;
      }
    }
    throw new Error(stringFormat(
      "Filtering '{0}' has no editor for '{1}' operator",
      getTypeFullName(getInstanceType(this)),
      this.get_operator().key
    ));
  }
  operatorFormat(op) {
    return op.format ?? FilterPanelTexts.asTry().OperatorFormats[op.key] ?? op.key;
  }
  getTitle(field) {
    return localText(field.title, field.title ?? field.name);
  }
  displayText(op, values) {
    if (!values || values.length === 0) {
      return stringFormat(this.operatorFormat(op), this.getTitle(this.field));
    } else if (values.length === 1) {
      return stringFormat(this.operatorFormat(op), this.getTitle(this.field), values[0]);
    } else {
      return stringFormat(this.operatorFormat(op), this.getTitle(this.field), values[0], values[1]);
    }
  }
  getCriteriaField() {
    return this.field.name;
  }
  getCriteria() {
    var result = {};
    var text2;
    var field = Criteria(this.getCriteriaField());
    var op = this.get_operator().key;
    switch (op) {
      case "true": {
        result.displayText = this.displayText(this.get_operator(), []);
        result.criteria = field.eq(true);
        return result;
      }
      case "false": {
        result.displayText = this.displayText(this.get_operator(), []);
        result.criteria = field.eq(false);
        return result;
      }
      case "isnull": {
        result.displayText = this.displayText(this.get_operator(), []);
        result.criteria = field.isNull();
        return result;
      }
      case "isnotnull": {
        result.displayText = this.displayText(this.get_operator(), []);
        result.criteria = field.isNotNull();
        return result;
      }
      case "contains": {
        text2 = this.getEditorText();
        result.displayText = this.displayText(this.get_operator(), [text2]);
        result.criteria = field.contains(text2);
        return result;
      }
      case "startswith": {
        text2 = this.getEditorText();
        result.displayText = this.displayText(this.get_operator(), [text2]);
        result.criteria = field.startsWith(text2);
        return result;
      }
      case "eq":
      case "ne":
      case "lt":
      case "le":
      case "gt":
      case "ge": {
        text2 = this.getEditorText();
        result.displayText = this.displayText(this.get_operator(), [text2]);
        result.criteria = field[op](this.getEditorValue());
        return result;
      }
    }
    throw new Error(stringFormat(
      "Filtering '{0}' has no handler for '{1}' operator",
      getTypeFullName(getInstanceType(this)),
      this.get_operator().key
    ));
  }
  loadState(state) {
    var input = this.get_container().querySelector(Fluent.inputLikeSelector);
    input && (input.value = state);
  }
  saveState() {
    switch (this.get_operator().key) {
      case "contains":
      case "startswith":
      case "eq":
      case "ne":
      case "lt":
      case "le":
      case "gt":
      case "ge": {
        var input = this.get_container().querySelector(Fluent.inputLikeSelector);
        return input?.value;
      }
    }
    return null;
  }
  argumentNull() {
    return new Error(FilterPanelTexts.ValueRequired);
  }
  validateEditorValue(value) {
    if (value.length === 0) {
      throw this.argumentNull();
    }
    return value;
  }
  getEditorValue() {
    var inputs = this.get_container().querySelectorAll(Fluent.inputLikeSelector + ":not(.select2-focusser)");
    if (inputs.length !== 1) {
      throw new Error(stringFormat(
        "Couldn't find input in filter container for {0}",
        this.field.title ?? this.field.name
      ));
    }
    let input = inputs[0];
    var value;
    let combobox = Combobox.getInstance(input);
    if (combobox) {
      value = combobox.isMultiple ? combobox.getValues().join(",") : combobox.getValue();
    } else {
      value = input.value;
    }
    value = (value ?? "").trim();
    return this.validateEditorValue(value);
  }
  getEditorText() {
    var input = this.get_container().querySelector(Fluent.inputLikeSelector + ":not(.select2-focusser):not(.select2-input)");
    if (!input) {
      return this.get_container().textContent?.trim();
    }
    var value;
    let combobox = Combobox.getInstance(input);
    if (combobox) {
      value = combobox.getSelectedItems()?.join(", ");
    } else {
      value = input.value;
    }
    return value;
  }
  initQuickFilter(filter) {
    filter.field = this.getCriteriaField();
    filter.type = StringEditor;
    filter.title = this.getTitle(this.field);
    filter.options = deepClone(this.get_field().quickFilterParams);
  }
  static registerClass(typeName, intfAndAttr) {
    if (Object.prototype.hasOwnProperty.call(this, Symbol.typeInfo) && this[Symbol.typeInfo])
      throw new Error(`Type ${this.name} already has a typeInfo property!`);
    const typeInfo = this[Symbol.typeInfo] = classTypeInfo(typeName, intfAndAttr);
    registerType(this);
    return typeInfo;
  }
  static {
    this[_a44] = this.registerClass(nsSerenity, [IFiltering, IQuickFiltering]);
  }
};

// src/ui/filtering/booleanfiltering.ts
var _a45, _b28;
var BooleanFiltering = class extends (_b28 = BaseFiltering, _a45 = Symbol.typeInfo, _b28) {
  static {
    __name(this, "BooleanFiltering");
  }
  static {
    this[_a45] = this.registerClass(nsSerenity);
  }
  getOperators() {
    return this.appendNullableOperators([
      { key: FilterOperators.isTrue },
      { key: FilterOperators.isFalse }
    ]);
  }
};

// src/ui/filtering/baseeditorfiltering.ts
var _a46, _b29;
var BaseEditorFiltering = class extends (_b29 = BaseFiltering, _a46 = Symbol.typeInfo, _b29) {
  constructor(editorTypeRef) {
    super();
    this.editorTypeRef = editorTypeRef;
  }
  static {
    __name(this, "BaseEditorFiltering");
  }
  static {
    this[_a46] = this.registerClass(nsSerenity);
  }
  useEditor() {
    switch (this.get_operator().key) {
      case "eq":
      case "ne":
      case "lt":
      case "le":
      case "gt":
      case "ge":
        return true;
    }
    return false;
  }
  createEditor() {
    if (this.useEditor()) {
      this.editor = new this.editorTypeRef({
        element: /* @__PURE__ */ __name((el) => {
          this.get_container().append(el);
        }, "element"),
        ...this.getEditorOptions()
      }).init();
      return;
    }
    this.editor = null;
    super.createEditor();
  }
  useIdField() {
    return false;
  }
  getCriteriaField() {
    if (this.useEditor() && this.useIdField() && this.get_field().filteringIdField) {
      return this.get_field().filteringIdField;
    }
    return super.getCriteriaField();
  }
  getEditorOptions() {
    var opt = deepClone(this.get_field().editorParams || {});
    delete opt["cascadeFrom"];
    return Object.assign(opt, this.get_field().filteringParams);
  }
  loadState(state) {
    if (this.useEditor()) {
      if (state == null) {
        return;
      }
      EditorUtils.setValue(this.editor, state);
      return;
    }
    super.loadState(state);
  }
  saveState() {
    if (this.useEditor()) {
      return EditorUtils.getValue(this.editor);
    }
    return super.saveState();
  }
  getEditorValue() {
    if (this.useEditor()) {
      var value = EditorUtils.getValue(this.editor);
      if (value == null || typeof value == "string" && value.trim().length === 0)
        throw this.argumentNull();
      return value;
    }
    return super.getEditorValue();
  }
  initQuickFilter(filter) {
    super.initQuickFilter(filter);
    filter.type = this.editorTypeRef;
    filter.options = Object.assign(/* @__PURE__ */ Object.create(null), deepClone(this.getEditorOptions()), deepClone(this.get_field().quickFilterParams));
  }
};

// src/ui/filtering/datefiltering.ts
var _a47, _b30;
var DateFiltering = class extends (_b30 = BaseEditorFiltering, _a47 = Symbol.typeInfo, _b30) {
  static {
    __name(this, "DateFiltering");
  }
  static {
    this[_a47] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(DateEditor);
  }
  getOperators() {
    return this.appendNullableOperators(this.appendComparisonOperators([]));
  }
};

// src/ui/filtering/datetimefiltering.ts
var _a48, _b31;
var DateTimeFiltering = class extends (_b31 = BaseEditorFiltering, _a48 = Symbol.typeInfo, _b31) {
  static {
    __name(this, "DateTimeFiltering");
  }
  static {
    this[_a48] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(DateTimeEditor);
  }
  getOperators() {
    return this.appendNullableOperators(
      this.appendComparisonOperators([])
    );
  }
  getCriteria() {
    var result = {};
    switch (this.get_operator().key) {
      case "eq":
      case "ne":
      case "lt":
      case "le":
      case "gt":
      case "ge": {
        {
          var text2 = this.getEditorText();
          result.displayText = this.displayText(this.get_operator(), [text2]);
          var date = parseISODateTime(this.getEditorValue());
          date = new Date(date.getFullYear(), date.getMonth(), date.getDate());
          var next = new Date(date.getFullYear(), date.getMonth(), date.getDate() + 1);
          var criteria = Criteria(this.getCriteriaField());
          var dateValue = formatDate(date, "yyyy-MM-dd");
          var nextValue = formatDate(next, "yyyy-MM-dd");
          switch (this.get_operator().key) {
            case "eq": {
              result.criteria = Criteria.and(criteria.ge(dateValue), criteria.lt(nextValue));
              return result;
            }
            case "ne": {
              result.criteria = Criteria.paren(Criteria.or(criteria.lt(dateValue), criteria.gt(nextValue)));
              return result;
            }
            case "lt": {
              result.criteria = criteria.lt(dateValue);
              return result;
            }
            case "le": {
              result.criteria = criteria.lt(nextValue);
            }
            case "gt": {
              result.criteria = criteria.ge(nextValue);
              return result;
            }
            case "ge": {
              result.criteria = criteria.ge(dateValue);
              return result;
            }
          }
        }
        break;
      }
    }
    return super.getCriteria();
  }
};

// src/ui/datagrid/datagrid-internal.tsx
function getDefaultSortBy(sleekGrid) {
  if (!sleekGrid)
    return [];
  var columns = sleekGrid.getColumns().filter(function(x) {
    return x.sortOrder && x.sortOrder !== 0 && x.field != null;
  });
  if (columns.length > 0) {
    columns.sort(function(x1, y2) {
      return Math.abs(x1.sortOrder) < Math.abs(y2.sortOrder) ? -1 : Math.abs(x1.sortOrder) > Math.abs(y2.sortOrder) ? 1 : 0;
    });
    var list = [];
    for (var i2 = 0; i2 < columns.length; i2++) {
      var col = columns[i2];
      list.push(col.field + (col.sortOrder < 0 ? " DESC" : ""));
    }
    return list;
  }
  return [];
}
__name(getDefaultSortBy, "getDefaultSortBy");
function getItemCssClass(item, activeFieldName, deletedFieldName) {
  if (activeFieldName && deletedFieldName)
    return null;
  if (activeFieldName) {
    var value = item[activeFieldName];
    if (value == null) {
      return null;
    }
    if (typeof value === "number") {
      if (value < 0) {
        return "deleted";
      } else if (value === 0) {
        return "inactive";
      }
    } else if (typeof value === "boolean") {
      if (value === false) {
        return "deleted";
      }
    }
  } else {
    return item[deletedFieldName] ? "deleted" : null;
  }
  return null;
}
__name(getItemCssClass, "getItemCssClass");
function propertyItemToQuickFilter(item) {
  let result = {};
  const name = item.name;
  const title = localText(item.title, item.title ?? name);
  const filteringType = FilteringTypeRegistry.get(item.filteringType ?? "String");
  if (filteringType === DateFiltering) {
    result = QuickFilterBar.dateRange(name, title);
  } else if (filteringType === DateTimeFiltering) {
    result = QuickFilterBar.dateTimeRange(name, title, item.editorParams?.useUtc);
  } else if (filteringType === BooleanFiltering) {
    const qfp = item.quickFilterParams || {};
    const fp = item.filteringParams || {};
    result = QuickFilterBar.boolean(name, title, qfp["trueText"] ?? fp["trueText"], qfp["falseText"] ?? fp["falseText"]);
  } else {
    const filtering = new filteringType(item.filteringParams ?? {});
    if (filtering && isInstanceOfType(filtering, IQuickFiltering)) {
      ReflectionOptionsSetter.set(filtering, item.filteringParams);
      filtering.set_field(item);
      filtering.set_operator({ key: FilterOperators.EQ });
      filtering.initQuickFilter(result);
      result.options = Object.assign(deepClone(result.options ?? {}), item.quickFilterParams ?? {});
    } else {
      return null;
    }
  }
  if (item.quickFilterSeparator) {
    result.separator = true;
  }
  result.cssClass = item.quickFilterCssClass;
  return result;
}
__name(propertyItemToQuickFilter, "propertyItemToQuickFilter");
function sleekGridOnSort(view, p2) {
  view.populateLock();
  try {
    var sortBy = [];
    var col;
    if (!!p2.multiColumnSort) {
      for (var i2 = 0; !!(i2 < p2.sortCols.length); i2++) {
        var x = p2.sortCols[i2];
        col = x.sortCol;
        if (col == null) {
          col = {};
        }
        sortBy.push(col.field + (!!x.sortAsc ? "" : " DESC"));
      }
    } else {
      col = p2.sortCol;
      if (col == null) {
        col = {};
      }
      sortBy.push(col.field + (!!p2.sortAsc ? "" : " DESC"));
    }
    view.seekToPage = 1;
    view.sortBy = sortBy;
  } finally {
    view.populateUnlock();
  }
  if (view.getLocalSort && view.getLocalSort()) {
    view.sort();
  } else {
    view.populate();
  }
}
__name(sleekGridOnSort, "sleekGridOnSort");

// src/ui/datagrid/slickpager.tsx
var _a49, _b32;
var SlickPager = class extends (_b32 = Widget, _a49 = Symbol.typeInfo, _b32) {
  static {
    __name(this, "SlickPager");
  }
  static {
    this[_a49] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    let opt = this.options;
    opt.showRowsPerPage ??= true;
    opt.rowsPerPageOptions ??= [20, 100, 500, 2e3];
    var v2 = opt.view;
    if (!v2) throw "SlickPager requires view option to be set!";
    const p2 = "slick-pg-";
    const Group2 = /* @__PURE__ */ __name(({ id, children }) => jsx("div", { class: `${p2}grp ${p2}grp-${id}`, children }), "Group");
    const Button = /* @__PURE__ */ __name(({ id, onClick }) => jsx("div", { class: `${p2}${id} ${p2}btn`, onClick, children: jsx("span", { class: `${p2}btn-span` }) }), "Button");
    const NavButton = /* @__PURE__ */ __name(({ id, onClick }) => {
      const b2 = Button({ id, onClick });
      Fluent.on(b2, "click", () => this._changePage(id));
      return b2;
    }, "NavButton");
    this.element.addClass("s-SlickPager slick-pg").append(
      jsx("div", { class: p2 + "in", children: [
        opt.showRowsPerPage && jsx(Group2, { id: "size", children: this.pageSize = jsx("select", { class: `${p2}size`, name: "rp", onChange: () => {
          if (opt.onRowsPerPageChange)
            opt.onRowsPerPageChange(+this.pageSize.value);
          else {
            v2["newp"] = 1;
            v2.setPagingOptions({
              page: 1,
              rowsPerPage: +this.pageSize.value
            });
          }
        }, children: opt.rowsPerPageOptions.map((rowsPerPage) => jsx("option", { value: rowsPerPage, selected: v2.rowsPerPage == rowsPerPage, children: rowsPerPage })) }) }),
        jsx(Group2, { id: "firstprev", children: [
          jsx(NavButton, { id: "first" }),
          jsx(NavButton, { id: "prev" })
        ] }),
        jsx(Group2, { id: "control", children: jsx("span", { class: `${p2}control`, children: [
          jsx("span", { class: `${p2}pagetext`, children: PagerTexts.Page }),
          this.currentPage = jsx("input", { class: `${p2}current mx-1`, type: "text", size: 4, value: "1", onKeyDown: (e2) => {
            if (e2.key === "Enter") this._changePage("input");
          } }),
          jsx("span", { class: `${p2}pagesep px-1`, children: "/" }),
          this.totalPages = jsx("span", { class: `${p2}total`, children: "1" })
        ] }) }),
        jsx(Group2, { id: "nextlast", children: [
          jsx(NavButton, { id: "next" }),
          jsx(NavButton, { id: "last" })
        ] }),
        jsx(Group2, { id: "reload", children: jsx(Button, { id: "reload", onClick: () => v2.populate() }) }),
        jsx(Group2, { id: "stat", children: this.stat = jsx("span", { class: `${p2}stat` }) })
      ] })
    );
    v2.onPagingInfoChanged.subscribe(() => this._updatePager());
  }
  _changePage(ctype) {
    var view = this.options.view;
    if (!view || view.loading)
      return true;
    var info = view.getPagingInfo();
    var pages = !info.rowsPerPage || !info.totalCount ? 1 : Math.ceil(info.totalCount / info.rowsPerPage);
    var newp;
    switch (ctype) {
      case "first":
        newp = 1;
        break;
      case "prev":
        if (info.page > 1) newp = parseInt(info.page) - 1;
        break;
      case "next":
        if (info.page < pages) newp = parseInt(info.page) + 1;
        break;
      case "last":
        newp = pages;
        break;
      case "input":
        var nv = parseInt(this.currentPage.value);
        if (isNaN(nv))
          nv = 1;
        else if (nv < 1)
          nv = 1;
        else if (nv > pages)
          nv = pages;
        this.currentPage.value = "" + nv;
        newp = nv;
        break;
    }
    if (newp == info.page)
      return false;
    if (this.options.onChangePage)
      this.options.onChangePage(newp);
    else {
      view.setPagingOptions({ page: newp });
    }
  }
  _updatePager() {
    var view = this.options.view;
    var info = view.getPagingInfo();
    var pages = !info.rowsPerPage || !info.totalCount ? 1 : Math.ceil(info.totalCount / info.rowsPerPage);
    this.currentPage.value = info.page?.toString();
    this.totalPages.textContent = "" + pages;
    var r1 = (info.page - 1) * info.rowsPerPage + 1;
    var r2 = r1 + info.rowsPerPage - 1;
    if (info.totalCount < r2)
      r2 = info.totalCount;
    var stat;
    if (info.loading) {
      stat = PagerTexts.LoadingStatus;
    } else if (info.error) {
      stat = info.error;
    } else if (info.totalCount > 0) {
      stat = PagerTexts.PageStatus;
      stat = stat.replace(/{from}/, r1);
      stat = stat.replace(/{to}/, r2);
      stat = stat.replace(/{total}/, info.totalCount);
    } else
      stat = PagerTexts.NoRowStatus;
    this.stat.textContent = stat;
    this.pageSize && (this.pageSize.value = (info.rowsPerPage || 0).toString());
  }
};

// src/ui/datagrid/datagrid.tsx
var _a50, _b33;
var DataGrid = class _DataGrid extends (_b33 = Widget, _a50 = Symbol.typeInfo, _b33) {
  constructor(props) {
    super(props);
    this.onAfterInit = new PubSub();
    this.onCanSubmit = new PubSub();
    this.onDataChanged = new PubSub();
    this.onFiltering = new PubSub();
    this.onPersistence = new PubSub();
    this.onProcessData = new PubSub();
    this.onSubmitting = new PubSub();
    /** Renders an edit link for the item in current row. Returns a DocumentFragment for non-data rows, and an anchor element otherwise. */
    this.EditLink = /* @__PURE__ */ __name((props) => {
      let children = props.children;
      if (children == null && props.context != null) {
        children = props.context.value?.toString() ?? "";
      }
      if (props?.context?.item?.__nonDataRow || props.context?.purpose && skipEditLinkFormatPurposes.has(props.context.purpose)) {
        return jsx(Fragment, { children });
      }
      let id = props.id;
      if (id === void 0 && props.context?.item != null) {
        id = props.context.item[props.idField ?? this.getIdProperty()];
      }
      return EditLink({
        itemType: props.itemType ?? this.getItemType(),
        itemId: id,
        children,
        cssClass: props.cssClass,
        tabindex: props.tabindex ?? props.tabIndex
      });
    }, "EditLink");
    this._persistenceLock = 0;
    this.restoringSettings = 0;
    this.domNode.classList.add("s-DataGrid");
    const layout = bindThis(this).layoutTimerCallback;
    this.element.addClass("require-layout").on("layout." + this.uniqueName, layout);
    if (this.useLayoutTimer())
      this._layoutTimer = LayoutTimer.onSizeChange(() => this.domNode && this.domNode, debounce(layout, 50), { debounceTimes: 1 });
    this.setTitle(this.getInitialTitle());
    var buttons = this.getButtons();
    if (buttons != null) {
      this.createToolbar(buttons);
    }
    this.slickContainer = this.createSlickContainer();
    this.view = this.createView();
    this.syncOrAsyncThen(this.getPropertyItemsData, this.getPropertyItemsDataAsync, (itemsData) => {
      this.propertyItemsReady(itemsData);
    });
  }
  static {
    __name(this, "DataGrid");
  }
  static {
    this[_a50] = this.registerClass(nsSerenity, [IReadOnly]);
  }
  static {
    this.defaultOptions = dataGridDefaults;
  }
  static get defaultRowHeight() {
    return dataGridDefaults.rowHeight;
  }
  static get defaultPersistenceStorage() {
    return dataGridDefaults.persistenceStorage;
  }
  static set defaultPersistenceStorage(value) {
    dataGridDefaults.persistenceStorage = value;
  }
  static get defaultColumnWidthScale() {
    return dataGridDefaults.columnWidthScale;
  }
  static set defaultColumnWidthScale(value) {
    dataGridDefaults.columnWidthScale = value;
  }
  static get defaultColumnWidthDelta() {
    return dataGridDefaults.columnWidthDelta;
  }
  static set defaultColumnWidthDelta(value) {
    dataGridDefaults.columnWidthDelta = value;
  }
  static {
    this.onAfterInit = new PubSub();
  }
  autoRegisteringPlugin(args) {
  }
  layoutTimerCallback() {
    this.layout();
    if (this._layoutTimer != null)
      LayoutTimer.store(this._layoutTimer);
  }
  propertyItemsReady(itemsData) {
    this.propertyItemsData = itemsData;
    const sleekGrid = this.createSlickGrid();
    this._grid ??= sleekGrid;
    this.initSleekGrid();
    if (this.enableAdvancedFiltering()) {
      this.createFilterBar();
    }
    if (this.usePager()) {
      this.createPager();
    }
    this.bindToSlickEvents();
    this.bindToViewEvents();
    if (this.toolbar) {
      this.createToolbarExtensions();
    }
    this.createQuickFilters();
    this.updateInterface();
    _DataGrid.onAfterInit.notify({ dataGrid: this });
    this.onAfterInit.notify({ dataGrid: this });
    this.afterInit();
    this.initialSettings = this.getCurrentSettings(null);
    var restoreResult = this.restoreSettings(null, null);
    if (restoreResult?.then)
      restoreResult.then(() => window.setTimeout(() => this.initialPopulate(), 0));
    else
      window.setTimeout(() => this.initialPopulate(), 0);
  }
  afterInit() {
  }
  useAsync() {
    return false;
  }
  useLayoutTimer() {
    return true;
  }
  layout() {
    if (!this.domNode || !Fluent.isVisibleLike(this.domNode) || !this.slickContainer || !this._grid)
      return;
    var responsiveHeight = this.domNode.classList.contains("responsive-height");
    var madeAutoHeight = this._grid != null && this._grid.getOptions().autoHeight;
    var shouldAutoHeight = responsiveHeight && window.innerWidth < 768;
    if (shouldAutoHeight) {
      if (!madeAutoHeight) {
        this._grid.setOptions({ autoHeight: true });
      }
    } else if (madeAutoHeight) {
      this.slickContainer.getNode().style.height = "";
      this.slickContainer.findAll(".slick-viewport").forEach((x) => x.style.height = "");
      this._grid.setOptions({ autoHeight: false });
    }
    this._grid.resizeCanvas();
  }
  getInitialTitle() {
    return null;
  }
  createToolbarExtensions() {
  }
  ensureQuickFilterBar() {
    if (this.quickFiltersDiv == null)
      this.createQuickFilters([]);
    return this.quickFiltersBar;
  }
  createQuickFilters(filters) {
    if (this.quickFiltersDiv == null && (filters != null || (filters = this.getQuickFilters()) && filters != null && filters.length)) {
      this.quickFiltersDiv = Fluent(jsx("div", { class: "quick-filters-bar" }));
      if (this.toolbar) {
        this.toolbar.domNode.append(jsx("div", { class: "clear" }), this.quickFiltersDiv.getNode());
      } else {
        this.quickFiltersDiv.appendTo(Fluent(jsx("div", { class: "s-Toolbar" })).insertBefore(this.slickContainer));
      }
      this.quickFiltersBar = new QuickFilterBar({
        filters,
        getTitle: /* @__PURE__ */ __name((filter) => this.determineText((pre) => pre + filter.field), "getTitle"),
        idPrefix: this.uniqueName + "_QuickFilter_",
        element: this.quickFiltersDiv
      });
      this.quickFiltersBar.onChange = (e2) => this.quickFilterChange(e2);
    }
  }
  getQuickFilters() {
    return this.allColumns.filter(function(x) {
      return x.sourceItem && x.sourceItem.quickFilter === true && (x.sourceItem.readPermission == null || Authorization.hasPermission(x.sourceItem.readPermission));
    }).map((x) => _DataGrid.propertyItemToQuickFilter(x.sourceItem)).filter((x) => x != null);
  }
  static propertyItemToQuickFilter(item) {
    return propertyItemToQuickFilter(item);
  }
  findQuickFilter(type, field) {
    if (this.quickFiltersBar != null)
      return this.quickFiltersBar.find(type, field);
    const selector = "#" + cssEscape(this.uniqueName + "_QuickFilter_" + field);
    return getWidgetFrom(this.domNode?.querySelector(selector) ?? selector, type);
  }
  tryFindQuickFilter(type, field) {
    if (this.quickFiltersBar != null)
      return this.quickFiltersBar.tryFind(type, field);
    const selector = "#" + cssEscape(this.uniqueName + "_QuickFilter_" + field);
    return tryGetWidget(this.domNode?.querySelector(selector) ?? selector, type);
  }
  createIncludeDeletedButton() {
    if (this.getIsActiveProperty() || this.getIsDeletedProperty())
      GridUtils.addIncludeDeletedToggle(this.toolbar.domNode, this.view, null, false);
  }
  getQuickSearchFields() {
    return null;
  }
  createQuickSearchInput() {
    const input = GridUtils.addQuickSearch({
      container: this.toolbar.domNode,
      view: this.view,
      fields: this.getQuickSearchFields(),
      beforeSearch: /* @__PURE__ */ __name(() => this.persistSettings(null), "beforeSearch")
    });
    input?.domNode?.setAttribute("id", this.idPrefix + "QuickSearchInput");
  }
  destroy() {
    this.onAfterInit?.clear();
    this.onDataChanged?.clear();
    this.onPersistence?.clear();
    if (this._layoutTimer) {
      this._layoutTimer = LayoutTimer.off(this._layoutTimer);
    }
    if (this.quickFiltersBar) {
      this.quickFiltersBar.destroy();
      this.quickFiltersBar = null;
    }
    if (this.toolbar) {
      this.toolbar.destroy();
      this.toolbar = null;
    }
    if (this._grid) {
      this._grid.onClick.unsubscribe(this.handleGridClick);
      this._grid.onSort.unsubscribe(this.handleGridSort);
      this._grid.onColumnsResized.unsubscribe(this.handleGridColumnsResized);
      this._grid.onColumnsReordered.unsubscribe(this.handleGridColumnsReordered);
      this._grid.destroy();
      this._grid = null;
    }
    if (this.view) {
      this.view.onDataChanged.unsubscribe(this.viewDataChanged);
      this.view.onSubmit = null;
      this.view.setFilter(null);
      this.view = null;
    }
    this.titleDiv = null;
    super.destroy();
  }
  getItemCssClass(item, index) {
    return getItemCssClass(item, this.getIsActiveProperty(), this.getIsDeletedProperty());
  }
  getItemMetadata(item, index) {
    var itemClass = this.getItemCssClass(item, index);
    if (!itemClass) {
      return new Object();
    }
    return { cssClasses: itemClass };
  }
  postProcessColumns(columns) {
    SlickHelper.setDefaults(columns, this.getLocalTextDbPrefix());
    var delta = this.getColumnWidthDelta();
    var scale = this.getColumnWidthScale();
    if (scale < 0)
      scale = 1;
    if (delta !== 0 || scale !== 1) {
      for (var col of columns) {
        if (typeof col.width === "number")
          col.width = Math.round(col.width * scale + delta);
        if (typeof col.minWidth === "number")
          col.minWidth = Math.round(col.minWidth * scale + delta);
        if (typeof col.maxWidth === "number")
          col.maxWidth = Math.round(col.maxWidth * scale + delta);
      }
    }
    return columns;
  }
  getColumnWidthDelta() {
    return _DataGrid.defaultColumnWidthDelta ?? 0;
  }
  getColumnWidthScale() {
    return _DataGrid.defaultColumnWidthScale ?? 1;
  }
  initialPopulate() {
    var self2 = this;
    if (this.populateWhenVisible()) {
      LazyLoadHelper.executeEverytimeWhenShown(this.domNode, function() {
        self2.refreshIfNeeded();
      }, false);
      if (Fluent.isVisibleLike(this.domNode) && this.view) {
        this.view.populate();
      }
    } else if (this.view) {
      this.view.populate();
    }
  }
  canFilterColumn(column) {
    return column.sourceItem != null && column.sourceItem.notFilterable !== true && (column.sourceItem.readPermission == null || Authorization.hasPermission(column.sourceItem.readPermission));
  }
  initializeFilterBar() {
    this.filterBar.set_store(new FilterStore(
      this.allColumns.filter((c2) => this.canFilterColumn(c2)).map((x) => x.sourceItem)
    ));
    this.filterBar.get_store().add_changed(bindThis(this).filterStoreChanged);
  }
  filterStoreChanged() {
    if (this.restoringSettings <= 0) {
      this.persistSettings();
      this.view && (this.view.seekToPage = 1);
      this.refresh();
    }
  }
  /**
   * Creates initial column set for this grid. This column set is then passed
   * to postProcessColumns to adjust widths etc, and then used as the initial
   * columns for the slickgrid.
   */
  createColumns() {
    const items = this.getPropertyItems();
    return this.propertyItemsToColumns(items);
  }
  /**
   * Creates the SleekGrid columns. This method calls createColumns (via getColumns for compatibility) and then post processes them.
   * @returns The SleekGrid columns.
   */
  createSleekColumns() {
    const columns = this.getColumns();
    return this.postProcessColumns(columns || []) || [];
  }
  /** Override initSleekGrid to add plugins to the sleekgrid */
  createSlickGrid() {
    const columns = this.createSleekColumns();
    const slickOptions = this.getSlickOptions();
    this._grid = new SleekGrid(this.slickContainer.getNode(), this.view, columns, slickOptions);
    this._grid.registerPlugin(new AutoTooltips({ enableForHeaderCells: true }));
    this.setInitialSortOrder();
    return this._grid;
  }
  initSleekGrid() {
  }
  setInitialSortOrder() {
    var sortBy = this.getDefaultSortBy();
    if (this.view) {
      this.view.sortBy = Array.prototype.slice.call(sortBy);
    }
    var mapped = sortBy.map(function(s2) {
      var x;
      if (s2 && s2.toLowerCase().endsWith(" desc")) {
        return {
          columnId: s2.substr(0, s2.length - 5).trimEnd(),
          sortAsc: false
        };
      } else return {
        columnId: s2,
        sortAsc: true
      };
      return x;
    });
    this._grid.setSortColumns(mapped);
  }
  itemAt(row) {
    return this._grid.getDataItem(row);
  }
  itemId(item) {
    return item[this.getIdProperty()];
  }
  rowCount() {
    return this._grid.getDataLength();
  }
  getItems() {
    return this.view.getItems();
  }
  setItems(value) {
    this.view.setItems(value, true);
  }
  handleGridSort(e2) {
    sleekGridOnSort(this.view, e2.args);
    this.persistSettings();
  }
  handleGridClick(e2) {
    this.onClick(e2, e2.row, e2.cell);
  }
  handleGridColumnsReordered() {
    this.persistSettings();
  }
  handleGridColumnsResized() {
    this.persistSettings();
  }
  bindToSlickEvents() {
    const boundThis = bindThis(this);
    this._grid.onSort.subscribe(boundThis.handleGridSort);
    this._grid.onClick.subscribe(boundThis.handleGridClick);
    this._grid.onColumnsReordered.subscribe(boundThis.handleGridColumnsReordered);
    this._grid.onColumnsResized.subscribe(boundThis.handleGridColumnsResized);
  }
  getAddButtonCaption() {
    return DataGridTexts.asTry().NewButton ?? "New";
  }
  getButtons() {
    return [];
  }
  editItem(entityOrId) {
    throw new Error("Not Implemented!");
  }
  editItemOfType(itemType, entityOrId) {
    if (itemType === this.getItemType()) {
      this.editItem(entityOrId);
      return;
    }
    throw new Error("Not Implemented!");
  }
  onClick(e2, row, cell) {
    if (Fluent.isDefaultPrevented(e2)) {
      return;
    }
    var target = e2.target;
    if (!target.classList.contains("s-EditLink")) {
      target = target.closest("a");
    }
    if (target && target.classList.contains("s-EditLink")) {
      e2.preventDefault();
      this.editItemOfType(target.dataset.itemType, target.dataset.itemId);
    }
  }
  viewDataChanged() {
    this.onDataChanged.notify({ dataGrid: this });
    this.markupReady();
    this.layout();
  }
  bindToViewEvents() {
    const boundThis = bindThis(this);
    this.view.onDataChanged.subscribe(boundThis.viewDataChanged);
    this.view.setFilter(boundThis.handleViewFilter);
    this.view.onSubmit = boundThis.handleViewSubmit;
    this.view.onProcessData = boundThis.handleViewProcessData;
  }
  handleViewFilter(item) {
    if (!this.onViewFilter(item))
      return false;
    const e2 = { dataGrid: this, item, isMatch: true };
    this.onFiltering.notify(e2, { isCancelled: /* @__PURE__ */ __name((x) => !x.isMatch, "isCancelled") });
    return e2.isMatch;
  }
  handleViewProcessData(response) {
    response = this.onViewProcessData(response);
    const e2 = { dataGrid: this, response };
    this.onProcessData.notify(e2);
    return e2.response;
  }
  handleViewSubmit() {
    if (!this.onViewSubmit())
      return false;
    const e2 = { dataGrid: this, cancel: false };
    this.onSubmitting.notify(e2, { isCancelled: /* @__PURE__ */ __name((x) => x.cancel, "isCancelled") });
    return !e2.cancel;
  }
  onViewProcessData(response) {
    return response;
  }
  onViewFilter(item) {
    return true;
  }
  getIncludeColumns(include) {
    var columns = this._grid.getColumns();
    for (var column of columns) {
      if (column.field) {
        include[column.field] = true;
      }
      if (column.referencedFields) {
        for (var x of column.referencedFields) {
          include[x] = true;
        }
      }
    }
  }
  setCriteriaParameter() {
    delete this.view.params["Criteria"];
    if (this.filterBar) {
      var criteria = this.filterBar.get_store().get_activeCriteria();
      if (!Criteria.isEmpty(criteria)) {
        this.view.params.Criteria = criteria;
      }
    }
  }
  setEquality(field, value) {
    setEquality(this.view.params, field, value);
  }
  setIncludeColumnsParameter() {
    var include = {};
    this.getIncludeColumns(include);
    var array = [];
    for (var key of Object.keys(include)) {
      array.push(key);
    }
    this.view.params.IncludeColumns = array;
  }
  onViewSubmit() {
    if (!this.getGridCanLoad())
      return false;
    this.setCriteriaParameter();
    this.setIncludeColumnsParameter();
    this.invokeSubmitHandlers();
    return true;
  }
  markupReady() {
  }
  createSlickContainer() {
    return Fluent(jsx("div", { class: "grid-container" })).appendTo(this.domNode);
  }
  createView() {
    var opt = this.getViewOptions();
    return new RemoteView(opt);
  }
  getDefaultSortBy() {
    return getDefaultSortBy(this._grid);
  }
  usePager() {
    return false;
  }
  enableAdvancedFiltering() {
    return this.getCustomAttribute(AdvancedFilteringAttribute)?.value ?? (typeof dataGridDefaults.enableAdvancedFiltering === "function" ? dataGridDefaults.enableAdvancedFiltering(this) : dataGridDefaults.enableAdvancedFiltering) ?? false;
  }
  populateWhenVisible() {
    return false;
  }
  createFilterBar() {
    this.filterBar = new FilterDisplayBar({
      element: /* @__PURE__ */ __name((el) => {
        const after = this.quickFiltersBar?.domNode ?? this.toolbar?.domNode;
        after ? Fluent(el).insertAfter(after) : this.domNode.prepend(el);
      }, "element")
    });
    this.initializeFilterBar();
  }
  getPagerOptions() {
    return {
      view: this.view,
      rowsPerPage: 20,
      rowsPerPageOptions: [20, 100, 500, 2500]
    };
  }
  createPager() {
    new SlickPager({ ...this.getPagerOptions(), element: /* @__PURE__ */ __name((el) => this.domNode.append(el), "element") });
  }
  getViewOptions() {
    var opt = {};
    opt.idField = this.getIdProperty();
    opt.sortBy = this.getDefaultSortBy();
    if (!this.usePager()) {
      opt.rowsPerPage = 0;
    } else if (this.domNode.classList.contains("responsive-height")) {
      opt.rowsPerPage = window.innerWidth < 768 ? 20 : 100;
    } else {
      opt.rowsPerPage = 100;
    }
    opt.getItemMetadata = (item, index) => {
      return this.getItemMetadata(item, index);
    };
    return opt;
  }
  createToolbar(buttons) {
    this.toolbar = new Toolbar({
      buttons,
      hotkeyContext: this.domNode,
      element: /* @__PURE__ */ __name((el) => this.domNode.appendChild(el).classList.add("grid-toolbar"), "element")
    }).init();
  }
  getTitle() {
    if (!this.titleDiv) {
      return null;
    }
    return this.titleDiv.findFirst(".title-text").text();
  }
  setTitle(value) {
    if (value !== this.getTitle()) {
      if (value == null) {
        if (this.titleDiv) {
          this.titleDiv.remove();
          this.titleDiv = null;
        }
      } else {
        if (!this.titleDiv) {
          this.titleDiv = Fluent(jsx("div", { class: "grid-title", children: jsx("div", { class: "title-text" }) })).prependTo(this.domNode);
        }
        this.titleDiv.findFirst(".title-text").text(value);
      }
      this.layout();
    }
  }
  getItemType() {
    return "Item";
  }
  itemLink(itemType, idField, text2, cssClass, encode = true) {
    return SlickFormatting.itemLink(itemType ?? this.getItemType(), idField ?? this.getIdProperty(), text2, cssClass, encode);
  }
  getColumnsKey() {
    return null;
  }
  getPropertyItems() {
    return this.propertyItemsData?.items || [];
  }
  getPropertyItemsData() {
    var columnsKey = this.getColumnsKey();
    if (this.getColumnsKey === _DataGrid.prototype.getColumnsKey && this.getPropertyItems !== _DataGrid.prototype.getPropertyItems && !ScriptData.canLoad("Columns." + columnsKey)) {
      return {
        items: this.getPropertyItems(),
        additionalItems: []
      };
    }
    if (columnsKey) {
      return getColumnsData(columnsKey);
    }
    return { items: [], additionalItems: [] };
  }
  async getPropertyItemsDataAsync() {
    var columnsKey = this.getColumnsKey();
    if (columnsKey) {
      return await getColumnsDataAsync(columnsKey);
    }
    return { items: [], additionalItems: [] };
  }
  /** @deprecated override createColumns */
  getColumns() {
    return this.createColumns();
  }
  wrapFormatterWithEditLink(column, item) {
    const orgFormat = column.format;
    const itemType = item.editLinkItemType || null;
    const idField = item.editLinkIdField || null;
    const linkClass = item.editLinkCssClass || null;
    column.format = this.itemLink(
      itemType,
      idField,
      (ctx) => orgFormat != null ? orgFormat(ctx) : ctx.escape(),
      () => linkClass,
      /*encode*/
      false
    );
    if (idField) {
      column.referencedFields = column.referencedFields || [];
      column.referencedFields.push(idField);
    }
  }
  propertyItemsToColumns(propertyItems) {
    var columns = PropertyItemColumnConverter.toColumns(propertyItems);
    for (var i2 = 0; i2 < propertyItems.length; i2++) {
      var item = propertyItems[i2];
      if (item.editLink) {
        this.wrapFormatterWithEditLink(columns[i2], item);
      }
    }
    return columns;
  }
  getSlickOptions() {
    var opt = {};
    opt.multiSelect = false;
    opt.multiColumnSort = true;
    opt.enableCellNavigation = false;
    if (!getjQuery()) {
      opt.emptyNode = Fluent.empty;
      opt.removeNode = Fluent.remove;
    }
    if (_DataGrid.defaultRowHeight)
      opt.rowHeight = _DataGrid.defaultRowHeight;
    return opt;
  }
  populateLock() {
    this.view.populateLock();
  }
  populateUnlock() {
    this.view.populateUnlock();
  }
  getGridCanLoad() {
    const e2 = { dataGrid: this, cancel: false };
    this.onCanSubmit.notify(e2);
    if (e2.cancel)
      return false;
    return true;
  }
  /** 
   * Prepares submit arguments in this.view.params by calling this.view.onSubmit if available, or this.handleViewSubmit if not. 
   * Note that if getGridCanLoad returns false, the prepared arguments might be in a incomplete state. */
  prepareSubmit() {
    if (this.view?.onSubmit) {
      const result = this.view.onSubmit(this.view);
      return result == null || !!result;
    }
    return this.handleViewSubmit();
  }
  refresh() {
    if (!this.populateWhenVisible()) {
      this.internalRefresh();
      return;
    }
    if (Fluent.isVisibleLike(this.slickContainer.getNode())) {
      this.slickContainer.data("needsRefresh", null);
      this.internalRefresh();
      return;
    }
    this.slickContainer.getNode().dataset.needsRefresh = "true";
  }
  refreshIfNeeded() {
    if (!!this.slickContainer.data("needsRefresh")) {
      this.slickContainer.data("needsRefresh", null);
      this.internalRefresh();
    }
  }
  internalRefresh() {
    this.view.populate();
  }
  get readOnly() {
    return this.get_readOnly();
  }
  set readOnly(value) {
    this.set_readOnly(value);
  }
  get_readOnly() {
    return !!this._readonly;
  }
  set_readOnly(value) {
    if (!!this._readonly != !!value) {
      this._readonly = !!value;
      this.updateInterface();
    }
  }
  updateInterface() {
    this.toolbar && this.toolbar.updateInterface();
  }
  getRowDefinition() {
    return null;
  }
  getLocalTextDbPrefix() {
    if (this._localTextDbPrefix != null)
      return this._localTextDbPrefix;
    this._localTextDbPrefix = this.getLocalTextPrefix() ?? "";
    if (this._localTextDbPrefix.length > 0 && !this._localTextDbPrefix.endsWith("."))
      this._localTextDbPrefix = "Db." + this._localTextDbPrefix + ".";
    return this._localTextDbPrefix;
  }
  getLocalTextPrefix() {
    var rowDefinition = this.getRowDefinition();
    if (rowDefinition)
      return rowDefinition.localTextPrefix;
    return void 0;
  }
  getIdProperty() {
    if (this._idProperty != null)
      return this._idProperty;
    var rowDefinition = this.getRowDefinition();
    if (rowDefinition)
      return this._idProperty = rowDefinition.idProperty ?? "";
    return this._idProperty = "ID";
  }
  getIsDeletedProperty() {
    return this.getRowDefinition()?.isDeletedProperty;
  }
  getIsActiveProperty() {
    if (this._isActiveProperty != null)
      return this._isActiveProperty;
    var rowDefinition = this.getRowDefinition();
    if (rowDefinition)
      return this._isActiveProperty = rowDefinition.isActiveProperty ?? "";
    return this._isActiveProperty = "";
  }
  resizeCanvas() {
    this._grid?.resizeCanvas();
  }
  subDialogDataChange() {
    this.refresh();
  }
  addFilterSeparator() {
    this.ensureQuickFilterBar().addSeparator();
  }
  determineText(getKey) {
    var localTextPrefix = this.getLocalTextDbPrefix();
    if (localTextPrefix) {
      var local2 = tryGetText(getKey(localTextPrefix));
      if (local2 != null) {
        return local2;
      }
    }
    return null;
  }
  addQuickFilter(opt) {
    return this.ensureQuickFilterBar().add(opt);
  }
  addDateRangeFilter(field, title) {
    return this.ensureQuickFilterBar().addDateRange(field, title);
  }
  dateRangeQuickFilter(field, title) {
    return QuickFilterBar.dateRange(field, title);
  }
  addDateTimeRangeFilter(field, title) {
    return this.ensureQuickFilterBar().addDateTimeRange(field, title);
  }
  dateTimeRangeQuickFilter(field, title) {
    return QuickFilterBar.dateTimeRange(field, title);
  }
  addBooleanFilter(field, title, yes, no) {
    return this.ensureQuickFilterBar().addBoolean(field, title, yes, no);
  }
  booleanQuickFilter(field, title, yes, no) {
    return QuickFilterBar.boolean(field, title, yes, no);
  }
  invokeSubmitHandlers() {
    if (this.quickFiltersBar != null) {
      this.quickFiltersBar.onSubmit(this.view.params);
    }
  }
  quickFilterChange(e2) {
    this.persistSettings();
    this.view && (this.view.seekToPage = 1);
    this.refresh();
  }
  getPersistenceStorage() {
    if (this.getPersistanceStorage) return this.getPersistanceStorage();
    return _DataGrid.defaultPersistenceStorage;
  }
  getPersistenceKey() {
    if (this.getPersistanceKey) return this.getPersistanceKey;
    var key = "GridSettings:";
    var path = window.location.pathname;
    if (path) {
      key += path.substring(1).split(String.fromCharCode(47)).slice(0, 2).join("/") + ":";
    }
    key += getTypeFullName(getInstanceType(this));
    return key;
  }
  gridPersistenceFlags() {
    if (this.gridPersistanceFlags) return this.gridPersistanceFlags;
    return {};
  }
  canShowColumn(column) {
    if (column == null) {
      return false;
    }
    var item = column.sourceItem;
    if (item == null) {
      return true;
    }
    if (item.filterOnly === true) {
      return false;
    }
    if (item.readPermission == null) {
      return true;
    }
    return Authorization.hasPermission(item.readPermission);
  }
  getPersistedSettings() {
    var storage = this.getPersistenceStorage();
    if (storage == null)
      return null;
    function fromJson(json) {
      json = json?.trim();
      if (json?.startsWith("{") && json.endsWith("}"))
        return JSON.parse(json);
      return null;
    }
    __name(fromJson, "fromJson");
    var jsonOrPromise = storage.getItem(this.getPersistenceKey());
    if (jsonOrPromise?.then)
      return jsonOrPromise.then((json) => fromJson(json));
    return fromJson(jsonOrPromise);
  }
  restoreSettings(settings, flags) {
    if (settings != null)
      return this.restoreSettingsFrom(settings, flags);
    var settingsOrPromise = this.getPersistedSettings();
    if (settingsOrPromise?.then)
      return settingsOrPromise.then((s2) => this.restoreSettingsFrom(s2));
    this.restoreSettingsFrom(settingsOrPromise);
  }
  restoreSettingsFrom(settings, flags) {
    if (!this._grid || !settings)
      return;
    this.view.beginUpdate();
    this.restoringSettings++;
    try {
      const flagsDefault = this.gridPersistenceFlags();
      const event = {
        after: false,
        dataGrid: this,
        flagsArgument: flags,
        flagsDefault,
        flagsToUse: flags || flagsDefault,
        restoring: true,
        persisting: false,
        settings
      };
      this.onPersistence.notify(event);
      event.flagsToUse ??= event.flagsDefault;
      restoreSettingsFrom({
        canShowColumn: bindThis(this).canShowColumn,
        filterStore: this.filterBar?.get_store(),
        flags: event.flagsToUse,
        includeDeletedToggle: this.domNode.querySelector(".s-IncludeDeletedToggle"),
        quickFiltersDiv: this.quickFiltersDiv?.getNode(),
        sleekGrid: this._grid,
        settings: event.settings,
        toolbarNode: this.toolbar?.domNode,
        uniqueName: this.uniqueName,
        view: this.view
      });
      event.after = true;
      this.onPersistence.notify(event);
    } finally {
      this.restoringSettings--;
      this.view.endUpdate();
    }
  }
  persistenceLock() {
    this._persistenceLock++;
  }
  persistenceUnlock() {
    this._persistenceLock--;
  }
  persistSettings(flags) {
    if (this._persistenceLock > 0)
      return;
    var storage = this.getPersistenceStorage();
    if (!storage) {
      return;
    }
    var settings = this.getCurrentSettings(flags);
    return storage.setItem(this.getPersistenceKey(), JSON.stringify(settings));
  }
  getCurrentSettings(flags) {
    const flagsDefault = this.gridPersistenceFlags();
    const event = {
      after: false,
      dataGrid: this,
      flagsArgument: flags,
      flagsDefault,
      flagsToUse: flags || flagsDefault,
      restoring: false,
      persisting: true,
      settings: null
    };
    event.flagsToUse ||= event.flagsDefault;
    event.settings = getCurrentSettings({
      filterStore: this.filterBar?.get_store(),
      flags,
      includeDeletedToggle: this.domNode.querySelector(".s-IncludeDeletedToggle"),
      quickFiltersDiv: this.quickFiltersDiv?.getNode(),
      sleekGrid: this._grid,
      toolbarNode: this.toolbar?.domNode,
      uniqueName: this.uniqueName
    });
    event.after = true;
    this.onPersistence.notify(event);
    return event.settings;
  }
  getElement() {
    return this.domNode;
  }
  getGrid() {
    return this._grid;
  }
  get sleekGrid() {
    return this._grid;
  }
  set sleekGrid(value) {
    this._grid = value;
  }
  /** @deprecated Use sleekGrid or getGrid() */
  get slickGrid() {
    return this._grid;
  }
  getView() {
    return this.view;
  }
  getFilterStore() {
    return this.filterBar == null ? null : this.filterBar.get_store();
  }
  get allColumns() {
    return this._grid?.getAllColumns();
  }
  get columns() {
    return this._grid?.getColumns();
  }
  get initialSettings() {
    return this._initialSettings;
  }
  set initialSettings(value) {
    this._initialSettings = value;
  }
  /** @deprecated use defaultPersistenceStorage, this one has a typo */
  static get defaultPersistanceStorage() {
    return _DataGrid.defaultOptions.persistenceStorage;
  }
  /** @deprecated use defaultPersistenceStorage, this one has a typo */
  static set defaultPersistanceStorage(value) {
    _DataGrid.defaultOptions.persistenceStorage = value;
  }
};

// src/ui/datagrid/entitygrid.ts
var _a51, _b34;
var EntityGrid = class extends (_b34 = DataGrid, _a51 = Symbol.typeInfo, _b34) {
  static {
    __name(this, "EntityGrid");
  }
  static {
    this[_a51] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("route-handler");
    Fluent.on(this.domNode, "handleroute." + this.uniqueName, bindThis(this).handleRoute);
  }
  destroy() {
    Fluent.off(document, "." + this.uniqueName + "_routerfix");
    super.destroy();
  }
  handleRoute(e2) {
    let route = Fluent.eventProp(e2, "route");
    if (typeof route !== "string")
      return;
    if (route === "new") {
      e2.preventDefault();
      this.addButtonClick();
      return;
    }
    var oldRequests = getActiveRequests();
    var parts = route.split("/");
    if (parts.length === 2 && parts[0] === "edit") {
      e2.preventDefault();
      this.editItem(decodeURIComponent(parts[1]));
    } else if (parts.length === 2 && parts[1] === "new") {
      e2.preventDefault();
      this.editItemOfType(parts[0], null);
    } else if (parts.length === 3 && parts[1] === "edit") {
      e2.preventDefault();
      this.editItemOfType(parts[0], decodeURIComponent(parts[2]));
    } else
      return;
    if (!Fluent.eventProp(e2, "isInitial"))
      return;
    Fluent.off(document, "." + this.uniqueName + "_routerfix");
    let evParts = Fluent.eventProp(e2, "parts");
    let evIndex = Fluent.eventProp(e2, "index");
    if (getActiveRequests() > oldRequests && evParts != null && evIndex != null && evIndex >= 0 && evIndex < evParts.length - 1 && !evParts[evIndex + 1].startsWith("!") && Fluent.isDefaultPrevented(e2)) {
      Fluent.one(document, "ajaxStop." + this.uniqueName + "_routerfix", () => {
        window.location.hash = "#" + evParts.join("/+/");
      });
    }
  }
  usePager() {
    return true;
  }
  createToolbarExtensions() {
    this.createIncludeDeletedButton();
    this.createQuickSearchInput();
  }
  getInitialTitle() {
    return this.getDisplayName();
  }
  getLocalTextPrefix() {
    var result = super.getLocalTextPrefix();
    if (result != null || this.getRowDefinition())
      return result;
    return this.getEntityType();
  }
  getEntityType() {
    if (this._entityType != null)
      return this._entityType;
    var name = getTypeFullName(getInstanceType(this));
    var px = name.indexOf(".");
    if (px >= 0) {
      name = name.substring(px + 1);
    }
    if (name.endsWith("Grid")) {
      name = name.substring(0, name.length - 4);
    } else if (name.endsWith("SubGrid")) {
      name = name.substring(0, name.length - 7);
    }
    this._entityType = name;
    return this._entityType;
  }
  getDisplayName() {
    if (this._displayName != null)
      return this._displayName;
    return this._displayName = localText(this.getLocalTextDbPrefix() + "EntityPlural", this.getEntityType());
  }
  getItemName() {
    if (this._itemName != null)
      return this._itemName;
    return this._itemName = localText(this.getLocalTextDbPrefix() + "EntitySingular", this.getEntityType());
  }
  getAddButtonCaption() {
    return stringFormat(EntityGridTexts.NewButton, this.getItemName());
  }
  getButtons() {
    var buttons = [];
    buttons.push({
      title: this.getAddButtonCaption(),
      action: "add",
      cssClass: "add-button",
      icon: faIcon("plus-circle", "green"),
      hotkey: "alt+n",
      onClick: /* @__PURE__ */ __name(() => {
        this.addButtonClick();
      }, "onClick"),
      disabled: /* @__PURE__ */ __name(() => !this.hasInsertPermission() || this.readOnly, "disabled")
    });
    buttons.push(this.newRefreshButton(true));
    buttons.push(ColumnPickerDialog.createToolButton(this));
    buttons.push(FilterDisplayBar.createToolButton({
      onClick: /* @__PURE__ */ __name(() => {
        if (!this.filterBar)
          return;
        const dialog = new FilterDialog({});
        dialog.get_filterPanel().set_store(this.filterBar.get_store());
        dialog.dialogOpen(null);
      }, "onClick"),
      visible: /* @__PURE__ */ __name(() => this.filterBar != null, "visible")
    }));
    return buttons;
  }
  setFilterBarVisibility() {
    if (!this.filterBar.domNode)
      return;
    if (this.filterBar.domNode.hidden != !this.filterBar?.get_store()?.get_items()?.length) {
      Fluent.toggle(this.filterBar.domNode);
      this.layout();
    }
  }
  createFilterBar() {
    super.createFilterBar();
    this.setFilterBarVisibility();
  }
  filterStoreChanged() {
    super.filterStoreChanged();
    this.setFilterBarVisibility();
  }
  newRefreshButton(noText) {
    return {
      title: noText ? null : EntityGridTexts.RefreshButton,
      hint: noText ? EntityGridTexts.RefreshButton : null,
      icon: faIcon("refresh", "blue"),
      action: "refresh",
      cssClass: "refresh-button",
      onClick: /* @__PURE__ */ __name(() => {
        this.refresh();
      }, "onClick")
    };
  }
  addButtonClick() {
    this.editItem(new Object());
  }
  editItem(entityOrId) {
    this.createEntityDialog(this.getItemType(), (dlg) => {
      var dialog = safeCast(dlg, IEditDialog);
      if (dialog != null) {
        dialog.load(entityOrId, () => {
          dialog.dialogOpen(this.openDialogsAsPanel ?? DataGrid.defaultOptions.openDialogsAsPanel);
        });
        return;
      }
      throw new Error(
        stringFormat(
          "{0} doesn't implement IEditDialog!",
          getTypeFullName(getInstanceType(dlg))
        )
      );
    });
  }
  editItemOfType(itemType, entityOrId) {
    if (itemType === this.getItemType()) {
      this.editItem(entityOrId);
      return;
    }
    this.createEntityDialog(itemType, (dlg) => {
      var dialog = safeCast(dlg, IEditDialog);
      if (dialog != null) {
        dialog.load(entityOrId, () => {
          dialog.dialogOpen(this.openDialogsAsPanel ?? DataGrid.defaultOptions.openDialogsAsPanel);
        });
        return;
      }
      throw new Error(
        stringFormat(
          "{0} doesn't implement IEditDialog!",
          getTypeFullName(getInstanceType(dlg))
        )
      );
    });
  }
  getService() {
    if (this._service != null)
      return this._service;
    return this._service = replaceAll(this.getEntityType(), ".", "/");
  }
  getServiceMethod() {
    return this.getService() + "/List";
  }
  getServiceUrl() {
    return resolveUrl("~/Services/" + this.getServiceMethod());
  }
  getViewOptions() {
    var opt = super.getViewOptions();
    opt.url = this.getServiceUrl();
    return opt;
  }
  getItemType() {
    return this.getEntityType();
  }
  routeDialog(itemType, dialog) {
    Router && Router.dialog && Router.dialog(this.domNode, dialog.domNode, () => {
      var hash = "";
      if (itemType !== this.getItemType())
        hash = itemType + "/";
      if (dialog != null && dialog.entityId != null)
        hash += "edit/" + dialog.entityId.toString();
      else
        hash += "new";
      return hash;
    });
  }
  getInsertPermission() {
    return this.getRowDefinition()?.insertPermission;
  }
  hasInsertPermission() {
    var insertPermission = this.getInsertPermission();
    return insertPermission == null || Authorization.hasPermission(this.getInsertPermission());
  }
  transferDialogReadOnly(dialog) {
    if (this.readOnly)
      EditorUtils.setReadOnly(dialog, true);
  }
  initDialog(dialog) {
    SubDialogHelper.bindToDataChange(dialog, this, (_2) => {
      this.subDialogDataChange();
    }, true);
    this.transferDialogReadOnly(dialog);
    this.routeDialog(this.getItemType(), dialog);
  }
  initEntityDialog(itemType, dialog) {
    if (itemType === this.getItemType()) {
      this.initDialog(dialog);
      return;
    }
    SubDialogHelper.bindToDataChange(dialog, this, (_2) => {
      this.subDialogDataChange();
    }, true);
    this.transferDialogReadOnly(dialog);
    this.routeDialog(itemType, dialog);
  }
  createEntityDialog(itemType, callback) {
    const dialogType = this.getDialogTypeFor(itemType);
    const then = /* @__PURE__ */ __name((dialogType2) => {
      var dialog = Widget.create({
        type: dialogType2,
        options: this.getDialogOptionsFor(itemType)
      });
      this.initEntityDialog(itemType, dialog);
      callback?.(dialog);
      return dialog;
    }, "then");
    if (isPromiseLike(dialogType)) {
      return dialogType.then(then);
    } else {
      return then(dialogType);
    }
  }
  getDialogOptions() {
    return {};
  }
  getDialogOptionsFor(itemType) {
    if (itemType === this.getItemType())
      return this.getDialogOptions();
    return {};
  }
  getDialogTypeFor(itemType) {
    if (itemType === this.getItemType()) {
      return this.getDialogType();
    }
    return DialogTypeRegistry.getOrLoad(itemType);
  }
  getDialogType() {
    if (this._dialogType != null)
      return this._dialogType;
    const promise = DialogTypeRegistry.getOrLoad(this.getEntityType());
    if (isPromiseLike(promise)) {
      return promise.then((t2) => {
        this._dialogType = t2;
        return t2;
      });
    }
    this._dialogType = promise;
    return this._dialogType;
  }
};

// src/ui/helpers/slicktreehelper.tsx
var SlickTreeHelper;
((SlickTreeHelper2) => {
  function filterCustom(item, getParent) {
    var parent2 = getParent(item);
    var loop = 0;
    while (parent2 != null) {
      if (!!parent2._collapsed) {
        return false;
      }
      parent2 = getParent(parent2);
      if (loop++ > 1e3) {
        throw new Error(
          "Possible infinite loop, check parents has no circular reference!"
        );
      }
    }
    return true;
  }
  SlickTreeHelper2.filterCustom = filterCustom;
  __name(filterCustom, "filterCustom");
  function filterById(item, view, getParentId) {
    return filterCustom(item, function(x) {
      var parentId = getParentId(x);
      if (parentId == null) {
        return null;
      }
      return view.getItemById(parentId);
    });
  }
  SlickTreeHelper2.filterById = filterById;
  __name(filterById, "filterById");
  function setCollapsed(items, collapsed) {
    if (items != null) {
      for (var item of items) {
        item._collapsed = collapsed;
      }
    }
  }
  SlickTreeHelper2.setCollapsed = setCollapsed;
  __name(setCollapsed, "setCollapsed");
  function setCollapsedFlag(item, collapsed) {
    item._collapsed = collapsed;
  }
  SlickTreeHelper2.setCollapsedFlag = setCollapsedFlag;
  __name(setCollapsedFlag, "setCollapsedFlag");
  function setIndents(items, getId, getParentId, setCollapsed2) {
    var depth = 0;
    var depths = {};
    for (var line = 0; line < items.length; line++) {
      var item = items[line];
      if (line > 0) {
        var parentId = getParentId(item);
        if (parentId != null && parentId === getId(items[line - 1])) {
          depth += 1;
        } else if (parentId == null) {
          depth = 0;
        } else if (parentId !== getParentId(items[line - 1])) {
          if (depths[parentId] != null) {
            depth = depths[parentId] + 1;
          } else {
            depth = 0;
          }
        }
      }
      depths[getId(item)] = depth;
      item._indent = depth;
      if (setCollapsed2 != null) {
        item._collapsed = setCollapsed2;
      }
    }
  }
  SlickTreeHelper2.setIndents = setIndents;
  __name(setIndents, "setIndents");
  function toggleClick(e2, row, cell, view, getId) {
    if (!e2 || !e2.target || Fluent.isDefaultPrevented(e2))
      return;
    var target = e2.target;
    if (!target.classList.contains("s-TreeToggle")) {
      return;
    }
    e2.preventDefault();
    if (target.classList.contains("s-TreeCollapse") || target.classList.contains("s-TreeExpand")) {
      var item = view.getItem(row);
      if (item != null) {
        if (!!!item._collapsed) {
          item._collapsed = true;
        } else {
          item._collapsed = false;
        }
        view.updateItem(getId(item), item);
      }
      if (e2.shiftKey) {
        view.beginUpdate();
        try {
          setCollapsed(view.getItems(), !!item._collapsed);
          view.setItems(view.getItems(), true);
        } finally {
          view.endUpdate();
        }
      }
    }
  }
  SlickTreeHelper2.toggleClick = toggleClick;
  __name(toggleClick, "toggleClick");
})(SlickTreeHelper || (SlickTreeHelper = {}));

// src/ui/datagrid/treegridmixin.ts
var TreeGridMixin = class _TreeGridMixin {
  constructor(options) {
    this.options = options;
    var dg = this.dataGrid = options.grid;
    var idProperty = dg.getIdProperty();
    var getId = /* @__PURE__ */ __name((item) => item[idProperty], "getId");
    Fluent.on(dg.domNode.querySelector(".grid-container"), "click", (e2) => {
      if (e2.target.classList.contains("s-TreeToggle")) {
        var src = dg.sleekGrid.getCellFromEvent(e2);
        if (src.cell >= 0 && src.row >= 0) {
          SlickTreeHelper.toggleClick(e2, src.row, src.row, dg.view, getId);
        }
      }
    });
    dg.onFiltering.subscribe((e2) => {
      e2.isMatch = SlickTreeHelper.filterById(e2.item, dg.view, options.getParentId);
    });
    dg.onProcessData.subscribe((e2) => {
      e2.response.Entities = _TreeGridMixin.applyTreeOrdering(e2.response.Entities, getId, options.getParentId);
      SlickTreeHelper.setIndents(
        e2.response.Entities,
        getId,
        options.getParentId,
        options.initialCollapse && options.initialCollapse() || false
      );
    });
    if (options.toggleField) {
      var col = dg.allColumns?.find((x) => x.field == options.toggleField || x.id == options.toggleField);
      if (col) {
        col.format = SlickFormatting.treeToggle(
          () => dg.view,
          getId,
          col.format || ((ctx) => ctx.escape())
        );
      }
    }
  }
  static {
    __name(this, "TreeGridMixin");
  }
  /**
   * Expands / collapses all rows in a grid automatically
   */
  toggleAll() {
    SlickTreeHelper.setCollapsed(
      this.dataGrid.view.getItems(),
      !this.dataGrid.view.getItems().every((x) => x._collapsed == true)
    );
    this.dataGrid.view.setItems(this.dataGrid.view.getItems(), true);
  }
  collapseAll() {
    SlickTreeHelper.setCollapsed(this.dataGrid.view.getItems(), true);
    this.dataGrid.view.setItems(this.dataGrid.view.getItems(), true);
  }
  expandAll() {
    SlickTreeHelper.setCollapsed(this.dataGrid.view.getItems(), false);
    this.dataGrid.view.setItems(this.dataGrid.view.getItems(), true);
  }
  /**
   * Reorders a set of items so that parents comes before their children.
   * This method is required for proper tree ordering, as it is not so easy to perform with SQL.
   * @param items list of items to be ordered
   * @param getId a delegate to get ID of a record (must return same ID with grid identity field)
   * @param getParentId a delegate to get parent ID of a record
   */
  static applyTreeOrdering(items, getId, getParentId) {
    var result = [];
    var byId = toGrouping(items, getId);
    var byParentId = toGrouping(items, getParentId);
    var visited = {};
    function takeChildren(theParentId) {
      if (visited[theParentId])
        return;
      visited[theParentId] = true;
      for (var child of byParentId[theParentId] || []) {
        result.push(child);
        takeChildren(getId(child));
      }
    }
    __name(takeChildren, "takeChildren");
    for (var item of items) {
      var parentId = getParentId(item);
      if (parentId == null || !(byId[parentId] || []).length) {
        result.push(item);
        takeChildren(getId(item));
      }
    }
    return result;
  }
};

// src/ui/widgets/propertygrid.tsx
function PropertyFieldCaption(props) {
  const labelWidth = props.item.labelWidth;
  const caption = determineText(props.localTextPrefix, props.item.title, (p2) => p2 + props.item.name) ?? "";
  return jsx(
    "label",
    {
      class: "caption",
      for: (props.idPrefix ?? "") + props.item.name,
      title: determineText(props.localTextPrefix, props.item.hint, (p2) => p2 + props.item.name + "_Hint") ?? caption,
      hidden: labelWidth == "0",
      style: { width: !!labelWidth ? labelWidth : null },
      children: [
        props.item.required && jsx("sup", { title: PropertyGridTexts.RequiredHint, children: "*" }),
        caption
      ]
    }
  );
}
__name(PropertyFieldCaption, "PropertyFieldCaption");
function PropertyFieldEditor(props) {
  const { fieldElement, item, idPrefix, localTextPrefix } = props;
  const placeHolder = determineText(localTextPrefix, item.placeholder, (p2) => p2 + item.name + "_Placeholder");
  const editorType = isPromiseLike(item.editorType) || typeof item.editorType === "function" ? item.editorType : EditorTypeRegistry.getOrLoad(item.editorType ?? "String");
  let loadingPoint;
  const then = /* @__PURE__ */ __name((editorType2) => {
    const editor = new editorType2({
      ...item.editorParams,
      id: idPrefix + item.name,
      element: /* @__PURE__ */ __name((el) => {
        !el.id && (el.id = idPrefix + item.name);
        el.classList.add("editor");
        if (item.editorCssClass)
          addClass(el, item.editorCssClass);
        if (Fluent.isInputLike(el))
          el.setAttribute("name", item.name ?? "");
        if (placeHolder)
          el.setAttribute("placeholder", placeHolder);
        let wrappedNode = el;
        if (item.editorAddons?.length) {
          wrappedNode = document.createDocumentFragment();
          wrappedNode.appendChild(el);
          for (var wrapper of item.editorAddons) {
            const wrapperComponent = typeof wrapper.type === "function" ? wrapper.type : getType(wrapper.type);
            if (typeof wrapperComponent !== "function")
              throw `Invalid editor addon type: ${wrapper.type} for property: "${item.name}"`;
            wrapperComponent({ ...wrapper.params, propertyItem: item, editorElement: el, documentFragment: wrappedNode });
          }
        }
        if (loadingPoint) {
          loadingPoint.parentElement?.replaceChild(wrappedNode, loadingPoint);
          loadingPoint = null;
          delete fieldElement.editorPromise;
        } else {
          fieldElement.append(wrappedNode);
        }
      }, "element")
    }).init();
    if (item.maxLength != null) {
      setMaxLength(editor, item.maxLength);
    }
    if (item.editorParams != null) {
      ReflectionOptionsSetter.set(editor, item.editorParams);
    }
    fieldElement.editorWidget = editor;
  }, "then");
  if (isPromiseLike(editorType)) {
    loadingPoint = document.createComment("Loading editor type...");
    fieldElement.append(loadingPoint);
    fieldElement.editorPromise = editorType.then(then);
  } else {
    then(editorType);
  }
}
__name(PropertyFieldEditor, "PropertyFieldEditor");
function PropertyFieldLineBreak(props) {
  const klass = props?.item?.formCssClass;
  if (!klass || klass.indexOf("line-break") < 0)
    return null;
  var splitted = klass.split(" ");
  if (splitted.indexOf("line-break-xs") >= 0) {
    return createLineBreak("line-break");
  }
  if (splitted.indexOf("line-break-sm") >= 0) {
    return createLineBreak("line-break hidden-xs");
  } else if (splitted.indexOf("line-break-md") >= 0) {
    return createLineBreak("line-break hidden-sm");
  } else if (splitted.indexOf("line-break-lg") >= 0) {
    return createLineBreak("line-break hidden-md");
  }
}
__name(PropertyFieldLineBreak, "PropertyFieldLineBreak");
function PropertyField(props) {
  const { item, container, localTextPrefix } = props;
  const idPrefix = props?.idPrefix ?? "";
  const fieldElement = jsx("div", { class: ["field", item.name, item.cssClass, item.formCssClass], "data-itemname": item.name, children: jsx(PropertyFieldCaption, { item, idPrefix, localTextPrefix }) });
  fieldElement.propertyItem = item;
  if (item.formCssClass && container) {
    const lineBreak = PropertyFieldLineBreak({ item });
    lineBreak && container.appendChild(lineBreak);
  }
  container?.appendChild(fieldElement);
  PropertyFieldEditor({
    fieldElement,
    item,
    idPrefix,
    localTextPrefix
  });
  fieldElement.appendChild(jsx("div", { class: "vx" }));
  fieldElement.appendChild(jsx("div", { class: "clear" }));
  return fieldElement;
}
__name(PropertyField, "PropertyField");
function PropertyCategoryTitle(props) {
  return jsx("div", { class: "category-title", children: determineText(props.localTextPrefix, props.category, (prefix) => prefix + "Categories." + props.category) });
}
__name(PropertyCategoryTitle, "PropertyCategoryTitle");
function PropertyCategory(props) {
  const categoryDiv = jsx("div", { class: "category" });
  const { category, children, collapsed, localTextPrefix } = props;
  if (category) {
    let key = category;
    let idx = category.lastIndexOf(".Categories.");
    if (idx >= 0) {
      key = category.substring(idx + 12);
    }
    categoryDiv.dataset.category = key;
    const title = categoryDiv.appendChild(jsx(PropertyCategoryTitle, { category, localTextPrefix }));
    if (collapsed != null) {
      categoryDiv.classList.add("collapsible");
      collapsed && categoryDiv.classList.add("collapsed");
      const icon = title.appendChild(jsx("i", { class: faIcon(collapsed ? "plus" : "minus") }));
      title.addEventListener("click", function() {
        categoryDiv.classList.toggle("collapsed");
        icon.classList.toggle("fa-plus");
        icon.classList.toggle("fa-minus");
      });
    }
  }
  appendToNode(categoryDiv, children);
  return categoryDiv;
}
__name(PropertyCategory, "PropertyCategory");
function PropertyTabItem(props) {
  const bs3 = isBS3();
  return jsx("li", { role: "tab", class: bs3 ? props.active ? "active" : "" : "nav-item", children: jsx(
    "a",
    {
      class: bs3 ? "" : `nav-link ${props.active ? "active" : ""}`,
      "data-bs-toggle": "tab",
      href: !!props.paneId && `#${props.paneId}`,
      "data-tabkey": extractTabKey(props.title),
      children: determineText(props.localTextPrefix, props.title, (prefix) => prefix + "Tabs." + props.title)
    }
  ) });
}
__name(PropertyTabItem, "PropertyTabItem");
function PropertyTabPane(props) {
  return jsx("div", { id: props.id, class: `tab-pane fade${props.active ? isBS3() ? " in active" : " show active" : ""}`, role: "tabpanel", children: props.children });
}
__name(PropertyTabPane, "PropertyTabPane");
function PropertyCategories(props) {
  const categoriesDiv = jsx("div", { class: "categories" });
  props.container && props.container.appendChild(categoriesDiv);
  const { items, fieldElements, idPrefix, localTextPrefix } = props;
  var categoryEl = null;
  var priorCategory = null;
  for (var i2 = 0; i2 < items.length; i2++) {
    var item = items[i2];
    var category = item.category ?? "";
    if (!categoryEl || priorCategory !== category) {
      categoryEl = categoriesDiv.appendChild(jsx(
        PropertyCategory,
        {
          category,
          localTextPrefix,
          collapsed: item.collapsible !== true ? null : item.collapsed ?? false
        }
      ));
    }
    const fieldElement = jsx(PropertyField, { item, container: categoryEl, idPrefix, localTextPrefix });
    fieldElements?.push(fieldElement);
    priorCategory = category;
  }
  return categoriesDiv;
}
__name(PropertyCategories, "PropertyCategories");
function PropertyTabList(props) {
  return jsx("ul", { class: "nav nav-underline property-tabs", role: "tablist", children: props?.children });
}
__name(PropertyTabList, "PropertyTabList");
function PropertyTabPanes(_2) {
  return jsx("div", { class: "tab-content property-panes" });
}
__name(PropertyTabPanes, "PropertyTabPanes");
function PropertyTabs(props) {
  const { items, container, fieldElements, idPrefix, localTextPrefix, paneIdPrefix } = props;
  const parentNode = container ?? document.createDocumentFragment();
  const createItems = /* @__PURE__ */ __name((container2, items2) => PropertyCategories({
    items: items2,
    container: container2,
    fieldElements,
    idPrefix,
    localTextPrefix
  }), "createItems");
  const itemsWithoutTab = items.filter((f2) => !f2.tab);
  if (itemsWithoutTab.length > 0) {
    createItems(parentNode, itemsWithoutTab);
    parentNode.appendChild(jsx("div", { class: "pad" }));
  }
  const itemsWithTab = items.filter((f2) => f2.tab);
  const tabList = parentNode.appendChild(PropertyTabList());
  const tabPanes = parentNode.appendChild(PropertyTabPanes());
  var tabIndex = 0;
  var i2 = 0;
  while (i2 < itemsWithTab.length) {
    var title = itemsWithTab[i2].tab?.trim() ?? "";
    var withSameTab = [];
    var j = i2;
    do {
      withSameTab.push(itemsWithTab[j]);
    } while (++j < itemsWithTab.length && (itemsWithTab[j].tab?.trim() ?? "") === title);
    i2 = j;
    var paneId = (paneIdPrefix ?? idPrefix ?? "") + "Tab" + tabIndex;
    tabList.appendChild(PropertyTabItem({ title, active: tabIndex === 0, paneId, localTextPrefix }));
    const pane = tabPanes.appendChild(PropertyTabPane({ active: tabIndex === 0, id: paneId }));
    createItems(pane, withSameTab);
    tabIndex++;
  }
  return container ? null : parentNode;
}
__name(PropertyTabs, "PropertyTabs");
var _a52, _b35;
var PropertyGrid = class _PropertyGrid extends (_b35 = Widget, _a52 = Symbol.typeInfo, _b35) {
  static {
    __name(this, "PropertyGrid");
  }
  static {
    this[_a52] = this.registerClass(nsSerenity);
  }
  renderContents() {
    this.domNode.classList.add("s-PropertyGrid");
    this.options.mode ??= 1 /* insert */;
    this.fieldElements = [];
    const items = this.options.items || [];
    const commonProps = {
      items,
      container: this.domNode,
      fieldElements: this.fieldElements,
      idPrefix: this.idPrefix,
      localTextPrefix: this.options.localTextPrefix
    };
    if (items.some((x) => !!x.tab)) {
      PropertyTabs({ ...commonProps, paneIdPrefix: this.uniqueName + "_" });
    } else {
      PropertyCategories(commonProps);
    }
    if (this.options.value !== false) {
      this.value = this.options.value ?? {};
    }
    this.updateInterface();
  }
  destroy() {
    if (this.fieldElements) {
      for (var fieldElement of this.fieldElements) {
        if (fieldElement) {
          fieldElement.editorWidget?.destroy();
          delete fieldElement.editorWidget;
          delete fieldElement.editorPromise;
          delete fieldElement.propertyItem;
        }
      }
      this.fieldElements = null;
    }
    Fluent.empty(this.domNode);
    super.destroy();
  }
  get_editors() {
    return this.fieldElements?.map((x) => x.editorWidget) ?? [];
  }
  get_items() {
    return this.fieldElements?.map((x) => x.propertyItem) ?? [];
  }
  get_idPrefix() {
    return this.idPrefix;
  }
  enumerateItems(callback) {
    for (let fieldElement of this.fieldElements) {
      var item = fieldElement.propertyItem;
      var editor = fieldElement.editorWidget;
      if (!editor && fieldElement.editorPromise)
        throw `Editor for "${item.name}" is not loaded yet.`;
      callback(item, editor);
    }
  }
  get_mode() {
    return this.options.mode;
  }
  set_mode(value) {
    if (this.options.mode !== value) {
      this.options.mode = value;
      this.updateInterface();
    }
  }
  static loadFieldValue(source, fieldElement, mode) {
    var item = fieldElement.propertyItem;
    if (!!(mode === 1 /* insert */ && item.defaultValue != null) && typeof source[item.name] === "undefined") {
      source[item.name] = item.defaultValue;
    }
    if (item.unbound ?? item.skipOnLoad)
      return;
    var editor = fieldElement.editorWidget;
    if (!editor && fieldElement.editorPromise) {
      fieldElement.editorPromise.then(() => {
        fieldElement.editorWidget && EditorUtils.loadValue(fieldElement.editorWidget, item, source);
      });
    } else {
      EditorUtils.loadValue(editor, item, source);
    }
  }
  load(source) {
    const mode = this.get_mode();
    for (let fieldElement of this.fieldElements) {
      _PropertyGrid.loadFieldValue(source, fieldElement, mode);
    }
  }
  static saveFieldValue(target, fieldElement, canModify) {
    var item = fieldElement.propertyItem;
    if ((item.unbound ?? item.skipOnSave ?? item.oneWay) !== true && (canModify ?? _PropertyGrid.canModifyItem(item))) {
      var editor = fieldElement.editorWidget;
      if (!editor && fieldElement.editorPromise)
        throw `Editor for "${item.name}" is not loaded yet.`;
      EditorUtils.saveValue(editor, item, target);
    }
  }
  save(target) {
    if (target == null)
      target = /* @__PURE__ */ Object.create(null);
    for (let fieldElement of this.fieldElements) {
      _PropertyGrid.saveFieldValue(target, fieldElement, !!this.canModifyItem(fieldElement.propertyItem));
    }
    return target;
  }
  get value() {
    return this.save();
  }
  set value(val) {
    if (val == null)
      val = /* @__PURE__ */ Object.create(null);
    this.load(val);
  }
  static canModifyItem(item, mode) {
    if (mode === 1 /* insert */) {
      if (item.insertable === false) {
        return false;
      }
      if (item.insertPermission == null) {
        return true;
      }
      return Authorization.hasPermission(item.insertPermission);
    } else if (mode === 2 /* update */) {
      if (item.updatable === false) {
        return false;
      }
      if (item.updatePermission == null) {
        return true;
      }
      return Authorization.hasPermission(item.updatePermission);
    }
    return true;
  }
  canModifyItem(item) {
    return _PropertyGrid.canModifyItem(item, this.get_mode());
  }
  static updateFieldElement(fieldElement, mode, canModify) {
    var item = fieldElement.propertyItem;
    canModify ??= _PropertyGrid.canModifyItem(item, mode);
    var readOnly = item.readOnly === true || !canModify;
    var editor = fieldElement.editorWidget;
    const then = /* @__PURE__ */ __name((editor2) => {
      if (!editor2)
        return;
      EditorUtils.setReadOnly(editor2, readOnly);
      EditorUtils.setRequired(editor2, !readOnly && !!item.required && item.editorType !== "Boolean");
      if (item.visible === false || item.readPermission != null || item.insertPermission != null || item.updatePermission != null || item.hideOnInsert === true || item.hideOnUpdate === true) {
        var hidden = item.readPermission != null && !Authorization.hasPermission(item.readPermission) || item.visible === false || mode === 1 /* insert */ && item.hideOnInsert === true || mode === 2 /* update */ && item.hideOnUpdate === true;
        editor2.getGridField().toggle(!hidden);
      }
    }, "then");
    if (!editor && fieldElement.editorPromise) {
      fieldElement.editorPromise.then(() => {
        fieldElement.editorWidget && then(fieldElement.editorWidget);
      });
    } else {
      then(editor);
    }
  }
  updateFieldElement(fieldElement) {
    _PropertyGrid.updateFieldElement(fieldElement, this.get_mode(), !!this.canModifyItem(fieldElement.propertyItem));
  }
  updateInterface() {
    for (let fieldElement of this.fieldElements) {
      this.updateFieldElement(fieldElement);
    }
  }
};
function determineText(localTextPrefix, text2, getKey) {
  let local2;
  if (text2 != null && !text2.startsWith("`")) {
    local2 = tryGetText(text2);
    if (local2 != null) {
      return local2;
    }
  }
  if (text2 != null && text2.startsWith("`")) {
    text2 = text2.substring(1);
  }
  if (localTextPrefix) {
    local2 = tryGetText(getKey(localTextPrefix));
    if (local2 != null) {
      return local2;
    }
  }
  return text2;
}
__name(determineText, "determineText");
function extractTabKey(title) {
  if (!title)
    return null;
  let idx = title.lastIndexOf(".Tabs.");
  if (idx >= 0) {
    return title.substring(idx + 6);
  }
  return title;
}
__name(extractTabKey, "extractTabKey");
function setMaxLength(widget, maxLength) {
  if (Fluent.isInputLike(widget.domNode)) {
    if (maxLength > 0) {
      widget.domNode.setAttribute("maxlength", (maxLength ?? 0).toString());
    } else {
      widget.domNode.removeAttribute("maxlength");
    }
  }
}
__name(setMaxLength, "setMaxLength");
function createLineBreak(klass) {
  return jsx("div", { class: klass, style: { width: "100%" } });
}
__name(createLineBreak, "createLineBreak");
var PropertyGridMode = /* @__PURE__ */ ((PropertyGridMode2) => {
  PropertyGridMode2[PropertyGridMode2["insert"] = 1] = "insert";
  PropertyGridMode2[PropertyGridMode2["update"] = 2] = "update";
  return PropertyGridMode2;
})(PropertyGridMode || {});

// src/ui/dialogs/entitylocalizer.tsx
var EntityLocalizer = class {
  static {
    __name(this, "EntityLocalizer");
  }
  constructor(opt) {
    this.options = opt;
    const { pgOptions, idPrefix } = opt;
    const pgDiv = this.options.getPropertyGrid();
    if (!pgDiv?.getNode() || !pgOptions.items.some((x) => x.localizable === true))
      return;
    const localGridDiv = jsx("div", { id: idPrefix + "LocalizationGrid", hidden: true });
    pgDiv.after(localGridDiv);
    const langs = this.options.getLanguages() || [];
    this.targetLanguage = jsx("select", { class: "target-language ms-2", hidden: true, children: [
      jsx("option", { value: "", children: [
        "--",
        localText("Site.Translation.TargetLanguage"),
        "--"
      ] }),
      langs.map((x) => jsx("option", { value: x.id, children: x.text }))
    ] });
    this.options.getButton()?.after(this.targetLanguage);
    const targetLang = localStorage.getItem("EntityLocalizer.TargetLanguage");
    if (langs.some((x) => x.id == targetLang)) {
      this.targetLanguage.value = targetLang;
    }
    pgOptions.idPrefix = idPrefix + "Localization_";
    var items = [];
    for (var item of pgOptions.items) {
      if (item.localizable === true) {
        items.push(Object.assign({}, item, {
          skipOnSave: true,
          readOnly: true,
          required: false,
          defaultValue: null
        }));
        for (var lang of langs) {
          items.push(Object.assign({}, item, {
            name: lang.id + "$" + item.name,
            title: lang.text,
            cssClass: Fluent.toClassName([item.cssClass, "translation", "language-" + lang.id]),
            insertable: true,
            updatable: true,
            skipOnSave: false,
            required: false,
            localizable: false,
            defaultValue: null
          }));
        }
      }
    }
    pgOptions.items = items;
    this.grid = new PropertyGrid({ element: localGridDiv, ...pgOptions }).init();
    localGridDiv.classList.add("s-LocalizationGrid");
    if (TranslationConfig.translateTexts) {
      this.grid.element.findAll("input[type=text].editor").forEach((input) => {
        if (!input.name || input.name.indexOf("$") < 0)
          return;
        const div = jsx("div", { class: "input-group w-100" });
        Fluent(div).insertBefore(input);
        input.classList.add("form-control");
        div.append(input);
        div.append(
          jsx("button", { class: "btn btn-primary btn-sm", title: localText("Site.Translation.TranslateText"), onClick: () => {
            TranslationConfig.translateTexts({
              Inputs: [{
                SourceText: this.grid.element.findFirst("[name=" + cssEscape(input.name.split("$")[1]) + "]").val() || input.placeholder,
                TargetLanguageID: input.name.split("$")[0]
              }]
            }).then((result) => {
              if (result.Translations && result.Translations.length > 0) {
                input.value = result.Translations[0].TranslatedText;
              }
            });
          }, children: jsx("span", { class: faIcon("language") }) })
        );
      });
    }
    const targetLanguageUpdate = /* @__PURE__ */ __name(() => {
      const val = this.targetLanguage?.value;
      this.grid.element.findAll(".translation").forEach((x) => Fluent.toggle(x.closest(".field"), !val || x.classList.contains("language-" + val)));
    }, "targetLanguageUpdate");
    targetLanguageUpdate();
    Fluent.on(this.targetLanguage, "change", () => {
      targetLanguageUpdate();
      localStorage.setItem("EntityLocalizer.TargetLanguage", this.targetLanguage?.value);
    });
  }
  destroy() {
    if (this.grid) {
      this.grid.destroy();
      this.grid = null;
    }
  }
  clearValue() {
    this.pendingValue = null;
    this.lastValue = null;
  }
  isEnabled() {
    return !!this.grid;
  }
  isLocalizationMode() {
    return !!(this.isEnabled() && this.options.getButton()?.hasClass("pressed"));
  }
  isLocalizationModeAndChanged() {
    if (!this.isLocalizationMode()) {
      return false;
    }
    var newValue = this.getLocalizationGridValue();
    return JSON.stringify(this.lastValue) != JSON.stringify(newValue);
  }
  buttonClick() {
    if (this.isLocalizationMode() && !this.options.validateForm()) {
      return;
    }
    if (this.isLocalizationModeAndChanged()) {
      var newValue = this.getLocalizationGridValue();
      this.lastValue = newValue;
      this.pendingValue = newValue;
    }
    this.options.getButton()?.toggleClass("pressed");
    this.updateInterface();
    if (this.isLocalizationMode()) {
      this.loadLocalization();
    }
  }
  loadLocalization() {
    if (this.lastValue == null && this.options.isNew()) {
      this.grid.load({});
      this.setLocalizationGridCurrentValues();
      this.lastValue = this.getLocalizationGridValue();
      return;
    }
    if (this.lastValue != null) {
      this.grid.load(this.lastValue);
      this.setLocalizationGridCurrentValues();
      return;
    }
    this.options.retrieveLocalizations().then((localizations) => {
      const copy = Object.assign(/* @__PURE__ */ Object.create(null), this.options.getEntity());
      Object.entries(localizations ?? {}).forEach(([language, entity]) => Object.entries(entity ?? {}).forEach(([field, value]) => copy[language + "$" + field] = value));
      this.grid.load(copy);
      this.setLocalizationGridCurrentValues();
      this.pendingValue = null;
      this.lastValue = this.getLocalizationGridValue();
    });
  }
  setLocalizationGridCurrentValues() {
    const valueByName = {};
    this.grid.enumerateItems((item, widget) => {
      if (item.name.indexOf("$") < 0 && Fluent.isInputLike(widget.domNode)) {
        valueByName[item.name] = this.options.byId(item.name).val();
        widget.element.val(valueByName[item.name]);
      }
    });
    this.grid.enumerateItems((item1, widget1) => {
      var idx = item1.name.indexOf("$");
      if (idx >= 0 && Fluent.isInputLike(widget1.domNode)) {
        var hint = valueByName[item1.name.substring(idx + 1)];
        if (hint != null && hint.length > 0) {
          widget1.element.attr("title", hint).attr("placeholder", hint);
        }
      }
    });
  }
  getLocalizationGridValue() {
    var value = {};
    this.grid.save(value);
    for (var k of Object.keys(value)) {
      if (k.indexOf("$") < 0) {
        delete value[k];
      }
    }
    return value;
  }
  editSaveRequest(req) {
    if (this.pendingValue != null) {
      req.Localizations = this.getPendingLocalizations();
    }
  }
  getPendingLocalizations() {
    if (this.pendingValue == null) {
      return null;
    }
    var result = {};
    var langs = this.options.getLanguages();
    for (var lang of langs) {
      var entity = {};
      var prefix = lang.id + "$";
      for (var k of Object.keys(this.pendingValue)) {
        if (k.startsWith(prefix))
          entity[k.substring(prefix.length)] = this.pendingValue[k];
      }
      result[lang.id] = entity;
    }
    return result;
  }
  updateInterface() {
    if (!this.isEnabled())
      return;
    const button = this.options.getButton()?.getNode();
    const locMode = this.isLocalizationMode();
    Fluent.toggle(this.targetLanguage, locMode);
    const inner = button?.querySelector(".button-inner");
    inner && (inner.textContent = locMode ? localText(EntityDialogTexts.LocalizationBack) : localText(EntityDialogTexts.LocalizationButton));
    this.options.getPropertyGrid()?.toggle(!locMode);
    Fluent.toggle(this.grid?.domNode, locMode);
    Fluent.toggle(this.targetLanguage, locMode);
    const buttons = this.options.getToolButtons().filter((x) => x !== button);
    if (locMode) {
      buttons?.filter((x) => !x.classList.contains("localization-hidden")).forEach((el) => {
        el.classList.add("localization-hidden");
        el.hidden = true;
      });
    } else {
      buttons?.filter((x) => x.classList.contains("localization-hidden")).forEach((el) => {
        el.classList.remove("localization-hidden");
        el.hidden = false;
      });
    }
  }
};

// src/ui/dialogs/entitytoolbuttons.tsx
function saveAndCloseToolButton(opt) {
  return {
    title: EntityDialogTexts.SaveButton,
    action: "save-and-close",
    cssClass: "save-and-close-button",
    icon: faIcon("check-circle", "purple"),
    hotkey: "alt+s",
    ...opt
  };
}
__name(saveAndCloseToolButton, "saveAndCloseToolButton");
function applyChangesToolButton(opt) {
  return {
    title: "",
    hint: EntityDialogTexts.ApplyChangesButton,
    action: "apply-changes",
    cssClass: "apply-changes-button",
    icon: faIcon("clipboard-check", "purple"),
    hotkey: "alt+a",
    ...opt
  };
}
__name(applyChangesToolButton, "applyChangesToolButton");
function deleteToolButton(opt) {
  return {
    title: EntityDialogTexts.DeleteButton,
    action: "delete",
    cssClass: "delete-button",
    icon: faIcon("trash-o", "danger"),
    hotkey: "alt+x",
    ...opt
  };
}
__name(deleteToolButton, "deleteToolButton");
function undeleteToolButton(opt) {
  return {
    title: EntityDialogTexts.UndeleteButton,
    action: "undo-delete",
    cssClass: "undo-delete-button",
    ...opt
  };
}
__name(undeleteToolButton, "undeleteToolButton");
function editToolButton(opt) {
  return {
    title: EntityDialogTexts.EditButton,
    action: "edit",
    cssClass: "edit-button",
    icon: faIcon("edit"),
    ...opt
  };
}
__name(editToolButton, "editToolButton");
function localizationToolButton(opt) {
  return {
    title: EntityDialogTexts.LocalizationButton,
    action: "localization",
    cssClass: "localization-button",
    ...opt
  };
}
__name(localizationToolButton, "localizationToolButton");
function cloneToolButton(opt) {
  return {
    title: EntityDialogTexts.CloneButton,
    action: "clone",
    cssClass: "clone-button",
    icon: faIcon("clone"),
    ...opt
  };
}
__name(cloneToolButton, "cloneToolButton");

// src/ui/dialogs/entitydialog.tsx
var _a53, _b36;
var EntityDialog = class _EntityDialog extends (_b36 = BaseDialog, _a53 = Symbol.typeInfo, _b36) {
  static {
    __name(this, "EntityDialog");
  }
  static {
    this[_a53] = this.registerClass(nsSerenity, [IEditDialog, IReadOnly, Attributes.panel]);
  }
  constructor(props) {
    super(props);
    this.syncOrAsyncThen(this.getPropertyItemsData, this.getPropertyItemsDataAsync, (itemsData) => {
      this.propertyItemsReady(itemsData);
      this.afterInit();
    });
  }
  propertyItemsReady(itemsData) {
    this.propertyItemsData = itemsData;
    this.initPropertyGrid();
    this.initLocalizer();
  }
  afterInit() {
  }
  useAsync() {
    return false;
  }
  destroy() {
    this.propertyGrid?.destroy();
    delete this.propertyGrid;
    this.localizer?.destroy();
    delete this.localizer;
    delete this.toolbar;
    Object.keys(this).filter((k) => Object.prototype.hasOwnProperty.call(this, k) && k.endsWith("Button")).forEach((k) => delete this[k]);
    super.destroy();
  }
  get entity() {
    return this._entity;
  }
  set entity(value) {
    this._entity = value || new Object();
  }
  /** @deprecated use entityId */
  get_entityId() {
    return this.entityId;
  }
  get entityId() {
    return this._entityId;
  }
  set entityId(value) {
    this._entityId = value;
  }
  getEntityNameFieldValue() {
    return (this.entity[this.getNameProperty()] ?? "").toString();
  }
  getEntityTitle() {
    if (!this.isEditMode())
      return stringFormat(EntityDialogTexts.NewRecordTitle, this.getEntitySingular());
    const titleFormat = this.isViewMode() || this.readOnly || !this.hasSavePermission() ? EntityDialogTexts.ViewRecordTitle : EntityDialogTexts.EditRecordTitle;
    const title = this.getEntityNameFieldValue() ?? "";
    return stringFormat(titleFormat, this.getEntitySingular(), !title ? "" : " (" + title + ")");
  }
  updateTitle() {
    this.dialogTitle = this.getEntityTitle();
  }
  isCloneMode() {
    return false;
  }
  isEditMode() {
    return this.entityId != null && !this.isCloneMode();
  }
  isDeleted() {
    if (this.entityId == null)
      return false;
    const isDeletedProperty = this.getIsDeletedProperty();
    if (isDeletedProperty)
      return !!this.entity[isDeletedProperty];
    const value = this.entity[this.getIsActiveProperty()];
    if (value == null)
      return false;
    return value < 0;
  }
  isNew() {
    return this.entityId == null;
  }
  isNewOrDeleted() {
    return this.isNew() || this.isDeleted();
  }
  getDeleteOptions(callback) {
    return {};
  }
  deleteHandler(options, callback) {
    serviceCall(options);
  }
  getDeleteServiceMethod() {
    return this.getService() + "/Delete";
  }
  doDelete(callback) {
    const request = {
      EntityId: this.entityId
    };
    const options = Object.assign({
      service: this.getDeleteServiceMethod(),
      request,
      onSuccess: /* @__PURE__ */ __name((response) => {
        this.onDeleteSuccess(response);
        callback?.(response);
        Fluent.trigger(this.domNode, "ondatachange", {
          entityId: request.EntityId,
          entity: this.entity,
          operationType: "delete"
        });
      }, "onSuccess")
    }, this.getDeleteOptions(callback));
    this.deleteHandler(options, callback);
  }
  onDeleteSuccess(response) {
  }
  getRowDefinition() {
    return null;
  }
  getEntityType() {
    if (this._entityType != null)
      return this._entityType;
    let name = getTypeFullName(getInstanceType(this));
    const px = name.indexOf(".");
    if (px >= 0)
      name = name.substring(px + 1);
    if (name.endsWith("Dialog") || name.endsWith("Control"))
      name = name.substring(0, name.length - 6);
    else if (name.endsWith("Panel"))
      name = name.substring(0, name.length - 5);
    return this._entityType = name;
  }
  getFormKey() {
    if (this._formKey != null)
      return this._formKey;
    return this._formKey = this.getEntityType();
  }
  getLocalTextDbPrefix() {
    if (this._localTextDbPrefix != null)
      return this._localTextDbPrefix;
    this._localTextDbPrefix = this.getLocalTextPrefix() ?? "";
    if (this._localTextDbPrefix.length > 0 && !this._localTextDbPrefix.endsWith("."))
      this._localTextDbPrefix = "Db." + this._localTextDbPrefix + ".";
    return this._localTextDbPrefix;
  }
  getLocalTextPrefix() {
    const rowDefinition = this.getRowDefinition();
    if (rowDefinition)
      return rowDefinition.localTextPrefix;
    return this.getEntityType();
  }
  getEntitySingular() {
    if (this._entitySingular != null)
      return this._entitySingular;
    return this._entitySingular = localText(this.getLocalTextDbPrefix() + "EntitySingular", this.getEntityType());
  }
  getNameProperty() {
    if (this._nameProperty != null)
      return this._nameProperty;
    const rowDefinition = this.getRowDefinition();
    if (rowDefinition)
      return this._nameProperty = rowDefinition.nameProperty ?? "";
    return this._nameProperty = "Name";
  }
  getIdProperty() {
    if (this._idProperty != null)
      return this._idProperty;
    const rowDefinition = this.getRowDefinition();
    if (rowDefinition)
      return this._idProperty = rowDefinition.idProperty ?? "";
    return this._idProperty = "ID";
  }
  getIsActiveProperty() {
    if (this._isActiveProperty != null)
      return this._isActiveProperty;
    const rowDefinition = this.getRowDefinition();
    return this._isActiveProperty = rowDefinition ? rowDefinition.isActiveProperty ?? "" : "";
  }
  getIsDeletedProperty() {
    return this.getRowDefinition()?.isDeletedProperty;
  }
  getService() {
    if (this._service != null)
      return this._service;
    return this._service = replaceAll(this.getEntityType(), ".", "/");
  }
  load(entityOrId, done, fail) {
    const action = /* @__PURE__ */ __name(() => {
      if (entityOrId == null) {
        this.loadResponse({});
        done?.();
        return;
      }
      const scriptType = typeof entityOrId;
      if (scriptType === "string" || scriptType === "number") {
        this.loadById(entityOrId, () => {
          done && window.setTimeout(done, 0);
        }, null);
        return;
      }
      this.loadResponse({ Entity: entityOrId || new Object() });
      done && done();
    }, "action");
    if (fail == null) {
      action();
      return;
    }
    try {
      action();
    } catch (ex1) {
      fail(ex1);
    }
  }
  loadNewAndOpenDialog(asPanel) {
    this.loadResponse({});
    this.dialogOpen(asPanel);
  }
  loadEntityAndOpenDialog(entity, asPanel) {
    this.loadResponse({ Entity: entity });
    this.dialogOpen(asPanel);
  }
  loadResponse(data) {
    this.init();
    data = data || {};
    this.onLoadingData(data);
    const entity = data.Entity || new Object();
    this.beforeLoadEntity(entity);
    this.loadEntity(entity);
    this.entity = entity;
    this.afterLoadEntity();
  }
  loadEntity(entity) {
    const idField = this.getIdProperty();
    if (idField != null)
      this.entityId = entity[idField];
    this.entity = entity;
    this.propertyGrid?.set_mode(this.isEditMode() ? 2 /* update */ : 1 /* insert */);
    this.propertyGrid?.load(entity);
  }
  beforeLoadEntity(entity) {
    this.localizer?.clearValue();
  }
  afterLoadEntity() {
    this.updateInterface();
    this.updateTitle();
  }
  loadByIdAndOpenDialog(entityId, asPanel, callback, fail) {
    this.loadById(
      entityId,
      (response) => window.setTimeout(() => {
        this.dialogOpen(asPanel);
        callback?.(response);
      }, 0),
      () => {
        if (!Fluent.isVisibleLike(this.domNode)) {
          this.domNode.remove();
        }
        fail?.();
      }
    );
  }
  onLoadingData(data) {
  }
  getLoadByIdOptions(id, callback) {
    return {
      blockUI: true,
      service: this.getRetrieveServiceMethod(),
      request: this.getLoadByIdRequest(id),
      onSuccess: /* @__PURE__ */ __name((response) => {
        this.loadResponse(response);
        callback?.(response);
      }, "onSuccess"),
      onCleanup: /* @__PURE__ */ __name(() => this.validator != null && validatorAbortHandler(this.validator), "onCleanup")
    };
  }
  getLoadByIdRequest(id) {
    return {
      EntityId: id
    };
  }
  reloadById() {
    this.loadById(this.entityId);
  }
  getRetrieveServiceMethod() {
    return this.getService() + "/Retrieve";
  }
  loadById(id, callback, fail) {
    this.loadByIdHandler(this.getLoadByIdOptions(id, callback), callback, fail);
  }
  loadByIdHandler(options, callback, fail) {
    const request = serviceCall(options);
    fail && (request?.fail ? request.fail(fail) : request.then(null, fail));
  }
  async retrieveLocalizations() {
    const opt = {
      ...this.getLoadByIdOptions(this.entityId, null),
      onSuccess: void 0,
      onCleanup: void 0
    };
    opt.request = {
      ...opt.request,
      ColumnSelection: 1 /* keyOnly */,
      IncludeColumns: ["Localizations"]
    };
    return (await serviceCall(opt)).Localizations;
  }
  getLocalizerOptions() {
    return {
      byId: /* @__PURE__ */ __name((id) => this.byId(id), "byId"),
      idPrefix: this.idPrefix,
      getButton: /* @__PURE__ */ __name(() => this.localizerButton, "getButton"),
      getEntity: /* @__PURE__ */ __name(() => this.entity, "getEntity"),
      getLanguages: /* @__PURE__ */ __name(() => this.getLanguages(), "getLanguages"),
      getPropertyGrid: /* @__PURE__ */ __name(() => this.byId("PropertyGrid"), "getPropertyGrid"),
      getToolButtons: /* @__PURE__ */ __name(() => this.toolbar.element.findAll(".tool-button"), "getToolButtons"),
      isNew: /* @__PURE__ */ __name(() => this.isNew(), "isNew"),
      pgOptions: this.getPropertyGridOptions(),
      retrieveLocalizations: /* @__PURE__ */ __name(() => this.retrieveLocalizations(), "retrieveLocalizations"),
      validateForm: /* @__PURE__ */ __name(() => this.validateForm(), "validateForm")
    };
  }
  initLocalizer() {
    if (this.findById("PropertyGrid"))
      this.localizer = new EntityLocalizer(this.getLocalizerOptions());
  }
  getLanguages() {
    return TranslationConfig.getLanguageList?.() || [];
  }
  initPropertyGrid() {
    const pgDiv = this.findById("PropertyGrid");
    if (!pgDiv)
      return;
    const pgOptions = this.getPropertyGridOptions();
    this.propertyGrid = new PropertyGrid({ element: pgDiv, ...pgOptions }).init();
  }
  getPropertyItems() {
    return this.propertyItemsData?.items || [];
  }
  getPropertyItemsData() {
    const formKey = this.getFormKey();
    if (this.getFormKey === _EntityDialog.prototype.getFormKey && this.getPropertyItems !== _EntityDialog.prototype.getPropertyItems && !ScriptData.canLoad("Form." + formKey)) {
      return {
        items: this.getPropertyItems(),
        additionalItems: []
      };
    }
    return formKey ? getFormData(formKey) : { items: [], additionalItems: [] };
  }
  async getPropertyItemsDataAsync() {
    const formKey = this.getFormKey();
    return formKey ? await getFormDataAsync(formKey) : { items: [], additionalItems: [] };
  }
  getPropertyGridOptions() {
    return {
      idPrefix: this.idPrefix,
      items: this.getPropertyItems(),
      mode: 1 /* insert */,
      localTextPrefix: "Forms." + this.getFormKey() + "."
    };
  }
  validateBeforeSave() {
    return true;
  }
  getCreateServiceMethod() {
    return this.getService() + "/Create";
  }
  getUpdateServiceMethod() {
    return this.getService() + "/Update";
  }
  getSaveOptions(callback, initiator) {
    const opt = {};
    opt.service = this.isEditMode() ? this.getUpdateServiceMethod() : this.getCreateServiceMethod();
    opt.onSuccess = (response) => {
      this.onSaveSuccess(response, initiator);
      callback?.(response);
      Fluent.trigger(this.domNode, "ondatachange", {
        operationType: this.isEditMode() ? "update" : "create",
        entity: opt.request == null ? null : opt.request.Entity,
        entityId: this.isEditMode() ? this.entityId : response == null ? null : response.EntityId
      });
    };
    opt.onCleanup = () => this.validator && validatorAbortHandler(this.validator);
    opt.request = this.getSaveRequest();
    return opt;
  }
  getSaveEntity() {
    const entity = new Object();
    this.propertyGrid?.save(entity);
    if (this.isEditMode()) {
      const idField = this.getIdProperty();
      if (idField != null && entity[idField] == null)
        entity[idField] = this.entityId;
    }
    return entity;
  }
  getSaveRequest() {
    const entity = this.getSaveEntity();
    const req = { Entity: entity };
    if (this.isEditMode() && this.getIdProperty() != null)
      req.EntityId = this.entityId;
    this.localizer?.editSaveRequest(req);
    return req;
  }
  onSaveSuccess(response, initiator) {
    initiator !== "save-and-close" && this.showSaveSuccessMessage(response, initiator);
  }
  save_submitHandler(callback, initiator) {
    const options = this.getSaveOptions(callback, initiator);
    this.saveHandler(options, callback, initiator);
  }
  save(callback, initiator) {
    return ValidationHelper.submit(
      this.byId("Form"),
      () => this.validateBeforeSave(),
      () => this.save_submitHandler(callback, initiator)
    );
  }
  saveHandler(options, callback, initiator) {
    serviceCall(options);
  }
  showSaveSuccessMessage(response, initiator) {
    notifySuccess(EntityDialogTexts.SaveSuccessMessage, "", null);
  }
  getToolbarButtons() {
    return [
      saveAndCloseToolButton({
        onClick: /* @__PURE__ */ __name(() => this.save(() => this.dialogClose("save-and-close"), "save-and-close"), "onClick"),
        visible: /* @__PURE__ */ __name(() => !this.isDeleted() && !this.isViewMode(), "visible"),
        disabled: /* @__PURE__ */ __name(() => !this.hasSavePermission() || this.readOnly, "disabled"),
        ref: /* @__PURE__ */ __name((el) => this.saveAndCloseButton = Fluent(el), "ref")
      }),
      applyChangesToolButton({
        onClick: /* @__PURE__ */ __name(() => this.save((response) => this.loadById(this.isEditMode() ? response?.EntityId ?? this.entityId : response?.EntityId), "apply-changes"), "onClick"),
        visible: /* @__PURE__ */ __name(() => !this.isDeleted() && !this.isViewMode(), "visible"),
        disabled: /* @__PURE__ */ __name(() => !this.hasSavePermission() || this.readOnly, "disabled"),
        ref: /* @__PURE__ */ __name((el) => this.applyChangesButton = Fluent(el), "ref")
      }),
      deleteToolButton({
        onClick: /* @__PURE__ */ __name(() => confirmDialog(
          EntityDialogTexts.DeleteConfirmation,
          () => this.doDelete(() => this.dialogClose("delete"))
        ), "onClick"),
        visible: /* @__PURE__ */ __name(() => this.isEditMode() && !this.isDeleted() && !this.isViewMode(), "visible"),
        disabled: /* @__PURE__ */ __name(() => !this.hasDeletePermission() || this.readOnly, "disabled"),
        ref: /* @__PURE__ */ __name((el) => this.deleteButton = Fluent(el), "ref")
      }),
      undeleteToolButton({
        onClick: /* @__PURE__ */ __name(() => this.isDeleted() && confirmDialog(EntityDialogTexts.UndeleteConfirmation, () => this.undelete(() => this.loadById(this.entityId))), "onClick"),
        visible: /* @__PURE__ */ __name(() => this.isEditMode() && this.isDeleted() && !this.isViewMode(), "visible"),
        disabled: /* @__PURE__ */ __name(() => !this.hasDeletePermission() || this.readOnly, "disabled"),
        ref: /* @__PURE__ */ __name((el) => this.undeleteButton = Fluent(el), "ref")
      }),
      editToolButton({
        onClick: /* @__PURE__ */ __name(() => {
          if (!this.isEditMode())
            return;
          this.editClicked = true;
          this.updateInterface();
          this.updateTitle();
        }, "onClick"),
        visible: /* @__PURE__ */ __name(() => this.isViewMode(), "visible"),
        disabled: /* @__PURE__ */ __name(() => !this.hasSavePermission() || this.readOnly, "disabled"),
        ref: /* @__PURE__ */ __name((el) => this.editButton = Fluent(el), "ref")
      }),
      localizationToolButton({
        onClick: /* @__PURE__ */ __name(() => this.localizer?.buttonClick(), "onClick"),
        visible: /* @__PURE__ */ __name(() => this.localizer?.isEnabled(), "visible"),
        ref: /* @__PURE__ */ __name((el) => this.localizerButton = Fluent(el), "ref")
      }),
      cloneToolButton({
        onClick: /* @__PURE__ */ __name(() => {
          if (!this.isEditMode())
            return;
          const cloneEntity = this.getCloningEntity();
          const cloneDialog = Widget.create({ type: getInstanceType(this) });
          SubDialogHelper.bubbleDataChange(SubDialogHelper.cascade(cloneDialog, this.domNode), this, true);
          cloneDialog.loadEntityAndOpenDialog(cloneEntity, null);
        }, "onClick"),
        visible: /* @__PURE__ */ __name(() => false, "visible"),
        disabled: /* @__PURE__ */ __name(() => !this.hasInsertPermission() || this.readOnly, "disabled"),
        ref: /* @__PURE__ */ __name((el) => this.cloneButton = Fluent(el), "ref")
      })
    ];
  }
  getCloningEntity() {
    const clone = Object.assign(/* @__PURE__ */ Object.create(null), this.entity);
    const idField = this.getIdProperty();
    if (idField)
      delete clone[idField];
    const isActiveField = this.getIsActiveProperty();
    if (isActiveField)
      delete clone[isActiveField];
    const isDeletedField = this.getIsDeletedProperty();
    if (isDeletedField)
      delete clone[isDeletedField];
    return clone;
  }
  updateInterface() {
    EditorUtils.setContainerReadOnly(this.byId("Form"), false);
    this.toolbar.updateInterface();
    TabsExtensions.setDisabled(this.tabs, "Log", this.isNewOrDeleted());
    this.localizer?.updateInterface();
    if (!this.hasSavePermission() || this.isViewMode() || this.readOnly)
      EditorUtils.setContainerReadOnly(this.byId("Form"), true);
  }
  getUndeleteOptions(callback) {
    return {};
  }
  undeleteHandler(options, callback) {
    serviceCall(options);
  }
  getUndeleteServiceMethod() {
    return this.getService() + "/Undelete";
  }
  undelete(callback) {
    const request = { EntityId: this.entityId };
    const options = Object.assign({
      service: this.getUndeleteServiceMethod(),
      request,
      onSuccess: /* @__PURE__ */ __name((response) => {
        callback && callback(response);
        Fluent.trigger(this.domNode, "ondatachange", {
          entityId: this.entityId,
          entity: this.entity,
          operationType: "undelete"
        });
      }, "onSuccess")
    }, this.getUndeleteOptions(callback));
    this.undeleteHandler(options, callback);
  }
  get readOnly() {
    return this.get_readOnly();
  }
  set readOnly(value) {
    this.set_readOnly(value);
  }
  get_readOnly() {
    return !!this._readonly;
  }
  set_readOnly(value) {
    if (!!this._readonly != !!value) {
      this._readonly = !!value;
      this.updateInterface();
      this.updateTitle();
    }
  }
  getInsertPermission() {
    return this.getRowDefinition()?.insertPermission;
  }
  getUpdatePermission() {
    return this.getRowDefinition()?.updatePermission;
  }
  getDeletePermission() {
    return this.getRowDefinition()?.deletePermission;
  }
  hasDeletePermission() {
    const deletePermission = this.getDeletePermission();
    return deletePermission == null || Authorization.hasPermission(deletePermission);
  }
  hasInsertPermission() {
    const insertPermission = this.getInsertPermission();
    return insertPermission == null || Authorization.hasPermission(insertPermission);
  }
  hasUpdatePermission() {
    const updatePermission = this.getUpdatePermission();
    return updatePermission == null || Authorization.hasPermission(updatePermission);
  }
  hasSavePermission() {
    return this.isNew() ? this.hasInsertPermission() : this.hasUpdatePermission();
  }
  isViewMode() {
    return this.useViewMode() && this.isEditMode() && !this.editClicked;
  }
  useViewMode() {
    return false;
  }
  renderContents() {
    if (this.legacyTemplateRender())
      return void 0;
    const id = this.useIdPrefix();
    return jsx(Fragment, { children: [
      jsx("div", { id: id.Toolbar }),
      jsx("div", { class: "s-Form", children: jsx("form", { id: id.Form, action: "", children: jsx("div", { id: id.PropertyGrid }) }) })
    ] });
  }
  static get defaultLanguageList() {
    return TranslationConfig.getLanguageList?.().map((x) => [x.id, x.text]);
  }
  static set defaultLanguageList(value) {
    TranslationConfig.getLanguageList = () => value?.map((x) => ({ id: x[0], text: x[1] }));
  }
};

// src/ui/dialogs/propertydialog.tsx
var _a54, _b37;
var PropertyDialog = class _PropertyDialog extends (_b37 = BaseDialog, _a54 = Symbol.typeInfo, _b37) {
  static {
    __name(this, "PropertyDialog");
  }
  static {
    this[_a54] = this.registerClass(nsSerenity, [Attributes.panel(false)]);
  }
  isClosable() {
    return !this.isStatic();
  }
  isStatic() {
    return false;
  }
  constructor(props) {
    super(props);
    this.syncOrAsyncThen(this.getPropertyItemsData, this.getPropertyItemsDataAsync, (itemsData) => {
      this.propertyItemsReady(itemsData);
      this.afterInit();
    });
  }
  propertyItemsReady(itemsData) {
    this.propertyItemsData = itemsData;
    this.initPropertyGrid();
    this.loadInitialEntity();
  }
  afterInit() {
  }
  useAsync() {
    return false;
  }
  destroy() {
    if (this.propertyGrid) {
      this.propertyGrid.destroy();
      this.propertyGrid = null;
    }
    super.destroy();
  }
  getDialogOptions() {
    var opt = super.getDialogOptions();
    opt.width = 400;
    return opt;
  }
  getDialogButtons() {
    if (this.getCustomAttribute(StaticPanelAttribute)?.value === true)
      return [];
    return [
      okDialogButton({
        click: /* @__PURE__ */ __name((e2) => {
          e2.preventDefault();
          this.okClick();
        }, "click")
      }),
      cancelDialogButton()
    ];
  }
  okClick() {
    if (!this.validateBeforeSave()) {
      return;
    }
    this.okClickValidated();
  }
  okClickValidated() {
    this.dialogClose("ok");
  }
  cancelClick() {
    this.dialogClose("cancel");
  }
  initPropertyGrid() {
    var pgDiv = this.findById("PropertyGrid");
    if (!pgDiv) {
      return;
    }
    var pgOptions = this.getPropertyGridOptions();
    this.propertyGrid = new PropertyGrid({ element: pgDiv, ...pgOptions }).init();
  }
  getFormKey() {
    var name = getTypeFullName(getInstanceType(this));
    var px = name.indexOf(".");
    if (px >= 0) {
      name = name.substring(px + 1);
    }
    if (name.endsWith("Dialog")) {
      name = name.substring(0, name.length - 6);
    } else if (name.endsWith("Panel")) {
      name = name.substring(0, name.length - 5);
    }
    return name;
  }
  getPropertyGridOptions() {
    return {
      idPrefix: this.idPrefix,
      items: this.getPropertyItems(),
      mode: 1,
      localTextPrefix: "Forms." + this.getFormKey() + "."
    };
  }
  getPropertyItems() {
    return this.propertyItemsData?.items || [];
  }
  getPropertyItemsData() {
    var formKey = this.getFormKey();
    if (this.getFormKey === _PropertyDialog.prototype.getFormKey && this.getPropertyItems !== _PropertyDialog.prototype.getPropertyItems && !ScriptData.canLoad("Form." + formKey)) {
      return {
        items: this.getPropertyItems(),
        additionalItems: []
      };
    }
    if (formKey) {
      return getFormData(formKey);
    }
    return { items: [], additionalItems: [] };
  }
  async getPropertyItemsDataAsync() {
    var formKey = this.getFormKey();
    if (formKey) {
      return await getFormDataAsync(formKey);
    }
    return { items: [], additionalItems: [] };
  }
  getSaveEntity() {
    var entity = new Object();
    if (this.propertyGrid) {
      this.propertyGrid.save(entity);
    }
    return entity;
  }
  loadInitialEntity() {
    this.propertyGrid && this.propertyGrid.load(new Object());
  }
  get entity() {
    return this._entity;
  }
  set entity(value) {
    this._entity = value ?? new Object();
  }
  get entityId() {
    return this._entityId;
  }
  set entityId(value) {
    this._entityId = value;
  }
  validateBeforeSave() {
    return this.validator.form();
  }
  updateTitle() {
  }
  renderContents() {
    if (this.legacyTemplateRender())
      return void 0;
    const id = this.useIdPrefix();
    return jsx("div", { class: "s-Form", children: jsx("form", { id: id.Form, action: "", children: jsx("div", { id: id.PropertyGrid }) }) });
  }
};

// src/ui/editors/autonumeric.ts
function getElementSelection(that) {
  var position = {};
  if (that.selectionStart === void 0) {
    that.focus();
    var select = document.selection.createRange();
    position.length = select.text.length;
    select.moveStart("character", -that.value.length);
    position.end = select.text.length;
    position.start = position.end - position.length;
  } else {
    position.start = that.selectionStart;
    position.end = that.selectionEnd;
    position.length = position.end - position.start;
  }
  return position;
}
__name(getElementSelection, "getElementSelection");
function setElementSelection(that, start, end) {
  if (that.selectionStart === void 0) {
    that.focus();
    var r2 = that.createTextRange();
    r2.collapse(true);
    r2.moveEnd("character", end);
    r2.moveStart("character", start);
    r2.select();
  } else {
    that.selectionStart = start;
    that.selectionEnd = end;
  }
}
__name(setElementSelection, "setElementSelection");
function runCallbacks(input, settings) {
  Object.keys(settings).forEach(function(k) {
    var val = settings[k];
    if (typeof val === "function") {
      settings[k] = val(input, settings, k);
    } else if (typeof AutoNumeric.getSettings(input)?.[val] === "function") {
      settings[k] = AutoNumeric.getSettings(input)?.[val](input, settings, k);
    }
  });
}
__name(runCallbacks, "runCallbacks");
function convertKeyToNumber(settings, key) {
  if (typeof settings[key] === "string") {
    settings[key] *= 1;
  }
}
__name(convertKeyToNumber, "convertKeyToNumber");
function autoCode(input, settings) {
  runCallbacks(input, settings);
  settings.oEvent = null;
  settings.tagList = ["B", "CAPTION", "CITE", "CODE", "DD", "DEL", "DIV", "DFN", "DT", "EM", "H1", "H2", "H3", "H4", "H5", "H6", "INS", "KDB", "LABEL", "LI", "OUTPUT", "P", "Q", "S", "SAMPLE", "SPAN", "STRONG", "TD", "TH", "U", "VAR"];
  var vmax = settings.vMax.toString().split("."), vmin = !settings.vMin && settings.vMin !== 0 ? [] : settings.vMin.toString().split(".");
  convertKeyToNumber(settings, "vMax");
  convertKeyToNumber(settings, "vMin");
  convertKeyToNumber(settings, "mDec");
  settings.mDec = settings.mRound === "CHF" ? 2 : settings.mDec;
  settings.allowLeading = true;
  settings.aNeg = settings.vMin < 0 ? "-" : "";
  vmax[0] = vmax[0].replace("-", "");
  vmin[0] = vmin[0].replace("-", "");
  settings.mInt = Math.max(vmax[0].length, vmin[0].length, 1);
  if (settings.mDec === null) {
    var vmaxLength = 0, vminLength = 0;
    if (vmax[1]) {
      vmaxLength = vmax[1].length;
    }
    if (vmin[1]) {
      vminLength = vmin[1].length;
    }
    settings.mDec = Math.max(vmaxLength, vminLength);
  }
  if (settings.altDec === null && settings.mDec > 0) {
    if (settings.aDec === "." && settings.aSep !== ",") {
      settings.altDec = ",";
    } else if (settings.aDec === "," && settings.aSep !== ".") {
      settings.altDec = ".";
    }
  }
  var aNegReg = settings.aNeg ? "([-\\" + settings.aNeg + "]?)" : "(-?)";
  settings.aNegRegAutoStrip = aNegReg;
  settings.skipFirstAutoStrip = new RegExp(aNegReg + "[^-" + (settings.aNeg ? "\\" + settings.aNeg : "") + "\\" + settings.aDec + "\\d].*?(\\d|\\" + settings.aDec + "\\d)");
  settings.skipLastAutoStrip = new RegExp("(\\d\\" + settings.aDec + "?)[^\\" + settings.aDec + "\\d]\\D*$");
  var allowed = "-" + settings.aNum + "\\" + settings.aDec;
  settings.allowedAutoStrip = new RegExp("[^" + allowed + "]", "gi");
  settings.numRegAutoStrip = new RegExp(aNegReg + "(?:\\" + settings.aDec + "?(\\d+\\" + settings.aDec + "\\d+)|(\\d*(?:\\" + settings.aDec + "\\d*)?))");
  return settings;
}
__name(autoCode, "autoCode");
function autoStrip(s2, settings, strip_zero) {
  if (settings.aSign) {
    while (s2.indexOf(settings.aSign) > -1) {
      s2 = s2.replace(settings.aSign, "");
    }
  }
  s2 = s2.replace(settings.skipFirstAutoStrip, "$1$2");
  s2 = s2.replace(settings.skipLastAutoStrip, "$1");
  s2 = s2.replace(settings.allowedAutoStrip, "");
  if (settings.altDec) {
    s2 = s2.replace(settings.altDec, settings.aDec);
  }
  var m = s2.match(settings.numRegAutoStrip);
  s2 = m ? [m[1], m[2], m[3]].join("") : "";
  if ((settings.lZero === "allow" || settings.lZero === "keep") && strip_zero !== "strip") {
    var parts = [], nSign = "";
    parts = s2.split(settings.aDec);
    if (parts[0].indexOf("-") !== -1) {
      nSign = "-";
      parts[0] = parts[0].replace("-", "");
    }
    if (parts[0].length > settings.mInt && parts[0].charAt(0) === "0") {
      parts[0] = parts[0].slice(1);
    }
    s2 = nSign + parts.join(settings.aDec);
  }
  if (strip_zero && settings.lZero === "deny" || strip_zero && settings.lZero === "allow" && settings.allowLeading === false) {
    var strip_reg = new RegExp("^" + settings.aNegRegAutoStrip + "0*(\\d" + (strip_zero === "leading" ? ")" : "|$)"));
    s2 = s2.replace(strip_reg, "$1$2");
  }
  return s2;
}
__name(autoStrip, "autoStrip");
function negativeBracket(s2, nBracket, oEvent) {
  var pBracket = nBracket.split(",");
  if (oEvent === "set" || oEvent === "focusout") {
    s2 = s2.replace("-", "");
    s2 = pBracket[0] + s2 + pBracket[1];
  } else if ((oEvent === "get" || oEvent === "focusin" || oEvent === "pageLoad") && s2.charAt(0) === nBracket[0]) {
    s2 = s2.replace(pBracket[0], "-");
    s2 = s2.replace(pBracket[1], "");
  }
  return s2;
}
__name(negativeBracket, "negativeBracket");
function truncateDecimal(s2, aDec, mDec) {
  if (aDec && mDec) {
    var parts = s2.split(aDec);
    if (parts[1] && parts[1].length > mDec) {
      if (mDec > 0) {
        parts[1] = parts[1].substring(0, mDec);
        s2 = parts.join(aDec);
      } else {
        s2 = parts[0];
      }
    }
  }
  return s2;
}
__name(truncateDecimal, "truncateDecimal");
function fixNumber(s2, aDec, aNeg) {
  if (aDec && aDec !== ".") {
    s2 = s2.replace(aDec, ".");
  }
  if (aNeg && aNeg !== "-") {
    s2 = s2.replace(aNeg, "-");
  }
  if (!s2.match(/\d/)) {
    s2 += "0";
  }
  return s2;
}
__name(fixNumber, "fixNumber");
function checkValue(value, settings) {
  if (value) {
    var checkSmall = +value;
    if (checkSmall < 1e-6 && checkSmall > -1) {
      value = +value;
      if (value < 1e-6 && value > 0) {
        value = (value + 10).toString();
        value = value.substring(1);
      }
      if (value < 0 && value > -1) {
        value = (value - 10).toString();
        value = "-" + value.substring(2);
      }
      value = value.toString();
    } else {
      var parts = value.split(".");
      if (parts[1] !== void 0) {
        if (+parts[1] === 0) {
          value = parts[0];
        } else {
          let dec = parts[1];
          let trailingZeros = 0;
          for (let i2 = dec.length - 1; i2 >= 0; i2--) {
            if (dec.charAt(i2) !== "0") break;
            trailingZeros++;
          }
          if (trailingZeros > 0) {
            parts[1] = dec.substring(0, dec.length - trailingZeros);
          }
          value = parts.join(".");
        }
      }
    }
  }
  return settings.lZero === "keep" ? value : value.replace(/^0*(\d)/, "$1");
}
__name(checkValue, "checkValue");
function presentNumber(s2, aDec, aNeg) {
  if (aNeg && aNeg !== "-") {
    s2 = s2.replace("-", aNeg);
  }
  if (aDec && aDec !== ".") {
    s2 = s2.replace(".", aDec);
  }
  return s2;
}
__name(presentNumber, "presentNumber");
function autoCheck(s2, settings) {
  s2 = autoStrip(s2, settings);
  s2 = truncateDecimal(s2, settings.aDec, settings.mDec);
  s2 = fixNumber(s2, settings.aDec, settings.aNeg);
  var value = +s2;
  return value >= settings.vMin && value <= settings.vMax;
}
__name(autoCheck, "autoCheck");
function checkEmpty(iv, settings, signOnEmpty) {
  if (iv === "" || iv === settings.aNeg) {
    if (settings.wEmpty === "zero") {
      return iv + "0";
    }
    if (settings.wEmpty === "sign" || signOnEmpty) {
      return iv + settings.aSign;
    }
    return iv;
  }
  return null;
}
__name(checkEmpty, "checkEmpty");
function autoGroup(iv, settings) {
  iv = autoStrip(iv, settings);
  var testNeg = iv.replace(",", "."), empty = checkEmpty(iv, settings, true);
  if (empty !== null) {
    return empty;
  }
  var digitalGroup = "";
  if (settings.dGroup == "2") {
    digitalGroup = /(\d)((\d)(\d{2}?)+)$/;
  } else if (settings.dGroup == "4") {
    digitalGroup = /(\d)((\d{4}?)+)$/;
  } else {
    digitalGroup = /(\d)((\d{3}?)+)$/;
  }
  var ivSplit = iv.split(settings.aDec);
  if (settings.altDec && ivSplit.length === 1) {
    ivSplit = iv.split(settings.altDec);
  }
  var s2 = ivSplit[0];
  if (settings.aSep) {
    while (digitalGroup.test(s2)) {
      s2 = s2.replace(digitalGroup, "$1" + settings.aSep + "$2");
    }
  }
  if (settings.mDec !== 0 && ivSplit.length > 1) {
    if (ivSplit[1].length > settings.mDec) {
      ivSplit[1] = ivSplit[1].substring(0, settings.mDec);
    }
    iv = s2 + settings.aDec + ivSplit[1];
  } else {
    iv = s2;
  }
  if (settings.aSign) {
    var has_aNeg = iv.indexOf(settings.aNeg) !== -1;
    iv = iv.replace(settings.aNeg, "");
    iv = settings.pSign === "p" ? settings.aSign + iv : iv + settings.aSign;
    if (has_aNeg) {
      iv = settings.aNeg + iv;
    }
  }
  if (settings.oEvent === "set" && testNeg < 0 && settings.nBracket !== null) {
    iv = negativeBracket(iv, settings.nBracket, settings.oEvent);
  }
  return iv;
}
__name(autoGroup, "autoGroup");
function autoRound(iv, settings) {
  iv = iv === "" ? "0" : iv.toString();
  convertKeyToNumber(settings, "mDec");
  if (settings.mRound === "CHF") {
    iv = (Math.round(iv * 20) / 20).toString();
  }
  var ivRounded = "", i2 = 0, nSign = "", rDec = typeof settings.aPad === "boolean" || settings.aPad === null ? settings.aPad ? settings.mDec : 0 : +settings.aPad;
  var truncateZeros = /* @__PURE__ */ __name(function(ivRounded2) {
    var regex = rDec === 0 ? /(\.(?:\d*[1-9])?)0*$/ : rDec === 1 ? /(\.\d(?:\d*[1-9])?)0*$/ : new RegExp("(\\.\\d{" + rDec + "}(?:\\d*[1-9])?)0*$");
    ivRounded2 = ivRounded2.replace(regex, "$1");
    if (rDec === 0) {
      ivRounded2 = ivRounded2.replace(/\.$/, "");
    }
    return ivRounded2;
  }, "truncateZeros");
  if (iv.charAt(0) === "-") {
    nSign = "-";
    iv = iv.replace("-", "");
  }
  if (!iv.match(/^\d/)) {
    iv = "0" + iv;
  }
  if (nSign === "-" && +iv === 0) {
    nSign = "";
  }
  if (+iv > 0 && settings.lZero !== "keep" || iv.length > 0 && settings.lZero === "allow") {
    iv = iv.replace(/^0*(\d)/, "$1");
  }
  var dPos = iv.lastIndexOf("."), vdPos = dPos === -1 ? iv.length - 1 : dPos, cDec = iv.length - 1 - vdPos;
  if (cDec <= settings.mDec) {
    ivRounded = iv;
    if (cDec < rDec) {
      if (dPos === -1) {
        ivRounded += ".";
      }
      var zeros = "000000";
      while (cDec < rDec) {
        zeros = zeros.substring(0, rDec - cDec);
        ivRounded += zeros;
        cDec += zeros.length;
      }
    } else if (cDec > rDec) {
      ivRounded = truncateZeros(ivRounded);
    } else if (cDec === 0 && rDec === 0) {
      ivRounded = ivRounded.replace(/\.$/, "");
    }
    if (settings.mRound !== "CHF") {
      return +ivRounded === 0 ? ivRounded : nSign + ivRounded;
    }
    if (settings.mRound === "CHF") {
      dPos = ivRounded.lastIndexOf(".");
      iv = ivRounded;
    }
  }
  var rLength = dPos + settings.mDec, tRound = +iv.charAt(rLength + 1), ivArray = iv.substring(0, rLength + 1).split(""), odd = iv.charAt(rLength) === "." ? iv.charAt(rLength - 1) % 2 : iv.charAt(rLength) % 2, onePass = true;
  odd = odd === 0 && iv.substring(rLength + 2, iv.length) > 0 ? 1 : 0;
  if (tRound > 4 && settings.mRound === "S" || /** Round half up symmetric */
  tRound > 4 && settings.mRound === "A" && nSign === "" || /** Round half up asymmetric positive values */
  tRound > 5 && settings.mRound === "A" && nSign === "-" || /** Round half up asymmetric negative values */
  tRound > 5 && settings.mRound === "s" || /** Round half down symmetric */
  tRound > 5 && settings.mRound === "a" && nSign === "" || /** Round half down asymmetric positive values */
  tRound > 4 && settings.mRound === "a" && nSign === "-" || /** Round half down asymmetric negative values */
  tRound > 5 && settings.mRound === "B" || /** Round half even "Banker's Rounding" */
  tRound === 5 && settings.mRound === "B" && odd === 1 || /** Round half even "Banker's Rounding" */
  tRound > 0 && settings.mRound === "C" && nSign === "" || /** Round to ceiling toward positive infinite */
  tRound > 0 && settings.mRound === "F" && nSign === "-" || /** Round to floor toward negative infinite */
  tRound > 0 && settings.mRound === "U" || settings.mRound === "CHF") {
    for (i2 = ivArray.length - 1; i2 >= 0; i2 -= 1) {
      if (ivArray[i2] !== ".") {
        if (settings.mRound === "CHF" && ivArray[i2] <= 2 && onePass) {
          ivArray[i2] = 0;
          onePass = false;
          break;
        }
        if (settings.mRound === "CHF" && ivArray[i2] <= 7 && onePass) {
          ivArray[i2] = 5;
          onePass = false;
          break;
        }
        if (settings.mRound === "CHF" && onePass) {
          ivArray[i2] = 10;
          onePass = false;
        } else {
          ivArray[i2] = +ivArray[i2] + 1;
        }
        if (ivArray[i2] < 10) {
          break;
        }
        if (i2 > 0) {
          ivArray[i2] = "0";
        }
      }
    }
  }
  ivArray = ivArray.slice(0, rLength + 1);
  ivRounded = truncateZeros(ivArray.join(""));
  return +ivRounded === 0 ? ivRounded : nSign + ivRounded;
}
__name(autoRound, "autoRound");
var AutoNumericHolder = class {
  static {
    __name(this, "AutoNumericHolder");
  }
  constructor(that, settings) {
    this.settings = settings;
    this.that = that;
    this.formatted = false;
    this.settingsClone = autoCode(this.that, this.settings);
    this.value = that.value;
  }
  init(e2) {
    this.value = this.that.value;
    this.settingsClone = autoCode(this.that, this.settings);
    this.ctrlKey = e2.ctrlKey;
    this.cmdKey = e2.metaKey;
    this.shiftKey = e2.shiftKey;
    this.selection = getElementSelection(this.that);
    if (e2.type === "keydown" || e2.type === "keyup") {
      this.kdCode = e2.keyCode;
    }
    this.which = e2.which;
    this.processed = false;
    this.formatted = false;
  }
  setSelection(start, end, setReal) {
    start = Math.max(start, 0);
    end = Math.min(end, this.that.value.length);
    this.selection = {
      start,
      end,
      length: end - start
    };
    if (setReal === void 0 || setReal) {
      setElementSelection(this.that, start, end);
    }
  }
  setPosition(pos, setReal) {
    this.setSelection(pos, pos, setReal);
  }
  getBeforeAfter() {
    var value = this.value, left = value.substring(0, this.selection.start), right = value.substring(this.selection.end, value.length);
    return [left, right];
  }
  getBeforeAfterStriped() {
    var parts = this.getBeforeAfter();
    parts[0] = autoStrip(parts[0], this.settingsClone);
    parts[1] = autoStrip(parts[1], this.settingsClone);
    return parts;
  }
  /**
   * strip parts from excess characters and leading zeroes
   */
  normalizeParts(left, right) {
    var settingsClone = this.settingsClone;
    right = autoStrip(right, settingsClone);
    var strip = right.match(/^\d/) ? true : "leading";
    left = autoStrip(left, settingsClone, strip);
    if ((left === "" || left === settingsClone.aNeg) && settingsClone.lZero === "deny") {
      if (right > "") {
        right = right.replace(/^0*(\d)/, "$1");
      }
    }
    var new_value = left + right;
    if (settingsClone.aDec) {
      var m = new_value.match(new RegExp("^" + settingsClone.aNegRegAutoStrip + "\\" + settingsClone.aDec));
      if (m) {
        left = left.replace(m[1], m[1] + "0");
        new_value = left + right;
      }
    }
    if (settingsClone.wEmpty === "zero" && (new_value === settingsClone.aNeg || new_value === "")) {
      left += "0";
    }
    return [left, right];
  }
  /**
   * set part of number to value keeping position of cursor
   */
  setValueParts(left, right) {
    var settingsClone = this.settingsClone, parts = this.normalizeParts(left, right), new_value = parts.join(""), position = parts[0].length;
    this.dirty = true;
    if (new_value.trim() === "") {
      new_value = "";
    } else if (autoCheck(new_value, settingsClone)) {
      new_value = truncateDecimal(new_value, settingsClone.aDec, settingsClone.mDec);
    } else {
      return false;
    }
    if (position > new_value.length) {
      position = new_value.length;
    }
    this.value = new_value;
    this.setPosition(position, false);
    return true;
  }
  /**
   * helper function for expandSelectionOnSign
   * returns sign position of a formatted value
   */
  signPosition() {
    var settingsClone = this.settingsClone, aSign = settingsClone.aSign, that = this.that;
    if (aSign) {
      var aSignLen = aSign.length;
      if (settingsClone.pSign === "p") {
        var hasNeg = settingsClone.aNeg && that.value && that.value.charAt(0) === settingsClone.aNeg;
        return hasNeg ? [1, aSignLen + 1] : [0, aSignLen];
      }
      var valueLen = that.value.length;
      return [valueLen - aSignLen, valueLen];
    }
    return [1e3, -1];
  }
  /**
   * expands selection to cover whole sign
   * prevents partial deletion/copying/overwriting of a sign
   */
  expandSelectionOnSign(setReal) {
    var sign_position = this.signPosition(), selection = this.selection;
    if (selection.start < sign_position[1] && selection.end > sign_position[0]) {
      if ((selection.start < sign_position[0] || selection.end > sign_position[1]) && this.value.substring(Math.max(selection.start, sign_position[0]), Math.min(selection.end, sign_position[1])).match(/^\s*$/)) {
        if (selection.start < sign_position[0]) {
          this.setSelection(selection.start, sign_position[0], setReal);
        } else {
          this.setSelection(sign_position[1], selection.end, setReal);
        }
      } else {
        this.setSelection(Math.min(selection.start, sign_position[0]), Math.max(selection.end, sign_position[1]), setReal);
      }
    }
  }
  /**
   * try to strip pasted value to digits
   */
  checkPaste() {
    if (this.valuePartsBeforePaste !== void 0) {
      var parts = this.getBeforeAfter(), oldParts = this.valuePartsBeforePaste;
      delete this.valuePartsBeforePaste;
      parts[0] = parts[0].substring(0, oldParts[0].length) + autoStrip(parts[0].substring(oldParts[0].length), this.settingsClone);
      if (!this.setValueParts(parts[0], parts[1])) {
        this.value = oldParts.join("");
        this.setPosition(oldParts[0].length, false);
      }
    }
  }
  /**
   * process pasting, cursor moving and skipping of not interesting keys
   * if returns true, futher processing is not performed
   */
  skipAllways(e2) {
    var kdCode = this.kdCode, which = this.which, ctrlKey = this.ctrlKey, cmdKey = this.cmdKey, shiftKey = this.shiftKey;
    if ((ctrlKey || cmdKey) && e2.type === "keyup" && this.valuePartsBeforePaste !== void 0 || shiftKey && kdCode === 45) {
      this.checkPaste();
      return false;
    }
    if (kdCode >= 112 && kdCode <= 123 || kdCode >= 91 && kdCode <= 93 || kdCode >= 9 && kdCode <= 31 || kdCode < 8 && (which === 0 || which === kdCode) || kdCode === 144 || kdCode === 145 || kdCode === 45) {
      return true;
    }
    if ((ctrlKey || cmdKey) && kdCode === 65) {
      return true;
    }
    if ((ctrlKey || cmdKey) && (kdCode === 67 || kdCode === 86 || kdCode === 88)) {
      if (e2.type === "keydown") {
        this.expandSelectionOnSign();
      }
      if (kdCode === 86 || kdCode === 45) {
        if (e2.type === "keydown" || e2.type === "keypress") {
          if (this.valuePartsBeforePaste === void 0) {
            this.valuePartsBeforePaste = this.getBeforeAfter();
          }
        } else {
          this.checkPaste();
        }
      }
      return e2.type === "keydown" || e2.type === "keypress" || kdCode === 67;
    }
    if (ctrlKey || cmdKey) {
      return true;
    }
    if (kdCode === 37 || kdCode === 39) {
      var aSep = this.settingsClone.aSep, start = this.selection.start, value = this.that.value;
      if (e2.type === "keydown" && aSep && !this.shiftKey) {
        if (kdCode === 37 && value.charAt(start - 2) === aSep) {
          this.setPosition(start - 1);
        } else if (kdCode === 39 && value.charAt(start + 1) === aSep) {
          this.setPosition(start + 1);
        }
      }
      return true;
    }
    if (kdCode >= 34 && kdCode <= 40) {
      return true;
    }
    return false;
  }
  /**
   * process deletion of characters
   * returns true if processing performed
   */
  processAllways() {
    var parts;
    if (this.kdCode === 8 || this.kdCode === 46) {
      if (!this.selection.length) {
        parts = this.getBeforeAfterStriped();
        if (this.kdCode === 8) {
          parts[0] = parts[0].substring(0, parts[0].length - 1);
        } else {
          parts[1] = parts[1].substring(1, parts[1].length);
        }
        this.setValueParts(parts[0], parts[1]);
      } else {
        this.expandSelectionOnSign(false);
        parts = this.getBeforeAfterStriped();
        this.setValueParts(parts[0], parts[1]);
      }
      return true;
    }
    return false;
  }
  /**
   * process insertion of characters
   * returns true if processing performed
   */
  processKeypress() {
    var settingsClone = this.settingsClone, cCode = String.fromCharCode(this.which), parts = this.getBeforeAfterStriped(), left = parts[0], right = parts[1];
    if (cCode === settingsClone.aDec || settingsClone.altDec && cCode === settingsClone.altDec || (cCode === "." || cCode === ",") && this.kdCode === 110) {
      if (!settingsClone.mDec || !settingsClone.aDec) {
        return true;
      }
      if (settingsClone.aNeg && right.indexOf(settingsClone.aNeg) > -1) {
        return true;
      }
      if (left.indexOf(settingsClone.aDec) > -1) {
        return true;
      }
      if (right.indexOf(settingsClone.aDec) > 0) {
        return true;
      }
      if (right.indexOf(settingsClone.aDec) === 0) {
        right = right.substring(1);
      }
      this.setValueParts(left + settingsClone.aDec, right);
      return true;
    }
    if (cCode === "-" || cCode === "+") {
      if (!settingsClone.aNeg) {
        return true;
      }
      if (left === "" && right.indexOf(settingsClone.aNeg) > -1) {
        left = settingsClone.aNeg;
        right = right.substring(1, right.length);
      }
      if (left.charAt(0) === settingsClone.aNeg) {
        left = left.substring(1, left.length);
      } else {
        left = cCode === "-" ? settingsClone.aNeg + left : left;
      }
      this.setValueParts(left, right);
      return true;
    }
    if (cCode >= "0" && cCode <= "9") {
      if (settingsClone.aNeg && left === "" && right.indexOf(settingsClone.aNeg) > -1) {
        left = settingsClone.aNeg;
        right = right.substring(1, right.length);
      }
      if (settingsClone.vMax <= 0 && settingsClone.vMin < settingsClone.vMax && this.value.indexOf(settingsClone.aNeg) === -1 && cCode !== "0") {
        left = settingsClone.aNeg + left;
      }
      this.setValueParts(left + cCode, right);
      return true;
    }
    return true;
  }
  /**
   * formatting of just processed value with keeping of cursor position
   */
  formatQuick() {
    var settingsClone = this.settingsClone, parts = this.getBeforeAfterStriped(), leftLength = this.value;
    if ((settingsClone.aSep === "" || settingsClone.aSep !== "" && leftLength.indexOf(settingsClone.aSep) === -1) && (settingsClone.aSign === "" || settingsClone.aSign !== "" && leftLength.indexOf(settingsClone.aSign) === -1)) {
      var subParts = [], nSign = "";
      subParts = leftLength.split(settingsClone.aDec);
      if (subParts[0].indexOf("-") > -1) {
        nSign = "-";
        subParts[0] = subParts[0].replace("-", "");
        parts[0] = parts[0].replace("-", "");
      }
      if (subParts[0].length > settingsClone.mInt && parts[0].charAt(0) === "0") {
        parts[0] = parts[0].slice(1);
      }
      parts[0] = nSign + parts[0];
    }
    var value = autoGroup(this.value, this.settingsClone), position = value.length;
    if (value) {
      var left_ar = parts[0].split(""), i2 = 0;
      for (i2; i2 < left_ar.length; i2 += 1) {
        if (!left_ar[i2].match("\\d")) {
          left_ar[i2] = "\\" + left_ar[i2];
        }
      }
      var leftReg = new RegExp("^.*?" + left_ar.join(".*?"));
      var newLeft = value.match(leftReg);
      if (newLeft) {
        position = newLeft[0].length;
        if ((position === 0 && value.charAt(0) !== settingsClone.aNeg || position === 1 && value.charAt(0) === settingsClone.aNeg) && settingsClone.aSign && settingsClone.pSign === "p") {
          position = this.settingsClone.aSign.length + (value.charAt(0) === "-" ? 1 : 0);
        }
      } else if (settingsClone.aSign && settingsClone.pSign === "s") {
        position -= settingsClone.aSign.length;
      }
    }
    this.that.value = value;
    this.setPosition(position);
    this.formatted = true;
  }
};
function getHolder(that, settings, update) {
  var data = AutoNumeric.getSettings(that);
  if (!data) {
    data = {};
    that.autoNumeric = data;
  }
  var holder = data.holder;
  if (holder === void 0 && settings || update) {
    holder = new AutoNumericHolder(that, settings);
    data.holder = holder;
  }
  return holder;
}
__name(getHolder, "getHolder");
var AutoNumeric = class _AutoNumeric {
  static {
    __name(this, "AutoNumeric");
  }
  static init(input, options) {
    if (!input) {
      throw new Error("autoNumeric called with null element!");
    }
    var settings = _AutoNumeric.getSettings(input), tagData = {};
    Object.keys(input.dataset).forEach((key) => tagData[key] = input.dataset[key]);
    if (typeof settings !== "object") {
      var defaults = {
        /** allowed numeric values
         * please do not modify
         */
        aNum: "0123456789",
        /** allowed thousand separator characters
         * comma = ','
         * period "full stop" = '.'
         * apostrophe is escaped = '\''
         * space = ' '
         * none = ''
         * NOTE: do not use numeric characters
         */
        aSep: ",",
        /** digital grouping for the thousand separator used in Format
         * dGroup: '2', results in 99,99,99,999 common in India for values less than 1 billion and greater than -1 billion
         * dGroup: '3', results in 999,999,999 default
         * dGroup: '4', results in 9999,9999,9999 used in some Asian countries
         */
        dGroup: "3",
        /** allowed decimal separator characters
         * period "full stop" = '.'
         * comma = ','
         */
        aDec: ".",
        /** allow to declare alternative decimal separator which is automatically replaced by aDec
         * developed for countries the use a comma ',' as the decimal character
         * and have keyboards\numeric pads that have a period 'full stop' as the decimal characters (Spain is an example)
         */
        altDec: null,
        /** allowed currency symbol
         * Must be in quotes aSign: '$', a space is allowed aSign: '$ '
         */
        aSign: "",
        /** placement of currency sign
         * for prefix pSign: 'p',
         * for suffix pSign: 's',
         */
        pSign: "p",
        /** maximum possible value
         * value must be enclosed in quotes and use the period for the decimal point
         * value must be larger than vMin
         */
        vMax: "9999999999999.99",
        /** minimum possible value
         * value must be enclosed in quotes and use the period for the decimal point
         * value must be smaller than vMax
         */
        vMin: "0.00",
        /** max number of decimal places = used to override decimal places set by the vMin & vMax values
         * value must be enclosed in quotes example mDec: '3',
         * This can also set the value via a call back function mDec: 'css:#
         */
        mDec: null,
        /** method used for rounding
         * mRound: 'S', Round-Half-Up Symmetric (default)
         * mRound: 'A', Round-Half-Up Asymmetric
         * mRound: 's', Round-Half-Down Symmetric (lower case s)
         * mRound: 'a', Round-Half-Down Asymmetric (lower case a)
         * mRound: 'B', Round-Half-Even "Bankers Rounding"
         * mRound: 'U', Round Up "Round-Away-From-Zero"
         * mRound: 'D', Round Down "Round-Toward-Zero" - same as truncate
         * mRound: 'C', Round to Ceiling "Toward Positive Infinity"
         * mRound: 'F', Round to Floor "Toward Negative Infinity"
         */
        mRound: "S",
        /** controls decimal padding
         * aPad: true - always Pad decimals with zeros
         * aPad: false - does not pad with zeros.
         * aPad: `some number` - pad decimals with zero to number different from mDec
         * thanks to Jonas Johansson for the suggestion
         */
        aPad: true,
        /** places brackets on negative value -$ 999.99 to (999.99)
         * visible only when the field does NOT have focus the left and right symbols should be enclosed in quotes and seperated by a comma
         * nBracket: null, nBracket: '(,)', nBracket: '[,]', nBracket: '<,>' or nBracket: '{,}'
         */
        nBracket: null,
        /** Displayed on empty string
         * wEmpty: 'empty', - input can be blank
         * wEmpty: 'zero', - displays zero
         * wEmpty: 'sign', - displays the currency sign
         */
        wEmpty: "empty",
        /** controls leading zero behavior
         * lZero: 'allow', - allows leading zeros to be entered. Zeros will be truncated when entering additional digits. On focusout zeros will be deleted.
         * lZero: 'deny', - allows only one leading zero on values less than one
         * lZero: 'keep', - allows leading zeros to be entered. on fousout zeros will be retained.
         */
        lZero: "allow",
        /** determine if the default value will be formatted on page ready.
         * true = automatically formats the default value on page ready
         * false = will not format the default value
         */
        aForm: true
      };
      settings = Object.assign({}, defaults, tagData, options);
      if (settings.aDec === settings.aSep) {
        throw new Error("autoNumeric will not function properly when the decimal character aDec: '" + settings.aDec + "' and thousand separator aSep: '" + settings.aSep + "' are the same character");
      }
      input.autoNumeric = settings;
    } else {
      return;
    }
    settings.runOnce = false;
    var holder = getHolder(input, settings);
    if (!settings.tagList?.includes(input.tagName) && input.tagName !== "INPUT") {
      throw new Error("The <" + input.tagName + "> is not supported by autoNumeric()");
    }
    if (settings.runOnce === false && settings.aForm) {
      if (input.matches("input[type=text], input[type=hidden], input[type=tel], input:not([type])")) {
        var setValue = true;
        if (input.value === "" && settings.wEmpty === "empty") {
          input.value = "";
          setValue = false;
        }
        if (input.value === "" && settings.wEmpty === "sign") {
          input.value = settings.aSign;
          setValue = false;
        }
        if (setValue) {
          _AutoNumeric.setValue(input, input.value);
        }
      }
      if (!settings.tagList?.includes(input.tagName) && input.textContent !== "") {
        _AutoNumeric.setValue(input, input.textContent);
      }
    }
    settings.runOnce = true;
    if (input.matches("input[type=text], input[type=hidden], input[type=tel], input:not([type])")) {
      Fluent.on(input, "keydown.autoNumeric", function(e2) {
        holder = getHolder(input);
        if (holder.settings.aDec === holder.settings.aSep) {
          throw new Error("autoNumeric will not function properly when the decimal character aDec: '" + holder.settings.aDec + "' and thousand separator aSep: '" + holder.settings.aSep + "' are the same character");
        }
        if (holder.that.readOnly) {
          holder.processed = true;
          return true;
        }
        holder.init(e2);
        holder.settings.oEvent = "keydown";
        if (holder.skipAllways(e2)) {
          holder.processed = true;
          return true;
        }
        if (holder.processAllways()) {
          holder.processed = true;
          holder.formatQuick();
          e2.preventDefault();
          return false;
        }
        holder.formatted = false;
        return true;
      });
      Fluent.on(input, "keypress.autoNumeric", function(e2) {
        var holder2 = getHolder(input), processed = holder2.processed;
        holder2.init(e2);
        holder2.settings.oEvent = "keypress";
        if (holder2.skipAllways(e2)) {
          return true;
        }
        if (processed) {
          e2.preventDefault();
          return false;
        }
        if (holder2.processAllways() || holder2.processKeypress()) {
          holder2.formatQuick();
          e2.preventDefault();
          return false;
        }
        holder2.formatted = false;
      });
      Fluent.on(input, "keyup.autoNumeric", function(e2) {
        var holder2 = getHolder(input);
        holder2.init(e2);
        holder2.settings.oEvent = "keyup";
        var skip = holder2.skipAllways(e2);
        holder2.kdCode = 0;
        delete holder2.valuePartsBeforePaste;
        if (input.value === holder2.settings.aSign) {
          if (holder2.settings.pSign === "s") {
            setElementSelection(input, 0, 0);
          } else {
            setElementSelection(input, holder2.settings.aSign.length, holder2.settings.aSign.length);
          }
        }
        if (skip) {
          return true;
        }
        if (input.value === "") {
          return true;
        }
        if (!holder2.formatted) {
          holder2.formatQuick();
        }
      });
      Fluent.on(input, "focusin.autoNumeric", function() {
        if (input.matches("[readonly]") || input.matches("[disabled]"))
          return;
        var holder2 = getHolder(input);
        holder2.settingsClone.oEvent = "focusin";
        if (holder2.settingsClone.nBracket !== null) {
          var checkVal = input.value;
          input.value = negativeBracket(checkVal, holder2.settingsClone.nBracket, holder2.settingsClone.oEvent);
        }
        holder2.inVal = input.value;
        holder2.dirty = false;
        var onempty = checkEmpty(holder2.inVal, holder2.settingsClone, true);
        if (onempty !== null) {
          input.value = onempty ?? "";
          if (holder2.settings.pSign === "s") {
            setElementSelection(input, 0, 0);
          } else {
            setElementSelection(input, holder2.settings.aSign.length, holder2.settings.aSign.length);
          }
        }
      });
      Fluent.on(input, "focusout.autoNumeric", function() {
        if (input.matches("[readonly]") || input.matches("[disabled]"))
          return;
        var holder2 = getHolder(input), settingsClone = holder2.settingsClone, value = input.value, origValue = value;
        holder2.settingsClone.oEvent = "focusout";
        var strip_zero = "";
        if (settingsClone.lZero === "allow") {
          settingsClone.allowLeading = false;
          strip_zero = "leading";
        }
        if (value !== "") {
          value = autoStrip(value, settingsClone, strip_zero);
          if (checkEmpty(value, settingsClone) === null && (!holder2.dirty && holder2.inVal == origValue || autoCheck(value, settingsClone))) {
            value = fixNumber(value, settingsClone.aDec, settingsClone.aNeg);
            value = autoRound(value, settingsClone);
            value = presentNumber(value, settingsClone.aDec, settingsClone.aNeg);
          } else {
            value = "";
          }
        }
        var groupedValue = checkEmpty(value, settingsClone, false);
        if (groupedValue === null) {
          groupedValue = autoGroup(value, settingsClone);
        }
        if (groupedValue !== origValue) {
          input.value = groupedValue;
        }
        if (groupedValue !== holder2.inVal) {
          Fluent.trigger(input, "change");
          delete holder2.inVal;
        }
        if (settingsClone.nBracket !== null && _AutoNumeric.getValue(input) < 0) {
          holder2.settingsClone.oEvent = "focusout";
          input.value = negativeBracket(input.value, settingsClone.nBracket, settingsClone.oEvent);
        }
      });
    }
  }
  /** method to remove settings and stop autoNumeric() */
  static destroy(input) {
    Fluent.off(input, ".autoNumeric");
    delete input?.autoNumeric;
  }
  /** method to update settings - can call as many times */
  static updateOptions(input, options) {
    var settings = _AutoNumeric.getSettings(input);
    if (typeof settings !== "object") {
      throw new Error("You must initialize autoNumeric('init', {options}) prior to calling the 'update' method");
    }
    var strip = _AutoNumeric.getValue(input);
    settings = Object.assign(settings, options);
    getHolder(input, settings, true);
    if (settings.aDec === settings.aSep) {
      throw new Error("autoNumeric will not function properly when the decimal character aDec: '" + settings.aDec + "' and thousand separator aSep: '" + settings.aSep + "' are the same character");
    }
    input.autoNumeric = settings;
    if (input.value !== "" || input.textContent !== "") {
      _AutoNumeric.setValue(input, strip);
    }
    return;
  }
  /** returns a formatted strings for "input:text" fields Uses jQuery's .val() method*/
  static setValue(input, valueIn) {
    var settings = _AutoNumeric.getSettings(input), value = valueIn.toString(), testValue = valueIn.toString();
    if (typeof settings !== "object") {
      throw new Error("You must initialize autoNumeric('init', {options}) prior to calling the 'set' method");
    }
    if (testValue !== input.getAttribute("value") && input.tagName === "INPUT" && settings.runOnce === false) {
      value = settings.nBracket !== null ? negativeBracket(input.value, settings.nBracket, "pageLoad") : value;
      value = autoStrip(value, settings);
    }
    if ((testValue === input.getAttribute("value") || testValue === input.textContent) && settings.runOnce === false) {
      value = value.replace(",", ".");
    }
    if (isNaN(value)) {
      return "";
    }
    value = checkValue(value, settings);
    settings.oEvent = "set";
    value.toString();
    if (value !== "") {
      value = autoRound(value, settings);
    }
    value = presentNumber(value, settings.aDec, settings.aNeg);
    value = autoGroup(value, settings);
    if (input.matches("input[type=text], input[type=hidden], input[type=tel], input:not([type])")) {
      input.value = value;
    }
    if (!settings.tagList?.includes(input.tagName)) {
      return input.textContent = value;
    }
    throw new Error("The <" + input.tagName + "> is not supported by autoNumeric()");
  }
  /** method to get the unformatted value from a specific input field, returns a numeric value */
  static getValue(input) {
    var settings = _AutoNumeric.getSettings(input);
    if (typeof settings !== "object") {
      throw new Error("You must initialize autoNumeric('init', {options}) prior to calling the 'get' method");
    }
    settings.oEvent = "get";
    var getValue = "";
    if (input.matches("input[type=text], input[type=hidden], input[type=tel], input:not([type])")) {
      getValue = input.value;
    } else if (settings.tagList?.includes(input.tagName)) {
      getValue = input.textContent;
    } else {
      throw new Error("The <" + input.tagName + "> is not supported by autoNumeric()");
    }
    if (getValue === "" && settings.wEmpty === "empty" || getValue === settings.aSign && (settings.wEmpty === "sign" || settings.wEmpty === "empty")) {
      return "";
    }
    if (settings.nBracket !== null && getValue !== "") {
      getValue = negativeBracket(getValue, settings.nBracket, settings.oEvent);
    }
    if (settings.runOnce || settings.aForm === false) {
      getValue = autoStrip(getValue, settings);
    }
    getValue = fixNumber(getValue, settings.aDec, settings.aNeg);
    if (+getValue === 0 && settings.lZero !== "keep") {
      getValue = "0";
    }
    if (settings.lZero === "keep") {
      return getValue;
    }
    getValue = checkValue(getValue, settings);
    return getValue;
  }
  /** returns the settings object for those who need to look under the hood */
  static getSettings(input) {
    return input?.autoNumeric;
  }
  static hasInstance(input) {
    return typeof input?.autoNumeric === "object";
  }
  static {
    this.allowedSettingKeys = /* @__PURE__ */ new Set([
      "aNum",
      "aSep",
      "dGroup",
      "aDec",
      "altDec",
      "aSign",
      "pSign",
      "vMax",
      "vMin",
      "mDec",
      "mRound",
      "aPad",
      "nBracket",
      "wEmpty",
      "lZero",
      "aForm",
      "tagList"
    ]);
  }
};

// src/ui/editors/booleaneditor.tsx
var _a55, _b38;
var BooleanEditor = class extends (_b38 = EditorWidget, _a55 = Symbol.typeInfo, _b38) {
  static {
    __name(this, "BooleanEditor");
  }
  static {
    this[_a55] = this.registerEditor(nsSerenity, [IBooleanValue]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "checkbox" });
  }
  get value() {
    return !!this.domNode.checked;
  }
  get_value() {
    return this.value;
  }
  set value(value) {
    this.domNode.checked = !!value;
  }
  set_value(value) {
    this.value = value;
  }
};

// src/ui/helpers/gridselectallbuttonhelper.tsx
var GridSelectAllButtonHelper;
((GridSelectAllButtonHelper2) => {
  function update(grid, getSelected) {
    var toolbar = grid.getElement().querySelector(".s-Toolbar");
    if (!toolbar) {
      return;
    }
    var btn = getWidgetFrom(toolbar, Toolbar).findButton("select-all-button");
    var items = grid.getView().getItems();
    btn.toggleClass("checked", items.length > 0 && !items.some(function(x) {
      return !getSelected(x);
    }));
  }
  GridSelectAllButtonHelper2.update = update;
  __name(update, "update");
  function define(getGrid, getId, getSelected, setSelected, text2, onClick) {
    if (text2 == null) {
      text2 = CheckTreeEditorTexts.SelectAll ?? "Select All";
    }
    return {
      title: text2,
      action: "select-all",
      cssClass: "select-all-button",
      onClick: /* @__PURE__ */ __name(function(e2) {
        var grid = getGrid();
        var view = grid.getView();
        var btn = e2.target.closest(".select-all-button");
        var makeSelected = !btn?.classList.contains("checked");
        view.beginUpdate();
        try {
          for (var item of view.getItems()) {
            setSelected(item, makeSelected);
            view.updateItem(getId(item), item);
          }
          onClick && onClick();
        } finally {
          view.endUpdate();
        }
        btn?.classList.toggle("checked", makeSelected);
      }, "onClick")
    };
  }
  GridSelectAllButtonHelper2.define = define;
  __name(define, "define");
})(GridSelectAllButtonHelper || (GridSelectAllButtonHelper = {}));

// src/ui/editors/checktreeeditor.tsx
var _a56, _b39;
var CheckTreeEditor = class extends (_b39 = DataGrid, _a56 = Symbol.typeInfo, _b39) {
  static {
    __name(this, "CheckTreeEditor");
  }
  static {
    this[_a56] = this.registerEditor(nsSerenity, [IGetEditValue, ISetEditValue, IReadOnly]);
  }
  static createDefaultElement() {
    return document.createElement("div");
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("s-CheckTreeEditor");
    this.updateItems();
  }
  getIdProperty() {
    return "id";
  }
  getTreeItems() {
    return [];
  }
  updateItems() {
    var items = this.getTreeItems();
    var itemById = {};
    for (var i2 = 0; i2 < items.length; i2++) {
      var item = items[i2];
      item.children = [];
      if (item.id) {
        itemById[item.id] = item;
      }
      if (item.parentId) {
        var parent2 = itemById[item.parentId];
        if (parent2 != null) {
          parent2.children.push(item);
        }
      }
    }
    this.view.addData({ Entities: items, Skip: 0, Take: 0, TotalCount: items.length });
    this.updateSelectAll();
    this.updateFlags();
  }
  getEditValue(property, target) {
    if (this.getDelimited())
      target[property.name] = this.get_value().join(",");
    else
      target[property.name] = this.get_value();
  }
  setEditValue(source, property) {
    var value = source[property.name];
    this.set_value(value);
  }
  getButtons() {
    var selectAllText = this.getSelectAllText();
    if (!selectAllText) {
      return null;
    }
    var self2 = this;
    var buttons = [];
    buttons.push(GridSelectAllButtonHelper.define(function() {
      return self2;
    }, function(x) {
      return x.id;
    }, function(x1) {
      return x1.isSelected;
    }, (x2, v2) => {
      if (x2.isSelected !== v2) {
        x2.isSelected = v2;
        this.itemSelectedChanged(x2);
      }
    }, null, () => {
      this.updateFlags();
    }));
    return buttons;
  }
  itemSelectedChanged(item) {
  }
  getSelectAllText() {
    return CheckTreeEditorTexts.asTry().SelectAll ?? "Select All";
  }
  isThreeStateHierarchy() {
    return false;
  }
  initSleekGrid() {
    this.domNode.classList.add("slick-no-cell-border", "slick-no-odd-even", "slick-hide-header");
    super.initSleekGrid();
    this.sleekGrid.resizeCanvas();
  }
  onViewFilter(item) {
    if (!super.onViewFilter(item)) {
      return false;
    }
    var items = this.view.getItems();
    var self2 = this;
    return SlickTreeHelper.filterCustom(item, function(x) {
      if (x.parentId == null) {
        return null;
      }
      if (self2.itemById == null) {
        self2.itemById = {};
        for (var i2 = 0; i2 < items.length; i2++) {
          var o2 = items[i2];
          if (o2.id != null) {
            self2.itemById[o2.id] = o2;
          }
        }
      }
      return self2.itemById[x.parentId];
    });
  }
  getInitialCollapse() {
    return false;
  }
  onViewProcessData(response) {
    response = super.onViewProcessData(response);
    this.itemById = null;
    SlickTreeHelper.setIndents(response.Entities, function(x) {
      return x.id;
    }, function(x1) {
      return x1.parentId;
    }, this.getInitialCollapse());
    return response;
  }
  onClick(e2, row, cell) {
    super.onClick(e2, row, cell);
    if (!Fluent.isDefaultPrevented(e2)) {
      SlickTreeHelper.toggleClick(e2, row, cell, this.view, function(x) {
        return x.id;
      });
    }
    if (Fluent.isDefaultPrevented(e2)) {
      return;
    }
    var target = e2.target;
    if (target.classList.contains("check-box")) {
      e2.preventDefault();
      if (this._readOnly)
        return;
      var checkedOrPartial = target.classList.contains("checked") || target.classList.contains("partial");
      var item = this.itemAt(row);
      var anyChanged = item.isSelected !== !checkedOrPartial;
      this.view.beginUpdate();
      try {
        if (item.isSelected !== !checkedOrPartial) {
          item.isSelected = !checkedOrPartial;
          this.view.updateItem(item.id, item);
          this.itemSelectedChanged(item);
        }
        anyChanged = this.setAllSubTreeSelected(item, item.isSelected) || anyChanged;
        this.updateSelectAll();
        this.updateFlags();
      } finally {
        this.view.endUpdate();
      }
      if (anyChanged) {
        Fluent.trigger(this.domNode, "change");
      }
    }
  }
  updateSelectAll() {
    GridSelectAllButtonHelper.update(this, function(x) {
      return x.isSelected;
    });
  }
  updateFlags() {
    var view = this.view;
    var items = view.getItems();
    var threeState = this.isThreeStateHierarchy();
    if (!threeState) {
      return;
    }
    view.beginUpdate();
    try {
      for (var i2 = 0; i2 < items.length; i2++) {
        var item = items[i2];
        if (item.children == null || item.children.length === 0) {
          var allsel = this.getDescendantsSelected(item);
          if (allsel !== item.isAllDescendantsSelected) {
            item.isAllDescendantsSelected = allsel;
            view.updateItem(item.id, item);
          }
          continue;
        }
        var allSelected = this.allDescendantsSelected(item);
        var selected = allSelected || this.anyDescendantsSelected(item);
        if (allSelected !== item.isAllDescendantsSelected || selected !== item.isSelected) {
          var selectedChange = item.isSelected !== selected;
          item.isAllDescendantsSelected = allSelected;
          item.isSelected = selected;
          view.updateItem(item.id, item);
          if (selectedChange) {
            this.itemSelectedChanged(item);
          }
        }
      }
    } finally {
      view.endUpdate();
    }
  }
  getDescendantsSelected(item) {
    return true;
  }
  setAllSubTreeSelected(item, selected) {
    var result = false;
    for (var i2 = 0; i2 < item.children.length; i2++) {
      var sub = item.children[i2];
      if (sub.isSelected !== selected) {
        result = true;
        sub.isSelected = selected;
        this.view.updateItem(sub.id, sub);
        this.itemSelectedChanged(sub);
      }
      if (sub.children.length > 0) {
        result = this.setAllSubTreeSelected(sub, selected) || result;
      }
    }
    return result;
  }
  allItemsSelected() {
    for (var i2 = 0; i2 < this.rowCount(); i2++) {
      var row = this.itemAt(i2);
      if (!row.isSelected) {
        return false;
      }
    }
    return this.rowCount() > 0;
  }
  allDescendantsSelected(item) {
    if (item.children.length > 0) {
      for (var i2 = 0; i2 < item.children.length; i2++) {
        var sub = item.children[i2];
        if (!sub.isSelected) {
          return false;
        }
        if (!this.allDescendantsSelected(sub)) {
          return false;
        }
      }
    }
    return true;
  }
  getDelimited() {
    return !!!!this.options["delimited"];
  }
  anyDescendantsSelected(item) {
    if (item.children.length > 0) {
      for (var i2 = 0; i2 < item.children.length; i2++) {
        var sub = item.children[i2];
        if (sub.isSelected) {
          return true;
        }
        if (this.anyDescendantsSelected(sub)) {
          return true;
        }
      }
    }
    return false;
  }
  createColumns() {
    var self2 = this;
    var columns = [];
    columns.push({
      field: "text",
      name: "Record",
      width: 80,
      format: SlickFormatting.treeToggle(function() {
        return self2.view;
      }, function(x) {
        return x.id;
      }, (ctx) => {
        const item = ctx.item;
        if (item.hideCheckBox) {
          return this.getItemText(ctx);
        }
        const threeState = this.isThreeStateHierarchy();
        return jsx(Fragment, { children: [
          jsx("span", { class: ["check-box", item.isSelected && (threeState && !item.isAllDescendantsSelected ? "partial" : "checked"), this._readOnly && "readonly"] }),
          this.getItemText(ctx)
        ] });
      })
    });
    return columns;
  }
  getItemText(ctx) {
    return ctx.escape();
  }
  getSlickOptions() {
    var opt = super.getSlickOptions();
    opt.forceFitColumns = true;
    return opt;
  }
  sortItems() {
    if (!this.moveSelectedUp()) {
      return;
    }
    var oldIndexes = {};
    var list = this.view.getItems();
    var i2 = 0;
    for (var $t1 = 0; $t1 < list.length; $t1++) {
      var x = list[$t1];
      oldIndexes[x.id] = i2++;
    }
    list.sort(function(x1, y2) {
      if (x1.isSelected && !y2.isSelected) {
        return -1;
      }
      if (y2.isSelected && !x1.isSelected) {
        return 1;
      }
      var c2 = Culture.stringCompare(x1.text, y2.text);
      if (c2 !== 0) {
        return c2;
      }
      return oldIndexes[x1.id] < oldIndexes[y2.id] ? -1 : oldIndexes[x1.id] > oldIndexes[y2.id] ? 1 : 0;
    });
    this.view.setItems(list, true);
  }
  moveSelectedUp() {
    return false;
  }
  get_readOnly() {
    return this._readOnly;
  }
  set_readOnly(value) {
    if (!!this._readOnly != !!value) {
      this._readOnly = !!value;
      this.view.refresh();
    }
  }
  get_value() {
    var list = [];
    var items = this.view.getItems();
    for (var i2 = 0; i2 < items.length; i2++) {
      if (items[i2].isSelected) {
        list.push(items[i2].id);
      }
    }
    return list;
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    var selected = {};
    if (value != null) {
      if (typeof value == "string") {
        value = value.split(",").map((x) => x?.trim()).filter((x) => !!x);
      }
      for (var i2 = 0; i2 < value.length; i2++) {
        selected[value[i2]] = true;
      }
    }
    this.view.beginUpdate();
    try {
      var items = this.view.getItems();
      for (var i1 = 0; i1 < items.length; i1++) {
        var item = items[i1];
        var select = selected[item.id];
        if (select !== item.isSelected) {
          item.isSelected = select;
          this.view.updateItem(item.id, item);
        }
      }
      this.updateSelectAll();
      this.updateFlags();
      this.sortItems();
    } finally {
      this.view.endUpdate();
    }
  }
  set value(v2) {
    this.set_value(v2);
  }
};
var _a57, _b40;
var CheckLookupEditor = class extends (_b40 = CheckTreeEditor, _a57 = Symbol.typeInfo, _b40) {
  static {
    __name(this, "CheckLookupEditor");
  }
  static {
    this[_a57] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.enableUpdateItems = true;
    this.setCascadeFrom(this.options.cascadeFrom);
    this.updateItems();
    this.lookupChangeOff = ScriptData.bindToChange("Lookup." + this.getLookupKey(), bindThis(this).updateItems);
  }
  destroy() {
    if (this.lookupChangeOff) {
      this.lookupChangeOff();
      this.lookupChangeOff = null;
    }
    super.destroy();
  }
  updateItems() {
    if (this.enableUpdateItems)
      super.updateItems();
  }
  getLookupKey() {
    return this.options.lookupKey;
  }
  getButtons() {
    return super.getButtons() ?? (this.options.hideSearch ? null : []);
  }
  createToolbarExtensions() {
    super.createToolbarExtensions();
    GridUtils.addQuickSearch({
      container: this.toolbar.domNode,
      search: /* @__PURE__ */ __name(({ query, done }) => {
        this.searchText = stripDiacritics(query || "").toUpperCase();
        this.view.setItems(this.view.getItems(), true);
        done(this.rowCount() > 0);
      }, "search")
    });
  }
  getSelectAllText() {
    if (!this.options.showSelectAll)
      return null;
    return super.getSelectAllText();
  }
  cascadeItems(items) {
    var val = this.get_cascadeValue();
    if (val == null || val === "") {
      if (this.get_cascadeField()) {
        return [];
      }
      return items;
    }
    var key = val.toString();
    var fld = this.get_cascadeField();
    return items.filter((x) => {
      var itemKey = x[fld];
      return !!(itemKey != null && itemKey.toString() === key);
    });
  }
  filterItems(items) {
    var val = this.get_filterValue();
    if (val == null || val === "") {
      return items;
    }
    var key = val.toString();
    var fld = this.get_filterField();
    return items.filter((x) => {
      var itemKey = x[fld];
      return !!(itemKey != null && itemKey.toString() === key);
    });
  }
  getLookupItems(lookup) {
    return this.filterItems(this.cascadeItems(lookup.items));
  }
  getTreeItems() {
    var lookup = getLookup(this.options.lookupKey);
    var items = this.getLookupItems(lookup);
    return items.map((item) => ({
      id: (item[lookup.idField] ?? "").toString(),
      text: (item[lookup.textField] ?? "").toString(),
      source: item
    }));
  }
  onViewFilter(item) {
    return super.onViewFilter(item) && (!this.searchText || stripDiacritics(item.text || "").toUpperCase().indexOf(this.searchText) >= 0);
  }
  moveSelectedUp() {
    return this.options.checkedOnTop;
  }
  get_cascadeFrom() {
    return this.options.cascadeFrom;
  }
  get cascadeFrom() {
    return this.get_cascadeFrom();
  }
  getCascadeFromValue(parent2) {
    return EditorUtils.getValue(parent2);
  }
  setCascadeFrom(value) {
    if (!value) {
      if (this.cascadeLink != null) {
        this.cascadeLink.set_parentID(null);
        this.cascadeLink = null;
      }
      this.options.cascadeFrom = null;
      return;
    }
    this.cascadeLink = new CascadedWidgetLink(Widget, this, (p2) => {
      this.set_cascadeValue(this.getCascadeFromValue(p2));
    });
    this.cascadeLink.set_parentID(value);
    this.options.cascadeFrom = value;
  }
  set_cascadeFrom(value) {
    if (value !== this.options.cascadeFrom) {
      this.setCascadeFrom(value);
      this.updateItems();
    }
  }
  set cascadeFrom(value) {
    this.set_cascadeFrom(value);
  }
  get_cascadeField() {
    return this.options.cascadeField ?? this.options.cascadeFrom;
  }
  get cascadeField() {
    return this.get_cascadeField();
  }
  set_cascadeField(value) {
    this.options.cascadeField = value;
  }
  set cascadeField(value) {
    this.set_cascadeField(value);
  }
  get_cascadeValue() {
    return this.options.cascadeValue;
  }
  get cascadeValue() {
    return this.get_cascadeValue();
  }
  set_cascadeValue(value) {
    if (this.options.cascadeValue !== value) {
      this.options.cascadeValue = value;
      this.value = [];
      this.updateItems();
    }
  }
  set cascadeValue(value) {
    this.set_cascadeValue(value);
  }
  get_filterField() {
    return this.options.filterField;
  }
  get filterField() {
    return this.get_filterField();
  }
  set_filterField(value) {
    this.options.filterField = value;
  }
  set filterField(value) {
    this.set_filterField(value);
  }
  get_filterValue() {
    return this.options.filterValue;
  }
  get filterValue() {
    return this.get_filterValue();
  }
  set_filterValue(value) {
    if (this.options.filterValue !== value) {
      this.options.filterValue = value;
      this.value = null;
      this.updateItems();
    }
  }
  set filterValue(value) {
    this.set_filterValue(value);
  }
};

// src/ui/editors/dateyeareditor.ts
var _a58, _b41;
var DateYearEditor = class extends (_b41 = SelectEditor, _a58 = Symbol.typeInfo, _b41) {
  static {
    __name(this, "DateYearEditor");
  }
  static {
    this[_a58] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.updateItems();
  }
  getItems() {
    var opt = this.options;
    if (opt.items != null && opt.items.length >= 1) {
      return opt.items;
    }
    var years = [];
    var minYear = (/* @__PURE__ */ new Date()).getFullYear();
    var maxYear = (/* @__PURE__ */ new Date()).getFullYear();
    opt.minYear = (opt.minYear ?? "-10").toString();
    if (opt.minYear.startsWith("-")) {
      minYear -= parseInt(opt.minYear.substring(1), 10);
    } else if (opt.minYear.startsWith("+")) {
      minYear += parseInt(opt.minYear.substring(1), 10);
    } else {
      minYear = parseInt(opt.minYear, 10);
    }
    opt.maxYear = (opt.maxYear ?? "+10").toString();
    if (opt.maxYear.startsWith("-")) {
      maxYear -= parseInt(opt.maxYear.substring(1), 10);
    } else if (opt.maxYear.startsWith("+")) {
      maxYear += parseInt(opt.maxYear.substring(1), 10);
    } else {
      maxYear = parseInt(opt.maxYear, 10);
    }
    if (opt.descending) {
      for (var i2 = maxYear; i2 >= minYear; i2--) {
        years.push(i2.toString());
      }
    } else {
      for (var i1 = minYear; i1 <= maxYear; i1++) {
        years.push(i1.toString());
      }
    }
    return years;
  }
};

// src/ui/editors/decimaleditor.tsx
var _a59, _b42;
var DecimalEditor = class _DecimalEditor extends (_b42 = EditorWidget, _a59 = Symbol.typeInfo, _b42) {
  static {
    __name(this, "DecimalEditor");
  }
  static {
    this[_a59] = this.registerEditor(nsSerenity, [IDoubleValue]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("decimalQ");
    this.initAutoNumeric();
  }
  destroy() {
    AutoNumeric.destroy(this.domNode);
    super.destroy();
  }
  initAutoNumeric() {
    AutoNumeric.init(this.domNode, this.getAutoNumericOptions());
  }
  getAutoNumericOptions() {
    var numericOptions = Object.assign({}, _DecimalEditor.defaultAutoNumericOptions(), {
      vMin: this.options.minValue ?? (this.options.allowNegatives ? this.options.maxValue != null ? "-" + this.options.maxValue : "-999999999999.99" : "0.00"),
      vMax: this.options.maxValue ?? "999999999999.99"
    });
    if (this.options.decimals != null) {
      numericOptions.mDec = this.options.decimals;
    }
    if (this.options.padDecimals != null) {
      numericOptions.aPad = this.options.padDecimals;
    }
    for (const key of Object.keys(this.options)) {
      if (AutoNumeric.allowedSettingKeys.has(key)) {
        numericOptions[key] = this.options[key];
      }
    }
    return numericOptions;
  }
  get_value() {
    var val;
    if (AutoNumeric.hasInstance(this.domNode)) {
      val = AutoNumeric.getValue(this.domNode);
      if (!!(val == null || val === ""))
        return null;
      return parseFloat(val);
    }
    val = this.domNode.value;
    return parseDecimal(val);
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (value == null || value === "") {
      this.domNode.value = "";
    } else if (AutoNumeric.hasInstance(this.domNode)) {
      AutoNumeric.setValue(this.domNode, value);
    } else
      this.domNode.value = formatNumber(value);
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_isValid() {
    return !isNaN(this.get_value());
  }
  static defaultAutoNumericOptions() {
    return {
      aDec: Culture.decimalSeparator,
      altDec: Culture.decimalSeparator === "." ? "," : ".",
      aSep: Culture.decimalSeparator === "." ? "," : ".",
      aPad: true
    };
  }
};

// src/ui/editors/emailaddresseditor.tsx
var _a60, _b43;
var EmailAddressEditor = class extends (_b43 = StringEditor, _a60 = Symbol.typeInfo, _b43) {
  static {
    __name(this, "EmailAddressEditor");
  }
  static {
    this[_a60] = this.registerEditor(nsSerenity);
  }
  static createDefaultElement() {
    return jsx("input", { type: "email" });
  }
  constructor(props) {
    super(props);
    this.domNode?.classList.add("email");
  }
};

// src/ui/editors/emaileditor.tsx
var _a61, _b44;
var EmailEditor = class _EmailEditor extends (_b44 = EditorWidget, _a61 = Symbol.typeInfo, _b44) {
  static {
    __name(this, "EmailEditor");
  }
  static {
    this[_a61] = this.registerEditor(nsSerenity, [IStringValue, IReadOnly]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    _EmailEditor.registerValidationMethods();
    this.domNode.classList.add("emailuser");
    this.domain = jsx("input", { type: "text", class: "emaildomain" });
    this.domNode.after(jsx(Fragment, { children: [
      jsx("span", { class: "emailat", children: "@" }),
      this.domain
    ] }));
    Fluent.on(this.domain, "blur." + this.uniqueName, function() {
      ValidationHelper.validateElement(this.domNode);
    });
    if (this.options.domain) {
      this.domain.value = this.options.domain;
    }
    if (this.options.readOnlyDomain) {
      this.domain.readOnly = true;
      this.domain.classList.add("readonly", "disabled");
      this.domain.tabIndex = -1;
    }
    this.element.on("keypress." + this.uniqueName, (e2) => {
      if (e2.key === "@") {
        e2.preventDefault();
        if (!this.options.readOnlyDomain) {
          this.domain.focus();
          this.domain.select();
        }
      }
    });
    Fluent.on(this.domain, "keypress." + this.uniqueName, function(e2) {
      if (e2.key === "@")
        e2.preventDefault();
    });
    if (!this.options.readOnlyDomain) {
      Fluent.on(this.domNode, "change", (e2) => this.set_value(this.domNode.value));
    }
  }
  static registerValidationMethods() {
    Validator.addMethod("emailuser", function(value, element) {
      var domain = Fluent(element).nextSibling(".emaildomain").getNode();
      if (domain && domain.getAttribute("readonly") == null) {
        if (Validator.optional(element) && Validator.optional(domain)) {
          return true;
        }
        return Validator.methods.email(value + "@" + domain.value, element);
      } else {
        return Validator.methods.email(value + "@dummy.com", element);
      }
    }, FormValidationTexts.Email);
  }
  get_value() {
    var value = this.domNode.value;
    var domainValue = this.domain.value;
    if (!value) {
      if (this.options.readOnlyDomain || !domainValue) {
        return "";
      }
      return "@" + domainValue;
    }
    return value + "@" + domainValue;
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    value = value?.trim();
    if (!value) {
      if (!this.options.readOnlyDomain)
        this.domain.value = "";
      this.domNode.value = "";
    } else {
      var parts = value.split("@");
      if (parts.length > 1) {
        if (!this.options.readOnlyDomain) {
          this.domain.value = parts[1];
          this.domNode.value = parts[0];
        } else if (this.options.domain) {
          if (parts[1] !== this.options.domain)
            this.domNode.value = value;
          else
            this.domNode.value = parts[0];
        } else
          this.domNode.value = parts[0];
      } else
        this.domNode.value = parts[0];
    }
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_readOnly() {
    return !(this.domNode.getAttribute("readonly") == null && (!this.options.readOnlyDomain || this.domain.getAttribute("readonly") == null));
  }
  set_readOnly(value) {
    setElementReadOnly(this.domNode, value);
    if (!this.options.readOnlyDomain) {
      setElementReadOnly(this.domain, value);
    }
  }
};

// src/ui/editors/enumeditor.ts
var _a62, _b45;
var EnumEditor = class extends (_b45 = ComboboxEditor, _a62 = Symbol.typeInfo, _b45) {
  static {
    __name(this, "EnumEditor");
  }
  static {
    this[_a62] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.updateItems();
  }
  updateItems() {
    this.clearItems();
    var enumType = this.options.enumType || EnumTypeRegistry.getOrLoad(this.options.enumKey);
    const then = /* @__PURE__ */ __name((enumType2) => {
      var enumKey = this.options.enumKey;
      if (enumKey == null && enumType2 != null) {
        enumKey = getCustomAttribute(enumType2, EnumKeyAttribute, false)?.value ?? getTypeFullName(enumType2);
      }
      var values = Enum.getValues(enumType2);
      for (var x of values) {
        var name = Enum.toString(enumType2, x);
        this.addOption(
          parseInt(x, 10).toString(),
          localText("Enums." + enumKey + "." + name, name),
          null,
          false
        );
      }
    }, "then");
    if (isPromiseLike(enumType))
      return enumType.then(then);
    else
      then(enumType);
  }
  allowClear() {
    return this.options.allowClear ?? true;
  }
};

// src/ui/editors/htmlcontenteditor-tiptap.tsx
function TiptapButton(props) {
  return jsx(
    "button",
    {
      class: { "btn btn-link": true, active: props.active },
      hidden: props.hidden,
      disabled: props.disabled,
      title: props.title,
      type: "button",
      tabindex: "-1",
      onClick: (e2) => {
        e2.preventDefault();
        props.onClick();
      },
      children: jsx("i", { class: props.icon })
    }
  );
}
__name(TiptapButton, "TiptapButton");
function TiptapToolbar(props) {
  const hidden = props?.hidden ?? {};
  const editor = props.editor;
  const { computed: computed2, update } = useUpdatableComputed();
  editor.on("transaction", update);
  const boldItalicUnderlineStrike = ["bold", "italic", "underline", "strike"];
  const superSubScriptCode = ["superscript", "subscript", "code"];
  const alignment = ["left", "center", "right", "justify"];
  const hiddenUndoRedo = computed2(() => hidden.undoRedo || !isEditable(editor));
  const hiddenMark = {
    bold: computed2(() => hidden.boldItalicUnderline || !showMark(editor, "bold")),
    italic: computed2(() => hidden.boldItalicUnderline || !showMark(editor, "italic")),
    underline: computed2(() => hidden.boldItalicUnderline || !showMark(editor, "underline")),
    strike: computed2(() => hidden.strike || !showMark(editor, "strike")),
    code: computed2(() => hidden.inlineCode || !showMark(editor, "code")),
    superscript: computed2(() => hidden.superSubScript || !showMark(editor, "superscript")),
    subscript: computed2(() => hidden.superSubScript || !showMark(editor, "subscript"))
  };
  const hiddenLeftCenterRight = computed2(() => hidden.alignment || !showTextAlign(editor));
  const hiddenJustify = computed2(() => hiddenLeftCenterRight.value || hidden.alignmentJustify);
  const hiddenBoldItalicUnderlineStrike = computed2(() => boldItalicUnderlineStrike.every((key) => hiddenMark[key].value));
  const hiddenSuperSubScriptCode = computed2(() => superSubScriptCode.every((key) => hiddenMark[key].value));
  const hiddenAlignmentAll = computed2(() => hiddenLeftCenterRight.value && hiddenJustify.value);
  return jsx("div", { role: "toolbar", title: "toolbar", "data-variant": "fixed", class: "btn-toolbar", children: [
    jsx("div", { class: "btn-group", hidden: hiddenUndoRedo, children: ["undo", "redo"].map(
      (key) => jsx(
        TiptapButton,
        {
          title: undoRedoTexts[key],
          icon: undoRedoIcons[key],
          disabled: computed2(() => !canUndoRedo(editor, key)),
          hidden: hiddenUndoRedo,
          onClick: () => execUndoRedo(editor, key)
        }
      )
    ) }),
    jsx("div", { class: "btn-group", hidden: hiddenBoldItalicUnderlineStrike, children: boldItalicUnderlineStrike.map(
      (key) => jsx(
        TiptapButton,
        {
          title: markTexts[key],
          icon: markIcons[key],
          active: computed2(() => isMarkActive(editor, key)),
          disabled: computed2(() => !canToggleMark(editor, key)),
          hidden: hiddenMark[key],
          onClick: () => toggleMark(editor, key)
        }
      )
    ) }),
    jsx("div", { class: "btn-group", hidden: hiddenSuperSubScriptCode, children: superSubScriptCode.map(
      (key) => jsx(
        TiptapButton,
        {
          title: markTexts[key],
          icon: markIcons[key],
          active: computed2(() => isMarkActive(editor, key)),
          disabled: computed2(() => !canToggleMark(editor, key)),
          hidden: hiddenMark[key],
          onClick: () => toggleMark(editor, key)
        }
      )
    ) }),
    jsx("div", { class: "btn-group", hidden: hiddenAlignmentAll, children: alignment.map((key) => jsx(
      TiptapButton,
      {
        title: textAlignTexts[key],
        icon: textAlignIcons[key],
        active: computed2(() => isTextAlignActive(editor, key)),
        disabled: computed2(() => !canSetTextAlign(editor, key)),
        hidden: key === "justify" ? hiddenJustify : hiddenLeftCenterRight,
        onClick: () => {
          if (isTextAlignActive(editor, key) && editor.chain().toggleTextAlign) {
            editor.chain().focus().toggleTextAlign(key).run();
          } else {
            setTextAlign(editor, key);
          }
        }
      }
    )) })
  ] });
}
__name(TiptapToolbar, "TiptapToolbar");
function isExtensionAvailable(editor, extensionNames) {
  if (!editor) return false;
  const names = Array.isArray(extensionNames) ? extensionNames : [extensionNames];
  return names.some((name) => editor.extensionManager.extensions.some((ext) => ext.name === name));
}
__name(isExtensionAvailable, "isExtensionAvailable");
var isMarkInSchema = /* @__PURE__ */ __name((markName, editor) => {
  if (!editor?.schema) return false;
  return editor.schema.spec.marks.get(markName) !== void 0;
}, "isMarkInSchema");
function isNodeTypeSelected(editor, types = []) {
  if (!editor || !editor.state.selection) return false;
  const { state } = editor;
  const { selection } = state;
  if (selection.empty) return false;
  if (selection.node?.type?.name) {
    const node = selection.node;
    return node ? types.includes(node.type.name) : false;
  }
  return false;
}
__name(isNodeTypeSelected, "isNodeTypeSelected");
var undoRedoTexts = {
  undo: HtmlContentEditorTexts.Undo,
  redo: HtmlContentEditorTexts.Redo
};
var undoRedoIcons = {
  undo: faIcon("undo"),
  redo: faIcon("redo")
};
function isEditable(editor) {
  return !!(editor && editor.isEditable);
}
__name(isEditable, "isEditable");
function canUndoRedo(editor, action) {
  if (!isEditable(editor) || isNodeTypeSelected(editor, ["image"])) return false;
  return action === "undo" ? editor.can().undo() : editor.can().redo();
}
__name(canUndoRedo, "canUndoRedo");
function execUndoRedo(editor, action) {
  if (!isEditable(editor) || !canUndoRedo(editor, action)) return false;
  const chain = editor.chain().focus();
  return action === "undo" ? chain.undo().run() : chain.redo().run();
}
__name(execUndoRedo, "execUndoRedo");
var markTexts = {
  bold: HtmlContentEditorTexts.Bold,
  italic: HtmlContentEditorTexts.Italic,
  strike: HtmlContentEditorTexts.StrikeThrough,
  code: HtmlContentEditorTexts.InlineCode,
  underline: HtmlContentEditorTexts.Underline,
  superscript: HtmlContentEditorTexts.Superscript,
  subscript: HtmlContentEditorTexts.Subscript
};
var markIcons = {
  bold: faIcon("bold"),
  italic: faIcon("italic"),
  strike: faIcon("strikethrough"),
  code: faIcon("code"),
  underline: faIcon("underline"),
  superscript: faIcon("superscript"),
  subscript: faIcon("subscript")
};
function canToggleMark(editor, type) {
  if (!isEditable(editor) || !isMarkInSchema(type, editor) || isNodeTypeSelected(editor, ["image"])) return false;
  return editor.can().toggleMark(type);
}
__name(canToggleMark, "canToggleMark");
function isMarkActive(editor, type) {
  if (!isEditable(editor)) return false;
  return editor.isActive(type);
}
__name(isMarkActive, "isMarkActive");
function toggleMark(editor, type) {
  if (!isEditable(editor) || !canToggleMark(editor, type)) return false;
  return editor.chain().focus().toggleMark(type).run();
}
__name(toggleMark, "toggleMark");
function showMark(editor, type) {
  return isEditable(editor) && isMarkInSchema(type, editor);
}
__name(showMark, "showMark");
var textAlignTexts = {
  left: HtmlContentEditorTexts.AlignLeft,
  center: HtmlContentEditorTexts.AlignCenter,
  right: HtmlContentEditorTexts.AlignRight,
  justify: HtmlContentEditorTexts.AlignJustify
};
var textAlignIcons = {
  left: faIcon("align-left"),
  center: faIcon("align-center"),
  right: faIcon("align-right"),
  justify: faIcon("align-justify")
};
function canSetTextAlign(editor, align) {
  if (!isEditable(editor)) return false;
  if (!isExtensionAvailable(editor, "textAlign") || isNodeTypeSelected(editor, ["image", "horizontalRule"])) return false;
  return editor.can().setTextAlign(align);
}
__name(canSetTextAlign, "canSetTextAlign");
function hasSetTextAlign(commands) {
  return "setTextAlign" in commands;
}
__name(hasSetTextAlign, "hasSetTextAlign");
function isTextAlignActive(editor, align) {
  if (!isEditable(editor)) return false;
  return editor.isActive({ textAlign: align });
}
__name(isTextAlignActive, "isTextAlignActive");
function setTextAlign(editor, align) {
  if (!isEditable(editor) || !canSetTextAlign(editor, align)) return false;
  const chain = editor.chain().focus();
  if (hasSetTextAlign(chain)) return chain.setTextAlign(align).run();
  return false;
}
__name(setTextAlign, "setTextAlign");
function showTextAlign(editor) {
  return isEditable(editor) && isExtensionAvailable(editor, "textAlign");
}
__name(showTextAlign, "showTextAlign");
function isTiptapContentEmpty(content) {
  if (content === null) return true;
  if (typeof content === "string") return content === "";
  if (Array.isArray(content)) return content.every((item) => isTiptapContentEmpty(item));
  if (!content.content) return true;
  return content.content.every((item) => !item.content);
}
__name(isTiptapContentEmpty, "isTiptapContentEmpty");
function getTiptapContent(editor) {
  if (isTiptapContentEmpty(editor.getJSON())) return "";
  return editor.getHTML();
}
__name(getTiptapContent, "getTiptapContent");
function getAllTiptapExtensions(tiptap) {
  return Object.entries(tiptap).filter(([k, e2]) => e2 && /[A-Z]/.test(k[0]) && e2 && (e2.type === "extension" || e2.type === "mark" || e2.type === "node")).map(([_2, e2]) => e2);
}
__name(getAllTiptapExtensions, "getAllTiptapExtensions");
function defaultTiptapFileHandlerConfig() {
  return {
    allowedMimeTypes: ["image/jpeg", "image/gif", "image/png", "image/webp"],
    onDrop: /* @__PURE__ */ __name((currentEditor, files, pos) => {
      files.forEach((file) => {
        const fileReader = new FileReader();
        fileReader.readAsDataURL(file);
        fileReader.onload = () => {
          currentEditor.chain().insertContentAt(pos, {
            type: "image",
            attrs: {
              src: fileReader.result
            }
          }).focus().run();
        };
      });
    }, "onDrop"),
    onPaste: /* @__PURE__ */ __name((currentEditor, files, htmlContent) => {
      files.forEach((file) => {
        if (htmlContent) {
          return false;
        }
        const fileReader = new FileReader();
        fileReader.readAsDataURL(file);
        fileReader.onload = () => {
          currentEditor.chain().insertContentAt(currentEditor.state.selection.anchor, {
            type: "image",
            attrs: {
              src: fileReader.result
            }
          }).focus().run();
        };
      });
    }, "onPaste")
  };
}
__name(defaultTiptapFileHandlerConfig, "defaultTiptapFileHandlerConfig");

// src/ui/editors/htmlcontenteditor.tsx
var tiptapModuleName = "@serenity-is/tiptap";
var _a63, _b46;
var HtmlContentEditor = class _HtmlContentEditor extends (_b46 = EditorWidget, _a63 = Symbol.typeInfo, _b46) {
  static {
    __name(this, "HtmlContentEditor");
  }
  static {
    this[_a63] = this.registerEditor(nsSerenity, [IStringValue, IReadOnly]);
  }
  static {
    this.tiptapModule = /* @__PURE__ */ __name(async () => {
      try {
        return await import(`${tiptapModuleName}`);
      } catch {
        return await import(resolveUrl("~/Serenity.Assets/tiptap/tiptap.bundle.js"));
      }
    }, "tiptapModule");
  }
  static createDefaultElement() {
    return document.createElement("textarea");
  }
  static {
    this.defaultOptions = {};
  }
  constructor(props) {
    super({
      editorProvider: _HtmlContentEditor.defaultEditorProvider,
      ..._HtmlContentEditor.defaultOptions,
      ...props
    });
    this.element.addClass("s-HtmlContentEditor");
    this._ckInstanceReady = false;
    let textArea = this.domNode;
    var id = textArea.getAttribute("id");
    if (isTrimmedEmpty(id)) {
      textArea.setAttribute("id", this.uniqueName);
      id = this.uniqueName;
    }
    if (this.options.cols != null)
      textArea.setAttribute("cols", "" + (this.options.cols ?? 0));
    if (this.options.rows != null)
      textArea.setAttribute("rows", "" + (this.options.rows ?? 0));
    this.addValidationRule(this.uniqueName, (input) => {
      if (input.classList.contains("required")) {
        if (!this.get_value()?.trim())
          return FormValidationTexts.Required;
      }
      return null;
    });
    if (_HtmlContentEditor.tiptapModule && this.options.editorProvider === "tiptap") {
      Promise.resolve(typeof _HtmlContentEditor.tiptapModule === "function" ? _HtmlContentEditor.tiptapModule() : _HtmlContentEditor.tiptapModule).then((tiptap) => {
        if (!tiptap?.Editor)
          return;
        this.tiptapElement = document.createElement("div");
        this.tiptapElement.id = "tiptap_" + (this.domNode?.id ?? this.uniqueName);
        this.tiptapElement.dataset.editorProvider = "tiptap";
        textArea.parentElement.insertBefore(this.tiptapElement, textArea);
        textArea.style.visibility = "hidden";
        for (const k of textArea.className.split(" ")) {
          k && this.tiptapElement.classList.add(k);
        }
        textArea.classList.add("s-offscreen");
        const extensions = this.getTiptapExtensions(tiptap).filter((ext) => ext != null).map((ext) => this.configureTiptapExtension(ext)).filter((ext) => ext != null);
        this.tiptapEditor = new tiptap.Editor({
          element: this.tiptapElement,
          content: sanitizeHtml(textArea.value),
          extensions,
          injectNonce: Config.cspNonce
        });
        const toolbar = this.createTiptapToolbar(this.tiptapEditor, this.getTiptapToolbarHidden(this.tiptapEditor));
        this.tiptapElement.prepend(toolbar);
        this.tiptapEditor.on("update", () => {
          const html = getTiptapContent(this.tiptapEditor);
          textArea.value = sanitizeHtml(html);
          Fluent.trigger(textArea, "change");
        });
        this.domNode.dataset.editorProvider = "tiptap";
      });
    } else if (this.options.editorProvider !== "tiptap") {
      _HtmlContentEditor.includeCKEditor(() => {
        LazyLoadHelper.executeOnceWhenShown(this.domNode, () => {
          if (window["CKEDITOR"]) {
            const config = this.getConfig();
            window["CKEDITOR"].replace(id, config);
            this.domNode.dataset.editorProvider = "ckeditor";
          }
        });
      });
    }
  }
  handleCKInstanceReady(x) {
    this._ckInstanceReady = true;
    x.editor?.container?.$ && (x.editor.container.$.dataset.editorProvider = "ckeditor");
    this.domNode.classList.forEach((clss) => {
      x.editor.container.$?.classList.add(clss);
    });
    this.domNode.classList.add("select2-offscreen");
    this.domNode.style.display = "block";
    x.editor.setData(this.domNode.value);
    x.editor.setReadOnly(this.get_readOnly());
  }
  handleCKEditorChange(e2) {
    e2.editor.updateElement();
    Fluent.trigger(this.domNode, "change");
    if (this.triggerKeyupEvent) {
      const validator = Validator.getInstance(this.domNode.form);
      if (validator && validator?.settings?.onkeyup) {
        validator.settings.onkeyup(this.domNode, this.triggerKeyupEvent, validator);
      }
      this.triggerKeyupEvent = null;
    }
  }
  handleCKKey(e2) {
    this.triggerKeyupEvent = e2?.data?.domEvent?.$;
  }
  getCKEditorLanguage() {
    if (!window["CKEDITOR"])
      return "en";
    var CKEDITOR = window["CKEDITOR"];
    var lang = document.documentElement.getAttribute("lang")?.trim() || "en";
    if (!!CKEDITOR.lang.languages[lang]) {
      return lang;
    }
    if (lang.indexOf(String.fromCharCode(45)) >= 0) {
      lang = lang.split(String.fromCharCode(45))[0];
    }
    if (!!CKEDITOR.lang.languages[lang]) {
      return lang;
    }
    return "en";
  }
  /** @deprecated Override and use getCKEditorConfig() */
  getConfig() {
    return this.getCKEditorConfig();
  }
  getCKEditorConfig() {
    const boundThis = bindThis(this);
    return {
      customConfig: "",
      language: this.getCKEditorLanguage(),
      bodyClass: "s-HtmlContentBody",
      versionCheck: false,
      on: {
        instanceReady: boundThis.handleCKInstanceReady,
        change: boundThis.handleCKEditorChange,
        key: boundThis.handleCKKey
      },
      toolbarGroups: [
        {
          name: "clipboard",
          groups: ["clipboard", "undo"]
        },
        {
          name: "editing",
          groups: ["find", "selection", "spellchecker"]
        },
        {
          name: "insert",
          groups: ["links", "insert", "blocks", "bidi", "list", "indent"]
        },
        {
          name: "forms",
          groups: ["forms", "mode", "document", "doctools", "others", "about", "tools"]
        },
        {
          name: "colors"
        },
        {
          name: "basicstyles",
          groups: ["basicstyles", "cleanup"]
        },
        {
          name: "align"
        },
        {
          name: "styles"
        }
      ],
      removeButtons: "SpecialChar,Anchor,Subscript,Styles",
      format_tags: "p;h1;h2;h3;pre",
      removeDialogTabs: "image:advanced;link:advanced",
      removePlugins: "uploadimage,image2",
      contentsCss: resolveUrl("~/Content/site/site.htmlcontent.css"),
      entities: false,
      entities_latin: false,
      entities_greek: false,
      autoUpdateElement: true,
      height: this.options.rows == null || this.options.rows === 0 ? null : this.options.rows * 20 + "px"
    };
  }
  getCKEditorInstance() {
    const id = this.domNode.getAttribute("id");
    return window["CKEDITOR"]?.instances?.[id];
  }
  configureTiptapExtension(extension) {
    if (!extension || !extension.name)
      return extension;
    switch (extension.name) {
      case "fileHandler":
        return extension.configure(defaultTiptapFileHandlerConfig());
      case "textAlign":
        return extension.configure({
          types: ["paragraph", "heading"],
          alignments: ["left", "center", "right", "justify"]
        });
    }
    return extension;
  }
  getTiptapExtensions(tiptap) {
    return getAllTiptapExtensions(tiptap);
  }
  createTiptapToolbar(editor, hidden) {
    return jsx(TiptapToolbar, { editor, hidden });
  }
  /** Can be overridden to hide some buttons even though they are registered in extensions */
  getTiptapToolbarHidden(editor) {
    return {};
  }
  destroy() {
    if (this.editorProvider === "ckeditor") {
      const ckInstance = this.getCKEditorInstance();
      ckInstance && ckInstance.destroy(true);
    } else if (this.tiptapEditor) {
      this.tiptapEditor.destroy();
      delete this.tiptapEditor;
    }
    super.destroy();
  }
  get_value() {
    if (this.tiptapEditor) {
      return sanitizeHtml(getTiptapContent(this.tiptapEditor));
    } else if (this.editorProvider === "ckeditor") {
      const ckInstance = this.getCKEditorInstance();
      if (this._ckInstanceReady && ckInstance) {
        return sanitizeHtml(ckInstance.getData());
      }
    }
    return sanitizeHtml(this.domNode.value);
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    value = sanitizeHtml(value ?? "");
    this.domNode.value = value;
    if (this.tiptapEditor) {
      this.tiptapEditor?.commands.setContent(value);
      return;
    }
    if (this.editorProvider === "ckeditor") {
      const ckInstance = this.getCKEditorInstance();
      if (this._ckInstanceReady && ckInstance)
        ckInstance.setData(value);
    }
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_readOnly() {
    return !!this.domNode.getAttribute("disabled");
  }
  set_readOnly(value) {
    if (this.get_readOnly() !== value) {
      if (value) {
        this.domNode.setAttribute("disabled", "disabled");
      } else {
        this.domNode.removeAttribute("disabled");
      }
      if (this.tiptapEditor) {
        this.tiptapEditor.setEditable(!value);
        return;
      }
      if (this.editorProvider !== "ckeditor") {
        const ckInstance = this.getCKEditorInstance();
        if (this._ckInstanceReady && ckInstance)
          ckInstance.setReadOnly(value);
      }
    }
  }
  static {
    this.CKEditorVer = "4.22.1";
  }
  static getCKEditorBasePath() {
    var path = _HtmlContentEditor.CKEditorBasePath;
    if (path == null) {
      if (typeof CKEDITOR_BASEPATH !== "undefined" && CKEDITOR_BASEPATH)
        path = CKEDITOR_BASEPATH;
      else
        return "https://cdnjs.cloudflare.com/ajax/libs/ckeditor/4.22.1/";
    }
    return path.endsWith("/") ? path : path + "/";
  }
  static includeCKEditor(then) {
    if (window["CKEDITOR"]) {
      return then();
    }
    var script = document.querySelector("#CKEditorScript");
    if (script) {
      return script.addEventListener("load", then);
    }
    document.head.appendChild(
      jsx(
        "script",
        {
          type: "text/javascript",
          id: "CKEditorScript",
          async: false,
          onLoad: then,
          nonce: Config.cspNonce,
          src: resolveUrl(_HtmlContentEditor.getCKEditorBasePath() + "ckeditor.js?v=" + _HtmlContentEditor.CKEditorVer)
        }
      )
    );
  }
  get editorProvider() {
    return this.domNode?.dataset?.editorProvider ?? null;
  }
};
var _a64, _b47;
var HtmlNoteContentEditor = class _HtmlNoteContentEditor extends (_b47 = HtmlContentEditor, _a64 = Symbol.typeInfo, _b47) {
  static {
    __name(this, "HtmlNoteContentEditor");
  }
  static {
    this[_a64] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super({
      editorProvider: _HtmlNoteContentEditor.defaultEditorProvider,
      ...props
    });
  }
  getCKEditorConfig() {
    var config = super.getCKEditorConfig();
    config.removeButtons += ",Cut,Copy,Paste,BulletedList,NumberedList,Indent,Outdent,SpecialChar,Subscript,Superscript,Styles,PasteText,PasteFromWord,Strike,Link,Unlink,CreatePlaceholder,Image,Table,HorizontalRule,Source,Maximize,Format,Font,FontSize,Anchor,Blockquote,CreatePlaceholder,BGColor,JustifyLeft,JustifyCenter,JustifyRight,JustifyBlock,Superscript,RemoveFormat";
    config.removePlugins = "elementspath,uploadimage,image2";
    return config;
  }
  configureTiptapExtension(extension) {
    extension = super.configureTiptapExtension(extension);
    switch (extension?.name) {
      case "starterKit":
        extension = extension.configure({
          blockquote: false,
          bulletList: false,
          code: false,
          codeBlock: false,
          heading: false,
          horizontalRule: false,
          listItem: false,
          listKeymap: false,
          link: false,
          orderedList: false,
          strike: false
        });
        break;
    }
    return extension;
  }
  getTiptapExtensions(tiptap) {
    return [tiptap.StarterKit];
  }
};
var _a65, _b48;
var HtmlReportContentEditor = class _HtmlReportContentEditor extends (_b48 = HtmlContentEditor, _a65 = Symbol.typeInfo, _b48) {
  static {
    __name(this, "HtmlReportContentEditor");
  }
  static {
    this[_a65] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super({
      editorProvider: _HtmlReportContentEditor.defaultEditorProvider,
      ...props
    });
  }
  getCKEditorConfig() {
    var config = super.getCKEditorConfig();
    config.removeButtons += ",Cut,Copy,Paste,BulletedList,NumberedList,Indent,Outdent,SpecialChar,Subscript,Superscript,Styles,PasteText,PasteFromWord,Strike,Link,Unlink,CreatePlaceholder,Image,Table,HorizontalRule,Source,Maximize,Format,Font,FontSize,Anchor,Blockquote,CreatePlaceholder,BGColor,JustifyBlock,Superscript,RemoveFormat";
    config.removePlugins = "elementspath,uploadimage,image2";
    return config;
  }
  getTiptapToolbarHidden(editor) {
    return {
      alignmentJustify: true
    };
  }
  configureTiptapExtension(extension) {
    extension = super.configureTiptapExtension(extension);
    switch (extension?.name) {
      case "starterKit":
        extension = extension.configure({
          blockquote: false,
          bulletList: false,
          code: false,
          codeBlock: false,
          heading: false,
          horizontalRule: false,
          listItem: false,
          listKeymap: false,
          link: false,
          orderedList: false,
          strike: false
        });
      case "textAlign":
        extension = extension.configure({
          alignments: ["left", "center", "right"]
        });
        break;
    }
    return extension;
  }
};

// src/ui/editors/integereditor.tsx
var _a66, _b49;
var IntegerEditor = class extends (_b49 = EditorWidget, _a66 = Symbol.typeInfo, _b49) {
  static {
    __name(this, "IntegerEditor");
  }
  static {
    this[_a66] = this.registerEditor(nsSerenity, [IDoubleValue]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("integerQ");
    this.initAutoNumeric();
  }
  destroy() {
    AutoNumeric.destroy(this.domNode);
    super.destroy();
  }
  initAutoNumeric() {
    AutoNumeric.init(this.domNode, this.getAutoNumericOptions());
  }
  getAutoNumericOptions() {
    var numericOptions = Object.assign({}, DecimalEditor.defaultAutoNumericOptions(), {
      vMin: this.options.minValue ?? (this.options.allowNegatives ? this.options.maxValue != null ? "-" + this.options.maxValue : "-2147483647" : "0"),
      vMax: this.options.maxValue ?? 2147483647,
      aSep: null
    });
    for (const key of Object.keys(this.options)) {
      if (AutoNumeric.allowedSettingKeys.has(key)) {
        numericOptions[key] = this.options[key];
      }
    }
    return numericOptions;
  }
  get_value() {
    var val;
    if (AutoNumeric.hasInstance(this.domNode)) {
      val = AutoNumeric.getValue(this.domNode);
      if (isTrimmedEmpty(val))
        return null;
      else
        return parseInt(val, 10);
    } else {
      val = this.domNode.value?.trim();
      if (!val)
        return null;
      return parseInteger(val);
    }
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (value == null || value === "")
      this.domNode.value = "";
    else if (AutoNumeric.hasInstance(this.domNode))
      AutoNumeric.setValue(this.domNode, value);
    else
      this.domNode.value = formatNumber(value);
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_isValid() {
    return !isNaN(this.get_value());
  }
};

// src/ui/editors/lookupeditor.ts
var _a67, _b50;
var LookupEditorBase = class extends (_b50 = ComboboxEditor, _a67 = Symbol.typeInfo, _b50) {
  static {
    __name(this, "LookupEditorBase");
  }
  static {
    this[_a67] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
    if (!this.hasAsyncSource()) {
      this.updateItems();
      this.lookupChangeOff = ScriptData.bindToChange("Lookup." + this.getLookupKey(), bindThis(this).updateItems);
    }
  }
  hasAsyncSource() {
    return !!this.options.async;
  }
  destroy() {
    if (this.lookupChangeOff) {
      this.lookupChangeOff();
      this.lookupChangeOff = null;
    }
    super.destroy();
  }
  getLookupKey() {
    if (this.options.lookupKey != null) {
      return this.options.lookupKey;
    }
    var key = getTypeFullName(getInstanceType(this));
    var idx = key.indexOf(".");
    if (idx >= 0) {
      key = key.substring(idx + 1);
    }
    if (key.endsWith("Editor")) {
      key = key.substring(0, key.length - 6);
    }
    return key;
  }
  getLookupAsync() {
    return getLookupAsync(this.getLookupKey());
  }
  getLookup() {
    return getLookup(this.getLookupKey());
  }
  getItems(lookup) {
    return this.filterItems(this.cascadeItems(lookup.items));
  }
  getIdField() {
    return this.lookup != null ? this.lookup.idField : super.getIdField();
  }
  getItemText(item, lookup) {
    if (lookup == null)
      return super.itemText(item);
    var textValue = item[lookup.textField];
    return textValue == null ? "" : textValue.toString();
  }
  mapItem(item) {
    return {
      id: this.itemId(item),
      text: this.getItemText(item, this.lookup),
      disabled: this.getItemDisabled(item, this.lookup),
      source: item
    };
  }
  getItemDisabled(item, lookup) {
    return super.itemDisabled(item);
  }
  updateItems() {
    if (this.hasAsyncSource())
      return;
    this.clearItems();
    this.lookup = this.getLookup();
    var items = this.getItems(this.lookup);
    for (var item of items)
      this.addItem(this.mapItem(item));
  }
  async asyncSearch(query) {
    this.lookup = await this.getLookupAsync();
    var items = this.getItems(this.lookup);
    if (query.idList != null) {
      items = items.filter((x) => query.idList.indexOf(this.itemId(x)) >= 0);
    }
    const getText = /* @__PURE__ */ __name((item) => this.getItemText(item, this.lookup), "getText");
    items = ComboboxEditor.filterByText(items, getText, query.searchTerm);
    return {
      items: items.slice(query.skip, query.take ? query.skip + query.take : items.length),
      more: query.take && items.length > 0 && items.length > query.skip + query.take
    };
  }
  getDialogTypeKey() {
    var dialogTypeKey = super.getDialogTypeKey();
    if (dialogTypeKey)
      return dialogTypeKey;
    return this.getLookupKey();
  }
  setCreateTermOnNewEntity(entity, term) {
    entity[this.getLookup().textField] = term;
  }
  editDialogDataChange() {
    reloadLookup(this.getLookupKey());
  }
};
var _a68, _b51;
var LookupEditor = class extends (_b51 = LookupEditorBase, _a68 = Symbol.typeInfo, _b51) {
  static {
    __name(this, "LookupEditor");
  }
  static {
    this[_a68] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
  }
};

// src/ui/editors/maskededitor.tsx
var _a69, _b52;
var MaskedEditor = class extends (_b52 = EditorWidget, _a69 = Symbol.typeInfo, _b52) {
  static {
    __name(this, "MaskedEditor");
  }
  static {
    this[_a69] = this.registerEditor(nsSerenity, [IStringValue]);
  }
  static createDefaultElement() {
    return jsx("input", { type: "text" });
  }
  constructor(props) {
    super(props);
    let $2 = getjQuery();
    if ($2?.fn?.mask) {
      $2(this.domNode).mask(this.options.mask || "", {
        placeholder: this.options.placeholder ?? "_"
      });
    } else {
      notifyError("MaskedInput requires jQuery masked input plugin (jquery.maskedinput.js, https://www.npmjs.com/package/jquery.maskedinput) to be loaded in the page along with jQuery!");
    }
  }
  get value() {
    Fluent.trigger(this.domNode, "blur.mask");
    return this.domNode.value;
  }
  get_value() {
    return this.value;
  }
  set value(value) {
    this.domNode.value = value;
  }
  set_value(value) {
    this.value = value;
  }
};

// src/ui/editors/passwordeditor.tsx
var _a70, _b53;
var PasswordEditor = class extends (_b53 = StringEditor, _a70 = Symbol.typeInfo, _b53) {
  static {
    __name(this, "PasswordEditor");
  }
  static {
    this[_a70] = this.registerEditor(nsSerenity);
  }
  static createDefaultElement() {
    return jsx("input", { type: "password" });
  }
};

// src/ui/editors/radiobuttoneditor.tsx
var _a71, _b54;
var RadioButtonEditor = class extends (_b54 = EditorWidget, _a71 = Symbol.typeInfo, _b54) {
  static {
    __name(this, "RadioButtonEditor");
  }
  static {
    this[_a71] = this.registerEditor(nsSerenity, [IStringValue, IReadOnly]);
  }
  constructor(props) {
    super(props);
    if (!this.options.enumKey && this.options.enumType == null && !this.options.lookupKey) {
      return;
    }
    if (this.options.lookupKey) {
      var lookup = getLookup(this.options.lookupKey);
      for (var item of lookup.items) {
        var textValue = item[lookup.textField];
        var text2 = textValue == null ? "" : textValue.toString();
        var idValue = item[lookup.idField];
        var id = idValue == null ? "" : idValue.toString();
        this.addRadio(id, text2);
      }
    } else {
      var enumType = this.options.enumType || EnumTypeRegistry.getOrLoad(this.options.enumKey);
      const then = /* @__PURE__ */ __name((enumType2) => {
        var enumKey = this.options.enumKey;
        if (enumKey == null && enumType2 != null) {
          enumKey = getCustomAttribute(enumType2, EnumKeyAttribute, false)?.value ?? getTypeFullName(enumType2);
        }
        var values = Enum.getValues(enumType2);
        for (var x of values) {
          var name = Enum.toString(enumType2, x);
          this.addRadio(x.toString(), localText("Enums." + enumKey + "." + name, name));
        }
      }, "then");
      if (isPromiseLike(enumType))
        enumType.then(then);
      else
        then(enumType);
    }
  }
  addRadio(value, text2) {
    this.domNode.appendChild(jsx("label", { children: [
      jsx("input", { type: "radio", name: this.uniqueName, id: this.uniqueName + "_" + value, value }),
      text2
    ] }));
  }
  get_value() {
    return this.element.findFirst("input:checked").val();
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (value !== this.get_value()) {
      var inputs = this.element.findAll("input");
      var checks = inputs.filter((x) => x.checked);
      if (checks.length > 0) {
        checks[0].checked = false;
      }
      if (value) {
        checks = inputs.filter((x) => x.value === value);
        if (checks.length > 0) {
          checks[0].checked = true;
        }
      }
    }
  }
  set value(v2) {
    this.set_value(v2);
  }
  get_readOnly() {
    return this.domNode.getAttribute("disabled") != null;
  }
  set_readOnly(value) {
    if (this.get_readOnly() !== !!value) {
      this.element.attr("disabled", value ? "disabled" : null).findEach("input[type=radio]", (x) => x.attr("disabled", value ? "disabled" : null));
    }
  }
};

// src/ui/editors/recaptcha.ts
var _a72, _b55;
var Recaptcha = class extends (_b55 = EditorWidget, _a72 = Symbol.typeInfo, _b55) {
  static {
    __name(this, "Recaptcha");
  }
  static {
    this[_a72] = this.registerEditor(nsSerenity, [IStringValue]);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("g-recaptcha");
    this.domNode.setAttribute("data-sitekey", this.options.siteKey);
    if (!!(window["grecaptcha"] == null && !document.querySelector("script#RecaptchaInclude"))) {
      var src = "https://www.google.com/recaptcha/api.js";
      var lng = this.options.language;
      if (lng == null) {
        lng = document.documentElement.getAttribute("lang") ?? "";
      }
      src += "?hl=" + lng;
      var script = document.createElement("script");
      script.setAttribute("id", "RecaptchaInclude");
      script.setAttribute("src", src);
      document.head.append(script);
    }
    var valInput = document.createElement("input");
    Fluent(valInput).insertBefore(this.domNode);
    valInput.setAttribute("id", this.uniqueName + "_validate");
    valInput.value = "x";
    valInput.style.visibility = "hidden";
    valInput.style.width = "0px";
    valInput.style.height = "0px";
    valInput.style.padding = "0px";
    addValidationRule(valInput, (e2) => {
      if (!this.get_value()) {
        return FormValidationTexts.Required;
      }
      return null;
    }, this.uniqueName);
  }
  get_value() {
    return this.domNode.querySelector(".g-recaptcha-response").value;
  }
  set_value(value) {
  }
};

// src/ui/editors/servicelookupeditor.ts
var _a73, _b56;
var ServiceLookupEditorBase = class extends (_b56 = ComboboxEditor, _a73 = Symbol.typeInfo, _b56) {
  static {
    __name(this, "ServiceLookupEditorBase");
  }
  static {
    this[_a73] = this.registerEditor(nsSerenity);
  }
  getDialogTypeKey() {
    var dialogTypeKey = super.getDialogTypeKey();
    if (dialogTypeKey)
      return dialogTypeKey;
    var service = this.getService();
    if (service.startsWith("~/Services/"))
      service = service.substring("~/Services/".length);
    if (service.split("/").length == 3)
      service = service.substring(0, service.lastIndexOf("/"));
    return service.replace("/", ".");
  }
  getService() {
    return this.options.service;
  }
  getServiceUrl() {
    var url = this.getService();
    if (url == null)
      throw new Error("ServiceLookupEditor requires 'service' option to be configured!");
    return resolveServiceUrl(url);
  }
  getIncludeColumns() {
    var include = this.options.includeColumns || [];
    var idField = this.getIdField();
    if (idField && include.indexOf(idField) < 0)
      include.push(idField);
    var textField = this.getTextField();
    if (textField && include.indexOf(textField) < 0)
      include.push(textField);
    return include;
  }
  getSort() {
    return this.options.sort || (this.getTextField() ? [this.getTextField()] : null);
  }
  getCascadeCriteria() {
    var val = this.get_cascadeValue();
    if (val == null || val === "") {
      if (this.get_cascadeField()) {
        return ["0", "=", "1"];
      }
      return null;
    }
    var fld = this.get_cascadeField();
    return Criteria(fld).eq(val);
  }
  getFilterCriteria() {
    var val = this.get_filterValue();
    if (val == null || val === "") {
      return null;
    }
    var fld = this.get_filterField();
    return [[fld], "=", val];
  }
  getIdListCriteria(idList) {
    if (idList == null)
      return null;
    if (idList.length == 0)
      return ["0", "=", "1"];
    var idField = this.getIdField();
    if (idField == null)
      throw new Error("ServiceLookupEditor requires 'idField' option to be configured!");
    return Criteria(idField).in(idList);
  }
  getCriteria(query) {
    return Criteria.and(
      Criteria.and(this.getIdListCriteria(query.idList), this.options.criteria),
      Criteria.and(this.getCascadeCriteria(), this.getFilterCriteria())
    );
  }
  getListRequest(query) {
    var request = {};
    if (query.searchTerm)
      request.ContainsText = query.searchTerm;
    request.Sort = this.getSort();
    request.ColumnSelection = this.options.columnSelection || 1 /* KeyOnly */;
    request.IncludeColumns = this.getIncludeColumns();
    request.ExcludeColumns = this.options.excludeColumns;
    request.ContainsField = this.options.containsField;
    request.EqualityFilter = this.options.equalityFilter;
    request.Criteria = this.getCriteria(query);
    request.Skip = query.skip || 0;
    request.Take = query.take ? query.checkMore ? query.take + 1 : query.take : 0;
    request.IncludeDeleted = this.options.includeDeleted;
    request.ExcludeTotalCount = true;
    return request;
  }
  getServiceCallOptions(query) {
    return {
      blockUI: false,
      service: this.getServiceUrl(),
      request: this.getListRequest(query),
      signal: query.signal
    };
  }
  hasAsyncSource() {
    return true;
  }
  canSearch(byId) {
    if (!byId && this.get_cascadeField()) {
      var val = this.get_cascadeValue();
      if (val == null || val === "")
        return false;
    }
    return true;
  }
  async asyncSearch(query) {
    if (!this.canSearch(query.idList != null)) {
      return Promise.resolve({
        items: [],
        more: false
      });
    }
    var opt = this.getServiceCallOptions(query);
    var response = await serviceCall(opt);
    var itemsPlus1 = response.Entities || [];
    var items = itemsPlus1;
    if (query.take && query.checkMore)
      items = items.slice(0, query.take);
    return {
      items,
      more: query.checkMore && query.take && itemsPlus1.length > query.take
    };
  }
};
var _a74, _b57;
var ServiceLookupEditor = class extends (_b57 = ServiceLookupEditorBase, _a74 = Symbol.typeInfo, _b57) {
  static {
    __name(this, "ServiceLookupEditor");
  }
  static {
    this[_a74] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
  }
};

// src/ui/editors/textareaeditor.ts
var _a75, _b58;
var TextAreaEditor = class extends (_b58 = EditorWidget, _a75 = Symbol.typeInfo, _b58) {
  static {
    __name(this, "TextAreaEditor");
  }
  static {
    this[_a75] = this.registerEditor(nsSerenity, [IStringValue]);
  }
  static createDefaultElement() {
    return document.createElement("textarea");
  }
  constructor(props) {
    super(props);
    let input = this.element;
    if (this.options.cols !== 0) {
      input.attr("cols", this.options.cols ?? 80);
    }
    if (this.options.rows !== 0) {
      input.attr("rows", this.options.rows ?? 6);
    }
  }
  get value() {
    return this.element.val();
  }
  get_value() {
    return this.value;
  }
  set value(value) {
    this.element.val(value ?? "");
  }
  set_value(value) {
    this.value = value;
  }
};

// src/ui/editors/timeeditor.ts
var _a76, _b59;
var TimeEditorBase = class extends (_b59 = EditorWidget, _a76 = Symbol.typeInfo, _b59) {
  static {
    __name(this, "TimeEditorBase");
  }
  static {
    this[_a76] = this.registerEditor(nsSerenity);
  }
  static createDefaultElement() {
    return document.createElement("select");
  }
  constructor(props) {
    super(props);
    let input = this.element;
    input.addClass("editor hour");
    if (!this.options.noEmptyOption) {
      addOption(input, "", "--");
    }
    for (let h2 = this.options.startHour || 0; h2 <= (this.options.endHour || 23); h2++) {
      addOption(input, "" + h2, zeroPad(h2, 2));
    }
    const select = document.createElement("select");
    select.classList.add("editor", "minute");
    this.minutes = Fluent(select).insertAfter(input);
    this.minutes.on("change", () => Fluent.trigger(this.domNode, "change"));
    for (var m = 0; m <= 59; m += this.options.intervalMinutes || 5) {
      addOption(this.minutes, "" + m, zeroPad(m, 2));
    }
  }
  get hour() {
    return toId(this.domNode.value);
  }
  get minute() {
    return toId(this.minutes.val());
  }
  get_readOnly() {
    return this.domNode.classList.contains("readonly");
  }
  set_readOnly(value) {
    if (value !== this.get_readOnly()) {
      setElementReadOnly([this.domNode, this.minutes.getNode()], value);
    }
  }
  /** Returns value in HH:mm format */
  get hourAndMin() {
    var hour = this.hour;
    var minute = this.minute;
    if (hour == null || minute == null) {
      return null;
    }
    return `${zeroPad(hour, 2)}:${zeroPad(minute, 2)}`;
  }
  /** Sets value in HH:mm format */
  set hourAndMin(value) {
    if (value == null || value === "") {
      if (this.options.noEmptyOption) {
        this.domNode.value = this.options.startHour;
        this.minutes.val("0");
      } else {
        this.domNode.value = "";
        this.minutes.val("0");
      }
      return;
    }
    var parts = value.split(":");
    this.domNode.value = "" + parseInt(parts[0], 10);
    this.minutes.val("" + parseInt(parts[1], 10));
  }
};
var _a77, _b60;
var TimeEditor = class extends (_b60 = TimeEditorBase, _a77 = Symbol.typeInfo, _b60) {
  static {
    __name(this, "TimeEditor");
  }
  static {
    this[_a77] = this.registerEditor(nsSerenity, [IDoubleValue, IReadOnly]);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("s-TimeEditor");
    this.minutes.addClass("s-TimeEditor");
  }
  get value() {
    var hour = this.hour;
    var minute = this.minute;
    if (hour == null || minute == null) {
      return null;
    }
    return (hour * 60 + minute) * (this.options.multiplier || 1);
    ;
  }
  get_value() {
    return this.value;
  }
  set value(value) {
    if (value == null || value === "" || isNaN(value)) {
      if (this.options.noEmptyOption) {
        this.domNode.value = this.options.startHour;
        this.minutes.val("0");
      } else {
        this.domNode.value = "";
        this.minutes.val("0");
      }
    } else {
      value /= this.options.multiplier || 1;
      var hour = Math.floor(value / 60);
      this.domNode.value = "" + hour;
      this.minutes.val("" + value % 60);
    }
  }
  set_value(value) {
    this.value = value;
  }
};
var _a78, _b61;
var TimeSpanEditor = class extends (_b61 = TimeEditorBase, _a78 = Symbol.typeInfo, _b61) {
  static {
    __name(this, "TimeSpanEditor");
  }
  static {
    this[_a78] = this.registerEditor(nsSerenity, [IStringValue, IReadOnly]);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("s-TimeSpanEditor");
    this.minutes.addClass("s-TimeSpanEditor");
  }
  get_value() {
    return this.value;
  }
  set_value(value) {
    this.value = value;
  }
  get value() {
    return this.hourAndMin;
  }
  set value(value) {
    this.hourAndMin = value;
  }
};

// src/ui/helpers/uploadhelper.tsx
var UploadHelper;
((UploadHelper2) => {
  function addUploadInput(options) {
    let container = isArrayLike(options.container) ? options.container[0] : options.container;
    let progress = Fluent(isArrayLike(options.progress) ? options.progress[0] : options.progress);
    var button = container.closest(".tool-button") ?? container.closest("button") ?? container;
    button.classList.add("fileinput-button");
    var uploadUrl = options.uploadUrl || "~/File/TemporaryUpload";
    if (options.uploadIntent) {
      if (uploadUrl.indexOf("?") < 0)
        uploadUrl += "?";
      else
        uploadUrl += "&";
      uploadUrl += "uploadIntent=";
      uploadUrl += encodeURIComponent(options.uploadIntent);
    }
    var uploadInput = Fluent(jsx("input", { type: "file", name: options.inputName + "[]", "data-url": resolveUrl(uploadUrl), multiple: !!options.allowMultiple })).appendTo(container);
    const setProgress = /* @__PURE__ */ __name((percent) => {
      let bar = progress.children()[0];
      bar && (bar.style.width = (percent ?? 0).toString() + "%");
    }, "setProgress");
    var uploader = new Uploader({
      batchSize: 1,
      batchSuccess: /* @__PURE__ */ __name((data) => {
        const response = data.response ?? {};
        if (response?.Error) {
          notifyError(response.Error.Message);
          return;
        }
        options.fileDone?.(response, data.batch?.filePaths?.[0], data);
      }, "batchSuccess"),
      input: uploadInput.getNode(),
      dropZone: options.zone,
      batchStart: /* @__PURE__ */ __name(() => {
        blockUI(null);
        progress.show();
        setProgress(0);
      }, "batchStart"),
      batchStop: /* @__PURE__ */ __name(() => {
        blockUndo();
        setProgress(100);
        progress.hide();
      }, "batchStop"),
      batchProgress: /* @__PURE__ */ __name((data) => {
        if (typeof data.loaded == "number" && data.total > 0) {
          var percent = data.loaded / data.total * 100;
          setProgress(percent);
        }
      }, "batchProgress")
    });
    return uploadInput;
  }
  UploadHelper2.addUploadInput = addUploadInput;
  __name(addUploadInput, "addUploadInput");
  function checkImageConstraints(file, opt) {
    if (!file.IsImage && !opt.allowNonImage) {
      notifyError(FileUploadTexts.NotAnImageFile);
      return false;
    }
    if (opt.minSize > 0 && file.Size < opt.minSize) {
      notifyError(stringFormat(
        FileUploadTexts.UploadFileTooSmall,
        fileSizeDisplay(opt.minSize)
      ));
      return false;
    }
    if (opt.maxSize > 0 && file.Size > opt.maxSize) {
      notifyError(stringFormat(
        FileUploadTexts.UploadFileTooBig,
        fileSizeDisplay(opt.maxSize)
      ));
      return false;
    }
    if (!file.IsImage) {
      return true;
    }
    if (opt.minWidth > 0 && file.Width < opt.minWidth) {
      notifyError(stringFormat(FileUploadTexts.MinWidth, opt.minWidth));
      return false;
    }
    if (opt.maxWidth > 0 && file.Width > opt.maxWidth) {
      notifyError(stringFormat(FileUploadTexts.MaxWidth, opt.maxWidth));
      return false;
    }
    if (opt.minHeight > 0 && file.Height < opt.minHeight) {
      notifyError(stringFormat(FileUploadTexts.MinHeight, opt.minHeight));
      return false;
    }
    if (opt.maxHeight > 0 && file.Height > opt.maxHeight) {
      notifyError(stringFormat(FileUploadTexts.MaxHeight, opt.maxHeight));
      return false;
    }
    return true;
  }
  UploadHelper2.checkImageConstraints = checkImageConstraints;
  __name(checkImageConstraints, "checkImageConstraints");
  function fileNameSizeDisplay(name, bytes) {
    return name + " (" + fileSizeDisplay(bytes) + ")";
  }
  UploadHelper2.fileNameSizeDisplay = fileNameSizeDisplay;
  __name(fileNameSizeDisplay, "fileNameSizeDisplay");
  function fileSizeDisplay(bytes) {
    var byteSize = round(bytes * 100 / 1024) * 0.01;
    var suffix = "KB";
    if (byteSize >= 1024) {
      byteSize = round(byteSize * 100 / 1024) * 0.01;
      suffix = "MB";
    }
    var sizeParts = byteSize.toString().split(String.fromCharCode(46));
    var value;
    if (sizeParts.length > 1) {
      value = sizeParts[0] + "." + sizeParts[1].substring(0, 2);
    } else {
      value = sizeParts[0];
    }
    return value + " " + suffix;
  }
  UploadHelper2.fileSizeDisplay = fileSizeDisplay;
  __name(fileSizeDisplay, "fileSizeDisplay");
  function hasImageExtension(filename) {
    if (!filename) {
      return false;
    }
    filename = filename.toLowerCase();
    return filename.endsWith(".jpg") || filename.endsWith(".jpeg") || filename.endsWith(".gif") || filename.endsWith(".png") || filename.endsWith(".webp");
  }
  UploadHelper2.hasImageExtension = hasImageExtension;
  __name(hasImageExtension, "hasImageExtension");
  function thumbFileName(filename) {
    filename = filename ?? "";
    var idx = filename.lastIndexOf(".");
    if (idx >= 0) {
      filename = filename.substring(0, idx);
    }
    return filename + "_t.jpg";
  }
  UploadHelper2.thumbFileName = thumbFileName;
  __name(thumbFileName, "thumbFileName");
  function dbFileUrl(filename) {
    filename = replaceAll(filename ?? "", "\\", "/");
    return resolveUrl("~/upload/") + filename;
  }
  UploadHelper2.dbFileUrl = dbFileUrl;
  __name(dbFileUrl, "dbFileUrl");
  function lightbox(link) {
    link = isArrayLike(link) ? link[0] : link;
    if (!link)
      return;
    const glightbox = globalThis.GLightbox;
    if (glightbox) {
      Fluent.on(link, "click", (e2) => {
        e2.preventDefault();
        const lightbox2 = glightbox({
          elements: [
            {
              href: link.getAttribute("href") || "",
              title: link.getAttribute("title") || ""
            }
          ]
        });
        lightbox2.on("close", () => lightbox2.destroy());
        lightbox2.open();
      });
      return;
    }
    const simpleLightbox = globalThis.SimpleLightbox;
    if (simpleLightbox) {
      simpleLightbox([link], {
        fileExt: null,
        history: false,
        overlayOpacity: 1
      });
      return;
    }
    let $2 = getjQuery();
    if (!$2)
      return;
    $2(link).colorbox?.({
      current: htmlEncode(FileUploadTexts.ColorboxCurrent),
      previous: htmlEncode(FileUploadTexts.ColorboxPrior),
      next: htmlEncode(FileUploadTexts.ColorboxNext),
      close: htmlEncode(FileUploadTexts.ColorboxClose)
    });
  }
  UploadHelper2.lightbox = lightbox;
  __name(lightbox, "lightbox");
  UploadHelper2.colorBox = lightbox;
  function populateFileSymbols(c2, items, displayOriginalName, urlPrefix) {
    let container = isArrayLike(c2) ? c2[0] : c2;
    if (!container)
      return;
    items = items || [];
    container.innerHTML = "";
    for (var index = 0; index < items.length; index++) {
      const item = items[index];
      const isImage = hasImageExtension(item.Filename);
      const originalName = item.OriginalName ?? "";
      let fileName = item.Filename;
      if (urlPrefix != null && fileName != null && !fileName.startsWith("temporary/")) {
        fileName = urlPrefix + fileName;
      }
      const thumb = jsx("a", { class: "thumb", href: dbFileUrl(fileName), target: "_blank" });
      if (originalName) {
        thumb.title = originalName;
      }
      if (isImage) {
        thumb.style.backgroundImage = "url('" + dbFileUrl(thumbFileName(item.Filename)) + "')";
        lightbox(thumb);
      }
      container.appendChild(
        jsx("li", { class: ["file-item", isImage ? "file-image" : "file-binary"], "data-index": index, children: [
          thumb,
          displayOriginalName && jsx("div", { class: "filename", title: originalName, children: originalName })
        ] })
      );
    }
  }
  UploadHelper2.populateFileSymbols = populateFileSymbols;
  __name(populateFileSymbols, "populateFileSymbols");
})(UploadHelper || (UploadHelper = {}));

// src/ui/editors/uploadeditors.tsx
var _a79, _b62;
var FileUploadEditor = class extends (_b62 = EditorWidget, _a79 = Symbol.typeInfo, _b62) {
  static {
    __name(this, "FileUploadEditor");
  }
  static {
    this[_a79] = this.registerEditor(nsSerenity, [IReadOnly, IGetEditValue, ISetEditValue, IValidateRequired]);
  }
  constructor(props) {
    super(props);
    if (!this.options || this.options.allowNonImage == null)
      this.options.allowNonImage = true;
    this.domNode.classList.add("s-FileUploadEditor");
    if (!this.options.originalNameProperty)
      this.domNode.classList.add("hide-original-name");
    this.toolbar = new Toolbar({
      buttons: this.getToolButtons(),
      element: /* @__PURE__ */ __name((el) => this.domNode.appendChild(el), "element")
    }).init();
    this.progress = this.toolbar.domNode.appendChild(jsx("div", { class: "upload-progress", children: jsx("div", {}) }));
    var uio = this.getUploadInputOptions();
    this.uploadInput = UploadHelper.addUploadInput(uio);
    if (this.options.readOnly)
      this.set_readOnly(true);
    this.fileSymbols = this.domNode.appendChild(jsx("ul", { class: "file-items" }));
    if (!this.domNode.id) {
      this.domNode.id = this.uniqueName;
    }
    this.hiddenInput = this.domNode.appendChild(jsx(
      "input",
      {
        class: "s-offscreen",
        type: "text",
        name: this.uniqueName + "_Validator",
        "data-vx-highlight": this.domNode.id
      }
    ));
    this.updateInterface();
  }
  getUploadInputOptions() {
    return {
      container: this.toolbar.findButton("add-file-button"),
      zone: this.domNode,
      inputName: this.uniqueName,
      progress: this.progress,
      uploadIntent: this.options.uploadIntent,
      uploadUrl: this.options.uploadUrl,
      fileDone: /* @__PURE__ */ __name((response, name, data) => {
        if (!UploadHelper.checkImageConstraints(response, this.options)) {
          return;
        }
        var newEntity = {
          OriginalName: name,
          Filename: response.TemporaryFile
        };
        this.entity = newEntity;
        this.populate();
        this.updateInterface();
        ValidationHelper.validateElement(this.hiddenInput);
      }, "fileDone")
    };
  }
  addFileButtonText() {
    return FileUploadTexts.AddFileButton;
  }
  getToolButtons() {
    return [
      {
        title: this.addFileButtonText(),
        action: "add-file",
        cssClass: "add-file-button",
        onClick: /* @__PURE__ */ __name(function() {
        }, "onClick")
      },
      {
        title: "",
        hint: FileUploadTexts.DeleteButtonHint,
        action: "delete",
        cssClass: "delete-button",
        onClick: /* @__PURE__ */ __name(() => {
          this.entity = null;
          this.populate();
          this.updateInterface();
          ValidationHelper.validateElement(this.hiddenInput);
        }, "onClick")
      }
    ];
  }
  populate() {
    var displayOriginalName = this.options.displayFileName || !isTrimmedEmpty(this.options.originalNameProperty);
    if (this.entity == null) {
      UploadHelper.populateFileSymbols(
        this.fileSymbols,
        null,
        displayOriginalName,
        this.options.urlPrefix
      );
    } else {
      UploadHelper.populateFileSymbols(
        this.fileSymbols,
        [this.entity],
        displayOriginalName,
        this.options.urlPrefix
      );
    }
    this.hiddenInput.value = (this.get_value() || {}).Filename?.trim() || null;
  }
  updateInterface() {
    var addButton = this.toolbar.findButton("add-file-button");
    var delButton = this.toolbar.findButton("delete-button");
    addButton.toggleClass("disabled", this.get_readOnly());
    delButton.toggleClass("disabled", this.get_readOnly() || this.entity == null);
  }
  get_readOnly() {
    return this.uploadInput.attr("disabled") != null;
  }
  set_readOnly(value) {
    if (this.get_readOnly() !== value) {
      let $2 = getjQuery();
      if (value) {
        this.uploadInput.attr("disabled", "disabled");
        try {
          $2 && $2(this.uploadInput).fileupload?.("disable");
        } catch {
        }
      } else {
        this.uploadInput.removeAttr("disabled");
        try {
          $2 && $2(this.uploadInput).fileupload?.("enable");
        } catch {
        }
      }
      this.updateInterface();
    }
  }
  get_required() {
    return this.hiddenInput.classList.contains("required");
  }
  set_required(value) {
    this.hiddenInput.classList.toggle("required", !!value);
  }
  get_value() {
    if (this.entity == null) {
      return null;
    }
    var copy = Object.assign(/* @__PURE__ */ Object.create(null), this.entity);
    return copy;
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    if (typeof value === "string") {
      var stringValue = value.trim();
      if (stringValue) {
        var idx = stringValue.indexOf("/");
        if (idx < 0)
          idx = stringValue.indexOf("\\");
        value = {
          Filename: value,
          OriginalName: stringValue.substring(idx + 1)
        };
      } else
        value = null;
    } else if (!value?.Filename?.trim())
      value = null;
    if (value != null) {
      if (value.Filename == null) {
        this.entity = null;
      } else {
        this.entity = Object.assign(/* @__PURE__ */ Object.create(null), value);
      }
    } else {
      this.entity = null;
    }
    this.populate();
    ValidationHelper.validateElement(this.hiddenInput);
    this.updateInterface();
  }
  set value(v2) {
    this.set_value(v2);
  }
  getEditValue(property, target) {
    target[property.name] = this.entity?.Filename?.trim() || null;
  }
  setEditValue(source, property) {
    var value = {};
    value.Filename = source[property.name];
    if (!this.options.originalNameProperty) {
      if (this.options.displayFileName) {
        var s2 = value.Filename ?? "";
        var idx = replaceAll(s2, "\\", "/").lastIndexOf("/");
        if (idx >= 0) {
          value.OriginalName = s2.substring(idx + 1);
        } else {
          value.OriginalName = s2;
        }
      }
    } else {
      value.OriginalName = source[this.options.originalNameProperty];
    }
    this.set_value(value);
  }
};
var _a80, _b63;
var ImageUploadEditor = class extends (_b63 = FileUploadEditor, _a80 = Symbol.typeInfo, _b63) {
  static {
    __name(this, "ImageUploadEditor");
  }
  static {
    this[_a80] = this.registerEditor(nsSerenity);
  }
  constructor(props) {
    super(props);
    if (this.options.allowNonImage == null)
      this.options.allowNonImage = false;
    this.domNode.classList.add("s-ImageUploadEditor'");
  }
};
var _a81, _b64;
var MultipleFileUploadEditor = class extends (_b64 = EditorWidget, _a81 = Symbol.typeInfo, _b64) {
  static {
    __name(this, "MultipleFileUploadEditor");
  }
  static {
    this[_a81] = this.registerEditor(nsSerenity, [IReadOnly, IGetEditValue, ISetEditValue, IValidateRequired]);
  }
  constructor(props) {
    super(props);
    this.entities = [];
    this.domNode.classList.add("s-MultipleFileUploadEditor");
    this.toolbar = new Toolbar({
      buttons: this.getToolButtons(),
      element: /* @__PURE__ */ __name((el) => this.domNode.append(el), "element")
    }).init();
    this.progress = this.toolbar.domNode.appendChild(jsx("div", { class: "upload-progress", children: jsx("div", {}) }));
    this.uploadInput = UploadHelper.addUploadInput(this.getUploadInputOptions());
    this.fileSymbols = this.domNode.appendChild(jsx("ul", { class: "file-items" }));
    if (!this.domNode.id) {
      this.domNode.id = this.uniqueName;
    }
    this.hiddenInput = jsx("input", { class: "s-offscreen", type: "text", name: this.uniqueName + "_Validator", "data-vx-highlight": this.domNode.id, multiple: true });
    this.updateInterface();
  }
  getUploadInputOptions() {
    var addFileButton = this.toolbar.findButton("add-file-button");
    return {
      container: addFileButton,
      zone: this.domNode,
      inputName: this.uniqueName,
      progress: this.progress,
      uploadIntent: this.options.uploadIntent,
      uploadUrl: this.options.uploadUrl,
      allowMultiple: true,
      fileDone: /* @__PURE__ */ __name((response, name) => {
        if (!UploadHelper.checkImageConstraints(response, this.options)) {
          return;
        }
        var newEntity = { OriginalName: name, Filename: response.TemporaryFile };
        this.entities.push(newEntity);
        this.populate();
        ValidationHelper.validateElement(this.hiddenInput);
        this.updateInterface();
      }, "fileDone")
    };
  }
  addFileButtonText() {
    return FileUploadTexts.AddFileButton;
  }
  getToolButtons() {
    return [{
      title: this.addFileButtonText(),
      action: "add-file",
      cssClass: "add-file-button",
      onClick: /* @__PURE__ */ __name(function() {
      }, "onClick")
    }];
  }
  populate() {
    UploadHelper.populateFileSymbols(
      this.fileSymbols,
      this.entities,
      true,
      this.options.urlPrefix
    );
    this.fileSymbols.childNodes.forEach((e2, i2) => {
      var x = i2;
      Fluent(jsx("a", { class: "delete" })).appendTo(Fluent(e2).children().find((x2) => x2.matches(".filename"))).on("click", (ev) => {
        ev.preventDefault();
        this.entities.splice(x, 1);
        this.populate();
        ValidationHelper.validateElement(this.hiddenInput);
      });
    });
    this.hiddenInput.value = this.get_value()[0]?.Filename || null;
  }
  updateInterface() {
    var addButton = this.toolbar.findButton("add-file-button");
    addButton.toggleClass("disabled", this.get_readOnly());
    this.fileSymbols.querySelectorAll("a.delete").forEach((x) => Fluent(x).toggle(!this.get_readOnly()));
  }
  get_readOnly() {
    return this.uploadInput.attr("disabled") != null;
  }
  set_readOnly(value) {
    if (this.get_readOnly() !== value) {
      let $2 = getjQuery();
      if (value) {
        this.uploadInput.attr("disabled", "disabled");
        try {
          $2 && $2(this.uploadInput).fileupload?.("disable");
        } catch {
        }
      } else {
        this.uploadInput.removeAttr("disabled");
        try {
          $2 && $2(this.uploadInput)?.fileupload("enable");
        } catch {
        }
      }
      this.updateInterface();
    }
  }
  get_required() {
    return this.hiddenInput.classList.contains("required");
  }
  set_required(value) {
    this.hiddenInput && this.hiddenInput.classList.toggle("required", !!value);
  }
  get_value() {
    return this.entities.map(function(x) {
      return Object.assign(/* @__PURE__ */ Object.create(null), x);
    });
  }
  get value() {
    return this.get_value();
  }
  set_value(value) {
    this.entities = (value || []).map(function(x) {
      return Object.assign(/* @__PURE__ */ Object.create(null), x);
    });
    this.populate();
    this.updateInterface();
  }
  set value(v2) {
    this.set_value(v2);
  }
  getEditValue(property, target) {
    if (this.jsonEncodeValue) {
      target[property.name] = JSON.stringify(this.get_value());
    } else {
      target[property.name] = this.get_value();
    }
  }
  setEditValue(source, property) {
    var val = source[property.name];
    if (typeof val == "string") {
      var json = val.trim();
      if (json.startsWith("[") && json.endsWith("]")) {
        this.set_value(JSON.parse(json));
      } else {
        this.set_value([{
          Filename: json,
          OriginalName: "UnknownFile"
        }]);
      }
    } else {
      this.set_value(val);
    }
  }
  get jsonEncodeValue() {
    return this.options.jsonEncodeValue;
  }
  set jsonEncodeValue(value) {
    this.options.jsonEncodeValue = value;
  }
};
var _a82, _b65;
var MultipleImageUploadEditor = class extends (_b65 = MultipleFileUploadEditor, _a82 = Symbol.typeInfo, _b65) {
  static {
    __name(this, "MultipleImageUploadEditor");
  }
  static {
    this[_a82] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("s-MultipleImageUploadEditor'");
  }
};

// src/ui/editors/urleditor.ts
var _a83, _b66;
var URLEditor = class extends (_b66 = StringEditor, _a83 = Symbol.typeInfo, _b66) {
  static {
    __name(this, "URLEditor");
  }
  static {
    this[_a83] = this.registerEditor(nsSerenity, [IStringValue]);
  }
  constructor(props) {
    super(props);
    this.domNode.classList.add("url");
    this.domNode.setAttribute("title", "URL should be entered in format: 'http://www.site.com/page'.");
    Fluent.on(this.domNode, "blur." + this.uniqueName, (e2) => {
      var validator = ValidationHelper.getValidator(this.domNode);
      if (validator == null)
        return;
      if (!this.domNode.classList.contains("error"))
        return;
      var value = this.domNode.value?.trim();
      if (!value)
        return;
      value = "http://" + value;
      if (Validator.methods.url(value, this.domNode)) {
        this.domNode.value = value;
        validator.element(this.domNode);
      }
    });
  }
};

// src/ui/filtering/decimalfiltering.ts
var _a84, _b67;
var DecimalFiltering = class extends (_b67 = BaseEditorFiltering, _a84 = Symbol.typeInfo, _b67) {
  static {
    __name(this, "DecimalFiltering");
  }
  static {
    this[_a84] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(DecimalEditor);
  }
  getOperators() {
    return this.appendNullableOperators(
      this.appendComparisonOperators([])
    );
  }
};

// src/ui/filtering/editorfiltering.ts
var _a85, _b68;
var EditorFiltering = class extends (_b68 = BaseEditorFiltering, _a85 = Symbol.typeInfo, _b68) {
  constructor(props = {}) {
    super(Widget);
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "EditorFiltering");
  }
  static {
    this[_a85] = this.registerClass(nsSerenity);
  }
  get editorType() {
    return this.props.editorType;
  }
  set editorType(value) {
    this.props.editorType = value;
  }
  get useRelative() {
    return this.props.useRelative;
  }
  set useRelative(value) {
    this.props.useRelative = value;
  }
  get useLike() {
    return this.props.useLike;
  }
  set useLike(value) {
    this.props.useLike = value;
  }
  getOperators() {
    var list = [];
    list.push({ key: FilterOperators.EQ });
    list.push({ key: FilterOperators.NE });
    if (this.useRelative) {
      list.push({ key: FilterOperators.LT });
      list.push({ key: FilterOperators.LE });
      list.push({ key: FilterOperators.GT });
      list.push({ key: FilterOperators.GE });
    }
    if (this.useLike) {
      list.push({ key: FilterOperators.contains });
      list.push({ key: FilterOperators.startsWith });
    }
    this.appendNullableOperators(list);
    return list;
  }
  useEditor() {
    var op = this.get_operator().key;
    return op === FilterOperators.EQ || op === FilterOperators.NE || this.useRelative && (op === FilterOperators.LT || op === FilterOperators.LE || op === FilterOperators.GT || op === FilterOperators.GE);
  }
  getEditorOptions() {
    var opt = super.getEditorOptions();
    if (this.useEditor() && this.editorType === (this.get_field().editorType ?? "String")) {
      opt = Object.assign(opt, this.get_field().editorParams);
    }
    return opt;
  }
  createEditor() {
    if (this.useEditor()) {
      var editorType = EditorTypeRegistry.get(this.editorType);
      this.editor = new editorType({
        element: /* @__PURE__ */ __name((el) => this.get_container().append(el), "element"),
        ...this.getEditorOptions()
      });
      return;
    }
    super.createEditor();
  }
  useIdField() {
    return this.useEditor();
  }
  initQuickFilter(filter) {
    super.initQuickFilter(filter);
    filter.type = EditorTypeRegistry.get(this.editorType);
  }
};

// src/ui/filtering/enumfiltering.ts
var _a86, _b69;
var EnumFiltering = class extends (_b69 = BaseEditorFiltering, _a86 = Symbol.typeInfo, _b69) {
  static {
    __name(this, "EnumFiltering");
  }
  static {
    this[_a86] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(EnumEditor);
  }
  getOperators() {
    var op = [{ key: FilterOperators.EQ }, { key: FilterOperators.NE }];
    return this.appendNullableOperators(op);
  }
  getEditorText() {
    if (this.useEditor()) {
      return this.editor.text;
    }
    return super.getEditorText();
  }
};

// src/ui/filtering/integerfiltering.ts
var _a87, _b70;
var IntegerFiltering = class extends (_b70 = BaseEditorFiltering, _a87 = Symbol.typeInfo, _b70) {
  static {
    __name(this, "IntegerFiltering");
  }
  static {
    this[_a87] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(IntegerEditor);
  }
  getOperators() {
    return this.appendNullableOperators(this.appendComparisonOperators([]));
  }
};

// src/ui/filtering/lookupfiltering.ts
var _a88, _b71;
var LookupFiltering = class extends (_b71 = BaseEditorFiltering, _a88 = Symbol.typeInfo, _b71) {
  static {
    __name(this, "LookupFiltering");
  }
  static {
    this[_a88] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(LookupEditor);
  }
  getOperators() {
    var ops = [{ key: FilterOperators.EQ }, { key: FilterOperators.NE }, { key: FilterOperators.contains }, { key: FilterOperators.startsWith }];
    return this.appendNullableOperators(ops);
  }
  useEditor() {
    var op = this.get_operator().key;
    return op == FilterOperators.EQ || op == FilterOperators.NE;
  }
  useIdField() {
    return this.useEditor();
  }
  getEditorText() {
    if (this.useEditor()) {
      return this.editor.text;
    }
    return super.getEditorText();
  }
};

// src/ui/filtering/servicelookupfiltering.ts
var _a89, _b72;
var ServiceLookupFiltering = class extends (_b72 = BaseEditorFiltering, _a89 = Symbol.typeInfo, _b72) {
  static {
    __name(this, "ServiceLookupFiltering");
  }
  static {
    this[_a89] = this.registerClass(nsSerenity);
  }
  constructor() {
    super(ServiceLookupEditor);
  }
  getOperators() {
    var ops = [{ key: FilterOperators.EQ }, { key: FilterOperators.NE }, { key: FilterOperators.contains }, { key: FilterOperators.startsWith }];
    return this.appendNullableOperators(ops);
  }
  useEditor() {
    var op = this.get_operator().key;
    return op == FilterOperators.EQ || op == FilterOperators.NE;
  }
  useIdField() {
    return this.useEditor();
  }
  getEditorText() {
    if (this.useEditor()) {
      return this.editor.text;
    }
    return super.getEditorText();
  }
};

// src/ui/filtering/stringfiltering.ts
var _a90, _b73;
var StringFiltering = class extends (_b73 = BaseFiltering, _a90 = Symbol.typeInfo, _b73) {
  static {
    __name(this, "StringFiltering");
  }
  static {
    this[_a90] = this.registerClass(nsSerenity);
  }
  getOperators() {
    var ops = [
      { key: FilterOperators.contains },
      { key: FilterOperators.startsWith },
      { key: FilterOperators.EQ },
      { key: FilterOperators.NE }
    ];
    return this.appendNullableOperators(ops);
  }
  validateEditorValue(value) {
    if (value.length === 0) {
      return value;
    }
    return super.validateEditorValue(value);
  }
};

// src/ui/formatters/booleanformatter.tsx
var _a91;
_a91 = Symbol.typeInfo;
var BooleanFormatter = class {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "BooleanFormatter");
  }
  static {
    this[_a91] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    const text2 = ctx.value == null ? localText(this.props.nullText, this.props.nullText ?? "") : ctx.value ? localText(this.props.trueText, this.props.trueText ?? DialogTexts.YesButton) : localText(this.props.falseText, this.props.falseText ?? DialogTexts.NoButton);
    const showText = this.props?.showText ?? true;
    const hint = this.props?.showHint ?? !showText ? text2 : void 0;
    let iconClass = ctx.value == null ? this.props.nullIcon : ctx.value ? this.props.trueIcon : this.props.falseIcon;
    if (iconClass)
      iconClass = iconClassName(iconClass);
    const icon = iconClass == "" ? "" : iconClass ? jsx("i", { class: iconClass, title: hint }) : null;
    if (!showText)
      return icon ?? "";
    if (icon == null && hint == null)
      return text2;
    if (!icon)
      return jsx("span", { title: hint, children: text2 });
    return jsx("span", { title: hint, children: [
      icon,
      " ",
      text2
    ] });
  }
  get falseText() {
    return this.props.falseText;
  }
  set falseText(value) {
    this.props.falseText = value;
  }
  get trueText() {
    return this.props.trueText;
  }
  set trueText(value) {
    this.props.trueText = value;
  }
};

// src/ui/formatters/checkboxformatter.tsx
var _a92;
_a92 = Symbol.typeInfo;
var CheckboxFormatter = class {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "CheckboxFormatter");
  }
  static {
    this[_a92] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    const text2 = ctx.value == null ? localText(this.props.nullText, this.props.nullText ?? "") : ctx.value ? localText(this.props.trueText, this.props.trueText ?? DialogTexts.YesButton) : localText(this.props.falseText, this.props.falseText ?? DialogTexts.NoButton);
    const showText = this.props?.showText ?? ctx.purpose === "header-filter";
    const hint = this.props?.showHint ?? (!showText && (this.props?.trueText || this.props?.falseText || this.props?.nullText)) ? text2 : void 0;
    let iconClass = ctx.value == null ? this.props.nullIcon : ctx.value ? this.props.trueIcon : this.props.falseIcon;
    if (iconClass != null)
      iconClass = iconClassName(iconClass);
    const icon = iconClass == "" ? "" : iconClass ? jsx("i", { class: [iconClass, "slick-edit-preclick"], title: hint }) : ctx.value == null || ctx.purpose === "header-filter" ? null : jsx("span", { class: ["check-box no-float readonly slick-edit-preclick", ctx.value && "checked"], title: hint });
    if (!showText)
      return icon ?? "";
    if (icon == null && hint == null)
      return text2;
    if (!icon)
      return jsx("span", { title: hint, children: text2 });
    return jsx("span", { title: hint, children: [
      icon,
      " ",
      text2
    ] });
  }
};

// src/ui/formatters/dateformatter.ts
var _a93;
_a93 = Symbol.typeInfo;
var _DateFormatter = class _DateFormatter {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
    this.props.displayFormat ??= Culture.dateFormat;
  }
  static {
    __name(this, "DateFormatter");
  }
  static {
    this[_a93] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  static format(value, format2) {
    if (value == null) {
      return "";
    }
    var date;
    if (value instanceof Date) {
      date = value;
    } else if (typeof value === "string") {
      date = parseISODateTime(value);
      if (date == null || isNaN(date.valueOf())) {
        return htmlEncode(value);
      }
    } else {
      return value.toString();
    }
    return htmlEncode(formatDate(date, format2));
  }
  get displayFormat() {
    return this.props.displayFormat;
  }
  set displayFormat(value) {
    this.props.displayFormat = value;
  }
  format(ctx) {
    return _DateFormatter.format(ctx.value, this.displayFormat);
  }
};
var DateFormatter = _DateFormatter;

// src/ui/formatters/datetimeformatter.ts
var _a94, _b74;
var DateTimeFormatter = class extends (_b74 = DateFormatter, _a94 = Symbol.typeInfo, _b74) {
  static {
    __name(this, "DateTimeFormatter");
  }
  static {
    this[_a94] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  constructor(props = {}) {
    super({ displayFormat: Culture.dateTimeFormat, ...props });
  }
};

// src/ui/formatters/enumformatter.ts
var _a95;
_a95 = Symbol.typeInfo;
var _EnumFormatter = class _EnumFormatter {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "EnumFormatter");
  }
  static {
    this[_a95] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    var enumType = EnumTypeRegistry.getOrLoad(this.enumKey);
    if (isPromiseLike(enumType)) {
      const node = document.createElement("span");
      enumType.then(() => {
        const text2 = new Text(_EnumFormatter.format(enumType, ctx.value));
        node.parentElement && node.replaceWith(text2);
      });
      return node;
    }
    return _EnumFormatter.format(enumType, ctx.value);
  }
  get enumKey() {
    return this.props.enumKey;
  }
  set enumKey(value) {
    this.props.enumKey = value;
  }
  static format(enumType, value) {
    if (value == null) {
      return "";
    }
    var name = Enum.toString(enumType, value);
    var enumKey = getCustomAttribute(enumType, EnumKeyAttribute, false)?.value ?? getTypeFullName(enumType);
    return _EnumFormatter.getText(enumKey, name);
  }
  static getText(enumKey, name) {
    if (!name)
      return "";
    return htmlEncode(localText("Enums." + enumKey + "." + name, name));
  }
  static getName(enumType, value) {
    if (value == null) {
      return "";
    }
    return Enum.toString(enumType, value);
  }
};
var EnumFormatter = _EnumFormatter;

// src/ui/formatters/iinitializecolumn.ts
var _a96;
_a96 = Symbol.typeInfo;
var IInitializeColumn = class {
  static {
    __name(this, "IInitializeColumn");
  }
  static {
    this[_a96] = interfaceTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/ui/formatters/filedownloadformatter.tsx
var _a97;
_a97 = Symbol.typeInfo;
var _FileDownloadFormatter = class _FileDownloadFormatter {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "FileDownloadFormatter");
  }
  static {
    this[_a97] = formatterTypeInfo(nsSerenity, [IInitializeColumn]);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    var dbFile = ctx.value;
    if (!dbFile)
      return "";
    var downloadUrl = _FileDownloadFormatter.dbFileUrl(dbFile);
    var originalName = this.originalNameProperty ? ctx.item[this.originalNameProperty] : null;
    originalName = originalName ?? "";
    var text2 = stringFormat(
      this.displayFormat ?? "{0}",
      originalName,
      dbFile,
      downloadUrl
    );
    var iconClass = iconClassName(this.iconClass ?? faIcon("download"));
    return jsx("a", { class: "file-download-link", target: "_blank", href: downloadUrl, children: [
      jsx("i", { class: iconClass }),
      " ",
      text2
    ] });
  }
  static dbFileUrl(filename) {
    filename = replaceAll(filename ?? "", "\\", "/");
    return resolveUrl("~/upload/") + filename;
  }
  initializeColumn(column) {
    column.referencedFields = column.referencedFields || [];
    if (this.originalNameProperty) {
      column.referencedFields.push(this.originalNameProperty);
      return;
    }
  }
  get displayFormat() {
    return this.props.displayFormat;
  }
  set displayFormat(value) {
    this.props.displayFormat = value;
  }
  get originalNameProperty() {
    return this.props.originalNameProperty;
  }
  set originalNameProperty(value) {
    this.props.originalNameProperty = value;
  }
  get iconClass() {
    return this.props.iconClass;
  }
  set iconClass(value) {
    this.props.iconClass = value;
  }
};
var FileDownloadFormatter = _FileDownloadFormatter;

// src/ui/formatters/formatterbase.ts
var _a98;
_a98 = Symbol.typeInfo;
var FormatterBase = class {
  static {
    __name(this, "FormatterBase");
  }
  static registerFormatter(typeName, intfAndAttr) {
    if (Object.prototype.hasOwnProperty.call(this, Symbol.typeInfo) && this[Symbol.typeInfo])
      throw new Error(`Type ${this.name} already has a typeInfo property!`);
    const typeInfo = this[Symbol.typeInfo] = formatterTypeInfo(typeName, intfAndAttr);
    registerType(this);
    return typeInfo;
  }
  static {
    this[_a98] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
};

// src/ui/formatters/minuteformatter.ts
var _a99;
_a99 = Symbol.typeInfo;
var _MinuteFormatter = class _MinuteFormatter {
  static {
    __name(this, "MinuteFormatter");
  }
  static {
    this[_a99] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    return _MinuteFormatter.format(ctx.value);
  }
  static format(value) {
    var hour = Math.floor(value / 60);
    var minute = value - hour * 60;
    var hourStr, minuteStr;
    if (value == null || isNaN(value))
      return "";
    if (hour < 10)
      hourStr = "0" + hour;
    else
      hourStr = hour.toString();
    if (minute < 10)
      minuteStr = "0" + minute;
    else
      minuteStr = minute.toString();
    return stringFormat("{0}:{1}", hourStr, minuteStr);
  }
};
var MinuteFormatter = _MinuteFormatter;

// src/ui/formatters/numberformatter.ts
var _a100;
_a100 = Symbol.typeInfo;
var _NumberFormatter = class _NumberFormatter {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "NumberFormatter");
  }
  static {
    this[_a100] = formatterTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    return _NumberFormatter.format(ctx.value, this.displayFormat);
  }
  static format(value, format2) {
    format2 = format2 ?? "0.##";
    if (value == null)
      return "";
    if (typeof value === "number") {
      if (isNaN(value))
        return "";
      return htmlEncode(formatNumber(value, format2));
    }
    var dbl = parseDecimal(value.toString());
    if (dbl == null || isNaN(dbl))
      return value?.toString() ?? "";
    return htmlEncode(formatNumber(dbl, format2));
  }
  get displayFormat() {
    return this.props.displayFormat;
  }
  set displayFormat(value) {
    this.props.displayFormat = value;
  }
};
var NumberFormatter = _NumberFormatter;

// src/ui/formatters/urlformatter.tsx
var _a101;
_a101 = Symbol.typeInfo;
var UrlFormatter = class {
  constructor(props = {}) {
    this.props = props;
    this.props ??= {};
  }
  static {
    __name(this, "UrlFormatter");
  }
  static {
    this[_a101] = formatterTypeInfo(nsSerenity, [IInitializeColumn]);
  }
  static {
    registerType(this);
  }
  format(ctx) {
    var url = this.urlProperty ? (ctx.item[this.urlProperty] ?? "").toString() : (ctx.value ?? "").toString();
    if (!url)
      return "";
    if (this.urlFormat)
      url = stringFormat(this.urlFormat, url);
    url = resolveUrl(url);
    url = sanitizeUrl(url);
    var display = this.displayProperty ? (ctx.item[this.displayProperty] ?? "").toString() : (ctx.value ?? "").toString();
    if (this.displayFormat)
      display = stringFormat(this.displayFormat, display);
    return jsx("a", { href: url, target: this.target, children: display });
  }
  initializeColumn(column) {
    column.referencedFields = column.referencedFields || [];
    if (this.displayProperty) {
      column.referencedFields.push(this.displayProperty);
    }
    if (this.urlProperty) {
      column.referencedFields.push(this.urlProperty);
    }
  }
  get displayProperty() {
    return this.props.displayProperty;
  }
  set displayProperty(value) {
    this.props.displayProperty = value;
  }
  get displayFormat() {
    return this.props.displayFormat;
  }
  set displayFormat(value) {
    this.props.displayFormat = value;
  }
  get urlProperty() {
    return this.props.urlProperty;
  }
  set urlProperty(value) {
    this.props.urlProperty = value;
  }
  get urlFormat() {
    return this.props.urlFormat;
  }
  set urlFormat(value) {
    this.props.urlFormat = value;
  }
  get target() {
    return this.props.target;
  }
  set target(value) {
    this.props.target = value;
  }
};

// src/ui/helpers/columnsbase.tsx
var ColumnsBase = class {
  static {
    __name(this, "ColumnsBase");
  }
  constructor(items) {
    let key;
    this.__items = items ?? [];
    for (const col of this.__items) {
      key = col.id;
      if (key && !this[key])
        this[key] = col;
    }
    for (const col of this.__items) {
      key = col.sourceItem?.name;
      if (key && !this[key])
        this[key] = col;
    }
    for (const col of this.__items) {
      key = col.field;
      if (key && !this[key])
        this[key] = col;
    }
  }
  valueOf() {
    return this.__items;
  }
};

// src/ui/helpers/gridradioselectionmixin.tsx
var _a102;
_a102 = Symbol.typeInfo;
var GridRadioSelectionMixin = class {
  static {
    __name(this, "GridRadioSelectionMixin");
  }
  static {
    this[_a102] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  constructor(grid, options) {
    this.include = {};
    this.grid = grid;
    this.idField = grid.getView().getIdPropertyName();
    this.options = options || {};
    grid.getGrid().onClick.subscribe((e2, p2) => {
      if (e2.target.classList.contains("rad-select-item")) {
        e2.preventDefault();
        var item = grid.getView().getItem(p2.row);
        if (!this.isSelectable(item)) {
          return;
        }
        var id = item[this.idField].toString();
        if (this.include[id] == true) {
          clearKeys(this.include);
        } else {
          clearKeys(this.include);
          this.include[id] = true;
        }
        for (var i2 = 0; i2 < grid.getView().getLength(); i2++) {
          grid.getGrid().updateRow(i2);
        }
      }
    });
  }
  isSelectable(item) {
    return item && (this.options.selectable == null || this.options.selectable(item));
  }
  clear() {
    clearKeys(this.include);
  }
  resetCheckedAndRefresh() {
    this.include = {};
    this.grid.getView().populate();
  }
  getSelectedKey() {
    var items = Object.keys(this.include);
    if (items != null && items.length > 0) {
      return items[0];
    }
    return null;
  }
  getSelectedAsInt32() {
    var items = Object.keys(this.include).map(function(x) {
      return parseInt(x, 10);
    });
    if (items != null && items.length > 0) {
      return items[0];
    }
    return null;
  }
  getSelectedAsInt64() {
    var items = Object.keys(this.include).map(function(x) {
      return parseInt(x, 10);
    });
    if (items != null && items.length > 0) {
      return items[0];
    }
    return null;
  }
  setSelectedKey(key) {
    this.clear();
    this.include[key] = true;
  }
  static createSelectColumn(getMixin, columnOptions) {
    return {
      name: "[\xD7]",
      nameFormat: /* @__PURE__ */ __name(() => "", "nameFormat"),
      toolTip: " ",
      id: "__select__",
      resizable: false,
      width: 27,
      minWidth: 27,
      maxWidth: 27,
      headerCssClass: "",
      sortable: false,
      format: /* @__PURE__ */ __name(function(ctx) {
        const mixin = getMixin();
        if (!mixin || !mixin.isSelectable(ctx.item)) {
          return "";
        }
        const isChecked = mixin.include[ctx.item[mixin.idField]];
        return jsx(
          "input",
          {
            type: "radio",
            name: "radio-selection-group",
            class: "rad-select-item no-float",
            style: { cursor: "pointer" },
            checked: isChecked
          }
        );
      }, "format")
    };
  }
};

// src/ui/helpers/gridrowselectionmixin.tsx
var _a103;
_a103 = Symbol.typeInfo;
var GridRowSelectionMixin = class {
  static {
    __name(this, "GridRowSelectionMixin");
  }
  static {
    this[_a103] = classTypeInfo(nsSerenity);
  }
  static {
    registerType(this);
  }
  constructor(grid, options) {
    this.include = {};
    this.grid = grid;
    this.idField = grid.getView().getIdPropertyName();
    this.options = options || {};
    const boundThis = bindThis(this);
    grid.getGrid().onClick.subscribe(boundThis.handleGridClick);
    grid.getGrid().onHeaderClick.subscribe(boundThis.handleHeaderClick);
    grid.getView().onRowsChanged?.subscribe(boundThis.updateSelectAll);
  }
  destroy() {
    this.include = {};
    this.grid?.getGrid()?.onClick?.unsubscribe(this.handleGridClick);
    this.grid?.getGrid()?.onHeaderClick?.unsubscribe(this.handleHeaderClick);
    (this.grid?.getView()).onRowsChanged?.unsubscribe(this.updateSelectAll);
    delete this.grid;
    delete this.options?.selectable;
    this.options = null;
  }
  handleGridClick(e2) {
    if (!e2.target.classList.contains("select-item"))
      return;
    const grid = this.grid;
    e2.preventDefault();
    var item = grid.getView().getItem(e2.row);
    var id = item[this.idField].toString();
    if (this.include[id]) {
      delete this.include[id];
    } else {
      this.include[id] = true;
    }
    for (var i2 = 0; i2 < grid.getView().getLength(); i2++) {
      grid.getGrid().updateRow(i2);
    }
    this.updateSelectAll();
  }
  handleHeaderClick(e2) {
    if (Fluent.isDefaultPrevented(e2))
      return;
    if (!e2.target.classList.contains("select-all-items"))
      return;
    e2.preventDefault();
    const grid = this.grid;
    if (Object.keys(this.include).length > 0) {
      clearKeys(this.include);
    } else {
      var items = grid.getView().getItems();
      for (var x of items.filter(bindThis(this).isSelectable)) {
        var id1 = x[this.idField];
        this.include[id1] = true;
      }
    }
    this.updateSelectAll();
    grid.getView().setItems(grid.getView().getItems(), true);
    setTimeout(bindThis(this).updateSelectAll, 0);
  }
  updateSelectAll() {
    var selectAllButton = this.grid.getElement().querySelector(".select-all-header .slick-column-name .select-all-items");
    if (selectAllButton) {
      var keys = Object.keys(this.include);
      selectAllButton.classList.toggle(
        "checked",
        keys.length > 0 && this.grid.getView().getItems().filter(
          bindThis(this).isSelectable
        ).length <= keys.length
      );
    }
  }
  clear() {
    clearKeys(this.include);
    this.updateSelectAll();
  }
  resetCheckedAndRefresh() {
    this.include = {};
    this.updateSelectAll();
    this.grid.getView().populate();
  }
  selectKeys(keys) {
    for (var k of keys) {
      this.include[k] = true;
    }
    this.updateSelectAll();
  }
  getSelectedKeys() {
    return Object.keys(this.include);
  }
  getSelectedAsInt32() {
    return Object.keys(this.include).map(function(x) {
      return parseInt(x, 10);
    });
  }
  getSelectedAsInt64() {
    return Object.keys(this.include).map(function(x) {
      return parseInt(x, 10);
    });
  }
  setSelectedKeys(keys) {
    this.clear();
    for (var k of keys) {
      this.include[k] = true;
    }
    this.updateSelectAll();
  }
  isSelectable(item) {
    return item && (this.options.selectable == null || this.options.selectable(item));
  }
  static createSelectColumn(getMixin, columnOptions) {
    return {
      name: "[\xD7]",
      nameFormat: /* @__PURE__ */ __name(() => jsx("span", { class: "select-all-items check-box no-float" }), "nameFormat"),
      toolTip: " ",
      id: "__select__",
      resizable: false,
      width: 27,
      minWidth: 27,
      maxWidth: 27,
      headerCssClass: "select-all-header",
      sortable: false,
      format: /* @__PURE__ */ __name(function(ctx) {
        var item = ctx.item;
        var mixin = getMixin();
        if (!mixin || !mixin.isSelectable(item)) {
          return "";
        }
        var isChecked = mixin.include[ctx.item[mixin.idField]];
        return jsx("span", { class: "select-item check-box no-float" + (isChecked ? " checked" : "") });
      }, "format"),
      ...columnOptions
    };
  }
};

// src/ui/widgets/basepanel.ts
var _a104, _b75;
var BasePanel = class extends (_b75 = Widget, _a104 = Symbol.typeInfo, _b75) {
  static {
    __name(this, "BasePanel");
  }
  static {
    this[_a104] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.initValidator();
    this.initTabs();
    this.initToolbar();
  }
  destroy() {
    TabsExtensions.destroy(this.tabs);
    this.tabs = null;
    if (this.toolbar) {
      this.toolbar.destroy();
      this.toolbar = null;
    }
    if (this.validator) {
      this.validator?.destroy?.();
      this.byId("Form").remove();
      this.validator = null;
    }
    super.destroy();
  }
  arrange() {
    this.element.findAll(".require-layout").forEach((el) => {
      Fluent.isVisibleLike(el) && Fluent.trigger(el, "layout");
    });
  }
  getToolbarButtons() {
    return [];
  }
  getValidatorOptions() {
    return {};
  }
  initTabs() {
    var tabsDiv = this.findById("Tabs");
    if (!tabsDiv)
      return;
    this.tabs = TabsExtensions.initialize(tabsDiv, null);
  }
  initToolbar() {
    var toolbarDiv = this.findById("Toolbar");
    if (!toolbarDiv)
      return;
    this.toolbar = new Toolbar({ buttons: this.getToolbarButtons(), element: toolbarDiv }).init();
  }
  initValidator() {
    var form = this.findById("Form");
    if (form) {
      var valOptions = this.getValidatorOptions();
      this.validator = new Validator(form, validateOptions(valOptions));
    }
  }
  resetValidation() {
    if (this.validator) {
      this.validator.resetAll();
    }
  }
  validateForm() {
    return this.validator == null || !!this.validator.form();
  }
};
var TemplatedPanel = BasePanel;

// src/ui/widgets/prefixedcontext.ts
var PrefixedContext = class {
  constructor(idPrefix) {
    this.idPrefix = idPrefix;
  }
  static {
    __name(this, "PrefixedContext");
  }
  byId(id) {
    return Fluent(document.querySelector("#" + this.idPrefix + id));
  }
  w(id, type) {
    return getWidgetFrom("#" + this.idPrefix + id, type);
  }
};

// src/ui/widgets/propertypanel.ts
var _a105, _b76;
var PropertyPanel = class extends (_b76 = BasePanel, _a105 = Symbol.typeInfo, _b76) {
  static {
    __name(this, "PropertyPanel");
  }
  static {
    this[_a105] = this.registerClass(nsSerenity);
  }
  constructor(props) {
    super(props);
    this.initPropertyGrid();
    this.loadInitialEntity();
  }
  destroy() {
    if (this.propertyGrid) {
      this.propertyGrid.destroy();
      this.propertyGrid = null;
    }
    if (this.validator) {
      this.validator?.destroy?.();
      this.byId("Form").remove();
      this.validator = null;
    }
    super.destroy();
  }
  initPropertyGrid() {
    var pgDiv = this.findById("PropertyGrid");
    if (!pgDiv)
      return;
    var pgOptions = this.getPropertyGridOptions();
    this.propertyGrid = new PropertyGrid({ element: pgDiv, ...pgOptions }).init();
  }
  loadInitialEntity() {
    if (this.propertyGrid) {
      this.propertyGrid.load(new Object());
    }
  }
  getFormKey() {
    var name = getTypeFullName(getInstanceType(this));
    var px = name.indexOf(".");
    if (px >= 0) {
      name = name.substring(px + 1);
    } else if (name.endsWith("Panel")) {
      name = name.substring(0, name.length - 5);
    }
    return name;
  }
  getPropertyGridOptions() {
    return {
      idPrefix: this.idPrefix,
      items: this.getPropertyItems(),
      mode: 1 /* insert */,
      localTextPrefix: "Forms." + this.getFormKey() + "."
    };
  }
  getPropertyItems() {
    var formKey = this.getFormKey();
    return getForm(formKey);
  }
  getSaveEntity() {
    var entity = new Object();
    if (this.propertyGrid) {
      this.propertyGrid.save(entity);
    }
    return entity;
  }
  get entity() {
    return this._entity;
  }
  get entityId() {
    return this._entityId;
  }
  set entity(value) {
    this._entity = value ?? new Object();
  }
  set entityId(value) {
    this._entityId = value;
  }
  validateBeforeSave() {
    return this.validator.form();
  }
};
export {
  AdvancedFilteringAttribute,
  AggregateFormatting,
  AggregatorTypeRegistry,
  Aggregators,
  Attributes,
  Authorization,
  AutoNumeric,
  BaseDialog,
  BaseEditorFiltering,
  BaseFiltering,
  BasePanel,
  BooleanEditor,
  BooleanFiltering,
  BooleanFormatter,
  CaptureOperationType,
  CascadedWidgetLink,
  CheckLookupEditor,
  CheckTreeEditor,
  CheckTreeEditorTexts,
  CheckboxFormatter,
  CloseButtonAttribute,
  ColumnPickerDialog,
  ColumnPickerDialogTexts,
  ColumnSelection,
  ColumnsBase,
  Combobox,
  ComboboxEditor,
  Component,
  Config,
  Criteria,
  CriteriaBuilder,
  CriteriaOperator,
  Culture,
  CustomAttribute,
  DataGrid,
  DataGridTexts,
  DataValidationTexts,
  DateEditor,
  DateFiltering,
  DateFormatter,
  DateTimeEditor,
  DateTimeEditorTexts,
  DateTimeFiltering,
  DateTimeFormatter,
  DateYearEditor,
  DecimalEditor,
  DecimalFiltering,
  Decorators,
  Dialog,
  DialogExtensions,
  DialogTexts,
  DialogTypeRegistry,
  EditLink,
  EditorAttribute,
  EditorFiltering,
  EditorTypeRegistry,
  EditorUtils,
  EditorWidget,
  ElementAttribute,
  EmailAddressEditor,
  EmailEditor,
  EntityDialog,
  EntityDialogTexts,
  EntityGrid,
  EntityGridTexts,
  EntityLocalizer,
  Enum,
  EnumEditor,
  EnumFiltering,
  EnumFormatter,
  EnumKeyAttribute,
  EnumTypeRegistry,
  ErrorHandling,
  FileDownloadFormatter,
  FileUploadEditor,
  FileUploadTexts,
  FilterDialog,
  FilterDisplayBar,
  FilterOperators,
  FilterPanel,
  FilterPanelTexts,
  FilterStore,
  FilterWidgetBase,
  FilterableAttribute,
  FilteringTypeRegistry,
  Fluent,
  FormValidationTexts,
  FormatterBase,
  FormatterTypeRegistry,
  Fragment,
  GridPageInit,
  GridRadioSelectionMixin,
  GridRowSelectionMixin,
  GridSelectAllButtonHelper,
  GridUtils,
  HtmlContentEditor,
  HtmlContentEditorTexts,
  HtmlNoteContentEditor,
  HtmlReportContentEditor,
  IBooleanValue,
  IDialog,
  IDoubleValue,
  IEditDialog,
  IFiltering,
  IGetEditValue,
  IInitializeColumn,
  IQuickFiltering,
  IReadOnly,
  ISetEditValue,
  ISlickFormatter,
  IStringValue,
  IValidateRequired,
  ImageCheckResultTexts,
  ImageUploadEditor,
  IntegerEditor,
  IntegerFiltering,
  Invariant,
  LT,
  LayoutTimer,
  LazyLoadHelper,
  Lookup,
  LookupEditor,
  LookupEditorBase,
  LookupFiltering,
  MaskedEditor,
  MathMLNamespace,
  MaximizableAttribute,
  MinuteFormatter,
  MultipleFileUploadEditor,
  MultipleImageUploadEditor,
  NumberFormatter,
  OptionAttribute,
  PagerTexts,
  PanelAttribute,
  PanelPageInit,
  PasswordEditor,
  PrefixedContext,
  PropertyCategories,
  PropertyCategory,
  PropertyCategoryTitle,
  PropertyDialog,
  PropertyField,
  PropertyFieldCaption,
  PropertyFieldEditor,
  PropertyFieldLineBreak,
  PropertyGrid,
  PropertyGridMode,
  PropertyGridTexts,
  PropertyItemColumnConverter,
  PropertyPanel,
  PropertyTabItem,
  PropertyTabList,
  PropertyTabPane,
  PropertyTabPanes,
  PropertyTabs,
  QuickFilterBar,
  QuickSearchInput,
  QuickSearchTexts,
  RadioButtonEditor,
  Recaptcha,
  ReflectionOptionsSetter,
  RemoteView,
  ResizableAttribute,
  RetrieveColumnSelection,
  Router,
  SVGNamespace,
  ScriptData,
  Select2,
  SelectEditor,
  SelectEditorTexts,
  SerenityNS,
  ServiceLookupEditor,
  ServiceLookupEditorBase,
  ServiceLookupFiltering,
  ShadowRootNode,
  SlickFormatting,
  SlickHelper,
  SlickPager,
  SlickTreeHelper,
  StaticPanelAttribute,
  StringEditor,
  StringFiltering,
  SubDialogHelper,
  SummaryType,
  TabsExtensions,
  TemplatedDialog,
  TemplatedPanel,
  TemplatedWidget,
  TextAreaEditor,
  TimeEditor,
  TimeEditorBase,
  TimeSpanEditor,
  Toastr,
  Toolbar,
  ToolbarButton,
  Tooltip,
  TranslationConfig,
  TreeGridMixin,
  TypeMemberKind,
  URLEditor,
  UploadHelper,
  Uploader,
  UrlFormatter,
  ValidationHelper,
  Validator,
  Widget,
  addClass,
  addCustomAttribute,
  addDisposingListener,
  addEmptyOption,
  addLocalText,
  addOption,
  addTypeMember,
  addValidationRule,
  alert2 as alert,
  alertDialog,
  any,
  appendToNode,
  applyChangesToolButton,
  associateWidget,
  attrEncode,
  batch,
  bgColor,
  bindThis,
  blockUI,
  blockUndo,
  canLoadScriptData,
  cancelDialogButton,
  cast,
  centerDialog,
  className,
  classTypeInfo,
  clearKeys,
  clearOptions,
  cloneToolButton,
  coalesce,
  computed,
  confirm,
  confirmDialog,
  count,
  createElement,
  createRef,
  cssEscape,
  currentLifecycleRoot,
  currentNamespaceURI,
  deassociateWidget,
  debounce,
  deepClone,
  defaultNotifyOptions,
  delegateCombine,
  delegateContains,
  delegateRemove,
  deleteToolButton,
  derivedSignal,
  dispatchDisposingEvent,
  editToolButton,
  editorTypeInfo,
  effect,
  endsWith,
  ensureScriptDataSync,
  ensureTypeInfo,
  executeEverytimeWhenVisible,
  executeOnceWhenVisible,
  extend,
  faIcon,
  fabIcon,
  fetchScriptData,
  fieldsProxy,
  findElementWithRelativeId,
  first,
  format,
  formatDate,
  formatDayHourAndMin,
  formatISODateTimeUTC,
  formatNumber,
  formatterTypeInfo,
  getActiveRequests,
  getBaseType,
  getColumns,
  getColumnsAsync,
  getColumnsData,
  getColumnsDataAsync,
  getColumnsScript,
  getCookie,
  getCustomAttribute,
  getCustomAttributes,
  getElementReadOnly,
  getForm,
  getFormAsync,
  getFormData,
  getFormDataAsync,
  getFormScript,
  getGlobalObject,
  getGlobalTypeRegistry,
  getInstanceType,
  getLookup,
  getLookupAsync,
  getNested,
  getRemoteData,
  getRemoteDataAsync,
  getReturnUrl,
  getScriptData,
  getScriptDataHash,
  getServiceOptions,
  getType,
  getTypeFullName,
  getTypeMembers,
  getTypeNameProp,
  getTypeRegistry,
  getTypeShortName,
  getTypes,
  getWidgetFrom,
  getWidgetName,
  getjQuery,
  gridPageInit,
  groupBy,
  createElement as h,
  handleScriptDataError,
  hasBSModal,
  hasCustomAttribute,
  hasUIDialog,
  htmlEncode,
  iconClassName,
  iframeDialog,
  inHTMLNamespace,
  inMathMLNamespace,
  inNamespaceURI,
  inSVGNamespace,
  indexOf,
  information,
  informationDialog,
  initFlatpickrLocale,
  initFormType,
  initFullHeightGridPage,
  initGlobalMappings,
  initPropHookSymbol,
  insert,
  interfaceTypeInfo,
  invokeDisposingListeners,
  isArray,
  isArrayLike,
  isAssignableFrom,
  isBS3,
  isBS5Plus,
  isEmptyOrNull,
  isEnum,
  isInstanceOfType,
  isMobileView,
  isPromiseLike,
  isSafeReturnUrl,
  isSameOrigin,
  isSignalLike,
  isTrimmedEmpty,
  isValue,
  jQueryPatch,
  jsx,
  jsx as jsxs,
  keyOf,
  layoutFillHeight,
  layoutFillHeightValue,
  localText,
  localeFormat,
  localizationToolButton,
  newBodyDiv,
  noDialogButton,
  notifyError,
  notifyInfo,
  notifySuccess,
  notifyWarning,
  nsSerenity,
  observeSignal,
  okDialogButton,
  omitAllGridPersistenceFlags,
  omitUndefined,
  outerHtml,
  padLeft,
  panelPageInit,
  parseCriteria,
  parseDate,
  parseDayHourAndMin,
  parseDecimal,
  parseHourAndMin,
  parseISODateTime,
  parseInteger,
  parseQueryString,
  peekScriptData,
  peekTypeInfo,
  positionToastContainer,
  postToService,
  postToUrl,
  proxyTexts,
  registerClass,
  registerEditor,
  registerEnum,
  registerFormatter,
  registerInterface,
  registerType,
  reloadLookup,
  reloadLookupAsync,
  removeClass,
  removeDisposingListener,
  removeValidationRule,
  replaceAll,
  requestFinished,
  requestStarting,
  resetApplicationPath,
  resetCspNonce,
  resetCultureSettings,
  resolveServiceUrl,
  resolveUrl,
  round,
  safeCast,
  sanitizeHtml,
  sanitizeUrl,
  saveAndCloseToolButton,
  scriptDataHooks,
  serviceCall,
  serviceRequest,
  setElementReadOnly,
  setEquality,
  setRef,
  setRegisteredScripts,
  setScriptData,
  setTypeNameProp,
  signal,
  single,
  skipEditLinkFormatPurposes,
  splitDateString,
  startsWith,
  stringFormat,
  stringFormatLocale,
  stripDiacritics,
  success,
  successDialog,
  text,
  textColor,
  toGrouping,
  toId,
  toSingleLine,
  today,
  toggleClass,
  triggerLayoutOnShow,
  trim,
  trimEnd,
  trimStart,
  trimToEmpty,
  trimToNull,
  trunc,
  tryFirst,
  tryGetText,
  tryGetWidget,
  turkishLocaleCompare,
  turkishLocaleToLower,
  turkishLocaleToUpper,
  uiAndBSButtonNoConflict,
  undeleteToolButton,
  untracked,
  useClassList,
  useIdPrefix,
  useImperativeHandle,
  usePropBinding,
  createRef as useRef,
  useSignal,
  useText,
  useUpdatableComputed,
  validateOptions,
  validatorAbortHandler,
  warning,
  warningDialog,
  yesDialogButton,
  zeroPad
};
//# sourceMappingURL=index.js.map
